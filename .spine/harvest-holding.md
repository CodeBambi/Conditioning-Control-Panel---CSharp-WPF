# Harvest holding — batch 20260804T114308 (SP-037) era
Commit NOTHING to base while a batch is active (T-9). Fold into port-lessons.md / memories / CONTEXT.md after `spine batch complete`.

## 2026-08-04 candidates (orchestrator, resume session, laptop)

1. **Engine-float (second-root pin recurrence):** the GLOBAL pi-spine floated 2.10.0 -> 2.12.2 (unpinned `~/.pi/agent/settings.json` entry + caret range in `~/.pi/agent/npm/package.json`); verify.mjs FAIL caught it pre-launch (standing rule worked). Fix: pin BOTH roots exact 2.10.0 -> npm install -> apply.mjs (12 across 2 roots) -> verify green. The CONTEXT.md pin lesson applies to the global root too. Recurrence #2 -> tooling candidate: pre-launch checklist/doctor note asserting pin==installed on BOTH roots (verify.mjs already covers patches, not version drift of the engine itself — the "installed version not in testedVersions" WARNING was the tell; consider making that warning fatal pre-launch).
2. **pi-spawn-on-Windows (fresh-machine trap):** Node 24 `spawnSync('pi')` -> ENOENT, `('pi.cmd')` -> EINVAL on a prefix with only shell/cmd shims; blocks spine doctor AND spine-worker-runner. Fix: 4.6KB pi.exe forwarder shim (`~/.pi/pi-shim/PiShim.cs` -> `AppData/Roaming/npm/pi.exe`, csc from .NET Framework). Belongs in the fresh-machine bootstrap (port.txt step 2 sibling). Desktop never hit it (its prefix resolves pi fine — uncharacterized difference).
3. **Per-machine premise re-probe:** SP-019's "Ollama absent" was desktop-scoped; laptop HAS Ollama. Packets that name environment absence must re-probe per machine (amended SP-035 accordingly: presence probe = `ollama --version` only, no socket/pull).
4. **WSL zero distros on laptop** (`wsl -l -q` empty exit 0): Linux gates = named limit here; provisioning = owner decision. Affects every packet with a WSL2 gate (SP-035/SP-037 amended; future packets must probe first).
5. **Consult config trap:** bare `consult()` falls to `defaultMode: council` in `.pi/bpx-consult.json` and fails on this laptop (kimi-api provider unregistered; T-7 class). ALWAYS pass `mode:"solo"` explicitly. Solo Opus-5 route VERIFIED 2026-08-04 (recovery-plan review, substantive verdict).
6. **12.1.1 bump landed outside the spine pipeline** (owner laptop session bccbabf3, no packet/board trail; baseline docs still say 12.1.0 in places) — flag at board reconcile; not a violation to unwind, a record-keeping gap to annotate.
7. **Cross-machine handoff limit:** the desktop's parked wave-4 batch `20260722T152755` lane commits (a178c5c4 etc.) never existed outside the desktop — handoff.md's resume instructions assumed resumable state that doesn't travel. Cross-machine parks must push lane branches (or land) before the other machine resumes; reconciliation must check lane-commit EXISTENCE, not handoff text.
8. **Floor truth:** measured 464/466 + 29/29 post-12.1.1 (2 red AssetManifestTests from merge 56f156fc) — matches the board row; SP-037 restores 466/29.
