// The one seam between the UI and the engine.
export { createProject, canRecordVideo, codeToSeed, normalizeCode, pickOrientation, kindForFile, probeMedia, randomCode, POOL_CAP, pickSet, defaultCount, clampCountFor, sameSet, isZipFile, unzip } from '../engine/index.js';
