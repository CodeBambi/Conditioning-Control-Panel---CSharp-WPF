# Port digest — owner's read-first log

Three lines per landed wave, newest last. Written by the phase that lands, so an unattended
run cannot bury named limits and owner questions inside ten records nobody opens.

Format:
- **LANDED:** what reached the tree.
- **DOES NOT PROVE:** the honest residual — the limit a reader would otherwise assume away.
- **OWNER:** the question or decision it raised, or `none`.

---

## Wave 19 — SP-062 (`7518c6a4`, 2026-08-12)

- **LANDED:** the SP-057 profile-isolation pin can no longer pass vacuously — loud `Assert.SkipWhen` at both checkpoints plus a probe-proven co-location fix for the process-env leak (`DisableParallelization` measurably does not serialize on this runner). Floor 892/35.
- **DOES NOT PROVE:** that the suite's signal is trustworthy in general. The tripwire fires only for an externally set override; in-suite leaks are prevented, not detected; a skip does not fail the contract gate, so detection still depends on a human comparing counts. The generalized vacuous-test class (conditional `return` shapes) was never swept — new P1 row filed.
- **OWNER:** ratification of the row. Also worth knowing: the wave-18 land shipped a RED base (an inventory disposition flipped without the field its guard requires) — found and repaired in-lane, lesson recorded.

## Wave 20 — SP-063 (`10c37650`, 2026-08-12)

- **LANDED:** your decree — injected test timeout budgets raised to one shared finite constant (`TestWait.InjectedBudget = 60 s`, ~75×), the two timeout-SUBJECT tests kept short and pinned, three inert budgets deleted, and one guard token added with a captured RED so the shape cannot return silently. 10 greens incl. a cold first-ever build; floor unchanged.
- **DOES NOT PROVE:** that the cold-start class is gone. A bigger number lengthens the fuse; it does not remove the time dependence. The class returns on a machine ~75× slower or if the constant is lowered, and the guard catches the option-assignment shape only (not a constant, a computed `TimeSpan`, or a method-argument bound). The deterministic alternative was set aside by decree, not refuted.
- **OWNER:** ratification. No new question.

## Wave 21 — SP-064 launched (`9d5f92c6`, 2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing product-side. Wave 21 was authored and its batch launched: SP-064 makes harness-only entry points refuse to start when `CCP_DATA_ROOT` is unset, so the SP-057 seam stops depending on someone remembering it. **Counts at this commit are unchanged: 892 unit / 35 headless / 0 skipped, 0W/0E** — this phase touched only packet and doc files.
- **DOES NOT PROVE:** anything about the gate; the worker has not run yet. Two things are already known limits of the design you approved: the row's own decree keeps demo flags exempt, so `--dtrh-demo --dtrh-auto-close 30` remains an unattended evidence-shaped run against your real profile, and the gate protects the data root only — WebView2/LibVLC writes under `%LOCALAPPDATA%`/`%TEMP%` were outside SP-057's claim and stay outside this one.
- **OWNER:** two questions. (1) The demo+auto-close hole above — close it in a follow-up row, or accept it as the price of keeping demo flags human-usable? (2) **Your new blind audit runs `dotnet build` + both test projects in the MAIN checkout whenever a phase moves HEAD — including the authoring phase that just launched a batch, so the audit and the lane build/test concurrently.** It should be sound (worktrees have their own obj/bin), but a flaky or port-contended run now writes `.spine/STOP` with a batch in flight. Consider skipping the audit when `spine status` is executing, or auditing after the land phase only.


## Wave 21 — SP-064 (`e8eab7c1`, 2026-08-13)

- **LANDED:** harness-only launches now refuse to start against your real profile. `--dtrh-m2test`, `--dtrh-fx-drive`, `--intake-drive`, `--loom-drive`, `--tunnel-drive` and the harness-only failure injectors exit 3 naming `CCP_DATA_ROOT` when it is unset, gated in the real `Program.Main` before anything can write. One registry holds the classification, a guard test fails the build if any future `"--flag"` in `client/src` is left unclassified, and the refusal was proven with real processes: the profile is BYTE-IDENTICAL across 2677 files after a refused run, while a plain unsealed launch still opens a window and exits 0. Floor 892 → **897 unit / 35 headless / 0 skipped**.
- **DOES NOT PROVE:** that evidence runs can no longer touch your profile. By your row's own decree demo flags stay exempt, so `--dtrh-demo --dtrh-auto-close 30` is still an unattended, evidence-shaped run against the real profile. Only the data root is protected — WebView2, LibVLC and `%TEMP%` writes were never in SP-057's claim and are not in this one. The guard sees `"--..."` string literals in `client/src/**/*.cs` only, so a concatenated or interpolated flag evades it, as does any harness path that does not go through `Program.Main`. Linux is unproven: zero WSL distros on this machine, and no Linux run was faked. **This is a deliberate behavior break** — every headed evidence script must now set `CCP_DATA_ROOT` or it will exit 3.
- **OWNER:** three. (1) The demo+auto-close hole — close it in a follow-up row, or accept it as the price of keeping demo flags human-usable? (2) **"TRX attached" has never meant "TRX committed"** — `*.trx` is gitignored repo-wide, so every wave's TRX has lived only in a scratch worktree and was deleted with it. SP-064's 8 files were copied out and hashed before the clean that the merge required, but this is a standing gap in the evidence chain: do you want a `.gitignore` negation filed so evidence TRX is committed from now on? (3) The batch reported **`failed`** even though SP-064 passed contract, code review and final review — the failure was a dirty-worktree check, and spine's suggested fix (`git rm -r --cached`) would have deleted 117 force-added tracked files including the loop script itself. It was not run; the correct recovery is recorded. Worth knowing that the headline remediation in `spine status --diagnose` is actively dangerous in this repo.

## Wave 22 — SP-065 launched (2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing product-side. Wave 22 was authored and its batch launched. SP-065 replaces the honour-system half of your test discipline with machinery: today `dotnet test` exits **0** on `891 passed / 1 skipped`, so "an unexpected skip is a red" and "the exact count is the floor" are enforced only when a human reads two numbers off a console — the same path that let the wave-18 land ship a red base. The packet delivers a wrapper that owns both test invocations and **fails the contract** on an unexpected skip or an off-floor count, proven both ways (induced skip → red, clean run → green). **Counts at this commit are unchanged: 897 unit / 35 headless / 0 skipped, 0W/0E** — this phase touched only packet and doc files. This is board row 49 **part (2) only**; part (1), the sweep for tests that silently assert nothing, stays claimable exactly as the row asked.
- **DOES NOT PROVE:** anything about the mechanism — the worker has not run yet. Three limits are already known by design. It will detect that the number moved but **not which test vanished**. It cannot judge whether the pinned number is the *right* number: a pin bumped in the same commit as a bad or vacuous test is blessed by it, so this upgrades "a human must compare numbers" to "a human must justify a bump" — better, still not verification. And it binds only runs that go through the wrapper, so a bare `dotnet test` typed by hand still exits 0 on a skip.
- **OWNER:** one new question, plus one standing obligation I am recording against myself. **Question:** the consult route is degrading as an instrument — three calls this phase, the first returned reasoning with no verdict, the second truncated mid-sentence, only the third came back whole; waves 17 and 21 hit the same thing. I worked around it by asking narrowly and capping reply length, and I verified every consult claim against the repo before encoding it (two of the three were about gitignore rules and both checked out). Do you want that treated as a named tooling row rather than absorbed silently each wave? **Obligation:** SP-065 deliberately does NOT edit the packet template — that is a policy file and stays mine to change at land. If I forget it, the next packet's lane goes red on its own contract by design, so the failure is loud rather than silent; it is written into the row and into CONTEXT so the next phase inherits it.

## Wave 22 — SP-065 LANDED (2026-08-13) — integrate `09b4b639`

- **LANDED:** the test floor is no longer enforced by a human reading two numbers. `node client/tests/floor/check-floor.mjs` now owns both test runs and **fails the build contract** on any unexpected skip, any off-floor count, a bad counter category, or a stale result file — it finds the test projects from the solution, so a new test project cannot quietly sit outside the floor, and it writes its TRX outside the worktree so it leaves no mess behind. Proven in both directions with captured output (induced skip → red, count drift either way → red, clean run → green), and a guard test now fails any future task packet that tries to run the tests the old bare way. New floor: **898 unit / 35 headless / 0 skipped, 0 warnings, 0 errors** (897 → 898 because the new guard adds one test). The land itself was clean — no recovery, unlike wave 21 — and I verified the merged tree through the new wrapper three times before approving, then proved the tree I tested was byte-identical to the tree that got pushed.
- **DOES NOT PROVE:** that the pinned number is the RIGHT number. The machine now forces you to justify a bump instead of forcing you to notice one — better, but it is not verification: a bad or empty test that is added and pinned in the same commit is blessed by this mechanism. It detects that the count moved, never **which** test vanished, so a commit that deletes one fact and adds another slips through unchanged. It binds only runs that go through the wrapper — your own keyboard is still unbound. It does nothing for the OTHER half of that board row: the sweep for tests that quietly assert nothing is **untouched and still claimable**. And it has **never run on Linux** (no WSL distro on this machine); portable-by-construction is not proven.
- **OWNER:** one flake filed by name and deliberately NOT fixed — `ChaosTunnelLoopbackTests.Logging_RouteClassesOnly_NeverFilenameOrQuery` went red once in fifteen full runs; it guards that tunnel logging never leaks a filename or query, so it must be fixed at the source, never weakened or quarantined. **Question for you:** the gate's own evidence has now been misleading five times running (this time a diff-stat that read as if the worker had deleted my instructions — it had not). I keep catching it because I re-verify the merged tree myself, which costs roughly ten minutes per land. Do you want me to keep paying that, or to spend one bounded task making the gate's evidence trustworthy so the re-verification can shrink?

## Wave 23 — SP-066 LANDED (2026-08-13) — integrate `29950e9b`

- **LANDED:** the other half of the test-honesty row. Every test in both projects that *could* silently assert nothing has now been looked at individually — 78 sites, each one carrying a written verdict and reason in a committed ledger, none left unjudged and none deleted. Five tests that used to hit a hidden `return` on the wrong OS (and were therefore counted as passes while doing nothing) now openly report themselves as skipped, and each is pinned in the floor by its full name, so an unexpected skip fails the build naming the offending test. Six more that could go empty at runtime got a check that they actually have something to loop over. A new guard test fails with `file:line` if anyone adds a test in one of these silencing shapes without judging it, and the blind auditor's instructions were fixed so it stops re-deriving the floor the old unreliable way. New floor **900 unit / 35 headless / 2 named skips, 0 warnings, 0 errors**. **The two skips are not a loosening** — those five tests were always non-executing on this OS; before, that was invisible and scored as green, and now it is named. No assertion was weakened, no tolerance widened, nothing quarantined, and no product code changed.
- **DOES NOT PROVE:** that the suite has no vacuous tests left. The detector reads source text, so it cannot see a test that runs a loop over an empty list at runtime and asserts nothing — that class is mitigated where it was found, not detected. The guard only binds the shapes it was taught; one more silencing shape (`Assert.All` over a possibly-empty collection, 21 uses) was **found during the sweep and deliberately left out of scope** rather than quietly ignored — it is filed as its own row. The permanent bans that stop the skip list becoming a quarantine list are **prose in a file, not code** — a follow-up row is filed to make the two most dangerous ones mechanical. The ledger's per-site reasons are human judgment nothing checks. The auditor half of T-17 is **not** finished: the instructions changed, but nobody has yet run the auditor against a deliberately broken tree to prove it now catches it, so that row stays open. And none of this has run on Linux — no WSL distro on this machine.
- **OWNER:** a real bug surfaced and I did not paper over it. `AsyncLifecycleTests.Heartbeat_SkipsProjectionUntilBound_ThenFlowsThroughBoundary` failed once during this work — a shutdown race where a cancelled heartbeat reports itself as completed. This is its **second** recorded occurrence (the first was back at SP-055) and both times it fired under a change that touches no lifecycle code, so it is a genuine race in the product's stop path, not test noise. Adding it to the new skip list would have made it disappear; that is exactly the abuse the rules ban, so it is filed as a P1 row to be reproduced and fixed at the source instead. **Recommendation, with the decision that is actually yours attached:** five rows now sit in the queue that exist only to make the *test suite* trustworthy. Those are not all equal, and I am not asking you to rank them from scratch. **The next wave should fix the StopAsync race regardless** — it is the one item on that list that is a defect in the *product's* shutdown path rather than scaffolding around the tests, it has now surfaced twice, and it is currently queued behind test tooling, which is the wrong order. I intend to do that unless you say otherwise. **The decision I do need from you is the ratio after that:** the remaining four suite-hardening rows (the `Assert.All` shape, the mechanical ban test, T-17's auditor run, the privacy flake) are all genuine but none is urgent, and every wave spent on them is a wave not spent on WPF parity. Tell me roughly how much of the run you want going to suite trustworthiness versus parity features, and I will schedule against that instead of asking again each wave.

## Wave 24 — SP-067 LANDED (2026-08-13) — integrate `75a09d61`

- **LANDED:** the shutdown bug is fixed at the source. When you stopped the app, the heartbeat could report itself as **"finished normally"** instead of **"stopped"** — the code checked for a stop request at the top of its loop, but if the stop arrived at that exact check rather than while it was sleeping, it fell out of the loop and reported success anyway. That is the intermittent failure that surfaced twice on its own (SP-055, then SP-066) under changes that touched nothing in this area. The fix is four lines: the exit now reports "stopped" when a stop was requested, matching what the status-ticker already did. **I did not take the worker's word for any of it.** On the exact tree that got pushed I rebuilt clean (0 warnings, 0 errors), ran the full suite **five times green** (903 unit / 35 headless / 2 expected skips), and re-ran the worker's 500-iteration reproduction myself: **500 out of 500 correct**. Then I did the part that actually matters — I **broke the code on purpose** to check the tests would catch it. Reverting the fix made the reproduction fail 498 times out of 500, and each of the three new regression tests fails when, and only when, its own piece of code is broken. So these are real guards, not decorations. New floor: **903 unit / 35 headless / 2 named skips, 0W/0E.**
- **DOES NOT PROVE:** that the original failing test can never fail again. My reproduction drives the *fastest* version of the bug (stop before the first beat), which is not the version that test hits — it stops *after* several beats. So neither the five green runs nor the 500 iterations re-measure that test's old "roughly 1 in 15" failure rate; I am closing this on **mechanism**, not on frequency: the only route by which it could report the wrong answer no longer exists. Worth knowing that the loop has no early exit, so in today's code the "finished normally" branch is now **unreachable** — that is why the new tests are reliable rather than timing-dependent, but it also means the fix is belt-and-braces rather than a knife-edge race repair. This closes that one mechanism only; the test can still fail for unrelated reasons like a timeout. And none of this ran on Linux — still zero WSL distros on this machine, as it has been for every wave.
- **OWNER:** one thing filed, and one decision I am going to stop asking you for. **Filed:** the same "reports finished when it was actually stopped" shape can arrive through a second door — if a capability probe swallows a cancellation, the registry records a probe verdict that never happened. It is a *different* mechanism, it was found by this wave's own sweep, and I deliberately did **not** fix it in the landing commit because that code would have gone in after my verification had already run, which is exactly how a bad wave shipped a broken base once before. It is a filed row with a "prove the test fails first" acceptance. **Decision:** I asked you last wave for the ratio of test-suite hardening to actual WPF parity work, and I am not going to hold the run waiting for an answer. **Default I am now proceeding on: back to WPF parity.** The reason is that the one item in that queue which was a genuine *product* defect is the one I just fixed; the four that remain (the `Assert.All` shape, the mechanical ban test, the auditor run, the privacy flake) are all scaffolding around the tests, all genuine, none urgent. I will take them only when one is small enough to ride along with parity work rather than spending a wave on it. **Say the word if you want it weighted differently** — otherwise this is the schedule. I will stop *asking* you to set the ratio, but I will raise one of those four rows again if evidence makes it urgent (a second occurrence of the privacy flake, say, or an audit that the auditor run would have caught). Silence keeps parity as the default; it does not mean I will ignore new evidence.

## Wave 25 — SP-068 launched (2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing yet — this phase authored the packet and launched the batch. It is the first wave under your back-to-parity default, and it takes the cheapest real parity available: three **subtractive** filters over code the port has already shipped. Each one *removes* data rather than adding any. (1) Window titles containing private/incognito markers are dropped before anything counts them. (2) Titles get scrubbed — email addresses, runs of six or more digits, and control characters stripped, then capped at 80 characters — using the exact rules and numbers the WPF app uses. (3) URLs the model invents are stripped out of the companion's replies before they can reach the chat bubble, the history, or the disk. These come from the Her Room audit that landed at wave 17, which found that the strongest things to adopt from upstream are the ones that *narrow* what flows. **Counts at this commit are unchanged: 903 unit / 35 headless / 2 expected skips, 0 warnings, 0 errors** — this phase touched only packet and planning files.
- **DOES NOT PROVE:** anything about the mechanism yet — the worker has not run. Two limits are known by design and are written into the packet so they cannot quietly disappear. **The first two filters guard a path nothing in the app currently drives**: the awareness title machinery is real, typed and wired into the moderation boundary, but today only the tests call it, so those two harden the boundary a future feature will inherit rather than change anything you can see. The third one is on a live path — your companion's replies. And this is **three rows of one audit table, selected by me at authoring** (the ones that are small, need no decision from you, and can be proven without a screen) — it is **not** a verdict on Her Room and it does not answer any of the audit's 12 questions to you. Those stay open.
- **OWNER:** one thing you should know about the shape of what is left. **Most of the remaining parity queue is desktop-only.** The four big upstream surfaces — Goon Game, For You Feed, Trainer Card, Haptics v2 — are still blocked behind a sizing pass, and three of them need a real display, real media, and a Linux box to evidence honestly. I did **not** spend this wave writing that sizing plan, because on this laptop it would produce a plan that cannot be executed and would be stale by the time it could be. It is recorded as deferred-and-machine-gated, not dropped. The practical consequence: waves run from this machine will keep being small, headless, provable slices like this one, and the big parity surfaces wait for a session on the desktop. Say the word if you would rather I write the sizing plan now anyway so it is ready when you are at the other machine.

## Wave 25 — SP-068 LANDED (2026-08-13) — integrate `f2662cd0`

- **LANDED:** three privacy filters that each **remove** something, over code the port had already shipped. (1) **Private/incognito windows are now dropped before anything counts them** — and the interesting part is that the WPF app turned out to keep **two separate lists** of the words that identify a private window, 35 entries each with **only 15 in common**. Porting either one alone would have silently dropped 20 markers the real app enforces, so the port took the **union of both (55)**, on the reasoning that this rule only ever *throws things away* — so combining the lists can never let through something WPF blocked. (2) **Window titles are scrubbed** — email addresses, runs of six or more digits, and control characters removed, then capped at 80 characters, using WPF's exact rules and numbers. (3) **URLs the model invents are stripped out of the companion's replies** before they reach the chat bubble, the history, or the disk; if a reply was *nothing but* invented links, no reply is shown and **nothing is written to the conversation history at all**. New floor **946 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 43 new tests, each filter with a control case proving it does not simply eat everything. **I did not take the worker's word for any of it:** I rebuilt and ran the full suite three times green on the exact merged tree in a separate checkout (the first run a cold, first-ever build), proved every code path byte-identical to what actually got pushed, and read the filters' code myself rather than its description. The worker also **broke each filter on purpose, one at a time** — four separate reverts — and each time only that filter's own tests went red, which is what makes them real guards rather than decoration.
- **DOES NOT PROVE:** that you can see any of this working. **The first two filters guard a path nothing in the app currently drives** — the window-title machinery is real, typed and wired into the safety boundary, but today only the tests call it, so those two harden a boundary a future feature will inherit rather than change anything on your screen. Only the third one, the link strip, is on a live path. Three more things are named rather than hidden: the link strip now also applies to the companion's *awareness* comments, which the WPF app never stripped — a deliberate choice to have **one** rule rather than two dialects, and it only ever removes more; a reply emptied by the strip shows the app's existing "companion is unavailable" line where WPF substituted a canned phrase, because the port has no canned phrase at that point in the code; and a title scrubbed down to nothing is sent as an **empty slot** rather than being left out entirely, because the port's request is one fixed text shape with no way to omit a field. Also unchanged: **none of this ran on Linux** — still zero WSL distros on this machine, as in every wave so far. And this is **three rows of one audit table, chosen by me at authoring** for being small, decision-free and provable without a screen; it is **not** a verdict on Her Room and it answers **none** of that audit's 12 questions to you. Those stay open.
- **OWNER:** one admission and one standing offer. **The admission:** for eight waves I have hit the same tooling defect — the advisor call comes back with good reasoning but no actual verdict, six times now — and each time I worked around it by asking narrowly and capping the reply, which works, and each time I did **not** file it as a task. That is precisely the rule this project has for everyone else: the same lesson twice means file the work, because a workaround that depends on someone remembering to type it fails the wave they forget. **I have now filed it**, along with a second one this wave surfaced (a test-isolation convention that nothing checks). No decision needed from you; you are seeing it because an unattended run that quietly absorbs its own recurring defects is the thing worth catching early. **The standing offer, unchanged from last wave:** the four big remaining parity surfaces — Goon Game, For You Feed, Trainer Card, Haptics v2 — need a real display, real media and a Linux box, so waves from this laptop will keep being small provable slices like this one. Say the word if you want me to write their sizing plan anyway so it is ready for a desktop session; otherwise I keep taking the cheapest honest parity available.

## Wave 26 — SP-069 launched (2026-08-13) — authoring phase, nothing landed

- **LANDED:** nothing yet — this phase authored the packet and launched the batch. It is the second wave under your back-to-parity default and the first one on a path **you can actually see**: the companion chat bubble. Right now the port shows you the model's reply **exactly as the model wrote it**. Between the model answering and the text hitting your screen, your conversation history and your disk, only two things happen to it — the safety check reads it, and last wave's link strip removes invented URLs. There is no other cleanup at all. WPF has three more, and shipped the last one as a bug fix: it strips the model's internal "thinking out loud" blocks, it strips the bracketed metadata tags, and it makes sure a raw JSON blob can never appear in the bubble. **The sharpest thing I found while authoring: the port creates the exact mess it does not clean up** — it sends the model a line shaped `[Category: … | App: … | Title: … | Duration: …]`, and models mirror that format straight back into their replies. So the packet cites the port's own line as the trigger, not just WPF's fix. **Counts at this commit are unchanged: 946 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — this phase touched only packet and planning files.
- **DOES NOT PROVE:** anything about the mechanism — the worker has not run. Three limits are written into the packet so they cannot quietly disappear. **The port will REFUSE a leaked JSON blob where WPF rescues the text out of it.** WPF tries to dig the readable sentence out and show it; the port will show nothing and record a typed reason. That is a deliberate choice, taken on advice: the digging-out step was the only part of this work that *adds* text rather than removing it, and the port never asks a model for JSON in the first place, so the case is rarer here. If you would rather see a rescued sentence than nothing, say so and it is a small follow-up. **Second:** the other half of that upstream fix — replies with effects being cut off at 100 words — has **no counterpart here**, because the port sends no length cap at all. I am recording that as a non-item rather than as work owed, so nobody re-files it in three waves' time. **Third:** none of this will run on Linux — still zero WSL distros on this machine.
- **OWNER:** no decision needed, but one thing worth knowing about how I am judging these. The safety check currently reads the model's raw text. Cleaning the text *before* that check would be exact WPF parity — but it would also mean a reply that is blocked today could pass tomorrow, which breaks the one promise this run has been holding to: every change makes less flow, never more. The advisor's fix was better than either option I brought: check **both** versions and block if **either** one trips. The gate can then only ever refuse more than it does today, never less, so the parity gain costs nothing in safety. That is what the packet does, and it is written up as a deliberate difference from WPF rather than smuggled in as parity. The standing offer from the last two waves is unchanged: Goon Game, For You Feed, Trainer Card and Haptics v2 all need a real display, real media and a Linux box, so waves from this laptop stay small and provable like this one until you are at the desktop.

## Wave 27 — SP-070 LANDED (2026-08-14) — integrate `9e6498b6`

- **LANDED:** the app can now recover its audio by itself. Until today, if the playback device failed to come up — a headset unplugged at the wrong moment, Windows restarting its audio service, the DtRH window opening while the endpoint was briefly gone — the port switched audio off **for the rest of the session**, on every channel, and the only way to get it back was to restart the app. That was not a slip: it was a faithful copy of how the WPF app used to behave, and the WPF app fixed exactly this in August ("no longer permanent"). Now the refusal expires: after 30 seconds, the next sound the app tries to make quietly schedules one re-check of the device, and if the endpoint is back, audio resumes. **New floor 1005 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 9 new tests, including a control case proving that a perfectly healthy session behaves exactly as before (one device call, no extra checks, no new log lines). **I did not take the worker's word for any of it:** three full green runs on the exact merged tree in a separate checkout, the first a cold first-ever build, and I broke the fix three different ways one at a time — each time only the tests guarding that one piece went red. The integration was a clean fast-forward, so what got pushed is byte-for-byte the tree I tested.
- **DOES NOT PROVE:** that it works against a real dead sound card. Every test drives a stand-in device, so what is proven is the decision logic, not how the actual audio driver behaves when Windows kills it — **the real check is yours to do when convenient: with the DtRH window open, restart the "Windows Audio Endpoint Builder" service, wait half a minute, and trigger a bark; audio should come back without relaunching.** Two limits are deliberate and named. The sound that *discovers* the 30 seconds have passed is itself still refused — recovery lands on the next one — because the alternative is freezing the window while the app asks a dead driver a question. And the upstream app also listens for "a device just came back" and recovers instantly; the port does not do that yet (Windows-only code that needs a real machine to prove), so it is filed as its own task rather than quietly missing. As always: **none of this ran on Linux** — still zero WSL distros on this machine.
- **OWNER:** nothing needed from you, but one thing worth seeing, because it is the honest shape of an unattended run. The whole point of this wave was "never block the app's window on a dead sound device", and the implementation got that right. Then a late review caught a genuine crash risk — the new background re-check could collide with the app shutting the device down — and the fix for it was a lock. Correct call; the collision it prevents is the kind that kills a process. But that lock is also taken when the DtRH window closes, on the window's own thread, in exactly the situation this feature exists for (a dead endpoint). So the freeze the wave banned from one place came back in another, smaller one. Nothing is broken and the lock had to land, but I filed it as its own task with a note on how to prove it, rather than letting it live as a comment in the code that nobody re-reads. **The standing offer is unchanged:** Goon Game, For You Feed, Trainer Card and Haptics v2 all need a real display, real media and a Linux box, so waves from this laptop stay small and provable like this one until you are at the desktop.

## Wave 28 — SP-071 LANDED (2026-08-14) — integrate `d1c69617`

- **LANDED:** closing the DtRH window can no longer freeze the app on a dead sound device. Last wave gave the audio a way to recover itself, and the fix for a genuine crash risk inside it was a lock — correct, but that lock was also taken when the window closes, on the window own thread, in exactly the situation the feature exists for. So the freeze banned from one place had come back in a smaller one, and this wave removes it. Now the part that has to wait for the sound driver runs on a background thread; the window waits at most **two seconds**, then writes one line saying so and closes anyway, and — the part that matters — the giving-up path never touches the sound device again, because touching a device while the driver is still inside a call is the kind of collision that kills the whole process. When the stuck call finally returns, the background thread finishes the shutdown exactly once, so nothing leaks and the window can be reopened. **New floor 1010 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 5 new tests, including a control proving an ordinary close behaves exactly as before. **I did not take the worker word for any of it:** three full green runs on the exact merged tree in a separate checkout (the first a cold, first-ever build), the pushed tree proven byte-identical to the tested one, and I read the ordering test myself rather than its description — because a test that only shows the window closing fast is also consistent with the dangerous version.
- **DOES NOT PROVE:** that a really wedged sound driver behaves like the stand-in one. Every test drives a fake that parks on command; what is proven is the decision logic and the ordering, not miniaudio inside a dead Windows audio service — and **that cannot be induced on this laptop, so it is named rather than simulated**. Two consequences are deliberate and stated rather than hidden: after a give-up the old sound backend stays alive until the stuck call returns, so a reopened window briefly runs a second one alongside it; and each give-up leaves one background thread that lives until the wedge clears — it can never hold the app open, and the abandoned audio object refuses every request from that point, but they are real residue, not zero. **I checked one claim the worker made and it did not hold:** the packet said that residue is bounded by you closing the window, and it is not — the window also closes itself, on a watchdog relaunch, on a forced-exit timer, and when the page reports a boot error. It is still bounded in practice (the watchdog only ever relaunches once), so nothing is broken, but "a person has to do it" was the wrong reason and it is now its own filed task with a real bound as its acceptance. **This closes one member of the family, not the family:** two other places still block on the sound driver with no bound (creating a player), left alone on purpose because fixing them changes what a caller is promised and risks a half-built player attaching itself to the mixer after everyone has stopped waiting — filed as its own task with that hazard as its central test. As always: **none of this ran on Linux** — still zero WSL distros on this machine.
- **OWNER:** the interesting failure this wave was not in the code, it was in the machinery. **The worker did everything right and the run still reported itself failed.** Every gate passed — the contract check, the code review, the final review — and then the step that tidies up after the worker refused, because the worker had been *too* tidy: it committed its own completion marker, so there was nothing left for that step to commit, and the only thing it could still see in the folder was the tooling install that every lane gets on purpose. Last wave survived this by pure luck. I recovered it through the tool own supported paths rather than editing its state file — the work was already reviewed and merged-ready, so re-running a 67-minute job to satisfy bookkeeping would have been the expensive wrong answer — and then wrote down the one-line change that prevents it recurring. **Nothing needed from you.** The standing offer is unchanged: Goon Game, For You Feed, Trainer Card and Haptics v2 all need a real display, real media and a Linux box, so waves from this laptop stay small and provable like this one until you are at the desktop.

## Upstream sync — WPF v6.7.4 → v6.8.0 (2026-08-14) — merge `db3e842f`, audit `a69d3c87`

- **LANDED:** the WPF reference tree the port reads for evidence is current again — 125 upstream commits, 384 files, no conflicts, and the client still builds clean and passes all 1010 + 35 tests untouched. Done while a batch was running, without ever checking out the WPF branch (that would have deleted the running engine and the whole port from the working folder). **The reason to do it now rather than later:** upstream fixed a bug in code we had already copied. Your companion chat strips one wrapper off a reply; upstream discovered replies can come back as a whole multi-speaker transcript, fixed it, and their own comment says the old code leaves "exactly the mangled shape that reached the bubble". **We are that old code** — so today a transcript-shaped reply reaches your bubble half-cleaned, and lines the model attributed to a *different* companion are shown as if the current one said them. Filed as a defect, not a feature.
- **DOES NOT PROVE:** that I found everything on the first pass — **I did not, and you were right to ask.** My first scan looked only at citations inside the port's code and reported "12 files affected". The port also cites the WPF app inside its own contract documents, and those back finished work. The real numbers are **297 files cited, 106 changed, 19 of them touching a finished contract**. Three things had been lost and are now filed: a **persistence** change (upstream now merges bundled presets into your saved settings on load — we have no such concept), a **finished-and-ratified** feature whose WPF ground truth was rewritten underneath it (the card quick-toggle), and a whole **"remote media"** capability I had split across three buckets so it read as unrelated small items. The remaining 91 files are not dropped: each carries a tier and a verdict in a new data file that is regenerated and compared at every future sync, and the leftovers are owed to two named review rows.
- **OWNER:** one decision, and it got wider rather than narrower. **v6.8.0 adds four surfaces that all reach out to the network** — THE DESCENT (reads your profile from the server with an auth token), JUST DROP (a hosted web shop), remote video clips inside the video service, and the prompt that offers you a remote media source in the first place. The port today makes **no external network calls at all** and that claim is machine-checked. **Please answer for all four at once:** approving just one of them (say, the shop) would leave a video path quietly pulling remote clips that nobody sanctioned. There is also a second, smaller thing to know: the quick-toggle card work you ratified in July still looks correct to me, but the WPF code its evidence points at has since been rewritten, so I filed a notice rather than silently re-opening a decision you already made.

## Wave 29 — SP-072 LANDED (2026-08-14) — integrate `c04ecb67`

- **LANDED:** the app can no longer be frozen by a sound file that will not load — and, more importantly, it cannot be haunted by one either. Two places built an audio player by asking the sound driver to open a file, and waited forever for an answer. Bounding that wait was the obvious fix and it would have been **worse than the freeze**: the loading does not stop just because we stopped waiting, and the last thing it does is attach the finished player to the live mixer. You would have got a sound firing minutes late, for a moment that had passed, plus a leak. So the real work was making an abandoned load safe: it never reaches the mixer, never plays, is cleaned up exactly once, and its cleanup can never collide with the sound device being shut down. **New floor 1017 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 7 new tests, each broken on purpose one at a time to prove it guards something real. Every place that asks for a player already knew how to say "no player available", so the bound went in everywhere with no caller left behind.
- **DOES NOT PROVE:** that a real sound driver misbehaves the way the stand-in does. The one line that actually attaches a player to the mixer is never run by any test on this machine — there is no audio device here to run it — and that is stated rather than glossed. Two smaller things are named the same way: the refusal handling in the DtRH effects layer is verified by compiling and reading, not by running, because its test file was outside this task's allowed scope; and none of it ran on Linux, as usual. One new cost is filed rather than hidden: an abandoned load still occupies a background worker until the driver answers, so a genuinely dead device could accumulate them — smaller than the freeze it replaces, but not zero.
- **OWNER:** nothing needed. Two notes. **Your decision on external connections is recorded and acted on** — the four surfaces are unblocked, and the one consequence I want you to see is that Linux has nowhere secure to keep a login token today, so I filed that as a P1 rather than letting it be discovered later by shipping a token in plain text. **And a small process win:** last wave failed on a tooling technicality after all its real checks had passed. The fix was one sentence added to the task template rather than a patch to the tooling; this wave was its first live test and it worked. I mention it because the alternative fix I was offered would have quietly broken something else.

## Wave 30 — SP-073 LANDED (2026-08-15) — squash `88a058ef`

- **LANDED:** repeatedly opening and closing the hole no longer leaves a piece of the app stuck behind each time. When the sound device stops answering, closing the window used to abandon one background worker per close, still holding the dead device. Now the close hands that cleanup to whichever thread is already inside the sound driver, so it happens at exactly the same moment it would have, and nothing is left parked. **New floor 1022 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 4 new tests, each broken on purpose one at a time to prove it guards something real. One of those deliberate breaks also broke three of last wave's tests, which is the best evidence available that the cleanup genuinely cannot be skipped.
- **DOES NOT PROVE:** that a real sound driver behaves like the stand-in. No real audio device is exercised anywhere in this, and none of it ran on Linux — still no Linux install on this laptop. One piece of the reasoning is argued rather than demonstrated: the fix depends on an ordering between two threads that no test can force to happen on demand, so what the tests prove is the effect, not the timing argument itself. Three smaller costs are written down rather than hidden, and each is now its own task: a wedged cleanup can still park one worker; a failure to start the cleanup thread would leave the device un-released while the log claims otherwise; and the abandoned-load problem from last wave still parks a shared worker thread.
- **OWNER:** nothing needed, but one thing is worth knowing. **The checking layer paid for itself four times this wave.** Before any code was written, the plan review found that the first design would have permanently leaked the sound device — and would have shown up as a test that fails once in a while rather than a clean break, which is the hardest kind to chase. It also caught that the design broke a rule written down one wave earlier. After the code was written, two more real problems were found. None of these would have been caught by the tests passing. **Nothing was ratified as done:** the task stays open pending your review, as always.

## Wave 31 — SP-079 + SP-081 LANDED (2026-08-15) — the first wave run at eight lanes

- **LANDED:** two of eight jobs. The real result is the experiment: **eight jobs ran at once for the first time, and running them at once was not the problem.** Everything I was told to watch held — the jobs stayed properly isolated from each other, none of them fought over the shared test-count file, and the machine never came close to running out of memory or disk. What broke was the review step. Six of the eight jobs were killed by their own plan review before a single line of code was written, and in several cases the reviewer approved the design in the same breath as blocking it — one wrote *"three text corrections, no re-plan… no source change, no fact, no test and no command needs to move"*, and that verdict still threw the job away. Of the two that finished: the AI reply filter can now tell you **which** of its two checks refused a reply instead of reporting both the same way, and the audit tooling got a correction plus proof of a problem described below. **New floor 1028 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 6 new tests, each broken on purpose one at a time to prove it guards something real, and the job reported two of its own predictions as measured **wrong** rather than quietly bending them to fit.
- **DOES NOT PROVE:** that the six killed jobs were killed unfairly. I checked, and four of them were stopped for at least one genuinely good reason — a missing test, a rule that would have failed on every file in the project, a list of callers that was wrong by about eight times, a test whose "proof" had no defined failure. That is the review layer doing its job. Only two of the six were stopped over wording. I originally wrote this up as "the gate failed" and that was an overstatement; an adversarial review of my own diagnosis corrected it, and the correction is recorded. Also unproven as ever: no Linux, no real audio device, and the new refusal code is checked by tests rather than by using the app.
- **OWNER:** **one thing you should know, because it changes what I can do while you are away.** The unattended overnight loop **cannot run on this machine at all.** It launches a fresh, independent Claude for each step, and every one of those exits immediately with "Not logged in" — the app holds the login and hands it only to its own children, so a separately launched copy has nothing to read. This was proved four different ways rather than assumed, and one of the two finished jobs is that proof. Two consequences. First, the independent audit that normally double-checks every landing had to be done by a helper inside this session instead; that is a real check but a weaker one than a completely separate program, and I have written it down as weaker rather than calling it equal. Second, if you want genuinely unattended overnight runs later, the fix is on your side, not mine: a logged-in command-line install or an API key in the environment. **Nothing was ratified as done** — both landed items stay open pending your review, as always.
- **AUDIT — READ THIS ONE, IT MATTERS MORE THAN THE LANDING:** the independent check of this landing came back **FAIL**, and it was right to. It re-ran the tests on the published code and got **two failures** where I had recorded three clean passes an hour earlier. I chased it rather than explaining it away, and the cause is this: two tests ask Windows "what window is in front?" and require a real answer. **While you are sitting at the machine they pass. Once you walk away and the session locks, Windows honestly answers "there is no window in front", the app honestly reports the feature as unavailable, and those two tests fail.** I proved it is not something the landing broke by re-running exactly those two tests, on the same code, in the same folder that had passed three times — they fail there too now. Nothing about the code changed; only whether someone was logged in. **The consequence is bigger than this landing:** the single automatic check this project uses before shipping anything is therefore **red for as long as nobody is at the desk** — which is exactly when unattended overnight work runs. I have filed it as the top-priority item and it is now the next thing I fix, because nothing else can be safely landed until it is. The two landed items themselves are unaffected and stay open pending your review.

## Wave 32 — SP-082 LANDED (2026-08-15) — the automatic check now works while you are away

- **LANDED:** the fix for the problem the last audit found. Two tests demanded that Windows report a real window in front; once you walked away and the session locked, Windows honestly said there was none, the app honestly reported the feature unavailable, and those tests failed — so the one automatic check this project runs before shipping anything was red the whole time nobody was at the desk. Those two tests now **step aside** in that situation instead of failing, and they step aside **only** on the app's own specific answer for it — not on "is this Windows", not on a setting, not on anything that could be quietly switched on. If the app reports any other problem they still fail, so this cannot hide a real fault by being lenient. **Floor unchanged at 1028 tests / 35 headless / 0 warnings, 0 errors** — nothing was added or removed, so the count is identical whether the machine is locked or in use.
- **DOES NOT PROVE:** that I watched it work on a genuinely locked machine. The landing ran while the session was interactive, so the locked case is still simulated rather than observed, and that check stays on the list for you. And there is a real price I want you to see rather than find later: the app's answer for "no window in front" is also its answer for "the code that looks for the window is broken". Those are now indistinguishable, and the single test that exercised that code for real is the one that steps aside — so a fault in it would go unnoticed on any machine, where before it would have failed loudly. That is unavoidable inside this fix (making the test smarter about it is exactly the loophole the fix exists to close), so it is written down and filed as its own task rather than quietly accepted.
- **OWNER:** nothing needed. Two things worth knowing. **The review layer paid for itself again, and in an unusual way:** the last reviewer noticed that the required proof — force the "step aside" to trigger and show everything still passes — had been claimed but never actually run, so it ran it itself and got a clean result. Both earlier reviewers had passed the work while the step-aside was never triggered, which proves nothing about the case it exists for. **And a process fix from earlier today shows its first result:** the plan review approved on the first round, where last wave six of eight jobs were killed after three rounds. One job is not proof, but it is the first sign the change worked. **Nothing was ratified as done** — both landed items stay open pending your review.

## Wave 33 — SIX packets LANDED (2026-08-15) — the biggest single land so far

- **LANDED:** six jobs at once, all six finished. **Test floor 1028 → 1046, 0 warnings, 0 errors** — 18 new tests, each one broken on purpose to prove it guards something real. What they fix: a sound-loading failure can no longer quietly pile up stuck background workers until the app refuses to play anything; a capability check that gets cancelled no longer records a result it never actually reached; the intermittent tunnel-logging failure is fixed at its cause rather than hidden; a test-isolation convention that was only written down is now enforced by machinery; the save-file lifecycle claims are corrected and pinned; and there is now a tool that notices when the WPF app changes a file the port quotes as its evidence. **The previous wave killed six of eight jobs before any code was written; this wave finished six of six.**
- **DOES NOT PROVE:** the usual, and one thing more. None of it ran on Linux, none of it touched real audio hardware, and no on-screen behaviour was checked — these are logic and test-suite changes. The tunnel fix in particular is **not** proven by the tests passing: the failure was intermittent, and what was proven is that the specific mechanism causing it is now impossible, which is a different and slightly weaker claim, stated that way deliberately. Several jobs also wrote down what they could **not** close — a sound-device wedge that only real hardware can produce, three blind spots in the new isolation guard, and a citation tool that no automatic check runs yet.
- **OWNER:** nothing needed, but one thing is worth your attention because it is the best sign of health here. **Four of the six jobs told their own reviewer it was wrong, and proved it with a measurement.** The strongest case: a reviewer said a particular sabotage test would leave all ten checks passing, so the job ran the sabotage — and five checks failed, for a reason the reviewer had not seen. It then built the sabotage that *does* isolate the one check, got exactly the result predicted, and wrote down both. That is the difference between a review layer that is obeyed and one that is used. **Nothing was ratified as done** — all six stay open pending your review.

## Wave 34 — SP-089 LANDED (2026-08-15) — buying back what the last fix gave up

- **LANDED:** the safety net for the code that reads the title of whatever window is in front. Last wave's fix made two tests step aside when Windows reports no window in front — necessary, but it meant the code that does the actual reading was no longer being run by anything, on any machine. Now a test drives that code against a window **it creates itself**, so it runs whether or not anyone is logged in. If that code is ever broken by a bad edit, something fails loudly again. **Floor 1046 → 1048, 0 warnings, 0 errors.** The change to the app itself is a pure lift-and-shift with no behaviour altered, which I checked from the diff rather than taking the job's word for it.
- **DOES NOT PROVE:** that every way this can break is now caught. If Windows itself started reporting "no window in front" when there is one, this still cannot tell that apart from a genuinely locked screen — that limit is written down rather than glossed. No Linux, and nothing on-screen was checked.
- **OWNER:** nothing needed, but this one is worth a moment. **It is the first job all night to pass every review stage first time**, after a wave where six of eight died at the first stage and a wave where all six needed a correction round. More importantly it closes a debt the previous wave created *and declared*: that fix's own final reviewer insisted the cost be written down as a task rather than buried in a code comment, and because it was a task, the next wave paid it. That is the process working end to end. **Nothing was ratified as done** — it stays open pending your review.

## Wave 35 — SP-090 LANDED (2026-08-15) — the safety rules on the safety net are now enforced, not just written

- **LANDED:** the project keeps one list of tests that are allowed to sit out on a given machine, and two tests are permanently forbidden from ever joining it, because their sitting out would hide exactly the faults they exist to catch. Until now that ban was **a sentence in a file** — nothing stopped anyone adding those two names. Now a check enforces it, and it is built so the obvious shortcut could not work: if the ban were read from the same file it guards, deleting the sentence and adding the name would slip through. So the two names are written into the check itself, and it separately confirms the sentence is still there — drift in either direction fails. **Floor 1048 -> 1052, 0 warnings, 0 errors.**
- **DOES NOT PROVE:** that every future ban is covered. The check enforces the two names it knows about; a third ban added later would have to be added here too, and nothing detects that omission. That limit is written on the task rather than left implied.
- **OWNER:** nothing needed. Worth knowing: **the job found a hole I had not thought of and closed it.** A ban naming a test that has since been renamed or deleted protects nothing while looking perfectly healthy — so it added a check that both banned names still match a real test. Nothing else in the suite would have caught that. It is also the second job in a row to pass every review stage first time.

## 2026-08-18 — the dashboard row's premise was wrong, and the evidence now says so

- **LANDED:** a document, not code. Before building the thing that finally makes the app usable — the way a user actually walks from the home screen to a feature — I had the real Windows app read in detail. **What came back contradicted the plan.** The task list said each finished feature should get a tile on the home screen that opens it. The real app does the opposite, and says so in its own source: **tiles navigate, they never open anything.** You always take two steps — a tile or a rail door takes you to a page, and one button on that page opens the window. None of the three features the port has finished even has a tile on the home screen; they each had one for a single day last August and it was deliberately removed. All of this is now written down with citations at `client/docs/wpf-surface-reachability.md`, and the task row has been corrected and raised to P1.
- **DOES NOT PROVE:** nothing was built. No navigation exists yet; six finished features are still reachable only by typing a command-line flag. The document is evidence for the work, not the work. I also could not prove the absence of keyboard shortcuts to these features beyond the places I searched, and window sizing on mixed-DPI multi-monitor setups is unverified because this machine cannot produce that evidence.
- **OWNER:** **one question, and it decides the shape of the next month of work.** Does the port's dashboard correspond to the Windows app's **home mosaic** (11 effect tiles, left-click opens the effect's page, right-click switches it on) or to its **Play page** (the big "DOWN THE RABBIT HOLE" card with FALL IN and Quick Drop)? Work can start either way — the two-step shape and the click grammar are the same in both — but the answer decides what the port's current placeholder window grows into. **Also worth your view:** the port's "Open companion" button matches neither Windows entry point (there, the dashboard strip navigates to her page, and the window itself simply appears at startup because the setting defaults on). Keep the port's version as a deliberate improvement, or match Windows?

## Wave 36 — SP-091, SP-092, SP-093 LANDED (2026-08-18) — the app has a shell, and one feature you can actually reach

- **LANDED:** the port finally has a **navigation shell**. Until today its entire user-facing surface was one demonstrator card and one button; six finished subsystems could only be reached by typing a command-line flag. Now there is a rail with three doors, and from a cold start with no arguments you can click **Studio -> Spiral Overlay -> THE LOOM** and the real Loom studio opens: spiral rendering, weave and thread and motion controls, and both saved spirals in its rack. That is **the first landed subsystem in this port a person can reach by ordinary use.** Two supporting capabilities landed beside it: a **tray icon** capability for Windows, and an **entitlement** capability that reads the login you already made in the shipping app. **Floor 1052 -> 1090 unit tests and 35 -> 39 headless, 0 warnings, 0 errors, blind-audited SHIP at the pushed commit by a context that did not do the work.**
- **DOES NOT PROVE — and one correction to the landing commit, found by that audit.** The commit message says the only presentation-verified claim in the wave is the Loom window. **That is wrong in both directions and it inflated the headline.** The harness declares three presentation-verified checks and none of them is a Loom surface: they are the two rail-door border states and the window background. The Loom window's own appearance rests on **a single headed run I drove by hand**, plus log lines and a clean exit — real evidence that it opens, but not the deterministic pixel check the other three get. Beyond that: **DTRH, Graded Intake, the AvatarTube demo and the Chaos tunnel are still command-line only** — the shell has exactly two buttons. The tray and entitlement capabilities are **built and tested but wired to nothing**, so no user can reach either yet, and the entitlement one **cannot currently say "yes, you are a patron" at all** (see below). Linux is entirely unmeasured, and the Linux half of the capture harness was rewritten and **never once run**. Everything here is one machine, one screen, one DPI.
- **OWNER: one decision blocks the entitlement bridge, and it is a privacy call, not a technical one.** Resolving your tier means the port would **send your borrowed login token to the server** and read a second file from the shipping app to get your account id. The current rule on the board says the token may never be transmitted — written when it was only being read locally — so as it stands the capability can never return "entitled". **The shipping app already makes this exact call as you, so the network exposure is not new; what would be new is a second program on your machine holding and sending your credential.** Your call, and nothing else in the entitlement work moves until you make it. Also worth knowing: I filed a row against SP-092 saying it had missed the evidence for this, and **its reviewer showed the fault was mine** — I had written the no-transmission rule myself and then blamed the lane for obeying it.


## Wave 37 — SP-094 LANDED (2026-08-18) — the rabbit hole has a door, and it tells the truth when it will not open

- **LANDED:** a **Play page with the Down The Rabbit Hole card**, reached from a new rail door, with `FALL IN` and `Quick Drop` wired to the real launcher (`FALL IN` goes through the real save-slot picker; `Quick Drop` deliberately skips it, exactly as the Windows app does). When you are entitled it opens the picker; when the app cannot tell whether you are entitled it **says so, in those words**, and the buttons stay live so the click still lands. **Floor 1090 -> 1100 unit and 39 -> 48 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE — and one thing here is worth more than the feature.** A **headed capture caught a defect every test had passed**: the refusal message rendered on top of the card's own title and blurb and was hard to read. The tests were green, the logic was right, and the screen was wrong. It is fixed and re-verified by eye, and the lesson is written into the record: when the promise is "the user can read this", only a picture measures it. Beyond that: **nobody can actually fall in yet**, because the tier lookup is unconfigured (see below); the game window itself is opened by no test; the minimize-and-restore around it has no test at all; Linux is unproven for this route; and the tray is still untouched.
- **OWNER: the same one decision, now the only thing between you and the door.** Everything on the port side is built. Until you decide whether the port may send your borrowed login to the server, **every user including you sees "could not verify"** rather than the game. Also recorded, because it is a real behaviour difference you should know about: the shipping app opens DTRH free for everyone on a **server-declared drop day**, and the port cannot do that — it has no server telling it which day. Building a local version was rejected on purpose, because the port would be picking that date itself and giving away paid content on it.

- **CORRECTIONS to the wave-37 landing commit, found by the blind audit and recorded here because published history cannot be edited.** (1) The commit says the headed self-test ran "green with four doors". **It did not check four.** `capture.ps1` carried a hard-coded list of three door names written when three was the whole rail, so the door this very wave added was never checked — while the harness still printed "every rail door published a layout probe". The product was fine; the check was blind to exactly the new thing. **Fixed in the same breath as this correction:** the harness now reads the doors out of the running app and demands a probe for each, so adding a door widens it automatically. Proven by breaking a probe on purpose and watching it fail by name. (2) The commit and one divergence row say "could not verify" is the ONLY thing a user can see. Slightly too strong: someone who has never installed the Windows app gets a different, also honest, "no login found on this machine". (3) The one picture-based claim in the wave has no saved picture — the capture was read and discarded, so that check rests on my word rather than an artifact.

## Wave 38 — SP-095 LANDED (2026-08-18) — the last command-line-only surfaces, settled

- **LANDED:** the rail now has **five doors** — Studio, Companion, Play, Graded Intake, System — and Graded Intake is the new one, with its own page and a **Begin Intake** button. The other two things that were command-line only got **no door on purpose**: the chaos tunnel turns out to be a backdrop that renders underneath the game and can never be focused or Alt-Tabbed to, whose only control is a checkbox on a setup screen the port has not built; and the avatar tube is not something the Windows app opens from the dashboard either. **Floor 1100 -> 1110 unit and 48 -> 54 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE — and one correction that mattered.** The intake first shipped **ungated**, on the argument that its weekly pass should not be ported. That was wrong and it was caught: the intake is paid content where patrons get unlimited runs and free accounts get one a week, so an ungated port would have handed every user the paid privilege. It is now gated, and because this port has no account to read, what you will actually see is an honest refusal saying so. Beyond that: no test opens a real intake window, two of the four gate outcomes are unreachable in this build and tested only as pure logic, Linux is unproven for this route, and the only picture-based check was mine rather than the packet's.
- **OWNER: still the same single decision, now blocking two features instead of one.** Both Down The Rabbit Hole and Graded Intake are built, reachable, and refusing everyone, because neither can determine your entitlement without the permission question you have not been asked to answer yet. Nothing else in the port is waiting on you.

## Wave 39 — SP-096 LANDED (2026-08-18) — a tray icon that works, and a behaviour deliberately not copied

- **LANDED:** when you start Down The Rabbit Hole, the app now minimises and puts a **real tray icon** in the notification area with the same four entries the Windows app uses -- Show Dashboard, Wake Up!, a separator, Exit -- plus the same first-time balloon. Closing the game brings the window back, and so does the icon or the menu. **Floor 1110 -> 1117 unit and 54 -> 62 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE -- and one thing the Windows app does that this deliberately does not.** Windows *hides* the main window during a descent; the port only minimises it. That is not laziness: it was **measured**. In Avalonia, hiding a window also hides every window it owns, and bringing it back does not bring them back -- so copying Windows here would have hidden the game itself. The measurement is now a permanent test whose failure message tells whoever sees it to re-open the decision on purpose. The practical difference is that the port leaves you a taskbar button as well, so there are three ways back where Windows has two. Not proven: that anyone can see the icon, that a real right-click opens the menu, that the balloon ever appears (Windows suppresses those silently under Focus Assist), or anything at all on Linux.
- **OWNER: nothing needed here, and one thing worth knowing.** Making the port hide the window like Windows does would mean changing how the game window is attached to the main one -- and the Windows source itself says that attachment exists *because* a tray restore used to drag the main window over the game. So copying the look would re-introduce the bug the attachment was added to fix. Left alone deliberately, written down, and yours to overrule if you want the exact Windows behaviour with that trade.

## Wave 40 — SP-097 LANDED (2026-08-18) — when something breaks, you now find out

- **LANDED:** if starting Down The Rabbit Hole or the Graded Intake fails, the app now **tells you**, on the page, with what went wrong. Before this it wrote a line to a log file and showed you nothing at all. The message is deliberately made to look different from the "could not check your subscription" one -- different colour, different wording, different panel -- so a broken app and an unknown subscription never read as the same thing. **Floor 1117 -> 1128 unit and 62 -> 70 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE -- and one thing found by accident that matters more than the feature.** While writing the failure text, a blank line in it made the automated test suite **hang instead of fail**. It was traced to an Avalonia bug, reproduced from scratch with no app code involved, and -- the important part -- **it reproduces in text the app already shipped three waves ago**. A hang is the worst thing a test suite can do, because it gives no signal at all; someone eventually kills it and assumes the machine was busy. There is now a check that sweeps every piece of on-screen copy the app can produce and refuses any that would trigger it. Not proven: that the amber reads as "broke" to a human eye, that it lays out legibly on a real screen, or anything on Linux.
- **OWNER: nothing needed.** This one was pure repair -- a gap where the Windows app talks to you and the port did not.

## Wave 41 — SP-098 LANDED (2026-08-18) — the app starts doing something

- **LANDED:** the port has a **session** now. There is a START button, it really starts work, and stopping really stops it -- proved by winding the clock forward ten more times after stop and getting nothing at all. The first effect, **Flash Images**, runs under it with three working dials, a live status dot and the right-click toggle. **Floor 1128 -> 1175 unit and 70 -> 81 headless, 0 warnings, 0 errors.** Everything before today was the building; this is the first room in it.
- **DOES NOT PROVE -- and be clear about what you would see.** **No flash appears on your screen yet.** Flash Images draws through a see-through always-on-top window, and that piece is deliberately not built here: it is the exact work that sank the previous attempt at this port, so it gets its own job rather than being rushed. The app says so on the module page before you press anything. Everything else about the effect is real -- it picks images, it paces them the way the Windows app does, and it stops when told. **Fourteen of the fifteen effect modules still do not exist.**
- **OWNER: nothing needed, but one judgement worth knowing about.** There was an easier first effect -- Mind Wipe, which is pure audio and would have looked completely finished. It was rejected on purpose, because nothing in this project can actually verify that a sound played, so shipping it would have meant claiming something nobody could check. A named missing piece beats an unverifiable claim. That is the standard the whole port is being held to.

- **LANDED (wave 45, SP-105):** the port's **third** effect, and the first that is simply *on* rather than timed -- **Pink Filter**, the screen tint. Both it and **Subliminals** now have working rows in the Studio rack, so you can switch them on from the UI; until today Subliminals could only be turned on by editing a file. **Floor 1314 -> 1372 unit and 81 -> 87 headless, 0 warnings, 0 errors.** The wave was spent to answer one question and it answered it: the effect *interface* was sound and fit the new module with no edit at all, but the shared base class every module inherited had quietly become a scheduler -- it demanded a clock and a "next firing" from things that have neither. That is now split, at module three instead of module fifteen.
- **DOES NOT PROVE.** **Nobody has seen the tint on a screen.** Every overlay in this wave's tests is a stand-in that records what it was told to do; no test anywhere proves a coloured pixel reached a real display, and the tests deliberately never start a real session, because a real pink filter would throw a full-screen always-on-top window over whoever was running them. On Linux the tint is refused by design and only the refusal is tested -- **it is not supported there**, and the app now says so honestly mid-session instead of telling you to start a session you already started. Multi-monitor is untested. **Twelve of the fifteen effect modules still do not exist**, and there is still no video and no webcam.
- **OWNER: two decisions still waiting on you**, both unchanged from before, both blocking real features. (1) May the port hand your logged-in token to the servers to check what you have paid for? Until you say, **THE DESCENT and Graded Intake refuse everybody** -- including you. (2) Subliminals keeps its own settings file, because the shared one resets *everything* to defaults when any part of it is broken; one bad phrase list would otherwise wipe your flash speed. Say the word and either can change.

- **LANDED (wave 46, SP-106):** the port's **fourth** effect and the first that has to keep *moving*: **Spiral Overlay**, the turning spiral, with a rack row, a live dot and the right-click toggle. **Floor 1372 -> 1472 unit and 87 -> 90 headless, 0 warnings, 0 errors.** The packet asked for **Bouncing Text** and got a documented refusal instead: the port's overlay cannot draw see-through glyphs, and moving a window means briefly making it catch your clicks -- sixty times a second at full speed. That evidence was written down *before* any code, and Spiral was built instead. Also fixed: a real defect in shared code where a stopping session could tear down the surface of the session that had just replaced it.
- **DOES NOT PROVE.** **Nobody has seen the spiral turn.** Every overlay in this wave's tests is a stand-in; nothing proves a real spiral reaches a real screen, and the port ships **no spiral art**, so switching it on with an empty library shows you the folder to drop a file into. Linux refuses by design and only the refusal is tested. **Eleven of the fifteen modules still do not exist**, and there is still no video and no webcam. One run out of the whole wave reported three failures whose names were lost and which never came back across twelve later clean runs -- **recorded as unexplained rather than quietly dropped.**
- **OWNER: still the same two decisions, plus one thing worth knowing.** The two waiting on you are unchanged: whether the port may hand your logged-in token to the servers (until then THE DESCENT and Graded Intake refuse everybody, including you), and Subliminals' separate settings file. The thing worth knowing: a fix that looked strictly safer, was argued carefully in the code, and passed a fully green test suite would have **hung shutdown** -- caught only by reading, because every test drives that path on one thread. It was reverted, and the reason is written at the exact line so nobody tries it again.

- **LANDED (wave 47, SP-107):** not a feature -- **the measuring instrument.** Every land in this port is certified by "three consecutive green test runs", and it turned out that gate was failing about one run in five for reasons that had nothing to do with the code being checked. Cause found and proven: the harness allows three test runs at once, and the tests that put a REAL window on your REAL screen all used the same file name, the same colour and the same screen position -- so they were tripping over each other. One failure read "expected 0, got 676161", and 676161 is exactly 951x711: one whole *other* run's flash being counted as this one's. Fixed with a machine-wide lease so only one run can touch the desktop at a time. **Red rate 67% under three-way concurrency, now 5.3%. Floor 1472 -> 1477 unit, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** **The gate is better, not trustworthy.** One test still fails about 1 run in 20 for a reason nobody has explained yet -- it is now instrumented so the next failure says which of four things went wrong, rather than guessed at. Two easy fixes were available and both were refused on purpose: adding a delay, and loosening the check. Either would have made the number look better while hiding the fault. **What three green runs now mean: no two test runs fought over the screen. NOT that everything is fine.** Also worth knowing: the 76 measurement runs were taken before a last change to the lease, and that is now written down where the number is published rather than left to look stronger than it is.
- **OWNER: nothing needed, and one thing you should probably know.** This flake was caused by how *I* run the work -- reviewers checking the gate at the same time as a land. It has been silently weakening the evidence behind earlier waves. No earlier wave is wrong; the confidence behind them was just lower than it looked, and only for runs that overlapped. The two decisions still waiting on you are unchanged: the login token for premium features, and Subliminals' separate settings file.

- **LANDED (wave 48, SP-108):** the **fifth** module, and the first that puts nothing on your screen: **Intensity Ramp** from WPF's TIMING group. It climbs the other modules' opacity dials over the session and hands the original values back when you stop. That means the rack now has **two groups instead of one** -- every module before this was an EFFECTS-group module that painted an overlay. **Floor 1477 -> 1527 unit and 90 -> 95 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** Nobody has watched an opacity climb on a real screen; there is still no headed capture anywhere in this port. Nothing here proves composited pixels. Concurrency is argued from reading the code, not measured -- and two waves ago that exact gap let a shutdown-hanging bug through a fully green test suite. **Ten of the fifteen modules still do not exist**, no video, no webcam.
- **OWNER: the useful finding is about what comes NEXT, and it is not another module.** The lane surveyed every remaining module and the answer is that the port's next gap is a **capability**, not a module: **audio**. One remaining module (Mind Wipe) is buildable today, and Brain Drain is only HALF buildable -- its audio half is ordinary, but the same switch also blurs your whole desktop at 30-60 frames a second, which this port cannot do yet. Everything else needs click-capturing windows, video, a device stack, or per-pixel transparency. **One useful correction for you:** an earlier draft claimed your Windows app has a bug where quitting mid-ramp saves the ramped opacity as if you had chosen it. **It does not** -- all three exit routes restore first. That claim was wrong and is retracted.

- **LANDED (wave 49, SP-109): sound.** **Mind Wipe** works, and **Brain Drain's audio half** works. That is seven of the fifteen modules. **Floor 1527 -> 1589 unit and 95 -> 100 headless, 0 warnings, 0 errors.** This is the capability an earlier wave deliberately refused to build, on the grounds that nothing here could check a sound had actually played -- so rather than take that on faith, the port now asks Windows four questions in a row: is there a working output device, does *this* program own an audio session, does Windows say that session is live, and **is the loudness meter on our own stream actually moving**. It measured 0, then 0.405, then 0 again. A version that opened the device and played nothing fails that test on purpose.
- **DOES NOT PROVE -- and the honest ceiling is written into the app itself.** Windows agreeing that our audio is playing is **not** proof you heard it: it does not prove the volume is up, that speakers are plugged in, that the output is a real device rather than a virtual one, or that the right clip played. **Nobody has confirmed a sound was audible**, and no automated test on any platform can -- that is a manual check, named as one. **Brain Drain is deliberately HALF** -- its audio works, but the same switch also blurs your whole desktop, which this port still cannot do, so the row says "audio half" in its title and the panel says so before you can turn it on. Eight of fifteen modules still do not exist; no video, no webcam, no headed capture.
- **OWNER: one thing found in YOUR app.** Mind Wipe's source says it works on a "30-second window", but the timer runs every 10 seconds and the maths divides by 360, so the real behaviour is the maths, not the comment. The port copied the **maths** -- so it matches what your app actually does, not what the comment claims. Nothing is broken; the comment is just wrong, and worth a look if you ever tune those numbers. Your two decisions are still open: the login token, and Subliminals' separate settings file.

- **LANDED (wave 50, SP-110): the first module that asks you something.** Every module before this was passive -- it drew, tinted, paced, ramped or played, and never wanted anything from you. **Lock Card** puts a real window in front of you that takes your keyboard and waits for an answer. That is **eight of the fifteen modules**, and it unblocks two more that were waiting on the same missing piece. **Floor 1589 -> 1648 unit and 100 -> 104 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** **No human has pressed a key.** Every keystroke in the tests is injected by software, which is not the same as a real one -- Windows itself treats them differently. Nothing here proves the card is legible, or that the words on it are the right words: the check counts coloured pixels, it does not read them. Linux is refused outright, in type, with the manual steps named. **Seven of fifteen modules still do not exist**, and there is still no video, no webcam and no headed capture anywhere in this port.
- **OWNER: one safety decision made for you, and one risk you should know.** The card can **always** be closed with Escape. Your Windows app makes that conditional on a panic hook being installed, and reading it the other way would have shipped a full-screen window holding your keyboard that you could not dismiss -- so the port takes the escapable reading, and that is now a standing rule: **this port will never ship a window you cannot get out of.** The risk: to take the keyboard reliably the port uses a Windows call your app does not, and if another program's window is hung at that moment the failure is not a polite refusal -- it is **this app freezing with a card half-drawn**. Bounded three ways, measured by nothing, and filed as the next thing worth testing.

- **LANDED (wave 51, SP-111): video.** **Mandatory Video** plays. That is **nine of the fifteen modules**, and it unblocks Bubble Count. **Floor 1648 -> 1742 unit and 104 -> 107 headless, 0 warnings, 0 errors.** The port did not settle for "the file decoded" -- that proves a file is readable and nothing else. It asks Windows for the picture back: it reads the pixels **out of the window's own copy** and checks they match the frame that was handed over, cross-checks with a Windows call the app itself never uses, and then reads the **actual desktop** and confirms it changes from frame to frame. The check that makes it real: hand the same picture twice and the reading does **not** move.
- **DOES NOT PROVE.** **Nobody has watched anything**, and no automated test can confirm that. **A window read-back is not a monitor** -- proven, not assumed: a window pushed 8000 pixels off-screen still reads back a perfect picture. Nothing measures speed, order or timing, so a clip playing at half speed or backwards would pass every check here. And **your video folder `Z:\CCP Vids` does not exist on this machine**, so every test ran against a video the test suite writes itself -- which is fine for proving the picture reaches the screen, but means **no real file of yours has ever been opened**. Six of fifteen modules still do not exist; no webcam, no headed capture.
- **OWNER: one cheap thing would close a real gap.** If you put a single ordinary compressed video where the port can reach it, that closes both remaining video unknowns at once -- whether real formats open, and whether the decoder's conversion path works. Everything else about video is already proven as far as this machine allows. Your two decisions are still open: the login token for premium features, and Subliminals' separate settings file.

- **LANDED (wave 52, SP-112): Bubble Count — TEN of the fifteen modules.** The first module built on machinery it did not design: it uses the video capability and the keyboard-window capability that the two previous waves built for other modules. **The question was whether those still fit when someone else uses them, and they did** -- the keyboard capability needed **no changes at all**, and video needed one addition that fixed a bug in the *first* module (it was leaking a video decoder every time a clip replaced another). **Floor 1742 -> 1830 unit and 107 -> 112 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** Nobody has watched or counted bubbles; no headed capture exists anywhere in this port. Nothing measures speed or ordering, so a game running at half speed would pass every check. **Five of fifteen modules still do not exist**, and there is no webcam.
- **OWNER: the most useful result is a refusal.** **Bubble Pop cannot be built yet, and now we know exactly why** -- not "it's hard", but a census: the keyboard-window capability handles five kinds of Windows message and **none of them is a mouse message**; it owns one window, places it once with no way to move it, and *requires* holding the keyboard, which is the opposite of what a floating bubble needs. So Bubble Pop needs a **new capability**, not a reuse -- that is a wave of work, and it is now scoped rather than guessed. Your two decisions are still open: the login token for premium features, and Subliminals' separate settings file.

- **LANDED (wave 53, SP-113): Bubble Pop — ELEVEN of the fifteen modules.** Bubbles float and pop when you click them. This was the hardest thing the port has had to prove: **a window that takes your click but must never steal your keyboard.** Every earlier proof leaned on "our window is in front and has focus" -- this one cannot, because a bubble that stole focus would interrupt whatever you were doing. So it proves the click arrived at that exact bubble while the foreground window **did not change at all**, and it proves it the honest way: a decoy window is put in the click's path, and when the bubble moves away the decoy catches it. A test that can only ever pass proves nothing; this one can fail. **Floor 1830 -> 1930 unit and 112 -> 117 headless, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** **No human has clicked anything.** Every click is injected by software, which Windows itself treats differently from a real one. Nothing proves a bubble is big enough or visible enough to actually aim at. **Four modules still do not exist**, and there is no webcam and still no headed capture.
- **OWNER: two process gaps found, and they are worth more than the module.** First: **nothing in this port ever checked compiler warnings mechanically.** Every wave has reported "0 warnings" based on a lane reading its own output -- and this one discovered its own filter was silently hiding warnings, so four of those reports were filtered rather than observed. Now filed. Second, and the reason to trust the rest: a sweep technique that had quietly approved **four wrong conclusions across three waves** was diagnosed and a rule adopted to stop it. Both are recorded rather than patched over. Your two decisions are still open: the login token, and Subliminals' separate settings file.

> **RETROACTIVE NOTE, added 2026-08-20 at the SP-114 land.** Every 0-warnings claim in the
> entries **above this line** (31 occurrences) was made before any gate existed, and **two
> independent mechanisms are now proven that could each have produced a false green**: a lane
> filter that could not match the warning class it was hiding, and MSBuild skipping `CoreCompile`
> on a rebuild so a plain `dotnet build` prints 0 warnings over a tree that still holds one. The
> second means those claims were **unobservable by construction on any rebuild**, however
> carefully anyone read the output.
>
> **Treat them as UNPROVEN rather than false.** Re-running them proves nothing, because those
> trees no longer exist as build states. **What IS proven:** a genuine cold build of the tree at
> `4332b8b9` - real restore, all four projects, log read whole - reports **0 warnings / 0 errors**,
> so no live warning survived into the current tree. The doubt is about the honesty of the past
> evidence, not the state of the code. Entries below this line are gated mechanically.

- **LANDED (wave 55, SP-115): Bouncing Text -- TWELVE of the fifteen modules.** Text that drifts across the screen with see-through edges. This one was **blocked since wave 46 and nobody faked it**: the port's overlay can only fade a whole rectangle uniformly, and at the shipped default it would have turned your desktop black. So a new surface was built that carries real per-pixel transparency. **Floor 1938 -> 2062 unit and 117 -> 121 headless, 0 warnings, 0 errors -- and the warnings are now MEASURED, not assumed.**
- **DOES NOT PROVE.** **Nobody has watched a word move.** Nothing measures speed or direction, so text drifting backwards would pass every check. Only **two** transparency settings -- full and half -- have ever been checked against an actual screen; every other setting is arithmetic nobody has looked at. **Three modules still do not exist**, and there is no webcam.
- **OWNER: one thing found that affects every wave, including the ones already landed.** The test suite is **intermittently red on the unchanged tree** -- a lane checked out the previous commit, ran it seven times, and it failed once on a test nothing had touched. Three separate strands of this now exist and they do not share an explanation. **It means every "three green runs" claim in this log is a weaker statement than it reads.** Nothing was retried or hidden to get around it; it is filed as its own task. Your two decisions are still open: the login token, and Subliminals' separate settings file.

- **LANDED (wave 56, SP-116): the test suite's random failures are explained.** No new module. For months the suite would occasionally fail on a machine where nothing had changed, and two earlier investigations both guessed the same wrong cause -- that another window was covering ours. **It wasn't.** On every single failure the window owned its own screen position and drew its own colour, with the identical background behind it. The real cause: the test read the screen **without waiting for Windows to finish drawing it**. Adding one ordering call fixed it: **34 failures in 1200 reads before, 0 in 1500 after.** A second, unrelated cause was a test reaching around its own code. **Floor 2062 -> 2067, 0 warnings, 0 errors.**
- **DOES NOT PROVE.** **The failure rate is bounded, not zero** -- 0 in 1500 means "below 0.2%", not "never". Two things stay unexplained and are written down as unexplained: an earlier run saw one of these failures seven times more often than this investigation could reproduce five days later on the same machine, and the evidence that would settle it was never captured. **So "three test runs passed" still is not proof**, and this log keeps saying so on purpose.
- **OWNER: nothing needed. One remark about how this was done.** The investigation wrote down its measurement plan **before taking any measurements**, including how many runs it would need, and committed it first. Then it reported the numbers its plan demanded rather than the ones that flattered the result -- including that its own first attempt at a fix **introduced a new failure of exactly the kind it was investigating**, which its own test gate caught. Your two decisions are still open: the login token, and Subliminals' separate settings file.

- **LANDED (wave 57, SP-117): Visuals -- THIRTEEN of the fifteen rack rows, and a bigger finding than the module.** Visuals turned out to have **no code of its own at all** in your app: it is a settings page whose five sliders are the **Flash Images** module's own draw dials, and the port's overlay was already accepting size, opacity and duration as parameters -- it was handing them constants. Three of the five now work. **The two dials that do not ship are named on the panel with the reason**, and one of them (`FadeDuration`) is **a dead dial in YOUR app**: every reference is a writer, and the fade a user actually sees is a fixed constant in the code. **Floor 2067 -> 2123, 0 warnings, 0 errors, and a 60-mutation sweep with zero survivors.**
- **THE FINDING: the session effect spine is COMPLETE -- and the product is NOT at parity.** The two remaining rows are **not effect modules at all**. **Scheduler** starts and stops the whole engine from a timer that runs when nothing is running, so it drives the session rather than being part of one. **Haptics** is app-wide and never started by the engine. So "thirteen of fifteen" was never the right way to count the spine -- **but do not read that as "it behaves like the Windows app", because it does not.**
- **OWNER: the honest gap, and one bug in your app.** Haptics is not just a missing row -- **it is a device your ported modules would drive if it existed.** In your Windows app, Flash Images reaches for it in four places, Mandatory Video in three and Subliminals in one: **eight in three modules that ARE ported here, and every one is silent.** So if you plug in a toy today, nothing this port runs will touch it. That is now written down as its own item rather than buried. **And a bug found in your app while surveying it:** a code comment in the Studio rack cites a dashboard card that does not exist -- it points into an unrelated dialog method. Harmless, but wrong, and worth a look if you ever chase it. **Your two decisions are still open:** the login token, and Subliminals' separate settings file.

- **LANDED (wave 58, SP-118): the Scheduler -- FOURTEEN of the fifteen rack rows.** It starts and stops a session on a day-and-time window, and it is the first thing this port has built that **runs the engine rather than running inside it**. **Floor 2123 -> 2191, 0 warnings, 0 errors, 73 mutations with one honestly-uncovered survivor.** Because a defect here would not spoil an effect but **start a session you did not ask for**, the work was aimed at the refusals rather than the trigger: it will not start outside its window, will not start one while one is running, will not restart one you stopped by hand, and does nothing at all when switched off.
- **ONE THING YOU SHOULD DECIDE, and it is deliberate.** On a **daylight-saving fall-back night the 02:00-03:00 hour happens twice** -- so a window inside it opens twice, and **your app starts a second session that night.** The port copies that exactly rather than correcting it, because a correction would start sessions at moments your Windows app does not, and that is your call rather than mine. It is written down as a test named for it, so nobody changes it by accident. Both starts are inside the window you configured; a start at a time you never chose remains impossible.
- **OWNER: two bugs found in YOUR app while porting this one.** `CheckSchedulerAfterSettingsChange` can never run -- its condition is always false. And the time boxes use a parser that is **not a time-of-day parser**: type `8` and it means **eight days**, which silently becomes a 00:00-22:00 window. Neither is fatal, both are real. **And one found in the port and fixed:** a scheduled tick could start a session while the app was already shutting down. Your two decisions are still open: the login token, and Subliminals' separate settings file.

- **LANDED (wave 59, SP-119): the haptics seam -- FIFTEEN of the fifteen rack rows.** The last row is ported, and it is the one where "ported" and "working" are furthest apart, so read this entry rather than the count. **Floor 2191 -> 2247 unit and 133 -> 141 headless, 0 warnings, 0 errors, 64 mutations with one honestly-uncovered survivor.** The row exists, its dot is truthful, the premium gate is real, and the whole thing is wired to shut down first when the app closes. **What it does when you switch it on is refuse -- and the refusal is the deliverable**, because it names the actual gap: **no provider is admitted in this build.** Not "no device found", which would have been a lie -- this port has no client to look with.
- **DOES NOT PROVE -- and this is the plainest "does not prove" in the log. Nothing has ever moved.** No device, no server, no client, no wire. Every measurement in this wave stops at a refusal, a control in a window, or a document. **Even with a toy plugged in and a server running, nothing would happen**, because the thirteen places your app drives a toy from are still silent here and the panel says so before you touch anything. And the last rung cannot be climbed by software at all: a haptics server reports what it *believes* it sent over Bluetooth, so **"you felt it" is a human check, permanently** -- the three rungs below it that machines *can* check are kept separate on purpose, so that limit never becomes an excuse.
- **OWNER: one decision, and it is smaller than it looked.** The blocker was recorded as "haptics needs a NuGet package". **That is only half true, and the half matters:** **Lovense needs no package at all** -- it is plain HTTP to a local address, one file plus a keep-alive, using only what .NET already ships. **Buttplug is the one that needs the package** (one reference, one file), and its .NET 10 compatibility and licence were **read out of your source rather than checked against the actual package**, so that one needs a five-minute check first and the other three options are answerable today. **Right now the port admits nothing, which is why it refuses.** Also found in your app: **your three haptic gates disagree with each other** -- the checkbox asks one question, the mixer and the premium rail ask two, so on a day your server names haptics as the free feature, **the rail unlocks and the mixer opens while the checkbox still says no.** Written down rather than "fixed", because making them agree is your call. **And a correction to what this log told you two waves ago:** it said your modules drive a toy from **eight** places. The real count is **thirteen**, and one of the paths quoted did not exist. Your two decisions are still open: the login token, and Subliminals' separate settings file.

- **LANDED (wave 60, SP-120 + SP-121): no new feature — two instruments, and one of them immediately found a bug of a kind that has bitten twice before.** **Floor 2247 -> 2270 unit, 141 headless unchanged, 0 warnings, 0 errors.** Neither packet wrote a line of product code, on purpose. The first mapped every place your Windows app tells a toy to buzz, against every place this port could do the same. The second answers a question nothing here could previously answer: **which shipped classes have never had a single line run by any test.** The answer is **42 of 649**, published as a document anyone can regenerate with one command.
- **THE FIND: `SystemSoundClock` has no crash guard, and its two sibling classes both have one and both explain why.** Their own comments say it: if an error escapes that callback, **.NET shuts the whole app down**. It is the default on three paths in the port. This is the **third** time this exact shape has turned up — the first killed the test runner, the second could start a session while the app was shutting down — and the first two were both found by luck, when somebody happened to write a test. **This one was found by a machine, which is the entire point of building the thing.** Two smaller finds came with it: a file-loading helper that promises "never throws" over a call that does throw and that no test has ever run, and a block of DTRH startup code where five dead things share one cause.
- **DOES NOT PROVE — and this wave proves less than usual, deliberately.** **Nothing was executed, drawn, composited or heard.** No haptics work; no toy will buzz. The port still cannot vibrate anything, and the mapping is a document, not wiring. "Never executed" is also not the same as "broken", and the census says what it cannot see: about 235 classes never enter the count at all, a dead half of a live class is invisible, and it ran on Windows only — so nothing here says whether the Linux paths work.
- **OWNER: one decision is now cheaper to make, and one thing found in YOUR app.** The haptics architecture question is priced rather than argued: four options with costs, one recommended, **none taken** — that choice is yours. And in your Windows app: **panic-keying a video leaves the toy running.** The stop lives in one teardown path; the panic key, the session lock and the suspend path each go somewhere else entirely. Worse, **there is a comment in that same block claiming its stop "runs on every teardown path ... because CloseAll is the single funnel"** — a written guarantee the code does not keep, which is exactly why nobody would go looking. (Precisely: that comment documents the script-player stop that sits two lines below, not the layer stop itself; both sit AFTER the call it names as the funnel. The divergence ledger states it exactly, and this summary said it loosely until a blind audit corrected it.) It has probably never been reported because at your default settings the level left running is very faint. Your three decisions are still open: the login token, Subliminals' settings file, and the haptic dependency.

- **LANDED (wave 61, SP-122 + SP-123): somebody has looked at the rack, and the three bugs the last wave's instrument found are fixed.** **Floor 2270 -> 2309 unit, 141 -> 144 headless, 0 warnings, 0 errors.** For the first time in fifteen modules, a real screenshot of the real app was taken on a real desktop and checked against the colours the app is supposed to paint: a row open, a row closed, and its little state dot both on and off. Every check was proved to **fail** by pointing it at a picture of the wrong state, so none of them is a check that can only pass. And `SystemSoundClock` — the one flagged last wave as missing a crash guard both its siblings have — now has it, along with a second missing guard nobody had noticed.
- **THE MOST USEFUL RESULT WAS A REFUSAL.** The job as written told the lane to add a small piece of code to the app so the rack could be photographed. **It measured, found the app already exposes everything needed, and refused** — and then showed the change would have pushed five of the fifteen rows off the bottom of the list it was meant to photograph. So no product code changed at all. It also found **four faults in the screenshot tool itself**, all pre-existing: it never took the machine-wide lock that stops two runs fighting over the screen, it read the screen without waiting for Windows to finish drawing, it never reported success or failure properly, and its "clean slate" cleared only half the saved settings — so a picture of the *off* state leaked into the next picture of the *on* state. That last one has been wrong since March's work and nobody knew.
- **DOES NOT PROVE — and the biggest one is unchanged.** **No human has compared this to your Windows app.** The checks confirm the app painted the colours it was told to paint, in the places it was told to paint them; whether that *looks right* next to the real thing is still a job for eyes, and it is written down as still owed. It is one row of fifteen, one machine, one screen scale, colour and position only — not fonts, not animation, not scrolling, and nothing on Linux. And haptics still moves nothing: these fixes make the code safer, not the toys busier.
- **OWNER: nothing needed, and one pattern worth knowing.** Almost everything found this wave was **a test that passed for the wrong reason**. A lock-check that stayed green when the lock was deleted outright. A dot-check whose whole discriminating power was one number nobody had pinned. A clock test that watched an unrelated timer. Six of these, in two jobs whose entire purpose was checking things — and two of them are sitting in tests that shipped two waves ago. They are all now either fixed or written down. **The lesson being recorded: a passing test is a claim about the test, not about the code**, and this project keeps having to learn it. Your three decisions are still open: the login token, Subliminals' settings file, and the haptic dependency.

- **LANDED (wave 62, SP-124 + SP-125): the thing blocking all future feature work is gone, and For You Feed has been scoped — the answer is that you have to decide something first.** **Floor 2309 -> 2332 unit, 144 headless, 0 warnings, 0 errors.** Neither job wrote product code. The first removed a self-inflicted roadblock: a check added two waves ago compared a live count against a number written in a document, so **adding a single new class anywhere in the app broke the test suite**, and the only fix was editing a file the jobs aren't allowed to edit. Every remaining feature needs new classes, so nothing could be built until this was fixed. It is, and the check it replaced is stronger rather than weaker — proved by deliberately breaking the underlying reader three different ways and confirming it still catches each.
- **THE FIND: For You Feed cannot be built without decisions only you can make, and the census says why with an inventory rather than an estimate.** Three things came out of reading your code. **(1) It uses the webcam.** The gaze-scrolling in that feature's own description is real: it starts the camera, checks consent, and follows your eyes. Webcam is not something this port has, and your own written rules say expanding sensors needs a consent-contract revision and your review. **(2) It fetches content from an outside service** — `api.scrolller.com`, twelve adult categories, weighted by what you linger on. So what you look at shapes what gets asked for. **(3) Agreeing to that inside the feed also unlocks remote content in five other places.** None of this is broken; all of it is yours to decide.
- **DOES NOT PROVE.** No feature was built and nothing was rendered, played or captured. The census read source code and directories — it did not run For You Feed, and it cannot tell you the reading is right, only that it matches what is in the files today. Nothing was tested on Linux. And the board row describing this feature was wrong in a way worth knowing: it said three files where there are nine, and **42% of what it points at is shared plumbing four other features use**, not part of the feed at all.
- **OWNER: one thing found in YOUR app that will save whoever builds this.** Ghost mode is not a transparency setting. Your code parks the real browser window off-screen and paints a live mirror of it that you can see through and click through — and there is a note beside it recording that the obvious approach was **tried and shipped and failed**: making that window transparent turns its contents solid black. That note is worth more than the code around it. Also: the smallest useful piece of this feature is **remote media on its own** — five files, no Windows-specific code, no webcam — which two features you already have would use. Your three older decisions are still open: the login token, Subliminals' settings file, and the haptic dependency.

- **LANDED (wave 63, SP-126 + SP-127): the modules finally talk to the toy — and nothing moves yet, on purpose.** **Floor 2332 -> 2399, 0 warnings, 0 errors.** This is the first wave in five that ships product behaviour rather than tools or scoping. Flash images, video, subliminals and bouncing text now command the haptic system at the same moments your Windows app does, with **your app's own numbers** — the eight-step fade-out at its real 3.5-second length rather than the "about 2 seconds" its own comment claims, the video background hum at its floored minimum, the subliminal pulse timed off the card's own wording. **Nothing buzzes**, because no device driver is admitted into the build yet, and that is the one decision still yours. When you admit one, this is what makes it work.
- **THE PORT'S STOP IS NOW BETTER THAN YOURS, DELIBERATELY.** In your app the video haptic stop lives on one shutdown path only — **panic-key a video and the toy keeps going.** A comment right beside that stop claims it runs on every path, which is why nobody would go looking. The port puts its stop on both paths, with a test that exercises both in one session so neither can be quietly deleted. Also found and fixed before landing: the test that claimed the shutdown happened *in the right order* did not actually check the order — reversing it left everything green. It now proves it by **attempting the dangerous thing and being refused**.
- **DOES NOT PROVE.** No device moved and nobody felt anything; there is no driver in the build, so the commands stop at a refusal. Nothing was rendered, played or captured. Linux is untouched and unproven. And the second job this wave built nothing at all — it scoped the Trainer Card and answered with an inventory.
- **OWNER: one real bug in YOUR app, and two things worth knowing.** **`honor_roll` can fire early and never un-fire.** It counts distinct categories, but the two places that report a finished quiz disagree about capitalisation — one sends `sissy`, the other `Sissy` — and the counter treats those as two. The award then unlocks a category short, permanently, because the list is saved. It is currently unreachable in the shipping UI (that launcher is hidden), so it is a trap rather than an active fault, and the fix is one comparer. Second: **the Trainer Card is buildable in part** — the graded-run award path is 34 lines with no assets and no decisions needed, while the wardrobe and achievement ledger are not. Third, a correction to something I told you earlier: the leaderboard sends **an identifier, a display name and a version — not a score upload**; where scores are submitted is a question that census did not answer and did not guess at. Your four decisions are still open: the login token, Subliminals' settings file, the haptic driver, and For You Feed's webcam-and-third-party question.

- **LANDED (wave 64, SP-128 + SP-129): the quiz awards work — and the last unmeasured feature turns out to be one you already built twice.** **Floor 2399 -> 2457, 0 warnings, 0 errors.** Finishing a graded intake now unlocks the same achievements at the same thresholds your Windows app uses: top marks at 90%, the distinct-category award over three categories, and the fail-streak one deliberately left unwired because an intake has no fail state. **It also refuses to copy your app's bug** — the casing defect I reported last wave — and that refusal is written down as a deliberate difference rather than a silent fix.
- **THE FIND: Goon Game exists twice, in two languages, and the duel people actually play is the JavaScript one.** The C# under `Services/GoonGame/` describes itself as the reference implementation and the web page as a second client. So a job told to "port the Goon Game service" would have rebuilt a game your payload already contains. Of those 25 files, **only five sit behind the door a user can open** — twenty are reachable only from developer test switches. **The buildable piece needs none of them as code:** serve the existing web files, answer two messages, and a person plays a complete duel against the scripted opponent, with no network, no microphone, no camera and no login.
- **DOES NOT PROVE.** Nothing was rendered, played, captured or clicked, and your Windows app was never launched. The award path reaches a saved file, not a screen — nothing in this port can display it yet, so today the visible result is opening a JSON file. Nobody has loaded the Goon page in the port's browser; "buildable" means the pieces exist and ship, not that it draws. Linux is unproven on every row, and so is Windows at runtime.
- **OWNER: six decisions on one feature, and a problem with your task board.** Goon Game would open **the first outbound network connection this port has ever had**, and it is peer-to-peer: with no relay server, the two players connect directly, so **each one learns the other's IP address** — your app never says this anywhere, so the census had to work it out — and the join link is free with no account. A second channel sends **your own photos and videos to the other person**, not to a service. Two of its eight endpoints are report-and-block, which is moderation, and your 2026-08-14 approval explicitly left moderation untouched. Nothing was decided; it is all described and priced at nothing. **Separately: your task board was not rendering.** 27 859 characters of acceptance notes across 17 rows were being silently dropped by the markdown table format, the worst row losing 7 480 — invisible, because malformed markdown does not complain. Repaired this wave, with the text proved identical afterwards. Your earlier decisions are still open: the login token, Subliminals' settings file, the haptic driver, and For You Feed's webcam-and-third-party question.

- **LANDED (wave 65, SP-130 + SP-131): the Goon practice door exists — and nobody has opened it yet.** **Floor 2457 -> 2547, 0 warnings, 0 errors.** Your Goon Game page now ships inside the port and is served by it: 184 files, byte-for-byte identical to your originals, none copied — they are linked straight out of your WPF tree. The port answers the two messages the page needs to start, and the four things it cannot do yet refuse honestly instead of pretending. **None of your 25 Goon C# files was ported**, because the previous wave found the game exists twice and the version people actually play is the JavaScript one. **What has not happened: nobody has loaded the page.** Showing that window starts a real embedded browser, which no test here can do, so a played duel is owed to someone sitting at the machine and clicking. The board row says exactly that and is deliberately not marked done.
- **THE FIND: your Goon page tells people things that are untrue when it runs inside this build.** With the port's settings, three of its four locked doors say a **purchase** would open them, and the fourth says the page is running in a plain browser — while it is running inside your app. There is no way for the app to replace those sentences, so the honest explanation now sits in the window's own chrome around the page. **A related correction found at the last moment:** the port was telling users "voice notes are not in this build" and "this build opens no capture device of any kind". The first is false and the second is only half true — the voice screen is reachable, its recorder asks the browser for the microphone itself, and the app can neither grant nor block that. Reworded before landing, and the test that had been enforcing the overstatement now enforces the honest version too.
- **DOES NOT PROVE.** Nothing was rendered, played, captured or clicked, and your WPF app was never launched. The page has never been loaded, so nothing is known about whether it draws, whether the handshake completes, or whether a duel works. Linux is untouched. The second job this wave built no product at all — it taught your citation checker to notice when a quoted line moves, which it now does, though it can only watch one file so far and cannot see its own tooling.
- **OWNER: six decisions on Goon Game, still none of them made, and none of them urgent.** Playing against another person would open **the first outbound network connection this port has ever had**, and it is direct machine-to-machine: with no relay in the middle, **each player learns the other's IP address**, and the join link needs no account. A second channel sends **your own photos and videos to the other person** rather than to a service. Two of its eight endpoints are report-and-block, which is moderation — something your 2026-08-14 approval specifically left out. Practice mode needs none of that, which is why it is what got built. Your earlier decisions are still open: the login token, Subliminals' settings file, the haptic driver, and For You Feed's webcam-and-third-party question.

- **LANDED (wave 66, SP-132 + SP-133): somebody finally opened the Goon door — and the page was broken.** **Floor 2547 -> 2573, 0 warnings, 0 errors.** The practice page now genuinely loads inside the port and completes its startup handshake, proved by reading the page's own state back out of the browser rather than trusting that the app sent the right messages. **The moment it loaded, it found twelve missing files** — six sound effects and six images the page pulls from your Down the Rabbit Hole folder, all returning "not found", so the bubbles, the effect icons, the pop and the win chime were silently absent. The page had been reporting this the whole time; nobody had ever been there to hear it. Fixed by letting the Goon page reach those shared files, the same way your intake page already does.
- **THE TRADE THAT BUYS IT, stated at its real size.** To reach those twelve files the Goon page can now also reach **1539 other files** in that shared folder — measured, not estimated, and pinned by a test whose failure message says outright that a change means *the reachable set moved*, not *update the number*. Your own photos and videos are **not** affected: they stay on a separate address and port, and nothing was opened there. One line reverts the whole thing if you would rather have the twelve broken.
- **DOES NOT PROVE.** **Nobody has played a duel** — that needs a person clicking, and no test here can do it. The plain title menu was never photographed, only the first-run explainer over it. Linux is untouched. The twelve restored files are proved to be *delivered*, not to be *heard*: whether the pop actually sounds is still a human check. The second job built no product at all — it put a checking tool's own logic under test, which proves the checks run, not that they are right.
- **OWNER: one thing about your machine, and it affects every future run.** **The test suite cannot be trusted while the Conditioning Control Panel is open.** Nine full runs during this land: three passed, six failed, and every failure was a test that needs sole control of the screen. Your app was in the foreground, and it re-asserts always-on-top on a timer — which your own test harness documents, by name, as something it cannot work around. The tests pass perfectly when run alone. **This is not a bug in the port and not something to paper over by ignoring those tests** — it means an unattended run cannot certify itself while you are using the product. Your earlier decisions are still open: the login token, Subliminals' settings file, the haptic driver, For You Feed's webcam question, and the six Goon Game network questions.
