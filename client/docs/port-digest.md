# Port digest — owner's read-first log

Three lines per landed wave, newest last. Written by the phase that lands, so an unattended
run cannot bury named limits and owner questions inside ten records nobody opens.

Format:
- **LANDED:** what reached the tree.
- **DOES NOT PROVE:** the honest residual — the limit a reader would otherwise assume away.
- **OWNER:** the question or decision it raised, or `none`.

---

## Wave 19 — SP-062 (`7518c6a4`, 2026-08-12)

- **LANDED:** the SP-057 profile-isolation pin can no longer pass vacuously — loud `Assert.SkipWhen` at both checkpoints plus a probe-proven co-location fix for the process-env leak (`DisableParallelization` measurably does not serialize on this runner). Floor 892/35.
- **DOES NOT PROVE:** that the suite's signal is trustworthy in general. The tripwire fires only for an externally set override; in-suite leaks are prevented, not detected; a skip does not fail the contract gate, so detection still depends on a human comparing counts. The generalized vacuous-test class (conditional `return` shapes) was never swept — new P1 row filed.
- **OWNER:** ratification of the row. Also worth knowing: the wave-18 land shipped a RED base (an inventory disposition flipped without the field its guard requires) — found and repaired in-lane, lesson recorded.

## Wave 20 — SP-063 (`10c37650`, 2026-08-12)

- **LANDED:** your decree — injected test timeout budgets raised to one shared finite constant (`TestWait.InjectedBudget = 60 s`, ~75×), the two timeout-SUBJECT tests kept short and pinned, three inert budgets deleted, and one guard token added with a captured RED so the shape cannot return silently. 10 greens incl. a cold first-ever build; floor unchanged.
- **DOES NOT PROVE:** that the cold-start class is gone. A bigger number lengthens the fuse; it does not remove the time dependence. The class returns on a machine ~75× slower or if the constant is lowered, and the guard catches the option-assignment shape only (not a constant, a computed `TimeSpan`, or a method-argument bound). The deterministic alternative was set aside by decree, not refuted.
- **OWNER:** ratification. No new question.

## Wave 21 — SP-064 launched (`9d5f92c6`, 2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing product-side. Wave 21 was authored and its batch launched: SP-064 makes harness-only entry points refuse to start when `CCP_DATA_ROOT` is unset, so the SP-057 seam stops depending on someone remembering it. **Counts at this commit are unchanged: 892 unit / 35 headless / 0 skipped, 0W/0E** — this phase touched only packet and doc files.
- **DOES NOT PROVE:** anything about the gate; the worker has not run yet. Two things are already known limits of the design you approved: the row's own decree keeps demo flags exempt, so `--dtrh-demo --dtrh-auto-close 30` remains an unattended evidence-shaped run against your real profile, and the gate protects the data root only — WebView2/LibVLC writes under `%LOCALAPPDATA%`/`%TEMP%` were outside SP-057's claim and stay outside this one.
- **OWNER:** two questions. (1) The demo+auto-close hole above — close it in a follow-up row, or accept it as the price of keeping demo flags human-usable? (2) **Your new blind audit runs `dotnet build` + both test projects in the MAIN checkout whenever a phase moves HEAD — including the authoring phase that just launched a batch, so the audit and the lane build/test concurrently.** It should be sound (worktrees have their own obj/bin), but a flaky or port-contended run now writes `.spine/STOP` with a batch in flight. Consider skipping the audit when `spine status` is executing, or auditing after the land phase only.


## Wave 21 — SP-064 (`e8eab7c1`, 2026-08-13)

- **LANDED:** harness-only launches now refuse to start against your real profile. `--dtrh-m2test`, `--dtrh-fx-drive`, `--intake-drive`, `--loom-drive`, `--tunnel-drive` and the harness-only failure injectors exit 3 naming `CCP_DATA_ROOT` when it is unset, gated in the real `Program.Main` before anything can write. One registry holds the classification, a guard test fails the build if any future `"--flag"` in `client/src` is left unclassified, and the refusal was proven with real processes: the profile is BYTE-IDENTICAL across 2677 files after a refused run, while a plain unsealed launch still opens a window and exits 0. Floor 892 → **897 unit / 35 headless / 0 skipped**.
- **DOES NOT PROVE:** that evidence runs can no longer touch your profile. By your row's own decree demo flags stay exempt, so `--dtrh-demo --dtrh-auto-close 30` is still an unattended, evidence-shaped run against the real profile. Only the data root is protected — WebView2, LibVLC and `%TEMP%` writes were never in SP-057's claim and are not in this one. The guard sees `"--..."` string literals in `client/src/**/*.cs` only, so a concatenated or interpolated flag evades it, as does any harness path that does not go through `Program.Main`. Linux is unproven: zero WSL distros on this machine, and no Linux run was faked. **This is a deliberate behavior break** — every headed evidence script must now set `CCP_DATA_ROOT` or it will exit 3.
- **OWNER:** three. (1) The demo+auto-close hole — close it in a follow-up row, or accept it as the price of keeping demo flags human-usable? (2) **"TRX attached" has never meant "TRX committed"** — `*.trx` is gitignored repo-wide, so every wave's TRX has lived only in a scratch worktree and was deleted with it. SP-064's 8 files were copied out and hashed before the clean that the merge required, but this is a standing gap in the evidence chain: do you want a `.gitignore` negation filed so evidence TRX is committed from now on? (3) The batch reported **`failed`** even though SP-064 passed contract, code review and final review — the failure was a dirty-worktree check, and spine's suggested fix (`git rm -r --cached`) would have deleted 117 force-added tracked files including the loop script itself. It was not run; the correct recovery is recorded. Worth knowing that the headline remediation in `spine status --diagnose` is actively dangerous in this repo.

## Wave 22 — SP-065 launched (2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing product-side. Wave 22 was authored and its batch launched. SP-065 replaces the honour-system half of your test discipline with machinery: today `dotnet test` exits **0** on `891 passed / 1 skipped`, so "an unexpected skip is a red" and "the exact count is the floor" are enforced only when a human reads two numbers off a console — the same path that let the wave-18 land ship a red base. The packet delivers a wrapper that owns both test invocations and **fails the contract** on an unexpected skip or an off-floor count, proven both ways (induced skip → red, clean run → green). **Counts at this commit are unchanged: 897 unit / 35 headless / 0 skipped, 0W/0E** — this phase touched only packet and doc files. This is board row 49 **part (2) only**; part (1), the sweep for tests that silently assert nothing, stays claimable exactly as the row asked.
- **DOES NOT PROVE:** anything about the mechanism — the worker has not run yet. Three limits are already known by design. It will detect that the number moved but **not which test vanished**. It cannot judge whether the pinned number is the *right* number: a pin bumped in the same commit as a bad or vacuous test is blessed by it, so this upgrades "a human must compare numbers" to "a human must justify a bump" — better, still not verification. And it binds only runs that go through the wrapper, so a bare `dotnet test` typed by hand still exits 0 on a skip.
- **OWNER:** one new question, plus one standing obligation I am recording against myself. **Question:** the consult route is degrading as an instrument — three calls this phase, the first returned reasoning with no verdict, the second truncated mid-sentence, only the third came back whole; waves 17 and 21 hit the same thing. I worked around it by asking narrowly and capping reply length, and I verified every consult claim against the repo before encoding it (two of the three were about gitignore rules and both checked out). Do you want that treated as a named tooling row rather than absorbed silently each wave? **Obligation:** SP-065 deliberately does NOT edit the packet template — that is a policy file and stays mine to change at land. If I forget it, the next packet's lane goes red on its own contract by design, so the failure is loud rather than silent; it is written into the row and into CONTEXT so the next phase inherits it.

## Wave 22 — SP-065 LANDED (2026-08-13) — integrate `09b4b639`

- **LANDED:** the test floor is no longer enforced by a human reading two numbers. `node client/tests/floor/check-floor.mjs` now owns both test runs and **fails the build contract** on any unexpected skip, any off-floor count, a bad counter category, or a stale result file — it finds the test projects from the solution, so a new test project cannot quietly sit outside the floor, and it writes its TRX outside the worktree so it leaves no mess behind. Proven in both directions with captured output (induced skip → red, count drift either way → red, clean run → green), and a guard test now fails any future task packet that tries to run the tests the old bare way. New floor: **898 unit / 35 headless / 0 skipped, 0 warnings, 0 errors** (897 → 898 because the new guard adds one test). The land itself was clean — no recovery, unlike wave 21 — and I verified the merged tree through the new wrapper three times before approving, then proved the tree I tested was byte-identical to the tree that got pushed.
- **DOES NOT PROVE:** that the pinned number is the RIGHT number. The machine now forces you to justify a bump instead of forcing you to notice one — better, but it is not verification: a bad or empty test that is added and pinned in the same commit is blessed by this mechanism. It detects that the count moved, never **which** test vanished, so a commit that deletes one fact and adds another slips through unchanged. It binds only runs that go through the wrapper — your own keyboard is still unbound. It does nothing for the OTHER half of that board row: the sweep for tests that quietly assert nothing is **untouched and still claimable**. And it has **never run on Linux** (no WSL distro on this machine); portable-by-construction is not proven.
- **OWNER:** one flake filed by name and deliberately NOT fixed — `ChaosTunnelLoopbackTests.Logging_RouteClassesOnly_NeverFilenameOrQuery` went red once in fifteen full runs; it guards that tunnel logging never leaks a filename or query, so it must be fixed at the source, never weakened or quarantined. **Question for you:** the gate's own evidence has now been misleading five times running (this time a diff-stat that read as if the worker had deleted my instructions — it had not). I keep catching it because I re-verify the merged tree myself, which costs roughly ten minutes per land. Do you want me to keep paying that, or to spend one bounded task making the gate's evidence trustworthy so the re-verification can shrink?

## Wave 23 — SP-066 LANDED (2026-08-13) — integrate `29950e9b`

- **LANDED:** the other half of the test-honesty row. Every test in both projects that *could* silently assert nothing has now been looked at individually — 78 sites, each one carrying a written verdict and reason in a committed ledger, none left unjudged and none deleted. Five tests that used to hit a hidden `return` on the wrong OS (and were therefore counted as passes while doing nothing) now openly report themselves as skipped, and each is pinned in the floor by its full name, so an unexpected skip fails the build naming the offending test. Six more that could go empty at runtime got a check that they actually have something to loop over. A new guard test fails with `file:line` if anyone adds a test in one of these silencing shapes without judging it, and the blind auditor's instructions were fixed so it stops re-deriving the floor the old unreliable way. New floor **900 unit / 35 headless / 2 named skips, 0 warnings, 0 errors**. **The two skips are not a loosening** — those five tests were always non-executing on this OS; before, that was invisible and scored as green, and now it is named. No assertion was weakened, no tolerance widened, nothing quarantined, and no product code changed.
- **DOES NOT PROVE:** that the suite has no vacuous tests left. The detector reads source text, so it cannot see a test that runs a loop over an empty list at runtime and asserts nothing — that class is mitigated where it was found, not detected. The guard only binds the shapes it was taught; one more silencing shape (`Assert.All` over a possibly-empty collection, 21 uses) was **found during the sweep and deliberately left out of scope** rather than quietly ignored — it is filed as its own row. The permanent bans that stop the skip list becoming a quarantine list are **prose in a file, not code** — a follow-up row is filed to make the two most dangerous ones mechanical. The ledger's per-site reasons are human judgment nothing checks. The auditor half of T-17 is **not** finished: the instructions changed, but nobody has yet run the auditor against a deliberately broken tree to prove it now catches it, so that row stays open. And none of this has run on Linux — no WSL distro on this machine.
- **OWNER:** a real bug surfaced and I did not paper over it. `AsyncLifecycleTests.Heartbeat_SkipsProjectionUntilBound_ThenFlowsThroughBoundary` failed once during this work — a shutdown race where a cancelled heartbeat reports itself as completed. This is its **second** recorded occurrence (the first was back at SP-055) and both times it fired under a change that touches no lifecycle code, so it is a genuine race in the product's stop path, not test noise. Adding it to the new skip list would have made it disappear; that is exactly the abuse the rules ban, so it is filed as a P1 row to be reproduced and fixed at the source instead. **Recommendation, with the decision that is actually yours attached:** five rows now sit in the queue that exist only to make the *test suite* trustworthy. Those are not all equal, and I am not asking you to rank them from scratch. **The next wave should fix the StopAsync race regardless** — it is the one item on that list that is a defect in the *product's* shutdown path rather than scaffolding around the tests, it has now surfaced twice, and it is currently queued behind test tooling, which is the wrong order. I intend to do that unless you say otherwise. **The decision I do need from you is the ratio after that:** the remaining four suite-hardening rows (the `Assert.All` shape, the mechanical ban test, T-17's auditor run, the privacy flake) are all genuine but none is urgent, and every wave spent on them is a wave not spent on WPF parity. Tell me roughly how much of the run you want going to suite trustworthiness versus parity features, and I will schedule against that instead of asking again each wave.

## Wave 24 — SP-067 LANDED (2026-08-13) — integrate `75a09d61`

- **LANDED:** the shutdown bug is fixed at the source. When you stopped the app, the heartbeat could report itself as **"finished normally"** instead of **"stopped"** — the code checked for a stop request at the top of its loop, but if the stop arrived at that exact check rather than while it was sleeping, it fell out of the loop and reported success anyway. That is the intermittent failure that surfaced twice on its own (SP-055, then SP-066) under changes that touched nothing in this area. The fix is four lines: the exit now reports "stopped" when a stop was requested, matching what the status-ticker already did. **I did not take the worker's word for any of it.** On the exact tree that got pushed I rebuilt clean (0 warnings, 0 errors), ran the full suite **five times green** (903 unit / 35 headless / 2 expected skips), and re-ran the worker's 500-iteration reproduction myself: **500 out of 500 correct**. Then I did the part that actually matters — I **broke the code on purpose** to check the tests would catch it. Reverting the fix made the reproduction fail 498 times out of 500, and each of the three new regression tests fails when, and only when, its own piece of code is broken. So these are real guards, not decorations. New floor: **903 unit / 35 headless / 2 named skips, 0W/0E.**
- **DOES NOT PROVE:** that the original failing test can never fail again. My reproduction drives the *fastest* version of the bug (stop before the first beat), which is not the version that test hits — it stops *after* several beats. So neither the five green runs nor the 500 iterations re-measure that test's old "roughly 1 in 15" failure rate; I am closing this on **mechanism**, not on frequency: the only route by which it could report the wrong answer no longer exists. Worth knowing that the loop has no early exit, so in today's code the "finished normally" branch is now **unreachable** — that is why the new tests are reliable rather than timing-dependent, but it also means the fix is belt-and-braces rather than a knife-edge race repair. This closes that one mechanism only; the test can still fail for unrelated reasons like a timeout. And none of this ran on Linux — still zero WSL distros on this machine, as it has been for every wave.
- **OWNER:** one thing filed, and one decision I am going to stop asking you for. **Filed:** the same "reports finished when it was actually stopped" shape can arrive through a second door — if a capability probe swallows a cancellation, the registry records a probe verdict that never happened. It is a *different* mechanism, it was found by this wave's own sweep, and I deliberately did **not** fix it in the landing commit because that code would have gone in after my verification had already run, which is exactly how a bad wave shipped a broken base once before. It is a filed row with a "prove the test fails first" acceptance. **Decision:** I asked you last wave for the ratio of test-suite hardening to actual WPF parity work, and I am not going to hold the run waiting for an answer. **Default I am now proceeding on: back to WPF parity.** The reason is that the one item in that queue which was a genuine *product* defect is the one I just fixed; the four that remain (the `Assert.All` shape, the mechanical ban test, the auditor run, the privacy flake) are all scaffolding around the tests, all genuine, none urgent. I will take them only when one is small enough to ride along with parity work rather than spending a wave on it. **Say the word if you want it weighted differently** — otherwise this is the schedule. I will stop *asking* you to set the ratio, but I will raise one of those four rows again if evidence makes it urgent (a second occurrence of the privacy flake, say, or an audit that the auditor run would have caught). Silence keeps parity as the default; it does not mean I will ignore new evidence.

## Wave 25 — SP-068 launched (2026-08-13) — authoring phase, nothing landed yet

- **LANDED:** nothing yet — this phase authored the packet and launched the batch. It is the first wave under your back-to-parity default, and it takes the cheapest real parity available: three **subtractive** filters over code the port has already shipped. Each one *removes* data rather than adding any. (1) Window titles containing private/incognito markers are dropped before anything counts them. (2) Titles get scrubbed — email addresses, runs of six or more digits, and control characters stripped, then capped at 80 characters — using the exact rules and numbers the WPF app uses. (3) URLs the model invents are stripped out of the companion's replies before they can reach the chat bubble, the history, or the disk. These come from the Her Room audit that landed at wave 17, which found that the strongest things to adopt from upstream are the ones that *narrow* what flows. **Counts at this commit are unchanged: 903 unit / 35 headless / 2 expected skips, 0 warnings, 0 errors** — this phase touched only packet and planning files.
- **DOES NOT PROVE:** anything about the mechanism yet — the worker has not run. Two limits are known by design and are written into the packet so they cannot quietly disappear. **The first two filters guard a path nothing in the app currently drives**: the awareness title machinery is real, typed and wired into the moderation boundary, but today only the tests call it, so those two harden the boundary a future feature will inherit rather than change anything you can see. The third one is on a live path — your companion's replies. And this is **three rows of one audit table, selected by me at authoring** (the ones that are small, need no decision from you, and can be proven without a screen) — it is **not** a verdict on Her Room and it does not answer any of the audit's 12 questions to you. Those stay open.
- **OWNER:** one thing you should know about the shape of what is left. **Most of the remaining parity queue is desktop-only.** The four big upstream surfaces — Goon Game, For You Feed, Trainer Card, Haptics v2 — are still blocked behind a sizing pass, and three of them need a real display, real media, and a Linux box to evidence honestly. I did **not** spend this wave writing that sizing plan, because on this laptop it would produce a plan that cannot be executed and would be stale by the time it could be. It is recorded as deferred-and-machine-gated, not dropped. The practical consequence: waves run from this machine will keep being small, headless, provable slices like this one, and the big parity surfaces wait for a session on the desktop. Say the word if you would rather I write the sizing plan now anyway so it is ready when you are at the other machine.

## Wave 25 — SP-068 LANDED (2026-08-13) — integrate `f2662cd0`

- **LANDED:** three privacy filters that each **remove** something, over code the port had already shipped. (1) **Private/incognito windows are now dropped before anything counts them** — and the interesting part is that the WPF app turned out to keep **two separate lists** of the words that identify a private window, 35 entries each with **only 15 in common**. Porting either one alone would have silently dropped 20 markers the real app enforces, so the port took the **union of both (55)**, on the reasoning that this rule only ever *throws things away* — so combining the lists can never let through something WPF blocked. (2) **Window titles are scrubbed** — email addresses, runs of six or more digits, and control characters removed, then capped at 80 characters, using WPF's exact rules and numbers. (3) **URLs the model invents are stripped out of the companion's replies** before they reach the chat bubble, the history, or the disk; if a reply was *nothing but* invented links, no reply is shown and **nothing is written to the conversation history at all**. New floor **946 tests / 35 headless / 2 expected skips, 0 warnings, 0 errors** — 43 new tests, each filter with a control case proving it does not simply eat everything. **I did not take the worker's word for any of it:** I rebuilt and ran the full suite three times green on the exact merged tree in a separate checkout (the first run a cold, first-ever build), proved every code path byte-identical to what actually got pushed, and read the filters' code myself rather than its description. The worker also **broke each filter on purpose, one at a time** — four separate reverts — and each time only that filter's own tests went red, which is what makes them real guards rather than decoration.
- **DOES NOT PROVE:** that you can see any of this working. **The first two filters guard a path nothing in the app currently drives** — the window-title machinery is real, typed and wired into the safety boundary, but today only the tests call it, so those two harden a boundary a future feature will inherit rather than change anything on your screen. Only the third one, the link strip, is on a live path. Three more things are named rather than hidden: the link strip now also applies to the companion's *awareness* comments, which the WPF app never stripped — a deliberate choice to have **one** rule rather than two dialects, and it only ever removes more; a reply emptied by the strip shows the app's existing "companion is unavailable" line where WPF substituted a canned phrase, because the port has no canned phrase at that point in the code; and a title scrubbed down to nothing is sent as an **empty slot** rather than being left out entirely, because the port's request is one fixed text shape with no way to omit a field. Also unchanged: **none of this ran on Linux** — still zero WSL distros on this machine, as in every wave so far. And this is **three rows of one audit table, chosen by me at authoring** for being small, decision-free and provable without a screen; it is **not** a verdict on Her Room and it answers **none** of that audit's 12 questions to you. Those stay open.
- **OWNER:** one admission and one standing offer. **The admission:** for eight waves I have hit the same tooling defect — the advisor call comes back with good reasoning but no actual verdict, six times now — and each time I worked around it by asking narrowly and capping the reply, which works, and each time I did **not** file it as a task. That is precisely the rule this project has for everyone else: the same lesson twice means file the work, because a workaround that depends on someone remembering to type it fails the wave they forget. **I have now filed it**, along with a second one this wave surfaced (a test-isolation convention that nothing checks). No decision needed from you; you are seeing it because an unattended run that quietly absorbs its own recurring defects is the thing worth catching early. **The standing offer, unchanged from last wave:** the four big remaining parity surfaces — Goon Game, For You Feed, Trainer Card, Haptics v2 — need a real display, real media and a Linux box, so waves from this laptop will keep being small provable slices like this one. Say the word if you want me to write their sizing plan anyway so it is ready for a desktop session; otherwise I keep taking the cheapest honest parity available.
