# Main Sync 2026-08-04 — merge main@b35facb6 into feat/crossplatform

Merge commit: `56f156fc`. Range: `9e9fc875` (base, 2026-07-17, ~v6.3.x) → `b35facb6`
(~v6.6.3+, 296 commits). WPF ground truth for the port moved from v6.3.x to v6.6.3+.

## Resolution rule (owner ruling, 2026-08-04)

**"Whatever is on main is leading concerning the stuff in WPF — we will not touch it."**

- WPF production files (10 conflicts: `App.xaml.cs`, `CLAUDE.md`, `en.json`,
  `MainWindow.Haptics.cs`, `MainWindow.Settings.cs`, `CompanionService.cs`,
  `HelpContentService.cs`, `SubliminalService.cs`, `VideoService.cs`,
  `QuizSessionGenerator.cs` [DU — restored]) → resolved to **main's version**.
- Old-port tree `ConditioningControlPanel/CCP.Core/**` (3 UU + 8 UA rename-detection
  artifacts) → resolved to **ours**; tree preserved as first-attempt reference only.
  Main's new feature models were restored at their real home
  `ConditioningControlPanel/Models/` (AudioLayerTrack, ContentPackInfo,
  CornerGifOverlaySetting, ExclusiveFeature, IntakePunchCardState,
  Program/{ProgramDefinition,ProgramEnrollment}, QuizRunResult).
- `.gitignore` → union (branch pi-spine rules + main node/marketing rules both kept).

**Caveat for future ground-truth reads:** this branch's WPF tree is *not* a byte-exact
copy of main — residual first-attempt debris lives in non-conflicted files (rename
artifacts absorbed some main edits into `CCP.Core` auto-merges). For WPF behavioral
truth, prefer reading **main** directly. The greenfield `client/` has zero compile
references to the WPF tree or `CCP.Core` (verified 2026-08-04).

## What changed WPF-side (port parity inventory)

| Area | What landed on main | Port implication |
|---|---|---|
| **Browser video engine** (PRs #104–109) | Hybrid out-of-process WebView2 playback for mandatory videos (`Services/Video/Browser/*`, `VideoService.Browser.cs`, `VideoDiag`, `AttentionTargets`), settings toggle + 9-lang loc, in-window attention targets + grace pause, wedge re-arm + adversarial-review fix waves, InteractionQueue slot release | Video stack decision input — WPF moved mandatory video toward WebView2-hosted playback alongside LibVLC. Absorb into greenfield video design (see `video-handoff-spike.md`) |
| **For You feed** (PRs #101, #102, #107) | Desktop WebView2 mosaic reel (`Services/Fyp/*`), ghost mode (true see-through, click-through), opacity parking, eye control; graduated Lab → Exclusives vault spotlight | New feature row eventually; ghost overlay = overlay-clickthrough skill territory |
| **Content packs** (PR #103) | `ContentLocator`, `ccp.content` host + JS shim, `ReleaseContentService`, mod-pack install path, first-run mod picker, installer strip + `Scripts/build-content-packs.ps1`, `pack-zips/` index | Asset pipeline changes: release-hosted packs instead of monolithic installer content |
| **Velvet Vault / Exclusives tab** (PR #104) | `Views/Tabs/ExclusivesTabView` replaces launcher popup | New dashboard surface for the port |
| **Haptics overhaul v2** (PR #101) | `Services/Haptics/Core/*` (contracts, device manager, mixer, patterns, temperament), providers V2 (Buttplug, Lovense + toy-events client), FunScript service, `DtrhHapticDirector`, 8-lang backfill + migration test | Haptics board row re-scopes to v2 architecture when reached |
| **Graded Intake web-core** | `Resources/web/intake/` full web app (engine, render, 4 captcha families, 4 persona banks, VO corpus + `vo_manifest.json`, menu/music/voice, corruption), `IntakeHostService`, `IntakeProfiler`, `IntakePassService`, `IntakePunchCardService` | Already web-core — ports like DTRH did (webview host pattern), high reuse potential |
| **Training Programs** | `Services/Program/*` (ProgramService, BuiltInPrograms.{Firmware,Kept,Presentation,Takeover}, ProgramSessionBuilder, ProgramArt), `ProgramsTabView`, reward track | New feature area for parity |
| **Layered audio** | `LayeredAudioService` + `LayeredAudioWindow`, `AudioLayerTrack` model | Audio row expansion |
| **Corner GIF overlays** | `CornerGifService`, `CornerGifWindow` — always-on corner GIFs | Overlay manifest addition |
| **Media Log** | `MediaHistoryService` + window (last 500 flashes/videos recap) | Small feature row |
| **Inspection Bureau** | `Services/Bureau/*` host + index, `tools/pack-indexer/` | New feature area (labeling game via cclabs.app/bureau) |
| **Misc services** | `AiTextHygiene` (shared model-output cleanup #739), `LeaderboardRankSnapshotService`, `MantraChantService`, `ChromeFxNav`, `MotionFx`, `FxTheme`, `BrainDrainCapturePump`, `GlyphFallback` | Absorb per-row when those rows are reached |
| **Release trains 6.4.0→6.6.3** | DTRH Hourglass/Bottomless Fall/Loom studio, Brain Drain rework + Brain Melt, FX overhaul (AmbientFxCanvas, tiers, reduced motion), NUX first-run, Weekly Intake Pass, season-key adoption, triage batches #614–#767 (video wedges, haptics gate, browser desync, settings races, webcam retry, lock-card, bubble+video freeze, avatar detach, discord share) | Behavior-delta rows per port planning; WPF parity target is now v6.6.3+ behavior |
| **Tests** | `Tests/ConditioningControlPanel.Tests/` — large xUnit suite (previously none) | Behavioral-spec source for port tests |
| **Localization** | `en.json` strict-JSON clean + 8–9 language backfills | Localization source of truth = main's files |

## Board hygiene

Task-board rows covering video, haptics, overlays, assets/content, dashboard tabs,
and AI companion should be re-checked against v6.6.3+ behavior when reached —
several rows were authored against v6.3.x-era WPF. No rows auto-invalidate; this
note is the delta pointer.
