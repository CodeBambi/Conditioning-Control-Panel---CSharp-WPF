# Greenfield Client Task Board

This is the only live product queue for the Windows/Linux Avalonia client. It contains current
outcomes and blockers, not execution logs, archived task records, or workflow-specific state.

## Board Rules

- Reconcile this board with the source tree, current contracts, and verification evidence before
  claiming or completing work.
- A row is `OPEN`, `WIP`, `BLOCKED`, or `DONE`. `WIP` means implementation exists but a named
  acceptance condition is still unresolved; it is not a claim that the feature is complete.
- Each row names its required behavior evidence and verification. Update it with concise evidence
  or the exact blocker when the work changes.
- Concurrent work requires isolated workspaces, disjoint file scope, and one owner for shared
  files such as this board, test-floor metadata, release metadata, and composition roots.
- New rows need an observed parity gap, product defect, safety concern, or owner decision. Do not
  manufacture work from historical workflow artifacts.

## Current Queue

| Priority | Status | Outcome | Required Evidence |
|---|---|---|---|
| P0 | WIP | Admit a haptic provider or keep the capability honestly unavailable. | BOTH ROUTES ADMITTED, TOGETHER, which is upstream's shape rather than a generalisation of it: `HapticDeviceManager` connects every ENABLED provider concurrently and its own class doc says the old single-active-provider design meant "a Lovense user could not also drive an Intiface toy". A `CompositeHapticSink` drives the enabled set, evaluated PER OPERATION rather than captured, with no fallback (a route the user did not tick is never substituted for one that failed) and a parallel all-stop with a per-route budget, so a stop waiting on a dead LAN socket cannot hold the other route's stop hostage. Both route flags DEFAULT FALSE, matching upstream, and the two provider checkboxes ship with them. Floor 2673 -> 2694 unit, 154 -> 155 headless; both gates green. REMAINING FOR DONE is unchanged and is not code: headed device evidence from the owner. Nothing in this repository has driven a real toy on any platform, no test opens a socket to a real haptic server, and the factory's previous unevidenced claim that "a Linux box running Intiface Central would answer identically" was REMOVED rather than softened, because no Linux run backs it. |
| P0 | WIP | Admit the Lovense route and build its sink. | ADMITTED AND WIRED. `AdmittedRoutes` carries Lovense, `HapticSinkFactory.Create()` returns a real `LovenseHapticSink`, and the fake-available throw still fires for any route without a client (asserted against Buttplug). Nine facts that pinned the unadmitted contract now ask for the empty route list EXPLICITLY rather than reaching the product factory, so they survive admission instead of being deleted by it, and no haptic fact touches a socket - the haptic suite runs in 2 s. REMAINING FOR DONE: headed device evidence from the owner (install Lovense Connect or Lovense Remote, pair a toy, enable haptics, confirm a level is felt and that a stop silences it), and Linux evidence separately. Nothing here claims a motor moved. |
| P1 | DONE | Decide whether the capability probe may contact a haptic server the user has not switched on. | DECIDED: NO, AND THE GATE IS BUILT. `CapabilityRegistry.RunAllAsync` probes every registered capability at launch, so registering the sink ungated meant a DEFAULT INSTALL fired an HTTP GET at `http://127.0.0.1:20010/GetToys` on EVERY launch for a feature whose master toggle was off - an unsolicited outbound attempt on behalf of a disabled feature, which is the boundary class a lane may not widen alone. The gate is upstream's own auto-connect conjunction, field for field (`App.xaml.cs:2176`, predicate `:3580-3589` = `lovense.Enabled || buttplug.Enabled`), and it lives in ONE expression (`HapticParticipant.ProbeSinkAsync`) that the composition root registers and a fact can call directly, rather than as a lambda in the root where nothing could reach it. When it refuses it answers the EXISTING not-probed arm - never a server-unreachable answer, which would be a claim about a socket it deliberately did not open. Three facts hold it, including one on the real composition root asserting the reason is `not-probed` and explicitly NOT `haptic-server-unreachable`. The other half of the decision was the defaults: both route flags now default FALSE (upstream's stored default - `HapticSettings.cs:769` has no initializer), because defaulting them true was what made the probe reach a wire at all. |
| P0 | WIP | Build the Buttplug sink (second of the two admitted routes). | BUILT AND PROVEN ON THE WIRE. `ButtplugHapticSink` + `RealButtplugSession` behind a deliberately behaviour-free `IButtplugSession` seam, driven END TO END through the real `ButtplugClient` against `ButtplugToyServer` - a loopback server speaking the wire CAPTURED from the real client, not read off a spec page. The capture corrected two things a spec would have got wrong: the library announces `ProtocolVersionMajor: 4` though its README says v3, and `DeviceList.Devices` is a DICTIONARY keyed by index (an array parses to zero devices in silence). THE ROW'S OPEN DESIGN QUESTION IS ANSWERED BY DOING BOTH, and the answer was right: the seam makes the sink's policy testable, and the captured server proves this port calls the library correctly - a fake session alone would have proved the policy against a fiction. Quantization is the LIBRARY's here and is measured, not assumed: `Percent(0.75)` against a feature advertising `Value:[0,20]` put `15` on the wire, so the sink passes 0..1 through untouched, which is the opposite of the Lovense route and is pinned as a fact. TWO DEADLOCKS WERE FOUND AND FIXED WHILE PROVING IT, both real in the app and neither visible as a test failure - the suite HUNG. (1) The library completes its promises from inside its own connector read loop on a `TaskCompletionSource` with no `RunContinuationsAsynchronously`, so every await of a Buttplug call resumes INLINE on that reader; a captured stack showed one thread running the websocket receive, the sorter, `StartScanningAsync`, the sink AND THE TEST BODY, which then released the sink and waited for a disconnect that was waiting for the read loop running it. Fixed by `DetachedAsync` (`await Task.Yield()` after every library await) and pinned by a choke-point guard, because the regression is a hang rather than a red. (2) `ButtplugWebsocketConnector` captures its owning `SynchronizationContext` in a FIELD INITIALIZER and its read loop ends in a BLOCKING `Send` back to it - construct it on the Avalonia UI thread and shutdown deadlocks against itself, so it is constructed on a pool thread. Both are documented at the fix with the decompiled evidence. Route timing went 20.8s -> 2.7s for the suite. `Buttplug 5.0.1` now committed; cold warning gate 0W/0E across 4 projects. REMAINING FOR DONE: the route is NOT YET ADMITTED (`AdmittedRoutes` still carries Lovense alone) - see the admission row, whose shape changed once upstream was read properly. And headed device evidence, which the OWNER HAS REPORTED THEY CANNOT GIVE RIGHT NOW: no device on hand as of 2026-08-23. Nothing here claims a motor moved. |
| P0 | WIP | Prove the haptic stop reaches an admitted provider before teardown completes. | WIRE-LEVEL HALF DONE. `THEALLSTOPPutsAZeroOnTHEWIRE_AgainstARealProviderBeforeTeardownCompletes` drives the real `ApplicationHost.ShutdownAsync` through the real composition root into a real `LovenseHapticSink` on a loopback HTTP server and asserts the SERVER received `Vibrate:0`. It reds against a `StopAllAsync` that reports success having sent nothing - the defect every other teardown fact here would pass, because they count calls on a double. SCOPE NAMED HONESTLY: it proves DELIVERY, not ordering. I claimed both, then checked - with the pre-drain head slot DELETED it still passes, because `StopAsync` also all-stops before releasing the sink. Ordering stays `THEALLSTOPRunsBEFOREEveryParticipantStop`, which does fail against that mutation; the two are complementary and the boundary is recorded in both. REMAINING FOR DONE: headed device evidence that a real toy actually goes quiet, which is the owner's gate and cannot be discharged in-repo. GATE UNAVAILABLE 2026-08-23: the owner reports no device on hand, so this stays WIP rather than waiting on a check nobody can run today. The Buttplug route now has the same wire-level proof (`StopAllPutsAStopCommandOnTheWireForEveryDeviceTheServerKnows`), so both admitted routes are evidenced to the server and neither is evidenced to a motor. |
| P0 | OPEN | Deliver For You Feed webcam gaze with the admitted local engine. | OWNER DECISION 2026-08-23, AMENDING THE 2026-08-22 ONE: ship the LOCAL ENGINE ONLY. The admission spike is closed as UNAVAILABLE with its finding recorded - every mainstream appearance-based engine fails A-011's commercial-rights bar on TRAINING DATA rather than code licence (L2CS-Net/Gaze360 covers "models trained on dataset"; PureGaze and GazeTR on ETH-XGaze CC BY-NC-SA 4.0; OpenSeeFace BSD-2 code but its gaze model trained on MPIIGaze CC BY-NC-SA 4.0, and a permissive code licence does not launder weights). Evidence stays in `client/docs/gaze-engine-admission-spike.md`. The user-visible feature is NOT reduced: the incumbent stack the WPF product ships is itself deep-learning and is Apache-2.0 on code AND weights, which is very likely why it landed there. The three unpicked options (buy a commercial SDK licence, train on permissive or synthetic data, request a written grant) are recorded as not taken rather than deleted. WHAT REMAINS IS THE PORT ITSELF, and it is not small: zero occurrences of `Gaze` under `client/src`. Note the FYP census verdict already on file - `Services/Fyp/Online/` is NOT part of For You Feed but the app-wide remote-media subsystem with twelve consumers, and it carries a third-party network client (scrolller.com) that is a separate boundary decision. Scope this row to the WEBCAM GAZE behaviour only. OWNER DECISION 2026-08-23 (SECOND, resolving the blocker a scoping pass raised): THE WEBCAM CAPABILITY GATE IS ALREADY CLEARED FOR THIS WORK, AND NO FURTHER DECISION IS OWED. The blocker was read off `capability-inventory.md`'s line "Expanding sensors, derived data, persistence, networking, diagnostics, or telemetry requires a consent-contract revision and owner review" and taken to mean the camera capability itself is unadmitted. It does not say that. Read in place, that clause governs EXPANSION BEYOND the contract the same section already writes down - and that section already admits the local engine explicitly, names it THE DEFAULT, and specifies its full privacy and honesty contract: frames, crops, tensors, landmarks, gaze samples and every per-frame biometric derivative stay MEMORY-ONLY and are never saved, logged, transmitted, screenshotted, put in telemetry, AI prompts or crash reports; audio capture is never opened; the camera starts only after CURRENT EXPLICIT CONSENT plus an explicit user start, and never from opening the dashboard, restoring settings or finding a calibration; the UI shows honest states including denied, removed and missing-runtime; acquisition and model init are cancellable and off the UI thread; and only consent metadata, engine/model identity and compact numeric calibration are persisted. So implementing the LOCAL engine AS SPECIFIED is executing an existing admission, not widening a boundary. WHAT WOULD STILL NEED A DECISION, named so this cannot be quietly stretched: any remote or third-party engine (already closed UNAVAILABLE on training-data rights), any persistence of a frame or derivative, any transmission of anything camera-derived, opening the microphone, or adding diagnostics/telemetry over camera data. Any of those is expansion and stops this row. WHAT REMAINS IS THE PORT ITSELF and it is large: zero occurrences of `Gaze` under `client/src`, no camera slot in `Capabilities/`, and no webcam consent type. Upstream surface, measured: `Services/Webcam/WebcamTrackingService.cs` 3,610 lines, `WebcamCalibrationData.cs` 369, `WebcamDeviceEnumerator.cs` 138, `WebcamWinRtEnumerator.cs` 61, `Windows/WebcamCalibrationWindow.xaml(.cs)` 1,910+275, `Dialogs/WebcamConsentDialog.xaml(.cs)` 187+277. THE FIRST SLICE IS THE CONSENT AND CAPABILITY SEAM, NOT THE ENGINE: a typed camera capability that refuses honestly with no device, no consent, denied permission or no engine, on BOTH platforms, is worth landing before a single frame is decoded - it is what makes every later slice's refusal truthful. Linux needs its own acquisition evidence and must prove XDG portal/PipeWire where sandboxed; a Windows-only capture path is not this capability. **SLICE 1 LANDED 2026-08-23: THE CAPABILITY AND CONSENT SEAM, NOT THE ENGINE.** `Camera/` now carries a typed `webcam-gaze` capability with an EMPTY `AdmittedEngines`, so every product run reports `Unavailable(camera-no-engine)` - honest, and the state every later slice's refusal is measured against. THE PRIVACY GATE IS PROVED RATHER THAN ASSERTED: `ProbeAsync` asks the engine gate and the consent gate BEFORE it can reach `Enumerate`, and a fact counts every ask across a whole launch - start, probe, GRANT CONSENT, probe again - and holds it at ZERO, because the engine rung is asked first. It names its own inversion (move the consent rung above the engine rung and it fails), which is `capability-inventory.md`'s "opening the dashboard, restoring settings, or finding calibration never starts it" turned into something that can fail. BOTH PLATFORMS HAVE A ROUTE rather than Windows plus a silent absence: DirectShow, and V4L2/sysfs on Linux with the XDG portal branch a TYPED REFUSAL naming the portal instead of a pretend client. Linux identity is honestly unstable - no durable id exists without udev or an `open(2)` - so `IdentityIsStable` is false there and this slice persists NO camera identity at all rather than inventing one the contract would then trust. Floor 2700 -> 2716. WHAT IT DOES NOT PROVE, and the row must keep saying so: no camera was opened, no frame decoded, no engine exists, and the V4L2 route has NEVER RUN on Linux - the 16 camera facts DO now run and pass ON LINUX (verified on the WSL2 host), so the seam behaves there - but `/sys/class/video4linux` is EMPTY on that host, so the route has still never enumerated a real device and its parsing is proved only against injected fixture trees, which is evidence about parsing and none at all about a running kernel's sysfs. The Windows route WAS executed against this machine's real integrated camera, metadata only, no open and no camera indicator. NEXT SLICE is capture plus the engine, and it is the one that needs headed evidence and a real Linux run. |
| P1 | WIP | Deliver the XP and level SPINE, so the four features that already compute progression stop throwing it away. | **COORDINATOR DECISION 2026-08-23: ADMITTED, and lifted out of the completeness census into a real row, because it is a KEYSTONE rather than one more gap.** Four LANDED features are degraded today for want of it, and each says so in its own source: `Features/Intake/IntakeDraft.cs:24-25` ("COMPUTED, never granted - no greenfield XP store"), `Features/Intake/IntakeHostWindow.axaml.cs:538-540`, `Features/Dtrh/DtrhMeta.cs:794`, and - as of today - `Features/Arcademy/ArcademyClassPayout.cs`, whose `XpBanked` is a computed `false` carrying its reason because there is nothing to bank into. The newly landed `Features/Progression/TrainerCard.cs` cannot show a level for the same reason. So this is not new surface: it is the missing floor under surface that already shipped. SCOPE IS DELIBERATELY NARROW - the store, the level curve, persistence, and wiring the ALREADY-COMPUTED-AND-DISCARDED sources. EXPLICITLY OUT, each recorded as out rather than half-built: the level-up ceremony (animation, tray balloon, sound, avatar change), Quests, the skill tree, Season Recap, the daily-free wheel, achievements beyond `GradedRunAwards`, and AFK/idle suppression - all separate census entries and separate decisions. THE FOUR LEVEL GATES BECOME REAL: `BrainDrainEffect.cs:51` (70), `BubbleCountEffect.cs:84` (50), `LockCardEffect.cs:87` (35), `MindWipeEffect.cs:40` (75) each already name their level as not-ported. **A gate that silently passes because the store is absent is the failure mode here**; a typed refusal naming the level is the outcome. HONESTY BAR, inherited from the Trainer Card: a level the store cannot know must NOT render as 0 or 1 - unknown answers Unknown, never a zero that reads as a score. And no XP number may be invented: every amount traces to an upstream citation or to a source the port already computes, or it stays ungranted. **LANDED 2026-08-23, AND MY OWN PREMISE ABOUT THE LEVEL GATES WAS WRONG.** I wrote that the four gates should "become real" and that a gate silently PASSING was the failure mode. Source says otherwise and the lane refused the instruction, correctly: `Models/AppSettings.cs:5439` `IsLevelUnlocked` is `return true;` under a comment reading "Feature level gating has been removed - every feature is available from level 1". Three of the four never had code at all: Bubble Count and Mind Wipe were doc-comment PROSE with no reader, and Lock Card's level survives nowhere in the shipping tree - it existed only in this port's own comment. Making them refuse would have taken four modules away from every user the shipping app hands them at level 1: a regression dressed as a port. So the four numbers are recorded, upstream's actual body is ported, and a fact RE-DERIVES `return true;` from the shipping bytes on every run - it reds if anyone 'makes them real' by guess, and equally if upstream ever puts a body back. THE CURVE is upstream's v1, a five-band FORMULA rather than a table, each band cited; v2 is unreachable here because only the Descent migration ceremony writes the epoch that selects it, and that is a named limit rather than dead code. A fact asserts `MidpointRounding` is ABSENT from v1's body, because no v1 band can land on a midpoint - the mode is unobservable, which is exactly why someone could 'tidy' it to match v2 with nothing going red. THREE of upstream's THIRTY AddXP sites are wired - precisely the ones this port already computed and discarded - and the other 27 are listed with the reason each is not, so the remainder stays visible. Floor 2794 -> 2807. REMAINING FOR DONE: **no level is rendered anywhere.** The ledger exists and nothing shows it; the Trainer Card's note now says so in words rather than claiming no level exists, deliberately a string change and not a card change, because rendering a level is a visual contract needing its own headed gate. Also out and recorded as out: the entire level-up ceremony, Quests, the skill tree, Season Recap, the daily-free wheel, AFK suppression, curve v2, and the server-confirmed XP watermark. Concurrent-writer behaviour is unproven and belongs to the app-wide lift row. |
| P1 | WIP | Deliver the SCRIPTED SESSION engine, the largest single gap and the second keystone. | **COORDINATOR DECISION 2026-08-23: ADMITTED, lifted out of the completeness census, and slice 1 is in flight.** Same argument that justified the XP spine: this is not new surface, it is a floor that LANDED code already names as missing, in five places and in its own words - `Session/SessionEngine.cs:13-17` ("a SCRIPTED session with phases, a duration, XP and a stop-confirmation dialog … which runs on TOP of the engine and is not ported"), `Effects/IntensityRampEffect.cs:66` ("the port has no scripted session"), `Effects/RampCurves.cs:54`, `Effects/LockCardSchedule.cs:53`, and `Features/Intake/IntakeDraft.cs:5-7` ("marked never-runnable - no session engine exists in greenfield"). NOTE THE NAME COLLISION so nobody trips on it: the client already HAS a `Session/SessionEngine.cs`, and it is a different thing - the ordinary effect session. Upstream's scripted session runs ON TOP of it. Upstream surface measured at 7,842 lines / 8 files, so this is sliced. SLICE 1 IS THE RUNTIME CORE, NOT THE UI: the `.session.json` model as persisted, phase advance on an INJECTED clock, start/stop with the settings SNAPSHOT taken at start and RESTORED when the session ends (user-visible and load-bearing - a session temporarily replaces the user's settings and must give them back), and **the clock-jump guard, which trusts a `Stopwatch` over the wall clock past 30 seconds** and is the most testable behaviour in the slice. OUT of slice 1 and recorded as out: the session editor window, the rack UI and its repaint, the session-complete recap and history windows, the media log, the XP pause penalty, the corner-GIF window, scheduled bubble bursts, and the randomised ramp starts. **SLICE 1 LANDED 2026-08-23 (merge of the lane branch), and the row stays WIP because NOTHING CAN START ONE.** What exists: `Session/ScriptedSession.cs` (the `.session.json` model as persisted - 14 top-level keys, 3 phase keys and 60 distinct settings keys across the four files, every one bound - and the fact asserts JSON extension data is EMPTY rather than asserting a count, which is the stronger form: an unmodelled key is NAMED in the failure, read with upstream's own options per `Services/Session/SessionFileService.cs:16-22`, malformed file -> null per `:105-108`), `ScriptedClock.cs` (the two-clock seam), `ScriptedSessionDials.cs` (snapshot / apply / restore over the eleven real preset stores), `ScriptedSessionRun.cs` (phases on an injected clock, START/STOP, and the jump guard). 22 facts, floor 2807 -> 2829. THE CLOCK-JUMP GUARD IS PORTED VERBATIM AND MUTATION-CHECKED: `Services/Session/SessionEngine.cs:104`, `Math.Abs(divergence.TotalSeconds) > 30` -> trust the Stopwatch, STRICTLY greater and in BOTH directions (`:100-103` names both halves - the speed-hack and the '149 minutes left' balloon, upstream #369) - and `>= 30`, `> 60`, and dropping `Math.Abs` each red exactly the fact that names them. FOUR DIVERGENCES ARE RECORDED RATHER THAN SMOOTHED: (1) the wall clock is UTC where upstream reads local, following `Scheduling/ScheduleClock.cs:14-20`, so DST stops being a jump instead of being survived by the guard; (2) apply does NOT write to disk where upstream saves mid-session (`Services/Session/SessionEngine.cs:262`), so an interrupted session gives the USER's settings back rather than the session's - a divergence in the user's favour, with one stated exception (`SessionEngine.Start()` saves its own flash preset); (3) a second start returns false where upstream throws (`:150-153`), matching this port's own engine; (4) dials are applied before the engine arms, because this port's modules read dials at arm. A LANE CORRECTION LANDED WITH IT: the lane recorded `flashScale` as dead at runtime upstream, and it is NOT - `UpdateRampingValues` reads it at `Services/Session/SessionEngine.cs:596-599` and pushes it through `AppSettings.SetSessionFlashRamp` (`Models/AppSettings.cs:908`), so a session file that sets it does change what the user sees; it is unapplied here only because the ramp is out of slice 1. `flashSmallSize` IS dead - grep finds writes and no read. WHAT THIS DOES NOT PROVE, and it is most of the feature: nothing visual, nothing headed, no Linux, and NOTHING CONSTRUCTS A `ScriptedSessionRun` - it is referenced only by its own files, its test and two doc comments, so no surface in this build can start a scripted session. Still unported: the rack and its repaint, the editor, the Session Complete recap and history, the media log, pause and its 100-XP penalty, the XP award, the corner-GIF window, scheduled bubble bursts, the randomised +/-3min ramp starts, `UpdateRampingValues` (`:564`) and `CheckDelayedFeatures` (`:663`) - so a delayed feature is applied OFF at t=0 exactly as upstream applies it (`:1288-1296`) and nothing turns it on yet. The four corroborating absence claims in landed code (`Effects/IntensityRampEffect.cs:66`, `Effects/RampCurves.cs:54`, `Effects/LockCardSchedule.cs:53`, `Features/Intake/IntakeDraft.cs:5-7`) and the census entry were amended at the land rather than left to rot. **SLICE 2 LANDED 2026-08-24: THE RAMPS AND THE DELAYED STARTS (+14 facts, class now 36/36).** `Session/ScriptedSessionRamp.cs` is the arithmetic of `UpdateRampingValues` (`Services/Session/SessionEngine.cs:564-661`) as a PURE function, and `ScriptedSessionRun` runs it plus `CheckDelayedFeatures` (`:663-772`) on the tick. THE MOST VALUABLE FACT IN THE SLICE IS THE ONE THAT PROVES A SEPARATION RATHER THAN A VALUE: upstream drives the pink ramp into the OVERLAY and deliberately not into the user's persisted dial, because writing it auto-saved to disk and an app kill mid-session froze the ramp maximum into settings.json permanently - the 'screen keeps getting more pink and stays that way' reports, #471 and #476, and `RestoreSettings` only heals a CLEAN stop. `TheRampClimbs_AndTheUsersOwnPinkOpacityNeverMovesWithIt` reds under mutation M12, which is that exact defect re-introduced. `flashScale` IS NOW APPLIED, closing the correction filed at slice 1: pinned for the whole session rather than lerped (`:596-599`), clamped [50,250], parked on `ScriptedSessionRun.Ramp` and written to NO persisted document, because the port persists no flash draw dials and upstream does not store it either. `flashSmallSize` stays unapplied - grep over the shipping tree returns assignments only, no read anywhere. THE JITTER IS PORTED, not skipped: `RandomizeStartTimes` (`:777-805`), +/-3 min, floored at zero, applied only to an ENABLED feature whose start minute is already > 0, with the randomness INJECTED through the port's existing `Random? random = null` seam so a fixed sample pins the start to an exact minute - no wall-clock, no sleep, no poll anywhere in the new facts. THE MUTATION CHECK CAUGHT TWO OF ITS OWN FACTS AND THAT IS THE POINT OF RECORDING IT: 23 mutations were applied to real product source, rebuilt and run, and TWO facts were GREEN under their first mutation - `Lerp(35,90,0.5)=62.5` rounds to 62 either way so the assertion could not tell truncation from rounding (fixed with a non-midpoint pin, 71.66->71), and removing the already-on guard turns a delayed start into a TOGGLE that an even number of ticks hides (fixed by asserting after every following tick). Both facts were strengthened rather than the mutations being retired. THREE DIVERGENCES RECORDED: the port writes the ramp's FIRST SAMPLE into the opacity dial at a delayed pink/spiral start, because this port has no sustained-overlay hold to carry the value and its modules read dials when they arm - without it a delayed filter would come up at the USER's opacity, which is neither the session's start nor its ramp; brain drain (`:652-660`, `:762-771`) is NOT ported because it is commented out upstream and porting dead code would be inventing behaviour; and the spiral file probe's early return is replaced by the port's own recorded refusal (`spiral-no-image`) rather than upstream's silent dial-off. WHAT IT STILL DOES NOT PROVE: `ScriptedSessionRun.Ramp` HAS NO CONSUMER in this build - a module that FOLLOWS the climbing value needs a live re-tint seam the port does not have, and a dial write would have been the #471 defect, so it was refused rather than faked. Every module in the rig was composed with a NULL SURFACE: what is proved is the dial, the arm and the recorded refusal, never a pixel. Nothing visual, nothing headed, and still nothing constructs a `ScriptedSessionRun`. LINUX IS NOW PROVED FOR THIS SLICE at unit level: 2843 total / 0 failed / 66 skipped, and the class 36/36 ten runs out of ten. IT TOOK A RACE FIX TO GET THERE, and the race is worth keeping: the headline fact compared EVERY document before and after a killed session, and one of them - `session_preset.json` - is legitimately written from t=0 by `SessionEngine.Start()` (`Session/SessionEngine.cs:150`, a fire-and-forget `_preset.Save()`), which is slice 1's own stated exception rather than a new one. The comparison had no ordering against that unawaited write, so it failed 2 runs in 5 on Linux and passed on Windows. THE FIRST FIX WAS KILLED BY ITS OWN MUTATION CHECK: awaiting `SaveImmediate()` as a barrier looked right and is not a passive wait, it is a WRITE of the same session values, so deleting BOTH product saves left the new assertion passing 10/10 - it would have pinned the test's own write. The shipped fix excludes the one legitimately-written file AT ENUMERATION so the contended file is never opened, and adds `IsDirty` teeth proving the session's pink mutations are still unwritten: race-free by CODE rather than by timing, with no barrier, sleep, poll or retry. Re-proved by mutation at 10/10 red for a delayed start saving the pink store, for the #471 write, and for any second document moving mid-session. |
| P0 | WIP | The unit suite permanently loses Win32 FOREGROUND PERMISSION mid-run, and every later topmost assertion is then refused with a success return. | **DIAGNOSED 2026-08-23, mechanism proved OUTSIDE the suite, fix in flight.** Symptom: `SetWindowPos(HWND_TOPMOST)` returns TRUE and `WS_EX_TOPMOST` (0x8) is not applied - read back as `0x80800A0` - so the product's 32-attempt re-assertion loop cannot help, because IT IS NOT A RACE, IT IS A REFUSAL. THE RULE IS DOCUMENTED AND WAS LOOKED UP RATHER THAN GUESSED: `SetWindowPos` Remarks - 'to use SetWindowPos to bring a window to the top, the process that owns the window must have SetForegroundWindow permission'; `SetForegroundWindow` Remarks lists the ways a process holds that permission. THE TRIGGER, characterised empirically in ~60 lines of PowerShell with no test code involved, and it needs BOTH halves: a click must have been delivered to one of our own windows, AND the process's top-level window count must then reach ZERO. Measured ladder: fresh process -> topmost YES; create and destroy a `WS_EX_NOACTIVATE|TOOLWINDOW` popup with no click -> YES; deliver a click to it, then destroy it -> NO, and permanently; `SetForegroundWindow` on one of our own windows -> YES again. **Keeping ANY other top-level window alive - including a hidden, never-shown one - preserves the band across the clicked window's destruction.** The poisoner is `PointerSurfaceObservations.RunDelivery` (every `PointerCapabilityTests` fact that reaches the desktop forces the one shared `Delivery` lazy), and the band is lost at the LAST `ScratchTarget` disposal - proved positional rather than identity-bound by reversing the disposal order and watching the loss move. IT PREDATES THE SCRIPTED-SESSION MERGE: the full assembly at `ed4d9bea3` (the merge's first parent) is 20 failed / 2807 with the identical `missing 0x8` signature. The merge reshuffled WHICH classes are downstream of the poisoner; it neither caused nor exposed the defect, and the board's earlier 'zero failures on Windows' readings were luckier orderings, not a healthy suite. TWO COORDINATOR HYPOTHESES WERE DISPROVEN BY MEASUREMENT AND ARE RECORDED SO NOBODY RETRIES THEM: Avalonia platform init / process DPI awareness via `CompositionRootTests` (`Program.BuildAvaloniaApp` -> `UsePlatformDetect()`) is NOT the cause - that class plus both victim classes is 24/24 green; and 'the real-desktop collection alone is green' holds only under DEFAULT parallelism - under `-parallel none` the collection alone is 19 failed / 253, because the poisoner is INSIDE it. Also ruled out and not to be retried: the developer's own open applications (53/53 green with them open and holding the foreground); `Win32InputPresence`'s `SetForegroundWindow` escalation ladder (76/76 green); and CPU starvation (`-parallel none`, 163s, zero concurrency, same failures). Environmental note, read but NOT proven causal: `HKCU\Control Panel\Desktop\ForegroundLockTimeout` is 2147483647 (~24.8 days) on this machine, so the one documented escape hatch that would restore permission on its own can never fire here. FIX IN FLIGHT: hold ONE hidden top-level sentinel window for the life of the `RealDesktopLease` collection fixture, so no desktop fact can walk the process to zero windows. That is not a weakening - it makes the test process REPRESENTATIVE, because the shipping app always owns at least one top-level window (the Avalonia main window, or `Win32TrayPresence`'s hidden owner window), which is exactly why this is not user-reachable. FORBIDDEN AS FIXES, explicitly: a retry loop, a skip, an `allowedSkips` entry, a weakened assertion, a wall-clock wait, or 'repairing' permission with a `SetForegroundWindow` at teardown - the last one works and is wrong, because it masks the revocation instead of not causing it. REQUIRED INVERSION BEFORE IT LANDS: with the sentinel in place, removing the tray visibility guard at `Tray/Win32TrayPresence.cs:604-611` must STILL red `TheRightClickGesture_LeavesTheProcessAbleToPlaceAWindowInTheTopmostBand` - if the sentinel blinds that fact it is masking a real defect class and must not land. |
| P2 | OPEN | Decide whether the four Win32 surfaces should assert HWND_TOPMOST through the attach ladder instead of bare. | **DEFERRED DELIBERATELY 2026-08-23, from the foreground-permission diagnosis above, and deferred on a risk argument rather than a scope one.** `Win32OverlayPresence`, `Win32GlyphSurface`, `Win32VideoPresence` and `Win32PointerSurface` all assert `HWND_TOPMOST` bare. In a process that has lost `SetForegroundWindow` permission they are refused with a success return, and the product path really does degrade correctly - `Present` returns `Unavailable(...style-refused)` rather than pretending. The repo already owns the remedy and it was PROVEN to restore the band in the poisoned state: the attach ladder at `Win32InputPresence.cs:525-588` and `RealDesktopCollection.cs:750-770`. NOT BUILT, for two stated reasons: (1) NO USER CAN REACH THE STATE - the trigger needs the process's top-level window count to hit zero after a click, and the app always owns at least one window, so this is hardening against a state that does not occur rather than a live defect; (2) putting `AttachThreadInput` against a FOREIGN foreground thread inside a ~1s re-assert cadence is a platform seam with real deadlock risk, and this port routes those through the advisor and headed evidence, not through a defect hunt. THE CAVEAT IS THE REASON THIS ROW EXISTS AT ALL, recorded verbatim and unproven: if a future teardown path ever destroys EVERY top-level window including the tray owner and then presents a new topmost surface, the overlay would silently stop appearing on top for the rest of that process's life, with every write call still returning success. |
| P2 | OPEN | Both `Pump` implementations drain the ENTIRE calling thread's message queue, and no fact pins which thread they run on in the product. | **FOUND 2026-08-23 while reading the foreground-permission diagnosis, and filed rather than chased.** `Win32InputPresence.Pump` (`Input/Win32InputPresence.cs:463-480`) and `Win32PointerSurface.Pump` (`Pointer/Win32PointerSurface.cs:290-305`) both call `PeekMessageW(out msg, 0, 0, 0, PM_REMOVE)`. **`hWnd = 0` means the whole thread**, not this class's windows: every message queued for ANY window on the calling thread is removed, translated and dispatched. HOW IT SURFACED: the sentinel-window fix for the topmost-band defect put one extra hidden window on the pump's thread, and `InputCapabilityTests.ADisposedPresence_RefusesEveryPathAndNeverPromptsAgain` - which asserts `Pump(16) == 0` after disposal - started reading 2. That is the harness noticing the breadth of the drain by accident; nothing asserts it deliberately. THE PRODUCT QUESTION, UNANSWERED AND THE REASON THIS IS A ROW: `BubblePopSurfacePresenter.StepOnce` (`Effects/BubblePopSurfacePresenter.cs:374`) calls `_surface.Pump(PumpBudget)` on whatever thread drives the animation step. IF that is ever the Avalonia UI thread, this drains and dispatches Avalonia's OWN window messages out of Avalonia's loop - which is a reentrancy and ordering hazard rather than lost messages, since `DispatchMessageW` still reaches the right WndProc. IF it is a dedicated thread owning only these windows, the breadth is harmless and deliberate. NOBODY HAS ESTABLISHED WHICH, and that is the whole finding: the invariant these two methods depend on ('this thread owns nothing else') is nowhere stated, nowhere asserted, and was silently violated by a one-window change. Scope when admitted: establish the thread by evidence, then either pin the invariant with a fact or narrow the filter to owned windows - not both, and not neither. |
| P2 | OPEN | A cap-outstanding concurrency fact flakes under the `dotnet test` host, and one clean re-run is not a diagnosis. | **OBSERVED 2026-08-24 during the topmost-band work and filed rather than waved through.** `SoundArbitrationTests.Construction_AtTheOutstandingCap_FurtherCreatesRefusedWithoutStartingAConstruction` failed once with `Expected 2, Actual 1` on the FIRST `check-floor.mjs` run and passed on the second; the class is 3/3 green run directly, several times. Nothing in that packet touched sound. THE SHAPE FITS A REAL RACE RATHER THAN NOISE, which is why this is a row and not a shrug: the fact counts how many constructions are OUTSTANDING at a cap, so it depends on a second construction still being in flight when the assertion reads the count. Under the `dotnet test` host both test projects run and the machine is loaded, which is exactly when an in-flight operation can complete early and drop the count to 1. If that reading is right the fact is racy BY CONSTRUCTION and would flake on any loaded machine, including CI - and 'it passed the second time' is the worst possible response to it. WHAT IS NOT KNOWN: whether the product's arbitration is correct and only the fact is racy, or whether the cap can genuinely be under-counted under load, which would be a real defect in the arbitration itself. NOBODY HAS SEPARATED THOSE TWO, and the row exists to stop the question being closed by a green re-run. Scope when admitted: reproduce under deliberate load, decide which of the two it is, then fix the one that is wrong - and if it is the fact, it must gain a deterministic signal rather than a wider tolerance. NO RETRY, NO SLEEP, NO WIDENED WINDOW: this port's rule against wall-clock waits exists for precisely this class of fix. |
| P1 | OPEN | A leaked `HapticToyServer` loopback listener fails the assembly at CLEANUP, intermittently, and it will red a land run at random. | **FOUND 2026-08-24 on Linux by a lane running the full unit assembly three times; it fired on ONE of six runs total.** The assembly ends `Errors: 1` with `Failed: 0` - every fact green, the ASSEMBLY fixture throwing in `Dispose`: `[Test Assembly Cleanup Failure] Assembly fixture 'LoopbackListenerLeakSelfCheck' threw in Dispose ---- System.InvalidOperationException : leaked loopback listener(s) holding port(s): HapticToyServer: http://127.0.0.1:49980 (port 49980)`. WHY THIS IS P1 AND NOT P2: it is a RANDOM red on a green tree. It costs nothing to re-run and everything to trust, because the correct response to a random red is never 'run it again' - and `Errors: 1 / Failed: 0` is the shape most likely to be waved through, since no fact names it. IT IS ALSO THE SELF-CHECK WORKING: `LoopbackListenerLeakSelfCheck` exists to catch exactly this, it caught it, and the leak is real rather than a false alarm. LEAD, NOT A CONCLUSION, and stated because the coordinator has a stake in it: `HapticToyServer`'s port acquisition was rewritten earlier in this same branch, replacing a racy `ReserveAndReleasePort()` with `HoldFreePort()` (`client/tests/CcpClient.Tests/HapticToyServer.cs:104`), which returns an `IDisposable` holding a socket bound with `ExclusiveAddressUse` and never listened on. That change is the obvious thing to look at FIRST and the obvious thing to be wrong about: the failure names a leaked LISTENER (`:167-192`, `_listener.Stop()` then `Close()`), not a held reservation socket, so the two may be unrelated. Do not assume either way. Scope when admitted: reproduce under repetition, establish whether the listener outlives its `Dispose` or the `Dispose` never runs, and fix the teardown - NOT the self-check, and not with a wall-clock wait for the port to free. |
| P1 | OPEN | Deliver the remaining Trainer Card behavior that is meaningful without unapproved sharing. | WPF behavior evidence, current platform research, focused tests, and headed evidence for user-visible work. **LANDED 2026-08-23, and the reconciliation was half the work.** The census was stale on more than its conclusion: it said no Trainer Card code existed in `client/`, that B7/B8/B9 had nothing subscribing, and that there was NO achievement subsystem at all - all false since `9057cfa5b`, and its own guard could not see it because nothing watched the port side. Corrected with a table of claim-versus-reality, each opened rather than inferred. THE CARD ITSELF renders what the port ACTUALLY GRANTED and claims no tier it cannot verify: an unreadable record answers `Unknown` with a NULL count rather than a zero that would read as a score, and an untracked achievement says NOT TRACKED rather than "0 of 25". SHARING AND EXPORT ARE ABSENT, NOT DISABLED - held by two chokepoint facts (the page declares exactly one interactive control; the type contains no write, no HttpClient, no clipboard, no process start). ITS READ DELIBERATELY AVOIDS `PersistenceStore`, and the reason is the good part: that load MUTATES - it adopts an orphaned temp, deletes a stale one and quarantines what it cannot bind - and a card being LOOKED AT must not rename a user's record; a fact seeds a corrupt record plus a stale temp and proves both survive byte for byte. Placed as a module on the Intake page rather than a sixth rail door, because `ShellRoutes` refuses a door whose destination is nearly empty. Floor +15 unit, +4 headless. REMAINING FOR DONE: a headed capture - nothing here proves the card is legible, correctly laid out or coloured, that it fits without clipping, or that anything composites. Portrait, wardrobe, pinned badges and the community banners stay out; they need bytes and owner decisions this row does not have. **THE HEADED GATE IS NOW DISCHARGED FOR THE CARD'S DEFAULT STATE.** The verification harness gained a `trainer-card` surface: driven to the Graded Intake door by REAL INPUT and gated on the CARD'S OWN UIA text - the title, all four award-row names and the record note - before any pixel is read. It also enumerates the page's buttons and refuses on a share/export/upload-shaped one, because a pixel cannot see a control that is not there, which is how the absent-sharing claim gets headed evidence at all. Measured: `trainer-card-ground` 108932/108932 = 1.000, `trainer-card-ink` 671/3020 = 0.222. Inverted against a REAL wrong capture (the dashboard): 0.013 and 0.000, exit 2. NO UNIFORM CAPTURE CAN PASS THIS SURFACE - the two colour bands are disjoint by arithmetic, and a fact asserts that over the manifest itself rather than trusting the measured numbers, which is precisely the vacuous green a CAPTURE PASS over an all-black image already cost this board once. WHAT IT DOES NOT PROVE: a pixel check is evidence about colour and geometry at named offsets - not legibility, wording, contrast or usability; only the `no-runs-yet` state has headed evidence, and the Read, Unreadable and earned-row states have none; Windows only, one scale, one window size. |
| P1 | BLOCKED | Define the safe, supported scope for Goon Game beyond local practice mode. | **DELIBERATELY NOT DECIDED BY ME, and this is the one row where I am declining the authority I was given.** Everything needed to decide it is measured and on file (`client/docs/goon-game-census.md`, the four upstream `docs/GOON_*.md` contracts, and the shipped client tree), so this is not blocked on evidence - only on a human saying yes to a specific risk surface. WHAT IT WOULD OPEN, each measured rather than feared: (1) it is the port's FIRST outbound network boundary - today the only `HttpClient` here is a loopback probe, so this builds a boundary rather than extending one; (2) with no relay server a successful connection is DIRECT, so **each player learns the other's public IP address**, and that composes with a `/join` route that is free and needs no account; (3) a second channel, `goon-media`, carries **the user's own photos and videos to another person**, not to a service; (4) two of the eight routes are `/report` and `/blocked`, which are MODERATION - explicitly named as untouched by the owner's 2026-08-14 authorization, so admitting them would be reversing a standing decision rather than making a new one; (5) hosting is tier-2 gated behind an `X-Auth-Token` header, so it also depends on the still-open question of whether the port may TRANSMIT the borrowed login token. WHAT DOES NOT NEED ANY OF IT: practice mode, which already ships (`Features/Goon/`, 7 files / 1,810 lines, wired at `Views/Pages/PlayPage.axaml.cs:34,40`) and needs no network, no partner media, no mic and no moderation. THE HONEST POSITION: 'finish the port' is not a mandate to turn a single-machine feature into one that leaks a home IP address and ships private media to a stranger. That is a product risk decision about real users, it is cheap to say yes to later and expensive to unwind, and it should carry a human's explicit sign-off rather than arrive as a side effect of a completeness push. A join-as-guest-only subset is the smallest thing that could be admitted separately, because it needs neither the token nor the moderation routes - but it still exposes the guest's IP to the host, so it is a smaller decision, not a free one. |
| P1 | WIP | Make real-desktop pointer coexistence verification reliable or explicitly machine-gated. | MECHANISM FOUND AND HALF THE CAUSE FIXED; the residual is characterised and is a different cause. THE DETERMINISTIC HALF: the tray right-click gesture's only real Win32 call is `SetForegroundWindow` on the tray's OWNER window (`Tray/Win32TrayPresence.cs:604`), and that owner is HIDDEN. A hidden window cannot come forward, so the request can only fail - and the REFUSED request leaves the whole PROCESS unable to place any window in the topmost band afterwards, which is the overlay, the glyph surface and the video surface. Measured, not inferred: a later `SetWindowPos(HWND_TOPMOST)` returns TRUE and applies NOTHING, extended-style read-back identical before and after, 32 assertions running. Guarded on `IsWindowVisible`. VERIFIED BY INVERSION, the only way this one can be shown: with the guard removed the new fact AND the three `PointerCoexistenceTests` facts fail together; with it the combined filter is 23/23. The fifth hypothesis was the right one - the four the row already killed stay killed, and the earlier "the single trigger is `TheShellsRightClickNotification`" finding was an artefact of a seven-test filter: with `TrayCapabilityTests` excluded ENTIRELY the full suite still failed, which is what sent this to the second cause. THE RESIDUAL, and why this stays WIP: the suite is also sensitive to whole-machine contention, and thread count is NOT the variable. Measured on this box, same 2649 facts: 16 threads idle = green in 44.9s; 8 threads with one agent running = one real-desktop fact red; 8 threads again = green; 16 threads with twenty-one agents running = three red; `-parallel none` = green in 188s. A parallelism cap was therefore measured, considered and NOT TAKEN - it would slow every run for a condition it does not cure, and the 8-thread red proves it does not cure it. What remains is to decide whether the real-desktop collection gets a contention precondition it reports honestly, or whether the band assertions get a documented machine class. Do not weaken the click-through assertions to close this. |
| P1 | WIP | The floor gate reds four real-desktop facts; ONE of the two causes is now removed. | THE RUNNER CAUSE IS FIXED. The floor now runs each project as the xunit v3 ASSEMBLY with `-trx` instead of shelling out to `dotnet test`; that is the project type's own first-class run mode and emits the TRX this gate already parses. FLOOR OK on the merged tree: 2665/2665 and 154/154, 0W/0E. The evidence, back to back on ONE build, interleaved A-B-A to rule out drift: `dotnet test` 4 failed / 2643 passed; the assembly directly 0 failed / 2665; `dotnet test` again 4 failed. **I FIRST WROTE THAT THIS MADE THE FOUR A PURE RUNNER ARTEFACT AND THAT NO PRODUCT CHANGE COULD CLOSE THEM. THAT WAS TOO STRONG AND IS CORRECTED HERE.** A-B-A ran on a QUIET box. It establishes that VSTest adds a reproducible failure the exe did not show under those conditions; it does NOT establish that the exe is immune - and a concurrent measurement on this same machine, while several headed suites were running at once, reds the SAME four under the direct exe with the same read-back (`overlay-style-refused ... missing 0x8 of WS_EX_TOPMOST after 32 topmost assertion(s), last SetWindowPos(HWND_TOPMOST) SUCCEEDED`). SO THERE ARE TWO CAUSES AND ONLY ONE IS GONE: these are real-desktop Win32 facts that need an UNCONTENDED DESKTOP, VSTest was one cause and is removed, and DESKTOP CONTENTION IS THE SECOND AND IS STILL OPEN. That is exactly the "or explicitly machine-gated" half of the pointer-coexistence row's title, and it is what keeps this row WIP. NOTHING WAS RELAXED TO GET GREEN: the result-list anchoring, both staleness checks, the bad-outcome and bad-counter refusals, the exact-total pin and the name-anchored allowed-skips all still run, and the gate still never retries. Two narrow adjustments were needed. (1) The head check demanded an XML DECLARATION, which XML 1.0 makes OPTIONAL and the xunit writer omits; truncation is caught by the `</TestRun>` check, which is the half that ever detected a run dying mid-write, so both openings now pass and anything else still fails. (2) `WarningGateGuardTests.TheTestFloorStillRunsNoBuild_AndKeepsItsStaleBuildGuard` asserted the floor invokes `dotnet test`; its INTENT is "the floor must never become a builder", which running a prebuilt assembly satisfies structurally. It now accepts zero dotnet invocations OR the `test` verb, still rejects a non-literal argument list, and - because "no dotnet invocation" is also true of a GUTTED gate - additionally pins the direct-runner shape and the stale-build guard. Reintroducing a build in either shape still reds it. Bonus: the xunit counters are STRICTER than VSTest's - they close over skips (2663 + 2 == 2665) where the VSTest arithmetic did not, which is why the result-list anchoring existed in the first place. |
| P1 | OPEN | Validate real media and multi-monitor video behavior on Windows, X11, and Wayland as separately claimed. | Decoding, geometry, compositing, audio, teardown, and headed capture evidence for each supported backend. |
| P1 | OPEN | Maintain an honest upstream payload inventory after legacy changes. | Compare the current source payload, client payload, and inventory; update consumed documents and run the inventory tests. |
| P1 | OPEN | Port the Arcademy upstream surface, decomposed into its eight slices. | OWNER DECISION 2026-08-23: IN SCOPE. Sized against source rather than guessed: `Services/Arcademy/` is 2 files / 2,546 lines (`ArcademyHostService.cs` 2,008 incl. a 305-row lexicon table, `ArcademyMetaStore.cs` 538), and `Resources/web/arcademy/` is 88 files / 2.6 MB (74 JS totalling 27,555 hand-written lines, 1 HTML, 1 CSS, 11 binary assets, plus vendored three.js r185, MIT). THE COST IS THE C# HOST, NOT THE JS: the payload is served byte-identical the way dtrh/intake/tunnel/goon already are, and the client already carries `Avalonia.Controls.WebView` 12.0.1. A T2-gated WebView2 school of three daily mini-games, reached from the Play tab's MORE zone ('Attend'), never a tab. TWO TRAPS FOUND: (a) the payload does NOT stand alone - the shell borrows DTRH's spiral assets by relative URL (`shell/shell.js:76`, `games/the-deep-end/pressure.js:182`), so the whole `Resources/web` root must be mapped, not just `arcademy/`; (b) UTC seeds content but LOCAL date rolls attendance, which is upstream's own regression #978. BOUNDARIES: outbound network ONLY through the optional, consent-gated remote-media broker (slice 7, deferrable, and the one slice that widens a boundary); clipboard write (text only); local user media via a CORS-Allow origin honouring the Assets deselection blacklist; subliminal phrases read-only. NO webcam, biometrics, gaze, mic, moderation, capture or secrets. SAFETY: the panic ladder (slice 6) must NOT be deferred behind the suspension slice - without it two Esc taps exit the whole app. SLICES, in dependency order: (1) payload serving + boot handshake, (2) init projection + set-setting echo loop, (3) the meta store - the cleanest slice and the one upstream admits has no machine verification at all, (4) XP + attendance payout, (5) native-state suspension (needs video + browser-media services), (6) the panic ladder, (7) remote-media broker (optional, gated), (8) entry point + T2 gating. Slices 1-4, 6 and 8 are the honest minimum for a playable Arcademy; 5 and 7 have documented null paths and can no-op cleanly. **UPSTREAM SYNC v6.8.3 -> v6.8.4 (merge `6a8b4c724`) CHANGED THIS ROW'S SHAPE, and the change is a constraint rather than more work. THE DOOR IS SHUT UPSTREAM.** `ArcademyHostService.DoorAvailable` is a `static readonly bool = false` (`ArcademyHostService.cs:116`); the Play card ships `Visibility="Collapsed"` (`Views/Tabs/PlayTabView.xaml:1312`) and is re-asserted from `DoorAvailable` on every repaint (`MainWindow/MainWindow.PlayTab.cs:106-112`); and `Launch()` refuses on the door BEFORE the T2 bar (`ArcademyHostService.cs:136-141`). Upstream's own reason, quoted: Semester 1 "landed on main (PR #241) ahead of its public reveal" and 6.8.4 "is an auth/stability patch that must ship those fixes without also shipping an unannounced feature" - a HIDE, not a lockband, "because a lockband advertises something the account could buy". CONSEQUENCE FOR THIS ROW: porting Arcademy VISIBLE would ship a feature upstream is deliberately withholding, so slice 8 (entry point + T2 gating) now owns a door gate ahead of the T2 gate, and the honest default is hidden. It also means this surface CANNOT be headed-verified against the shipping app by a user - the door is shut there too. SIZES MOVED: payload 88 -> 91 files (`games/impulse-control/{casino,pressure,trickster}.js` from the Impulse Control House Rules wave, plus edits to index/lex/render/style/tube2d/tube3d), and `ArcademyHostService.cs` +51 lines including 13 new `ic_*` lexicon entries. The row's earlier 2,546-line and 88-file figures are superseded. **SLICES 1-2 LANDED 2026-08-23, AND THE DOOR SHIPS SHUT.** The 91-file payload is served BYTE-IDENTICAL as the sixth linked read-only `Content` glob, and the port is the FOURTH consumer of the same `LoopbackServer` that dtrh, intake and goon already run on - not a new server. THE MEASURED TRAP WAS SOLVED RATHER THAN PAPERED OVER: the payload does not stand alone, because its shell borrows DTRH's spiral assets by RELATIVE URL, so the serving root is the whole web mirror rather than the arcademy subtree - no asset was copied to make it work. The `.svg` MIME row went into the PAYLOAD table ONLY and deliberately NOT the user-media table, because an SVG served as a document can carry script and `/umedia/*` serves files a user dropped into their own folder; deny-by-default is unchanged and a new `.md -> 415` fact keeps that leg honest. `ArcademyDoor.Available` is a `static readonly false` with NO override seam, and `Attend()` refuses BEFORE any allocation - no participant, no bound origin, no port, no page - so this surface is unreachable exactly as upstream withheld it at v6.8.4. Floor +24 unit. NOT DONE: slices 3-8. NOT PROVED: no browser was started, no page loaded, not one line of the payload's JavaScript ran, no window opened - the handshake and echo loop are proved as FRAMES AND STORE EFFECTS, never as a page that received them. Nothing here says a class is playable. **SLICES 3-4 ALSO LANDED 2026-08-23.** The meta store is ported with the port's own persistence idiom, and its facts are the FIRST MACHINE VERIFICATION this behaviour has ever had - upstream ships none for it. THE TWO CLOCKS ARE NAMED APART, which was this row's stated trap: upstream reads UtcNow for the ledger and Now for attendance (its own regression #978), and the port carries the SPLIT rather than quietly fixing or quietly copying it - collapsing it is user-visibly wrong in BOTH directions, because one clock either pays twice every evening or freezes the streak at the wrong hour for most of the planet. It is explicit in the type as `ArcademyClassDay(UtcSeedDay, LocalAttendanceDate)` - no bare `string date` exists anywhere on the path - and the fact drives ONE injected instant that falls on two different days. Verified by MUTATION: handing the UTC day to the attendance call reds it with the wrong streak. The payout has nowhere to land and SAYS SO rather than inventing an XP economy to receive it. A real .NET 10 defect surfaced on the way: `[JsonExtensionData]` over a `JsonObject` deserializes but throws on WRITE, so every read of the blob would have died; 18 of 22 facts caught it at once. Floor 2756 -> 2778. STILL NOT DONE: slices 5-8, and nothing here is reachable - the door remains a `static readonly false` with no override seam and no fact flips it. NOT PROVED: no browser, no page, not one line of the payload's 27k lines of JavaScript ran, no headed evidence, no Linux run. **SLICES 6 AND 8 LANDED 2026-08-23; THE HONEST MINIMUM (1-4, 6, 8) IS NOW COMPLETE.** Slice 6 was done FIRST because this row calls it a safety slice: the panic ladder, so an Esc inside a class cannot reach the application. Every rung is mutation-checked, and the ORDER fact counts authority calls (zero) rather than reading code, so reordering the gates makes it one. **AN HONEST LIMIT, recorded rather than papered over: the port has no app-wide panic hook to hand a press to, so the fall-through this row feared cannot occur today because the fall-through TARGET does not exist yet** - the rung above it is written and proved for when it does. Slice 8 puts the T2 bar BEHIND the door in upstream's own order (the door is checked synchronously before the first await), and the Play-tab entry ships HIDDEN and is re-asserted on every mount; two headless facts prove it mounted and unreachable, and revealing the strip reds both. TWO SOURCE-VERSUS-SPEC CORRECTIONS TO THIS ROW: `resume-request` is slice 6's frame, NOT slice 5's - upstream refuses every reason but panic, because a video suspend lifts when the video ends and an audio-only one when the session does; and the AudioOnlySession gate is deliberately NOT ported, because this build has no audio-only session for it to refuse and a hard-coded false would be a gate pretending to have an input. Floor 2778 -> 2794 unit, 159 -> 161 headless. REMAINING: slices 5 and 7 only, both with documented null paths. NOT PROVED: no browser, no page, not one line of the payload's JavaScript, no window, no key pressed on a real desktop (there is no hook to press into), no headed evidence, no Linux run. |
| P1 | OPEN | A not-ported upstream payload tree can grow by any amount with the whole suite green. | EXPOSED BY THE v6.8.4 SYNC, and it is the fourth time a payload guard has been the only thing between this port and a silent upstream surface. `Resources/web/arcademy/` went 88 -> 91 files in this sync and `UpstreamPayloadInventoryTests` stayed GREEN at 19/19, because `fileCountAtBaseline` is recorded as "record data, not an assertion" and the per-tree manifest tests that DO pin counts only exist for trees the client actually serves. So the guard catches a tree ARRIVING and a tree DISAPPEARING, but not a tree CHANGING - which is the state every not-ported tree is in, permanently, until its row lands. The inventory has been corrected by hand to 91 (`client/docs/upstream-payload-inventory.json`), which is exactly the hand-maintenance the citation row is currently removing elsewhere. Wanted: a mechanical count/hash per not-ported tree that reds on drift and names the tree, so the next wave's arrival is a test failure rather than something a human happened to diff. Do NOT close this by pinning counts the sync then has to bump silently - the bump must carry its reason the way `floor.json` does. |
| P1 | WIP | Regenerate the upstream citation inventory, which is two syncs behind the tree it validates against. | THE TOOL IS BUILT, which was the owner's instruction and is the half that turns bulk into tooling. `client/tools/citations/detect.mjs --regenerate` recomputes `changedAtSync` for every entry from GIT ALONE - add/del via the same `readNumstat` DELTA-MISMATCH re-measures with, so a regenerated inventory leaves that class silent by construction; status from `git diff --name-status` over the same window; `sync` from the committer date of the window's endpoint. It writes NOTHING without `--write`, and the write is BYTE-SURGICAL: only the value span after each `changedAtSync` key is replaced, in the file's own line ending at the key's own indent, and the produced bytes are re-parsed and compared against the intended inventory before anything reaches disk. It never touches `baseline` and never touches a `verdict`, which is what keeps it clear of the trap this row names - advancing `baseline.previous` moves the needle mode onto a window holding zero rows and makes `TheNeedleMode_ExitsZero_WithANonEmptyReviewList` vacuous. INDEPENDENT REVIEW FOUND THE MODE'S NAMED REFUSALS WERE THEMSELVES DELETABLE: three of the four - the rename ceiling the operator is told is COUNTED rather than guessed at, the BINARY-numstat refusal (whose removal writes `add: null, del: null` that the writer's own verification provably cannot catch, because `Number("-")` is `NaN` and both sides stringify to `null`), and the re-parse-and-compare itself - could each be mutated away with the suite fully green. All three are now pinned by facts F30-F32, and each mutation was shown to red EXACTLY its own fact and no other. Self-test 33/33; floor unchanged at 2673/154 because these are node facts. REMAINING FOR DONE: run the regeneration for real over `db3e842f..fb89d087a` (measured dry: 127 of 297 entries move, 33 rewritten / 21 added / 73 dropped, with 4 rename destinations hitting the named plain-numstat ceiling), and then the 81 tier-1 VERDICTS, which are judgment and are the only part the tool cannot do. |
| P1 | WIP | Hosted pages silently lose their motion when Windows animation effects are off. | LANDED, and the row stays WIP on headed evidence rather than on code. `MotionLevel {Full,Reduced,Off}` is ported with its picker on the System door, default `Full` from first run, persisted by ORDINAL (upstream's, and the picker order is load-bearing because it IS the persisted value - pinned by a fact that writes the int and reads it back). All five hosted surfaces route through ONE helper and a choke-point guard asserts both halves: the switch literals exist in exactly one file, and every one of the five still calls it - a surface that quietly stopped would fall back to the OS answer, which is this row's whole defect. The OS flag is READ AND LOGGED, never obeyed, so the user keeps an in-app restore; that is what makes this a fix rather than the accessibility regression the naive version would have been. INDEPENDENT REVIEW CAUGHT A RED THE LANE'S OWN FILTER COULD NOT SEE: joining the flag with the separator INSIDE the string literal defeats `HarnessEntryPointGuardTests`, which captures startup flag literals verbatim, so all five surfaces failed it while a `~MotionLevel` filter stayed green. Fixed by joining the way upstream joins. Floor 2649 -> 2665 and headless 152 -> 154; both suites green. REMAINING FOR DONE: (1) headed evidence on a machine with Windows animation effects OFF - nothing here observed a hosted page moving or not moving; (2) the review's two named test holes, an OS-flag seam so `BrowserArgument` is pinned OS-INDEPENDENTLY (today a mutant that let the OS win still passes on a machine with animations on) and a vacuous `Assert.All` over an empty list; (3) `Reduced` currently has no consumer beyond the hosted flag, so choosing it changes nothing the port renders itself - the picker ships a level with no local effect and the System-door blurb over-claims scope in its first sentence. Linux is out by construction: the switch reaches only the WebView2 branch and the GTK branch takes no Chromium switches. |
| P1 | DONE | A Brain Drain clip is cut off mid-track instead of playing to its end. | FIXED. `Effects/AudioCueEffect.cs` now skips a due window while the module's own slot is still sounding, taken BEFORE the draw so it can never read as "rolled and lost" - the same place and shape as upstream `1f8fe465b` (`if (_waveOut != null) return;`). The guard is OPT-IN (`SkipWhileSounding`, default false) and only `Effects/BrainDrainEffect.cs` turns it on: upstream fixed Brain Drain alone, and Mind Wipe's displacement is still correct, so a guard on the shared base would have silently changed a behaviour nobody asked about. Required a new presence query - `IAudioPresence.IsSounding(slot)` asks the PLAYER for its state, NOT whether the slot dictionary has a key: slots are cleared only by displacement or Silence and nothing sweeps a naturally-finished player, so an occupancy check would have answered true forever and made Brain Drain play one clip per session. Verified by inversion: the fact asserts Brain Drain skips, Mind Wipe still replaces, AND Brain Drain resumes once the clip ends, and was shown FAILING with the opt-in off. |
| P2 | DONE | Reveal in Explorer opens the Desktop instead of selecting the file. | FIXED. `Features/Dtrh/DtrhLoomReveal.cs` built `/select,"<path>"` as one already-quoted string and passed it to the string-`Arguments` overload; the CRT re-parses those quotes before explorer sees them and, on a mismatch, explorer silently opens the Desktop rather than reporting an error. Now one pre-tokenized `ArgumentList` element (`/select,<fullpath>`, unquoted - the runtime applies Windows quoting), a `Path.GetFullPath` so `/select` does not resolve against the launching process's working directory, and a missing-file fallback to the containing folder. The Linux `xdg-open` leg carried the same pre-quoting and is fixed with it. Verified by inversion: `DtrhLoomStudioTests.Reveal_ExistingSpiral_LaunchesOsSeam` now pins the exact token and rejects any quote, and was shown FAILING against the restored defect before being accepted. The old assertion was `Contains(path, args)`, which is true of the broken shape too - that is why nothing caught this. |
| P2 | DONE | Finished audio players are never released from their slot. | FIXED. `Audio/WasapiAudioPresence.cs` now subscribes `IAudioPlayer.PlaybackEnded` and releases the slot on a natural end. The subscription is taken BEFORE `Play()` and that ordering is the correctness argument, not a preference: `Play()` only queues the device thread, so the event can only arrive from that thread, which must take the gate the publish is holding - there is no window in which an end can be missed or seen against a half-published slot. Ownership is the SLOT: the handler removes and disposes only if the slot still holds THAT player, so a late end from an already-displaced player does nothing rather than evicting its replacement or double-disposing. Enabling change in the harness: `StubPlayer.PlaybackEnded` was a no-op `add {} remove {}` pair, so no fact could express a clip reaching its end - that is why this survived. It is a real event now. Verified by inversion TWICE, independently: removing the subscription reds it, and weakening the ownership check from reference-equality to occupancy reds it on the race half. |
| P2 | DONE | Brain Drain clips added while the app runs are never picked up. | FIXED, and the root cause was narrower than the row assumed: `AudioCuePool.Invalidate()` existed and was correct, but it was NEVER ON `IAudioCuePool`, so nothing the product holds could reach it - a re-scan mechanism only the tests could call. It is now on the interface, and `Effects/AudioCueEffect.cs` re-reads the folder at the arm hook (`Ready`, the only hook that runs on an arm) behind an opt-in `RescanOnArm` that only `BrainDrainEffect` turns on. That asymmetry is upstream's: Brain Drain re-scans on every session start because its folder is a drop target outside the app (`BrainDrainService.cs:222-227`), while Mind Wipe re-scans only from its panel and never from Start. Verified by inversion: the fact asserts Brain Drain re-scans, Mind Wipe does not, and the re-scan happens per ARM rather than once per process (stop, start, read again), and was shown FAILING with the opt-in off. |
| P2 | DONE | Audit uncited intra-client source references and correct stale behavior claims. | DETECTOR BUILT AND THE TREE CLEANED. `client/tools/citations/intra.mjs` is a SEPARATE tool rather than a fourth mode of `detect.mjs`, and the deciding reason is the exit contract: `detect.mjs` is pinned three times over to exit 0 on a populated list, because a changed UPSTREAM file is not automatically a defect. An intra-client citation is checkable against a file in the same repo at the same commit, so this one MUST exit non-zero and can. Everything reusable is imported from `detect.mjs`, not copied. Four classes: WRONG-LINE, DEAD-ANCHOR, UNRESOLVABLE, AMBIGUOUS-BASENAME, with exit 0/1/2 separating clean from rot from could-not-run so a broken detector never reads as a verdict. FIRST RUN FOUND 163 ROT SITES, all now fixed: 64 dead anchors into the divergence register `1bdf998e4` deleted, 14 into two documents deleted at `734741fb7`, 13 wrong lines (eight into `task-board.md`, which is rewritten every wave - those now name the ROW rather than a line, which is the root-cause fix rather than a re-count), 72 ambiguous basenames (`bridge.js` alone answers to FOUR real files), and 13 stale `AdmittedRoutes is empty` prose sites. SEVEN OF ITS 28 FACTS ARE MUTATIONS, each rewriting one uniquely-occurring span and asserting the fixture then produces the row the refusal was suppressing - with an exactly-once guard so a drifted mutation fails loudly instead of testing nothing, which is the defect that blocked the sibling tool's review. IT IMMEDIATELY EARNED ITS KEEP: it caught rot created by the haptics merge in this same session, three symbol citations whose lines had moved, before that merge was pushed. Now exits 0 on this tree - 675 intra-client references, 672 resolved clean, 0 rows. WHAT IT DOES NOT CHECK, counted rather than implied away: 2,967 bare `:NNN` continuations, because binding those needs the nearest-preceding-token heuristic `detect.mjs` measured and rejected - which is exactly the class I MYSELF misread earlier this session when I wrongly called a correct `GoonProtocol.cs` citation rot. |
| P1 | WIP | Take the Linux evidence the board has been deferring, now that a host exists. | **LINUX IS GREEN: 2,716 unit / 0 FAILED / 66 skipped, and 155 headless / 0 failed / 0 skipped.** It began at 88 failures and went 88 -> 74 -> 6 -> 2 -> 1 -> 0. Host: WSL2 Ubuntu, .NET SDK 10.0.400, on a NATIVE ext4 clone at `~/ccp` rather than the `/mnt/c` checkout - deliberately, because a Linux build over `/mnt/c` overwrites the same `bin/Debug/net10.0` assemblies `check-floor.mjs`'s `assertBuildIsFresh` measures, and the two platforms would have lied to each other. Build: 0W/0E across all four projects. WHAT THE RUN ACTUALLY BOUGHT, because a green number is not the point: it found FOUR REAL DEFECTS that no amount of Windows testing could have surfaced. (1) A Windows path separator reaching a USER - `System.IO.Path` answers with the HOST's rules, so `GetFileName` of a Windows-authored path returns the whole string on Linux and a `C:\...` literal appeared in a message on a machine with no `C:` drive; five product sites fixed behind `PortablePath`, with a source guard because the behaviour facts CANNOT invert on Windows. (2) THE REAL-DESKTOP LEASE WAS NOT A LEASE - .NET maps every `FileShare` mode except `None` to `LOCK_SH` on Unix, so two runs could both hold it; now `FileShare.None`, with the fixture taking the lease a second time while holding it and refusing loudly if that succeeds, because `flock` is advisory and NFS/SMB/DrvFs would otherwise silently stop excluding. (3) A byte-count census measuring the CHECKOUT rather than the content - git normalises text, so the same commit was 12,471,900 bytes on Windows and 12,376,646 on Linux, a difference of exactly the CRLF count; now counted CRLF-as-LF and identical on both. (4) `process.exit()` in both citation tools THROWING AWAY THE REPORT - on POSIX node's stdout is asynchronous when it is a PIPE and synchronous when it is a file, so a 975-line report lost its tail exactly when something read it programmatically; Windows never showed it because pipe writes are synchronous there. THE 66 SKIPS ARE THE HONEST HALF: 60 real-desktop facts are gated to the platform they measure, because off Windows their readings do not go absent, they go WRONG - every handle is 0, so a comparison like `IsForeground` becomes `0 == 0` and answers YES about a window that was never created. Nine further facts were KEYED instead of gated and now assert the honest Linux refusal rather than stepping aside. Every skip is in `floor.json` `allowedSkips` with the machine class where it executes, derived from the MEASURED skip list rather than transcribed. WHAT THIS STILL DOES NOT ESTABLISH, and it is most of what Linux support means: no presentation, input, focus, window behaviour or media is verified on Linux by ANY of this - the gated facts stop measuring there rather than starting to, which is exactly why they are gated and why this row stays WIP. WSLg is present (`wayland-0` and `X0` both live), so a headed Linux leg is now reachable for the first time, but a nested Weston compositor with no desktop environment is not GNOME or KDE and must not be reported as either. X11 and Wayland still need separating. |
| P1 | OPEN | The headed capture harness reports CAPTURE PASS on an all-black image. | FOUND WHILE ATTEMPTING THE FIRST HEADED LINUX EVIDENCE, and it is a vacuous green in the one harness whose whole job is to be non-vacuous. `client/tools/verify/capture-wslg.sh` ran, probed the door's real geometry (`175.0x44.0 DIP @ scale 1 @ screen 12,45`), wrote a correctly-sized BMP and printed **CAPTURE PASS** - and the image is 7,700 pixels of a SINGLE COLOUR, `(0,0,0)`, 100% black. A capture that contains no second colour cannot be evidence of anything drawn, so PASS is the wrong answer and the harness should refuse. Reproduced in THREE display configurations, so it is not a one-off: WSLg RAIL (Wayland), WSLg with `WAYLAND_DISPLAY` unset so Avalonia takes XWayland, and a real `Xvfb :99` framebuffer where `XGetImage` unquestionably holds pixels. WANTED: the capture step fails when the image carries fewer than two distinct colours, or when it is entirely the background, with the count in the message. The tier-3 `CcpVerify` checks DID catch it downstream - `rail-door-selected-border 0/525 pixels matched` - so the named checks are honest and it is the capture step's own PASS that lies. Note the ordering trap: `CcpVerify` was invoked through `dotnet run`, whose exit code is the RUNNER's, so a caller reading `$?` sees 0 even on FIRST FAILED CHECK; any harness that gates on this must read the printed verdict or invoke the built assembly directly. |
| P1 | DONE | The shell renders a diagnostic footer to the USER, unconditionally. | FIXED. `DiagnosticFooterPolicy.Rendered` is `#if DEBUG`, the shell asks it (`MainWindow.axaml.cs:52`), and a published build no longer draws the footer. THE CHANNEL SURVIVES DELIBERATELY: the probe is still built, still UIA-readable on Windows, and still logged once on first layout so it stays stderr-readable on Linux - which is the ONLY reading this port has of its rail geometry on a platform where every capture comes back black. Deleting the probe would have taken the Linux leg's evidence with it, so only the RENDERING is gated. The guard (`DiagnosticFooterGuardTests`) is a SOURCE guard by necessity: visibility is decided by `#if DEBUG` and every test run IS a Debug build, so a behavioural assertion could only ever observe the visible arm and could never fail against the defect. It pins BOTH halves - the gate must exist (a literal there would pass every behavioural fact in this suite while restoring the defect exactly) AND the probe line must survive, so the guard cannot be satisfied by deleting the channel. Verified by inversion: replacing the policy call with a literal reds it. The headed harness still passes unchanged. Floor 2755 -> 2756. |
| P1 | DONE | Headed evidence on Windows: obtained, checked, and no longer a standing gate for the rail. | THE PORT'S FIRST VERIFIED HEADED CAPTURES. `client/tools/verify/capture.ps1` launched the real shell (window up in 6.3s), all five rail doors published their layout probes, the System door was reached BY REAL INPUT, the state was driven by a real left-click on the Companion door (confirmed `route: companion` by a UIA read BEFORE any pixel was read), the screen read was fenced through `DwmFlush`, and the machine-wide real-desktop lease was taken and released around it. TWO SURFACES FULLY CHECKED by `CcpVerify`'s ten named checks: `rail-door/selected` ALL CHECKS PASSED with the border at 888/918 pixels (0.967), and `rail-door/unselected` ALL CHECKS PASSED at 896/918 (0.976). The captures carry 59 distinct sampled colours - the dark-neon ground `42,33,48`, the accent `224,102,255`, near-white text - so the theme is proved to RENDER rather than merely to be declared. A full-window `dashboard/selected` capture (1925x1330) was also taken and shows the shell, the five doors, the Companion page and the action bar; `checks.json` DEFINES NO CHECKS for that surface/state pair, so it is a picture and not evidence, and that gap is worth its own row rather than a quiet pass. WHAT THIS DOES NOT DISCHARGE: Linux, where every capture is still 100% black; and every OTHER surface - the Trainer Card, the feature popups, the hosted pages - which have no checks and no captures. |
| P1 | BLOCKED | Headed Linux evidence: the app RUNS and LAYS OUT on Linux, but no pixel has ever been read back. | ATTEMPTED 2026-08-23 AND HONESTLY NOT OBTAINED - recorded as the exact unavailable gate rather than left implied. WHAT IS PROVED: the client starts fully on Linux under WSLg. Its own startup log shows the dtrh payload Present (1,542 files), the loopback page and media origins bound on 127.0.0.1, and ALL FIVE navigation doors laid out with real DIP geometry at real screen coordinates (studio 12,45 - companion 12,95 - play 12,145 - intake 12,195 - system 12,245). Every capability refused honestly on the platform: `camera: settings restored and NO camera was enumerated or opened - consent not current, gaze engine not implemented in this build`, `entitlement: unavailable(unsupported-platform)`, `haptics: disabled in settings - no provider was contacted`. That is real Linux behaviour and it is the first time any of it has been observed. WHAT IS NOT PROVED, AND MUST NOT BE REPORTED AS PROVED: not one pixel. Every capture came back 100% black in three separate display configurations (WSLg RAIL/Wayland, XWayland with `WAYLAND_DISPLAY` unset, and a real `Xvfb` framebuffer), so nothing here establishes rendering, compositing, theming, focus, input or window behaviour on Linux. LAYOUT IS NOT RENDERING: the geometry probe reads the visual tree, which is exactly what a headless run already gives, so the capture adds a file and no evidence. NEXT: find why the surface has no readable content - a compositor/backend question, not a product one on this evidence - or name a different Linux host (a real desktop session rather than a nested RAIL compositor) as the gate. Do NOT close this by loosening the checks; the checks were right and the capture was empty. |
| P1 | OPEN | A Windows path separator reaches the user on Linux. | FOUND BY THE FIRST LINUX RUN, in product code rather than in a test. `IntakeHostSupportTests` expects `a_b_c` and gets `a_b\c` - a backslash treated as a literal instead of a separator - and `SpiralSurfacePresenterTests` puts `C:\spirals\classic.gif` into a message a USER READS, on a machine with no `C:` drive. These are not test-gating problems: they are Windows assumptions compiled into the port's own logic, and they are the class the constitution means when it says a build is not evidence of Linux support. Wanted: the two sites fixed at the source (`Path.Combine` / `Path.DirectorySeparatorChar` rather than a literal, and a display path that does not assume a drive letter), plus a look for the same shape elsewhere - two found by accident in a suite that mostly could not reach Linux code paths at all is not a reason to believe there are only two. |
| P1 | OPEN | The real-desktop lease is not a lease on Linux. | FOUND BY THE FIRST LINUX RUN, and it is a MECHANISM defect rather than a test one. `RealDesktopLeaseTests` and `RealDesktopLeasePrimitiveTests` assert that a second open of the held lease returns null; on Linux it returns a live `FileStream`. The lease relies on WINDOWS EXCLUSIVE-OPEN semantics, and POSIX advisory locking does not provide them - so on Linux two concurrent runs could BOTH believe they hold the desktop, which is precisely the condition the lease exists to prevent and precisely the condition this board has spent days chasing on Windows under the name 'desktop contention'. `CompanionViewModelTests` is the same finding from the other side: a delete that FAILS on Windows because a file is locked SUCCEEDS on Linux, so the 'could not be deleted' branch is unreachable there and the product's error path is untested on that platform. Wanted: a lease primitive whose exclusion is real on both platforms (`flock`/`fcntl` on POSIX, or an approach that does not depend on open semantics at all), or an honest refusal that says the lease cannot be held on this platform. Do NOT close this by relaxing the assertion to match Linux - the assertion is right and the mechanism is what is missing. |
| P2 | OPEN | About 66 real-desktop facts assume Windows without ever saying so. | MEASURED BY THE FIRST LINUX RUN. Of 88 Linux failures, roughly 66 are facts whose PRODUCT behaviour is correct and whose EXPECTATION is Windows-only: `GlyphCapabilityTests` (21) dies on a bare `DllNotFoundException` for `user32.dll`, and Video (18), Pointer (9), Input (5), Flash (4), Glyph coexistence (4), Overlay (1), the two surface presenters and `RealClipDecodeTests` (6) each get a TYPED absent-mechanism refusal (`overlay-mechanism-absent`, `video-mechanism-absent`, "the Windows decoder was asked for from a process that is not on Windows") where the fact demands the Windows answer. The port is being honest and the tests are not. This matters beyond tidiness: while these are ungated, the Linux column of this suite can never be green, so no Linux regression can ever be SEEN - the noise floor is 66. Wanted: each gated to the platform it actually measures, in the shape `floor.json`'s admission rule already requires (a machine/OS property, named, with the machine class where it executes) - NOT a blanket skip, and NOT a weakened assertion. The Linux-gated counterparts that already exist (`SecretStoreTests.LinuxProbe`, `ChaosTunnelCapabilityTests.Linux_*`) are the precedent and they executed correctly on this run. |
| P2 | OPEN | Extend deterministic test-wait coverage to unbounded joins and injected timeout budgets. | A focused source guard plus tests that fail on the previously invisible shapes without adding timing sleeps. |

## Completeness Backlog

**The queue above was not the remaining work, and this section exists because nothing in the
repository said so.** A cluster-by-cluster sweep of the shipping product against `client/src` and
against this board found **115 user-visible behaviours that are absent or partial here and that no row
covered**, across 161 behaviours examined. Each one is recorded in
`client/docs/port-completeness-census.md` with a WPF citation, the grep that proved absence, and a
measured size.

**How this happened is traceable, and it is not a mystery.** `client/docs/wpf-surface-reachability.md`
was 1,906 lines carrying a divergence register with 326 distinct `D` anchors that enumerated exactly
these gaps. Commit `1bdf998e4` ("Refactor tests and documentation for clarity and accuracy") cut the
file to 95 lines and deleted the register whole. The enumeration existed, was deleted, and never became
rows - and 28 client source files still cite section and `D` anchors that no longer resolve.

**These rows are P2 because they are SCOPE DECISIONS, not because the behaviour is unimportant.** None
of them is work yet. Each entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its
own row with its own evidence and verification before any code is written. The line counts are WPF
surface measured with `wc -l`; they indicate the size of the behaviour, **not** the cost of porting it,
because this port reproduces outcomes rather than files.

| Priority | Status | Outcome | Required Evidence |
|---|---|---|---|
| P2 | OPEN | Scope the **Application shell and its windows** cluster: 16 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 16 entries is ~54,944 lines (largest: The dialog layer; Home / Dashboard tab (the app's opening page) + its embedded browser panel; Guided tutorial system). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Game modes and special surfaces** cluster: 6 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 6 entries is ~47,436 lines (largest: Deeper; AvatarTube; Embedded browser card on the Home page). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Companion, AI and autonomy** cluster: 17 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 17 entries is ~43,846 lines (largest: Awareness: she notices what you are doing on the PC and reacts to it; She talks during the whole app, with a voice (the bark engine); Awareness privacy surface: the dial, the app picker, the hour-long pause, the wire view). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Session, scheduling, progression and economy** cluster: 10 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 10 entries is ~37,044 lines (largest: Training Programs; Scripted session runs; Skill tree / Enhancements). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Content, mods, assets and presets** cluster: 11 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 11 entries is ~31,223 lines (largest: Active mod (persona pack) switching; Mod Creator studio (author your own mod); Assets tab: asset browser, asset presets, phrase presets, and drag-drop asset import). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Account, updates and platform services** cluster: 9 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 9 entries is ~23,449 lines (largest: Multi-day Training Programs (the Programs tab); Remote control from a phone or web controller; Bug report / suggestion submission with redaction preview). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Visual effects and overlays** cluster: 14 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 14 entries is ~13,989 lines (largest: Unified overlay compositor; Native UI motion and ambient FX (the things MotionFx gates, and FxTheme colours); Spiral and Pink Filter panel controls). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Video, media and webcam** cluster: 12 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 12 entries is ~12,424 lines (largest: Webcam sensor events other than positional gaze; For You Feed (ghost mode, mirror opacity, Show-Desktop survival, decode notice-and-swap, the feed page itself); Online and direct-URL video (remote clips, per-mod video link sets, the web-video takeover)). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Audio, speech and verbal conditioning** cluster: 12 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 12 entries is ~11,774 lines (largest: Offline microphone stack and the 'She's Listening' panel; App-wide bark/quip engine; Audio-synced haptics from a web video). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |
| P2 | OPEN | Scope the **Input, attention and detection** cluster: 8 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 8 entries is ~11,005 lines (largest: Awareness Engine; Blink Trainer; On-screen keyword highlight overlay). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. |

## Completion Criteria

A row is complete only when its behavior, implementation, required Windows/Linux evidence, and
recorded verification agree. A failed or unavailable gate keeps the row `WIP` or `BLOCKED`; it does
not become a successful claim through a compile, stub, no-op fallback, or workflow report.
