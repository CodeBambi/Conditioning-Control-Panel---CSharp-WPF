# Client task board

This is the only live queue for the greenfield Windows/Linux client.

Do not import completion claims, test floors, parity percentages, or row states from the first Avalonia attempt.

Execution engine: **pi-spine** (owner decision 2026-07-18 — see gate history and `port-workflow.md` §Extension admission). Phase scope and owner-held gates live in [`spine-tasks/CONTEXT.md`](../../spine-tasks/CONTEXT.md); its Phase 0 gates apply before any batch.

## Status values

- `OPEN`
- `WIP`
- `BLOCKED`
- `DONE`

## Queue

No implementation rows have been approved yet. Bootstrap must first inventory WPF behavior, capture lessons from the first attempt, research current Avalonia v12, and propose the smallest solution shape.

| Priority | Status | Work item | Acceptance evidence |
|---|---|---|---|
| P0 | WIP | Bootstrap discovery and architecture proposal | **SP-002 executed 2026-07-18:** proposal at `client/docs/architecture-proposal.md` (single-head topology, Avalonia 12.1.0/net10.0 baseline, per-A-### lesson dispositions, 3 flagged owner questions) + minimal `client/` scaffold green with 0 warnings on Windows and WSL2 Ubuntu 26.04 (SDK 10.0.110); evidence in `spine-tasks/SP-002-bootstrap-architecture/record.md`. **Owner review of the proposal is the remaining gate** — row stays WIP until the owner ratifies. Original acceptance: WPF capability inventory, first-attempt lessons, current v12 sources, and an owner-reviewed Windows/Linux solution proposal |
| P0 | WIP | Define startup, shutdown, and integration contract | **SP-003 executed 2026-07-18:** contract at `client/docs/startup-shutdown-contract.md` (4 ordered cancellable phases, typed failure taxonomy, manual-construction composition root with fail-fast validation, one-owner-per-participant rule, single guarded teardown entry point for close/startup-failure/panic, single-instance carved out as seam-only, `TerminateProcess` analogue rejected); 23/23 tests green, 0 warnings; headed Windows smoke observed the rendered phase trace + clean exit 0; evidence + both Fable consult verdicts in `spine-tasks/SP-003-startup-shutdown-contract/record.md`. **Land:** batch `20260718T212127`, integrate `eb801810` (2026-07-18) — orchestrator re-ran the contract testCommand on the exact merge content (23/23, 0W/0E) because the gate's own evidence artifacts were stale (see gate history). **Remaining gate:** ~~Windows-only evidence~~ — WSL2 unit-test debt CLOSED 2026-07-19 by SP-005's in-packet WSL2 run (full suite incl. this slice's tests, 51/51, 0W/0E, Ubuntu SDK 10.0.110; see SP-005 record.md). Headed Linux smoke (WSLg) still unproven — remains with the Release/publish row. Original acceptance: A minimal bootstrap slice has ordered cancellable startup phases, composition-root validation, typed initialization failures, one owner for each background participant, panic/close/startup-failure teardown, and an end-to-end proof that registered code is reachable from a user path; no global service locator or constructor-started background work |
| P0 | WIP | Define persistence and migration contract | **SP-005 executed 2026-07-19:** contract at `client/docs/persistence-migration-contract.md` (one schema/version authority with DOM-level `JsonNode` version read; System.Text.Json tolerance stack decided — Newtonsoft rejected as new package admission, revisit trigger recorded; atomic temp+rename+flush-to-disk write with injection points; single serialized writer as SP-004 owned operations via chained writes; corruption quarantine preserving original bytes with typed Degraded + flagged defaults, never silent; unknown-member preserve via `[JsonExtensionData]`; migration journal + idempotence with SchemaVersion as run-authority; replacement notification raised before persist with documented caller-context delivery; secret-exclusion seam `ISecretStore` declared-only; import/backup boundary contract-only; debounce policy-only; Core deferral with revisit trigger; newer-than-app schema lockout; defaults-never-auto-saved; **SP-003's reserved teardown flush slot ACTIVATED** — bounded flush before generation-cancel/participant-stop, panic-path attempt-flush policy stated); 51/51 tests green on Windows AND WSL2 Ubuntu 26.04 (SDK 10.0.110) — the first in-packet Linux gate, closing the rows-2/3 missing-Linux debt pattern for this slice; 0 warnings; both Fable consult verdicts + engine-review absence (skipped ×3 — T-2) in `spine-tasks/SP-005-persistence-migration-contract/record.md`. **Land:** batch `20260719T010403`, integrate `0c2c849f` (2026-07-19) — 51/51 verified by orchestrator re-run on the exact merge content (gate evidence stale, T-3 3rd occurrence; gate history). **Remaining gate:** owner ratification only — Windows AND WSL2 Linux evidence delivered. Original acceptance: Specify one schema/version authority, atomic replacement and flush, serialized writes, corruption quarantine/recovery, unknown-member policy, migration journal/idempotence, settings-object replacement notification, secret exclusion, import/backup boundary, and failure-injection tests before broad settings models are added |
| P0 | WIP | Define truthful runtime capability contract | **SP-006 executed 2026-07-19:** contract at `client/docs/runtime-capability-contract.md` (six typed states incl. Degraded-with-named-surviving-semantics/PermissionRequired/DependencyMissing/Faulted; probe rule — Available only from a runtime probe of the selected backend in the current environment, OS checks/DI registration/assets/stubs/no-op fallbacks/swallowed exceptions NEVER claim support; declared evidence class per capability with downgrade-only environment evidence; honesty rule — faking availability is a contract violation and a test requiring it is defective; row-3/row-5 boundary activated with the outcome→state mapping as the single bridge; fallback policy explicit-optionality-only; session facts never backend claims) + `Capabilities/` implementation (registry: registered-unprobed → Unavailable(not-probed), unknown → Unavailable(unknown-capability); probes as SP-004 owned operations in a dedicated `CapabilityProbes` phase; two demonstrators: session-type probe with typed SessionKind and XWayland distinction, atomic-filesystem probe performing REAL I/O with mount-table downgrade-only). 78/78 tests green on Windows AND WSL2 (0W/0E both); **WSL2 observed states recorded: WSLg session → Available(LinuxWaylandWithX11, no-backend-claim disclaimer), ext4 → Available(real I/O verified), /mnt/e DrvFs → Degraded(durability-unverified, 9p) — the truthful degraded report IS the honesty proof**; headed Windows smoke observed capability states via UIA, exit 0; pre-approach + pre-completion Fable consults applied (3 claimed-but-untested gaps closed), engine reviews skipped ×3 (T-2); evidence in `spine-tasks/SP-006-truthful-capability-contract/record.md`. **Remaining gate:** owner ratification — row stays WIP. Original acceptance: Windows, Linux X11, and Linux Wayland capabilities return typed availability/degradation/failure reasons based on the selected backend's runtime probe; OS checks, DI registration, assets present, stubs, external-browser/no-op fallbacks, and swallowed exceptions cannot claim support |
| P0 | OPEN | Define asset and packaged-output manifest | One catalogue covers logical IDs, case-sensitive paths, embedded/copied/user/mod sources, override policy and trust, optionality, provenance/license, and target heads; Debug, Release, and publish tests open every required first-slice asset from packaged Windows/Linux output |
| P0 | WIP | Establish async lifecycle and fault policy | **SP-004 executed 2026-07-18:** contract at `client/docs/async-lifecycle-fault-contract.md` (registry-owned operations: one owner, per-owner monotonic cancellation generation, owned completion task, typed `OperationOutcome` reusing the SP-003 failure-kind vocabulary — Recoverable/Degraded activated as per-operation classifications; Post-only `IUiDispatch` late-bound in phase 4, pre-binding `Post` throws, demonstrator skips-until-bound; stale-generation completions discarded at application; teardown = cancel generations → drain owned completions → record unobserved → reverse stop through SP-003's single guarded entry point, no second path); 34/34 tests green (23 SP-003 intact + 11 new), 0 warnings; headed Windows smoke observed tick text advancing via UIA (tick 16→24), exit 0; evidence + both Fable consult verdicts in `spine-tasks/SP-004-async-lifecycle-fault-policy/record.md` (engine reviews skipped ×3 — T-2). **Land:** batch `20260718T235923`, integrate `33d5a19a` (2026-07-19) — orchestrator re-ran the contract testCommand on the exact merge content (34/34, 0W/0E) after the gate's evidence proved stale again (T-3, 2nd occurrence); batch recovered from 3 silent worker deaths (Windows 32KB command-line limit — worker-runner `@file` patch, T-1) and a GitignoredDirtyWorktree failure caused by an orchestrator diagnostic probe (gate history). **Remaining gate:** ~~Windows-only evidence~~ — WSL2 unit-test debt CLOSED 2026-07-19 by SP-005's in-packet WSL2 run (full suite incl. this slice's tests, 51/51, 0W/0E, Ubuntu SDK 10.0.110; see SP-005 record.md). Headed Linux smoke (WSLg) still unproven — remains with the Release/publish row. Original acceptance: Define owners, cancellation generations, completion outcomes, UI delivery context, idempotent stop/dispose, and recoverable/degraded/fatal handling; seed tests for out-of-order completion, background callbacks, shutdown, panic, native/resource failure, and forbid unobserved required work or blanket catch-as-success |
| P0 | OPEN | Establish Release and publish gates | Run the first vertical slice in Debug, Release, and published Windows/Linux artifacts; verify startup/shutdown, native dependencies, assets/localization, data path, logs, version consistency, and no configuration-only crash; derive version/update/package metadata from one authority |
| P0 | OPEN | Validate official migration checklist in first visible slice | One small owner-approved surface reaches the screen early on Windows, Linux X11, and Linux Wayland; task evidence cites A-012, the current official migration index/cheat sheet, and deeper pages; exercises at least selector/pseudo-class state, compiled binding including a named/ancestor case, one direct `ICommand`, `IsVisible` layout intent, an `avares://` asset, keyboard/pointer input, scaling, and teardown; no WPF trigger/routed-command/pack-URI/literal-XAML transplant; targeted visual/interaction checks and a small commit prove the workflow before broader UI work |
| P0 | OPEN | Audit and admit bounded Avalonia MCP use | Inspect the exact Pi-installed server/config; verify commit/version/hash, tool inventory, startup health, outbound connections, Sentry/telemetry disablement, and behavior on seeded valid/invalid Avalonia v12 AXAML; document false positives/negatives and safe redaction; admit only useful advisory tools for dashboard/UX review; prove no MCP-generated output bypasses official docs, real compilation, K3 screenshots, or headed Windows/Linux gates |
| P0 | OPEN | Spike official WebView with the copied DTRH payload | `Avalonia.Controls.WebView 12.0.1` restores/builds against the selected Avalonia 12 baseline; exact page boots on Windows, Linux X11, and Linux Wayland; results cover bridge ordering, loopback routes, WebGL, workers, WebAudio/autoplay, video seek, CORS-clean media upload, fullscreen, focus, exit, failure injection, startup time, and frame behavior |
| P0 | BLOCKED | Admit DTRH browser and origin design | Owner review of the spike; package version and Linux dependencies pinned; unchanged `bridge.js` or one minimal transport-only diff selected; loopback security/range/MIME/CORS contract approved; no classic fallback |
| P0 | BLOCKED | Implement web-only DTRH host | Blocked by browser admission; then prove save picker/quick start, three local slots, protocol v1, native SFX/audio/video, freeze, progression/payout, Loom, user/mod media, watchdog recovery, graceful exit, and rendered tint safety on Windows and Linux |
| P0 | OPEN | Spike browser-to-native online-video handoff | Define and exercise the supported matrix for direct MP4/WebM, approved HLS/DASH manifests, target sites, cookies/headers, expiring URLs, `blob:`/MSE, and DRM; prove safe transfer where possible; unsupported sources report a limitation without browser fullscreen, capture mirroring, DRM bypass, or sensitive logging |
| P0 | OPEN | Spike one-decoder multi-monitor video geometry | One native decoder/timeline/audio stream presents the same frame identity on every monitor on Windows, Linux X11, and Linux Wayland; exercise negative X/Y, monitors above/below, vertical stacks, gaps, mixed scaling/resolution, portrait/landscape/flipped orientation, hot-plug, rotation, and rearrangement |
| P0 | BLOCKED | Implement unified fullscreen video presentation | Blocked by the handoff and geometry spikes; then prove aspect-fit per monitor, centered opaque black bars, one decoder/network/audio source, all screens always targeted, video below spiral and bounded tint, unchanged higher layer order, no browser fullscreen/capture path, and clean teardown for end/stop/panic/decode/display/browser/shutdown failures |
| P0 | OPEN | Prove feature-popup scrolling | Every feature popup remains inside the owner monitor's working area and reaches its final control by mouse wheel, trackpad/touch, keyboard focus, scrollbar controls, and thumb drag on Windows, Linux X11, and Linux Wayland; short content remains compact; mixed scaling and nested scrolling pass |
| P0 | OPEN | Replace card-title quick-toggle dispatch | Every unlocked toggleable card uses stable identity and one command path; plain right-click toggles immediately, live-starts/stops during a session, saves, and updates its ring across all languages/themes; locked, help, Visuals, and System exceptions pass; no plain-right-click context-menu substitution |
| P0 | OPEN | Build per-window behavior manifest | Inventory every retained WPF window's category, owner, modality, activation/focus, taskbar/Alt-Tab, topmost, resize, placement, decorations, and close/reuse lifecycle; exercise every row on Windows and supported Linux backends before approving shared chrome |
| P0 | OPEN | Prove AvatarTube rendered animation | Static pose fades, looping GIF, idle/talk/reaction emote crossfades, click reaction, gentle float, pause/resume, mod switching, attach/detach, owner transitions, and cleanup produce changing rendered-frame evidence on Windows/Linux with no blanks, duplicates, multiplied speed, or growing timers/subscriptions |
| P0 | DONE | Pilot pinned spine batch workflow | **Owner ratified 2026-07-18 (chat): multi-packet waves admitted, continuous autonomous run authorized — see gate history.** Pilot executed 2026-07-18 (batch `20260718T112944`, integrated `9a24a78a`): real worker resolved Avalonia.Templates 12.1.0 / Avalonia 12.1.0 / .NET SDK 10.0.302 from live feeds and built the template green; clean one-slice merge; evidence in `spine-tasks/SP-001-avalonia-template-pilot/record.md` incl. two Windows fixes (`core.hideDotFiles false` repo-wide; local pi-spine fsync patch — does NOT survive reinstall, upstream report needed). Gaps: STATUS.md not updated, in-worker consult gates skipped (manual-assisted execution). **Owner admission decision pending.** Original acceptance: Run packet SP-001 (bounded Avalonia 12 template spike) through the full spine pipeline — validate, stub batch, real `kimi-coding/k3` worker, plan review, contract verify, gate, integrate; the packet follows architecture/skills, executes a real dotnet contract testCommand, changes only its declared slice, updates this board, and leaves a reviewable one-task slice; then the owner decides whether multi-packet waves are admitted for phase 1 |
| P0 | OPEN | Probe bpx-consult council and task integration | Restart trusted Pi; verify project-pinned `0.10.1`, `/consult status`, and every architect/critic/simplifier/tester/security/performance route; run focused solo and council calls from the main session and a spine worker child; inspect fit ledgers and delivery; confirm no sensitive context, bounded calls, and actionable dissent before admitting mandatory consultation gates |
| P0 | OPEN | Build tiered targeted verification harness | Replace the rejected first-attempt whole-app smoke/layer strategy with: fast affected build/unit/headless checks that do not launch the app; targetable one-surface/state capture and headed actions for task close; K3 image inspection with defect-specific assertions such as bubble edge/border geometry; broader theme/language/platform matrices only at named milestones/releases; record runtime budgets and prove the targeted gate catches seeded visual regressions |
| P0 | OPEN | Spike cross-platform audio channel backend | Compare maintained backends using exact versions/licenses/native dependencies; prove Windows/Linux companion voice completion/interruption/pause, bounded low-latency overlapping SFX, whisper busy/completion, output-device enumerate/select/fallback, volume, teardown, and packaging; select explicit channel ownership rather than one generic player |
| P0 | BLOCKED | Implement reliable quips and sound arbitration | Blocked by audio-backend spike and Linux audio-stack decision; then prove ordinary/priority bark freshness and queueing, text/audio/emotion payload integrity, mute text-only behavior, disabled phrase persistence, required rapid click cues under voice/video, stale-device fallback, reference-counted ducking, and panic/error cleanup |
| P0 | OPEN | Define provider-neutral AI operation contract | Specify typed outcomes, cancellation/generation, provider switching, interactive versus awareness operations, local memory, endpoint classification/disclosure, moderation, strict command envelope, per-command results, secret storage, offline behavior, and content-free diagnostics before implementation topology |
| P0 | OPEN | Spike cancellable AI providers and strict commands | Exercise cloud, loopback Ollama, remote-host rejection/policy, and approved OpenAI-compatible endpoint with cancellation, timeout/rate/error/refusal/malformed output; fuzz strict command schema and verify zero command execution on mixed/invalid/moderated/out-of-range payloads; capture no sensitive logs |
| P0 | BLOCKED | Implement AI companion and awareness integration | Blocked by AI contract/spike and owner network/memory decisions; then prove provider switching without late results, accurate badges/status, moderation on every surface/command field, explicit memory clear, awareness consent/cooldowns, secret storage, offline zero-network, and panic cancellation on Windows/Linux |
| P0 | OPEN | Audit camera model provenance and privacy contract | Inventory BlazeFace/FaceMesh/Iris and candidate gaze weights with source/license/training-data rights/hash; approve consent scope, retained semantic events, calibration identity, no-frame/no-biometric persistence/network/logging/crash rules, and Linux release matrix |
| P0 | OPEN | Spike Windows and Linux camera acquisition | Prove stable device identity, enumeration/selection, denial/busy/black/frozen/unplug/timeout states, cancellable start/stop, unsandboxed Linux capture, and sandbox portal/PipeWire if required; a fake-running stub fails |
| P0 | OPEN | Spike local ONNX face/iris and gaze accuracy | Retain sessions/reuse buffers; measure BlazeFace/FaceMesh/Iris plus legally usable gaze candidates on identical Windows/Linux calibration scenarios for accuracy, latency, FPS, CPU, memory, startup, power, teardown, glasses/lighting/head movement; no raw real-face fixtures persisted |
| P0 | BLOCKED | Implement deep-learning webcam and gaze tracking | Blocked by model provenance, capture, performance, calibration, and consent decisions; then prove real cross-platform semantic events, monitor-bound calibration/quick recalibration, honest failure states, panic/revoke cleanup, and empirical audit that frames/biometric derivatives never leave memory |
| P0 | OPEN | T-2 (tooling/diagnostic): spine engine review pipeline empirically dead | Five consecutive batches (SP-001…SP-005) ran ZERO engine reviews — `spine_review_step` returns `skipped=true, reviewLevel=0` every call (SP-003 record.md engine-review note; `agents.reviewer` was configured; fable-5 repin `af010caa` did not make it fire). Quality model of every packet assumes a reviewer that never runs; Fable consults are silently substituting. Diagnose root cause (config vs real-pi structural) and either restore reviews or amend `port-workflow.md`/packets to state the consult-substitution explicitly | 
| P2 | OPEN | T-3 (tooling): spine gate evidence stale after retry-recovered batches | Batch `20260718T212127` gate evidence (`test-output.txt`/`coverage-output.txt`: 1 test) came from the orphaned 21:29 attempt and was never refreshed after retry/resume (salvage json 21:29 vs `lane.completed` 22:03); land only proceeded on orchestrator re-run of the merge content (23/23). Every retry-recovered batch will present failed-attempt evidence at its integrate gate — trust-critical. **Second occurrence:** batch `20260718T235923` (SP-004 land) — gate `test-output.txt` showed 23 stale tests vs actual 34/34 verified by orchestrator re-run on merge `33d5a19a`; reproduced at both P0 lands. **Third occurrence:** batch `20260719T010403` (SP-005 land) — gate showed 34 stale tests vs actual 51/51. **Root-cause lead:** gate evidence "collection" completed in 8s (02:01:42→02:01:50) — far too fast for a dotnet build+test; evidence is being copied from cache, not generated. Fix pi-spine evidence refresh/cache invalidation (upstream or local patch via T-1 mechanism) |
| P2 | OPEN | T-1 (tooling): durable pi-spine local-patch mechanism | Three node_modules patches are now load-bearing and die on reinstall (fsync `"r"`→`"r+"` in abort/lifecycle-archive/journal; `dotnet` on the gate-evidence allowlist in evidence-command.mjs; **worker-runner `@file` tail prompt** in `bin/spine-worker-runner.mjs` — tails >16KB written to `%TEMP%` and passed as `@file` because referenceDocs growth pushed the worker command line past the Windows 32,767-char CreateProcess limit, killing SP-004's worker silently ×3). Provide one checked-in patch manifest + apply script (or upstream PRs), re-applied automatically after any pi-spine install/update; evidence: fresh `npm i pi-spine` followed by patch application and a green `spine preflight` + evidence command validation + a batch whose worker tail exceeds 16KB |
| P3 | OPEN | T-4 (tooling): scope worker-tail referenceDocs injection | Every spine worker launch inlines ALL of `referenceDocs` (constitution + full task-board + port-lessons ≈ 33.6KB and growing one-directionally with every land) into the worker prompt tail. The `@file` patch removed the hard ceiling but not the cost/noise. Inject only the packet's own board row + gate-history tail + lessons (or move board evidence out of referenceDocs); evidence: worker tail byte size before/after on a real batch |
| P1 | OPEN | T-5 (tooling): pi-spine GitignoredDirtyWorktree finalization misclassification | Two consecutive land-blockers: batch `20260718T235923` (`.pi/npm` from an orchestrator probe) and batch `20260719T010403` (plain `bin/obj` from the worker's own dotnet test runs — inevitable on every batch). The engine's own message diagnoses the dirt as worktree-only artifacts safe to `git clean -fdX`, then fails the task anyway — classification bug. Fix in pi-spine: auto-clean or treat pure-gitignored dirt as non-blocking at finalization (compose with T-1's patch mechanism). Workaround encoded in the steering loop: clean -fdX → retry → resume (~2 min, deterministic). Do NOT fix via packet/constitution worker-clean rituals (worker rebuilds bin/obj after any clean — wrong layer, per Fable land consult) |

## Decisions needed

- Which Linux distributions and minimum WPE/WebKit package versions are release targets?
- Does the official WebView run the unchanged WebView2-shaped `bridge.js` on Linux? If not, approve the documented minimal transport-only edit rather than native injection on each platform.
- Does one protected loopback origin with route prefixes satisfy DTRH media isolation and performance, or does measured evidence require another origin layout?
- Is WPE SHM presentation fast enough for the full DTRH scene on supported Linux hardware? If not, can the WebKitGTK dedicated-window fallback satisfy the same behavior?
- Which online source forms and sites are supported for native handoff? Direct media can be supported; browser-local `blob:`, MSE, authenticated, expiring, and DRM sources cannot be promised generically.
- Confirm that unified fullscreen video always targets every connected monitor, superseding old `DualMonitorEnabled` and `FillAllMonitorsWithVideo` limits.
- Should normal online video use the same input-capture policy as regular video, or remain passive while mandatory policy captures input?
- Which Linux distributions, display backends, and window managers define the window-behavior acceptance matrix?
- What maximum popup height fraction and AvatarTube transition/liveness bounds should become fixed acceptance constants?
- When an AvatarTube animation asset exists but cannot decode on a target, should the visible static fallback include an in-app warning or only bounded diagnostics?
- Which reviewed task-board rows form phase 1 for spine batches, and which remain explicit blockers or exclusions? (Current proposal: the nine foundation rows in `spine-tasks/CONTEXT.md` Phase 1.)
- ~~Avalonia MCP scope~~ **Owner decision 2026-07-18: Avalonia MCP ADMITTED as advisory** for UI/effects/dashboard AXAML work (A-013 role: redacted-snippet review after official v12 research; never authority; unavailability never blocks; Sentry risk accepted under snippets-only redaction — no repo source, secrets, or user data). Verify server connection at session reconciliation; note its validation is pinned to Avalonia 11.3.1 heuristics while the project baseline is 12.1.0 (per SP-001) — treat version-specific hints skeptically.
- **Ratified 2026-07-18 (chat, with continuous-run authorization):** first-visible-slice surface = main dashboard window with one real toggleable feature card; Linux execution environment = WSL2 Ubuntu 26.04 (WSLg X11/Wayland, dotnet SDK setup part of row 1).
- Which advisor routes pass live probes without shared-provider rate limits, and should any council seat use a CLI backend instead of inline execution?
- Which quip/SFX events are required acknowledgements, what polyphony/latency/freshness limits apply, and what Linux PipeWire/PulseAudio ducking behavior is required?
- Should disabling local AI memory retain dormant history or delete it, are remote Ollama hosts supported, and which AI effect commands remain admissible?
- Which webcam semantic events remain in scope, what accuracy/performance budgets apply, and may deep gaze ship only after commercially usable training-data provenance is proven?

## Gate history

- 2026-07-19 — **SP-005 landed (batch `20260719T010403`, integrate `0c2c849f`).** Row 4 delivered `client/docs/persistence-migration-contract.md` + `Persistence/` implementation (schema-versioned STJ store with JsonNode DOM-level version/migration, atomic temp+rename+flush writer, single serialized writer via OperationRegistry, quarantine→typed Degraded + flagged defaults, `[JsonExtensionData]` unknown-member preserve, demonstrator v0→v1 migration with journal, replacement notification with documented delivery context, secret seam, Core-deferral recorded). 51/51 tests Windows AND **WSL2 Linux delivered in-packet** (0W/0E both — first row without Windows-only debt; rows 2/3 WSL2 unit-test debt retroactively closed by the full-suite run, headed-Linux-smoke remains). Teardown flush wired at SP-003's reserved position + activation note. **Annotations:** gate evidence stale a THIRD time (34 vs actual 51 — collection took 8s, copied-from-cache lead recorded in T-3); finalization failed once on `GitignoredDirtyWorktree` from the worker's OWN bin/obj build outputs (no probe this time) → T-5 filed (engine classification bug; workaround clean+retry+resume). Pre-completion Fable consult caught 3 test gaps the worker closed — consult-substitution quality model working. Land approved per Fable land consult. Row 4 stays **WIP** pending owner ratification.
- 2026-07-19 — **SP-004 landed (batch `20260718T235923`, integrate `33d5a19a`).** Row 3 delivered `client/docs/async-lifecycle-fault-contract.md` + implementation (OperationRegistry: one owner/generation/owned completion/typed outcome per op; late-bound phase-4 `IUiDispatch` with tested pre-binding throw; Recoverable/Degraded activated as per-operation classifications; row-5 boundary stated). 34/34 tests (23 SP-003 intact + 11 new), headed Windows smoke observed tick 16→24 via UIA through the real `Dispatcher.UIThread` boundary, exit 0. **Recovery annotations (orchestrator-owned):** (a) 3 silent worker deaths at launch = Windows 32,767-char CreateProcess limit exceeded by the referenceDocs-inlined worker tail (33.6KB) — root-caused by manual runner reproduction, fixed via worker-runner `@file` patch (T-1); (b) `GitignoredDirtyWorktree` failure was caused by an ORCHESTRATOR diagnostic probe running bare `pi` in the lane (auto-installed `.pi/npm` into the worktree) — not a worker fault; recovered via `git clean -fdX` + retry→resume; (c) gate evidence stale AGAIN post-retry (23 vs actual 34 — T-3 2nd occurrence); land approved on orchestrator re-run of merge content per Fable land consult. **Retrospective:** no rework indicated for SP-002/SP-003 slices — SP-004 extends them cleanly. Row 3 stays **WIP** (owner ratification + WSL2 Linux gate). Tooling rows updated: T-1 (3rd patch), T-3 (2nd occurrence); new T-4 (referenceDocs scoping).
- 2026-07-18 — **SP-003 landed (batch `20260718T212127`, integrate `eb801810`).** Row 2 delivered `client/docs/startup-shutdown-contract.md` + Lifecycle implementation (4 ordered cancellable phases, typed failures, manual-construction composition root, one-owner-per-participant incl. runner-owns-start, single guarded teardown entry point for close/startup-failure/panic; single-instance carved out seam-only; `TerminateProcess` analogue rejected). Batch recovered from `worker_orphaned` ×2 (21:42/21:54) via retry; worker completed with both Fable consults persisted + headed Windows smoke (phase trace observed via UIA, clean exit 0). **Gate-evidence anomaly:** gate's `test-output.txt`/`coverage-output.txt` were stale from the orphaned first attempt (showed 1 test); land approved on orchestrator direct re-run of the exact merge content (orch == task branch, empty diff; 23/23, 0W/0E, .NET 10) — annotate-don't-rewrite per Fable land consult. Third consecutive batch with zero engine reviews → tooling row T-2; stale-evidence defect → tooling row T-3. Row 2 stays **WIP**: owner ratification + WSL2 Linux build/test gate outstanding.
- 2026-07-18 — **SP-002 landed (batch `20260718T120441`, integrate `5fd1d540`).** Row 1 delivered `client/docs/architecture-proposal.md` + minimal `client/` scaffold (Avalonia 12.1.0/net10.0), build+test green on Windows and WSL2 Ubuntu 26.04, independently re-verified post-integrate (`dcb43e15` config land). Batch killed mid-run by parent-session exit (external SIGINT), recovered via retry→resume in-lane; `human_base_diverged` (orchestrator docs commit vs worker lesson line) resolved by base-into-orch union merge + gate re-open/re-approve. Engine ran zero reviews (`resume_fast_path`; SP-001 also ran none — review pipeline unproven until SP-003). Plan reviewer repinned uva→fable-5. Row 1 stays **WIP** pending owner ratification of the proposal; 3 owner questions flagged in proposal §5. Tooling row T-1 filed for the two load-bearing node_modules patches.
- 2026-07-18 — **Continuous autonomous run authorized (owner, chat).** The port proceeds without per-task owner checkpoints until no claimable work remains. Quality gates: mandatory solo consults on `anthropic/claude-fable-5` (pre-decomposition, pre-approach, pre-completion, pre-land for P0/high-risk) plus spine review/contract/integrate gates. **Pause protocol:** a failing Fable consult route = assumed 5-hour token-window exhaustion → park work, write `.spine/handoff.md`, stop until owner resumes. Owner may pause/steer/veto at any time; owner-authored proposals (row-6 surface, Linux env) ratified; pilot row DONE. Consult-probe row remains OPEN (council still unproven — solo-only).

- 2026-07-18 — Avalonia MCP audit executed (agent-driven pilot, branch `pilot/avalonia-mcp-audit`): admission record at `client/docs/avalonia-mcp-admission.md`; CONDITIONAL recommendation pending owner Sentry-mitigation decision. Note: this pilot exercised agent workflow + loop orchestration, not the pi-task pipeline — the "Pilot pinned pi-task workflow" row remains OPEN and still gates `/task-auto`/auto-commit admission.
- 2026-07-18 — **Execution engine decision: pi-spine.** After the pi-task `/task-auto` decomposition failures (0 titles from the full doc set; 11 titles with 7 unmapped requirements from the milestone doc), the owner replaced `@mjasnikovs/pi-task` with `pi-spine@2.8.0` (pin swapped in `.pi/settings.json`). Deterministic packets under `spine-tasks/` are authored from board rows; `client/docs/milestone-1.md` was deleted and its scope moved to `spine-tasks/CONTEXT.md`, its unratified owner proposals to "Decisions needed" above. Entry protocol: `client/docs/port-session-prompt.md` (rewritten for spine); admission record: `port-workflow.md` §Extension admission.
- 2026-07-18 — **Owner reset to not-started.** After the aborted `/task` session, every row status was returned to `OPEN`/`BLOCKED` — no row carries progress, and all owner sign-offs in `milestone-1.md` are unratified. Prior evidence docs (`avalonia-mcp-admission.md`, `row-1-research-inputs.md`) stay on disk as reference inputs only; they do not advance any row and any execution gated on them must be re-run and re-evidenced.
