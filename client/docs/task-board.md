# Greenfield Client Task Board

This is the only live product queue for the Windows/Linux Avalonia client. It contains current
outcomes and blockers, not execution logs, archived task records, or workflow-specific state.

## Board Rules

- Reconcile this board with the source tree, current contracts, and verification evidence before
  claiming or completing work.
- A row is `OPEN`, `WIP`, `BLOCKED`, or `DONE`. `WIP` means implementation exists but a named
  acceptance condition is still unresolved; it is not a claim that the feature is complete.
- Each row names its required behavior evidence and verification. Update it with concise evidence
  or the exact blocker when the work changes.
- Concurrent work requires isolated workspaces, disjoint file scope, and one owner for shared
  files such as this board, test-floor metadata, release metadata, and composition roots.
- New rows need an observed parity gap, product defect, safety concern, or owner decision. Do not
  manufacture work from historical workflow artifacts.

## Current Queue

| Priority | Status | Outcome | Required Evidence |
|---|---|---|---|
| P0 | WIP | Admit a haptic provider or keep the capability honestly unavailable. | Owner decision on provider, dependency, privacy boundary, and supported device matrix; Windows and Linux verification for any admitted route. |
| P0 | OPEN | Prove the haptic stop reaches an admitted provider before teardown completes. | Wire-level test with an admitted provider; headed device evidence remains a separate human gate. |
| P0 | WIP | Deliver For You Feed webcam gaze with a user-selectable local WPF-equivalent engine and an admitted third-party deep-learning engine. | Owner decision recorded 2026-08-22: both engines are required and the choice is visible in webcam settings. Admit the named third-party provider/model, execution location, commercial rights, outbound-data policy, and Windows/Linux support; require current consent, separate calibration per engine, and no silent fallback. |
| P1 | OPEN | Deliver the remaining Trainer Card behavior that is meaningful without unapproved sharing. | WPF behavior evidence, current platform research, focused tests, and headed evidence for user-visible work. |
| P1 | OPEN | Define the safe, supported scope for Goon Game beyond local practice mode. | Owner decisions for partner media, voice, invitations, presence, and network boundaries; platform-specific evidence. |
| P1 | WIP | Make real-desktop pointer coexistence verification reliable or explicitly machine-gated. | REPRODUCTION FOUND, mechanism NOT yet identified. It is deterministic and needs only SEVEN tests: `--filter "FullyQualifiedName~PointerCoexistence|FullyQualifiedName~TheShellsRightClickNotification"` fails 3; PointerCoexistence alone passes 6/6 in 759 ms. The single trigger is `TrayCapabilityTests.TheShellsRightClickNotification_ReachesTheRealMenu_AndTheChosenEntryRunsItsAction`, which is the only test that calls `SetForegroundWindow` (required before `TrackPopupMenu`). Bisected against Overlay, Video, Glyph, Input, Flash, BubbleCount, RackPresentation and DesktopPreflight - all pass. MEASURED at the failure: `SetWindowPos(HWND_TOPMOST)` returns TRUE, owner is 0, our window is `WS_POPUP|WS_VISIBLE|WS_CLIPSIBLINGS` parented to the desktop, yet `WS_EX_TOPMOST` never appears in the read-back after 32 assertions and z-order puts it at index 5 with the first ordinary window at 4 - genuinely outside the topmost band. FOUR HYPOTHESES TESTED AND KILLED, do not repeat them: (1) transient contention recoverable by re-assertion - 32 Raise() calls do not recover it; (2) a null foreground window - foreground is non-null; (3) ownership/child-window - owner 0, parent is the desktop; (4) fullscreen-foreground suppression - the foreground window is a foreign windowed `Chrome_WidgetWin_1` at 1221x821 on a 1646x1029 screen, not fullscreen. NARROWED FURTHER: `TrackPopupMenu` is NOT involved - that test's tracker seam stands in for the modal loop, so the only real Win32 call in the path is `SetForegroundWindow(_ownerWindow)` at `Tray/Win32TrayPresence.cs:604`, on a HIDDEN window. Next: identify what that single call leaves that denies the band, then either fix it or gate the machine state honestly. Do not weaken the click-through assertions. |
| P1 | OPEN | Validate real media and multi-monitor video behavior on Windows, X11, and Wayland as separately claimed. | Decoding, geometry, compositing, audio, teardown, and headed capture evidence for each supported backend. |
| P1 | OPEN | Maintain an honest upstream payload inventory after legacy changes. | Compare the current source payload, client payload, and inventory; update consumed documents and run the inventory tests. |
| P1 | OPEN | Decide the port's disposition for the Arcademy upstream surface. | New at v6.8.3: upstream `Services/Arcademy/` plus `Resources/web/arcademy/` (88 files). Zero occurrences of `arcademy` under `client/`. Owner decision on whether it is in scope at all; if admitted, it needs its own behavior evidence and Windows/Linux verification. |
| P1 | OPEN | Regenerate the upstream citation inventory, which is two syncs behind the tree it validates against. | `client/docs/upstream-citation-inventory.json` pins `baseline.merge` at `db3e842f` (v6.8.0, recorded 2026-08-14) while the tree is merged to v6.8.3 (`fb89d087a`), so `CitationNeedleTests` resolves every needle in a WPF snapshot two syncs old. Its own `purpose` field says it is "Regenerated at each upstream sync and DIFFED", and that regeneration did not happen at this sync. A bare SHA bump is NOT the fix and was tried and reverted: advancing `baseline.merge` alone moves `baseline.previous` to `db3e842f`, whose review window holds 0 rows (9 needles, none moved in v6.8.0 -> v6.8.3), which reds `TheNeedleMode_ExitsZero_WithANonEmptyReviewList` because the guard becomes vacuous. Needs the entries and their `changedAtSync` recomputed, needles seeded for newly-cited files, and a `previous` window that still exercises the guard. `detect.mjs` only emits a review list; it has no regenerate mode. |
| P1 | BLOCKED | Hosted pages silently lose their motion when Windows animation effects are off. | OWNER DECISION REQUIRED - the obvious fix is an accessibility regression here, and the row as first filed missed the dependency. Confirmed inherited from upstream `fix(justdrop)`: the port sets only `--autoplay-policy=no-user-gesture-required` at `Features/Dtrh/DtrhHostWindow.axaml.cs:637`, `Features/Dtrh/DtrhLoomWindow.axaml.cs:162`, `Features/Goon/GoonHostWindow.axaml.cs:312`, `Features/Intake/IntakeHostWindow.axaml.cs:246` and `Features/Chaos/ChaosTunnelWindow.cs:163`, so with Windows animation effects off, DTRH drops to 2D with `canTry3d:false`, Goon's spiral stops and Intake's beats stop advancing. BUT upstream's remedy (`Chaos/ChaosWebViewHost.cs:800-818`) forces `--force-prefers-no-reduced-motion` from its OWN `AppSettings.MotionLevel {Full,Reduced,Off}` (`Models/AppSettings.cs:70-75`), explicitly NOT from the OS flag - "Reduced is not Off; a user who asked for calmer chrome did not ask for a session that never moves". THE PORT HAS NO MotionLevel SETTING (zero hits across `client/src`), so forcing the flag here would override an OS accessibility preference with no user-facing control to restore it - which is the direction the port's own `Features/Dtrh/DtrhMotionPreference.cs:40-45` names as "the user-observable betrayal" and says "no forcing mechanism can or should fix". Decide: port MotionLevel and its settings UI first (scope expansion), or accept OS inheritance and close this as intentional divergence. Do not force the flag without the setting. |
| P1 | DONE | A Brain Drain clip is cut off mid-track instead of playing to its end. | FIXED. `Effects/AudioCueEffect.cs` now skips a due window while the module's own slot is still sounding, taken BEFORE the draw so it can never read as "rolled and lost" - the same place and shape as upstream `1f8fe465b` (`if (_waveOut != null) return;`). The guard is OPT-IN (`SkipWhileSounding`, default false) and only `Effects/BrainDrainEffect.cs` turns it on: upstream fixed Brain Drain alone, and Mind Wipe's displacement is still correct, so a guard on the shared base would have silently changed a behaviour nobody asked about. Required a new presence query - `IAudioPresence.IsSounding(slot)` asks the PLAYER for its state, NOT whether the slot dictionary has a key: slots are cleared only by displacement or Silence and nothing sweeps a naturally-finished player, so an occupancy check would have answered true forever and made Brain Drain play one clip per session. Verified by inversion: the fact asserts Brain Drain skips, Mind Wipe still replaces, AND Brain Drain resumes once the clip ends, and was shown FAILING with the opt-in off. |
| P2 | DONE | Reveal in Explorer opens the Desktop instead of selecting the file. | FIXED. `Features/Dtrh/DtrhLoomReveal.cs` built `/select,"<path>"` as one already-quoted string and passed it to the string-`Arguments` overload; the CRT re-parses those quotes before explorer sees them and, on a mismatch, explorer silently opens the Desktop rather than reporting an error. Now one pre-tokenized `ArgumentList` element (`/select,<fullpath>`, unquoted - the runtime applies Windows quoting), a `Path.GetFullPath` so `/select` does not resolve against the launching process's working directory, and a missing-file fallback to the containing folder. The Linux `xdg-open` leg carried the same pre-quoting and is fixed with it. Verified by inversion: `DtrhLoomStudioTests.Reveal_ExistingSpiral_LaunchesOsSeam` now pins the exact token and rejects any quote, and was shown FAILING against the restored defect before being accepted. The old assertion was `Contains(path, args)`, which is true of the broken shape too - that is why nothing caught this. |
| P2 | OPEN | Brain Drain clips added while the app runs are never picked up. | Confirmed inherited from upstream `fix(braindrain): clips never hot-reloaded`. `Effects/AudioCuePool.cs:103-106` re-enumerates only when the cache is empty, `Invalidate()` at `:114-120` has zero production callers, the pool is built once per process (`Session/SessionParticipant.cs:409` under `Lifecycle/CompositionRoot.cs:239`), and no `FileSystemWatcher` exists under `client/src`. Upstream added three reload sites; the port has an analogue of none. Fix needs a re-scan trigger plus a test that fails on the stale-cache path. |
| P2 | OPEN | Audit uncited intra-client source references and correct stale behavior claims. | A reproducible detector or a bounded review procedure that distinguishes current source evidence from historical references. |
| P2 | OPEN | Extend deterministic test-wait coverage to unbounded joins and injected timeout budgets. | A focused source guard plus tests that fail on the previously invisible shapes without adding timing sleeps. |

## Completion Criteria

A row is complete only when its behavior, implementation, required Windows/Linux evidence, and
recorded verification agree. A failed or unavailable gate keeps the row `WIP` or `BLOCKED`; it does
not become a successful claim through a compile, stub, no-op fallback, or workflow report.
