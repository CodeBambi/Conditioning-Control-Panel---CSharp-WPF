# Greenfield Client Task Board

This is the only live product queue for the Windows/Linux Avalonia client. It contains current
outcomes and blockers, not execution logs, archived task records, or workflow-specific state.

## Board Rules

- Reconcile this board with the source tree, current contracts, and verification evidence before
  claiming or completing work.
- A row is `OPEN`, `WIP`, `BLOCKED`, or `DONE`. `WIP` means implementation exists but a named
  acceptance condition is still unresolved; it is not a claim that the feature is complete.
- Each row names its required behavior evidence and verification. Update it with concise evidence
  or the exact blocker when the work changes.
- Concurrent work requires isolated workspaces, disjoint file scope, and one owner for shared
  files such as this board, test-floor metadata, release metadata, and composition roots.
- New rows need an observed parity gap, product defect, safety concern, or owner decision. Do not
  manufacture work from historical workflow artifacts.

## Current Queue

| Priority | Status | Outcome | Required Evidence |
|---|---|---|---|
| P0 | WIP | Admit a haptic provider or keep the capability honestly unavailable. | BOTH ROUTES ADMITTED, TOGETHER, which is upstream's shape rather than a generalisation of it: `HapticDeviceManager` connects every ENABLED provider concurrently and its own class doc says the old single-active-provider design meant "a Lovense user could not also drive an Intiface toy". A `CompositeHapticSink` drives the enabled set, evaluated PER OPERATION rather than captured, with no fallback (a route the user did not tick is never substituted for one that failed) and a parallel all-stop with a per-route budget, so a stop waiting on a dead LAN socket cannot hold the other route's stop hostage. Both route flags DEFAULT FALSE, matching upstream, and the two provider checkboxes ship with them. Floor 2673 -> 2694 unit, 154 -> 155 headless; both gates green. REMAINING FOR DONE is unchanged and is not code: headed device evidence from the owner. Nothing in this repository has driven a real toy on any platform, no test opens a socket to a real haptic server, and the factory's previous unevidenced claim that "a Linux box running Intiface Central would answer identically" was REMOVED rather than softened, because no Linux run backs it. |
| P0 | WIP | Admit the Lovense route and build its sink. | ADMITTED AND WIRED. `AdmittedRoutes` carries Lovense, `HapticSinkFactory.Create()` returns a real `LovenseHapticSink`, and the fake-available throw still fires for any route without a client (asserted against Buttplug). Nine facts that pinned the unadmitted contract now ask for the empty route list EXPLICITLY rather than reaching the product factory, so they survive admission instead of being deleted by it, and no haptic fact touches a socket - the haptic suite runs in 2 s. REMAINING FOR DONE: headed device evidence from the owner (install Lovense Connect or Lovense Remote, pair a toy, enable haptics, confirm a level is felt and that a stop silences it), and Linux evidence separately. Nothing here claims a motor moved. |
| P1 | DONE | Decide whether the capability probe may contact a haptic server the user has not switched on. | DECIDED: NO, AND THE GATE IS BUILT. `CapabilityRegistry.RunAllAsync` probes every registered capability at launch, so registering the sink ungated meant a DEFAULT INSTALL fired an HTTP GET at `http://127.0.0.1:20010/GetToys` on EVERY launch for a feature whose master toggle was off - an unsolicited outbound attempt on behalf of a disabled feature, which is the boundary class a lane may not widen alone. The gate is upstream's own auto-connect conjunction, field for field (`App.xaml.cs:2176`, predicate `:3580-3589` = `lovense.Enabled || buttplug.Enabled`), and it lives in ONE expression (`HapticParticipant.ProbeSinkAsync`) that the composition root registers and a fact can call directly, rather than as a lambda in the root where nothing could reach it. When it refuses it answers the EXISTING not-probed arm - never a server-unreachable answer, which would be a claim about a socket it deliberately did not open. Three facts hold it, including one on the real composition root asserting the reason is `not-probed` and explicitly NOT `haptic-server-unreachable`. The other half of the decision was the defaults: both route flags now default FALSE (upstream's stored default - `HapticSettings.cs:769` has no initializer), because defaulting them true was what made the probe reach a wire at all. |
| P0 | WIP | Build the Buttplug sink (second of the two admitted routes). | BUILT AND PROVEN ON THE WIRE. `ButtplugHapticSink` + `RealButtplugSession` behind a deliberately behaviour-free `IButtplugSession` seam, driven END TO END through the real `ButtplugClient` against `ButtplugToyServer` - a loopback server speaking the wire CAPTURED from the real client, not read off a spec page. The capture corrected two things a spec would have got wrong: the library announces `ProtocolVersionMajor: 4` though its README says v3, and `DeviceList.Devices` is a DICTIONARY keyed by index (an array parses to zero devices in silence). THE ROW'S OPEN DESIGN QUESTION IS ANSWERED BY DOING BOTH, and the answer was right: the seam makes the sink's policy testable, and the captured server proves this port calls the library correctly - a fake session alone would have proved the policy against a fiction. Quantization is the LIBRARY's here and is measured, not assumed: `Percent(0.75)` against a feature advertising `Value:[0,20]` put `15` on the wire, so the sink passes 0..1 through untouched, which is the opposite of the Lovense route and is pinned as a fact. TWO DEADLOCKS WERE FOUND AND FIXED WHILE PROVING IT, both real in the app and neither visible as a test failure - the suite HUNG. (1) The library completes its promises from inside its own connector read loop on a `TaskCompletionSource` with no `RunContinuationsAsynchronously`, so every await of a Buttplug call resumes INLINE on that reader; a captured stack showed one thread running the websocket receive, the sorter, `StartScanningAsync`, the sink AND THE TEST BODY, which then released the sink and waited for a disconnect that was waiting for the read loop running it. Fixed by `DetachedAsync` (`await Task.Yield()` after every library await) and pinned by a choke-point guard, because the regression is a hang rather than a red. (2) `ButtplugWebsocketConnector` captures its owning `SynchronizationContext` in a FIELD INITIALIZER and its read loop ends in a BLOCKING `Send` back to it - construct it on the Avalonia UI thread and shutdown deadlocks against itself, so it is constructed on a pool thread. Both are documented at the fix with the decompiled evidence. Route timing went 20.8s -> 2.7s for the suite. `Buttplug 5.0.1` now committed; cold warning gate 0W/0E across 4 projects. REMAINING FOR DONE: the route is NOT YET ADMITTED (`AdmittedRoutes` still carries Lovense alone) - see the admission row, whose shape changed once upstream was read properly. And headed device evidence, which the OWNER HAS REPORTED THEY CANNOT GIVE RIGHT NOW: no device on hand as of 2026-08-23. Nothing here claims a motor moved. |
| P0 | WIP | Prove the haptic stop reaches an admitted provider before teardown completes. | WIRE-LEVEL HALF DONE. `THEALLSTOPPutsAZeroOnTHEWIRE_AgainstARealProviderBeforeTeardownCompletes` drives the real `ApplicationHost.ShutdownAsync` through the real composition root into a real `LovenseHapticSink` on a loopback HTTP server and asserts the SERVER received `Vibrate:0`. It reds against a `StopAllAsync` that reports success having sent nothing - the defect every other teardown fact here would pass, because they count calls on a double. SCOPE NAMED HONESTLY: it proves DELIVERY, not ordering. I claimed both, then checked - with the pre-drain head slot DELETED it still passes, because `StopAsync` also all-stops before releasing the sink. Ordering stays `THEALLSTOPRunsBEFOREEveryParticipantStop`, which does fail against that mutation; the two are complementary and the boundary is recorded in both. REMAINING FOR DONE: headed device evidence that a real toy actually goes quiet, which is the owner's gate and cannot be discharged in-repo. GATE UNAVAILABLE 2026-08-23: the owner reports no device on hand, so this stays WIP rather than waiting on a check nobody can run today. The Buttplug route now has the same wire-level proof (`StopAllPutsAStopCommandOnTheWireForEveryDeviceTheServerKnows`), so both admitted routes are evidenced to the server and neither is evidenced to a motor. |
| P0 | OPEN | Deliver For You Feed webcam gaze with the admitted local engine. | OWNER DECISION 2026-08-23, AMENDING THE 2026-08-22 ONE: ship the LOCAL ENGINE ONLY. The admission spike is closed as UNAVAILABLE with its finding recorded - every mainstream appearance-based engine fails A-011's commercial-rights bar on TRAINING DATA rather than code licence (L2CS-Net/Gaze360 covers "models trained on dataset"; PureGaze and GazeTR on ETH-XGaze CC BY-NC-SA 4.0; OpenSeeFace BSD-2 code but its gaze model trained on MPIIGaze CC BY-NC-SA 4.0, and a permissive code licence does not launder weights). Evidence stays in `client/docs/gaze-engine-admission-spike.md`. The user-visible feature is NOT reduced: the incumbent stack the WPF product ships is itself deep-learning and is Apache-2.0 on code AND weights, which is very likely why it landed there. The three unpicked options (buy a commercial SDK licence, train on permissive or synthetic data, request a written grant) are recorded as not taken rather than deleted. WHAT REMAINS IS THE PORT ITSELF, and it is not small: zero occurrences of `Gaze` under `client/src`. Note the FYP census verdict already on file - `Services/Fyp/Online/` is NOT part of For You Feed but the app-wide remote-media subsystem with twelve consumers, and it carries a third-party network client (scrolller.com) that is a separate boundary decision. Scope this row to the WEBCAM GAZE behaviour only. OWNER DECISION 2026-08-23 (SECOND, resolving the blocker a scoping pass raised): THE WEBCAM CAPABILITY GATE IS ALREADY CLEARED FOR THIS WORK, AND NO FURTHER DECISION IS OWED. The blocker was read off `capability-inventory.md`'s line "Expanding sensors, derived data, persistence, networking, diagnostics, or telemetry requires a consent-contract revision and owner review" and taken to mean the camera capability itself is unadmitted. It does not say that. Read in place, that clause governs EXPANSION BEYOND the contract the same section already writes down - and that section already admits the local engine explicitly, names it THE DEFAULT, and specifies its full privacy and honesty contract: frames, crops, tensors, landmarks, gaze samples and every per-frame biometric derivative stay MEMORY-ONLY and are never saved, logged, transmitted, screenshotted, put in telemetry, AI prompts or crash reports; audio capture is never opened; the camera starts only after CURRENT EXPLICIT CONSENT plus an explicit user start, and never from opening the dashboard, restoring settings or finding a calibration; the UI shows honest states including denied, removed and missing-runtime; acquisition and model init are cancellable and off the UI thread; and only consent metadata, engine/model identity and compact numeric calibration are persisted. So implementing the LOCAL engine AS SPECIFIED is executing an existing admission, not widening a boundary. WHAT WOULD STILL NEED A DECISION, named so this cannot be quietly stretched: any remote or third-party engine (already closed UNAVAILABLE on training-data rights), any persistence of a frame or derivative, any transmission of anything camera-derived, opening the microphone, or adding diagnostics/telemetry over camera data. Any of those is expansion and stops this row. WHAT REMAINS IS THE PORT ITSELF and it is large: zero occurrences of `Gaze` under `client/src`, no camera slot in `Capabilities/`, and no webcam consent type. Upstream surface, measured: `Services/Webcam/WebcamTrackingService.cs` 3,610 lines, `WebcamCalibrationData.cs` 369, `WebcamDeviceEnumerator.cs` 138, `WebcamWinRtEnumerator.cs` 61, `Windows/WebcamCalibrationWindow.xaml(.cs)` 1,910+275, `Dialogs/WebcamConsentDialog.xaml(.cs)` 187+277. THE FIRST SLICE IS THE CONSENT AND CAPABILITY SEAM, NOT THE ENGINE: a typed camera capability that refuses honestly with no device, no consent, denied permission or no engine, on BOTH platforms, is worth landing before a single frame is decoded - it is what makes every later slice's refusal truthful. Linux needs its own acquisition evidence and must prove XDG portal/PipeWire where sandboxed; a Windows-only capture path is not this capability. **SLICE 1 LANDED 2026-08-23: THE CAPABILITY AND CONSENT SEAM, NOT THE ENGINE.** `Camera/` now carries a typed `webcam-gaze` capability with an EMPTY `AdmittedEngines`, so every product run reports `Unavailable(camera-no-engine)` - honest, and the state every later slice's refusal is measured against. THE PRIVACY GATE IS PROVED RATHER THAN ASSERTED: `ProbeAsync` asks the engine gate and the consent gate BEFORE it can reach `Enumerate`, and a fact counts every ask across a whole launch - start, probe, GRANT CONSENT, probe again - and holds it at ZERO, because the engine rung is asked first. It names its own inversion (move the consent rung above the engine rung and it fails), which is `capability-inventory.md`'s "opening the dashboard, restoring settings, or finding calibration never starts it" turned into something that can fail. BOTH PLATFORMS HAVE A ROUTE rather than Windows plus a silent absence: DirectShow, and V4L2/sysfs on Linux with the XDG portal branch a TYPED REFUSAL naming the portal instead of a pretend client. Linux identity is honestly unstable - no durable id exists without udev or an `open(2)` - so `IdentityIsStable` is false there and this slice persists NO camera identity at all rather than inventing one the contract would then trust. Floor 2700 -> 2716. WHAT IT DOES NOT PROVE, and the row must keep saying so: no camera was opened, no frame decoded, no engine exists, and the V4L2 route has NEVER RUN on Linux - the 16 camera facts DO now run and pass ON LINUX (verified on the WSL2 host), so the seam behaves there - but `/sys/class/video4linux` is EMPTY on that host, so the route has still never enumerated a real device and its parsing is proved only against injected fixture trees, which is evidence about parsing and none at all about a running kernel's sysfs. The Windows route WAS executed against this machine's real integrated camera, metadata only, no open and no camera indicator. NEXT SLICE is capture plus the engine, and it is the one that needs headed evidence and a real Linux run. |
| P1 | WIP | Deliver the XP and level SPINE, so the four features that already compute progression stop throwing it away. | **COORDINATOR DECISION 2026-08-23: ADMITTED, and lifted out of the completeness census into a real row, because it is a KEYSTONE rather than one more gap.** Four LANDED features are degraded today for want of it, and each says so in its own source: `Features/Intake/IntakeDraft.cs:24-25` ("COMPUTED, never granted - no greenfield XP store"), `Features/Intake/IntakeHostWindow.axaml.cs:538-540`, `Features/Dtrh/DtrhMeta.cs:794`, and - as of today - `Features/Arcademy/ArcademyClassPayout.cs`, whose `XpBanked` is a computed `false` carrying its reason because there is nothing to bank into. The newly landed `Features/Progression/TrainerCard.cs` cannot show a level for the same reason. So this is not new surface: it is the missing floor under surface that already shipped. SCOPE IS DELIBERATELY NARROW - the store, the level curve, persistence, and wiring the ALREADY-COMPUTED-AND-DISCARDED sources. EXPLICITLY OUT, each recorded as out rather than half-built: the level-up ceremony (animation, tray balloon, sound, avatar change), Quests, the skill tree, Season Recap, the daily-free wheel, achievements beyond `GradedRunAwards`, and AFK/idle suppression - all separate census entries and separate decisions. THE FOUR LEVEL GATES BECOME REAL: `BrainDrainEffect.cs:51` (70), `BubbleCountEffect.cs:84` (50), `LockCardEffect.cs:87` (35), `MindWipeEffect.cs:40` (75) each already name their level as not-ported. **A gate that silently passes because the store is absent is the failure mode here**; a typed refusal naming the level is the outcome. HONESTY BAR, inherited from the Trainer Card: a level the store cannot know must NOT render as 0 or 1 - unknown answers Unknown, never a zero that reads as a score. And no XP number may be invented: every amount traces to an upstream citation or to a source the port already computes, or it stays ungranted. **LANDED 2026-08-23, AND MY OWN PREMISE ABOUT THE LEVEL GATES WAS WRONG.** I wrote that the four gates should "become real" and that a gate silently PASSING was the failure mode. Source says otherwise and the lane refused the instruction, correctly: `Models/AppSettings.cs:5439` `IsLevelUnlocked` is `return true;` under a comment reading "Feature level gating has been removed - every feature is available from level 1". Three of the four never had code at all: Bubble Count and Mind Wipe were doc-comment PROSE with no reader, and Lock Card's level survives nowhere in the shipping tree - it existed only in this port's own comment. Making them refuse would have taken four modules away from every user the shipping app hands them at level 1: a regression dressed as a port. So the four numbers are recorded, upstream's actual body is ported, and a fact RE-DERIVES `return true;` from the shipping bytes on every run - it reds if anyone 'makes them real' by guess, and equally if upstream ever puts a body back. THE CURVE is upstream's v1, a five-band FORMULA rather than a table, each band cited; v2 is unreachable here because only the Descent migration ceremony writes the epoch that selects it, and that is a named limit rather than dead code. A fact asserts `MidpointRounding` is ABSENT from v1's body, because no v1 band can land on a midpoint - the mode is unobservable, which is exactly why someone could 'tidy' it to match v2 with nothing going red. THREE of upstream's THIRTY AddXP sites are wired - precisely the ones this port already computed and discarded - and the other 27 are listed with the reason each is not, so the remainder stays visible. Floor 2794 -> 2807. REMAINING FOR DONE: **no level is rendered anywhere.** The ledger exists and nothing shows it; the Trainer Card's note now says so in words rather than claiming no level exists, deliberately a string change and not a card change, because rendering a level is a visual contract needing its own headed gate. Also out and recorded as out: the entire level-up ceremony, Quests, the skill tree, Season Recap, the daily-free wheel, AFK suppression, curve v2, and the server-confirmed XP watermark. Concurrent-writer behaviour is unproven and belongs to the app-wide lift row. |
| P1 | WIP | Deliver the SCRIPTED SESSION engine, the largest single gap and the second keystone. | **COORDINATOR DECISION 2026-08-23: ADMITTED, lifted out of the completeness census, and slice 1 is in flight.** Same argument that justified the XP spine: this is not new surface, it is a floor that LANDED code already names as missing, in five places and in its own words - `Session/SessionEngine.cs:13-17` ("a SCRIPTED session with phases, a duration, XP and a stop-confirmation dialog … which runs on TOP of the engine and is not ported"), `Effects/IntensityRampEffect.cs:66` ("the port has no scripted session"), `Effects/RampCurves.cs:54`, `Effects/LockCardSchedule.cs:53`, and `Features/Intake/IntakeDraft.cs:5-7` ("marked never-runnable - no session engine exists in greenfield"). NOTE THE NAME COLLISION so nobody trips on it: the client already HAS a `Session/SessionEngine.cs`, and it is a different thing - the ordinary effect session. Upstream's scripted session runs ON TOP of it. Upstream surface measured at 7,842 lines / 8 files, so this is sliced. SLICE 1 IS THE RUNTIME CORE, NOT THE UI: the `.session.json` model as persisted, phase advance on an INJECTED clock, start/stop with the settings SNAPSHOT taken at start and RESTORED when the session ends (user-visible and load-bearing - a session temporarily replaces the user's settings and must give them back), and **the clock-jump guard, which trusts a `Stopwatch` over the wall clock past 30 seconds** and is the most testable behaviour in the slice. OUT of slice 1 and recorded as out: the session editor window, the rack UI and its repaint, the session-complete recap and history windows, the media log, the XP pause penalty, the corner-GIF window, scheduled bubble bursts, and the randomised ramp starts. **SLICE 1 LANDED 2026-08-23 (merge of the lane branch), and the row stays WIP because NOTHING CAN START ONE.** What exists: `Session/ScriptedSession.cs` (the `.session.json` model as persisted - 14 top-level keys, 3 phase keys and 60 distinct settings keys across the four files, every one bound - and the fact asserts JSON extension data is EMPTY rather than asserting a count, which is the stronger form: an unmodelled key is NAMED in the failure, read with upstream's own options per `Services/Session/SessionFileService.cs:16-22`, malformed file -> null per `:105-108`), `ScriptedClock.cs` (the two-clock seam), `ScriptedSessionDials.cs` (snapshot / apply / restore over the eleven real preset stores), `ScriptedSessionRun.cs` (phases on an injected clock, START/STOP, and the jump guard). 22 facts, floor 2807 -> 2829. THE CLOCK-JUMP GUARD IS PORTED VERBATIM AND MUTATION-CHECKED: `Services/Session/SessionEngine.cs:104`, `Math.Abs(divergence.TotalSeconds) > 30` -> trust the Stopwatch, STRICTLY greater and in BOTH directions (`:100-103` names both halves - the speed-hack and the '149 minutes left' balloon, upstream #369) - and `>= 30`, `> 60`, and dropping `Math.Abs` each red exactly the fact that names them. FOUR DIVERGENCES ARE RECORDED RATHER THAN SMOOTHED: (1) the wall clock is UTC where upstream reads local, following `Scheduling/ScheduleClock.cs:14-20`, so DST stops being a jump instead of being survived by the guard; (2) apply does NOT write to disk where upstream saves mid-session (`Services/Session/SessionEngine.cs:262`), so an interrupted session gives the USER's settings back rather than the session's - a divergence in the user's favour, with one stated exception (`SessionEngine.Start()` saves its own flash preset); (3) a second start returns false where upstream throws (`:150-153`), matching this port's own engine; (4) dials are applied before the engine arms, because this port's modules read dials at arm. A LANE CORRECTION LANDED WITH IT: the lane recorded `flashScale` as dead at runtime upstream, and it is NOT - `UpdateRampingValues` reads it at `Services/Session/SessionEngine.cs:596-599` and pushes it through `AppSettings.SetSessionFlashRamp` (`Models/AppSettings.cs:908`), so a session file that sets it does change what the user sees; it is unapplied here only because the ramp is out of slice 1. `flashSmallSize` IS dead - grep finds writes and no read. WHAT THIS DOES NOT PROVE, and it is most of the feature: nothing visual, nothing headed, no Linux, and NOTHING CONSTRUCTS A `ScriptedSessionRun` - it is referenced only by its own files, its test and two doc comments, so no surface in this build can start a scripted session. Still unported: the rack and its repaint, the editor, the Session Complete recap and history, the media log, pause and its 100-XP penalty, the XP award, the corner-GIF window, scheduled bubble bursts, the randomised +/-3min ramp starts, `UpdateRampingValues` (`:564`) and `CheckDelayedFeatures` (`:663`) - so a delayed feature is applied OFF at t=0 exactly as upstream applies it (`:1288-1296`) and nothing turns it on yet. The four corroborating absence claims in landed code (`Effects/IntensityRampEffect.cs:66`, `Effects/RampCurves.cs:54`, `Effects/LockCardSchedule.cs:53`, `Features/Intake/IntakeDraft.cs:5-7`) and the census entry were amended at the land rather than left to rot. **SLICE 2 LANDED 2026-08-24: THE RAMPS AND THE DELAYED STARTS (+14 facts, class now 36/36).** `Session/ScriptedSessionRamp.cs` is the arithmetic of `UpdateRampingValues` (`Services/Session/SessionEngine.cs:564-661`) as a PURE function, and `ScriptedSessionRun` runs it plus `CheckDelayedFeatures` (`:663-772`) on the tick. THE MOST VALUABLE FACT IN THE SLICE IS THE ONE THAT PROVES A SEPARATION RATHER THAN A VALUE: upstream drives the pink ramp into the OVERLAY and deliberately not into the user's persisted dial, because writing it auto-saved to disk and an app kill mid-session froze the ramp maximum into settings.json permanently - the 'screen keeps getting more pink and stays that way' reports, #471 and #476, and `RestoreSettings` only heals a CLEAN stop. `TheRampClimbs_AndTheUsersOwnPinkOpacityNeverMovesWithIt` reds under mutation M12, which is that exact defect re-introduced. `flashScale` IS NOW APPLIED, closing the correction filed at slice 1: pinned for the whole session rather than lerped (`:596-599`), clamped [50,250], parked on `ScriptedSessionRun.Ramp` and written to NO persisted document, because the port persists no flash draw dials and upstream does not store it either. `flashSmallSize` stays unapplied - grep over the shipping tree returns assignments only, no read anywhere. THE JITTER IS PORTED, not skipped: `RandomizeStartTimes` (`:777-805`), +/-3 min, floored at zero, applied only to an ENABLED feature whose start minute is already > 0, with the randomness INJECTED through the port's existing `Random? random = null` seam so a fixed sample pins the start to an exact minute - no wall-clock, no sleep, no poll anywhere in the new facts. THE MUTATION CHECK CAUGHT TWO OF ITS OWN FACTS AND THAT IS THE POINT OF RECORDING IT: 23 mutations were applied to real product source, rebuilt and run, and TWO facts were GREEN under their first mutation - `Lerp(35,90,0.5)=62.5` rounds to 62 either way so the assertion could not tell truncation from rounding (fixed with a non-midpoint pin, 71.66->71), and removing the already-on guard turns a delayed start into a TOGGLE that an even number of ticks hides (fixed by asserting after every following tick). Both facts were strengthened rather than the mutations being retired. THREE DIVERGENCES RECORDED: the port writes the ramp's FIRST SAMPLE into the opacity dial at a delayed pink/spiral start, because this port has no sustained-overlay hold to carry the value and its modules read dials when they arm - without it a delayed filter would come up at the USER's opacity, which is neither the session's start nor its ramp; brain drain (`:652-660`, `:762-771`) is NOT ported because it is commented out upstream and porting dead code would be inventing behaviour; and the spiral file probe's early return is replaced by the port's own recorded refusal (`spiral-no-image`) rather than upstream's silent dial-off. WHAT IT STILL DOES NOT PROVE: `ScriptedSessionRun.Ramp` HAS NO CONSUMER in this build - a module that FOLLOWS the climbing value needs a live re-tint seam the port does not have, and a dial write would have been the #471 defect, so it was refused rather than faked. Every module in the rig was composed with a NULL SURFACE: what is proved is the dial, the arm and the recorded refusal, never a pixel. Nothing visual, nothing headed, and still nothing constructs a `ScriptedSessionRun`. LINUX IS NOW PROVED FOR THIS SLICE at unit level: 2843 total / 0 failed / 66 skipped, and the class 36/36 ten runs out of ten. IT TOOK A RACE FIX TO GET THERE, and the race is worth keeping: the headline fact compared EVERY document before and after a killed session, and one of them - `session_preset.json` - is legitimately written from t=0 by `SessionEngine.Start()` (`Session/SessionEngine.cs:150`, a fire-and-forget `_preset.Save()`), which is slice 1's own stated exception rather than a new one. The comparison had no ordering against that unawaited write, so it failed 2 runs in 5 on Linux and passed on Windows. THE FIRST FIX WAS KILLED BY ITS OWN MUTATION CHECK: awaiting `SaveImmediate()` as a barrier looked right and is not a passive wait, it is a WRITE of the same session values, so deleting BOTH product saves left the new assertion passing 10/10 - it would have pinned the test's own write. The shipped fix excludes the one legitimately-written file AT ENUMERATION so the contended file is never opened, and adds `IsDirty` teeth proving the session's pink mutations are still unwritten: race-free by CODE rather than by timing, with no barrier, sleep, poll or retry. Re-proved by mutation at 10/10 red for a delayed start saving the pink store, for the #471 write, and for any second document moving mid-session. **SLICE 3 LANDED 2026-08-24: THE SURFACE, so a user can finally start one (+17 unit, +8 headless; floor 2861 / 169).** The rack is on the STUDIO door and upstream CORROBORATES that placement rather than merely allowing it: `MainWindow/MainWindow.TabNavigation.cs:592` declares `("studio", "studio", ["studio", "presets", "haptics"])`, so the `presets` tab that hosts `SessionRackPanel` (`Views/Tabs/PresetsTabView.xaml:857`) is inside the STUDIO door's own entry list, and `play` is a separate row (`:600-601`). Four data-built rows, one button that doubles as START/STOP (upstream's own resilience against stale click subscriptions, `MainWindow/MainWindow.Presets.cs:1455-1459`), the confirmation carrying upstream's promise verbatim in meaning (`:1465-1476`), and a live readout off ONE `ReadProgress()` so elapsed and remaining cannot disagree. Composed in `SessionParticipant` against the real engine, the real eleven stores and the real clock. **THE MOST VALUABLE THING IN THIS SLICE IS A DEFECT NOBODY ASKED FOR, and it existed only because the surface now exists:** a running session holds eleven documents at the SESSION's values, dirty and deliberately unwritten, and the teardown flush (persistence contract §11) writes every dirty document - so CLOSING THE APP MID-SESSION would have persisted the session's dials over the user's own and never given them back. That is upstream's #471/#476 class arriving on the first day this build had a way to start a session. The stop now runs in the reserved PRE-DRAIN slot, because `StopAsync` is too late (the registry is cancelled by then, so the restore's write would terminate `Cancelled`), and the fact reads the answer back off the JSON on disk. HEADED EVIDENCE (Windows, real desktop, real input): four checks, each 1.0000 on its own state and 0.0000 on the wrong one. THE STRONGEST INVERSION IS `session-row`, where the two states are two different ROWS - Morning Drift against Good Girls Don't Cum - same page, same panel, same derivation, same cell, so the only difference between the captures is the difficulty in the `.session.json`. The `running` capture is reachable ONLY by four real gestures ending on the confirmation's own button, and every pixel is gated behind UIA on the four shipped names and durations, the confirmation's promise, THE ASSERTION THAT NOTHING HAS STARTED WHILE THAT QUESTION IS UP, then the phase line and the countdown. TWO HARNESS GATES CAUGHT REAL DEFECTS DURING THE RUN INSTEAD OF PASSING THEM: the grid cross-check refused a stripe 43 DIP short of the trailing edge (a real layout bug - the rack livery aligns content `Left`, so the row arranged at desired width), and the run gate refused a capture demanding `00:00 elapsed` when the desktop had reached `00:01 elapsed, 29:58 remaining`, which was a real session's clock running between the click and the read; the needle is now a live-countdown pattern rather than an instant. THE MUTATION CHECK CAUGHT TWO OF THE SLICE'S OWN FACTS - the readout fact repainted through `PhaseChanged` and the completion fact through the engine's own re-arm, so each survived a mutation it should have reddened - and BOTH FACTS WERE STRENGTHENED rather than the mutations retired. THREE REFUSALS TO SOURCE: `vibeSummary` is dead upstream (one hit in the tree, its own declaration), so the row shows the description's first line as upstream does (`MainWindow.SessionIO.cs:470-478`); upstream NEVER shows the user a phase name (`OnSessionPhaseChanged` logs it and nothing else, `MainWindow.Presets.cs:1770-1776`), so the phase line is recorded as a PORT ADDITION on the `SchedulerPanelNotices` precedent; and the `+N XP` cell, the provenance badge and the hover actions are left out because nothing in this build awards XP and custom or imported sessions do not exist here. STILL UNPORTED and named on screen by `ScriptedSessionAbsenceState`: the editor, custom and community sessions, rack filter/sort/search, the Session Complete recap, session history, the media log, pause and its 100-XP penalty, the XP award, and the corner-GIF window. NOT PROVED: NOTHING ON LINUX - every capture is Windows, and WSLg has no UIA and no input automation; the pixel checks prove four flat fills at derived offsets, not that the text is legible or the rows well laid out; and nothing here photographs a ramp or a delayed start, which slice 2's facts still own against a null surface. |
| P0 | DONE | The unit suite permanently loses Win32 FOREGROUND PERMISSION mid-run, and every later topmost assertion is then refused with a success return. | **DIAGNOSED 2026-08-23, mechanism proved OUTSIDE the suite, fix in flight.** Symptom: `SetWindowPos(HWND_TOPMOST)` returns TRUE and `WS_EX_TOPMOST` (0x8) is not applied - read back as `0x80800A0` - so the product's 32-attempt re-assertion loop cannot help, because IT IS NOT A RACE, IT IS A REFUSAL. THE RULE IS DOCUMENTED AND WAS LOOKED UP RATHER THAN GUESSED: `SetWindowPos` Remarks - 'to use SetWindowPos to bring a window to the top, the process that owns the window must have SetForegroundWindow permission'; `SetForegroundWindow` Remarks lists the ways a process holds that permission. THE TRIGGER, characterised empirically in ~60 lines of PowerShell with no test code involved, and it needs BOTH halves: a click must have been delivered to one of our own windows, AND the process's top-level window count must then reach ZERO. Measured ladder: fresh process -> topmost YES; create and destroy a `WS_EX_NOACTIVATE|TOOLWINDOW` popup with no click -> YES; deliver a click to it, then destroy it -> NO, and permanently; `SetForegroundWindow` on one of our own windows -> YES again. **Keeping ANY other top-level window alive - including a hidden, never-shown one - preserves the band across the clicked window's destruction.** The poisoner is `PointerSurfaceObservations.RunDelivery` (every `PointerCapabilityTests` fact that reaches the desktop forces the one shared `Delivery` lazy), and the band is lost at the LAST `ScratchTarget` disposal - proved positional rather than identity-bound by reversing the disposal order and watching the loss move. IT PREDATES THE SCRIPTED-SESSION MERGE: the full assembly at `ed4d9bea3` (the merge's first parent) is 20 failed / 2807 with the identical `missing 0x8` signature. The merge reshuffled WHICH classes are downstream of the poisoner; it neither caused nor exposed the defect, and the board's earlier 'zero failures on Windows' readings were luckier orderings, not a healthy suite. TWO COORDINATOR HYPOTHESES WERE DISPROVEN BY MEASUREMENT AND ARE RECORDED SO NOBODY RETRIES THEM: Avalonia platform init / process DPI awareness via `CompositionRootTests` (`Program.BuildAvaloniaApp` -> `UsePlatformDetect()`) is NOT the cause - that class plus both victim classes is 24/24 green; and 'the real-desktop collection alone is green' holds only under DEFAULT parallelism - under `-parallel none` the collection alone is 19 failed / 253, because the poisoner is INSIDE it. Also ruled out and not to be retried: the developer's own open applications (53/53 green with them open and holding the foreground); `Win32InputPresence`'s `SetForegroundWindow` escalation ladder (76/76 green); and CPU starvation (`-parallel none`, 163s, zero concurrency, same failures). Environmental note, read but NOT proven causal: `HKCU\Control Panel\Desktop\ForegroundLockTimeout` is 2147483647 (~24.8 days) on this machine, so the one documented escape hatch that would restore permission on its own can never fire here. FIX IN FLIGHT: hold ONE hidden top-level sentinel window for the life of the `RealDesktopLease` collection fixture, so no desktop fact can walk the process to zero windows. That is not a weakening - it makes the test process REPRESENTATIVE, because the shipping app always owns at least one top-level window (the Avalonia main window, or `Win32TrayPresence`'s hidden owner window), which is exactly why this is not user-reachable. FORBIDDEN AS FIXES, explicitly: a retry loop, a skip, an `allowedSkips` entry, a weakened assertion, a wall-clock wait, or 'repairing' permission with a `SetForegroundWindow` at teardown - the last one works and is wrong, because it masks the revocation instead of not causing it. REQUIRED INVERSION BEFORE IT LANDS: with the sentinel in place, removing the tray visibility guard at `Tray/Win32TrayPresence.cs:604-611` must STILL red `TheRightClickGesture_LeavesTheProcessAbleToPlaceAWindowInTheTopmostBand` - if the sentinel blinds that fact it is masking a real defect class and must not land. **CLOSED 2026-08-24.** The floor is armed per TEST THREAD by a `RealDesktopFacts` base constructor - xunit builds the test class on the thread that runs the fact, which is the only hook guaranteed to be the right one. Two earlier shapes were built, MEASURED USELESS and thrown away rather than kept: a fixture-lifetime floor (constructed once, on the wrong thread) and a per-fact suspension (produced a FALSE red, worse than the mask it removed). A new guard pins membership and carries its own broken-detector control, because a walk finding zero members would pass for the worst possible reason. Verified: 20 failures -> 0, unit 2861/0 failed on Windows over many runs in BOTH parallel modes with the machine verified quiet each time, and 2861/0/66 on Linux. No assertion was weakened, skipped, retried or allow-listed to get there. |
| P2 | OPEN | The tier refusal is still a dead end, and the obvious fix was BUILT, MEASURED and TAKEN BACK OUT. | **MEASURED 2026-08-24 rather than assumed, and the measurement is why nothing shipped.** Census #41. The ACTION half cannot be built at all: it opens App Info & Data and this port has no such page. The ANNOUNCEMENT half was wired at upstream's own severity and duration (`Services/TierGate.cs:133`, Warning, 8s, no action button) because upstream's own lesson argues for it - `MainWindow/MainWindow.Lab.cs:282-288` records that a bare call 'teleported a free account off the Play door with no dialog, no toast and nothing tying the jump to the card they clicked'. TWO LANDED HEADLESS FACTS THEN WENT RED, and the cause was geometric: in the shell's 1100x760 window the toast at `724,40 360x153` CONTAINS the `FallInButton` at `883,131 172x46` whole. A real press at the button's centre returns `pressed=0` and the hit test resolves to the toast. A toast body carries a background and captures its own clicks - exactly as upstream's do. So the toast would have taken away the one thing the card guarantees in every branch, that A GATED PRESS MUST ARRIVE, in order to repeat a sentence the plate already carries durably without covering anything. Reverted. CLOSE CONDITION, specific: an App Info page, OR a toast placement clear of this card's buttons. A HARNESS TRAP WORTH KEEPING was found doing it: `InputHitTest` answered 'FallInButton' for that point between the toast's layout pass and the first pointer event, and only agreed with the delivered press afterwards - so a fact that QUERIED instead of PRESSING would have concluded the button was reachable and been wrong. |
| P1 | OPEN | THE LAYER INPUT/CAPTURE CONTRACT: SIX native-window surfaces, no document naming them together - and one safety invariant with NO coverage. | **SURVEYED 2026-08-24 against `unified-compositor-engine` and `overlay-clickthrough`, both owner-invoked.** THE INPUT HALF IS IN BETTER SHAPE THAN EXPECTED: every surface already declares its policy AT ITS CREATION SITE, with a reason and an upstream citation. `Overlay/Win32OverlayPresence.cs` and `Glyph/Win32GlyphSurface.cs` are LAYERED+NOACTIVATE+TOOLWINDOW+TOPMOST+TRANSPARENT - passive, click-through - and neither TRUSTS the style: both carry a typed refusal for the case where the style is set and the window manager STILL routes a point to them (`:613`, `:641`). `Pointer/Win32PointerSurface.cs:850-852` is NOACTIVATE+TOOLWINDOW with TRANSPARENT *deliberately absent*, citing upstream's own three-flag decision (`Services/BubbleService.cs:4877-4894`). `Input/Win32InputPresence.cs:1097-1099` carries TOOLWINDOW alone - 'neither WS_EX_NOACTIVATE nor WS_EX_TRANSPARENT ... their absence is what makes this window the opposite kind of window'. And `PointerCoexistenceTests` proves all four COEXISTING on a real desktop: the overlay stays click-through through a move and never becomes the foreground, while the card keeps the foreground AND the keyboard through an open, a move and a close. THREE GAPS, DIFFERENT IN KIND. (1) NO CONTRACT NAMES THEM TOGETHER, so nothing catches a fifth surface inventing a fourth policy - which is A-001's own unresolved question 'which composed effects contain interactive regions, and should handled pointer events be swallowed?'. (2) **CAPTURE IS UNDECLARED ON EVERY SURFACE**, which is A-001's other open question - and `overlay-clickthrough` is explicit that capture visibility is PER FEATURE CONTRACT and must NOT be inferred from click-through or shared composition, so the shortcut is closed. Note this may be the CORRECT state rather than a gap: `client/port.txt` reserves the capture boundary to the owner. (3) All four are Win32; Linux is typed-unavailable and every WSLg capture still comes back black, so any contract must state the gap rather than imply parity. **THE ONE FINDING THAT IS A SAFETY MATTER RATHER THAN A DOCUMENT MATTER:** the skill's invariant 'no failure leaves an invisible input-blocking or permanently topmost surface' has NO COVERAGE - `client/tests/CcpClient.Tests/TeardownTests.cs` contains zero mentions of topmost, overlay, input or pointer. The two non-transparent surfaces (the pointer target and the lock card) are exactly the ones that WOULD block a user's desktop if one were stranded. The atomic second-card claim landed this week enumerates its release across all twelve exits plus Dismiss and Dispose, which covers the lock card's own path - but nothing proves the OUTCOME at teardown. That is the piece worth doing first, ahead of any contract prose. Both advisor seats were put the separability question, as both skills require for an input/capture split. **CORRECTED 2026-08-24 BY THE ADVISOR SEAT, AND MY SURVEY WAS THE THING THAT WAS WRONG: THERE ARE SIX `CreateWindowExW` SITES, NOT FOUR.** I missed `Video/Win32VideoPresence.cs:404` and `Tray/Win32TrayPresence.cs:452`, and a seventh site MUTATES an Avalonia window's ex-style (`Features/Chaos/ChaosTunnelWindow.cs:59-61`). Verified by grep, not taken on trust. That changes the shape of this row twice over. First, ENFORCEMENT IS UNEVEN RATHER THAN UNIFORM: overlay (`:520-540`) and glyph (`:488-489`) read the OS back at runtime and refuse in type, which is STRICTLY STRONGER than any document could be; pointer only REPORTS its two bits into an observation record for tests to assert; and input presence, video, tray and the chaos tunnel pin nothing. Second, the chaos tunnel ORs its flags onto WHATEVER AVALONIA ALREADY SET (`exStyle | WsExNoactivate | WsExToolwindow`) - which is exactly the base-unknown pattern `Pointer/Win32PointerSurface.cs:850-852` rejects in its own comment, citing upstream's three-flag decision. SO THE PLAN NARROWS, on the advisor's own dissent, which I am taking: do NOT write per-surface guards - overlay and glyph already have a stronger check and a lexical duplicate would create a second authority that can drift, and guarding the UNEXAMINED sites would freeze exactly the accident this row exists to avoid. `client/docs/window-behavior-manifest.md` already carries the right per-field table (Category, Owner, Modality, Activation, Taskbar/Alt-Tab, Topmost, Resize, Placement, Decorations, Close) and is missing EXACTLY TWO FIELDS - input passthrough and capture - so reuse beats writing. What is worth landing is those two fields, a provenance tag per site with only three legal values (upstream-cited / runtime-enforced / UNEXAMINED), and ONE tree-wide census guard that reds when a SEVENTH native window appears with no declared policy. Capture stays owner-reserved: `client/port.txt:34-35` reserves BROADENING it, not RECORDING it, and A-001 notes `SetWindowDisplayAffinity` applies at WINDOW scope - so capture policy is an INPUT TO the compositor's window-count decision rather than a peer of it, and writing it first inverts the dependency. |
| P1 | DONE | During video playback a FULLSCREEN TOPMOST window that is not click-through sits over the desktop, and nothing declares that on purpose. | **FOUND 2026-08-24 by the advisor seat and VERIFIED BEFORE FILING, including the reachability check that has caught this board twice.** `Video/Win32VideoPresence.cs:405` creates `WS_EX_LAYERED | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE` - **no `WS_EX_TRANSPARENT`** - and the surface is pushed `HWND_TOPMOST` (`:138`, `:493`). Its only ex-style read-back (`:606`) checks TOPMOST, i.e. whether the band was lost; nothing reads or asserts the INPUT policy, and there is no comment declaring one. The surface paints its own bars and frames with no class background brush (`:400-403`), so it is opaque by construction. IT IS REACHABLE - `Session/SessionParticipant.cs:273` builds `VideoSurfacePresenter.Product(...)` and the composition root builds the participant - so this is a live user-facing behaviour and not a dormant class. WHAT IS NOT KNOWN IS WHETHER IT IS RIGHT, and that is the row: a mandatory video arguably SHOULD be unavoidable, but upstream ships a separate STRICT LOCK dial for exactly that (census #21, 'cannot make a video undismissable'), which implies its default is NOT a click sink. So the port may be stricter than upstream by omission. COMPARE THE SIBLINGS: the overlay and the glyph both set `WS_EX_TRANSPARENT` AND refuse in type when the window manager routes a point to them anyway; the pointer target and the lock card OMIT it deliberately and say so at the creation site. This one omits it and says nothing - which is the difference between a decision and an accident that looks like one. Scope when admitted: establish upstream's default from source, decide, and either declare the sink with its reason or make it pass clicks through - then pin the OUTCOME the way the overlay does, not the flag. **CLOSED 2026-08-24 AS DECISION (a) — THE SURFACE SHOULD SWALLOW CLICKS, BECAUSE UPSTREAM'S DOES — AND MY ROW WAS WRONG THREE WAYS.** (1) UPSTREAM SWALLOWS TOO, in all three of its render paths: `Services/Video/VideoService.cs:2894-2907` handles `PreviewMouseDown` with its own comment 'it swallows every one of them', matched at `:4162-4166` and `:4226-4230`, plus a transparent hit-test-visible rectangle at `:2862-2874` whose comment is 'This overlay catches all clicks before they reach the video surface'. (2) MY CENSUS-#21 READING WAS WRONG: strict lock governs DISMISSAL, not clicks - `:4274` vetoes Closing, `:4276-4306` eats the panic key and Alt+F4, and the NON-strict branch at `:4328-4345` consumes ESC as 'dismiss this video'. BOTH branches presuppose the window already owns the input, so a click-through video would be LESS like upstream, not more. (3) IT IS NOT FULLSCREEN: `Effects/VideoSurfacePresenter.cs:230-233` places 0.55 x 0.42 of the primary display, centred, and D123's stated reason is that 'a fullscreen topmost surface the user cannot dismiss is the one shape this port must not ship while it has no panic key'. My row's premise was stale. What WAS genuinely missing is now landed: a creation-site declaration with the upstream citations and the two named divergences, plus an OUTCOME pin - a real `SendInput` click measured three times (before the surface exists, while it is up, after Withdraw), gated on the OS having already said the point is ours. A MUTATION WORTH KEEPING: adding `WS_EX_TRANSPARENT` to a window that is NOT layered does not pass clicks through at all - it is `WS_EX_LAYERED` that gives the bit its force, and that is recorded at the creation site. Capture untouched and confirmed absent tree-wide. |
| P2 | OPEN | Four `overlay-clickthrough` safety invariants have partial or no evidence, listed so a lane can close them one at a time. | **DERIVED 2026-08-24 by checking the skill's seven invariants against the tree, rather than assuming the headed suite covers them.** PROVEN TODAY: interactive regions receive intended input (the pointer capability reads the OS routing a click to its own centre, and the card holds foreground plus keyboard across open/move/close); overlays never activate or steal focus (`TheOverlayKeepsItsBandAndItsAlpha_AndNeverBecomesTheForeground`); and a silent Linux no-op is not claimed as support - the port types `Unavailable` with a reason, which is what that invariant asks for. NOT PROVEN, and each is a separate small packet: (a) passive regions allow normal desktop CLICK, TYPE, DRAG AND SCROLL - only the CLICK half is proved, at four moments including during a move; type, drag and scroll are untested; (b) a handled overlay click does not leak through and activate the application underneath - no fact looks for that leakage; (c) overlays do not appear as ordinary task-switching windows - `WS_EX_TOOLWINDOW` implies it and nothing measures Alt-Tab or the taskbar; (d) teardown and display transitions restore normal desktop input - covered by neither the teardown suite nor a display-change fact. NONE OF THESE IS A KNOWN DEFECT. They are claims the port makes implicitly and has not measured, which is exactly the distinction this board keeps. |
| P2 | OPEN | Decide whether the four Win32 surfaces should assert HWND_TOPMOST through the attach ladder instead of bare. | **DEFERRED DELIBERATELY 2026-08-23, from the foreground-permission diagnosis above, and deferred on a risk argument rather than a scope one.** `Win32OverlayPresence`, `Win32GlyphSurface`, `Win32VideoPresence` and `Win32PointerSurface` all assert `HWND_TOPMOST` bare. In a process that has lost `SetForegroundWindow` permission they are refused with a success return, and the product path really does degrade correctly - `Present` returns `Unavailable(...style-refused)` rather than pretending. The repo already owns the remedy and it was PROVEN to restore the band in the poisoned state: the attach ladder at `Win32InputPresence.cs:525-588` and `RealDesktopCollection.cs:750-770`. NOT BUILT, for two stated reasons: (1) NO USER CAN REACH THE STATE - the trigger needs the process's top-level window count to hit zero after a click, and the app always owns at least one window, so this is hardening against a state that does not occur rather than a live defect; (2) putting `AttachThreadInput` against a FOREIGN foreground thread inside a ~1s re-assert cadence is a platform seam with real deadlock risk, and this port routes those through the advisor and headed evidence, not through a defect hunt. THE CAVEAT IS THE REASON THIS ROW EXISTS AT ALL, recorded verbatim and unproven: if a future teardown path ever destroys EVERY top-level window including the tray owner and then presents a new topmost surface, the overlay would silently stop appearing on top for the rest of that process's life, with every write call still returning success. |
| P2 | DONE | Both `Pump` implementations drain the ENTIRE calling thread's message queue, and no fact pins which thread they run on in the product. | **ANSWERED 2026-08-24, AND THE ANSWER IS THE BAD ONE - the invariant does NOT hold, so the filter was NARROWED (outcome b).** THE PRIOR LANE'S CLAIM THAT NOTHING UNDER `client/src` CONSTRUCTS `BubblePopSurfacePresenter` IS REFUTED BY SOURCE: `Session/SessionParticipant.cs:269` builds it through `BubblePopSurfacePresenter.ForProduct(sessionClock, Dispatch)`, and `SessionParticipant` itself is built by the composition root at `Lifecycle/CompositionRoot.cs:263`. THE THREAD, ESTABLISHED RATHER THAN GUESSED: that `Dispatch` (`SessionParticipant.cs:213-219`) posts through `infra.UiDispatch`, whose production binding is `AvaloniaUiDispatch.Post` = `Dispatcher.UIThread.Post` (`Lifecycle/UiDispatch.cs:22-26`). So `StepOnce` - and therefore `_surface.Pump(PumpBudget)` at `Effects/BubblePopSurfacePresenter.cs:374` - runs ON THE AVALONIA UI THREAD in the shipped composition, and `PeekMessageW(hWnd 0)` there removes, translates and dispatches Avalonia's OWN window messages out of Avalonia's loop from inside a bubble step. That is the reentrancy/ordering hazard the row named, not a hypothetical. NARROWED: `Win32PointerSurface.Pump` now peeks ONCE PER OWNED WINDOW, round-robin over `_byWindow` so one busy target cannot spend the whole budget, with the handles snapshotted because dispatching a press can close a target from inside the callback. `Win32InputPresence.Pump` peeks on `_window` and answers 0 when it owns none. ITS DOC'S CLAIM WAS VERIFIED TRUE - `IInputPresence.Pump` says "the product does not call this" and there is no `.Pump(` call site under `client/src` other than the pointer one - and it was narrowed ANYWAY, because the SUITE is its caller and the suite is exactly where the breadth bit: `RealDesktopWindowFloor` puts a hidden window on every `RealDesktopFacts` thread, and its own drain already had to be filtered for this precise reason (see its comment in `RealDesktopCollection.cs`, which names this method). PINNED BY TWO NEW FACTS, both in `PointerCapabilityTests`. (1) BEHAVIOURAL and fully deterministic - nothing injected, nothing waited for: one `WM_LBUTTONDOWN` is POSTED to the surface's own target and one to a scratch window on the same thread that it does not own; the surface's pump must see its own press, must leave the foreign one alone, and the foreign one must still be there for the probe's own unfiltered drain to take (so a pump that SWALLOWED it fails too). (2) LEXICAL, covering BOTH classes, because fact (1) is machine-keyed and degenerates to `0 == 0` on Linux or on a session with no interactive desktop: it reads the two `Pump` bodies and asserts the window argument of their `PeekMessageW` call is not the literal `0`. INVERTED: with both pumps mutated back to `hWnd 0`, fact (1) reds with "the surface's pump dispatched 1 message(s) into a window it does not own" and fact (2) reds naming the file and signature. The lexical fact also carries a top-level `Assert.Equal(2, pumps.Length)` so an emptied table is a red rather than a silent pass - the vacuous-shape guard caught its absence and is the reason it is there. VERIFIED: unit assembly 2863/0 failed/2 skipped on Windows, headless 169/0, warning gate 0/0; and the three touched facts run and PASS on Linux (WSL Ubuntu, .NET 10.0.400, out-of-tree build - 108 tests across the four affected classes, 0 failed, 9 pre-existing platform skips). NOT PROVED: no headed evidence that a running bubble field ever actually misordered an Avalonia message - the defect is established from the call chain and the API contract, not from an observed misbehaviour; and the lexical fact is LEXICAL, so it cannot see a filter that is a variable holding zero. |
| P2 | DONE | A cap-outstanding concurrency fact flakes under the `dotnet test` host, and one clean re-run is not a diagnosis. | **REPRODUCED DELIBERATELY 2026-08-24, AND IT IS (ii): THE FACT WAS RACY AND THE ARBITRATION IS CORRECT.** MECHANISM, measured not guessed: `OrphanSafePlayerFactory.Create` (`Audio/AudioSeams.cs`) queues the construction with `Task.Run` and then waits a WALL-CLOCK budget for it (`task.Wait(_budget)`; the fact injects 200 ms). `h.ConstructCount` is the FAKE's record that the POOL entered the construct delegate - so it is a measurement of thread-pool dispatch latency against a wall-clock budget, and nothing to do with the bound. REPRODUCTION: on a 16-core machine, with N pool threads deliberately parked on a blocking work item, the fact fails 3/3 with EXACTLY the reported `Expected: 2, Actual: 1` at N=14, and `Actual: 0` at N=15. AND THE SEPARATION THE ROW EXISTED FOR: at the same instant, in the same run, the arbitration was PERFECT - `abandonmentLines=2, outstanding=2, capRefusalLines=1`, with the third caller holding the typed `PlayerConstructionTimeoutException` reading "refused at the outstanding-parked cap (2) ... no construction started". The cap counted both abandonments and refused the third correctly while the FACT read short. No product change was needed and none was made. THE DETERMINISTIC SIGNAL: each of the first two callers is now held on `TestWait.UntilSync(() => Volatile.Read(ref h.ConstructCount) == i + 1)` - the shared bounded helper on a condition, the same one the fact already used for `done.IsSet` - until its construction has DEMONSTRABLY entered the fake. The third caller is deliberately NOT held, because a refused create queues nothing at all, which is the very claim being made about it. NO RETRY, NO SLEEP, NO WIDENED TOLERANCE, NO WEAKENED ASSERTION. VERIFIED: 5/5 green with the identical N=14 starvation that reddened it 3/3; 6/6 whole-class runs (52 facts) with 12 cores held busy; and green on Linux (WSL Ubuntu, .NET 10.0.400). INVERTED: mutating the product's cap check in `AudioSeams.cs` from `>=` to `>` - the "bounded by one" the fact's own comment names - still reds it with `Expected: 2, Actual: 3`, so the deterministic wait did not blunt what it detects. NOT PROVED: the flake was never reproduced under an UNAIDED `check-floor.mjs` run - only under deliberate pool starvation, which is the same mechanism and a stronger demonstration but not the same event; and nothing here says the other 51 facts in the class are free of the same shape. |
| P1 | DONE | Phrase backup is BUILT and PROVED and a user still cannot reach it: there is no button. | **FILED 2026-08-24 by the lane that built it, which stopped at its File Scope rather than widening - the right call, and this row is the consequence.** `Session/PhraseBackup.cs` and `Session/PhraseBackupFile.cs` land with 18 facts; `Storage/**` lands with 17. Nothing in the shell calls `ExportAsync` or `ImportAsync`, so TODAY THE DATA-LOSS RISK THAT JUSTIFIED ADMITTING THIS ROW IS STILL LIVE: every phrase the user has written still dies with a bad update or a machine move. A feature that cannot be reached is not delivered, and the census counts user-visible behaviour rather than code. THE REMAINING WORK IS ONE PAGE EDIT and the lane named it precisely: `Views/Pages/SystemPage.axaml(.cs)` - the port's equivalent of upstream's Settings -> Data - needs two buttons, `AvaloniaUserFilePicker.For(this)`, a confirm dialog for `confirmReplace`, and text driven by the TYPED outcomes, where `Persisted == false` must read 'restored, but not yet saved' rather than a bare success. It needs the three phrase stores, which `Session/SessionParticipant.cs` owns. ALSO STILL OWED AND NOT DISCHARGED BY ANY FACT: six lines that only a human can exercise - the capability probe against a real provider, both picker calls, cancellation mapping, and `IStorageItem.Dispose`. No dialog has ever been opened, on either platform. **CLOSED 2026-08-24, AND THE DATA-LOSS RISK IS GONE.** `Views/Pages/SystemPage` has Export and Import, and the lane did NOT need to widen scope to reach the three phrase stores - it checked, and `SessionParticipant.SubliminalPreset` / `.LockCardPreset` / `.BouncingTextPreset` are already public and `MainWindow` already resolves the participant, so `SystemPage` takes it exactly as `StudioPage` does. Every sentence comes from a TYPED outcome, including the one that matters most: a restore that has not reached disk reads 'restored, but not yet saved: they are in use now and will be written to disk when the app closes' and wears the WARNING livery, rather than a bare success. A cancelled picker says NOTHING, which is upstream's own early return. An escaped exception reports the exception TYPE and never `ex.Message`, because that string carries the path. THIS IS ALSO THE FIRST TIME THIS PORT HAS OPENED A REAL WINDOWS FILE DIALOG: the headed run typed a destination into a genuine save dialog and wrote 1386 bytes carrying `ccp-phrases/v1`, then handed a genuine open dialog a real non-JSON file and read the refusal off the screen. That discharges FOUR of the six human-only lines the picker seam owed - the CanSave/CanOpen probe, both picker calls, and Dispose. ONLY CANCELLATION MAPPING REMAINS UNPROVEN: no run has closed a dialog without choosing, on either platform. |
| P1 | DONE | The Session Complete recap cannot open on the app-close path: it tries to own a window that is already closed. | **FOUND 2026-08-24 by the session-lock lane, in the app's OWN LOG during a headed capture - not by a test.** Verbatim: `session recap: could not be shown (InvalidOperationException: Cannot show a window with a closed owner.)`. The chain is entirely of this branch's own making, which is why it is filed rather than shrugged at: slice 4 put the scripted session's stop in the reserved PRE-DRAIN slot so closing the app mid-session cannot persist the session's dials over the user's - correct, and proved. That stop finalises the log and raises `LogReady`. `Navigation/SessionRecapLaunch.cs` then tries to open the recap owned by a shell that is already closing. IT IS CONTAINED - logged, not fatal, because the port ported upstream's try/catch around opening either window - so a user loses the recap silently rather than seeing a crash. THAT IS THE DEFECT: on the one path where a user most needs to be told what just happened, they are told nothing. Scope when admitted: decide whether the recap is owed at all on app close (upstream never faces this, because its stop is not in a pre-drain slot), and if it is, give it an owner that outlives the shell or show it before the shell closes. NO try/catch WIDENING: the catch is already why this is invisible. **CLOSED 2026-08-24 BY DECIDING IT IS NOT OWED, rather than by making the window open.** Three reasons, recorded on `SessionRecapLaunch`. (1) The user asked the app to QUIT; a recap is a card to read and dismiss, and producing one out of a close gesture resurrects the window they just dismissed. (2) IT COULD NOT BE READ EVEN IF IT OPENED: teardown runs from `desktop.Exit` with `ShutdownAsync().GetAwaiter().GetResult()` ON THE UI THREAD (`App.axaml.cs:88-97`), so a window shown there never pumps a frame - giving it an owner that outlives the shell would produce a LIE IN A LOG LINE, not a recap. (3) NOTHING IS LOST: `ScriptedSessionLogStore.Complete` persists the file SYNCHRONOUSLY before it raises `LogReady`, so the run is the newest row under Recent sessions next launch. The ask is now refused before a window is constructed and the line says DECISION rather than failure; the try/catch is untouched and was not widened. Proved in both directions: removing the guard reds the fact AND prints the verbatim defect line, and a mutation making the guard ALWAYS fire reds a separate fact, so it is not a blanket suppression - a run that ends while the shell is open still opens its recap. |
| P2 | DONE | The headless suite has no teardown guarantee when a fact throws mid-body, so one failure can leak a live host into the next test. | **FOUND 2026-08-24 as an ANOMALY INSIDE A MUTATION RUN, and reported rather than buried - which is the only reason anyone knows.** While mutation-checking the session lock, one run reddened an eighth fact that the mutation should not have touched; a second run of the same mutation did not reproduce it, and the clean build passed that fact 10/10 in isolation, 6/6 as a class, and twice in full assembly. The cause is real and is not the product: under the mutation a PRECEDING fact threw mid-body and therefore never reached `ShutdownAsync`, leaking a live `ApplicationHost` WITH A RUNNING SESSION into the next test. So the artefact only appears on a deliberately broken build - but what it reveals does not: A FAILING HEADLESS FACT CAN CORRUPT THE ONE AFTER IT, which means a single genuine failure can produce a cascade of misleading ones and send whoever reads the run at the wrong defect. Scope when admitted: give the headless host a disposal that runs on the throwing path (`IAsyncLifetime`/`await using`, or a fixture that owns the host), and prove it by making a fact throw deliberately and showing the next one still passes. **CLOSED 2026-08-24, and the extent was far larger than the row guessed.** 188 of 208 facts construct an `ApplicationHost`; 17 already used try/finally; **171 were exposed**, with `ShutdownAsync` as the last statement and unreachable through a throw. SEVEN LEAKED ON GREEN RUNS TOO, never tearing down on any path: `GoonPracticeHeadlessTests` (4) and `RackPresentationAnchorHeadlessTests` (3). Fixed with a `HeadlessTest` base whose `DisposeAsync` shuts tracked hosts down in reverse - chosen against four named alternatives, the decisive one being that a class fixture has the WRONG LIFETIME, since every fact boots its own host against its own temp data root and sharing one would change what the facts test. The base ASSERTS rather than assumes that disposal lands on the dispatcher thread, because `AvaloniaTestRunner` overrides the per-test Run. THE DEMONSTRATION LEAVES NO RED BEHIND: the throwing type is real and test-class-shaped but carries NO fact attribute, so xunit never schedules it. And the runner half was measured OUT OF BAND WITH A NEGATIVE CONTROL - a genuinely red fact gave Failed 1 by name with no cascade and its teardown lines present; with the tracking removed, nothing followed. STILL OPEN AND VISIBLE: a fact that `Show()`s a window still never `Close()`s it, on any path. Extending teardown to close windows would be a behaviour change wearing a harness change's clothes, so it was left out deliberately. | |
| P1 | DONE | Both the global ramp and the scripted session's ramp drive spiral and pink opacity at once during a session. | **The owed guard is named IN LANDED CODE and is still not written**: `Effects/IntensityRampEffect.cs:65-74` records that upstream's `!sessionActive` stand-down (`MainWindow/MainWindow.StartStop.cs:490,507,513,521`) makes the global visual links stand down while a SCRIPTED session runs, and that this port has no such guard. That comment was AMENDED at the slice-1 land to say the condition was still constantly false because nothing could start a session - and slice 3 then shipped a surface that can. So the premise the amendment rested on expired the same week it was written, and the guard is now genuinely owed: two independent ramps write the same two opacities on every tick, and which one the user sees is whichever wrote last. Scope when admitted: port the stand-down, and pin it with a fact that reds when both ramps drive the same dial in one tick. **CLOSED 2026-08-24, and the row's own citations were wrong.** THE GUARD IS PER-LINK AND GUARDS THREE OF FIVE, not wholesale: `!sessionActive` is written into flash opacity (`MainWindow/MainWindow.StartStop.cs:509`), spiral opacity (`:515`) and pink opacity (`:523`), and DELIBERATELY NOT into master volume (`:529`) or subliminal volume (`:537`). That asymmetry is checkable in upstream's own session code - a session imposes exactly those three opacities and then climbs them itself, and imposes no volume - so the rule is 'yield the dials a session is already driving'. My line numbers were off by two throughout. THERE IS A FOURTH GUARDED SITE THIS ROW NEVER NAMED: `:549`, the ramp-complete auto-stop, guarded for upstream's stated reason (#444 - a shorter ramp calling StopEngine tore down the manual services while the preset timer kept counting). The port's handler was literally `Ramp.Completed += () => Engine.Stop()`, so THE SAME DEFECT WAS ONE RAISE AWAY. Ported, and deliberately NOT latched, because upstream re-tests the whole condition every tick. THE PORT HAD TO BE STRICTER THAN UPSTREAM: upstream skips only the WRITE, because its bases were captured once before any session existed; this port captures a base the first time a dial is LINKED, so a tick during a session would have captured the session's imposed opacity as the user's own and handed it back at STOP - #471/#476 again. A standing-down dial is therefore skipped whole: no read, no capture, no write. AND A SECOND, WORSE DEFECT WAS FOUND WHILE TRACING IT: `ScriptedSessionRun.Start` captured its dial snapshot BEFORE stopping the manual engine, so with a ramp holding spiral/pink/flash the snapshot preserved the RAMPED value as if the user had chosen it, the ramp's disarm-restore then landed on top of the session's applied opacities, and the user got the ramp's value back PERMANENTLY. Fixed by handing the engine's borrowed dials back first, with the second-start refusal moved ahead of the stop so a refused start disturbs nothing. |
| P0 | BLOCKED | OWNER DECISION: should the companion's replies be read as COMMANDS at all? | **RAISED 2026-08-24 by the lane that built the effect bridge, which found my premise wrong.** I briefed it that 'the validator will accept it, the executor will route it, and nothing will happen'. NEITHER HAPPENS. A model reply never reaches `AiEnvelopeValidator` at all: `Ai/AiOperationPipeline.cs:340-346` refuses envelope-shaped replies as `MalformedOutput`, and `Ai/AiTextHygiene.cs:24-30` records that as a DELIBERATE DECISION rather than an omission. There are zero product callers of `AiCommandExecutor.Execute`. SO THE BRIDGE IS BUILT, TESTED AND UNREACHABLE BY DESIGN, and the surface says so unconditionally: CompanionWindow's `EffectDispatchNotice` reads 'Nothing dispatches yet: her replies are not read as commands in this build, so these switches decide what would be admitted, not what is happening.' WHY THIS IS YOURS AND NOT A LANE'S: closing it means putting MODEL TEXT ON THE EXECUTION PATH - a prompt contract that does not exist, plus reordering hygiene behind a strict parse. That is a consent and safety decision about what a language model may do to the user's screen, and `client/port.txt` reserves exactly that. The permissions grid is already built and defaults CLOSED (master off, empty set) so that whenever you decide, the user decides too. Nothing is blocked on you to keep the current behaviour: today she cannot drive anything, honestly and visibly. |
| P2 | DONE | A refusal string bakes in `verification-harness.md:47`, and line 47 is no longer what it cites. | **FOUND 2026-08-24 by two lanes independently.** `DesktopPreflight` builds a refusal message naming `verification-harness.md:47` for the lease contract, and `DesktopPreflightTests.cs:144` ASSERTS that string - so the citation is pinned by a test while the document underneath it has moved: line 47 is now the `session-row` paragraph. The test passes, because it asserts the string and not the destination. THAT IS THE INTERESTING PART and the reason this is a row rather than a one-line fix: this repo has a citation checker (`client/tools/citations/intra.mjs`) that catches exactly this class of rot, and it does not reach here because the anchor is a LITERAL INSIDE A REFUSAL MESSAGE rather than a comment or a doc reference. A user or a developer following that pointer lands on the wrong paragraph. Scope when admitted: repoint it, and decide whether a line-numbered citation belongs in a runtime string at all - a named anchor cannot rot the same way. Also stale and found with it: `verification-harness.md` says 'the eighteen checks today are'; the manifest now holds twenty-two. **CLOSED 2026-08-24, and the row's second question - whether a line-numbered citation belongs in a runtime string at all - is answered NO, with a caveat that changed the fix.** The paragraph had moved from line 47 to line 55 while the assertion stayed green, because it asserted the STRING and never the destination. A NAMED ANCHOR ALONE WOULD HAVE TRADED SILENT WRONGNESS FOR UNVERIFIABILITY: this repo's own citation checker records named anchors as 'not resolvable without judgment' and skips them, so swapping `:47` for a section name would have removed the wrong pointer and removed the ability to detect the next one. So the judgment is supplied where the anchor is used: the refusal now cites `section "Tier 2"`, and the test READS THE DOCUMENT and asserts both that the heading still exists and that the section still carries the lease contract. PROVED BY INVERSION on my own change, which is the standard I have been holding lanes to: renaming the heading to `### Tier Two` reds the fact (11 total, 1 failed); restoring it greens it (11/11). Three doc-comment citations to the same moved line were repointed with it. | |
| P2 | OPEN | The audio dials are NOT 'dials over landed seams' - the seams exist but are not app-wide. | **MY SIZING WAS WRONG AND THE LANE CORRECTED IT WITH SOURCE, 2026-08-24.** I admitted census #115 (master volume, video volume, output device, Test audio) on the reading that `Audio/SoundArbitration.cs:429 SetPreferredDevice` and `Companion/BarkPipeline.cs:262 MasterVolume` are landed and merely unwired. They are landed and they are NOT REACHABLE: `SoundArbitration` exists only inside the DTRH host window (`Features/Dtrh/DtrhHostWindow.axaml.cs:220-256`, whose own comment says the app-wide lift is a future row), `BarkPipeline` is built by that same window, and the only app-wide audio object the Studio page can reach is `SessionParticipant.Audio`, an `IAudioPresence` with NO device enumeration, NO preferred-device seam and NO gain seam (`Audio/IAudioPresence.cs:93-165`). There is also no persisted audio-settings document; every volume-bearing document is per-module. So this is the app-wide audio lift plus a new document plus a composition-root change - not four dials. THIS IS THE SECOND TIME A CENSUS-DERIVED SIZE OF MINE WAS WRONG IN THE SAME DIRECTION (flash sound was the first: the census called it one wiring job, and upstream's pops need OVERLAPPING playback where this port's Cue replaces per slot). Both were sized from what EXISTS rather than from what is REACHABLE, and a seam that one window owns is not a seam the app has. |
| P1 | OPEN | **REFILED: the twelve companion owner questions, which existed only outside the live queue.** | **REFILED 2026-08-24, discharging census entry #58, which gates nine other entries in that cluster.** The census said the questions 'exist nowhere a reader can find them'. Precisely: `client/docs/her-room-divergence-audit.md` is intact and carries all twelve in its §4, each written to be answerable WITHOUT reading the table - but six rows were deleted from THIS board while OPEN or WIP, and this board is the only live queue, so a reader of the queue found nothing. The audit was evidence; it was never the queue. THREE ARE ALREADY ANSWERED and are recorded here rather than asked again: **Q7 effect-command defaults** - DECIDED 2026-08-24, the port ships EVERYTHING OFF, diverging from upstream's pre-ticked bubbles/subliminal/bounce, because which effects a companion may drive is a consent question and the permissions grid is what makes it the user's; **Q8 custom AI endpoints** - already the standing loopback-only admission (`Ai/AiEndpointAdmission.cs:28`), unchanged; **Q12 screen OCR** - already owner-blocked in the cluster decisions, and the constitution's standing posture is no broadening absent a decree. NINE REMAIN GENUINELY OPEN, and they are listed here so the queue carries them: Q1 memory default (remember conversations by default? upstream yes, port no); Q2 app awareness (may the port watch which foreground APP you use, process name and duration?); Q3 activity ledger (a 30-day on-disk per-app history of visits, minutes and time-of-day habits?); Q4 media/input/mic sensing, three separate yes/nos (now-playing title/artist, input-idle and typing CADENCE never content, mic-in-use for do-not-disturb); Q5 consent persistence and granularity (does awareness/memory consent survive a restart, and are page titles per-app opt-in?); Q6 cooldown and talkativeness VALUES (the port carries old-WPF placeholders 10/10/15/5s including a recorded 10-vs-90 discrepancy; upstream v6.7 is 60s global / 90s between LLM lines / 10 min per app / an hourly budget by intensity); Q9 product identity (does the companion surface become 'Her Room' - the room metaphor, relationship stages, spice switch, attention gauge with its upsell? each separable, all product-identity and monetization calls); Q10 facts memory (durable facts about the user - name, preferences, BOUNDARIES, goals - beyond the pairs-only model); Q11 community prompts (fetch shared personalities from a first-party proxy - a network boundary plus a content-policy question). WHY THIS ROW IS P1 AND NOT A DOC TASK: nine of the cluster's seventeen entries cannot be sized, let alone built, until these are answered - and every one of them touches a boundary `client/port.txt` reserves, so no lane may quietly pick one up. THE AUDIT ALSO SIZED THE WORK THAT NEEDS NO ANSWER AT ALL, and that half is actionable today: A3 dial copy, A4 per-app title allow-list, A6 incognito hard-drop, C10 three forget scopes and D11 transcript are each S with no dependency and no owner question - they were buildable the day the rows were deleted, and they still are. |
| P1 | DONE | Pop Quiz is built and proved and NOTHING CONSTRUCTS IT - the third feature this week to land without a way in. | **FILED 2026-08-24 by the lane that built it, which stopped at its File Scope rather than widening.** `Effects/PopQuiz*.cs` lands with 22 facts, every one mutation-checked. Racking it needs `Session/SessionParticipant.cs`; a panel needs `Views/Pages/StudioPage.*`; both were owned by other live lanes. Remaining work, named by the lane: the rack row, a panel with its two dials (upstream session-locks BOTH - `GradedIntakeTabView.xaml:269,286` - so the feature-lock rule already covers them), a Test button, and a headed gate. THE PATTERN IS NOW WORTH NAMING RATHER THAN REPEATING: phrase backup, the AI effect bridge and Pop Quiz have all landed complete, proved, and unreachable, each because the composition root or a page belonged to another lane. Parallelism bought the work and deferred the wiring, and a feature a user cannot reach is not delivered - the census counts user-visible behaviour, not code. Whoever picks this up should consider taking all three wirings in ONE packet that owns `Lifecycle/CompositionRoot.cs`, `Session/SessionParticipant.cs` and the two pages together, rather than three more slices that each stop at the same boundary. SPECIFIC TO THIS ONE, and it is a presentation claim nobody has discharged: the card's LEGIBILITY is unverified - a question plus four keyed answers goes into the input capability's single wrapped band (`Input/Win32InputPresence.cs:817`), and whether five or six lines fit `0.45 x 0.5` of a real display has never been rendered. **CLOSED 2026-08-24: A USER CAN NOW MEET A POP QUIZ, END TO END, AND IT WAS WATCHED ON A REAL DESKTOP.** Racked in `SessionParticipant` between Brain Drain and the ramp - a position with only one upstream order to take, since upstream has no rack row for this module and its `StartEngine` sequence is unopposed - with a Studio row, upstream's two dials, the dot, the quick-toggle and a frequency handler that re-paces the LIVE schedule. The headed drive used real input throughout: row clicked, box enabled, slider dragged to 100/hour, START pressed, and the card came up after 39.4s inside the 25.2-46.8s window the schedule predicts, held the foreground and the keyboard focus, and went down to a real Escape. **THE BOARD'S OPEN LEGIBILITY QUESTION IS ANSWERED, AND THE ANSWER IS THE OPPOSITE OF THE FEAR**: all six lines fit with room to spare, about 14% of a 900px card. What the capture found instead is that the CARD scales with the display and the TYPE does not - the painter never creates a font, so GDI draws with the stock system font at a fixed pixel size, and on a 2880x1800 display the question is markedly smaller than the app's body text. That is a manual judgement over the artifact and no automated check can close it. TWO THINGS ARE DELIBERATELY NOT WIRED: the 25 XP, because `ProgressionLedger.Open`'s own remarks scope it to modal windows opened one at a time and a session-lifetime store would be a second WRITER - `Grant` mutates its in-memory copy and saves, so a quiz answered after a DTRH run had banked would write a stale level back over it - so the module is racked with `xp: null` and the panel says why; and the Test button, which needs a public fire-now the packet could not add. |
| P1 | DONE | The app cannot tell the user anything without a modal. | **BUILT 2026-08-24 (census #54, with #83 merged into it).** `Views/ToastHost` docks top-right over the page host with upstream's four kinds and exact accents, upstream's 380 width and 40/16 offsets, a dismiss affordance that also retires the toast's timer, and auto-dismiss at upstream's 5s on an INJECTED schedule. The host carries no background, so empty space stays click-through - upstream's own shape, and pinned by a fact that reds when it becomes Transparent. Three pieces are deliberately not ported with a reason each: sticky toasts and their dismissed-key persistence belong to the deferred notification-settings entry; the action button's only consumer is the tier gate, which needs PlayPage and an App Info page; and the replay queue exists because upstream's service is static and pre-window, where this host is created with the window. ITS FIRST CONSUMER IS PHRASE BACKUP, not the tier refusal - `PlayPage` was outside the lane's scope and wiring it would have widened. **#41 IS THEREFORE STILL A DEAD END**: the GateBand at `Views/Pages/PlayPage.axaml:110` still has no route onward, and now that the surface exists it is one small packet away. |
| P2 | OPEN | A SECOND `SoundArbitrationTests` fact rides the same 200 ms wall-clock budget, and it red a floor run today. | **CAUGHT WITH ITS TRX 2026-08-24 by a lane that did not touch `Audio/**` and flagged it rather than re-running.** `Construction_LockUnavailableAtCompletion_AbandonsWithoutCounting_NothingWasParked` failed `Expected 0, Actual 1` at `SoundArbitrationTests.cs:1648` on the second of three full floor runs; runs 1 and 3 were green and the class is 8/8 in isolation. THE FACT'S OWN COMMENT PREDICTS IT: 'the one scheduling assumption is that a trivial already-ungated construction returns inside the 200 ms budget; if a starved pool ever broke that, this fact REDS (it would take route (a) and count 1)' - and `Outstanding == 1` is exactly route (a). So this is the documented starved-pool case under concurrent-lane load, not a product defect. WHY IT IS A ROW RATHER THAN A SHRUG: its SIBLING in the same class was closed this week by proving the arbitration correct and giving the FACT a deterministic condition wait instead of a wall-clock budget. This one still reads a 200 ms `task.Wait` against thread-pool dispatch latency, so the same fix shape applies and the same separation must be done first - reproduce under deliberate starvation, establish whether the PRODUCT can under-count or only the FACT reads short, and fix the one that is wrong. NO RETRY, NO SLEEP, NO WIDENED BUDGET. Evidence preserved: the TRX at the floor run's results directory names the fact and the line. **UPGRADED 2026-08-24: IT IS A THIRD FACT, WHICH MAKES IT A CLASS-WIDE PATTERN RATHER THAN A PAIR OF ONE-OFFS.** Caught with its TRX on a full run at floor 3159: `Construction_AbandonedThenFaults_CountStillDrops_CapNeverRefusesForever`, `Expected 1, Actual 0` at `SoundArbitrationTests.cs:1583`. Confirmed as the same intermittent - the class is 3/3 green in isolation and the full suite is green on re-run - and confirmed NOT caused by the merge it appeared under. So the known instances are now THREE: `Construction_AtTheOutstandingCap_...` (closed this week by giving the FACT a deterministic condition wait after proving the product's arbitration correct under deliberate starvation), `Construction_LockUnavailableAtCompletion_...`, and this one. THE FIX IS NO LONGER 'give this fact a deterministic wait': every construction fact in the class measures THREAD-POOL DISPATCH LATENCY against a 200 ms `task.Wait` budget, so the class needs one answer rather than three patches. Scope when admitted: do the separation ONCE for the class - reproduce under deliberate pool starvation, establish whether the PRODUCT can ever under-count or only the FACTS read short, then fix whichever is wrong for all of them. NO RETRY, NO SLEEP, NO WIDENED BUDGET. |
| P1 | DONE | LIVE REGRESSION: closing the DTRH window now cuts off an in-flight flash sound and any live pops. | **INTRODUCED 2026-08-24 BY THE SOUND SLICE, AND NAMED BY THE LANE THAT INTRODUCED IT rather than left for someone to find.** `Features/Dtrh/DtrhHostWindow.axaml.cs:288-293` calls `PanicReset()` on window close, which stops EVERY channel on the app-wide arbitration - and its own comment already predicted this: 'the day a second one lands this call has to become a stop of the channels this window used rather than of all of them.' The flash and bubble sounds ARE that second consumer, so the hypothetical is now real: close the DTRH window while a flash whisper or a pop is playing and it is cut off. Both halves of the fix are outside that packet's scope - `Features/Dtrh/DtrhHostWindow.axaml.cs` for the caller and a per-channel stop on `Audio/SoundArbitration.cs` for the mechanism - so it was reported rather than widened into, which is correct. Scope when admitted: give the arbitration a stop scoped to the channels one owner used, and pin it with a fact that reds when a second consumer's audio dies with the first consumer's window. **FIXED 2026-08-24, AND THE FACT SAW THE DEFECT BEFORE IT SAW THE FIX.** Run in that order and reported: with the seam reproducing the window's old line, the new fact FAILED at `Assert.True(flash.Playing)` - the in-flight flash whisper really was cut off by the DTRH window's close - while the panic-still-total fact PASSED, which is what makes the first failure a defect rather than a harness that cannot stop a whisper. OWNER = A `SoundChannel`, and the alternatives were rejected with reasons rather than waved past: a per-play token is a genuinely NEW concept that would decide nothing extra here, because voice and whisper are exclusive stop-replace so a second consumer's line has already stopped the first's long before any window closes; and a registered-consumer handle adds that same new concept plus lifetime management to a type already juggling generations and a two-path teardown. `PanicReset()` KEEPS ITS NAME, SIGNATURE, LOG LINE, ORDERING AND REACHABILITY, and its totality is now a property of the ENUM rather than of one method remembering three field names - so a fourth channel joins panic without an edit, which makes the panic path harder to get wrong rather than easier. One mutation pins the CHANNEL CHOICE and not merely 'something scoped': swapping Whisper for Voice reds it. |
| P1 | OPEN | The product's OWN stated teardown path - surfaces reclaimed by the OS at process exit - is untested, and the new safety fact does not reach it. | **NAMED 2026-08-24 BY THE LANE THAT BUILT THE TEARDOWN FACT, against its own result.** `Session/SessionParticipant.cs:889-900` records that its dispatched teardown DOES NOT RUN on the ordinary path, because the dispatcher is already down by then, and that the surfaces are instead 'reclaimed by the OPERATING SYSTEM at process exit'. The new `SurfaceTeardownTests` proves the invariant for the case where the disposals DO reach the creating thread - it asserts that they did - so what is now covered is the guarded `ShutdownAsync` path, and the product's stated ORDINARY path is not. THAT IS THE MORE IMPORTANT HALF: relying on process death means a surface's lifetime is the process's, and anything that keeps the process alive after the shell closes - a background participant, a non-background thread, a hung native call - leaves a topmost input-blocking window on a user's desktop with nothing on screen to close. Also untested and named: abnormal termination (kill, panic without unwinding), and the invariant's other clause about display changes and monitor hot-plug. Scope when admitted: decide whether the dispatched teardown should be made to run before the dispatcher goes down, or whether process-exit reclamation is genuinely sufficient - and if it is, prove it rather than asserting it. `Session/SessionParticipant.cs` is a product change, so this is not a test-only row. |
| P1 | BLOCKED | A user cannot click OR type their way out of a playing video - the clip cap is the only exit. | **NAMED 2026-08-24 by the lane that settled the click question, against its own result.** Having established that swallowing clicks is CORRECT and matches upstream, it then said what that leaves: upstream ACTIVATES its primary video window so ESC can dismiss the clip (`Services/Video/VideoService.cs:2619-2636` `ShowActivated`, `:2920-2921` `Activate()`, and the non-strict branch at `:4328-4345` consuming ESC as 'dismiss this video'). THIS PORT SETS `WS_EX_NOACTIVATE` and has no dismiss key, no strict lock and no grace pause (census #21/#22, `Effects/VideoSurfacePresenter.cs:143-152`). So the surface correctly owns the pointer and correctly refuses the keyboard, and the combination is that a playing video can only be waited out. WHAT MAKES IT TOLERABLE TODAY IS A DELIBERATE CHOICE, NOT LUCK: D123 places the surface at 0.55 x 0.42 of the display rather than fullscreen, precisely because 'a fullscreen topmost surface the user cannot dismiss is the one shape this port must not ship while it has no panic key'. So the exit gap and the size choice are the same decision seen from two sides, and closing one without the other would be a regression: MAKING THE VIDEO FULLSCREEN BEFORE THERE IS AN EXIT WOULD SHIP EXACTLY THE SHAPE THAT REASONING FORBIDS. Scope when admitted: decide the exit first - a dismiss key needs the window to take the keyboard, which is a real input-policy change with its own headed evidence, and the panic key itself is owner-blocked behind the system-wide hook boundary. **ANSWERED 2026-08-24 AND THE ANSWER IS BLOCKED ON TWO NAMED GATES, established from source before any code was written.** Census #22's grace pause is upstream's exit, and ITS TRIGGER IS A KEY IN ALL FOUR DOORS: non-strict ESC (`Services/Video/VideoService.cs:4328-4346`), the panic key on the video window (`:4347-4360`), strict-branch ESC (`:4290-4301`), and the panic key from ANY application (`MainWindow/MainWindow.xaml.cs:1135` via a `WH_KEYBOARD_LL` hook). Upstream can use the first three because it ACTIVATES its primary video window (`:2624` `ShowActivated`, `:2920` `Activate()`). RESUME is the only pointer half, and even that is a button on a window built `ShowActivated = false` so it never steals focus from the video. SO: doors 1-3 need `Video/Win32VideoPresence.cs:441` to drop `WS_EX_NOACTIVATE` and take the foreground - which is 'changing an effect's input policy', reserved by `overlay-clickthrough` to BOTH advisor seats with its own headed evidence - and door 4 needs the system-wide hook, which is owner-blocked behind the input-capture boundary. **CLICK-TO-PAUSE IS NOT A THIRD DOOR**, and the lane ruled it out rather than reaching for it: it would change the surface's pointer policy from capturing to interactive (the same reserved clause), it has NO upstream citation because all three upstream render paths go out of their way to SWALLOW the press, and it would re-open the question `VideoInputRoutingTests` settled the day before. NOTHING WAS BUILT, DELIBERATELY: a pausable clip cap and frame cadence with no reachable trigger is exactly the 'landed complete and unreachable' pattern this board already has a row lamenting. WHAT REMAINS WORTH DOING WITHOUT OPENING EITHER GATE: measure the blocker instead of asserting it. The claim 'the surface refuses the keyboard' currently rests on a STYLE BIT, and this tree's own precedent (`Overlay/Win32OverlayPresence.cs:504-511`) is that a style write can succeed and read back wrong. That work was in flight when the lane died to a provider-side API error and is preserved on its branch. | |
| P1 | DONE | Twenty-four lane branches carry commits that never reached the port branch, and at least one of them is a REAL unmerged fix. | **FOUND 2026-08-24 doing the reconciliation `client/port.txt` asks for - 'preserve work whose owner or purpose is not yet clear' - and it is filed rather than swept up, because the sweeping is the dangerous part.** `git rev-list --count feat/crossplatform..<branch>` is non-zero for 24 branches: nineteen `lane/SP-0xx` and `sp-0xx` packets, four `worktree-agent-*` lanes, and one live lane. THE EASY ASSUMPTION IS WRONG AND WAS TESTED: most of these look like packets whose CONTENT landed by another path (SP-096's `TrayObservations.cs` and `TrayShellProbe.cs` are in the tree and were edited again today), so it is tempting to write the whole set off as stale tips. **It is not.** `lane/SP-143-...` ('refuse a second card ATOMICALLY, not with a read of `_prompting`', +525 lines over 5 files including `InputCaptureObservations.cs`) is NOT in HEAD: `Input/Win32InputPresence.cs:339` still reads `if (!_prompting || _window == 0)` and the file contains no `Interlocked` at all, so the check-then-act race that packet fixed is still live in the product. One counter-example is enough to make the whole set an audit rather than a cleanup. WHAT THIS ROW IS NOT: it is not a request to merge 24 branches. Several are explicitly self-cancelled (`worktree-agent-a3110cf2f2c26e373` tip reads 'SP-138: CANCELLED - accurate and pointless at the same time'), and merging a stale tip would revert live code. Scope when admitted: for EACH branch decide landed-by-other-path / genuinely-unmerged / deliberately-abandoned, by checking whether the BEHAVIOUR is in the tree rather than whether the commit is an ancestor - the two disagree here and only the first one matters. Recover the genuinely-unmerged ones as fresh work against today's tree, never by merging an old tip. SP-143 IS THE FIRST, and it must wait until the current `lane/pump-and-cap` lane lands, because both touch `Input/Win32InputPresence.cs`. Delete nothing until its branch is dispositioned; a deleted branch is the one failure mode with no recovery. **CORRECTED AND PARTLY DISCHARGED 2026-08-24.** TWO ERRORS OF MINE IN THE TEXT ABOVE, and the first one is dangerous on a row whose own rule is 'delete nothing until dispositioned': **THERE IS NO `lane/SP-143-*` BRANCH.** That commit (`355511f6d`) lives on `worktree-agent-a317780fb9c84ce4f`. A row that names a branch which does not exist is how the WRONG branch gets deleted by someone following it. Second, I wrote that `Input/Win32InputPresence.cs` 'contains no `Interlocked` at all' - it already used it three times (`_keystrokesSeen` twice, `_callbackFaults`), which I missed by reading a truncated grep. That strengthens the fix's rationale rather than weakening it: the file already reached for an interlocked write wherever a field crossed threads outside `_gate`. THE ITEM ITSELF IS DONE: the atomic second-card claim is recovered as FRESH WORK against today's tree, not by merging the tip - which would have reverted the `_disposed` guard on `Pump` and the narrowed `PeekMessageW` filter that landed the same day. TWENTY-THREE BRANCHES REMAIN UNAUDITED and the method in this row still stands: decide each by whether the BEHAVIOUR is in the tree, never by whether the commit is an ancestor. **AUDIT COMPLETE 2026-08-24. Every branch is dispositioned: SP-143 was the ONLY genuinely unmerged one, and it is recovered.** The method is the one this row demanded - decide by whether the BEHAVIOUR is in the tree, not by whether the commit is an ancestor - executed in three passes. (1) FILES: for every branch, each file it ADDED under `client/src` or `client/tests` was checked for existence at HEAD. Result: 23 branches, 82 added files, ZERO missing. (2) BEHAVIOUR: five branches modify a source file without adding one - the shape SP-143 had - so each branch's added LINES were compared against HEAD's copy of that file. Result: SP-084 100%, SP-079 91.3%, SP-083 85.4%, SP-085 84.8%, SP-089 84.6%, and EVERY line missing at HEAD is a COMMENT carrying its own packet number (`// SP-085 ordering invariant...`, `/// SP-089 seam:...`). The code landed; the packet-numbered commentary was reworded on integration. (3) THE METHOD WAS VALIDATED AGAINST A KNOWN POSITIVE RATHER THAN TRUSTED: the same comparison run on SP-143 against the tree as it stood BEFORE its recovery scores **6.8%**, with 14 missing NON-COMMENT lines including `private int _promptClaim;` and `Interlocked.CompareExchange(ref _promptClaim, 1, 0)` - the mechanism itself. So the separation is 6.8% against 84-100%, and code against comments, in the two directions that matter. THE REMAINING BRANCHES ARE: eight feature packets whose surfaces demonstrably exist (SP-091 navigation shell, SP-095 doors, SP-096 tray menu, SP-097 user-visible failures, SP-098 session spine, SP-099 overlay surface, SP-100 flash draws, SP-101 second effect); nine that change no product source at all; two exact duplicates of others (`worktree-agent-a2d700285a664b508` is SP-096, `worktree-agent-ae87422620aac72af` is SP-084); and one self-cancelled tip whose own message reads 'SP-138: CANCELLED - accurate and pointless at the same time'. THE HONEST LIMIT OF THIS METHOD, stated rather than left implicit: it compares ADDED lines, so a divergence expressed ONLY as a deletion - HEAD keeping something a branch removed - would not be caught. The control shows it catches the realistic shape, because a replaced mechanism appears as missing added lines, which is exactly how SP-143 surfaced. DELETION IS DELIBERATELY NOT DONE HERE. `client/port.txt` says not to keep dead workflow state, and these branches now qualify - but the risk is asymmetric: they cost nothing to keep and a deleted branch is the one failure mode with no recovery, so removing 23 refs should be its own deliberate act rather than a side effect of an audit. |
| P1 | OPEN | `AudioCapabilityTests` asserts the OS reports NO active render session for this process, and belongs to no collection while three other classes open real devices. | **FOUND 2026-08-24 by the lane recovering the atomic second-card claim, which chased it, cleared its own change, and handed it over rather than shipping past it.** The sequence: a floor run reported `WithNoDeviceOpen_TheOsReportsNoACTIVERenderSessionForThisProcess` failing, and a direct full run right after failed that plus `AfterTeardown_TheOsNoLongerReportsAnActiveRenderSession`. The lane STASHED its change, rebuilt, and ran the base assembly: 2863, 0 failed - so it looked like its own work. Three subsequent runs WITH the change (full assembly, floor gate, and the audio classes three times) were all green. WHY THIS IS THE RIGHT DIAGNOSIS RATHER THAN A SHRUG: both facts are NEGATIVE CONTROLS - they assert the ABSENCE of an active render session for this process - and `AudioCapabilityTests` carries no `[Collection]` at all, while three other classes open real render devices and can run in parallel with it, with no lease between them. A negative control that shares a machine-global resource with unserialised peers can see a peer's session and report it as its own. That is the same SHAPE as the real-desktop defect this branch already closed: the harness creating a state and then reporting it as the product's. THIS IS ALSO THE MOST LIKELY EXPLANATION FOR THE UNNAMED ONE-IN-EIGHT FAILURE ROWED DIRECTLY BELOW, and it should be checked against it first. Scope when admitted: establish which classes open real render devices, then serialise them the way the interactive desktop is serialised - a collection plus, if two processes can collide, a lease - or make the negative control attributable to this process rather than to the machine. NOT by weakening the assertion: 'no ACTIVE render session' is exactly the claim worth making. |
| P2 | OPEN | One unit fact failed once in eight full runs and I DID NOT CAPTURE ITS NAME. | **RECORDED 2026-08-24 as an admission rather than a finding, because the alternative was to call a tree green that had just gone red in front of me.** Immediately after the effects-slice merge, a full `CcpClient.Tests` run reported `Total: 2912, Failed: 1`. Seven subsequent full runs on the same binaries were `Failed: 0` (one re-run plus a deliberate six-run hunt), and the floor gate is green. So the observation is ONE failure in EIGHT runs and no idea which fact. THE PROCESS FAILURE IS MINE AND IS THE MORE USEFUL HALF: I ran the assembly with output going to the terminal instead of to a file, so when it failed the identity was already unrecoverable - I could only re-run, and a re-run cannot name what a previous run saw. Two rows on this board exist because somebody re-ran a red and moved on; this one exists so that does not quietly happen a third time. WHAT IS NOT KNOWN: whether it is a real race in newly landed code (the effects slice added a 500 ms reconcile loop and a real-desktop rebuild observation, both new shapes in this suite), a pre-existing intermittent that has simply never been seen, or an environmental collision with a concurrent lane build. IT IS NOT ASSUMED HARMLESS. Scope when admitted: reproduce with output CAPTURED - `./CcpClient.Tests.exe -trx results.trx` or a redirect - across enough runs to see it twice, then treat it like the two intermittents already closed on this board: name the mechanism, decide whether the product or the fact is wrong, and fix that one. NO RETRY, NO SLEEP, NO WIDENED TOLERANCE. Note for whoever picks it up: `check-floor.mjs` PRESERVES its results directory and writes TRX, so the cheapest reproduction is a loop over the floor gate rather than over the raw executable - the machinery to catch this by name already exists and I simply did not use it. |
| P1 | DONE | During a scripted session the Studio dials LOOK live and are not, and slice 3 is what made that reachable. | **ADMITTED 2026-08-24 as a DEFECT rather than a feature, and it is one I introduced.** Census entry #89, upstream `MainWindow/MainWindow.SessionFeatureLock.cs` (754 lines + 42 attribute sites). A scripted session borrows the eleven preset documents and writes its own values into them; the Studio dials bind to those same documents. So while a session runs, a user can drag a slider, watch it move, and have it silently rewritten within a second by the next tick - and then discarded entirely at the restore. THIS WAS HARMLESS UNTIL 2026-08-24 because nothing could start a session; slice 3 gave it a surface and made it reachable the same day. `Views/Pages/StudioPage.axaml.cs` contains no `IsEnabled` anywhere, so nothing currently tells the user the dials are on loan. Upstream's answer is to LOCK the affected controls for the session's duration and say why. Queued behind the recap lane, which owns that file today. **CLOSED 2026-08-24.** THE RULE WAS DERIVED FROM SOURCE RATHER THAN FROM MY DESCRIPTION: a dial is owned iff its document is one of the ELEVEN a run borrows AND upstream marks its counterpart `Owned` - because the client's restore is a whole-document `Replace`, so every member of a borrowed document is discarded at the end, which is why upstream's 42 `Owned` sites and not `Apply`'s write set are the specification. 30 controls lock, each traced to its upstream site. WHAT DELIBERATELY STAYS LIVE IS CITED TOO, and that half matters as much: BouncingText opacity, BubblePop size, MindWipe volume, LockCard strict, all four Brain Drain controls, the ramp's enable/duration/multiplier, the whole Scheduler panel, haptics, Loom and both new pickers. Over-locking is as much a regression as under-locking. A DIVERGENCE FROM UPSTREAM WAS REPORTED RATHER THAN IMPROVISED: upstream marks the haptics tab to stop a premium-rail mid-session refusal becoming decorative, and this port has no such refusal and never touches that document, so locking it would be over-locking. THE TOOLTIP HALF IS DELIBERATELY NOT PORTED - Avalonia has no `ToolTipService.ShowOnDisabled`, and shipping without it would mean the explanation never appears at all, so a BANNER naming the running session is the explanation instead. Headed evidence picks the Lock Card Repeats slider BECAUSE A SESSION LOCKS IT BUT NEVER WRITES IT, so both captures are the same control at the same value and the only thing a check can read is the disabled livery: 0.222 against 0.000 both ways. NOT PROVED: the pixel evidence covers ONE of the thirty; the other 29 are headless `IsEnabled` facts, which are claims about the visual tree rather than about pixels. **CORRECTION 2026-08-24, and it is mine.** This row told a later packet that upstream session-locks both Pop Quiz dials 'so the feature-lock rule already covers them'. Right about upstream, WRONG about the consequence here, and the lane resolved it from source rather than taking it: **`Owned` is about CUSTODY, not prescription.** Upstream marks those dials because its run SNAPSHOTS both pop quiz values (`Services/Session/SessionEngine.cs:919-920`) and WRITES THEM BACK (`:1544-1545`), so a dial the user moves mid-session is silently discarded at the end - that is the harm the lock prevents. This port borrows eleven documents and `session_popquiz.json` is not one: nothing snapshots it, nothing writes it back, so the value is still the user's afterwards. Greying it would claim a custody this build does not take, which is the direction the lock's own rule points when in doubt. Same divergence Brain Drain and Haptics already carry, not a new one. The close condition is written at the marker: the day `ScriptedSessionDials` takes a twelfth document and it is the pop quiz one, both dials must gain the marker in the same change. |
| P1 | DONE | The client has no open-or-save mechanism at all, and that single absence blocks nine census entries. | **ADMITTED 2026-08-24 as a SEAM, deliberately ahead of any feature that wants it.** Verified: zero `StorageProvider` and zero `DragDrop` hits anywhere under `client/src`. Nine entries are blocked on it - phrase backup (#9, a DATA-LOSS risk), preset export/import (#8), mod install/export (#2, #4), the assets tab's import (#7/#48), wallpaper and startup-clip folders (#23, #25), corner GIFs (#91), audio layers (#112). WHY THIS IS A SEAM ROW AND NOT A FEATURE ROW: `client/port.txt` reserves the PATH boundary to the owner, and the honest reading is that a USER-INITIATED picker is not a broadening of it - the user chooses the file, in a system dialog, per operation. The rule exists to stop the app reading or writing paths of its own accord, and this seam must be built so that it CANNOT: no default directory that is not the user's own choice, no remembered path used without a fresh gesture, no enumeration beyond what the picker returns. Avalonia 12's `IStorageProvider` is the API; research it against current official documentation before choosing it, because this port has no other file dialog and the first one sets the pattern. Cross-platform by construction or it does not land: a Windows-only picker would fail the same rule every other seam here is held to. **BUILT 2026-08-24 and all four constraints are pinned by facts.** `Storage/UserFilePicker.cs` carries them in its own doc comment as the REASON for its shape. No default directory; no remembered path (the picker holds one readonly field, proved by reflection); no enumeration (a SOURCE SCAN over all of `client/src` for ten Avalonia storage members, so it binds the whole tree and the still-blocked consumers inherit it); and no path or file name out (exactly one string leaves the seam - the file's content - and failure reasons are enum codes, never `ex.Message`, because an IOException's text carries the path). THE API WAS RESEARCHED FROM PRIMARY SOURCES - the avalonia-docs MCP tools were NOT in the lane's tool set, so it used the official docs, the shipped `Avalonia.Base.xml`, and Avalonia source at tag 12.1.1. Three findings came from source rather than assumption: `TopLevel.StorageProvider` is NON-NULLABLE and degrades to `NoopStorageProvider`, so without a probe a missing backend is a DEAD BUTTON rather than an error; `DefaultExtension` carries no leading dot; and the seam must truncate itself. `[NotClientImplementable]` broke the first design outright - a fake provider does not compile - so the seam was SPLIT, with the bytes half fully headless-provable and the platform half reduced to a probe, one call and a dispose. LINUX: `UsePlatformDetect` selects X11, whose `FallbackStorageProvider` chains xdg-desktop-portal, GTK3 and Avalonia's managed dialog, so there is always a picker; the NATIVE WAYLAND backend in 12.1 exposes NO storage provider at all, this client does not opt into it, and the probe makes that a typed refusal. |
| P1 | DONE | The AI validator and executor are built, reach an EMPTY handler map, and so ask her for a spiral and nothing happens. | **ADMITTED 2026-08-24: the highest visible-win-per-line item in the census.** Entries #65 and #85 are the same gap counted twice and are merged here. `Ai/AiCommandExecutor.cs:45-71` has real, tested permission gates defaulting to `NoneAdmitted`; `Features/Companion/CompanionParticipant.cs:87` constructs the executor with an EMPTY handler map; and 52 files under `Effects/` are live under `Session/SessionParticipant`. So every part exists except the bridge between them. The census flags `Ai/AiCommandExecutor.cs:15` ('NO effect backends exist in the greenfield client') as a stale in-source claim and that is confirmed - it must be corrected when this lands. PAIR IT WITH #64, the permissions grid, which is a surface over those same gates: shipping the bridge without the grid would let her act with no way for the user to see or change what she is allowed to do, and the gates default closed precisely so that cannot happen by accident. **NOTE 2026-08-24:** the executor's map is now wired in the composition root, so `Executor.Handles` is real and the permission rows stop reading 'not in this build'. The DISPATCH gap is unchanged and is the P0 owner row: her replies are still not read as commands, and `CompanionWindow`'s `EffectDispatchNotice` says so unconditionally. |
| P1 | DONE | A leaked `HapticToyServer` loopback listener fails the assembly at CLEANUP, intermittently, and it will red a land run at random. | **FIXED 2026-08-24, AND IT WAS THE OPPOSITE BUG: nothing was leaking.** Reproduced first - 1 of 14 serial assembly runs on Linux, `Errors: 1 / Failed: 0`, `HapticToyServer: http://127.0.0.1:53798`. Then caught in the act with temporary instrumentation (since deleted), which recorded the throw with its stack: `HttpListenerException errorcode=98 "Address already in use"` out of `HttpEndPointManager.GetEPListener` <- `RemovePrefixInternal` <- `RemoveListener` <- `HttpListener.Close(force)` <- `HapticToyServer.Dispose()`. MECHANISM: off Windows this is the MANAGED `HttpListener`, where `Stop()` and `Close()` BOTH call `HttpEndPointManager.RemoveListener`. The first really does remove the endpoint and release the port; the second finds nothing registered for that port, so `RemovePrefixInternal`'s `GetEPListener` **binds and listens on it AGAIN** purely to remove it (dotnet/runtime `Managed/HttpEndPointManager.cs:148-164`). When that re-bind loses a race it comes back as `HttpListenerException`, `Dispose`'s catch swallows it, the server stays registered by the registry's own rule, and the fixture reports a leak for a listener that is already shut - the instrumentation bound the same port successfully a moment later, and `ss` showed nothing on it. So the registry was lying, not the teardown. FIX: `Close()` alone, in BOTH `HapticToyServer.Dispose` and `ButtplugToyServer.Dispose` - the sibling had the identical defect and was caught throwing the identical exception in the same run. Every other loopback server in the tree already calls `Close()` once (`AiProviderLab.cs:330`, `LoopbackServer.cs:145`, `ChaosTunnelLoopback.cs:82`), so this restores the house pattern rather than inventing one. THE LEAD IN THE ORIGINAL ROW IS REFUTED: `HoldFreePort` is not involved. A bound-but-not-listening reservation socket does not block the re-bind at all - measured 10/10 - and neither does TIME_WAIT; only a LISTENING socket on the port does, and `HoldFreePort` never listens. PROVED: 15/15 serial assembly runs clean, 0 `Errors:` lines, after 1/14 before - plus 9/9 clean under THREE CONCURRENT assemblies, the condition in which the old code leaked two ports (one `HapticToyServer`, one `ButtplugToyServer`) inside a single process. INVERTED: a deliberately undisposed `HapticToyServer` in a scratch fact still reds the fixture with the same message and port, so the self-check kept its teeth. No skip, no retry, no wall-clock wait, no assertion weakened, and the floor is untouched (no test added or removed). COORDINATOR'S FILED LEAD WAS WRONG AND IS RECORDED AS REFUTED RATHER THAN QUIETLY DROPPED: this row originally pointed at the `HoldFreePort()` rewrite as the first thing to look at. It is NOT involved, and the refutation is a measurement rather than an opinion - a bound-but-never-listened socket does not block the re-bind (10/10), and neither does TIME_WAIT; only a LISTENING squatter reproduces it (10/10). The row also called `HttpListenerException` "genuinely ambiguous"; it is not - that exception can only come from the re-bind, which is what resolves the direction. WINDOWS EVIDENCE, added by the coordinator at the land because the fixing lane explicitly had none: `Stop()` there is the HTTP.sys-backed listener, not the managed one, so its removal needed its own proof - unit assembly 2844 / 0 failed / 2 skipped / **Errors: 0** on three consecutive Windows runs, plus both gates. |
| P1 | OPEN | Deliver the remaining Trainer Card behavior that is meaningful without unapproved sharing. | WPF behavior evidence, current platform research, focused tests, and headed evidence for user-visible work. **LANDED 2026-08-23, and the reconciliation was half the work.** The census was stale on more than its conclusion: it said no Trainer Card code existed in `client/`, that B7/B8/B9 had nothing subscribing, and that there was NO achievement subsystem at all - all false since `9057cfa5b`, and its own guard could not see it because nothing watched the port side. Corrected with a table of claim-versus-reality, each opened rather than inferred. THE CARD ITSELF renders what the port ACTUALLY GRANTED and claims no tier it cannot verify: an unreadable record answers `Unknown` with a NULL count rather than a zero that would read as a score, and an untracked achievement says NOT TRACKED rather than "0 of 25". SHARING AND EXPORT ARE ABSENT, NOT DISABLED - held by two chokepoint facts (the page declares exactly one interactive control; the type contains no write, no HttpClient, no clipboard, no process start). ITS READ DELIBERATELY AVOIDS `PersistenceStore`, and the reason is the good part: that load MUTATES - it adopts an orphaned temp, deletes a stale one and quarantines what it cannot bind - and a card being LOOKED AT must not rename a user's record; a fact seeds a corrupt record plus a stale temp and proves both survive byte for byte. Placed as a module on the Intake page rather than a sixth rail door, because `ShellRoutes` refuses a door whose destination is nearly empty. Floor +15 unit, +4 headless. REMAINING FOR DONE: a headed capture - nothing here proves the card is legible, correctly laid out or coloured, that it fits without clipping, or that anything composites. Portrait, wardrobe, pinned badges and the community banners stay out; they need bytes and owner decisions this row does not have. **THE HEADED GATE IS NOW DISCHARGED FOR THE CARD'S DEFAULT STATE.** The verification harness gained a `trainer-card` surface: driven to the Graded Intake door by REAL INPUT and gated on the CARD'S OWN UIA text - the title, all four award-row names and the record note - before any pixel is read. It also enumerates the page's buttons and refuses on a share/export/upload-shaped one, because a pixel cannot see a control that is not there, which is how the absent-sharing claim gets headed evidence at all. Measured: `trainer-card-ground` 108932/108932 = 1.000, `trainer-card-ink` 671/3020 = 0.222. Inverted against a REAL wrong capture (the dashboard): 0.013 and 0.000, exit 2. NO UNIFORM CAPTURE CAN PASS THIS SURFACE - the two colour bands are disjoint by arithmetic, and a fact asserts that over the manifest itself rather than trusting the measured numbers, which is precisely the vacuous green a CAPTURE PASS over an all-black image already cost this board once. WHAT IT DOES NOT PROVE: a pixel check is evidence about colour and geometry at named offsets - not legibility, wording, contrast or usability; only the `no-runs-yet` state has headed evidence, and the Read, Unreadable and earned-row states have none; Windows only, one scale, one window size. |
| P1 | BLOCKED | Define the safe, supported scope for Goon Game beyond local practice mode. | **DELIBERATELY NOT DECIDED BY ME, and this is the one row where I am declining the authority I was given.** Everything needed to decide it is measured and on file (`client/docs/goon-game-census.md`, the four upstream `docs/GOON_*.md` contracts, and the shipped client tree), so this is not blocked on evidence - only on a human saying yes to a specific risk surface. WHAT IT WOULD OPEN, each measured rather than feared: (1) it is the port's FIRST outbound network boundary - today the only `HttpClient` here is a loopback probe, so this builds a boundary rather than extending one; (2) with no relay server a successful connection is DIRECT, so **each player learns the other's public IP address**, and that composes with a `/join` route that is free and needs no account; (3) a second channel, `goon-media`, carries **the user's own photos and videos to another person**, not to a service; (4) two of the eight routes are `/report` and `/blocked`, which are MODERATION - explicitly named as untouched by the owner's 2026-08-14 authorization, so admitting them would be reversing a standing decision rather than making a new one; (5) hosting is tier-2 gated behind an `X-Auth-Token` header, so it also depends on the still-open question of whether the port may TRANSMIT the borrowed login token. WHAT DOES NOT NEED ANY OF IT: practice mode, which already ships (`Features/Goon/`, 7 files / 1,810 lines, wired at `Views/Pages/PlayPage.axaml.cs:34,40`) and needs no network, no partner media, no mic and no moderation. THE HONEST POSITION: 'finish the port' is not a mandate to turn a single-machine feature into one that leaks a home IP address and ships private media to a stranger. That is a product risk decision about real users, it is cheap to say yes to later and expensive to unwind, and it should carry a human's explicit sign-off rather than arrive as a side effect of a completeness push. A join-as-guest-only subset is the smallest thing that could be admitted separately, because it needs neither the token nor the moderation routes - but it still exposes the guest's IP to the host, so it is a smaller decision, not a free one. |
| P1 | DONE | Make real-desktop pointer coexistence verification reliable or explicitly machine-gated. | MECHANISM FOUND AND HALF THE CAUSE FIXED; the residual is characterised and is a different cause. THE DETERMINISTIC HALF: the tray right-click gesture's only real Win32 call is `SetForegroundWindow` on the tray's OWNER window (`Tray/Win32TrayPresence.cs:604`), and that owner is HIDDEN. A hidden window cannot come forward, so the request can only fail - and the REFUSED request leaves the whole PROCESS unable to place any window in the topmost band afterwards, which is the overlay, the glyph surface and the video surface. Measured, not inferred: a later `SetWindowPos(HWND_TOPMOST)` returns TRUE and applies NOTHING, extended-style read-back identical before and after, 32 assertions running. Guarded on `IsWindowVisible`. VERIFIED BY INVERSION, the only way this one can be shown: with the guard removed the new fact AND the three `PointerCoexistenceTests` facts fail together; with it the combined filter is 23/23. The fifth hypothesis was the right one - the four the row already killed stay killed, and the earlier "the single trigger is `TheShellsRightClickNotification`" finding was an artefact of a seven-test filter: with `TrayCapabilityTests` excluded ENTIRELY the full suite still failed, which is what sent this to the second cause. THE RESIDUAL, and why this stays WIP: the suite is also sensitive to whole-machine contention, and thread count is NOT the variable. Measured on this box, same 2649 facts: 16 threads idle = green in 44.9s; 8 threads with one agent running = one real-desktop fact red; 8 threads again = green; 16 threads with twenty-one agents running = three red; `-parallel none` = green in 188s. A parallelism cap was therefore measured, considered and NOT TAKEN - it would slow every run for a condition it does not cure, and the 8-thread red proves it does not cure it. What remains is to decide whether the real-desktop collection gets a contention precondition it reports honestly, or whether the band assertions get a documented machine class. Do not weaken the click-through assertions to close this. **CLOSED 2026-08-24, and the answer was RELIABLE rather than machine-gated - which matters, because a machine gate here would have hidden a real mechanism behind 'this box is unlucky'.** The three `PointerCoexistenceTests` reds were the foreground-permission defect, not contention and not this machine: proved by a standalone ~60-line repro outside the suite entirely, and by the full assembly reproducing them at `-parallel none` with zero concurrency. Now green in both modes on Windows and on Linux. |
| P1 | DONE | The floor gate reds four real-desktop facts; ONE of the two causes is now removed. | THE RUNNER CAUSE IS FIXED. The floor now runs each project as the xunit v3 ASSEMBLY with `-trx` instead of shelling out to `dotnet test`; that is the project type's own first-class run mode and emits the TRX this gate already parses. FLOOR OK on the merged tree: 2665/2665 and 154/154, 0W/0E. The evidence, back to back on ONE build, interleaved A-B-A to rule out drift: `dotnet test` 4 failed / 2643 passed; the assembly directly 0 failed / 2665; `dotnet test` again 4 failed. **I FIRST WROTE THAT THIS MADE THE FOUR A PURE RUNNER ARTEFACT AND THAT NO PRODUCT CHANGE COULD CLOSE THEM. THAT WAS TOO STRONG AND IS CORRECTED HERE.** A-B-A ran on a QUIET box. It establishes that VSTest adds a reproducible failure the exe did not show under those conditions; it does NOT establish that the exe is immune - and a concurrent measurement on this same machine, while several headed suites were running at once, reds the SAME four under the direct exe with the same read-back (`overlay-style-refused ... missing 0x8 of WS_EX_TOPMOST after 32 topmost assertion(s), last SetWindowPos(HWND_TOPMOST) SUCCEEDED`). SO THERE ARE TWO CAUSES AND ONLY ONE IS GONE: these are real-desktop Win32 facts that need an UNCONTENDED DESKTOP, VSTest was one cause and is removed, and DESKTOP CONTENTION IS THE SECOND AND IS STILL OPEN. That is exactly the "or explicitly machine-gated" half of the pointer-coexistence row's title, and it is what keeps this row WIP. NOTHING WAS RELAXED TO GET GREEN: the result-list anchoring, both staleness checks, the bad-outcome and bad-counter refusals, the exact-total pin and the name-anchored allowed-skips all still run, and the gate still never retries. Two narrow adjustments were needed. (1) The head check demanded an XML DECLARATION, which XML 1.0 makes OPTIONAL and the xunit writer omits; truncation is caught by the `</TestRun>` check, which is the half that ever detected a run dying mid-write, so both openings now pass and anything else still fails. (2) `WarningGateGuardTests.TheTestFloorStillRunsNoBuild_AndKeepsItsStaleBuildGuard` asserted the floor invokes `dotnet test`; its INTENT is "the floor must never become a builder", which running a prebuilt assembly satisfies structurally. It now accepts zero dotnet invocations OR the `test` verb, still rejects a non-literal argument list, and - because "no dotnet invocation" is also true of a GUTTED gate - additionally pins the direct-runner shape and the stale-build guard. Reintroducing a build in either shape still reds it. Bonus: the xunit counters are STRICTER than VSTest's - they close over skips (2663 + 2 == 2665) where the VSTest arithmetic did not, which is why the result-list anchoring existed in the first place. **CLOSED 2026-08-24: the SECOND cause is now removed too, and it was never desktop contention.** It was documented Win32 foreground permission - `SetWindowPos(HWND_TOPMOST)` returns TRUE and silently declines to apply `WS_EX_TOPMOST` when the process lacks it - and the harness was creating that state itself by letting a clicked window's own thread reach zero windows. See the foreground-permission row for the mechanism and the fix. |
| P1 | OPEN | Validate real media and multi-monitor video behavior on Windows, X11, and Wayland as separately claimed. | Decoding, geometry, compositing, audio, teardown, and headed capture evidence for each supported backend. |
| P1 | OPEN | Maintain an honest upstream payload inventory after legacy changes. | Compare the current source payload, client payload, and inventory; update consumed documents and run the inventory tests. |
| P1 | OPEN | Port the Arcademy upstream surface, decomposed into its eight slices. | OWNER DECISION 2026-08-23: IN SCOPE. Sized against source rather than guessed: `Services/Arcademy/` is 2 files / 2,546 lines (`ArcademyHostService.cs` 2,008 incl. a 305-row lexicon table, `ArcademyMetaStore.cs` 538), and `Resources/web/arcademy/` is 88 files / 2.6 MB (74 JS totalling 27,555 hand-written lines, 1 HTML, 1 CSS, 11 binary assets, plus vendored three.js r185, MIT). THE COST IS THE C# HOST, NOT THE JS: the payload is served byte-identical the way dtrh/intake/tunnel/goon already are, and the client already carries `Avalonia.Controls.WebView` 12.0.1. A T2-gated WebView2 school of three daily mini-games, reached from the Play tab's MORE zone ('Attend'), never a tab. TWO TRAPS FOUND: (a) the payload does NOT stand alone - the shell borrows DTRH's spiral assets by relative URL (`shell/shell.js:76`, `games/the-deep-end/pressure.js:182`), so the whole `Resources/web` root must be mapped, not just `arcademy/`; (b) UTC seeds content but LOCAL date rolls attendance, which is upstream's own regression #978. BOUNDARIES: outbound network ONLY through the optional, consent-gated remote-media broker (slice 7, deferrable, and the one slice that widens a boundary); clipboard write (text only); local user media via a CORS-Allow origin honouring the Assets deselection blacklist; subliminal phrases read-only. NO webcam, biometrics, gaze, mic, moderation, capture or secrets. SAFETY: the panic ladder (slice 6) must NOT be deferred behind the suspension slice - without it two Esc taps exit the whole app. SLICES, in dependency order: (1) payload serving + boot handshake, (2) init projection + set-setting echo loop, (3) the meta store - the cleanest slice and the one upstream admits has no machine verification at all, (4) XP + attendance payout, (5) native-state suspension (needs video + browser-media services), (6) the panic ladder, (7) remote-media broker (optional, gated), (8) entry point + T2 gating. Slices 1-4, 6 and 8 are the honest minimum for a playable Arcademy; 5 and 7 have documented null paths and can no-op cleanly. **UPSTREAM SYNC v6.8.3 -> v6.8.4 (merge `6a8b4c724`) CHANGED THIS ROW'S SHAPE, and the change is a constraint rather than more work. THE DOOR IS SHUT UPSTREAM.** `ArcademyHostService.DoorAvailable` is a `static readonly bool = false` (`ArcademyHostService.cs:116`); the Play card ships `Visibility="Collapsed"` (`Views/Tabs/PlayTabView.xaml:1312`) and is re-asserted from `DoorAvailable` on every repaint (`MainWindow/MainWindow.PlayTab.cs:106-112`); and `Launch()` refuses on the door BEFORE the T2 bar (`ArcademyHostService.cs:136-141`). Upstream's own reason, quoted: Semester 1 "landed on main (PR #241) ahead of its public reveal" and 6.8.4 "is an auth/stability patch that must ship those fixes without also shipping an unannounced feature" - a HIDE, not a lockband, "because a lockband advertises something the account could buy". CONSEQUENCE FOR THIS ROW: porting Arcademy VISIBLE would ship a feature upstream is deliberately withholding, so slice 8 (entry point + T2 gating) now owns a door gate ahead of the T2 gate, and the honest default is hidden. It also means this surface CANNOT be headed-verified against the shipping app by a user - the door is shut there too. SIZES MOVED: payload 88 -> 91 files (`games/impulse-control/{casino,pressure,trickster}.js` from the Impulse Control House Rules wave, plus edits to index/lex/render/style/tube2d/tube3d), and `ArcademyHostService.cs` +51 lines including 13 new `ic_*` lexicon entries. The row's earlier 2,546-line and 88-file figures are superseded. **SLICES 1-2 LANDED 2026-08-23, AND THE DOOR SHIPS SHUT.** The 91-file payload is served BYTE-IDENTICAL as the sixth linked read-only `Content` glob, and the port is the FOURTH consumer of the same `LoopbackServer` that dtrh, intake and goon already run on - not a new server. THE MEASURED TRAP WAS SOLVED RATHER THAN PAPERED OVER: the payload does not stand alone, because its shell borrows DTRH's spiral assets by RELATIVE URL, so the serving root is the whole web mirror rather than the arcademy subtree - no asset was copied to make it work. The `.svg` MIME row went into the PAYLOAD table ONLY and deliberately NOT the user-media table, because an SVG served as a document can carry script and `/umedia/*` serves files a user dropped into their own folder; deny-by-default is unchanged and a new `.md -> 415` fact keeps that leg honest. `ArcademyDoor.Available` is a `static readonly false` with NO override seam, and `Attend()` refuses BEFORE any allocation - no participant, no bound origin, no port, no page - so this surface is unreachable exactly as upstream withheld it at v6.8.4. Floor +24 unit. NOT DONE: slices 3-8. NOT PROVED: no browser was started, no page loaded, not one line of the payload's JavaScript ran, no window opened - the handshake and echo loop are proved as FRAMES AND STORE EFFECTS, never as a page that received them. Nothing here says a class is playable. **SLICES 3-4 ALSO LANDED 2026-08-23.** The meta store is ported with the port's own persistence idiom, and its facts are the FIRST MACHINE VERIFICATION this behaviour has ever had - upstream ships none for it. THE TWO CLOCKS ARE NAMED APART, which was this row's stated trap: upstream reads UtcNow for the ledger and Now for attendance (its own regression #978), and the port carries the SPLIT rather than quietly fixing or quietly copying it - collapsing it is user-visibly wrong in BOTH directions, because one clock either pays twice every evening or freezes the streak at the wrong hour for most of the planet. It is explicit in the type as `ArcademyClassDay(UtcSeedDay, LocalAttendanceDate)` - no bare `string date` exists anywhere on the path - and the fact drives ONE injected instant that falls on two different days. Verified by MUTATION: handing the UTC day to the attendance call reds it with the wrong streak. The payout has nowhere to land and SAYS SO rather than inventing an XP economy to receive it. A real .NET 10 defect surfaced on the way: `[JsonExtensionData]` over a `JsonObject` deserializes but throws on WRITE, so every read of the blob would have died; 18 of 22 facts caught it at once. Floor 2756 -> 2778. STILL NOT DONE: slices 5-8, and nothing here is reachable - the door remains a `static readonly false` with no override seam and no fact flips it. NOT PROVED: no browser, no page, not one line of the payload's 27k lines of JavaScript ran, no headed evidence, no Linux run. **SLICES 6 AND 8 LANDED 2026-08-23; THE HONEST MINIMUM (1-4, 6, 8) IS NOW COMPLETE.** Slice 6 was done FIRST because this row calls it a safety slice: the panic ladder, so an Esc inside a class cannot reach the application. Every rung is mutation-checked, and the ORDER fact counts authority calls (zero) rather than reading code, so reordering the gates makes it one. **AN HONEST LIMIT, recorded rather than papered over: the port has no app-wide panic hook to hand a press to, so the fall-through this row feared cannot occur today because the fall-through TARGET does not exist yet** - the rung above it is written and proved for when it does. Slice 8 puts the T2 bar BEHIND the door in upstream's own order (the door is checked synchronously before the first await), and the Play-tab entry ships HIDDEN and is re-asserted on every mount; two headless facts prove it mounted and unreachable, and revealing the strip reds both. TWO SOURCE-VERSUS-SPEC CORRECTIONS TO THIS ROW: `resume-request` is slice 6's frame, NOT slice 5's - upstream refuses every reason but panic, because a video suspend lifts when the video ends and an audio-only one when the session does; and the AudioOnlySession gate is deliberately NOT ported, because this build has no audio-only session for it to refuse and a hard-coded false would be a gate pretending to have an input. Floor 2778 -> 2794 unit, 159 -> 161 headless. REMAINING: slices 5 and 7 only, both with documented null paths. NOT PROVED: no browser, no page, not one line of the payload's JavaScript, no window, no key pressed on a real desktop (there is no hook to press into), no headed evidence, no Linux run. |
| P1 | OPEN | A not-ported upstream payload tree can grow by any amount with the whole suite green. | EXPOSED BY THE v6.8.4 SYNC, and it is the fourth time a payload guard has been the only thing between this port and a silent upstream surface. `Resources/web/arcademy/` went 88 -> 91 files in this sync and `UpstreamPayloadInventoryTests` stayed GREEN at 19/19, because `fileCountAtBaseline` is recorded as "record data, not an assertion" and the per-tree manifest tests that DO pin counts only exist for trees the client actually serves. So the guard catches a tree ARRIVING and a tree DISAPPEARING, but not a tree CHANGING - which is the state every not-ported tree is in, permanently, until its row lands. The inventory has been corrected by hand to 91 (`client/docs/upstream-payload-inventory.json`), which is exactly the hand-maintenance the citation row is currently removing elsewhere. Wanted: a mechanical count/hash per not-ported tree that reds on drift and names the tree, so the next wave's arrival is a test failure rather than something a human happened to diff. Do NOT close this by pinning counts the sync then has to bump silently - the bump must carry its reason the way `floor.json` does. |
| P1 | WIP | Regenerate the upstream citation inventory, which is two syncs behind the tree it validates against. | THE TOOL IS BUILT, which was the owner's instruction and is the half that turns bulk into tooling. `client/tools/citations/detect.mjs --regenerate` recomputes `changedAtSync` for every entry from GIT ALONE - add/del via the same `readNumstat` DELTA-MISMATCH re-measures with, so a regenerated inventory leaves that class silent by construction; status from `git diff --name-status` over the same window; `sync` from the committer date of the window's endpoint. It writes NOTHING without `--write`, and the write is BYTE-SURGICAL: only the value span after each `changedAtSync` key is replaced, in the file's own line ending at the key's own indent, and the produced bytes are re-parsed and compared against the intended inventory before anything reaches disk. It never touches `baseline` and never touches a `verdict`, which is what keeps it clear of the trap this row names - advancing `baseline.previous` moves the needle mode onto a window holding zero rows and makes `TheNeedleMode_ExitsZero_WithANonEmptyReviewList` vacuous. INDEPENDENT REVIEW FOUND THE MODE'S NAMED REFUSALS WERE THEMSELVES DELETABLE: three of the four - the rename ceiling the operator is told is COUNTED rather than guessed at, the BINARY-numstat refusal (whose removal writes `add: null, del: null` that the writer's own verification provably cannot catch, because `Number("-")` is `NaN` and both sides stringify to `null`), and the re-parse-and-compare itself - could each be mutated away with the suite fully green. All three are now pinned by facts F30-F32, and each mutation was shown to red EXACTLY its own fact and no other. Self-test 33/33; floor unchanged at 2673/154 because these are node facts. REMAINING FOR DONE: run the regeneration for real over `db3e842f..fb89d087a` (measured dry: 127 of 297 entries move, 33 rewritten / 21 added / 73 dropped, with 4 rename destinations hitting the named plain-numstat ceiling), and then the 81 tier-1 VERDICTS, which are judgment and are the only part the tool cannot do. |
| P1 | WIP | Hosted pages silently lose their motion when Windows animation effects are off. | LANDED, and the row stays WIP on headed evidence rather than on code. `MotionLevel {Full,Reduced,Off}` is ported with its picker on the System door, default `Full` from first run, persisted by ORDINAL (upstream's, and the picker order is load-bearing because it IS the persisted value - pinned by a fact that writes the int and reads it back). All five hosted surfaces route through ONE helper and a choke-point guard asserts both halves: the switch literals exist in exactly one file, and every one of the five still calls it - a surface that quietly stopped would fall back to the OS answer, which is this row's whole defect. The OS flag is READ AND LOGGED, never obeyed, so the user keeps an in-app restore; that is what makes this a fix rather than the accessibility regression the naive version would have been. INDEPENDENT REVIEW CAUGHT A RED THE LANE'S OWN FILTER COULD NOT SEE: joining the flag with the separator INSIDE the string literal defeats `HarnessEntryPointGuardTests`, which captures startup flag literals verbatim, so all five surfaces failed it while a `~MotionLevel` filter stayed green. Fixed by joining the way upstream joins. Floor 2649 -> 2665 and headless 152 -> 154; both suites green. REMAINING FOR DONE: (1) headed evidence on a machine with Windows animation effects OFF - nothing here observed a hosted page moving or not moving; (2) the review's two named test holes, an OS-flag seam so `BrowserArgument` is pinned OS-INDEPENDENTLY (today a mutant that let the OS win still passes on a machine with animations on) and a vacuous `Assert.All` over an empty list; (3) `Reduced` currently has no consumer beyond the hosted flag, so choosing it changes nothing the port renders itself - the picker ships a level with no local effect and the System-door blurb over-claims scope in its first sentence. Linux is out by construction: the switch reaches only the WebView2 branch and the GTK branch takes no Chromium switches. |
| P1 | DONE | A Brain Drain clip is cut off mid-track instead of playing to its end. | FIXED. `Effects/AudioCueEffect.cs` now skips a due window while the module's own slot is still sounding, taken BEFORE the draw so it can never read as "rolled and lost" - the same place and shape as upstream `1f8fe465b` (`if (_waveOut != null) return;`). The guard is OPT-IN (`SkipWhileSounding`, default false) and only `Effects/BrainDrainEffect.cs` turns it on: upstream fixed Brain Drain alone, and Mind Wipe's displacement is still correct, so a guard on the shared base would have silently changed a behaviour nobody asked about. Required a new presence query - `IAudioPresence.IsSounding(slot)` asks the PLAYER for its state, NOT whether the slot dictionary has a key: slots are cleared only by displacement or Silence and nothing sweeps a naturally-finished player, so an occupancy check would have answered true forever and made Brain Drain play one clip per session. Verified by inversion: the fact asserts Brain Drain skips, Mind Wipe still replaces, AND Brain Drain resumes once the clip ends, and was shown FAILING with the opt-in off. |
| P2 | DONE | Reveal in Explorer opens the Desktop instead of selecting the file. | FIXED. `Features/Dtrh/DtrhLoomReveal.cs` built `/select,"<path>"` as one already-quoted string and passed it to the string-`Arguments` overload; the CRT re-parses those quotes before explorer sees them and, on a mismatch, explorer silently opens the Desktop rather than reporting an error. Now one pre-tokenized `ArgumentList` element (`/select,<fullpath>`, unquoted - the runtime applies Windows quoting), a `Path.GetFullPath` so `/select` does not resolve against the launching process's working directory, and a missing-file fallback to the containing folder. The Linux `xdg-open` leg carried the same pre-quoting and is fixed with it. Verified by inversion: `DtrhLoomStudioTests.Reveal_ExistingSpiral_LaunchesOsSeam` now pins the exact token and rejects any quote, and was shown FAILING against the restored defect before being accepted. The old assertion was `Contains(path, args)`, which is true of the broken shape too - that is why nothing caught this. |
| P2 | DONE | Finished audio players are never released from their slot. | FIXED. `Audio/WasapiAudioPresence.cs` now subscribes `IAudioPlayer.PlaybackEnded` and releases the slot on a natural end. The subscription is taken BEFORE `Play()` and that ordering is the correctness argument, not a preference: `Play()` only queues the device thread, so the event can only arrive from that thread, which must take the gate the publish is holding - there is no window in which an end can be missed or seen against a half-published slot. Ownership is the SLOT: the handler removes and disposes only if the slot still holds THAT player, so a late end from an already-displaced player does nothing rather than evicting its replacement or double-disposing. Enabling change in the harness: `StubPlayer.PlaybackEnded` was a no-op `add {} remove {}` pair, so no fact could express a clip reaching its end - that is why this survived. It is a real event now. Verified by inversion TWICE, independently: removing the subscription reds it, and weakening the ownership check from reference-equality to occupancy reds it on the race half. |
| P2 | DONE | Brain Drain clips added while the app runs are never picked up. | FIXED, and the root cause was narrower than the row assumed: `AudioCuePool.Invalidate()` existed and was correct, but it was NEVER ON `IAudioCuePool`, so nothing the product holds could reach it - a re-scan mechanism only the tests could call. It is now on the interface, and `Effects/AudioCueEffect.cs` re-reads the folder at the arm hook (`Ready`, the only hook that runs on an arm) behind an opt-in `RescanOnArm` that only `BrainDrainEffect` turns on. That asymmetry is upstream's: Brain Drain re-scans on every session start because its folder is a drop target outside the app (`BrainDrainService.cs:222-227`), while Mind Wipe re-scans only from its panel and never from Start. Verified by inversion: the fact asserts Brain Drain re-scans, Mind Wipe does not, and the re-scan happens per ARM rather than once per process (stop, start, read again), and was shown FAILING with the opt-in off. |
| P2 | DONE | Audit uncited intra-client source references and correct stale behavior claims. | DETECTOR BUILT AND THE TREE CLEANED. `client/tools/citations/intra.mjs` is a SEPARATE tool rather than a fourth mode of `detect.mjs`, and the deciding reason is the exit contract: `detect.mjs` is pinned three times over to exit 0 on a populated list, because a changed UPSTREAM file is not automatically a defect. An intra-client citation is checkable against a file in the same repo at the same commit, so this one MUST exit non-zero and can. Everything reusable is imported from `detect.mjs`, not copied. Four classes: WRONG-LINE, DEAD-ANCHOR, UNRESOLVABLE, AMBIGUOUS-BASENAME, with exit 0/1/2 separating clean from rot from could-not-run so a broken detector never reads as a verdict. FIRST RUN FOUND 163 ROT SITES, all now fixed: 64 dead anchors into the divergence register `1bdf998e4` deleted, 14 into two documents deleted at `734741fb7`, 13 wrong lines (eight into `task-board.md`, which is rewritten every wave - those now name the ROW rather than a line, which is the root-cause fix rather than a re-count), 72 ambiguous basenames (`bridge.js` alone answers to FOUR real files), and 13 stale `AdmittedRoutes is empty` prose sites. SEVEN OF ITS 28 FACTS ARE MUTATIONS, each rewriting one uniquely-occurring span and asserting the fixture then produces the row the refusal was suppressing - with an exactly-once guard so a drifted mutation fails loudly instead of testing nothing, which is the defect that blocked the sibling tool's review. IT IMMEDIATELY EARNED ITS KEEP: it caught rot created by the haptics merge in this same session, three symbol citations whose lines had moved, before that merge was pushed. Now exits 0 on this tree - 675 intra-client references, 672 resolved clean, 0 rows. WHAT IT DOES NOT CHECK, counted rather than implied away: 2,967 bare `:NNN` continuations, because binding those needs the nearest-preceding-token heuristic `detect.mjs` measured and rejected - which is exactly the class I MYSELF misread earlier this session when I wrongly called a correct `GoonProtocol.cs` citation rot. |
| P1 | WIP | Take the Linux evidence the board has been deferring, now that a host exists. | **LINUX IS GREEN: 2,716 unit / 0 FAILED / 66 skipped, and 155 headless / 0 failed / 0 skipped.** It began at 88 failures and went 88 -> 74 -> 6 -> 2 -> 1 -> 0. Host: WSL2 Ubuntu, .NET SDK 10.0.400, on a NATIVE ext4 clone at `~/ccp` rather than the `/mnt/c` checkout - deliberately, because a Linux build over `/mnt/c` overwrites the same `bin/Debug/net10.0` assemblies `check-floor.mjs`'s `assertBuildIsFresh` measures, and the two platforms would have lied to each other. Build: 0W/0E across all four projects. WHAT THE RUN ACTUALLY BOUGHT, because a green number is not the point: it found FOUR REAL DEFECTS that no amount of Windows testing could have surfaced. (1) A Windows path separator reaching a USER - `System.IO.Path` answers with the HOST's rules, so `GetFileName` of a Windows-authored path returns the whole string on Linux and a `C:\...` literal appeared in a message on a machine with no `C:` drive; five product sites fixed behind `PortablePath`, with a source guard because the behaviour facts CANNOT invert on Windows. (2) THE REAL-DESKTOP LEASE WAS NOT A LEASE - .NET maps every `FileShare` mode except `None` to `LOCK_SH` on Unix, so two runs could both hold it; now `FileShare.None`, with the fixture taking the lease a second time while holding it and refusing loudly if that succeeds, because `flock` is advisory and NFS/SMB/DrvFs would otherwise silently stop excluding. (3) A byte-count census measuring the CHECKOUT rather than the content - git normalises text, so the same commit was 12,471,900 bytes on Windows and 12,376,646 on Linux, a difference of exactly the CRLF count; now counted CRLF-as-LF and identical on both. (4) `process.exit()` in both citation tools THROWING AWAY THE REPORT - on POSIX node's stdout is asynchronous when it is a PIPE and synchronous when it is a file, so a 975-line report lost its tail exactly when something read it programmatically; Windows never showed it because pipe writes are synchronous there. THE 66 SKIPS ARE THE HONEST HALF: 60 real-desktop facts are gated to the platform they measure, because off Windows their readings do not go absent, they go WRONG - every handle is 0, so a comparison like `IsForeground` becomes `0 == 0` and answers YES about a window that was never created. Nine further facts were KEYED instead of gated and now assert the honest Linux refusal rather than stepping aside. Every skip is in `floor.json` `allowedSkips` with the machine class where it executes, derived from the MEASURED skip list rather than transcribed. WHAT THIS STILL DOES NOT ESTABLISH, and it is most of what Linux support means: no presentation, input, focus, window behaviour or media is verified on Linux by ANY of this - the gated facts stop measuring there rather than starting to, which is exactly why they are gated and why this row stays WIP. WSLg is present (`wayland-0` and `X0` both live), so a headed Linux leg is now reachable for the first time, but a nested Weston compositor with no desktop environment is not GNOME or KDE and must not be reported as either. X11 and Wayland still need separating. |
| P1 | OPEN | The headed capture harness reports CAPTURE PASS on an all-black image. | FOUND WHILE ATTEMPTING THE FIRST HEADED LINUX EVIDENCE, and it is a vacuous green in the one harness whose whole job is to be non-vacuous. `client/tools/verify/capture-wslg.sh` ran, probed the door's real geometry (`175.0x44.0 DIP @ scale 1 @ screen 12,45`), wrote a correctly-sized BMP and printed **CAPTURE PASS** - and the image is 7,700 pixels of a SINGLE COLOUR, `(0,0,0)`, 100% black. A capture that contains no second colour cannot be evidence of anything drawn, so PASS is the wrong answer and the harness should refuse. Reproduced in THREE display configurations, so it is not a one-off: WSLg RAIL (Wayland), WSLg with `WAYLAND_DISPLAY` unset so Avalonia takes XWayland, and a real `Xvfb :99` framebuffer where `XGetImage` unquestionably holds pixels. WANTED: the capture step fails when the image carries fewer than two distinct colours, or when it is entirely the background, with the count in the message. The tier-3 `CcpVerify` checks DID catch it downstream - `rail-door-selected-border 0/525 pixels matched` - so the named checks are honest and it is the capture step's own PASS that lies. Note the ordering trap: `CcpVerify` was invoked through `dotnet run`, whose exit code is the RUNNER's, so a caller reading `$?` sees 0 even on FIRST FAILED CHECK; any harness that gates on this must read the printed verdict or invoke the built assembly directly. |
| P1 | DONE | The shell renders a diagnostic footer to the USER, unconditionally. | FIXED. `DiagnosticFooterPolicy.Rendered` is `#if DEBUG`, the shell asks it (`MainWindow.axaml.cs:52`), and a published build no longer draws the footer. THE CHANNEL SURVIVES DELIBERATELY: the probe is still built, still UIA-readable on Windows, and still logged once on first layout so it stays stderr-readable on Linux - which is the ONLY reading this port has of its rail geometry on a platform where every capture comes back black. Deleting the probe would have taken the Linux leg's evidence with it, so only the RENDERING is gated. The guard (`DiagnosticFooterGuardTests`) is a SOURCE guard by necessity: visibility is decided by `#if DEBUG` and every test run IS a Debug build, so a behavioural assertion could only ever observe the visible arm and could never fail against the defect. It pins BOTH halves - the gate must exist (a literal there would pass every behavioural fact in this suite while restoring the defect exactly) AND the probe line must survive, so the guard cannot be satisfied by deleting the channel. Verified by inversion: replacing the policy call with a literal reds it. The headed harness still passes unchanged. Floor 2755 -> 2756. |
| P1 | DONE | Headed evidence on Windows: obtained, checked, and no longer a standing gate for the rail. | THE PORT'S FIRST VERIFIED HEADED CAPTURES. `client/tools/verify/capture.ps1` launched the real shell (window up in 6.3s), all five rail doors published their layout probes, the System door was reached BY REAL INPUT, the state was driven by a real left-click on the Companion door (confirmed `route: companion` by a UIA read BEFORE any pixel was read), the screen read was fenced through `DwmFlush`, and the machine-wide real-desktop lease was taken and released around it. TWO SURFACES FULLY CHECKED by `CcpVerify`'s ten named checks: `rail-door/selected` ALL CHECKS PASSED with the border at 888/918 pixels (0.967), and `rail-door/unselected` ALL CHECKS PASSED at 896/918 (0.976). The captures carry 59 distinct sampled colours - the dark-neon ground `42,33,48`, the accent `224,102,255`, near-white text - so the theme is proved to RENDER rather than merely to be declared. A full-window `dashboard/selected` capture (1925x1330) was also taken and shows the shell, the five doors, the Companion page and the action bar; `checks.json` DEFINES NO CHECKS for that surface/state pair, so it is a picture and not evidence, and that gap is worth its own row rather than a quiet pass. WHAT THIS DOES NOT DISCHARGE: Linux, where every capture is still 100% black; and every OTHER surface - the Trainer Card, the feature popups, the hosted pages - which have no checks and no captures. |
| P1 | BLOCKED | Headed Linux evidence: the app RUNS and LAYS OUT on Linux, but no pixel has ever been read back. | ATTEMPTED 2026-08-23 AND HONESTLY NOT OBTAINED - recorded as the exact unavailable gate rather than left implied. WHAT IS PROVED: the client starts fully on Linux under WSLg. Its own startup log shows the dtrh payload Present (1,542 files), the loopback page and media origins bound on 127.0.0.1, and ALL FIVE navigation doors laid out with real DIP geometry at real screen coordinates (studio 12,45 - companion 12,95 - play 12,145 - intake 12,195 - system 12,245). Every capability refused honestly on the platform: `camera: settings restored and NO camera was enumerated or opened - consent not current, gaze engine not implemented in this build`, `entitlement: unavailable(unsupported-platform)`, `haptics: disabled in settings - no provider was contacted`. That is real Linux behaviour and it is the first time any of it has been observed. WHAT IS NOT PROVED, AND MUST NOT BE REPORTED AS PROVED: not one pixel. Every capture came back 100% black in three separate display configurations (WSLg RAIL/Wayland, XWayland with `WAYLAND_DISPLAY` unset, and a real `Xvfb` framebuffer), so nothing here establishes rendering, compositing, theming, focus, input or window behaviour on Linux. LAYOUT IS NOT RENDERING: the geometry probe reads the visual tree, which is exactly what a headless run already gives, so the capture adds a file and no evidence. NEXT: find why the surface has no readable content - a compositor/backend question, not a product one on this evidence - or name a different Linux host (a real desktop session rather than a nested RAIL compositor) as the gate. Do NOT close this by loosening the checks; the checks were right and the capture was empty. |
| P1 | DONE | A Windows path separator reaches the user on Linux. | FOUND BY THE FIRST LINUX RUN, in product code rather than in a test. `IntakeHostSupportTests` expects `a_b_c` and gets `a_b\c` - a backslash treated as a literal instead of a separator - and `SpiralSurfacePresenterTests` puts `C:\spirals\classic.gif` into a message a USER READS, on a machine with no `C:` drive. These are not test-gating problems: they are Windows assumptions compiled into the port's own logic, and they are the class the constitution means when it says a build is not evidence of Linux support. Wanted: the two sites fixed at the source (`Path.Combine` / `Path.DirectorySeparatorChar` rather than a literal, and a display path that does not assume a drive letter), plus a look for the same shape elsewhere - two found by accident in a suite that mostly could not reach Linux code paths at all is not a reason to believe there are only two. **CLOSED 2026-08-24 (evidence written back late - the fix landed earlier in the branch and this row was never updated).** `client/src/CcpClient.Desktop/PortablePath.cs` supplies `FileName()` and `InvalidFileNameChars` that answer by the PORT's rules rather than the HOST's, because `System.IO.Path` on Linux does not treat a backslash as a separator. Both offenders consume it: `Features/Intake/IntakeDraftSink.cs` and `Effects/SpiralSurfacePresenter.cs`. Verified on Linux: 2861/0 failed/66 skipped. |
| P1 | DONE | The real-desktop lease is not a lease on Linux. | FOUND BY THE FIRST LINUX RUN, and it is a MECHANISM defect rather than a test one. `RealDesktopLeaseTests` and `RealDesktopLeasePrimitiveTests` assert that a second open of the held lease returns null; on Linux it returns a live `FileStream`. The lease relies on WINDOWS EXCLUSIVE-OPEN semantics, and POSIX advisory locking does not provide them - so on Linux two concurrent runs could BOTH believe they hold the desktop, which is precisely the condition the lease exists to prevent and precisely the condition this board has spent days chasing on Windows under the name 'desktop contention'. `CompanionViewModelTests` is the same finding from the other side: a delete that FAILS on Windows because a file is locked SUCCEEDS on Linux, so the 'could not be deleted' branch is unreachable there and the product's error path is untested on that platform. Wanted: a lease primitive whose exclusion is real on both platforms (`flock`/`fcntl` on POSIX, or an approach that does not depend on open semantics at all), or an honest refusal that says the lease cannot be held on this platform. Do NOT close this by relaxing the assertion to match Linux - the assertion is right and the mechanism is what is missing. **CLOSED 2026-08-24 (evidence written back late - the fix landed earlier in the branch).** The lease opens with `FileShare.None`, and the reason is written at the code: .NET maps a share mode to an ADVISORY `flock`, `FileShare.None` to `LOCK_EX` and EVERY OTHER MODE to `LOCK_SH`, so the previous `FileShare.Read` excluded nothing on Linux and two runs would each have believed they held the one interactive desktop. Exclusion and identity are now two different files, because on Linux they cannot be the same one: the lease file carries the lock and no readable bytes, and the holder's pid goes in a sidecar nothing holds open. |
| P2 | OPEN | About 66 real-desktop facts assume Windows without ever saying so. | MEASURED BY THE FIRST LINUX RUN. Of 88 Linux failures, roughly 66 are facts whose PRODUCT behaviour is correct and whose EXPECTATION is Windows-only: `GlyphCapabilityTests` (21) dies on a bare `DllNotFoundException` for `user32.dll`, and Video (18), Pointer (9), Input (5), Flash (4), Glyph coexistence (4), Overlay (1), the two surface presenters and `RealClipDecodeTests` (6) each get a TYPED absent-mechanism refusal (`overlay-mechanism-absent`, `video-mechanism-absent`, "the Windows decoder was asked for from a process that is not on Windows") where the fact demands the Windows answer. The port is being honest and the tests are not. This matters beyond tidiness: while these are ungated, the Linux column of this suite can never be green, so no Linux regression can ever be SEEN - the noise floor is 66. Wanted: each gated to the platform it actually measures, in the shape `floor.json`'s admission rule already requires (a machine/OS property, named, with the machine class where it executes) - NOT a blanket skip, and NOT a weakened assertion. The Linux-gated counterparts that already exist (`SecretStoreTests.LinuxProbe`, `ChaosTunnelCapabilityTests.Linux_*`) are the precedent and they executed correctly on this run. |
| P2 | OPEN | Extend deterministic test-wait coverage to unbounded joins and injected timeout budgets. | A focused source guard plus tests that fail on the previously invisible shapes without adding timing sleeps. |

## Completeness Backlog

**The queue above was not the remaining work, and this section exists because nothing in the
repository said so.** A cluster-by-cluster sweep of the shipping product against `client/src` and
against this board found **115 user-visible behaviours that are absent or partial here and that no row
covered**, across 161 behaviours examined. Each one is recorded in
`client/docs/port-completeness-census.md` with a WPF citation, the grep that proved absence, and a
measured size.

**How this happened is traceable, and it is not a mystery.** `client/docs/wpf-surface-reachability.md`
was 1,906 lines carrying a divergence register with 326 distinct `D` anchors that enumerated exactly
these gaps. Commit `1bdf998e4` ("Refactor tests and documentation for clarity and accuracy") cut the
file to 95 lines and deleted the register whole. The enumeration existed, was deleted, and never became
rows - and 28 client source files still cite section and `D` anchors that no longer resolve.

**These rows are P2 because they are SCOPE DECISIONS, not because the behaviour is unimportant.** None
of them is work yet. Each entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its
own row with its own evidence and verification before any code is written. The line counts are WPF
surface measured with `wc -l`; they indicate the size of the behaviour, **not** the cost of porting it,
because this port reproduces outcomes rather than files.

| Priority | Status | Outcome | Required Evidence |
|---|---|---|---|
| P2 | DECIDED | Scope the **Application shell and its windows** cluster: 16 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 16 entries is ~54,944 lines (largest: The dialog layer; Home / Dashboard tab (the app's opening page) + its embedded browser panel; Guided tutorial system). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT the in-app toast surface (#54, and #83 is the SAME ROW - merged) so a gate refusal stops being silent, paired with the tier-refusal route (#41) so it has something to say. DEFER the rest: dashboard, settings tab, dialog kit, tutorials, presets tab, palette, chrome, first-run wizard, help, roadmap, rail motion, the Spiral tracker. REFUSE nothing. #57 is a restatement of #35/#36/#40 and is struck rather than counted twice. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Game modes and special surfaces** cluster: 6 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 6 entries is ~47,436 lines (largest: Deeper; AvatarTube; Embedded browser card on the Home page). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #89 SESSION FEATURE LOCK as a P1 DEFECT, not a feature - see its own row. ADMIT #65/#85 (one entry, not two): the AI validator and executor are BUILT and reach an empty handler map, so asking her for a spiral does nothing; bridging it to the 52 landed Effects/ files is the highest visible-win-per-line item in the repo. DEFER AvatarTube. DEFER Deeper (26,624 lines, the largest single surface in the census) and the embedded browser. Gaze Minigame is owner-blocked on webcam. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Companion, AI and autonomy** cluster: 17 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 17 entries is ~43,846 lines (largest: Awareness: she notices what you are doing on the PC and reacts to it; She talks during the whole app, with a voice (the bark engine); Awareness privacy surface: the dial, the app picker, the hour-long pause, the wire view). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #58 FIRST AND BEFORE ANY OTHER ENTRY HERE: six companion rows were deleted while OPEN or WIP and took twelve owner questions with them, so nine of this cluster's entries are being judged against a record that no longer exists. ADMIT #64 (a surface over gates already built and tested) and #72 (one unused flag). DEFER the room, personas, memory scopes and bark breadth. OWNER-BLOCKED, not deferred: voice input, cloud AI, awareness observation, moderation content - each crosses a boundary port.txt reserves. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Session, scheduling, progression and economy** cluster: 10 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 10 entries is ~37,044 lines (largest: Training Programs; Scripted session runs; Skill tree / Enhancements). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #39 POP QUIZ - uniquely in this cluster it is NOT blocked behind the scripted session, because upstream starts it from the ordinary engine too. #32 and #33 are already admitted and in flight. DEFER quests, skill tree, season recap, training programs (#40 and #75 are ONE entry, merged), and the XP ceremony. #38 and #77 are ONE entry, merged, and deferred with the network group. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Content, mods, assets and presets** cluster: 11 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 11 entries is ~31,223 lines (largest: Active mod (persona pack) switching; Mod Creator studio (author your own mod); Assets tab: asset browser, asset presets, phrase presets, and drag-drop asset import). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #9 PHRASE BACKUP - it is the only entry in this cluster whose absence is a DATA-LOSS risk rather than a missing feature: every phrase the user wrote dies with a bad update. It needs the file-picker seam, which I am admitting separately. DEFER mods, mod manager, mod creator, content packs, preset export, assets tab (#7 and #48 are near-duplicates, decide together, deferred together). REFUSE the web catalogue and JUST DROP: network plus third-party moderation, and neither is worth that boundary today. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Account, updates and platform services** cluster: 9 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 9 entries is ~23,449 lines (largest: Multi-day Training Programs (the Programs tab); Remote control from a phone or web controller; Bug report / suggestion submission with redaction preview). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** #76 TIER RESOLUTION IS THE HIGHEST UNBLOCK-PER-LINE ITEM IN THE CENSUS - every gated door currently refuses everybody, including a paying owner - and it is NOT MINE TO ADMIT: it needs the borrowed token, which is a secret plus a network boundary and is already posed as an open owner question. It stays blocked, and that is the honest answer rather than a convenient one. DEFER performance mode, autostart, cloud backup, bug report. REFUSE remote control: it publishes live session state and lists the user in a subjects directory to strangers. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Visual effects and overlays** cluster: 14 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 14 entries is ~13,989 lines (largest: Unified overlay compositor; Native UI motion and ambient FX (the things MotionFx gates, and FxTheme colours); Spiral and Pink Filter panel controls). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT FOUR, all cheap and all user-visible: #96 flash sound (three silent surfaces, one wiring job over an IAudioPresence three effects already use), #26 animated GIF/WebP (a user's animated asset is silently a still frame, and SpiralFrameSource already walks GDI+ frames), #103 overlay resilience (~26 lines), and #102 the spiral library and tint pickers (backend landed, only the picker missing). ADMIT #115's dials with them where they touch Effects. DEFER Brain Drain's visual half, corner GIFs, multi-monitor, the unified compositor, flash draw dials, solid mode, the announcer, and the bubble payloads. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Video, media and webcam** cluster: 12 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 12 entries is ~12,424 lines (largest: Webcam sensor events other than positional gaze; For You Feed (ghost mode, mirror opacity, Show-Desktop survival, decode notice-and-swap, the feed page itself); Online and direct-URL video (remote clips, per-mod video link sets, the web-video takeover)). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #26 (counted in the effects cluster, not twice). DEFER attention checks, strict lock, grace pause, wallpaper, startup video, duration filter. #31's real cost is the content-pack store and it is struck here rather than double-counted. OWNER-BLOCKED: every webcam entry, plus online video and For You Feed on the network boundary. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Audio, speech and verbal conditioning** cluster: 12 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 12 entries is ~11,774 lines (largest: Offline microphone stack and the 'She's Listening' panel; App-wide bark/quip engine; Audio-synced haptics from a web video). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT #115 - master volume, video volume, device picker and Test audio are DIALS OVER SEAMS THAT ALREADY EXIST: SoundArbitration.SetPreferredDevice's only caller today is a test, and BarkPipeline.MasterVolume is set by nothing. ADMIT #108 typed mantra minigame: 821 lines and it needs no microphone. DEFER audio layers, mantra chant, subliminal whisper audio and cross-app ducking (two platform implementations behind one seam). OWNER-BLOCKED: the whole microphone stack, wake word, spoken mantras, voice-solve cards. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |
| P2 | DECIDED | Scope the **Input, attention and detection** cluster: 8 user-visible behaviours are absent or partial and no row covers them. | Measured per entry in `client/docs/port-completeness-census.md`, each with a WPF citation and the grep that proved absence. WPF surface across the 8 entries is ~11,005 lines (largest: Awareness Engine; Blink Trainer; On-screen keyword highlight overlay). THIS ROW IS A SCOPE DECISION, NOT AN IMPLEMENTATION TASK: every entry needs an owner ADMIT / DEFER / REFUSE, and an admitted entry becomes its own row before any code is written. The line count is WPF surface, not an estimate of port cost. **DECIDED 2026-08-24 by the coordinator under standing owner authority, from a read-only sizing pass that verified every entry's CLIENT COVERAGE against the tree rather than trusting the census's own 'Client state' line - which was stale for seven entries.** ADMIT NOTHING, and this is the one cluster where that is the right answer rather than a deferral of convenience. Seven of its eight entries cross a boundary port.txt reserves to the owner - a system-wide keyboard hook IS a keylogger, and screen OCR reads whatever the user is looking at in someone else's window. #12 PANIC KEY is called out separately because it is a SAFETY affordance rather than a feature: its absence is already the reason InputPanelNotices.cs:36 says what it says, and it deserves an owner answer on its own terms rather than being buried in a cluster verdict. #16 is cheap and worthless without #13. Every ADMIT above becomes its own row before any code is written; a DEFER is a real decision and stays in the census; an OWNER-BLOCKED entry is NOT deferred and must not be quietly picked up by a lane. |

## Completion Criteria

A row is complete only when its behavior, implementation, required Windows/Linux evidence, and
recorded verification agree. A failed or unavailable gate keeps the row `WIP` or `BLOCKED`; it does
not become a successful claim through a compile, stub, no-op fallback, or workflow report.
