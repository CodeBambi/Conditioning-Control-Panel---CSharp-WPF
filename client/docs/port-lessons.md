# Port lessons — second attempt (living ledger)

Append-only, dated, one-to-three lines per entry. Harvested by the steering loop during batches (see `port-session-prompt.md` §Steering loop). Every spine worker reads this file via `referenceDocs` — keep it short and current; prune entries that are superseded or encoded into a skill/agent/constitution rule (note where they went). This is attempt-2 operational learning; WPF/attempt-1 lessons live in `first-attempt-lessons.md` and `first-attempt-systemic-lessons.md`.

## Entries

- 2026-07-18 — Windows worktree lanes need `git config core.hideDotFiles false` (hidden `.git` gitfile EPERMs pi-spine path rewrite). Applied repo-wide.
- 2026-07-18 — pi-spine fsync bug on Windows patched locally in `node_modules` (`abort.mjs`/`lifecycle-archive.mjs` open flag `"r"`→`"r+"`); does NOT survive reinstall — re-apply after any pi-spine update until fixed upstream.
- 2026-07-18 — Stub workers write only `.DONE`: packets with `fileScopeMustChange` cannot pass contract under `SPINE_WORKER_STUB=1`; use stubs only to smoke packet parsing.
- 2026-07-18 — bpx-consult solo routes: only `anthropic/claude-fable-5` engages outside interactive windows; `uva/*` providers are not registered in child contexts. Keep solo default on Fable.
- 2026-07-18 — Project Avalonia baseline is empirically 12.1.0 on net10.0 (SP-001); Avalonia MCP validation is 11.3.x-pinned — treat its version-specific hints skeptically.
- 2026-07-18 — SP-001 worker skipped STATUS.md updates and in-worker consults during manual-assisted execution; packets must state these as explicit checkboxes, and reviews should check them.
- 2026-07-18 — WSL2 Linux env (SP-002): the 26.04 distro registers as plain `Ubuntu` (check `/etc/os-release`, not distro name); `dotnet-sdk-10.0` (10.0.110) is in the native Ubuntu apt feed; no passwordless sudo — install packages via `wsl -u root` from the host.
- 2026-07-18 — `spine wait` exits silently (code 0, no output) within seconds-to-minutes in this environment regardless of batch state; it is unusable as a completion signal here. Do not recreate wait monitors — the cron steering loop + `spine status --diagnose` is the only reliable terminal-state signal.
- 2026-07-18 — Spine lane salvage inspection only checks DIRTY paths; committed lane-branch work is invisible to it (`salvageable:false` with 2 good commits on the lane). Before any retry, `git diff --stat <base> <lane-branch>` — retry/resume continues from lane STATUS.md.
- 2026-07-18 — node_modules patches needed so far: fsync `"r"`→`"r+"` (abort.mjs/lifecycle-archive.mjs/journal.mjs) and `dotnet` on the gate-evidence allowlist (evidence-command.mjs). NEITHER survives pi-spine reinstall/update — re-apply together; a durable patch mechanism is filed as tooling row T-1.
- 2026-07-18 — Orchestrator committing ANY file to the base branch mid-batch (even a port-lessons harvest) triggers `human_base_diverged` at land if the worker touched the same file. Batch harvest commits until after `spine batch complete`, or keep them on a side branch.
- 2026-07-18 — Integrate gate pins the orch tip SHA (`targetRevision`); manually advancing orch (conflict resolution) requires gate re-open: reset `.spine/runtime/<batch>/gate.json` to `status:pending` + new pin, then `spine gate approve` (CLI re-records the audit trail). Never edit `.spine/batch-state.json`.
- 2026-07-18 — BOTH batches so far ran ZERO engine reviews (SP-001 manual-assisted; SP-002 `resume_fast_path` skipped the stage; `agents.reviewer` was configured all along). SP-001's "proved review end-to-end" claim is stale — the review pipeline is empirically unproven until SP-003 (first clean-run batch).
- 2026-07-18 — When resolving merge conflicts with scripted edits, verify marker removal BEFORE committing; a failed python assert in a newline-chained (non-`&&`) command sequence silently committed conflict markers into 5fd1d540's first attempt.
- 2026-07-18 — Spine integrate-gate evidence (test/coverage output) is NOT refreshed after a retry/recovered batch — it presents the failed attempt's artifacts (batch 20260718T212127 gate showed 1 stale test vs actual 23). At every land after any retry, re-run the contract testCommand on the merge content before approving. Tooling row T-3.
- 2026-07-18 — Third consecutive batch with zero engine reviews (`spine_review_step` → skipped=true every call, reviewer configured). Do not rely on engine plan/code review existing; Fable solo consults are the de-facto review gate until T-2 diagnoses it.
- 2026-07-18 — `worker_orphaned` (lane worker process vanishing; journal `reconciled:true`) recovers cleanly via `spine batch retry` — resume continues from lane STATUS.md, no work lost. Cause still unknown (2 occurrences in one batch; SP-002 lost its worker to external SIGINT — watch for a pattern of parent-shell death killing detached workers).
- 2026-07-19 — Silent spine worker death (exit 1, empty output, ~4s after rules_selected) = spawn-layer failure. Root cause: worker tail prompt inlining referenceDocs exceeded the Windows 32,767-char CreateProcess limit (33.6KB). Fixed via local patch: `bin/spine-worker-runner.mjs` writes tails >16KB to `%TEMP%` and passes `@file` (T-1 manifest). Diagnose by running the runner manually in the lane with SPINE_* env.
- 2026-07-19 — NEVER run bare `pi` inside a spine lane worktree for diagnostics: it auto-installs project extensions (`.pi/npm/`) into the worktree → `GitignoredDirtyWorktree` failure at land. Probe in a throwaway dir, or `git clean -fdX` the lane afterwards (orchestrator-caused, own it in the gate history).
- 2026-07-19 — pi-spine gate evidence is copied from cache, not generated (collection finished in 8s — impossible for dotnet build+test); it has been stale at ALL THREE lands so far. Orchestrator re-run of the contract testCommand on the merge content is the controlling evidence at every land until T-3 is fixed.
- 2026-07-19 — `GitignoredDirtyWorktree` at finalization also fires on the worker's OWN bin/obj build outputs (not just foreign pollution) — expect it on every batch; recovery is deterministic: `git clean -fdX` in the lane → `spine batch retry <task>` → `spine batch resume` (~2 min). Engine classification bug filed as T-5.
- 2026-07-19 — In-packet WSL2 gate (SP-005 pattern) works end-to-end: worker copies `client/` to a native WSL dir (`~/ccp-spNNN`, never /mnt/e), runs the same testCommand, records output in record.md. One full-suite WSL2 run retroactively closes earlier rows' unit-test Linux debt — say so on their rows at land.
- 2026-07-19 — `maxConsultsPerTurn` (bpx-consult.json, default 3) counts across the WHOLE pi session (loop fires share the turn) and the running session caches it — editing the file mid-session does NOT lift the cap. Continuous-mode orchestration needs ≥2 orchestrator consults per cycle (land + pre-authoring) on top of anything else; raise the cap BEFORE starting a run (now set to 8) and restart the session. When capped: worker in-packet consults still run (fresh worker processes), so park new packet authoring/launching but landing fully-evidenced completed batches is defensible with the skip documented.
- 2026-07-19 — Fable consult replies truncate mid-sentence under long multi-question prompts (2nd occurrence: SP-005 pre-authoring, SP-006 pre-approach). Packet-authoring pattern: keep consult questions few and pointed, request condensed verdicts; when truncated, label the truncation and apply the received portion (SP-005/006 precedent) — do not re-ask the whole outline.
- 2026-07-19 — STUB-FIRST POISON: a stub-validation batch (`SPINE_WORKER_STUB=1`) leaves a stub `.DONE` in the lane; `spine batch retry` + `resume` on the SAME batch then takes the resume fast-path, classifies the task terminal-success with ZERO real worker output, and advances to needs_integrate (batch 20260719T093943 — caught before integrate: only commit was the stub .DONE marker, no step commits, no deliverables). New playbook: stub validation and the real run are SEPARATE batches — after a stub run, abort + clean lanes/branches, then fresh `spine batch start` for the real worker. Never retry a stub batch into a real one. Verify at every land: lane step commits exist beyond "worker completion". Tooling row T-6.
- 2026-07-19 — Avalonia 12.1 headed-harness facts (SP-007): (1) no UIA automation peers for Border/Grid/StackPanel — an app-side layout probe (LayoutUpdated → DIP bounds + RenderScaling + PointToScreen) is the reproducible locator; (2) the app opens UNACTIVATED behind windows and SetForegroundWindow is foreground-locked from background processes — pixel captures can false-positive on the occluder; use SetWindowPos(HWND_TOPMOST) on proc.MainWindowHandle and let pixel checks verify content themselves; (3) WSLg RAIL windows are invisible to GDI CopyFromScreen/PrintWindow (black bitmaps) — capture Linux-side via XGetImage (python3 ctypes + libX11, no packages).
- 2026-07-19 — Avalonia MCP advisory first real use (SP-007, A-013): ValidateXaml consistent with the real compiler (nothing new); AnalyzePerformance emitted a self-contradictory report ("Invalid XAML syntax" AND "Score: 90/100" in one response) — the exact authoritative-looking-garbage failure mode A-013 warns about. Bounded redacted review stays useful as a second opinion only; the 12.1.0 compiler + seeded AVLN2000 negative proof is the authority.
- 2026-07-19 — Repo-root `.gitignore` has a bare `tools/` rule (line ~167) that silently excludes ANY new `client/tools/**` path from commits; SP-008's harness nearly landed missing (the sln referenced files git never tracked — would have broken the build at land). Worker protocol now proven: run `git check-ignore` on new paths and audit the task's changed-file list before .DONE; force-add with care (`-f` also sweeps bin/obj — re-audit the index).
- 2026-07-19 — Budget/cold-build measurements must VERIFY the precondition (bin/obj actually gone) — a silently-failing prune produced a fake 2s "cold" build before the honest 12.9s was measured. Also: WSLg/X11 PointToScreen coords are WINDOW-RELATIVE (WSLg tiles monitors under one X root) — never mix coordinate spaces in capture crops; the failure mode is loud, which is acceptable.
- 2026-07-19 — Avalonia's `!AvaloniaResources` bundle also carries compiler metadata entries at ROOT level (`!AvaloniaResourceXamlInfo`); any completeness sweep over the bundle must expect `!`-prefixed ROOT entries — but exclude only root-level (a `!` final segment deeper in the tree would silently filter a real asset; SP-009 pre-completion consult caught the over-broad rule).
- 2026-07-19 — A Windows GUI-subsystem (WinExe) binary still delivers `Console.Out` through `> file` redirection — diagnostic self-check flags on the real binary need NO AttachConsole native interop.
- 2026-07-19 — After a bin/obj cold-clean, REBUILD before running a configuration (exit 127 = file gone, not a product failure); SP-009 budget pass bit once.
- 2026-07-19 — Advisor framing < empirical evidence, in both directions (SP-010): the pre-approach consult claimed settings land at `~/.config` literal; the store empirically honors `XDG_CONFIG_HOME` via .NET's Unix `ApplicationData` mapping — contract + doc comment corrected to observed truth, pre-completion consult acknowledged. Authority order working as intended; record the correction, don't hide it.
- 2026-07-19 — Incremental `dotnet publish` into an EXISTING single-file output dir silently DROPS native sidecars (libSkiaSharp/libHarfBuzzSharp/av_libglesv2) → BadImageFormatException 0x8007000B at run (SP-010 reproduced + fixed). Publish scripts always clean the output dir first.
- 2026-07-19 — `ldd` on a .NET apphost shows NOTHING useful (Avalonia's X11/fontconfig natives are runtime-dlopened) — the honest native-deps floor is `ldd` per shipped `.so` ∪ `/proc/<pid>/maps` at runtime (SP-010, MS docs verified).
- 2026-07-19 — `spine wait` early-exit defect confirmed a 3rd time (MonitorCreate exited ~2 min into batch 20260719T194819, batch healthy) — filed tooling row T-8; do not recreate wait monitors.
- 2026-07-19 — pi-loop hybrid trigger JSON rejected by LoopCreate (two spec forms) — de-facto hybrid steering = cron-only loop + MonitorCreate `onDone` one-shot wake.
- 2026-07-19 — `git worktree remove` can fail Permission denied on Windows right after running the artifact (file lock) while git has ALREADY unregistered it — `rm -rf` + `git worktree prune` recovers (SP-010 land scratch cleanup).
- 2026-07-19 — **`spine batch complete` is a REQUIRED explicit land step:** status says "completed successfully" but `.spine/batch-state.json` persists and fails the NEXT preflight (`no-active-batch`). Sequence: gate approve → integrate → batch complete → THEN post-land docs commits. Violating it costs an orch-first recovery (base-into-orch merge → batch complete) — SP-010→SP-011 transition.
- 2026-07-19 — Post-land human commits on orch-touched paths (task-board.md) with a leftover batch state → `human_base_diverged` at the next preflight. Recovery: `git checkout orch/<batch>` → `git merge feat/crossplatform` → checkout base → `spine sync-base` → `spine batch complete`. `spine batch dismiss` only allows limbo_stale/completed_manual/aborted (else `--force`).
- 2026-07-19 — T-2 closed: `## Review Level: N` HEADING is the only parsed form (bold prose never parses); in-worker review skips are `nested_spawn_blocked` BY DESIGN (SP-195/278); the engine runs code+final reviews post-.DONE with real verdicts (SP-011: APPROVE + PASS — first ever).
- 2026-07-19 — T-5's `git clean -fdX` lane recovery also deletes the lane's gitignored `.reviews/` — review EVIDENCE must be read from the journal events + record.md BEFORE cleaning when it matters (SP-011's verdicts were journal-recorded, no loss).
- 2026-07-19 — `HttpListener` cannot bind port 0 → retry loop over an ephemeral range (SP-011 loopback); 127.0.0.1 prefixes needed no URL ACL on this box. CORS-less error responses surface to fetch() as opaque TypeErrors — refusal responses must carry CORS headers to stay diagnosable.
- 2026-07-20 — T-5 (4th occurrence) sharpened root cause: with T-2 fixed, the ENGINE's post-.DONE code+final reviews write gitignored `.reviews/` into the lane AFTER the worker's completion commit — every Review-Level-2 batch now self-dirties and hits `DirtyWorktree` at finalization. Recovery unchanged (clean → retry → resume --force); read review verdicts from the journal BEFORE cleaning (line 2026-07-19 `.reviews` entry).
- 2026-07-20 — `spine batch retry` takes the BARE task ID (`SP-012`), NOT the folder slug (`SP-012-window-behavior-manifest` → "Task not found in batch").
- 2026-07-20 — WSLg/X11 window enumeration: `_NET_CLIENT_LIST` is ABSENT under WSLg (SP-012 demonstrator) — X11 EWMH client-list procedures must degrade to XGetImage/xprop per-window probes and record the absence honestly; never claim enumeration support from the protocol existing in docs.
- 2026-07-20 — MonitorCreate wrappers on this box report `[error]` with captured output lost (spine-wait monitor + T-3 scratch monitor both, same session) — treat monitor output as suspect, re-run the wrapped command directly for controlling evidence; the cron steering loop remains the reliable signal.
- 2026-07-20 — pi worker hard timeout = `stallTimeoutMinutes` (spine-config, 120 here) × task-size scaling, PER WORKER PROCESS; a healthy-but-slow worker dies at exit 124 exactly at the cap (SP-013 mid-Step-3). Retry→resume spawns a fresh process with a fresh budget. Override: `SPINE_WORKER_PI_TIMEOUT_MS` env. **Packet-template fix: headed-evidence-heavy packets declare Size L for the larger stall budget (applied to SP-015-avatartube on authoring).**
- 2026-07-20 — T-5 5th + T-8 6th occurrences (SP-013 batch 20260720T022627): Level-2 batches self-dirty via engine `.reviews/` every time (deterministic); `spine wait` early-exits every time. Both rows carry the counts; the workarounds are stable.
- 2026-07-20 — Avalonia 12.1.0 API rename: `SystemDecorations` is `[Obsolete("Use WindowDecorations instead.")]` (shim at `src/Avalonia.Controls/Window.cs:395-400` release/12.1.0) — the api-docs "latest" site still presents the old API as current. **Pinned-package XML/source is the authority, never the docs site** (avalonia-research §3; SP-013 caught it pre-implementation).
- 2026-07-20 — X11 window ids captured early DIE: a window resizes/moves through generations in its first ~2s (WSLg reparenting); xprop/xwininfo on a first-appearance id → BadWindow. Poll the tree until two identical SETTLED samples, or `xprop -name` (resolves fresh per query, immune) — SP-013 WSLg gate.
- 2026-07-20 — A-013 `AnalyzePerformance` self-contradiction 2nd occurrence (SP-007, SP-013: "invalid XAML" + "90/100 Excellent" in one response). **A-013 advisory template going forward: ValidateXaml only; AnalyzePerformance always reject; the 12.1.0 compiler + headless suite are the authority.**
- 2026-07-20 — Steering: loop fires do not imply 15-min spacing when monitor wakes interleave — verify the wall clock (and the 10-min heartbeat cadence) before diagnosing a worker stall.
- 2026-07-20 — T-5 6th + T-8 7th occurrences (SP-014 batch 20260720T052700; `spine wait` now exits ~0 min in). Both deterministic; rows carry counts; workarounds stable. SP-014 archaeology premise correction: **the title-keyed dispatch "being replaced" lived only in the FIRST Avalonia attempt — WPF's pre-state was keyless hard-wiring**; row title implies a WPF mechanism that does not exist (empirical evidence proves doc premises stale — record prominently, don't hunt WPF for it).
- 2026-07-20 — **spine's worker timeout kill does NOT reap the pi node process tree on Windows (T-10):** SP-015 run-1 (PID 11212) survived its exit-124 'death' and kept committing to the lane ~16h unsupervised (Steps 2–3 landed post-mortem, journal events written with the lane correlationId AFTER `batch.failed`), poisoning the retry — two writers, one worktree; run-2 detected it and yielded cleanly (SP-015 `DUPLICATE-WORKER.md`). `lane.died` ≠ worker gone. **Before ANY `spine batch retry` after a worker failure: enumerate processes matching the batch worktree path (`Win32_Process CommandLine -match spine-<batchId>`) and kill zombie trees first; salvage-commit dirty lane WIP with UNVERIFIED provenance (workers resume from lane STATUS.md, so verified steps survive).** Likely contributor to past `worker_orphaned` mysteries. **2nd occurrence 2026-07-21 (same batch, run-3): anatomy sharpened — the kill gets the pi.exe SHIM; the node.exe AGENT survives. Salvage commits must EXCLUDE `.pi/loops/*.json` / pi session metadata (engine auto-commit swept 2 into the branch; removed pre-integrate).**
- 2026-07-21 — **`SPINE_WORKER_PI_TIMEOUT_MS` override CONFIRMED:** run-3 died exit 124 exactly 4h after spawn (vs the flat 2h `stallTimeoutMinutes` config) — the env override is inherited by detached engines/workers from the resume shell. Set it at launch for headed-evidence packets; see T-11 for the deeper packet-sizing rule (SP-015 run-3 hit even the 4h cap on a healthy worker — budgets stretch, they don't solve).
- 2026-07-21 — **Window-evidence harness facts (SP-015 record.md Step-4, reusable for W-row exercises):** (a) an OWNED window nests UNDER its owner in the UIA tree — it is NOT a root child; match windows by NAME, never root-child text search (tube/dashboard conflation root-caused all G9 failures); (b) pinned 12.1.0 `WindowImpl.SetParent` (src/Windows/Avalonia.Win32/WindowImpl.cs:752-768) parents ownerless `ShowInTaskbar=false` windows to its HIDDEN OffscreenParent — GW_OWNER==0 gates are wrong for them; use GW_OWNER!=owner + a behavioral survive-owner-minimize gate; (c) UIA `BoundingRectangle` != `GetWindowRect` on this box ((34,57)px measured divergence) — window geometry for input/caption math must use Win32 `GetWindowRect`.
- 2026-07-21 — **Retry fast-path land provenance (first):** a T-5 finalization failure on a fully-reviewed/verified lane → `git clean` → `spine batch retry` → the fast-path classified `task.completed` from the clean lane re-inspection (real .DONE + step commits — NOT the T-6 stub poison shape) — but opened NO integrate gate. Recovery: orchestrator writes `evidence/summary.md` + `gate.json` (status:pending, targetRevision=orch tip after ff-advancing orch to the verified lane tip) per the documented gate-reopen pattern → `spine gate approve` (CLI audit trail) → `spine integrate`.
- 2026-07-21 — **Engine suggested-fixes can be DESTRUCTIVE — verify against repo reality before following (T-12, 2nd engine-output-vs-reality incident):** SP-016's merge stage failed `merge blocked by gitignored paths on a lane branch` and suggested "drop cached ignored files" — the flagged paths were legitimately-tracked `client/tools/**` (SP-008/SP-010 deliverables) + legacy `Tools/`; following the advice would have merged DELETIONS of landed work into base. The check must set-minus paths already tracked on the target. Recovery: manual orch ff + gate-record recovery + integrate + retry fast-path complete. First incident: engine auto-commit sweeping `.pi/loops/*.json` into a lane branch. **A third incident triggers a broader standing-order review (Fable land consult, SP-016).** **T-12 is DETERMINISTIC (2nd consecutive batch, SP-017): the over-broad scan fires on every Level-2 batch — the manual-ff + gate-record + integrate + retry-complete playbook is now the EXPECTED land path encoded in steering-loop templates, not a contingency. Keep packets as-is; the tax is entirely in the merge stage (Fable, SP-017 land).**
- 2026-07-21 — T-8 8th + 9th confirmations (SP-015 monitor `[error]` lost output; SP-016 wait exited ~4 min into a healthy batch). Row count updated; cron steering loop remains the only reliable terminal signal.
- 2026-07-21 — **Worker wrote port-lessons entries in CP1252 (raw 0x97 em-dashes) — mixed-encoding invalid UTF-8 caught at SP-018 land (orchestrator re-encoded on-lane pre-integrate).** referenceDocs are prompt-injected into every future worker — encoding corruption propagates. Land protocol now includes a UTF-8 validity check on lane-touched docs; recurrence → encoding-check tooling row in the worker doc-edit path.
- 2026-07-21 — **pi-spine upgraded 2.8.0 → 2.10.0 (owner-admitted):** upstream diff shows **T-5 PARTIALLY fixed — CONFIRMED HALF at SP-022's land (10th occurrence, ON 2.10.0): SP-601's auto-clean handles build-artifact gitignored dirs but does NOT cover the engine's own `.reviews/` — T-5 still fires on every Level-2 batch; playbook stays** — and **T-12 likely FIXED (merge auto-resolves + skips gitignored paths with `batch.merge_gitignored_skipped` journal events — no more block + destructive suggestion; untested at a real land as of 2026-07-21)**; **T-10 NOT fixed (terminate-tree.mjs byte-identical, same callers — zombie mechanism intact, keep the zombie-check protocol)**; T-8 wait.mjs heavily reworked (unverified — cron steering stays the signal regardless). All 5 local patches re-applied cleanly on 2.10.0 (anchors certified); preflight green. Next lands confirm whether T-5/T-12 stopped firing. **npm subtlety (repeat of the gate finding): `rm -rf node_modules/pi-spine` FIRST — npm skips re-extract of satisfied versions.**
- 2026-07-21 — **Edit-tool calls are ATOMIC: one failed `oldText` rolls back the WHOLE call silently for the applied-looking edits too.** At the SP-018 reconcile a two-edit call (gate-history + wrong-file second edit) failed on the second — the gate-history edit never entered the working tree, and the reconcile commit message claimed it did (message/content drift caught at the SP-019 reconcile; SP-018 gate-history + T-5 7th-8th-9th counts restored then). **Protocol: after every reconcile commit, `git show --stat` and compare against the commit message before moving on.**
- 2026-07-21 — **Consult gates bind to the ACTION `spine gate approve`, never to a land-path branch (SP-020 violation):** the loop script's land consult lived inside the T-5/T-12 playbook branch; when SP-020 exited via the engine-opened-real-gate + human_base_diverged path, the consult fell out and the gate was approved unconsulted (post-hoc Fable RATIFIED — evidence intact; violation recorded in gate history). Second orchestrator-process slip this session (edit-atomicity drift was the first) — same family: the checklist lived in prose, not at the action site; a third triggers the recurrence rule for a standing-order.
- 2026-07-21 — **Contract `fileScopeMustNotChange` must be intersected against the File Scope list at authoring (SP-020 `contract_failed` ×3):** a blanket `client/**` ban contradicted the declared `client/docs/task-board.md` row edit + Check-If-Affected port-lessons convention — `spine tasks validate` does NOT catch scope-set self-contradictions. And: **the engine re-verifies the contract against the LANE packet copy, not base** — packet fixes for a retry must land on the lane branch (base-side fixes are invisible to re-verify). Also: a python one-liner that asserts-then-writes can lose the edit if a LATER assert aborts it — grep-verify the file after scripted fixes, never trust the script's intent.
- 2026-07-21 — SP-020 inventory facts (T-1 row carries the full table): only 2 of 5 known local pi-spine patches survived in vivo (2 fsync + 1 undocumented SKILL.md amendment; dotnet-allowlist + worker-tail `@file` were silently LOST to a past reinstall — the T-1 problem demonstrated); `journal.mjs` needs no patch (upstream at 2.8.0); windowsHide mass-patch ABSENT and not load-bearing; live 2.8.0 vs latest 2.10.0 — NONE of the fixes upstreamed, all anchors byte-identical in 2.10.0. Hidden-.git EPERM: recorded as repo-config (`core.hidedotfiles=false`), correctly NOT a 6th patch (fails the strictly-load-bearing admission rule).
- 2026-07-21 — libvlc headless-probe shape (SP-018): dummy vout + sw decode crashes a BACKGROUND thread ~at/after EndReached, and native teardown segfaults in EVERY disposal order (both LibVLCSharp 3.10.0+3.0.23.1 and WPF-pinned 3.8.5+3.0.21) — vmem memory-callback vout is stable AND gives frame-level decode proof; harnesses hard-exit after flushing evidence, product teardown discipline is the unified-video row's open problem.
- 2026-07-21 — Token-bearing media URLs (signed/expiring) PERSIST in the WebView2 HTTP cache (`Cache_Data`) unless the source sends `Cache-Control: no-store` (SP-018 V5 — caught by the spike's own secret audit). Any host serving credentialed URLs to a WebView needs no-store at the source and/or profile hygiene.
- 2026-07-21 — **git-for-Windows `core.hidedotfiles` default (true on init) makes worktree `.git` pointer files Hidden; Node `writeFileSync` EPERMs opening hidden files** — broke pi-spine `normalizeLaneWorktreeGitPaths` on a fresh `%TEMP%` scratch repo (SP-020: four failed batch starts to diagnose). The real repo carries local `core.hidedotfiles=false` (set it on any fresh scratch/clone before running spine batches). Latent upstream fragility, recorded not patched (fails the strictly-load-bearing manifest admission rule).
- 2026-07-21 — **HttpListener retry loops must use a FRESH instance per attempt (SP-023):** a FAILED `HttpListener.Start()` DISPOSES the instance (retry → ObjectDisposedException; `Stop()` in the catch disposes too), so the SP-011 spike's retry shape could never have retried — masked because its few connections never collided. Collisions are systematic, not rare: the 49152–65535 ephemeral range IS the OS dynamic client range, so outbound connections (incl. the host's own long-polls, TIME_WAIT churn) collide with server binds. The §4 contract's retry loop is the answer — but only if it actually works (fresh listener per attempt; close the failed one).
- 2026-07-21 — **STANDING ORDER (3rd engine-output-vs-repo-reality incident, SP-023): land protocol ALWAYS diffs the lane delta for `.pi/**`/session debris pre-integrate and removes it — engine auto-commits sweep untracked files** (`.pi/loops/*.json` swept into SP-015's AND SP-023's lane branches; the destructive drop-cached suggestion at SP-016 was the other class). Root `.gitignore` now carries `.pi/loops/` + `.pi/tasks/` (sweep class killed at source; 2 previously-tracked loop files untracked). **Worker File-Scope self-expansion is acceptable ONLY with per-file necessity justification + consult flag + `mustNotChange` untouched** — SP-023's 4-wiring-file amendment is the exemplar (Program.cs/App.axaml.cs launch path, CompositionRoot.cs sole registration site, AssetManifest.cs sole verifier).
- 2026-07-21 — **OWNER CONVENTION (designated test surface):** headed evidence/test windows launch on **DISPLAY3 ((-2576,1091) 2560×1440, working area (-2576,1091) 2560×1392)**. Topology: DISPLAY2 = primary (0,0) 1920×1080; DISPLAY1 = (-1440,-1469) 1440×2560 portrait; DISPLAY3 = (-2576,1091) 2560×1440. **All headed harnesses position app windows inside DISPLAY3's bounds (SetWindowPos/Window.Position, verified by GetWindowRect before captures).** Encoded in the create-spine-tasks T-11 amendment block (patch `skill-headed-evidence-sizing`) + CONTEXT §9 + `Tools/run-client-display3.ps1` for manual dev launches. Applies from SP-024 onward.
- 2026-07-21 — **pi extension sync REVERTS version deviations (major discovery):** pi re-syncs `.pi/settings.json` `packages` on pi-process start when node_modules version ≠ the pin — at 19:36 mid-SP-023-batch the pin (`npm:pi-spine@2.8.0`) REVERTED the live 2.10.0 engine upgrade (all 5 patches wiped; batch survived in-memory). **This is the killer behind the T-1 inventory's 2 "lost patches" (dotnet-allowlist + worker-tail `@file`)** — earlier pi-process starts wiped them silently. Rule: **the pin must always match the intended engine version** (now `npm:pi-spine@2.10.0`); after ANY intentional bump: `node .spine/patches/apply.mjs && verify.mjs`. Also: the SP-020 manifest stored the skill patch's replacement with double-encoded em-dashes (worker encoding bug) — repaired by regenerating from the live file; keep manifest text sourced from file bytes.
- 2026-07-21 — **Gate-record recovery path (corrected at SP-024 land):** the recovered integrate gate lives at `.spine/runtime/<batchId>/gate.json` — NOT under `evidence/` — with shape `{gateId(uuid), batchId, kind:"integrate", category:"execute", status:"pending", openedAt, targetRevision, evidenceRefs:["evidence/summary.md"], summary, reopenedReason}`. First SP-024 attempt wrote it under `evidence/` and `spine gate approve` reported "gate not opened" — corrected from SP-023's prior record; engine source `gate-evidence-read.mjs::gateRecordPath` is the authority.
- 2026-07-21 — **Modal-window drive rule for headed harnesses (SP-024 E-series forensics, E3–E7):** cross-process z-order/topmost manipulation (HWND_TOPMOST wrapper) of a MODAL Avalonia window + backgrounded/tee'd stderr can wedge the app into a zero-HWND state — the modal HWND vanishes WITHOUT firing Closed, `ShowDialog<T>` pends forever, UI thread alive in `Dispatcher.MainLoop`, exit code lost (dotnet-dump evidence in SP-024 record). Identical product flow runs clean when unmanipulated (E8 foreground). Rule: **UIA InvokePattern (name-matched, no coordinates) or timed drive for modal buttons; topmost raise only for canvas clicks on non-modal windows; foreground runs for exit-code evidence.** Also: b2 changed the DTRH harness entry — plain `--dtrh-demo` now opens the picker; probe/headless entry is `--dtrh-quick` (b3–b5 packets must encode this).
- 2026-07-22 — **SoundFlow 1.4.1 AssetDataProvider ctor is SYNC-OVER-ASYNC and DEADLOCKS the Avalonia dispatcher (SP-025 run-A forensics, dump-proven):** the constructor does `GetResult()` on an async metadata read; on any thread carrying a SynchronizationContext (the UI thread) the continuation can never run and the dispatcher wedges silently (window alive, UIA dead, posted callbacks never fire). The SP-017 spike never saw it because a console host has no sync context. Rule: **any SoundFlow player/provider construction must run off-sync-context** (the `SoundFlowDtrhAudio.CreatePlayer` seam marshals when `SynchronizationContext.Current` is present). Binding on the quips/sound-arbitration row, which will construct players on UI paths.
- 2026-07-22 — **libvlc vmem rules (SP-025 run-A forensics):** (1) the format callback RE-FIRES with jittering proposals (1920x1088 -> 1920x1090 on the same stream) - freeze the output format on the first proposal per stream; a mid-stream buffer realloc frees the pin the vout still writes into (freed-pin class, luma-0 black frames). (2) h264 CROP-RECTANGLE sources (coded width != visible width, e.g. 3832/1916-wide clips) deliver luma-0 buffers through vmem at EVERY target size in console AND product (`Failed to create video converter`); 16-aligned sources deliver real content. Verify vmem CONTENT (a backend luma probe), never frame-count alone. (3) the frame counter keeps counting during pause (held-frame redisplays) - freeze evidence is decoder PositionSec, never frame deltas.
- 2026-07-22 — **WSL gate trees are post-land DEBRIS (owner AppData cleanup discovery):** every packet's in-packet WSL gate leaves a `~/ccp-spNNN` tree (2–5 GB each; 19 trees = ~45 GB reclaimed via export/unregister/import — diskpart compact alone is ineffective because WSL2 non-sparse does not pass fstrim discard to the vhdx; sparse VHD is Microsoft-gated behind `--allow-unsafe`). Convention: the orchestrator deletes `~/ccp-spNNN` at each land's reconcile (first applied: ccp-sp025). The distro itself is long-lived (SDK + ~/.nuget cache stay).
- 2026-07-22 — **DISPLAY3 rect-verification output must be PERSISTED (SP-025 land-consult binding condition):** the WH drive scripts SetWindowPos to (-2576,1091) and call GetWindowRect before captures, but SP-025's driver printed the rect to the invoking console — never redirected into committed logs (placement executed + verified at runtime, evidence not persisted). From SP-026: headed drive scripts MUST redirect the GetWindowRect output line into the committed run logs, and land checklists verify the literal coordinates from the committed artifact.
- 2026-07-22 — **Synthetic keyboard drive of a WebView2 page (SP-027 b5 ESC-cell forensics, three stacked causes):** (1) keybd_event needs the real SCANCODE (ESC = 0x01) - scancode-0 keys never reach the page (the SP-011 sendkeys precedent masked this because its spike window was already focused). (2) SetForegroundWindow LIES under the foreground lock while the owner uses the machine - it can return success with foreground unchanged; a real canvas CLICK is the only reliable foreground claim, and the harness must verify GetForegroundWindow == target immediately before keydown and fail LOUD otherwise. (3) a fresh DTRH slot opens the cheshire hub_welcome fullscreen VN scene whose CAPTURE-phase keydown handler stopImmediatePropagations EVERY key incl. ESC (cheshireVn.js:484-491, WPF-shared payload = WPF parity, ESC-hold exit is dead during any fullscreen VN scene BY DESIGN) - headed ESC-exit cells must click the scene through first (first click completes the typewriter, second advances; ~2 clicks/beat). Related: every headed run script needs an orphan guard (bounded WaitForExit + loud FAIL + Kill) - an undriven app with no auto-close runs forever.
- 2026-07-22 — **k3 silent-wedge class (SP-027 run-1):** a worker can sit at 0 CPU (dead API call) while the engine's `lane.heartbeat worker_alive` keeps firing — heartbeats watch PROCESS liveness, not progress. Detection rule that worked: 84 min no commit + 30 min no file writes + 0.0s CPU delta over a 20s probe = functionally dead. T-10 kill + salvage + retry recovered. First occurrence; recurrence → stall-detector tooling row (CPU/write-delta check, not just liveness).
- 2026-07-22 — **Harness exit-cells must kill on WaitForExit timeout (SP-027 run B1 forensics):** the graceful-exit fast-path cell (1500ms keybd_event ESC-hold → WaitForExit(30s)) FAILED silently — the app never got the hold-to-exit and kept beating ~70 min as an orphan holding the WebView2 profile lock (blocking later runs via the stale-profile class). Rule: any WaitForExit timeout in a drive script MUST kill the launched process and record EXIT=timeout (never leave an orphan); the B1 failure mode itself (ESC-hold not reaching the page's 1.2s hold-to-exit — focus/routing?) is the retry worker's forensics to explain.
- 2026-07-22 — **T-5 CLOSED by local anchor-patch (SP-028, `t5-reviews-autoclean` in `.spine/patches/`):** root cause sharpened past "classification bug" — git 2.49 `check-ignore --no-index` false-matches ANY trailing-slash untracked dir against a BLANK `.gitignore` line (repo line 179), so `filterGitignoredPaths` mis-skips the engine-written `.reviews/` at lane-commit (12/12 auto-commits swept `.DONE`, never `.reviews/`) and the post-commit porcelain re-check fails DirtyWorktree. Patch deletes `.reviews/` at `commitLaneWorktree` entry (the ONE convergence point of all 4 post-review finalization callers — incl. the resume paths where 4/17 occurrences fired; chosen over the packet-drafted `commitLaneAndValidateWorktree` via consult + `resumed:true` journal attribution). Verdicts are journal-durable, deletion mirrors the 15x manual recovery. Proof boundary: fixture + history in-lane; REAL proof = next Level-2 land skipping the playbook (named post-land gate on row T-5). The underlying `filterGitignoredPaths` quirk stays unpatched (T-12 exclusion stands).
- 2026-07-22 — **Never install a wedge-condition SynchronizationContext on an xUnit runner thread (SP-029):** a never-pumping context set on a shared runner thread swallows the runner's own posted continuations and wedges the whole test process (14-min hang, exit 143 on timeout kill) even though the same test passes in isolation. Contain hostile sync contexts on a dedicated `Thread`.
- 2026-07-22 — **Play()-outside-the-gate is a panic-race class (SP-029 pre-completion consult finding, fixed same-day):** any start path that installs a player under a lock and then calls Play() OUTSIDE it lets PanicReset/Dispose land in the window → Play() on a disposed player (typed failure on the UI thread; an UNHANDLED threadpool exception on a timer thread — process-fault class). Guard = try/catch around Play() (typed Failed) + an ownership re-check under the gate after Play() with best-effort StopDispose of the unowned player. Applies to every channel-start path incl. timer-driven ones.
- 2026-07-22 — **Capture contract-failure logs BEFORE the T-5 clean (wave-1 land):** the playbook's `rm -rf .reviews/` also deletes `contract-exec`'s failure logs for a failed testCommand attempt — SP-030's attempt-1 (exit 1, passed attempt 2) is unrecoverable from the journal (exit code only). Transient class candidates for the record: lane `.pi` re-sync racing `verify.mjs`, or 2-lane dotnet build contention. Watch for recurrence next wave; steering loops now grep the failure log into the land notes BEFORE deleting `.reviews/`.
- 2026-07-22 — **SUPERSEDED (same-day, SP-031 falsification): the following entry's factual basis is FALSE — the SP-028 fixture's taskFolder-inside-worktree shape matched the real callers exactly (source census: all 4 callers pass `taskFolderInWorktree`); the real failure was applied≠loaded (engine runs from the GLOBAL 2.8.0 CLI install, not the repo 2.10.0 tree). The fixture-provenance RULE survives as a general principle, but it was not the flaw here.** Original (falsified) entry: **Fixture parameter provenance must mirror the engine's REAL call (SP-028 patch live-gate failure):** the `t5-reviews-autoclean` patch used `path.join(taskFolder, ".reviews")`, but the engine's `taskFolder` at the finalization call is the BASE-repo packet path while `.reviews/` is written to the LANE WORKTREE's task folder — the patch no-ops on a nonexistent path and T-5 fired on the patch's first live gate (wave 20260722T101444 lane-2). The worker's fixture passed because its taskFolder sat INSIDE its fixture worktree — it never mirrored the real base-path provenance. Rule: fixture harnesses for engine patches must replicate the engine's actual parameter sources (read the batch-state/journal for real values), not convenient fixture-local ones. `verify.mjs` verifies patch TEXT presence, not runtime correctness — the named post-land gate is the correctness check and it worked as designed. T-5 row REOPENED; fix = anchor re-base to `path.join(worktreePath, path.relative(projectRoot, taskFolder), ".reviews")` via a follow-up packet (SP-031).
- 2026-07-22 — **Wave-era recovery rule (first 2-lane batch):** `spine batch retry <lane>` is refused while the batch phase is running ("Cannot retry task while batch phase is running. Pause the batch first") — the per-lane T-5 recovery on a live wave is: `spine batch pause` → `spine batch retry <failed-lane>` → `spine batch resume --force`. **CRITICAL: resume spawns a REPLACEMENT worker for the still-running lane WITHOUT killing the original** (duplicate-worker class, SP-015) — after resume, enumerate the batch's pi/node processes and T-10-kill the engine-untracked original IMMEDIATELY (the engine tracks the newest spawned pair; the lane's dirty WIP survives on disk for the replacement to inherit).
- 2026-07-22 — **k3 silent-wedge class — SECOND occurrence same day (SP-027 run-1, SP-029 replacement worker 10:53 UTC):** the class is now a pattern, not an anomaly. Detection rule proven twice: heartbeats keep firing (engine watches process liveness) while the worker shows 0.0s CPU delta over a 15-20s probe + zero file writes. **RECURRENCE → tooling row: stall-detector should probe CPU/write-delta, not just liveness** (candidate for the T-5-patch-fix packet's wave or the next tooling slot — the current 84-min no-commit human-judgment threshold wastes 30-90 min per occurrence).
- 2026-07-22 — **SP-034 authoring defect (orchestrator): PROMPT.md omitted the `## Review Level: 2` structured heading — the T-2 format bug REINTRODUCED.** Engine parsed reviewLevel=0 → plan/code/final reviews AND contract verification ALL skipped for lane-2 of wave 3 (lane.completed → lane.committed → task.completed in ~1.1s, zero review/contract events in the journal). The lane's clean finalization is NOT a T-5-gate proof (no .reviews/ artifacts ever existed). Caught at land by the missing-events check. **Authoring checklist addition: every packet MUST carry the `## Review Level: N` heading — grep-verify at authoring (`grep -c "Review Level" PROMPT.md` ≥ 2: the section heading + the T-2 note line).** Land handling per the T-2-era substitution norm: orchestrator verification + land consult explicitly covering the absent chain, recorded honestly in the gate history. SP-033 (same wave) has the heading and finalizes normally.
- 2026-07-22 — **pi-spine lifecycle-complete ordering bug (wave-3):** `cleanupBatchLaneWorktrees` THROWS on an rm failure (EBUSY — VS Code's watcher / Defender holding the empty lane dir) BEFORE `clearCompletedBatchState` runs → the root `.spine/batch-state.json` never clears → **preflight `no-active-batch` deadlocks the next wave even though the batch is completed+archived.** Manual unblock (content verified in archive first): delete the root state file. Engine fix class: tolerate/continue past cleanup failure (or clear state BEFORE worktree cleanup) — manifest-patch candidate. Also: lane-2's empty dir stayed EBUSY for 40+ min (17 VS Code processes + MsMpEng on the box) — worktree rm must not be load-bearing.
- 2026-08-04 — **Engine version floats on the GLOBAL root too (second-root pin recurrence):** the laptop's global pi-spine floated 2.10.0 → 2.12.2 via an UNPINNED `~/.pi/agent/settings.json` entry + caret range in `~/.pi/agent/npm/package.json`; the mandatory pre-launch `verify.mjs` caught it (3 patches missing + 2 fsync anchors drifted). Fix: pin BOTH files exact → `npm install` → `apply.mjs` (12 across 2 roots) → verify green. The 2026-07-21 pin lesson (`pin must match the intended engine version`) applies to the global root exactly as to the repo root. Recurrence #2 → bootstrap/doctor-class encoding candidate: assert pin==installed on BOTH roots before every launch (the verify.mjs "installed version not in testedVersions" WARNING was the only tell — consider treating it as fatal pre-launch).
- 2026-08-04 — **pi spawn broken on fresh Windows machines (blocks spine doctor AND worker-runner):** Node 24 `spawnSync('pi')` → ENOENT and `('pi.cmd')` → EINVAL when the npm prefix carries only the shell/cmd shims. Fix: compile a 15-line C# forwarder (`~/.pi/pi-shim/PiShim.cs`, stock .NET Framework csc) to `AppData/Roaming/npm/pi.exe` — `spawnSync('pi')` then returns the CLI version. Fresh-machine bootstrap needs this next to the PATH export. Desktop never hit it (its prefix resolves `pi` — uncharacterized difference).
- 2026-08-04 — **Hidden-.git EPERM REPEAT (SP-020 lesson, bootstrap gap):** the laptop's clone lacked repo-local `core.hidedotfiles=false` (git config does NOT travel with clone/push), so two batch starts died at `normalizeLaneWorktreeGitPaths` EPERM ~36s pre-worker. Fixed GLOBAL on the laptop (`git config --global core.hidedotfiles false`) — global covers future clones/scratches on the machine. Diagnostic refinement: a plain `git worktree add` probe SUCCEEDS and misleads — git writes the hidden pointer fine; the failing write is the ENGINE's Node rewrite, so probes must replicate the engine's exact write, not just the git call. port.txt fresh-machine bootstrap should carry both machine fixes (this entry + the pi.exe shim).
- 2026-08-04 — **A batch that fails DURING worktree provisioning cannot `resume --force`** ("Lane 1 worktree not found") — the resume path assumes the lane exists. Clean path: `spine batch abort` → verify orch/task branch husks carry ZERO unique commits → `git branch -D` the husks → fresh `spine batch start`. Two occurrences same morning (both the EPERM class).
- 2026-08-04 — **Per-machine premise re-probe (SP-035 amendment class):** SP-019's "Ollama absent" was desktop-scoped; the laptop HAS Ollama (`C:\Users\Micha\AppData\Local\Programs\Ollama\ollama.exe`). Environment-absence premises in packets must be re-probed per machine, never inherited (presence probe = `ollama --version` ONLY — no socket, no model pull). Also: laptop WSL has ZERO distros (`wsl -l -q` empty exit 0) — every WSL2 gate on this machine is a named limit until the owner provisions one.
- 2026-08-04 — **bpx-consult `defaultMode` trap:** the project config sets `defaultMode: "council"` — a bare `consult()` call attempts council and fails where the `kimi-api` provider is unregistered ("No synthesizer model configured"). ALWAYS pass `mode:"solo"` (or `gut-check`) explicitly. Solo Opus-5 route verified working in-session 2026-08-04.
- 2026-08-04 — **Anthropic fresh-subprocess route DOWN account-wide (wave-4 park):** engine reviewer spawns (`pi -p --no-session --model anthropic/*`) fail `400 invalid_request_error: "Third-party apps now draw from your extra usage, not your plan limits."` — opus-5 AND fable-5 identically, reproducible outside the engine; in-session bpx-consult (orchestrator + worker in-packet) UNAFFECTED (SP-037's worker consults completed at 12:10–12:25Z, engine spawn 400'd at 12:27Z). Consequences: (1) SP-037 landed via the SP-034 substitution norm (self-verifying deliverable + orchestrator inspection + land consult naming the absent chain + post-integrate independent re-run); (2) wave 4 (L2 product/audit packets) PARKED — no gate downgrade (constitution); (3) `spine batch complete`/`--detect-manual-merge` REFUSE on failed-task state post-manual-land — close via post-integrate `abort` (merge durable, state archived) + husk cleanup; (4) engine misclassifies the 400 as "code review timed out — increase SPINE_REVIEW_TIMEOUT_MS" — do NOT chase that knob.
- 2026-08-04 — **Cross-machine handoff limit (wave-4 resume moot):** the desktop's parked batch `20260722T152755` lane commits (`a178c5c4` etc.) never existed outside the desktop — handoff text assumed resumable state that doesn't travel. Cross-machine parks must push lane branches (or land) before the other machine resumes; reconciliation must verify lane-commit EXISTENCE in the local repo, not trust handoff prose.
- 2026-08-04 — **The anthropic-400 "extra usage" park was a pi request-shape defect, NOT account exhaustion (fix = hermes-agent #48176):** Anthropic's June-2026 metering pause restores on-plan billing only for requests carrying the machine billing token as `system[0]` (`x-anthropic-billing-header: cc_version=<v>; cc_entrypoint=sdk-cli;`); the prose "You are Claude Code" identity alone (pi-ai 0.83.0's `__PI_OAUTH_FIX__`) is routed third-party → 400. Related classifier signals: tool-name shape (single-underscore `mcp_` = third-party; `mcp__server__tool` = first-party, hermes #46675) — pi-ai's `toClaudeCodeName` normalization already covers pi's tools. **Local patch `__PI_BILLING_HEADER_FIX__`** (billing block system[0], identity system[1], UA `claude-cli/2.1.221 (external, sdk-cli)`, version 2.1.123→2.1.221) → `pi -p` 200 on opus-5 AND fable-5. Durability: npm-global node_modules — RE-CHECK after every pi upgrade (grep the marker); patch the NESTED pi-ai under pi-coding-agent (the copy the CLI loads). Diagnostic lesson: the in-session-vs-subprocess split (in-session bpx-consult worked throughout — tool-less requests pass the classifier) masqueraded as an account-level outage; A/B the request SHAPE before believing a billing message.
- 2026-08-04 — **Timeout classifiers are PER-ATTEMPT, never whole-operation, when a retry backoff exists (SP-035):** one linked CTS around the retry loop makes the Retry-After backoff eat the request bound — transient 429/5xx misclassifies as `timeout`. Per-attempt CTS; backoff observes only the external token. Server-side hit-count tests made the misclassification falsifiable.
- 2026-08-04 — **Port defaults come from the SAME WPF path being ported (SP-035, two consult corrections):** the local provider's WPF timeout is 5 min (cold model loads), not the cloud path's 30s; conservative-posture defaults (retry OFF) follow the c6 none-admitted precedent. Check the same-path WPF value before proposing a default.
- 2026-08-04 — **Pre-socket rejection discipline extends to PROBES (SP-035):** probing a remote host is itself undeclared remote traffic — classify-first applies to every network path, not just the operation path. Probe Detail must not overclaim ("ollama-shaped service reachable; model presence unproven").
- 2026-08-04 — **Live stale-discard proofs need the dual-transport shape (SP-035):** a cooperative real provider cancels cleanly (client-gone, no late result possible); proving the seam against a REAL late arrival needs the token-swallowing decorator over the real socket (SP-019 RequestDetachedAsync), asserting the lab outcome `completed` as the arrival proof. Wait keys on the provider's SEND seam (`SendAttempts >= 1`), never a lab hit count (the probe's api/version hit can satisfy it early — the SlowOk race the pre-completion consult caught).
- 2026-08-04 — **Windows-reserved `nul` file as sole GitignoredDirtyWorktree blocker (wave-4 merge stage):** after bin/obj cleans, finalization failed on a 0-byte `nul` in the lane root (git can enumerate it but Windows can't stat it normally; `git clean -fdX` skips it). MSYS `rm -f nul` removes it. Cause UNEXPLAINED (appeared during a retry window — suspect an MSYS `> nul` redirect in some spawned step; never entered any branch). New probe rule: when T-5-class failures cycle through path lists, look for the reserved-name singleton (`nul`, `con`, `aux`, `com1`…).
- 2026-08-04 — **Lane-local pi-spine ALWAYS needs re-patching on a fresh machine/lane (3rd occurrence in one day → T-14 tooling row filed):** a fresh lane worktree's `.pi/npm` install carries none of the 7 project patches → the packet's verify.mjs contract step reds → worker remediates with apply.mjs mid-task. Recurrence rule fired; T-14 = worktree-setup-hook running apply.mjs at lane creation. Also: verify.mjs in a BARE scratch worktree (no .pi install) fails "project root missing" — the engine-root line is the load-bearing one there.
- 2026-08-04 — **bpx-consult provenance correction:** TWO config files exist — `~/.pi/agent/bpx-consult.json` (global, says solo=fable-5) and `.pi/bpx-consult.json` (project, says solo=opus-5). The PROJECT file governs in this repo (T-7 shadowing history). SP-036's worker cited the global file as answering-model provenance — honest but the wrong file; config-file provenance is unreliable either way (the file says the ROUTE, not the answering model). Verdict records must keep naming the actual answering model from response evidence, or record unknown.
- 2026-08-04 — **T-3 ordering norm restated (land-consult correction):** orchestrator re-run of the exact merge content comes BEFORE `spine gate approve`, never after (SP-037's sequence: ff → re-run → approve → integrate). And: on a loaded box, ONE timing-bound test may flake red on attempt 1 — re-run on IDENTICAL content before declaring a floor problem; record the flake with the test name (lost here — console filtered before capture; capture the [FAIL] name FIRST next time).
- 2026-08-04 — **Escalation/cooldown scopes are VALUES, not mechanics (SP-038, consult-caught pre-commit):** when porting a counter mechanism, the set of surfaces it gates is part of the owner-pending values — port the baseline's scope EXACTLY (WPF ModerationCounter gates user chat input only) and record extensions as questions, never silently extend to new operation classes.
- 2026-08-04 — **A boundary's coverage claim needs a tripwire, not a table (SP-038):** the wired/reserved inventory is executable — reflection over entry points and data variants fails the suite when a new surface appears unregistered. Tables drift; tripwires don't. (SP-009-sweep class discipline.)
- 2026-08-04 — **pi's session-start needsInstall version-satisfies gate makes lane pre-staging viable (SP-039):** a satisfying pinned version already present in a lane's `.pi/npm` is never reinstalled (and pinned sources skip pi's update checks) — the T-14 hook copies the main checkout's PATCHED tree at lane creation and it survives. Engine hook contracts decided from engine SOURCE, never shipped templates (the Flutter `.sh` template is POSIX-only; Windows no-shell spawnSync empirically rejects `.sh/.cmd/.mjs` — EFTYPE/EINVAL — only a real `.exe` spawns; same Node-24 class as the pi.exe shim).
- 2026-08-04 — **Zombie test hosts are the progressive-flake class (wave-5 T-3 forensics):** consecutive full-suite runs on IDENTICAL content flaked 1→2→3 red — leaked `dotnet.exe` test-host processes from earlier runs held loopback ports and poisoned later runs (zombie kill → immediate green). When a flake GROWS across runs, enumerate lingering test/dotnet processes before touching product code or doubting the floor; the failing test's name comes from a TRX logger (`--logger "trx"`), the default console output truncates it away. T-15 owns the harness hardening (the c2 lab's HttpListener disposal race, SP-023 fragility class).
- 2026-08-04 — **RECURRENCE (3rd today): gate-history edit-atomicity slips** — inserting an entry with an oldText that spans the previous entry's header ATE the header twice more (wave-4, wave-5 entries), each caught + repaired by an immediate line-structure audit (`awk` length list + per-entry grep count). Durable rule: after EVERY board edit, run the structure audit BEFORE the next edit — never chain two edits to the same file without an audit between. (The 2026-07-21 edit-tool atomicity lesson's third absorption; standing-order candidate.)
- 2026-08-04 — **Placeholder policy values belong in injected mechanism records, never in persisted document defaults (SP-040, consult-caught):** a persisted value reads as a decision and conflates "owner decided" with "default wrote"; null on disk = honestly undecided. Same class as c3's session-scoped escalation state (serialize the SHAPE, decide the value never).
- 2026-08-04 — **Explicit-clear on SP-005 machinery = write-empty-then-delete through the chained writer, plus the stray .tmp (SP-040):** dirty must be cleared before the delete or teardown flush resurrects the file; an orphaned temp is adopted by the next load's crash recovery (second resurrection path). And: a clear must not delete what the store is forbidden to write (newer-schema survives, typed Degraded); quarantine backups are contract-preserved, never part of "the persisted document". A typed outcome must report REALITY (File.Exists after the deletes) — a privacy operation that silently fails is worse than none; admission ≠ persistence (the admitted write's disk result is a separately observable completion).
- 2026-08-04 — **The SP-023 fresh-instance rule must be APPLIED, not just known (SP-041):** the c2 lab's retry loop was written AFTER the lesson was recorded and still reused the disposed instance (failed Start() → Prefixes.Clear() → ODE escaping into the test body — the wave-5 flake). When a lesson names a mechanism, grep the repo for the mechanism's other call sites AT lesson-recording time. Never throw a self-check from Dispose (masks the real red during unwind) — assembly-fixture teardown is the loud-and-safe hook (xUnit v3, zero test-count cost).
- 2026-08-04 — **The capture-before-clean rule extends to hook logs (wave-6 land):** the T-5-recovery `git clean -fdX` evaporated lane-1/2's `.pi/npm/worktree-setup-hook.log` BEFORE the land-consult check could read them (the consult's pre-approve check caught it; lane-3's survived). Rule: before ANY lane clean, capture `.pi/npm/worktree-setup-hook.log` + contract-failure logs + .reviews verdicts. Also: lane-1's record shows the T-14 hook works — first red-free contract run in 6 packets.
- 2026-08-04 — **DTRH cap-timer tests are the remaining timing-bound flake class (SP-041 stability chain + wave-4 precedent):** `DtrhNativeEffectsTests.FirePayload_Video_*` (0.05s cap + wall-clock poll under parallel load), ~1 red per 10 full-suite runs on a loaded box. T-16 filed; never doubt the floor on one red — TRX for the name, re-run on identical content.
- 2026-08-04 — **WPF string channels collapse distinct outcomes — porting routing means mapping the COLLAPSE, not the code (SP-042):** `GetKeywordCommentAsync`'s null return conflates refusal, unavailability, and empty reply; each needs an explicit typed disposition, and one of them (canned-on-refusal) is a behavior the typed contract deliberately changes. Also: cooldown class unions ≡ merged-max-dict (typed separate classes + union suppression are behaviorally identical — port the extend-not-shrink WRITE verbatim, not the storage shape); WPF has canned fallback ONLY on the keyword path (never generalized); reaction and keyword cooldowns are separate families and stay separate.
- 2026-08-04 — **Per-call CancellationTokens on generation-cancelled services are false affordances (SP-042):** cancellation here is generation/owner-based (SP-004) — per-call tokens mislead callers, and the analyzer (xUnit1051) taxes every test call site (17 warnings → removed the params; the 0W gate enforced the better design).
- 2026-08-04 — **The repo already owns the fix for every timer flake class (SP-043):** `ISoundClock` (`Audio/AudioSeams.cs`) was built 2026-07-22 for exactly this — grep `ISoundClock`/`ManualClock` before designing any new timing seam (proven fake pattern in THREE test classes now). Latent real timers are a class even when no assertion observes them — inject the manual clock CLASS-WIDE. Toy durations hide under fake clocks: the REAL parity value (SEGMENT_SEC=15) runs instantly on the fake clock and the exact-advance window verifies the due value itself (strictly stronger than a toy-duration wall-clock poll).
- 2026-08-04 — **Tooling-row closure must be consistent across same-class rows (wave-7 land consult):** T-15 (harness, zero product change) and T-16 (additive product seam) both closed DONE on evidence — closing one and not the other reads as an unstated quality judgment. Rule: same-evidence-class tooling rows close together with the citation (owner async-veto standing).
- 2026-08-04 — **Deferred work dies as footnotes — use named limits on the board row (land consult, SP-009 precedent):** the Reserved→Wired inventory flip and the residual bool-door are named limits WITH retirement conditions on the AI row (visible at every reconcile), and the discharging packet (c6) must name the coverage test in File Scope explicitly (SP-038's known limitation — a tripwire with hardcoded counts blocks the flip when the test sits outside the packet's scope).
- 2026-08-04 — **Packet-authored call-site counts rot (SP-044):** the packet said "4 bool-overload call sites"; the execution-time grep found 6 (later slices added callers). Retirement/migration obligations must re-grep at execution time and quote the fresh list; a stale count silently converts an all-or-nothing obligation into a partial one.
- 2026-08-04 — **Two representations of one consent state drift (SP-044 consult):** validation-time policy vs dispatch-time gates must derive from ONE source via factory (`AiExecutionGates.FromPolicy`) — parallel sources let a validated command pass a stale gate. Also: an executor that CANNOT be invoked on invalid input (internal-ctor plan) is stronger than any runtime guard — keep zero-execution semantics in the type system.
- 2026-08-04 — **Hook-log durability gap (wave-7/8 observation):** task-lane `.pi/npm/worktree-setup-hook.log` files are wiped by the WORKER lifecycle (not the T-5 clean — spare lanes' logs survive); spare-lane logs + journal `lane.setup_hook.completed` are the durable evidence. If hook-log forensics ever matter, move the log out of the cleanable zone (`.spine/runtime/<batch>/`).
- 2026-08-04 — **First zero-recovery wave (wave 8):** no merge-stage GitignoredDirtyWorktree at all. The T-14 hook (lanes arrive pre-patched) + T-15 harness hardening + the bin/obj/nul playbook have converted the wave-4-era endgame failures into routine lands. The playbook stays documented; don't expect the failure class.
- 2026-08-05 — **Environment facts rot (SP-046):** "no Ollama on the evidence box" was spike-time true (SP-019 limit 1) and became false — a real Ollama 0.32.5 now runs on the laptop. Capability assertions must be typed-state-honest, never environment-assumed; re-probe per machine per session, and write tests that pass in EITHER environment state without weakening the claim.
- 2026-08-05 — **A panic path that leaves the owner cancelled needs an explicit RE-ARM (SP-046 consult catch):** panic-quiet is not "surface calm" — after PanicAsync the owner stays cancelled and every later send terminates Cancelled forever. The surface re-arms via SelectProvider(current); the proof must include a post-panic SUCCESS. Applies to every future panic surface.
- 2026-08-05 — **MCP tool params fail SILENTLY (avalonia-live windowId drop, bit twice in one task):** `windowId` is ignored on screenshot/click calls — the first capture pass photographed the wrong window and only the manifest's dimension check caught it. Mitigation rule: validate a drive primitive end-to-end on a cheap target AND validate capture dimensions against the intended window BEFORE building an evidence pass. `target`/`handle` are the working params (the seat's guide names only those).
- 2026-08-05 — **Inspection scripts are code too (SP-046):** a wrong-case JSON field read (`j.Turns` vs camelCase `turns`) manufactured a nonexistent store defect and nearly cost a root-cause hunt — read the raw bytes before theorizing; a quick repro test is cheaper than a theory. Same class as "verify the inspection before the machinery".
- 2026-08-05 — **Board-edit slip count this session is now 5 (orchestrator):** WAVE-3 junction, WAVE-4/5 junctions, wave-6 ID-loss, spurious geometry-title edit, camera-header absorption — all caught by the immediate structure audit, zero shipped. The audit stays mandatory after EVERY board edit; consider scripting the pipe-count+entry-count check into the reconcile ritual (recurrence rule).
