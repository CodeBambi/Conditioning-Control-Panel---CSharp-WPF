# Port lessons — second attempt (living ledger)

Append-only, dated, one-to-three lines per entry. Harvested by the steering loop during batches (see `port-session-prompt.md` §Steering loop). Every spine worker reads this file via `referenceDocs` — keep it short and current; prune entries that are superseded or encoded into a skill/agent/constitution rule (note where they went). This is attempt-2 operational learning; WPF/attempt-1 lessons live in `first-attempt-lessons.md` and `first-attempt-systemic-lessons.md`.

## Entries

- 2026-07-18 — Windows worktree lanes need `git config core.hideDotFiles false` (hidden `.git` gitfile EPERMs pi-spine path rewrite). Applied repo-wide.
- 2026-07-18 — pi-spine fsync bug on Windows patched locally in `node_modules` (`abort.mjs`/`lifecycle-archive.mjs` open flag `"r"`→`"r+"`); does NOT survive reinstall — re-apply after any pi-spine update until fixed upstream.
- 2026-07-18 — Stub workers write only `.DONE`: packets with `fileScopeMustChange` cannot pass contract under `SPINE_WORKER_STUB=1`; use stubs only to smoke packet parsing.
- 2026-07-18 — bpx-consult solo routes: only `anthropic/claude-fable-5` engages outside interactive windows; `uva/*` providers are not registered in child contexts. Keep solo default on Fable.
- 2026-07-18 — Project Avalonia baseline is empirically 12.1.0 on net10.0 (SP-001); Avalonia MCP validation is 11.3.x-pinned — treat its version-specific hints skeptically.
- 2026-07-18 — SP-001 worker skipped STATUS.md updates and in-worker consults during manual-assisted execution; packets must state these as explicit checkboxes, and reviews should check them.
- 2026-07-18 — WSL2 Linux env (SP-002): the 26.04 distro registers as plain `Ubuntu` (check `/etc/os-release`, not distro name); `dotnet-sdk-10.0` (10.0.110) is in the native Ubuntu apt feed; no passwordless sudo — install packages via `wsl -u root` from the host.
- 2026-07-18 — `spine wait` exits silently (code 0, no output) within seconds-to-minutes in this environment regardless of batch state; it is unusable as a completion signal here. Do not recreate wait monitors — the cron steering loop + `spine status --diagnose` is the only reliable terminal-state signal.
- 2026-07-18 — Spine lane salvage inspection only checks DIRTY paths; committed lane-branch work is invisible to it (`salvageable:false` with 2 good commits on the lane). Before any retry, `git diff --stat <base> <lane-branch>` — retry/resume continues from lane STATUS.md.
- 2026-07-18 — node_modules patches needed so far: fsync `"r"`→`"r+"` (abort.mjs/lifecycle-archive.mjs/journal.mjs) and `dotnet` on the gate-evidence allowlist (evidence-command.mjs). NEITHER survives pi-spine reinstall/update — re-apply together; a durable patch mechanism is filed as tooling row T-1.
- 2026-07-18 — Orchestrator committing ANY file to the base branch mid-batch (even a port-lessons harvest) triggers `human_base_diverged` at land if the worker touched the same file. Batch harvest commits until after `spine batch complete`, or keep them on a side branch.
- 2026-07-18 — Integrate gate pins the orch tip SHA (`targetRevision`); manually advancing orch (conflict resolution) requires gate re-open: reset `.spine/runtime/<batch>/gate.json` to `status:pending` + new pin, then `spine gate approve` (CLI re-records the audit trail). Never edit `.spine/batch-state.json`.
- 2026-07-18 — BOTH batches so far ran ZERO engine reviews (SP-001 manual-assisted; SP-002 `resume_fast_path` skipped the stage; `agents.reviewer` was configured all along). SP-001's "proved review end-to-end" claim is stale — the review pipeline is empirically unproven until SP-003 (first clean-run batch).
- 2026-07-18 — When resolving merge conflicts with scripted edits, verify marker removal BEFORE committing; a failed python assert in a newline-chained (non-`&&`) command sequence silently committed conflict markers into 5fd1d540's first attempt.
- 2026-07-18 — Spine integrate-gate evidence (test/coverage output) is NOT refreshed after a retry/recovered batch — it presents the failed attempt's artifacts (batch 20260718T212127 gate showed 1 stale test vs actual 23). At every land after any retry, re-run the contract testCommand on the merge content before approving. Tooling row T-3.
- 2026-07-18 — Third consecutive batch with zero engine reviews (`spine_review_step` → skipped=true every call, reviewer configured). Do not rely on engine plan/code review existing; Fable solo consults are the de-facto review gate until T-2 diagnoses it.
- 2026-07-18 — `worker_orphaned` (lane worker process vanishing; journal `reconciled:true`) recovers cleanly via `spine batch retry` — resume continues from lane STATUS.md, no work lost. Cause still unknown (2 occurrences in one batch; SP-002 lost its worker to external SIGINT — watch for a pattern of parent-shell death killing detached workers).
- 2026-07-19 — Silent spine worker death (exit 1, empty output, ~4s after rules_selected) = spawn-layer failure. Root cause: worker tail prompt inlining referenceDocs exceeded the Windows 32,767-char CreateProcess limit (33.6KB). Fixed via local patch: `bin/spine-worker-runner.mjs` writes tails >16KB to `%TEMP%` and passes `@file` (T-1 manifest). Diagnose by running the runner manually in the lane with SPINE_* env.
- 2026-07-19 — NEVER run bare `pi` inside a spine lane worktree for diagnostics: it auto-installs project extensions (`.pi/npm/`) into the worktree → `GitignoredDirtyWorktree` failure at land. Probe in a throwaway dir, or `git clean -fdX` the lane afterwards (orchestrator-caused, own it in the gate history).
- 2026-07-19 — pi-spine gate evidence is copied from cache, not generated (collection finished in 8s — impossible for dotnet build+test); it has been stale at ALL THREE lands so far. Orchestrator re-run of the contract testCommand on the merge content is the controlling evidence at every land until T-3 is fixed.
- 2026-07-19 — `GitignoredDirtyWorktree` at finalization also fires on the worker's OWN bin/obj build outputs (not just foreign pollution) — expect it on every batch; recovery is deterministic: `git clean -fdX` in the lane → `spine batch retry <task>` → `spine batch resume` (~2 min). Engine classification bug filed as T-5.
- 2026-07-19 — In-packet WSL2 gate (SP-005 pattern) works end-to-end: worker copies `client/` to a native WSL dir (`~/ccp-spNNN`, never /mnt/e), runs the same testCommand, records output in record.md. One full-suite WSL2 run retroactively closes earlier rows' unit-test Linux debt — say so on their rows at land.
- 2026-07-19 — `maxConsultsPerTurn` (bpx-consult.json, default 3) counts across the WHOLE pi session (loop fires share the turn) and the running session caches it — editing the file mid-session does NOT lift the cap. Continuous-mode orchestration needs ≥2 orchestrator consults per cycle (land + pre-authoring) on top of anything else; raise the cap BEFORE starting a run (now set to 8) and restart the session. When capped: worker in-packet consults still run (fresh worker processes), so park new packet authoring/launching but landing fully-evidenced completed batches is defensible with the skip documented.
- 2026-07-19 — Fable consult replies truncate mid-sentence under long multi-question prompts (2nd occurrence: SP-005 pre-authoring, SP-006 pre-approach). Packet-authoring pattern: keep consult questions few and pointed, request condensed verdicts; when truncated, label the truncation and apply the received portion (SP-005/006 precedent) — do not re-ask the whole outline.
- 2026-07-19 — STUB-FIRST POISON: a stub-validation batch (`SPINE_WORKER_STUB=1`) leaves a stub `.DONE` in the lane; `spine batch retry` + `resume` on the SAME batch then takes the resume fast-path, classifies the task terminal-success with ZERO real worker output, and advances to needs_integrate (batch 20260719T093943 — caught before integrate: only commit was the stub .DONE marker, no step commits, no deliverables). New playbook: stub validation and the real run are SEPARATE batches — after a stub run, abort + clean lanes/branches, then fresh `spine batch start` for the real worker. Never retry a stub batch into a real one. Verify at every land: lane step commits exist beyond "worker completion". Tooling row T-6.
- 2026-07-19 — Avalonia 12.1 headed-harness facts (SP-007): (1) no UIA automation peers for Border/Grid/StackPanel — an app-side layout probe (LayoutUpdated → DIP bounds + RenderScaling + PointToScreen) is the reproducible locator; (2) the app opens UNACTIVATED behind windows and SetForegroundWindow is foreground-locked from background processes — pixel captures can false-positive on the occluder; use SetWindowPos(HWND_TOPMOST) on proc.MainWindowHandle and let pixel checks verify content themselves; (3) WSLg RAIL windows are invisible to GDI CopyFromScreen/PrintWindow (black bitmaps) — capture Linux-side via XGetImage (python3 ctypes + libX11, no packages).
- 2026-07-19 — Avalonia MCP advisory first real use (SP-007, A-013): ValidateXaml consistent with the real compiler (nothing new); AnalyzePerformance emitted a self-contradictory report ("Invalid XAML syntax" AND "Score: 90/100" in one response) — the exact authoritative-looking-garbage failure mode A-013 warns about. Bounded redacted review stays useful as a second opinion only; the 12.1.0 compiler + seeded AVLN2000 negative proof is the authority.
- 2026-07-19 — Repo-root `.gitignore` has a bare `tools/` rule (line ~167) that silently excludes ANY new `client/tools/**` path from commits; SP-008's harness nearly landed missing (the sln referenced files git never tracked — would have broken the build at land). Worker protocol now proven: run `git check-ignore` on new paths and audit the task's changed-file list before .DONE; force-add with care (`-f` also sweeps bin/obj — re-audit the index).
- 2026-07-19 — Budget/cold-build measurements must VERIFY the precondition (bin/obj actually gone) — a silently-failing prune produced a fake 2s "cold" build before the honest 12.9s was measured. Also: WSLg/X11 PointToScreen coords are WINDOW-RELATIVE (WSLg tiles monitors under one X root) — never mix coordinate spaces in capture crops; the failure mode is loud, which is acceptable.
- 2026-07-19 — Avalonia's `!AvaloniaResources` bundle also carries compiler metadata entries at ROOT level (`!AvaloniaResourceXamlInfo`); any completeness sweep over the bundle must expect `!`-prefixed ROOT entries — but exclude only root-level (a `!` final segment deeper in the tree would silently filter a real asset; SP-009 pre-completion consult caught the over-broad rule).
- 2026-07-19 — A Windows GUI-subsystem (WinExe) binary still delivers `Console.Out` through `> file` redirection — diagnostic self-check flags on the real binary need NO AttachConsole native interop.
- 2026-07-19 — After a bin/obj cold-clean, REBUILD before running a configuration (exit 127 = file gone, not a product failure); SP-009 budget pass bit once.
- 2026-07-19 — Advisor framing < empirical evidence, in both directions (SP-010): the pre-approach consult claimed settings land at `~/.config` literal; the store empirically honors `XDG_CONFIG_HOME` via .NET's Unix `ApplicationData` mapping — contract + doc comment corrected to observed truth, pre-completion consult acknowledged. Authority order working as intended; record the correction, don't hide it.
- 2026-07-19 — Incremental `dotnet publish` into an EXISTING single-file output dir silently DROPS native sidecars (libSkiaSharp/libHarfBuzzSharp/av_libglesv2) → BadImageFormatException 0x8007000B at run (SP-010 reproduced + fixed). Publish scripts always clean the output dir first.
- 2026-07-19 — `ldd` on a .NET apphost shows NOTHING useful (Avalonia's X11/fontconfig natives are runtime-dlopened) — the honest native-deps floor is `ldd` per shipped `.so` ∪ `/proc/<pid>/maps` at runtime (SP-010, MS docs verified).
- 2026-07-19 — `spine wait` early-exit defect confirmed a 3rd time (MonitorCreate exited ~2 min into batch 20260719T194819, batch healthy) — filed tooling row T-8; do not recreate wait monitors.
- 2026-07-19 — pi-loop hybrid trigger JSON rejected by LoopCreate (two spec forms) — de-facto hybrid steering = cron-only loop + MonitorCreate `onDone` one-shot wake.
- 2026-07-19 — `git worktree remove` can fail Permission denied on Windows right after running the artifact (file lock) while git has ALREADY unregistered it — `rm -rf` + `git worktree prune` recovers (SP-010 land scratch cleanup).
- 2026-07-19 — **`spine batch complete` is a REQUIRED explicit land step:** status says "completed successfully" but `.spine/batch-state.json` persists and fails the NEXT preflight (`no-active-batch`). Sequence: gate approve → integrate → batch complete → THEN post-land docs commits. Violating it costs an orch-first recovery (base-into-orch merge → batch complete) — SP-010→SP-011 transition.
- 2026-07-19 — Post-land human commits on orch-touched paths (task-board.md) with a leftover batch state → `human_base_diverged` at the next preflight. Recovery: `git checkout orch/<batch>` → `git merge feat/crossplatform` → checkout base → `spine sync-base` → `spine batch complete`. `spine batch dismiss` only allows limbo_stale/completed_manual/aborted (else `--force`).
- 2026-07-19 — T-2 closed: `## Review Level: N` HEADING is the only parsed form (bold prose never parses); in-worker review skips are `nested_spawn_blocked` BY DESIGN (SP-195/278); the engine runs code+final reviews post-.DONE with real verdicts (SP-011: APPROVE + PASS — first ever).
- 2026-07-19 — T-5's `git clean -fdX` lane recovery also deletes the lane's gitignored `.reviews/` — review EVIDENCE must be read from the journal events + record.md BEFORE cleaning when it matters (SP-011's verdicts were journal-recorded, no loss).
- 2026-07-19 — `HttpListener` cannot bind port 0 → retry loop over an ephemeral range (SP-011 loopback); 127.0.0.1 prefixes needed no URL ACL on this box. CORS-less error responses surface to fetch() as opaque TypeErrors — refusal responses must carry CORS headers to stay diagnosable.
- 2026-07-20 — T-5 (4th occurrence) sharpened root cause: with T-2 fixed, the ENGINE's post-.DONE code+final reviews write gitignored `.reviews/` into the lane AFTER the worker's completion commit — every Review-Level-2 batch now self-dirties and hits `DirtyWorktree` at finalization. Recovery unchanged (clean → retry → resume --force); read review verdicts from the journal BEFORE cleaning (line 2026-07-19 `.reviews` entry).
- 2026-07-20 — `spine batch retry` takes the BARE task ID (`SP-012`), NOT the folder slug (`SP-012-window-behavior-manifest` → "Task not found in batch").
- 2026-07-20 — WSLg/X11 window enumeration: `_NET_CLIENT_LIST` is ABSENT under WSLg (SP-012 demonstrator) — X11 EWMH client-list procedures must degrade to XGetImage/xprop per-window probes and record the absence honestly; never claim enumeration support from the protocol existing in docs.
- 2026-07-20 — MonitorCreate wrappers on this box report `[error]` with captured output lost (spine-wait monitor + T-3 scratch monitor both, same session) — treat monitor output as suspect, re-run the wrapped command directly for controlling evidence; the cron steering loop remains the reliable signal.
- 2026-07-20 — pi worker hard timeout = `stallTimeoutMinutes` (spine-config, 120 here) × task-size scaling, PER WORKER PROCESS; a healthy-but-slow worker dies at exit 124 exactly at the cap (SP-013 mid-Step-3). Retry→resume spawns a fresh process with a fresh budget. Override: `SPINE_WORKER_PI_TIMEOUT_MS` env. **Packet-template fix: headed-evidence-heavy packets declare Size L for the larger stall budget (applied to SP-015-avatartube on authoring).**
- 2026-07-20 — T-5 5th + T-8 6th occurrences (SP-013 batch 20260720T022627): Level-2 batches self-dirty via engine `.reviews/` every time (deterministic); `spine wait` early-exits every time. Both rows carry the counts; the workarounds are stable.
- 2026-07-20 — Avalonia 12.1.0 API rename: `SystemDecorations` is `[Obsolete("Use WindowDecorations instead.")]` (shim at `src/Avalonia.Controls/Window.cs:395-400` release/12.1.0) — the api-docs "latest" site still presents the old API as current. **Pinned-package XML/source is the authority, never the docs site** (avalonia-research §3; SP-013 caught it pre-implementation).
- 2026-07-20 — X11 window ids captured early DIE: a window resizes/moves through generations in its first ~2s (WSLg reparenting); xprop/xwininfo on a first-appearance id → BadWindow. Poll the tree until two identical SETTLED samples, or `xprop -name` (resolves fresh per query, immune) — SP-013 WSLg gate.
- 2026-07-20 — A-013 `AnalyzePerformance` self-contradiction 2nd occurrence (SP-007, SP-013: "invalid XAML" + "90/100 Excellent" in one response). **A-013 advisory template going forward: ValidateXaml only; AnalyzePerformance always reject; the 12.1.0 compiler + headless suite are the authority.**
- 2026-07-20 — Steering: loop fires do not imply 15-min spacing when monitor wakes interleave — verify the wall clock (and the 10-min heartbeat cadence) before diagnosing a worker stall.
- 2026-07-20 — T-5 6th + T-8 7th occurrences (SP-014 batch 20260720T052700; `spine wait` now exits ~0 min in). Both deterministic; rows carry counts; workarounds stable. SP-014 archaeology premise correction: **the title-keyed dispatch "being replaced" lived only in the FIRST Avalonia attempt — WPF's pre-state was keyless hard-wiring**; row title implies a WPF mechanism that does not exist (empirical evidence proves doc premises stale — record prominently, don't hunt WPF for it).
- 2026-07-20 — **spine's worker timeout kill does NOT reap the pi node process tree on Windows (T-10):** SP-015 run-1 (PID 11212) survived its exit-124 'death' and kept committing to the lane ~16h unsupervised (Steps 2–3 landed post-mortem, journal events written with the lane correlationId AFTER `batch.failed`), poisoning the retry — two writers, one worktree; run-2 detected it and yielded cleanly (SP-015 `DUPLICATE-WORKER.md`). `lane.died` ≠ worker gone. **Before ANY `spine batch retry` after a worker failure: enumerate processes matching the batch worktree path (`Win32_Process CommandLine -match spine-<batchId>`) and kill zombie trees first; salvage-commit dirty lane WIP with UNVERIFIED provenance (workers resume from lane STATUS.md, so verified steps survive).** Likely contributor to past `worker_orphaned` mysteries. **2nd occurrence 2026-07-21 (same batch, run-3): anatomy sharpened — the kill gets the pi.exe SHIM; the node.exe AGENT survives. Salvage commits must EXCLUDE `.pi/loops/*.json` / pi session metadata (engine auto-commit swept 2 into the branch; removed pre-integrate).**
- 2026-07-21 — **`SPINE_WORKER_PI_TIMEOUT_MS` override CONFIRMED:** run-3 died exit 124 exactly 4h after spawn (vs the flat 2h `stallTimeoutMinutes` config) — the env override is inherited by detached engines/workers from the resume shell. Set it at launch for headed-evidence packets; see T-11 for the deeper packet-sizing rule (SP-015 run-3 hit even the 4h cap on a healthy worker — budgets stretch, they don't solve).
- 2026-07-21 — **Window-evidence harness facts (SP-015 record.md Step-4, reusable for W-row exercises):** (a) an OWNED window nests UNDER its owner in the UIA tree — it is NOT a root child; match windows by NAME, never root-child text search (tube/dashboard conflation root-caused all G9 failures); (b) pinned 12.1.0 `WindowImpl.SetParent` (src/Windows/Avalonia.Win32/WindowImpl.cs:752-768) parents ownerless `ShowInTaskbar=false` windows to its HIDDEN OffscreenParent — GW_OWNER==0 gates are wrong for them; use GW_OWNER!=owner + a behavioral survive-owner-minimize gate; (c) UIA `BoundingRectangle` != `GetWindowRect` on this box ((34,57)px measured divergence) — window geometry for input/caption math must use Win32 `GetWindowRect`.
- 2026-07-21 — **Retry fast-path land provenance (first):** a T-5 finalization failure on a fully-reviewed/verified lane → `git clean` → `spine batch retry` → the fast-path classified `task.completed` from the clean lane re-inspection (real .DONE + step commits — NOT the T-6 stub poison shape) — but opened NO integrate gate. Recovery: orchestrator writes `evidence/summary.md` + `gate.json` (status:pending, targetRevision=orch tip after ff-advancing orch to the verified lane tip) per the documented gate-reopen pattern → `spine gate approve` (CLI audit trail) → `spine integrate`.
- 2026-07-21 — **Engine suggested-fixes can be DESTRUCTIVE — verify against repo reality before following (T-12, 2nd engine-output-vs-reality incident):** SP-016's merge stage failed `merge blocked by gitignored paths on a lane branch` and suggested "drop cached ignored files" — the flagged paths were legitimately-tracked `client/tools/**` (SP-008/SP-010 deliverables) + legacy `Tools/`; following the advice would have merged DELETIONS of landed work into base. The check must set-minus paths already tracked on the target. Recovery: manual orch ff + gate-record recovery + integrate + retry fast-path complete. First incident: engine auto-commit sweeping `.pi/loops/*.json` into a lane branch. **A third incident triggers a broader standing-order review (Fable land consult, SP-016).** **T-12 is DETERMINISTIC (2nd consecutive batch, SP-017): the over-broad scan fires on every Level-2 batch — the manual-ff + gate-record + integrate + retry-complete playbook is now the EXPECTED land path encoded in steering-loop templates, not a contingency. Keep packets as-is; the tax is entirely in the merge stage (Fable, SP-017 land).**
- 2026-07-21 — T-8 8th + 9th confirmations (SP-015 monitor `[error]` lost output; SP-016 wait exited ~4 min into a healthy batch). Row count updated; cron steering loop remains the only reliable terminal signal.
- 2026-07-21 � libvlc headless-probe shape (SP-018): dummy vout + sw decode crashes a BACKGROUND thread ~at/after EndReached, and native teardown segfaults in EVERY disposal order (both LibVLCSharp 3.10.0+3.0.23.1 and WPF-pinned 3.8.5+3.0.21) � vmem memory-callback vout is stable AND gives frame-level decode proof; harnesses hard-exit after flushing evidence, product teardown discipline is the unified-video row's open problem.
- 2026-07-21 � Token-bearing media URLs (signed/expiring) PERSIST in the WebView2 HTTP cache (`Cache_Data`) unless the source sends `Cache-Control: no-store` (SP-018 V5 � caught by the spike's own secret audit). Any host serving credentialed URLs to a WebView needs no-store at the source and/or profile hygiene.
