<Project Sdk="Microsoft.NET.Sdk">

  <!--
    SP-019 quarantined AI-provider spike host. NOT referenced by client/CcpClient.sln.
    Inherits client/Directory.Build.props (single <Version> authority) — nothing else.
    Console host: acceptance is lab-verified probe evidence, not a test project.
    ProjectReference to CcpClient.Desktop is packet-permitted (PROMPT File Scope):
    it exercises SP-016's REAL AiEnvelopeValidator/vocabulary — references are not
    product-code changes. Zero product-code change is the quarantine invariant.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>12.0</LangVersion>
    <RootNamespace>CcpSpike.AiProvider</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\CcpClient.Desktop\CcpClient.Desktop.csproj" />
  </ItemGroup>

</Project>
