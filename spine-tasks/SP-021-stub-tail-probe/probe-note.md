# SP-021 stub tail-spawn probe

Stub-only packet: exists to give `spine batch start` a pending task for the T-1 post-land reinstall gate's >16KB worker-tail spawn proof (worker-runner `@file` patch). Never run with a real worker. The referenceDocs tail (~40KB: constitution + task-board + port-lessons) is what exercises the `@file` path.
