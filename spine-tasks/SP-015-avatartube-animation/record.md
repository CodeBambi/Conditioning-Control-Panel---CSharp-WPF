# SP-015 record — prove AvatarTube rendered animation

Worker session: batch `20260720T072956` lane-1. Review Level 2 (T-2 heading present; engine-review presence recorded per `spine_review_step` call below).

## Step 1 — decoder claim verification (FIRST checkbox)

**Engine-review presence (T-2):** `spine_review_step` step 1 type=plan → `skipped: true` (nested-spawn blocked by design SP-195; `reviewLevel: 2` echoed — heading parse holds). Engine code+final reviews run post-.DONE.

**Claim under test:** "animated-GIF decode in Avalonia 12" — packet-declared UNVERIFIED CLAIM.

**FINDING: the pinned Avalonia 12.1.0 surface has NO public animated-GIF decode/animate API. Own-frame composition is the demonstrator shape.**

Evidence (pinned package + pinned source, never the docs site — WindowDecorations lesson):

1. **Pinned-package reference XML** (`~/.nuget/packages/avalonia/12.1.0/ref/net10.0/`): zero `gif` mentions in `Avalonia.Base.xml` and `Avalonia.Controls.xml` (grep count 0). The entire `Avalonia.Media.Imaging` public surface is static-bitmap only: `Bitmap`, `WriteableBitmap`, `CroppedBitmap`, `RenderTargetBitmap`, encoder options, `IBitmap`.
2. **Pinned source tree @ tag 12.1.0** (github.com/AvaloniaUI/Avalonia): `src/Avalonia.Base/Media/Imaging/` = 14 files, all static-bitmap (no Gif/Animation/Codec files); `src/Skia/Avalonia.Skia/` complete 37-entry listing — no codec/anim/gif files; decode lives in `ImmutableBitmap.cs`/`WriteableBitmapImpl.cs` (static, Skia-backed).
3. **Local cache's only GIF package:** `AvaloniaGif 1.0.0` targeting `.NETCoreApp3.1` / `Avalonia 0.9.3` — prehistoric, v12-incompatible on its face; `xamlanimatedgif 2.3.0` is the WPF product's library (WPF-only). No v12 GIF decoder package is present or admitted; package admission would be a council-class decision AND is unnecessary (synthetic packs are generated by us — stills suffice).
4. Supporting APIs verified present in the pinned ref surface: `WriteableBitmap` ctors + `DecodeToWidth/Height` (Avalonia.Base.xml), `TopLevel.RequestAnimationFrame(Action<TimeSpan>)` (Avalonia.Controls.xml) — noted for completeness; the engine drive is an SP-004-owned operation regardless.

**Consequence (pre-authoring consult's predicted outcome):** packs = still-frame sequences + delay metadata; the engine advances frames on per-frame delays inside ONE SP-004-owned operation. The acceptance's timing properties (cadence, multiplied-speed falsification, successor-frame resume) become directly testable because the timing machinery is ours.

## Step 1 — WPF AvatarTube archaeology (READ-ONLY, `File.cs:line`)

Full extraction by sliced reads of `ConditioningControlPanel/AvatarTube/*`; citation-dense report held in worker context. Load-bearing semantics:

- **Static pose mode:** `DispatcherTimer` 3s cadence, index wraps modulo set (`AvatarTubeWindow.xaml.cs:173-178`, `Avatar.cs:1355-1361`); fade = dip-to-0.3 over 150ms then source swap and opacity reset (`Avatar.cs:1364-1371`) — NOT a crossfade. Start only when not animated and poses>1 (`xaml.cs:178-179`).
- **Animated GIF mode:** XamlAnimatedGif library drives decode/loop Forever; app keeps no frame timer; pose timer stopped; load failure → static mode restored (`Avatar.cs:274-333`). In Avalonia 12.1.0 no such library exists (decoder finding above) — the engine owns frames.
- **Circe emote system:** two-layer crossfade with `_circeFadeMs` default **1000ms** (`emotes.json fadeMs` override; `CirceEmotes.cs:84,957-1023`); clips play ONCE (`RepeatBehavior(1)`, `:978`) and `AnimationCompleted` drives rotation (`:855-904`); weighted idle rotation excluding current (`:1124-1139`); load gate — fade never starts until incoming has a live animator, grace caps 5000ms idle/900ms talk/900ms empty-layer, **roll back to previous visible layer on expiry — never fade to blank** (`:1057-1116`); watchdog 500ms + stall retry 1200ms + force-advance 3000ms (`:475-533`); min-hold 2000ms coalesces interrupts (`:78,907-942`); decode error → log + watchdog stays armed, no synchronous advance (anti-hot-loop) (`:870-880`).
- **Speech/talk:** `PlanTalk` — snappy first clip (speakStart ≤ 700ms), speak-window coverage, cap 3 clips; timer-driven schedule so each clip's mouth opens as the previous ends; reaction lands `talkLeadOutMs` before voice end; settle: reaction completes → idle; interruption → `StopTalkSequence` (`CirceEmotes.cs:545-853`). Needs the audio/bark pipeline — demonstrator substitutes a synthetic declared-duration trigger (no audio).
- **Click reaction:** own pool (shy/sultry/adoring/tender/blowkiss default), 3000ms cooldown, interrupts talk, plays once → idle (`CirceEmotes.cs:137-139,777-801`). Distinct from speech.
- **Gentle float:** ONE 16ms timer, `sin(phase)×4 DIP`, period ≈ 2.01s, transform on avatar content only (`Windowing.cs:501-520,52-53`); portrait extras ride the SAME timer (no second clock); stop → Y→0 (`:571-585`).
- **Pause/resume:** owner minimize / owner-not-visible / explicit hide → pause animators + hide; restore → resume + show only if enabled (`Windowing.cs:643-716`); float uses stop-then-recreate; GIF resume idempotent; Circe resume re-plays existing animators + re-arms watchdog only — **resume never creates a second pipeline**; pause also stops talk scheduling (`CirceEmotes.cs:1339-1357`).
- **Theme/mod switching:** dispose before replace — GIF sources nulled, talk seq + watchdog + min-hold stopped, opacity animations killed, both layers cleared, opacities reset (`Avatar.cs:447`, `CirceEmotes.cs:380-412`); set-switch 200ms out/in fade with `_avatarSwitchGen` generation token dropping stale fade completions ("ghost avatar" fix, `Avatar.cs:389-392,476-496`).
- **Attach/detach:** detach → tool-window style, `ShowInTaskbar=false`, topmost + reassert, drag/resize contract, context-menu swap, state (bubble text, clips) preserved — animation pipeline untouched (`Windowing.cs:1449-1496`); attach → reverse, scale reset, reposition (`:1498-1563`).
- **Owner transitions:** `LocationChanged`/`SizeChanged` → reposition + z-order; placement left of owner with `BaseOffsetFromParent=-350`, `VerticalOffset=20`, sanity clamps (`Windowing.cs:587-641,46-49`); minimize/restore above; `WM_DPICHANGED` + 450ms quiesce debounce (`:334-413`).
- **Cleanup on close:** explicit stops of `_poseTimer`, fullscreen check, float, speech ×2, idle, trigger, random-bubble timers; GIF source nulled (frames freed); WndProc + parent WndProc hooks removed; 15+ service-event unsubscriptions; all in try/catch with logging (`Windowing.cs:863-984`). Noted gap (archaeology): Circe watchdog/talk timers not explicitly stopped in the read body — dispatcher teardown reliance.
- **Undecodable/missing assets:** pose → set-1 fallback → empty bitmap, per-pose Warning log (`Avatar.cs:1300-1348`); set GIF load exception → static mode (`:321-333`); emote clip names pruned at map load, unknown clip → early return; decode error → watchdog keeps rotation; **user never sees a blank beyond ~900ms empty-layer cap; diagnostics are log-only, no dialog** (`CirceEmotes.cs:870-880,1147-1160`).

## Step 1 — first-attempt timer/subscription-leak REJECT lessons (cited explicitly)

The acceptance's "no growing timers/subscriptions" targets exactly this scar tissue (`ConditioningControlPanel/CCP.Avalonia/AvatarTube/*`, READ-ONLY):

1. **REJECT fresh-timer-per-entry without disposal** — `_portraitAmbientTimer`: `StartPortraitAmbientAnimation()` creates a NEW 33ms `DispatcherTimer` on every portrait re-entry without disposing the old (`AvatarTubeWindow.Avatar.cs:1152-1162`) → multiple simultaneous UI-thread timers, cascading transforms. The multiplied-speed/duplicate-pipeline defect class in its purest form.
2. **REJECT untracked per-instance window+timer spawn** — `AvatarRandomBubble` created a fresh Window + 50ms timer per bubble with no live-set tracking/ceiling; fixed later via tracked `Live` list + `DestroyAll()`/`DrainPool()` from `OnClosed` + `MaxLive=6` (`AvatarRandomBubble.cs:18-31,68-93`; header comment documents the defect). Orphaned windows/timers on tube close and theme switch.
3. **REJECT created-but-not-started / created-before-stop timer lifecycle** — constructor creates `_poseTimer` but never starts it (`AvatarTubeWindow.axaml.cs:309-310`; already a landed lesson line); `StartFloatingAnimation()` can create a new 16ms timer while the old still runs (`AvatarTubeWindow.Windowing.cs:416-428`). Initialization/liveness defects: timers as evidence of animation is exactly the lie this task's rendered-frame acceptance exists to refute (first-attempt-lessons: "REJECT Animation timers or decoder events prove AvatarTube animation").
4. **REJECT subscriptions without symmetric unsubscribe** — `WireCompanionReactionEvents()` subscribes 7+ service events (`axaml.cs:407-466`); the unwire call's body could not be verified complete in the read (INFERRED risk) → dead-window handlers firing on recreated tubes. Greenfield rule: field-backed handlers, symmetric unwire at the same indentation level as wire, counted in tests.
5. **Counter-examples (safe patterns observed, not adopted as templates):** per-call speech/idle/trigger/bubble timers all follow stop-before-create (`axaml.cs:1362-1747`); `CirceEmoteEngine` timers are field-backed and stopped in `Leave()`/`Pause()` (`CirceEmoteEngine.cs:41-44,149-175`); `TubeGeometryController` disposes its timers in `Dispose()` (`TubeGeometryController.cs:108-126,216-221`).

**Greenfield consequence (design rule, binds Steps 2-3):** ONE frame pipeline = ONE SP-004 owned operation per attach/run generation — no `DispatcherTimer`/`Threading.Timer`/`PeriodicTimer` anywhere in the feature; float rides the same operation tick; crossfade opacity is engine-stepped (no Avalonia `Transitions` clock as a second timing authority); pause = an in-operation gate (resume continues the SAME operation → single pipeline + successor-frame by construction); every event subscription field-backed with symmetric unwire; leak acceptance reads REAL registries (OperationRegistry outstanding/discarded counts + engine subscriber count) across N attach/detach/pack-switch cycles.

## Step 1 — owner-transition demonstrable-vs-contract split (from archaeology)

Dashboard = a real movable/resizable/minimizable window; no session engine, no audio/bark, no mods/themes system, no quiz windows, single 1.0-scale monitor box.

- **DEMONSTRABLE (Windows-headed):** attach/detach with state preservation (strip identical across switch, then continues); detached topmost property (Win32 `WS_EX_TOPMOST` per SP-012 procedure); owner move → tube follows (position delta == owner delta); owner resize → reposition; owner minimize → pause + hide (frozen strip, window hidden); owner restore → resume + show + SUCCESSOR frame, unchanged cadence; cleanup on tube close and owner close (registry zero).
- **CONTRACT-ONLY named limits:** monitor changes / mixed scaling (single 1.0 box — inherited SP-007 gate); detached drag/resize input contract (demonstrator detached mode = position/topmost/state only; WPF dead-zone/Ctrl+wheel scale semantics need the full widget contract); quiz-window z-order exceptions (no quiz windows exist); fullscreen-detection hide policy (no session concept); DPI-change quiesce (single-scale box); speech/audio-driven talk triggering (synthetic declared-duration trigger substitutes; real trigger needs the bark/audio rows, BLOCKED on the audio spike).

## Step 1 — pre-approach consult verdict (solo; requested `anthropic/claude-fable-5` — council unavailable T-7)

**Actual answering model: Fable family, consistent with the requested route (T-7 discipline).** Verdict (applied, binds Steps 2-5):

1. **Pause gate MUST observe the generation token** (`WaitAsync(token)`) — a paused engine whose gate ignores cancellation wedges teardown's drain and breaks the zero-unobserved rule.
2. **Fixed-quantum engine loop (~16ms) with MONOTONIC-clock (Stopwatch) deadline accumulation** — not per-frame `Task.Delay` chains. Frame N appears at cumulative-delay deadline ± quantum; deadline-based scheduling absorbs Task.Delay's systematic 1-15ms overshoot (Windows) instead of accumulating drift — load-bearing for cadence falsification. Pause freezes deadline advancement; resume re-bases (`deadlineBase += pauseDuration`) and waits out the remainder of the current frame's delay — resume semantics pinned EXACTLY so "cadence unchanged" is assertable.
3. **Float:** step from the same monotonic clock every quantum — stepping float only on frame ticks (80-300ms) would be visibly choppy and couples float cadence to clip delays (WPF's 16ms tick exists for a reason). One operation, one clock, one loop.
4. **Engine-stepped opacity CONFIRMED** (vs Avalonia Transitions): pause freezes the crossfade automatically; the never-blank invariant (`opacityA + opacityB >= 1 - epsilon` at every step) is pure math, unit-testable; ~60 posts/s through IUiDispatch is nothing. Transitions would be a second timing authority the engine can't pause — rejected.
5. **Capture-sampling correction:** PS `CopyFromScreen` costs 50-200ms jitter per capture; 80-300ms holds cannot be sampled reliably. ALL synthetic clip delays must be >= ~400ms (non-uniform, e.g. 430-900ms) so the slowest capture cadence samples every hold >= 2x for the no-blank/dup-run gates. Multiplied-speed falsification becomes a **schedule-fit test**: observed (t, index) pairs must fit the 1x cumulative-delay schedule within tolerance AND must NOT fit 2x or 0.5x schedules — non-uniform delays make the schedules distinguishable (why the packet bans uniform delays).
6. **Duplicate-pipeline falsification:** add **monotonic-modular-advance** assertion (each observed frame is a forward step within its clip; an interleaved second pipeline produces backward jumps) + registry outstanding==1 corroboration during the leak long-run.
7. **Temporal math lives in CcpVerify, not the script:** new pure sequence evaluator (samples JSON -> named PASS/FAIL verdicts: advance, no-blank, monotonic-modular, no-dup-run-beyond-hold, schedule-fit-1x, schedule-not-2x, schedule-not-half-x, resume-successor, pause-freeze, pack-switch-clean), unit-tested like CheckEvaluator (SP-008 pattern). The PS script stays dumb: capture -> decode -> append -> call evaluator once. Cadence tolerance windows are machine-checked, never hand-held PowerShell.
8. **Crossfade strip-blend trap (false-blank):** mid-crossfade the two layers' strips BLEND and strip decode fails — a decode failure there is NOT a blank. During engine-declared crossfade windows (known from the frame trace) the no-blank gate = union-of-visible-content (incoming OR outgoing OR blend non-blank); strip assertions apply outside crossfade windows. The engine logs each applied frame (pack/clip/index + monotonic ticks) to ILogSink as a **frame trace** — corroboration and crossfade-window knowledge, never standalone liveness evidence (REJECT lesson stands).
9. Under-thinking flagged: teardown ordering of the pause gate (point 1) and the false-positive risk of naive blank checks (point 8) — both designed in above.

## Step 2 — synthetic asset pipeline + animation engine (DONE)

**Engine-review presence (T-2):** `spine_review_step` step 2 type=plan → skipped (SP-195 by design; reviewLevel 2 echoed).

**Files (all new unless noted):**
- `client/src/CcpClient.Desktop/Features/AvatarTube/SyntheticAvatarPacks.cs` (contract-named): deterministic generator (pure integer math, no PRNG/clock) of TWO packs — `circuit` (id 0, cyan/magenta) and `pulse` (id 1, amber/green) — sprite-sheet BMP (row = clipId, col = frame; 6 clips × ≤6 frames of 96×128) + pack.json (per-frame NON-UNIFORM delays, all ≥ 430ms per consult #5); fallback sheet (pack 3, clip 7, single static frame at row 7); strip written into the painted cell directly.
- `BmpCodec.cs`: pure 32/24-bpp BMP codec (bottom-up + top-down decode; BI_RGB only — rejections are loud). BMP = the cross-platform evidence format (XGetImage/CopyFromScreen both emit it; SP-013 precedent); zero decode dependencies anywhere.
- `AvatarStripCodec.cs`: machine-checkable strip (magenta marker + 2 pack bits + 3 clip bits + 4 frame bits, 4×4 blocks, bottom-left 40×4); marker auto-locate in the bottom 16 rows (float/centering immune); luminance-bit decode with ambiguity rejection; `ContentFraction` for the union no-blank rule.
- `AvatarPackLoader.cs`: stream-seam loader (production: StandardAssetLoader avares://; tests: in-memory/corrupt) + grid validation + per-frame strip self-check (generator/slicing drift fails loudly).
- `AvatarSchedule.cs` + `AvatarSequenceEvaluator.cs`: pure cadence math + named verdicts (no-blank union, frames-advance, monotonic-modular + elapsed-bounded jumps, no-dup-run-beyond-hold with pause-window subtraction, schedule-fit 1x/not-2x/not-0.5x, pause-freeze, resume-successor, cadence-unchanged-after-resume, pack-switch-clean). Schedule tolerance 500ms — discrimination math recorded in-code (true-run quantization ~420ms + pause systematic ~300ms vs 2x misfit ~1000-1900ms / 0.5x ~2000-3800ms — decisive margin).
- `AvatarAnimationEngine.cs`: ONE SP-004 owned operation per generation drives frames (monotonic deadline accumulation), engine-stepped crossfades (opacity sum == 1 invariant), dip-fades (identity swap at midpoint, deadlines on the DECLARED schedule — no drift), float (sin from the same effective clock every quantum). Pause = in-operation gate observing the generation token, freeze stamped at call time; resume = deadline re-base (successor-frame + unchanged cadence by construction). `IAvatarClock` seam (System/Manual) — cadence tests are deterministic. WPF-parity demonstrator constants pending-owner: fade 1000ms, min-hold 2000ms, click cooldown 3000ms, talk lead-out 500ms, dip 0.3/150ms, float ±4 DIP/2000ms, quantum 16ms.
- `AvatarTubeParticipant.cs` (registered in CompositionRoot): pack decode-probe per activation (the capability probe), typed SP-006 state — `Available` on decode / `Degraded(asset-undecodable)` + static fallback (new reason code, additive) / bounded diagnostics (exactly one decode attempt per switch, no retry loop). Pack switch = same operation, atomic pack swap (old frames dropped before replacement activates — never two avatars, zero operation churn). `Completion` survives StopTube for awaiting.
- `AvatarEvidence.cs` + Program.cs bounded flags (pre-phase, --verify-assets discipline): `--generate-avatar-packs <dir>`, `--avatar-strip-decode --capture <bmp>` (JSON line: decode + content fraction), `--avatar-sequence <samples.jsonl> --pack <def.json> [--trace <trace.jsonl>]` (named verdicts, exit 0/2).

**Manifest:** 5 new embedded entries (2 sheets, 2 defs, 1 fallback) — `--verify-assets` PASS (7 entries) on **Debug AND Release** (Windows). Generation SHA-256 (recorded per packet):
- pack-circuit.bmp `d8ecd4f8f98fa9b21f359d187fb25220fc4d15a8d6aa4de9853308b2b57f5f90`
- pack-circuit.json `3dd71bbc2bced46eae4ba4b2ec302fdfb22384035511da84b2de2566596cc638`
- pack-pulse.bmp `2834b81435cdcd4e0289c67d6ed82f1a78910268d14642095c933a447399bc11`
- pack-pulse.json `a5c55b20718f093e621afbf2b7f088178cace8ba171c92958359a5b7fd08b207`
- avatar-fallback.bmp `81844dfe4a34fdb7dc357b7e67721e2ce2fa52f68bd7f347279823a07871e8e0`

**Tests (169/169 unit + 15/15 headless green, 0W/0E solution):** 23 new (pack/codecs/defs/loader/determinism-vs-committed-assets ×8, engine ×8, evaluator ×7) + 2 participant-count updates (3→4). Cadence asserted at exact declared non-uniform deadlines (±1 quantum); pause/resume = successor-frame + unchanged cadence at exact effective-time boundaries; pack switch + 10 start/stop cycles with REAL OperationRegistry counts (outstanding 1→0, subscribers constant 1, zero unobserved); undecodable → Degraded + fallback + exactly-one-attempt bound; evaluator verdicts pass/fail shapes incl. 2x/0.5x falsification; strip round-trip over every frame of both packs; committed assets pixel-identical to regeneration (determinism). Bugs caught by the tests: strip-blit erasing art alpha, unused-sheet-cell alpha holes, fallback grid mismatch, first-click cooldown overflow (`long.MinValue` arithmetic), pause-stamp timing (call-time vs loop-observe-time), stop-during-pause gate cancellation path, ManualAvatarClock park-race (test driver now parks-aware via `DelayPending`).

## Step 3 — demonstrator surface + behaviors (DONE)

**Engine-review presence (T-2):** `spine_review_step` step 3 type=plan → skipped (SP-195 by design; reviewLevel 2 echoed).

**Files:** `AvatarTubeDemonstratorWindow.axaml(.cs)` (explicitly-labeled DEMONSTRATOR tube: two crossfade Image layers at natural size with nearest-neighbor (crisp strip), float Canvas inside a 4-DIP-headroom stage (strip never clips), capability + probe texts (UIA-readable: pack/clip/frame/mode/outstanding/subs), demonstrator control strip — Animate/Static, Talk, Pause/Resume, Switch pack, Detach/Attach); `AvatarBitmapCache.cs` (per-pack platform bitmaps, old pack's disposed on switch — disposal parity); App/Program wiring: `--avatartube-demo` (open at startup — WSLg no-input rule), `--avatar-corrupt-demo` (in-memory pulse corruption via `CorruptPackForDemo`), `--avatar-trace <path>` (JSONL trace sink).

**Behaviors, all through the ONE engine (no parallel timers):** static pose dip-fade-in + cycling; animated looping; idle/idle2 rotation with engine-stepped crossfades; talk sequence (synthetic declared-duration trigger — audio pipeline is a BLOCKED row, named limit) → reaction → idle; click reaction via the REAL gesture on the avatar stage (cooldown, talk interruption, distinct pool); gentle float (content-only TranslateTransform, sin from the engine's effective clock); pause/resume button + owner-minimize pause/hide + restore resume/show; pack switching (button; old pack bitmaps disposed); attach/detach (button; view-only — engine untouched, `Show(owner)`/`Show()` establishes the relationship — Avalonia 12.1.0 resets the Owner property on the unowned path, property-set does not stick); owner move/resize follow (`PositionChanged`/`SizeChanged`); cleanup on close (symmetric unwiring + generation cancel + bitmap disposal).

**Draw-level headless tests (7 new, 22/22 green):** open/layers/capability; frame advance + float transform + probe; attach/detach view-only preservation; real-routed avatar click → click emote; pause freeze + resume successor; corrupt switch → typed Degraded text + fallback rendered; close → engine disposed, subscribers 0, registry back to heartbeat baseline. Fixes the tests caught: `args.Pack` self-sufficiency (queued post from the old pack resolving against the new pack = KeyNotFound), Show-owner vs Owner-property, heartbeat-baseline registry accounting.
