# SP-054 — Graded Intake web-core host: record

Wave-14 solo lane (L). Review Level 2. Contract: verify.mjs + build 0W/0E + both test projects (floor 683/33).

## Step 1 — archaeology (READ-ONLY, WPF against the worktree head = main content)

Method: one `wpf-archaeologist` agent (read-only, File.cs:line citations; hit its turn limit — residual gaps listed below) + direct payload/landed-machinery reads by the worker. SP-050 record §Delta 6 (the obligation table cell) re-read as the row's carried contract.

### Window/host (WPF `Services/Quiz/IntakeHostService.cs`, static class, 1081 lines)

- `ProductName="Graded Intake"` (:42), `Protocol=1` (:44), `ProxyBaseUrl="https://codebambi-proxy.vercel.app"` (:50).
- `Launch(testMode, duckMainWindow=true)` (:74) idempotent (live instance → focus only :75); fire-and-forget audio-web pack request (:81); 3 virtual-host mappings (:88-101); `ChaosWebViewHost` (:103-127): StartUrl `https://ccp.game/intake/index.html` (:105), profile `browser_data_intake` (:108), `StartFullscreen = !_recoveryWindowed && IntakeFullscreen==true` (:115 — recovery relaunch always windowed), OwnedByMainWindow (:119), autoplay-without-gesture (:123).
- Fullscreen: page `fullscreen-set {on}` → `ApplyHostFullscreen` (:352-366): set, echo ACTUAL state `fullscreen {on}`, persist `IntakeFullscreen` only if changed (:356-358); identical echo right after init (:~295).
- Duck/restore: `DuckMainWindow()` = plain MainWindow minimize (records prior state; NOT tray tuck; **no audio ducking exists** — the word is window-only); `RestoreMainWindow()` from `DisposeAll()` (the single close funnel).
- Watchdogs: 5s heartbeat watch, no-op unless ready && !exiting, **>20s silent → Recover** (:993-1004); heartbeat AND pong stamp (:305-308). Recovery ladder (:1018-1041): relaunch ONCE (`_relaunchedOnce`, `_recoveryWindowed=true`), second failure = clean give-up. ProcessFailed → Recover (:1015). Exit watchdog 1200ms → DisposeAll (:1045-1051), armed by CloseActive after `end-run {reason:"host"}` (:146-160) and by page `exit` (:317-320).
- Completion loop `OnQuizResult` (:373-508): XP `min(100, 25 + round(clamp(peakDepth,0,1)*50) + min(mantras,5)*5)` (:389-397); **pass spend HERE and nowhere else** (:406); `QuizSessionGenerator.GenerateSession` → unique path (suffix -2…-999 :515-528) → export (:421-427); punch `NotifyIntakeCompleted(session.Id)` (:459); 12s "Run it now" toast (:472-487); reply `session-drafted {ok,name,path}` (:496/:504); window STAYS OPEN.

### Bridge vocabulary (C#-side authoritative)

**OUT (6):** `init {protocol:1, config, ai}` (OnPageReady :241-292 — config: niche, caps{flashRate,flashOpacity,subDensity,duckDepth,bubbleRate,binauralDepth,bgIntensity,masterIntensity}=1.0 (:658-668), endless=false, steerValve=1.0, priorRun=null, m2Test, micEnabled=MicConsentGiven (:~264), media{gifs≤18, images≤18, bubbleSprite?, subliminals≤40?}|null (:723-781), subjectId 4-digit (:969-985), subliminals[{text,audio?}]|null (:846-918)); ai={serverBase proxy, authToken Patreon bearer, "" when logged out}); `fullscreen {on}` (:~295,:354); `end-run {reason}` (:~156); `session-drafted {ok,name,path}` (:496,:504); `loom-result {op:"save",ok,slug,error}` (:568,:574); `intake-save-image-result {ok,path,error∈too-big/bad-image/io-failed}` (:624).

**IN (12):** `ready`, `log {msg}` (ChaosWebViewHost :577-602), `heartbeat`, `pong`, `quiz-result {result:QuizRunResult}`, `boot-error {msg}` (→ teardown + MessageBox + BootFailedThisSession :633-650), `fullscreen-set {on}`, `exit`, `exit-done`, `intake-close` (jumpscare abort → DisposeAll, NO result, NO spend; :532-543), `loom-save {name,gifBase64,params,overwrite}` (:562-577 → the SHARED DtrhLoomStore), `intake-save-image {pngBase64 ≤12MB chars (:554), index clamp 1..99 (:609)}` → PNG 8-byte magic (:628-631) → `%APPDATA%/…/intake_spirals/intake-spiral-yyyyMMdd-HHmmss-NN.png` (:552,:610-612).

**ping/payload-state: C# emit sites REFUTED for intake** (grep-verified: `payload-state` emits only in DtrhHostService.cs:809,850 + DtrhHostOrchestrator.cs:401,407; `ping` only in a DTRH test; zero in ChaosWebViewHost/IntakeHostService). Page-side attested: web-shim.js:218-227 (payload-state video cover, ping→pong). **Authoring obligation resolves as TYPED** (vocabulary typed + pinned never-emitted-by-this-host).

### Pass state machine (`Services/Progression/IntakePassService.cs`, 320 lines)

- Week key `$"{ISOWeek.GetYear(local):D4}-W{ISOWeek.GetWeekOfYear(local):D2}"` (:71-72) — ISOWeek (Jan 1 can key to prior-year W52/W53: the New-Year guard), authority = the stored STRING, reset effectively Monday 00:00 local. `StartOfWeek` (:80-81), `NextPassLocal` (:84), `DaysUntilNextPass = Max(1, Ceiling)` (:87-93).
- FutureSkewTolerance 5min (:58): `SpentUtc > now+5min` → rolled-back → Spent (:104-110).
- State (:114-138): Premium (either provider) → Premium; !logged-in → NeedsLogin; rolled-back → Spent; spentWeek==current (Ordinal) → Spent; else Available. **Any exception → Spent (fail-closed :130-136).** CanStartIntake = Premium || Available (:144-151).
- Completion-spend ONLY `ConsumeForCompletedIntake` (:166-185): patron no-op; writes week+utc, `_spentThisSession`, PassStateChanged.
- Dual-provider refund (:232-251 hooks both TierChanged; :278-302 refund only when `_spentThisSession` && now-premium && week==current → clears; prior-session stamps never refunded).

### Punch card (`IntakePunchCardService.cs` 395 + `IntakePunchCardState.cs` 59)

- 8 holes (:53), credit ≥50% (:56), **UiEnabled=false static readonly (:62) — UI hidden, stamping continues silently** (greenfield parity = silent stamping, no painted card), MaxPendingDrafts 8 (:65), PendingDraftMaxAge 90d (:68).
- File `%LOCALAPPDATA%/…/intake_punchcard.json`; fields SchemaVersion=1, CardStartedUtc?, PunchedCount, PunchedUtc[], PendingDrafts[]{SessionId,DraftedUtc}, CompletedUtc?, PrizeClaimedUtc?.
- First hole FREE `EnsureCardStarted` (PunchedCount=1 :139-148). Holes 2-8: `NotifyIntakeCompleted(sessionId)` queues pending draft (no-op complete/blank/dup; evict oldest >8 :154-176); redeem on `NotifySessionProgress ≥50%` (:181-185) OR `NotifySessionCompleted` natural end (:189-193) → TryRedeem → Punch (immediate save, CompletedUtc on 8th, PunchCardCompleted once :212-236). 8th hole prize NOT granted client-side; `MarkPrizeClaimed` once (:215-249).
- 30s dirty-flush timer (:101-107,:345-363) + atomic .tmp-swap (:327-342); load ladder main → .tmp promote → default (:261-290); **load repairs (Sanitize :300-324, set dirty so the file heals)**: null lists→new; PunchedCount clamp 0..8; full card w/o CompletedUtc backfilled; blank/90d-old drafts removed; pending trimmed to 8 from front.

### Profiler (`Services/Quiz/IntakeProfiler.cs`, 276 lines — namespace `Services`)

Deterministic pure (no RNG/clock/IO). MinItems=3 (:114), MinHeat=2 (:117), AutonomyHotHeat=3 (:120), recovery band excluded (:123).
- Exclusions `IsScoreable` (:213-224): band=="recovery" (OrdinalIgnoreCase); IsTrick; IsFreeChoice; ChosenIndex<0; OptionCount<2; null/empty Tags; any tag ∈ DisqualifyingTags{trivia,colorpick,trick} (:136-139). StructuralTags{trivia,curious,colorpick,trick,mantra,mono} never satisfy an axis (:130-133,:255-268).
- Axis `ScoreAxis` (:232-253): usable records PromptHeat≥minHeat sharing ≥1 axis tag; endorse = Correct?1:0 (binary — index-lean measured+rejected :74-101); **A5 inverts**; axis=Σ(heat·endorse)/Σ(heat) clamped 0..1; n<3 or den≤0 → 0.5 UnderSampled.
- Tags: A1 Blankness {blank,erasure,sinking,dropping,stillness,calm,iq-lock,trance}; A2 Service {obedience,servitude,service,compliance,protocol,order,worship,property,hive,sync,surrender}; A3 Arousal {arousal,cockslut,denial,chastity,locked,keyholder,permission,discipline}; A4 Presentation {femme,dressing,pink,doll,uniform,good-girl,giggly,soft,trigger}; A5 Autonomy {confession,honesty,exposure} @heat≥3.

### Session drafting (`Services/Quiz/QuizSessionGenerator.cs`, 878 lines — WPF head copy)

`GenerateSession(run, content=null)` (:74-172): niche fallback "bambi" (:80); profiler (:83); difficulty `peakDepth ≤0.20 Easy, ≤0.45 Medium, ≤0.72 Hard, else Extreme` (:191-199); **A5 gate** !UnderSampled && ≥0.70 → step tier down one (:93-94,:178-187); scorePercent = TotalScore/MaxScore·100 else depth·100 (:96-98); affirmed mantras merged VERBATIM first (case-insensitive dedupe) into Subliminal/BouncingText/LockCard phrases (:104,:221-253); name `{Gentle|Guided|Intense|Deep} {Niche} Intake` (+ ` - {PrettyId(archetype)}` when revealed :107-146); Session: new Guid, 60min, Source=Custom, BonusXP per difficulty (:158-172). `ApplyRunShaping` (:269-372) deterministic, lean=axis-0.5 (0 when under-sampled), Nudge=Clamp(base+Round(delta,AwayFromZero),lo,hi): chase>0.15 → bubbles+intermittent+flash-hydra, flash rates ×(1+0.25·chase) ≤180; A1 → MindWipe ±2 (1..4), SubOpacity ±20 (20..100), SpiralOpacity ±8/End ±16 (0..50); A2 → LockCard ON ≥0.75 (freq 1, start 10min), LockCardFreq ±2 (1..4), VideosPerHour ±2 (1..4); A3 → FlashPerHour ±30/End ±60 (1..180), FlashOpacity ±15/End ±25 (10..100); A4 → SubPerMin ±2 (1..6), PinkFilterEnd ±20 (0..100); A5≥0.70 → LockCard OFF. `EnsureRising` End≥Start (:390-397). Easy baseline FlashPerHour 12→30, Opacity 25→45, Hydra off (:425-440).
- **Residual gap (agent turn limit):** QuizSessionGenerator.cs:441-878 unread (Medium/Hard/Extreme tier tables, per-niche fallback content, BuildPhases). Worker reads these against main in Step 2 before porting the tier tables.

### Niche (`Services/Quiz/IntakeNiche.cs`, 85)

All={bambi,sissy,drone,circe} (:23), Fallback=bambi (:25). Current() (:38-59): built-in mod ids (Dronification→drone, SissyHypno→sissy, Locked→circe) → manifest tags (drone/sissy/circe direct; locked AND chastity → circe) → legacy ContentMode.SissyHypno→sissy → bambi; exception → bambi. Host clamp: banks/{niche}.json missing → bambi, one warning/session (:674-702). **Greenfield: all 4 banks present on disk** (verified ls); no mod system → typed seam default bambi.

### Serving origins

WPF: `ccp.game`→{exe}/Resources/web (Deny same-origin), `ccp.assets`→user assets (Allow CORS-clean), `ccp.content`→content pack mirror (Allow) (:88-100). Page origin ccp.game; start `/intake/index.html`. `CCP_CONTENT_READY` injected pre-script when pack mapped (ChaosWebViewHost.cs:514-532). **Greenfield: no pack pipeline (Decisions-needed row) → CCP_CONTENT_READY never injected → audioSrc.js keeps everything on the page origin; missing VO → the page's own silent/text-only fallback (degraded contract).**

### Payload-side (this worktree, verified by worker)

- web-shim.js: PROTOCOL=1; in {ready:94, log:89, quiz-result:188-190, pong:222, heartbeat ~2s rAF:224-227, boot-error:231, intake-close:242}; host→page {init:107 mapped:122-141, ping:222, payload-state:218-220}; fullscreen-set ui/fullscreen.js:102.
- boot.js:2119 loom-save `{name:intake-<base36>-<idx>, params:q, gifBase64, overwrite:false}` expects `loom-result` (ok/slug/error; cap-reached/too-big phrasings); boot.js:2142 intake-save-image expects intake-save-image-result {ok,path,error}.
- index.html:17-22: importmap `../dtrh/vendor/three/...` (needs the dtrh tree a SIBLING route under the page origin).
- render/audio.js:222-229: chimes `../../dtrh/assets/bubbles/sfx/chime{1..3}.mp3` via import.meta.url; audioUrl() no-ops without CCP_CONTENT_READY.
- banks/: bambi.json circe.json drone.json sissy.json all present.

### Landed machinery consumed (verified by worker reads)

- `DtrhProtocol` (328 lines): the tolerance pattern (Parsed/UnknownType/ForwardVersion/Malformed + Handled/Deferred classification) — IntakeProtocol mirrors it.
- `LoopbackServer` (548): §4 contract — GET-only, /dtrh/* overlay-first over payload, /bridge/<token>/inbox, media origin /media/* + /spirals/* + /umedia/* with CORS scoped to page origin, Range 206/416, MIME allowlist + 415, traversal 403, route-class logging. **Routes hardcode the `/dtrh/` prefix; overlayRoot/payloadRoot/mediaRoot/spiralsRoot/userMediaRoot are constructor parameters.**
- `DtrhWatchdog` + `DtrhExitFlow` (241): generic state machines — runActive=false yields the 20s-only intake discipline; ExitDoneWait=1200ms exact parity.
- `DtrhLoom` (231) + `DtrhLoomDispatch` (102): shared store `<dataDir>/Spirals`, slug/cap/magic discipline, one-write-path binding (SP-049 consult).
- `DtrhHostWindow` (1256) + `DtrhLaunchCoordinator` (177): capability-driven surface (embedded/dialog/unsupported), ready-flush boot contract, synthetic-dispatch/inbox SendToPage, relaunch-once coordinator pattern.
- `PersistenceStore<T>` (508): schema/journal/quarantine/newer-schema typed loads, Mutate/dirty/Save/Flush, AtomicWriteHooks test seam; owners via OperationRegistry.OwnerFor.
- DemoSettings: declares itself "NOT a feature model" (§1 rule 4) → intake settings keys do NOT go there.

## Step 1 — design (pre-consult)

**Layout (all new under `client/src/CcpClient.Desktop/Features/Intake/`):**

| File | Content |
|---|---|
| `IntakeProtocol.cs` | Full typed vocabulary: 6 out builders (init/fullscreen/end-run/session-drafted/loom-result/intake-save-image-result) + 12 in records + tolerance outcomes (Parsed/UnknownType/ForwardVersion/Malformed) + classification. **ping + payload-state TYPED per the authoring obligation**: host→page builders exist (page-attested web-shim.js:218-227) with a pinned classification `PageAttestedNeverEmitted` (archaeology refuted every C# emit site) — the vocabulary is typed; the emission absence is pinned honest. |
| `IntakeHostWindow.axaml(.cs)` | Capability-driven surface (embedded WebView2 / NativeWebDialog / honest unsupported) mirroring DtrhHostWindow: ready-flush boot contract, SendToPage (synthetic dispatch / inbox), fullscreen persist (echo ACTUAL + persist-if-changed), duck/restore of the dashboard window (minimize only — WPF has NO audio ducking), watchdog wiring (DtrhWatchdog reused: runActive=false → 20s-only), DtrhExitFlow reused (1200ms), CloseForRecovery. |
| `IntakeLaunchCoordinator.cs` | Owns the watchdog across relaunch; RecoveryRequested → CloseForRecovery + recreate window ONCE windowed (`_recoveryWindowed` parity); exhausted → honest close. |
| `IntakeParticipant.cs` | Owns the intake LoopbackServer + inbox + bridge token (host-local construction, DtrhHostWindow-bark precedent — CompositionRoot is out of scope; app-wide lift = future row). |
| `IntakeServingRoots.cs` | **Serving design (the overlay-first borrow):** the intake server is a LoopbackServer UNMODIFIED with payloadRoot = the dtrh tree (`payload/dtrh`, output-copied by the existing glob) and overlayRoot = the intake tree → overlay-first means every intake page URL hits the intake tree and every borrowed dtrh asset (`vendor/three/*`, `assets/bubbles/sfx/chime*.mp3`) falls through to the dtrh tree. URL math verified: page at `/dtrh/index.html`; `../dtrh/vendor/three/x` → `/dtrh/vendor/three/x` → payload hit; `../../dtrh/assets/...` from `/dtrh/render/audio.js` → `/dtrh/assets/...` → payload hit; intake's own `assets/vo/*` shadow via overlay. Intake-tree resolution probe (SP-048 discipline): (1) `<BaseDir>/payload/intake` (published-artifact location — a future row adds the copy glob; the csproj is out of THIS packet's File Scope), (2) walk-up from BaseDir for `ConditioningControlPanel/Resources/web/intake/index.html` (dev/test worktree), (3) typed Missing → window shows the honest surface, never a substitute. Probe result logged (root + state). |
| `IntakeProfileLock.cs` | Intake-scoped stale-profile recovery (browser_data_intake parity → `<dataDir>/intake-webview2`); same 0x800700AA class (the relaunch path WILL hit it — SP-023 surprise #7). |
| `IntakeSettingsDocument.cs` + store | **Settings keys on SP-005 machinery** (dedicated document — DemoSettings is a self-declared non-feature model and Persistence/ is out of scope): `intake_settings.json` {Fullscreen, PassSpentWeek, PassSpentUtc, NudgeDismissedWeek, NudgeEnabled} schema v1 + ExtensionData. |
| `IntakePassService.cs` | The state machine placeholder-shaped: ISO-week key via `System.Globalization.ISOWeek` (D4/W02, New-Year guard), StartOfWeek/NextPass/DaysUntil, >5min future-stamp rollback → Spent, fail-closed on exception, completion-spend ONLY, PassStateChanged. **Entitlement = typed seam** `IIntakeEntitlementSource { bool IsPremium; bool? IsLoggedIn; event TierChanged }`; host default = no providers (IsPremium=false, IsLoggedIn=null→not-applicable maps to Available with a logged named-limit line — the intake RUNS per the degraded contract; NeedsLogin unreachable until the entitlement rows land); refund method ported against the seam (fires only on seam TierChanged; unit-tested with a fake seam). |
| `IntakePunchCard.cs` + document | `PersistenceStore<IntakePunchCardDocument>` at `<dataDir>/intake_punchcard.json` (own named owner "IntakePunchCard"): 8 holes, first-hole-free, pending drafts (8 max, 90d), redeem ≥50%/natural end = typed seams no caller invokes yet (punches pend forever silently — the degraded contract verbatim), 8th hole sets CompletedUtc, prize never granted client-side, MarkPrizeClaimed once. Load repairs ported as a pure `Repair` (null lists, clamp 0..8, CompletedUtc backfill, stale/blank draft eviction, front-trim) run post-load (SP-005 typed outcomes replace WPF's .tmp ladder; dirty→heal on next save). UiEnabled=false parity = silent stamping only, no UI built. |
| `IntakeProfiler.cs` | The pure function ported exact (axes/tags/exclusions/thresholds/neutral-0.5 per archaeology §Profiler) + QuizRunResult model (shape from the payload's emit: boot.js quiz-result). |
| `IntakeSessionDrafter.cs` | GenerateSession port: difficulty bands, A5 tier-step-down, XP formula, name synthesis, mantra verbatim merge, ApplyRunShaping nudges + Easy baseline (Step 2 reads the unread :441-878 tier tables against main first). **Degraded contract encoded:** the drafted document carries `"runnable": false` + `runnableReason: "no-session-engine (degraded-delivery contract)"`; sink writes `<dataDir>/intake/drafted_sessions/{name}.session.json` with collision suffixes (-2…-999); session-drafted reply {ok,name,path}. XP grant = typed seam (no greenfield XP store — logged fact, named limit). |
| `IntakeSubjectId.cs` | `intake_subject.txt`: mint 0001-9999 once, atomic .tmp-swap, local-only (never in any bridge message out — it rides init TO THE PAGE ONLY, presence+shape logging). |
| `IntakeSaveImageSink.cs` | intake-save-image: 12MB base64-char ceiling, PNG 8-byte magic, index clamp 1..99, host-built filename `intake-spiral-yyyyMMdd-HHmmss-NN.png` under `<dataDir>/intake_spirals/`, atomic write, reply {ok,path,error}. |
| `IntakeNiche.cs` | Typed seam (no mod system) → default bambi; bank-clamp against the SERVED intake tree's banks/{niche}.json (all 4 present; missing → bambi + one warning/session). |
| `IntakeHarness.cs` (HARNESS-ONLY) | `--intake-drive` scripted steps through the REAL parse+dispatch (fx-drive precedent): quiz-result (drafted+spend+stamp), intake-close abort (NO spend), fullscreen-set round trip; `--intake-kill-renderers` relaunch-once evidence. |

**Loom-share (in-scope `DtrhLoom*.cs` glue):** `DtrhLoomDispatch.TryHandle` currently takes `DtrhProtocol.DtrhPageMessage`. Justified minimal edit: extract a primitives overload `TryHandleSave(name, overwrite, raw)` used by BOTH the existing DTRH path and the intake window (one write path — the SP-049 consult binding), so intake loom-save writes the SHARED `<dataDir>/Spirals` store (never a second root) and replies loom-result {op:"save",ok,slug,error}.

**init provisions (greenfield honest shape):** niche = seam default bambi (bank-clamped); caps all 1.0; endless=false; steerValve=1.0; priorRun=null; m2Test=false; **micEnabled=false (typed consent seam — no consent subsystem; the privacy-honest default; recorded)**; media = 18 random gifs + 18 stills from `<dataDir>/assets/images` as intake-server `/umedia/` URLs (+bubbleSprite null; empty pool → media:null, page renders shipped art); subjectId from intake_subject.txt; subliminals=null (typed seam — no subliminal subsystem); ai={serverBase:proxy, authToken:""} — empty token → the page's own deterministic local stub, NO network (the privacy boundary verbatim).

**Privacy boundaries (never widened):** no bearer transmission by the host (token empty, and even a future real token rides init to the page only); subject id local fiction; media/subliminal/whisper hand-off = presence+shape logging only; mic consent-gated OFF.

**Launch glue (flagged scope expansion, SP-049 precedent a42ebdf4):** Program.cs + App.axaml.cs gain `--intake-demo` / `--intake-drive` / `--intake-kill-renderers` / `--intake-auto-close` demonstrator wiring (the only path to the PROMPT-mandated avalonia-live headed evidence; HeadlessTests cannot run WebView2 headed). Recorded in STATUS Discoveries (D3) for orchestrator reconciliation at land.

**Tests (client/tests/CcpClient.Tests/Intake*):** protocol parse/classify/tolerance matrix (incl. ping/payload-state typing pins + malformed/forward-version/unknown); pass-week arithmetic (ISO New-Year guard cases, rollback guard, fail-closed, spend-only-on-completion, refund via fake seam); punch card (first-hole-free, pending/redeem seams, repairs matrix, 8th-hole CompletedUtc, prize-claim-once); profiler matrix vs WPF-pinned cases (axis math, exclusions, under-sampled 0.5, A5 inversion + heat≥3); drafter (difficulty bands, A5 step-down, XP clamps, mantra merge, never-runnable marker, collision suffixes); stores round-trip on SP-005 machinery (settings document, punch document, subject id); serving roots probe (present/dev-tree/missing typed); save-image sink (magic/ceiling/clamp/atomic); loom-share (shared-root proof: intake save lands in the same Spirals dir the DTRH loom lists).

**Linux named limit:** WSL zero distros — never faked; the dialog surface path compiles and is code-complete, evidence is Windows-only.

## Step 1 — pre-approach consult

**Mode:** solo (T-7: council unproven — PROMPT Do-NOT). **Requested route:** Opus 5 main (2026-08-04 rewire). **Actual answering model:** NOT surfaced by the consult tool response (no model identity header — recorded honestly, same provenance discipline as SP-022…027, SP-049…053). **Two calls:** call 1's verdict TRUNCATED mid-ruling-7 (the SP-027 truncation class — recorded, never silently stitched); call 2 (terse completion) first attempt failed with a connection error, retry succeeded and completed rulings 7-8. **Verdict: design APPROVED with corrections — all adopted:**

**Call 1 (rulings 1-6):**
1. **Serving trick SOUND, with two sweep obligations (both DISCHARGED by worker before call 2):** (a) MIME allowlist sweep — intake tree extensions = mp3/png/js/json/md/html/css; every page-fetched type is inside the §4.4 allowlist (md is never fetched) → no 415 break; (b) absolute-path sweep — no root-absolute page-origin paths in the intake tree (`core/ai.js:21 '/intake/ai'` is the REMOTE proxy path; `boot.js:89` banks fetch is `./banks/${niche}.json` relative) → the overlay-first borrow is sweep-clean.
2. **Intake bytes: take the csproj glob.** The walk-up is a Z:\-class smell (runtime dependency on the dev worktree; published artifact could never serve the page). The csproj Content glob (mirroring SP-023's dtrh glob, intake tree → `payload/intake/`) bundles with the Program.cs glue as ONE flagged scope expansion — more coherent than entry-point edits + a runtime hack. Resolution simplifies to the SP-048 probe discipline over `payload/intake` (Present/Missing/Incomplete → typed honest surface; no walk-up).
3. **Scope glue: proceed + flag.** Minimal demonstrator shape (mirrors --loom-demo: no default-behavior change when flags absent); enumerate every out-of-scope hunk in this record; Step-4's "only File Scope paths" criterion is consciously deviated-by-name (Program.cs, App.axaml.cs, CcpClient.Desktop.csproj — the launch+serving glue bundle), never silently.
4. **Settings home: correct reading.** Dedicated `PersistenceStore<IntakeSettingsDocument>` (intake_settings.json); unique OperationRegistry owner name; record the deviation (WPF keeps the keys in settings.json — the greenfield split folds into a future settings row).
5. **Entitlement seam: state+reason TYPED.** `IntakePassState.Available` carries a reason (`AvailableNoEntitlementProvider`); the NeedsLogin branch is unreachable-this-build (no login provider) — recorded, exercised only via fake-seam unit tests (premium / needs-login / spent / rollback / exception → Spent).
6. **ping/payload-state: typed vocabulary + pinned classification** (`PageAttestedNeverEmitted` — archaeology refuted every C# emit site) — NOT orphaned public builders that invite future misuse.

**Call 2 (rulings 7-8):**
7. **Reuse APPROVED with three corrections:** **7a (BUG, adopted):** `intake-close` does NOT route through DtrhExitFlow — WPF abort is immediate DisposeAll (:532-543), no end-run, no bounded wait; the intake window gets an `_abortClosing` latch (the `_recoveryClosing` shape) skipping the graceful branch. DtrhExitFlow stays for exit/exit-done/window-X only. **7b (BUG, adopted):** the shared `TryHandleSave(name, overwrite, raw)` overload returns the Result and sends `loom-result` ONLY — `PostList()` stays in the DTRH caller (intake's 6-out table has NO loom-list; posting one would invent a WPF-never-sent message). The existing DTRH case refactors to CALL the overload (never duplicate the body — one write path is a fact, not a claim). **7c (drift net, adopted):** `IntakeWatchdogContractTests` pin HubSilenceLimit==20s (WPF :1000-1004) + ExitDoneWait==1200ms (:1045-1051) + the single runActive:false call site — a future DTRH retune must not silently retune intake.
8. **Degraded encodings APPROVED with four framing fixes (all adopted):** `authToken=""` is genuine PARITY (WPF's logged-out branch :272-279; the page's local stub is WPF's own no-network fallback — the packet's strongest honesty point, never filed as "stubbed"); `micEnabled=false` is a TIGHTENING by absence ("no consent subsystem → consent-gated OFF, tighter than WPF", not "mic gate ported"); XP is **computed, not granted** (no XP store — typed seam; any test asserting "granted" is the fake); punches pend silently **until the 90-day/8-entry eviction the WPF card already applies** (MaxPendingDrafts/PendingDraftMaxAge ported — "forever" would be unbounded growth, a DIFFERENT behavior); the two bambi fallbacks stay distinct in the record (seam default vs bank clamp — the untested clamp must not read as exercised).

**Evidence obligations (binding for Step 3):** (a) the chime borrow is proven only by a **200 on `/dtrh/assets/bubbles/sfx/chime1.mp3`** in the loopback route-class log — a 404 collapses the overlay-first claim; (b) "abort = NO spend" is proven only by pass-store **file content before AND after** the intake-close run.

**Adopted design changes vs the pre-consult table:** csproj intake glob joins the flagged glue bundle (walk-up deleted); IntakeSettingsDocument/owner naming per ruling 4; pass state carries typed reason (ruling 5); ping/payload-state = typed constants + pinned classification (ruling 6); `_abortClosing` latch (7a); loom overload shape per 7b; watchdog contract tests (7c); framing per ruling 8; evidence obligations (a)+(b) join Step 3.

## Step 2 — implementation (build 0W/0E; 795/795 + 33/33 first-run green, floor 683/33)

**Files (all new under `client/src/CcpClient.Desktop/Features/Intake/` unless noted):**
`IntakeQuizRun.cs` (the QuizRunResult model; chosenIndex=-1 default load-bearing), `IntakeProfiler.cs` (full-source port — the WPF file was read end-to-end for this port), `IntakeDraft.cs` (drafting core, scoped below), `IntakeDraftSink.cs` (collision suffixes -2…-999, atomic), `IntakePassService.cs` (+`IntakeSettingsDocument.cs`), `IntakePunchCard.cs` (+document + pure Repair), `IntakeProtocol.cs` (12 in / 6 out + tolerance + emission pins), `IntakeServingRoots.cs` (SP-048 probe), `IntakeParticipant.cs` (second §4 LoopbackServer, overlay-first borrow), `IntakeSubjectId.cs`, `IntakeSaveImageSink.cs`, `IntakeNiche.cs` (seam default + caller-logged bank clamp — the static one-warning latch was DELETED during implementation: it pollutes test processes; the clamp is logged once per boot by the caller), `IntakeMediaManifest.cs` (18+18 sampling), `IntakeHostWindow.axaml(.cs)` (capability-driven embedded surface; dialog NOT ADMITTED for intake — see below), `IntakeLaunchCoordinator.cs` (+`IntakeHostContext.cs`), `IntakeHarness.cs` (HARNESS-ONLY drive).
**In-scope DtrhLoom glue (justification, required by File Scope):** `DtrhLoomDispatch` gained `HandleSave(name, overwrite, raw)` — the ONE write path both hosts share (SP-049 + consult 7b bindings); the existing DTRH `TryHandle` LoomSave case was refactored to CALL it (never a duplicated body) and keeps its PostList; the intake host calls it directly (loom-result ONLY — the intake 6-out table has no loom-list).

**Advisor Correction 1 (adopted — the drafter scope decision, as a named limit):** WPF's `BuildSettings` full tier tables (4 tiers × ~35 knobs), `BuildPhases`, `GetCategoryIcon`, `GetFallbackContent` per-niche phrase banks, `BonusXP`, and the BrainDrain ramp pair are **NOT ported**. The row's verbatim degraded contract: "the drafted session is never runnable (no session engine — punches pend forever, silently)" — there is no SessionSettings/SessionEngine/SessionPhase in greenfield, and drafting phases/icons/phrase banks into a never-runnable document would invent a schema no consumer validates (and would drag the WITHHELD Delta-1 BrainDrain fields into the artifact). The drafted document carries: the profiler axes, the difficulty (bands + A5 gate), the #614 naming, the verbatim mantra merge, the axis→knob-table knobs via `IntakeDraftKnobs` (exactly the knobs the table names + the two enabled-gates the nudges read, with per-tier baselines), XP computed (never granted), and `runnable:false` + the typed reason. The unported remainder belongs to the unfiled session-engine + content rows.

**Advisor Correction 2 (verified + pinned):** the intake tree sends **loom-save ONLY** (grep over the intake tree: boot.js:2119; zero loom-delete/loom-reveal/loom-list hits) — those types classify UnknownType in IntakeProtocolTests.

**Platform honesty (recorded):** the intake page's web-shim speaks WebView2 chrome.webview ONLY (web-shim.js:28-52 — no §3.3 inbox transport; the DTRH dialog path works because the overlay bridge.js polyfills it, and the intake tree has no such shadow). The NativeWebDialog path is therefore NOT ADMITTED for intake: on non-embedded surfaces the window shows the honest unsupported surface naming the exact gap (a standalone page would silently lose quiz results). Linux = the packet's named limit (WSL zero distros, never faked); Windows WebView2 = the evidenced surface.

**Degraded-contract encodings (ruling 8 framings):** `authToken=""` logged as WPF logged-out PARITY (the page's local stub, no network); `micEnabled=false` logged as consent-gated OFF (a TIGHTENING — no consent subsystem); XP "computed, not granted"; punch redemption seams present but unwired (punches pend silently until the ported 90-day/8-entry eviction); VO absence → the page's own silent fallback (CCP_CONTENT_READY never injected — no pack pipeline); niche = seam default bambi DISTINCT from the bank clamp (the clamp is real and unit-tested; the seam default is the recorded one).

**Flagged scope expansions (the launch+serving glue bundle, consult ruling 3; Step-4's "only File Scope paths" criterion is consciously deviated-by-name — orchestrator reconciles at land):**
1. `client/src/CcpClient.Desktop/CcpClient.Desktop.csproj` — the intake Content glob (the SP-023 linked-glob convention; serving is impossible without the bytes at output; the consult's ruling-2 bundle).
2. `client/src/CcpClient.Desktop/Program.cs` + `App.axaml.cs` — `--intake-demo` / `--intake-drive` / `--intake-kill-renderers` / `--intake-auto-close` demonstrator wiring (the --loom-demo precedent a42ebdf4; the ONLY path to the PROMPT-mandated avalonia-live headed evidence; no default-behavior change when flags absent).
3. `client/src/CcpClient.Desktop/Assets/assets.manifest.json` — 2137 intake copied entries (the SP-009 manifest discipline follows the glob; generated by script in the dtrh entry shape, uniform SP-054 provenance).
4. `client/tests/CcpClient.Tests/AssetManifestTests.cs` — the copied-count pin moved 1544 → 3681 + an intake pin assertion (the count pin follows the glob; no assertion weakened, one strengthened).

**Test counts:** 112 new Intake* tests across 6 files (protocol/loom-share, profiler, draft, pass, punch, support incl. the 7c watchdog pins) + serving-contract tests with a REAL LoopbackServer (the DtrhLoomTests ServerHarness precedent — real HttpListener, real 200/403/404/405/415 assertions incl. THE chime-borrow 200 line).

