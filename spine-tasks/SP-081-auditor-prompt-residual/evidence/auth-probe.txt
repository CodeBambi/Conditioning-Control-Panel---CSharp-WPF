SP-081 Step 1 stop condition: the blind auditor cannot be invoked on this machine.

The packet requires a fresh, independent `claude` process. `claude` IS on PATH and IS
invocable; it refuses to authenticate. Four probes, all on 2026-08-15, all exit 1 with the
same single line of stdout and an empty stderr:

  1. The faithful audit invocation (Get-AuditArgs reproduced verbatim, prompt on stdin,
     -WorkingDirectory C:\ccp-sp081\induced):
       C:\Users\Micha\.local\bin\claude.exe -p --safe-mode --model sonnet \
         --permission-mode bypassPermissions --no-session-persistence \
         --tools Bash,Read,Grep,Glob < <worktree>/client/tools/port-audit-prompt.md
     -> exit 1, 0.1 minutes, stdout: "Not logged in - Please run /login"

  2. A bare invocation with NONE of the audit flags, to prove the refusal is not caused by
     --safe-mode / --no-session-persistence / --tools:
       C:\Users\Micha\.local\bin\claude.exe -p --model sonnet < probe.txt
     -> exit 1, stdout: "Not logged in - Please run /login"

  3. The second install on PATH (npm global), in case it carries different credentials:
       C:\Users\Micha\AppData\Roaming\npm\claude.cmd -p --model sonnet < probe.txt
     -> exit 1, stdout: "Not logged in - Please run /login"

  4. `claude --version` -> 2.1.232 (Claude Code). The binary runs; only auth fails.

Why: this machine holds no credentials an independently spawned claude.exe can read.
  - C:\Users\Micha\.claude\.credentials.json does not exist (directory listed; absent).
  - No ANTHROPIC_API_KEY and no CLAUDE_CODE_OAUTH_TOKEN in the environment.
  - `cmdkey /list` has no Claude or Anthropic entry.
  - The executing session is a host-attached child: CLAUDE_CODE_CHILD_SESSION=1,
    CLAUDE_CODE_SDK_HAS_HOST_AUTH_REFRESH=1, CLAUDE_CODE_SDK_HAS_OAUTH_REFRESH=1. The
    Claude Code host holds the OAuth material and injects it into its own SDK child. It is
    not on a path a separately spawned process can reach.

No login was attempted. `/login` is interactive, and entering credentials is outside what
this executor may do.

Consequence: packet Step 1 says "If it is not [on PATH], or if the invocation in Step 3
cannot be made faithfully, STOP and report. Do not simulate the auditor, and do not read the
prompt and render your own verdict." Neither audit run was performed. No verdict of any kind
was manufactured. The exact manual gate is named in record.md.
