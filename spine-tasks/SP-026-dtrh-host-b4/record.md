# SP-026 record — DTRH host slice b4: progression/payout, Loom, user/mod media

**Task:** spine-tasks/SP-026-dtrh-host-b4 · **Review Level:** 2 · **Binding spec:** `client/docs/dtrh-admission.md` §7 (b4 row) + §4 (loopback contract)
**Engine-review presence (T-2):** recorded per `spine_review_step` call below.

---

## Engine-review log (T-2)

| Step | Type | Result | Artifact |
|------|------|--------|----------|
| 1 | plan | **SKIPPED BY DESIGN** (nested reviewer spawn blocked in worker session; `skipped=true, spawnFailed=false` — engine runs reviews after `.DONE`, SP-195) | `.reviews/1-20260722T024313.md` |
| 2 | plan | SKIPPED BY DESIGN (same) | `.reviews/2-20260722T032045.md` |
| 3 | plan | SKIPPED BY DESIGN (same) | `.reviews/3-20260722T034114.md` |
| 4 | plan | SKIPPED BY DESIGN (same) | `.reviews/4-20260722T044506.md` |

---

## Step 5 — verification (final tree, post consult fixes)

- **Windows:** `dotnet build client/CcpClient.sln -c Debug -t:Rebuild` **0W/0E** (warnings measured on Rebuild per the xUnit1051 lesson); CcpClient.Tests **366/366**; CcpClient.HeadlessTests **29/29** (≥ the 313/29 b3 floor).
- **WSL2 (`~/ccp-sp026`, synced final tree):** sln **0W/0E**; **366/366 + 29/29**; Rebuild 0 warnings.
- `git diff --check` clean; `git status --short` = File Scope paths only (+ the documented 3-file wiring amendment: Program.cs / App.axaml.cs / Lifecycle/CompositionRoot.cs — per-file necessity in record Step 2 + STATUS + board row, SP-023 norm).

---

## Step 1 — meta/payout/Loom/media archaeology + design + pre-approach consult

### WPF archaeology (READ-ONLY, `File.cs:line`; verified against the worktree tree)

**Meta-command ops** (`DtrhMetaBridge.Handle`, DtrhMetaBridge.cs:89-397 — 23 ops; shared validators `RequireId` id ≤128 non-blank :458-462, `Cost` = max(0, cost) :464; applied → `Bump()` (rev++ + meta snapshot broadcast :412-416) + 500ms debounce-save :418-425; rejected → debug log only, NO per-op error reply :392-393; exceptions → warning, not-applied :386-389):

| op (line) | semantics |
|---|---|
| purchase-upgrade :97-109 | `Sparks >= cost` && not owned → `Sparks -= cost`, add; `extreme_tier` also sets `ExtremeUnlocked` :104. Client-supplied cost (trust = integrity, not anti-cheat). |
| toggle-upgrade :110-114 | `on` default true; on → `DisabledUpgrades.Remove`, off → `Add`; applied = set changed. |
| purchase-dial :116-143 | skip if owned; rank gate: `hydra`/`glitchTimer` need RankIndex ≥ Entranced(3) :126; `Gold >= cost` → pay+add :128-132; else if `!GiftGiven && PurchasedDials.Count == 0` → HER GIFT: GiftGiven=true, Gold=0, add, bark (non-test) :133-138. |
| set-lifetime-boon :144-162 | two halves: `level` present → applies only `level == cur+1 && Sparks >= cost` (climb-only paid step); `active` present → `ActiveLifetimeBoons.Add/Remove`. |
| equip-boon :164-168 | `EquippedStartBoon = null` if blank else raw id; always applied. |
| add-gold :170-174 | `0 < amount <= 100000` → `Gold += amount`. |
| spend-gold :176-180 | `amount > 0 && Gold >= amount` → `Gold -= amount`. |
| material-add :182-194 | id ∈ 6 materials (ChaosCraftingIds.cs:18-21); amount 1..30; `Materials[id] += amount`. |
| craft :195-228 | id ∈ 21 recipes (ChaosCraftingIds.cs:24-30); cost JObject: every entry whitelisted material, 1..9 per cell, 1..9 total cells :207-214; holdings cover :216-217 → decrement, `DiscoveredRecipes.Add`, `CraftedItems[id] += 1` :219-223. |
| consume-crafted :229-240 | id ∈ {slippery_slope, rubber_up, sugar_cube} (ChaosCraftingIds.cs:34-37); held ≥1; held==1 → remove key else decrement. |
| pin-boon :241-251 | requires owning `the_padlock`; id >64 rejected; blank → unpin (null). |
| set-denial :252-262 | arming requires owning `the_cage`; disarm free; no-op when unchanged. Two-way. |
| buy-consumable-slot :263-271 | `Sparks >= cost && ConsumableSlots < 5` (MAX_CONSUMABLE_SLOTS, ChaosUpgrades.cs:381) → pay, ++. |
| bench-buy :272-285 | id ∈ {start_mantra, diary, stats_panel} (BenchIds, ChaosRevealService.cs:31-40); not owned && `Gold >= cost` → pay, add. |
| first-time :286-300 | id ∈ first_taste:5/first_snap:10/first_whisper:10/first_yes:15/first_play:15 (ChaosLessons.cs:152-163); once-ever set; `Sparks += amount`; bark (non-test). |
| lesson-progress :301-309 | `value > cur` → set (climb-only); `complete==true` → `LessonsComplete.Add`. |
| set-flag :311-318 | camelCase key → any public writable bool member; ONE-WAY false→true only. |
| add-to-set :320-325 | camelCase set → any HashSet<string> member; `set.Add(id)`. |
| remove-from-set :327-333 | ONLY `pendingReveals`; everything else rejected. |
| set-num :335-344 | `lastRankSeen`: `value > cur && value < 64`; `tutorialStage`: `value > cur && value <= 16`. Climb-only. |
| map-set :346-352 | ONLY `narrativeCooldownEnds`; `map[id] = value` (epoch ms). |
| add-channel-seconds :354-358 | `0 < seconds < 36000`; `TotalChannelSeconds += sec`. |
| reset-onboarding :360-378 | re-arm 21 bools false; `DiscoveredCodexIds.RemoveWhere(!"boon:")`; BubbleHintsLearned clear; Seen/PendingReveals remove "dollhouse"; TutorialStage=0; cheshire:-prefixed seen-lines + cooldowns purged; `ForceScriptedRun = true`. Always applied. Rewards kept. |
| unknown :380-382 | warning log, ignored. |

**Meta message** (`SnapshotMessage` :58-64): `{type:'meta', rev, state(camelCase FULL ChaosMetaState), testMode}`; posts at ready-seed (DtrhHostService.cs:198), every applied op, `Rebroadcast()` :399 after C#-side mutations (award banking, reveal sync, ForceScriptedRun consume). Test mode (`--dtrh-m2test`): deep clone :40-49; ops mutate the clone; saves → `chaos_meta.test.json` :427-444 (real save never touched).

**ChaosMetaState fields** (ChaosMetaState.cs; additive-only, neutral defaults :8-9): SchemaVersion=3 (store-owned in greenfield), Sparks, PurchasedUpgrades, PurchasedDials, DisabledUpgrades, ExtremeUnlocked, EquippedStartBoon, DiscoveredCodexIds, LifetimeBoonLevels, ActiveLifetimeBoons, 21 seen-once bools (SeenDefuseTutorial, SeenBarkDefuseFirst/NoFocus/Release, SeenBarkClickDetonate, SeenEcho, SeenChaperone, SeenTease, SeenBound, SeenBrittle, SeenBraindrain, SeenIntroGuide, SeenDuoDemo, SeenSkipDebut, SeenGoldFirst, SeenDollhouse, SeenFirstSin, SeenFocusTip, SeenRippleTeach, SeenHeatTeach, SeenWarrenWelcome, SeenFirstReturn, SeenBoudoirIntro), Gold, ToyPockets/AccessoryPockets (RETIRED do-not-read :62-63 — NOT ported), ConsumableSlots=1 (load-clamped ≥1, ChaosMetaStore.cs:71), BenchPurchases, GiftGiven, LessonProgress, LessonsComplete, PendingReveals, SeenReveals, FirstTimesAwarded, BubbleHintsLearned, LastRankSeen, TutorialStage, SeenNarrativeLines, NarrativeCooldownEnds, Materials, DiscoveredRecipes, CraftedItems, PinnedBoon, DenialArmed, PaperwallSketches, RunsCompleted, BestScore, BestCombo, TotalDefused, TotalRunSeconds, TotalChannelSeconds. No xp/level fields — XP goes to App.Progression; rank derived from RunsCompleted.

**Payout** (`OnRunEnded` DtrhHostService.cs:510-613): clamps — score :516, durationSec `max(1, ??60)` :517, elapsedSec clamp 0..2×duration :518, diffMult/sparkGainMult clamp 0.5..5.0 :519-520, diff default "Gentle" :521. XP: `durMin=durationSec/60; capBase=250*durMin*diffMult; baseXp=min(score,capBase); skillMult=App.SkillTree?.GetTotalXpMultiplier() ?? 1.0; finalXp=baseXp*skillMult` :523-527. `previousBest` = 0 test-mode else BestScore read BEFORE banking :529. Sparks via `_meta.AwardRun` :531-544 forwarding trickleDrops/dripFeedMaxed/bestCombo/defused/elapsedSec. Non-test block :546-573: AddXP(**baseXp**) :549, bubblesPopped credit :555-558, RevealService.Sync :561, rank-up `ChaosRanks.For(RunsCompleted)` > LastRankSeen AFTER banking :563-566, bark :568, sentinel clear :569, Rebroadcast :572. Session telemetry → DtrhSessionStatsStore :577-596. Reply `{type:'payout-result', baseXp, skillMult, finalXp, sparksEarned, previousBest, rankUp(enum-name-string|null), dryRun=testMode}` :598-607.

**Spark formula** (`ChaosMeta.AwardRunRewards(in ChaosRunRewardInput)` ChaosUpgrades.cs:578-610): `durationMin=max(0,sec)/60; completionBonus=35*DifficultyMult*min(1,durationMin/3); scorePart=1.5*sqrt(max(0,Score)); sparks=round((scorePart+completionBonus)*SparkGainMult); sparks+=(int)max(0,TrickleDrops); DripFeedMaxed → round(sparks*1.10); sparks=round(sparks*ShotSparkMult)` where `ShotSparkMult = 1+0.04*min(10, CraftedItems["the_shot"] ?? 0)` :565-569; `RunsCompleted==0 → sparks += 25` (FIRST_FALL_BONUS :106, :600). Banking :602-608: Sparks+=max(0,sparks), RunsCompleted+1, BestScore/BestCombo max, TotalDefused+=, TotalRunSeconds+=max(0,elapsed), save. Test mirror `ComputeSparksOnly` (DtrhMetaBridge.cs:446-456) omits FIRST_FALL_BONUS and banks only Sparks/RunsCompleted/BestScore (:71-78).

**run-started** (:255-269): `_runActive=true`, `_vnSpeaking=false` :258, `ApplyWorldFreeze(false)` :259, ResetRunMetrics :260, diff default "Gentle"; non-test: sentinel mark + bark.

**request-run** (`OnRequestRun` :395-417): setup present → `PersistRunSetup` (:421-444: absent fields untouched; durationSec clamp 60..1200, waveCount 1..12, effectIntensity 0.2..1.5, enabledVariants JSON-null-aware, difficulty/motion/colorFlashes/boonDraftEnabled/allowCurses/dartersEnabled/key1/key2 raw); `force = !test && ForceScriptedRun`; `scripted = !test && (RunsCompleted==0 || force)`; cfg = `ChaosHappyPath.BuildFirstRunConfig()` (ChaosHappyPath.cs:77-89: ScriptedFirstRun, Easy, 180s, 5 waves, variants {flash,subliminal}, no drafts/curses/darters, SpawnRateMult 0.6 (R1_SPAWN_RATE_MULT :20), sin 0) else `ChaosRunConfig.FromSettings()`; force consumed AT DEAL TIME (persist + Rebroadcast) :403-411; reply `{type:'run-config', runConfig=BuildRunConfig(cfg)}` :412.

**run-config shape** (`BuildRunConfig` :930-1040): cfg knobs (difficulty, difficultyMult (Easy 1.0/Medium 1.3/Hard 1.7/Extreme 2.2, ChaosModels.cs:276-283), durationSec, waveCount, effectIntensity, enabledVariants, motionOverride, fuseTimeMult, baseMult, sparkGainMult, spawnRateMult, colorFlashes, screenShake, boonDraftEnabled, allowCurses, dartersEnabled, draftChoices, draftAutoResumeSec, sinChance (DefaultSinChance ramp: <2 runs → 0, ≥10 → 0.5, linear 0.25→0.5 between, ChaosModels.cs:226-237), scriptedFirstRun, hitboxScale, magnetEnabled, popupHeartEnabled, pendulumSwing) + meta-derived: ownedHabitIds (IsUpgradeActive && id != extreme_tier && catalog-resolved :998-1002), rankIndex, runsCompleted, equippedStartBoon, thoughtTexts (settings BouncingTextPool — N/A greenfield), levels (LifetimeBoonLevels), consumableSlots, discoveredCodexIds, craftedItems, pinnedBoonId, denialArmed, flags{18 seen-once bools}. Upgrade catalog (ChaosUpgrades.cs:38-93): 6 upgrades — slow_fuses (FuseTimeMult ×1.15), silk_touch (HitboxScale 1.25 + MagnetEnabled), popup_notification (PopupHeartEnabled), pendulum_swing (PendulumSwing), draft4 (DraftChoices 4), extreme_tier (flag only). `ApplyTo` = foreach PurchasedUpgrades where IsUpgradeActive → Apply (ChaosUpgrades.cs:360-366). FromSettings clamps (ChaosModels.cs:190-215): duration 60..1200, waves 1..12, intensity 0.2..1.5, shake 0..1. Difficulty/variant reveal clamps (ChaosModels.cs:239-274) ride RevealService — variants passthrough since 2026-07 ("both reveals open from the first run"); difficulty clamp needs the reveal subsystem.

**asset-stats** (:280-284 → DtrhAssetStatsStore.cs): per-name `{kind, seconds, weighted, grabs, pops, defuses, flings, lastSeenUtc}`; name ≤260, deltas max(0,·) :60-80; persisted `dtrh_asset_stats.json` on each merge (best-effort background write) :88-113; case-insensitive rebuild on load :46-55. `TopAssets(12)` ranking `Weighted + Grabs*8 + Pops*2` desc :96-110 → `favorites` message at ready (DtrhHostService.cs:215-216). NO reply message to asset-stats.

**Loom** (`DtrhLoomStore.cs`): folder `%APPDATA%/ConditioningControlPanel/Spirals` :28; MaxSpirals 12 :21; base64 ≤8MB chars :22; decoded ≤8MB :23; slug regex `^[a-z0-9_-]{1,24}$` :26; Slugify (lower, trim, `[^a-z0-9_-]+`→`-`, trim `-`, cap 24, revalidate) :31-38; Save error codes bad-name/too-big/exists/cap-reached/bad-gif/io-failed :65-104; GIF check GIF87a/89a magic + 0x3B trailer + ≥16 bytes :146-155; temp+move overwrite write :85-87; params sidecar `loom_<slug>.json` best-effort :88-92; Delete: regex + defense-in-depth path check :109-114, missing → io-failed, clears Settings.SpiralPath only after true delete :122-130, sidecar best-effort. Dispatch (DtrhHostService.cs:285-301): loom-save → `{type:'loom-result', op:'save', ok, slug, error}` + PostLoomList on ok; loom-delete → `op:'delete'` + list. `PostLoomList` :327-343: `{type:'loom-list', spirals:[{slug, url:"https://ccp.spirals/loom_{slug}.gif", params:parsed|null}]}` at ready :209 + after each successful mutation. Payload: loomStudio.js sends `{type:'loom-save', name, overwrite, params, gifBase64}` (:84-91), `{type:'loom-delete', slug}` (:330), arms overwrite on `error:'exists'` (:113), renders `s.url` as `<img>` (:305-307), MAX_SPIRALS 12 mirrored (:301).

**User/mod media** (`DtrhAssetManifest.cs`): image exts jpg/jpeg/png/webp/gif :19, video mp4/webm/m4v :20; caps 50MB/500MB :21-22; MaxEntries 5000 :23; walk depth 8, dot-dirs skipped :107-122; DisabledAssetPaths honored :48-50,94-98 (N/A greenfield — no assets tree); media-like undecodable counted skipped (wmv/avi/mkv/mov/flv/mpg/mpeg/bmp/tiff/heic :124-125); >5000 → ratio-preserving Fisher-Yates downsample + truncated :127-137; URL `https://ccp.assets/<rel, segment-escaped>` :139-144; counts-only logging :70. `App.EffectiveAssetsPath` (App.xaml.cs:165-187): CustomAssetsPath if set && exists else UserAssetsPath. ccp.assets virtual host CORS-clean "Allow" for WebGL texture upload (DtrhHostService.cs:90). Mod content: `DtrhModContent.ModDtrhRoot()` = active mod `resources/dtrh` (DtrhModContent.cs:55-65) — greenfield has NO mod system (SP-025 consult): init `modContent` stays null (page falls back to shipped assets, modContent.js:22).

**m2test.js harness** (payload): loaded only when init `m2Test:true`; drives (1) in-world payloads, (2) the meta vocabulary walk with delta expectations off the cloned snapshot (incl. retired-op rejection buy-pocket/bench-purchase, whitelist rejection bench-buy pocket ids, gift/rank-gate behavior, crafting Part-2/3), (3) payout round-trip: run-started + run-ended {score 12000, durationSec 180, elapsedSec 180, difficultyMult 1.0, sparkGainMult 1.0, bestCombo 14, defused 9, trickleDrops 5, dripFeedMaxed false} → asserts baseXp == 750 (250·3·1.0) and sparksEarned > 0 && dryRun === true (m2test.js:131-146). Results as `M2TEST pass/FAIL` bridge.log lines.

**cheshireGuide meta consumption** (cheshireGuide.js:82-84 verified): `ensureInit()` — `const m = meta(); if (!m) return false;` — seeds `stage = m.tutorialStage|0` :86, self-heals veterans (`stage===0 && runsCompleted>0 && !forceScriptedRun` → stamp ARC_DONE via set-num :89-93), seeds seenLines/cooldowns from `seenNarrativeLines`/`narrativeCooldownEnds` :95-104. The Cheshire VN overlay (cheshireVn.js — CHESHIRE_DISABLED=false :39, the ACTIVE engine; tinted figure via `--vnA/--vnB` + ACCENT_A drop-shadow CSS) renders her beats ONLY after ensureInit succeeds → the `meta` message is the gating message for the tinted-portrait beats. (vnPortrait.js's scripted-run portrait is pre-release hidden, VN_BEATS_DISABLED=true :38 — a different file; the b3 record's "VN portrait tint" = the Cheshire figure tint.)

### Payload field verification (b2 `DtrhProtocol.cs` records vs payload, tree 40be29df)

- `meta-command {op, ...fields}` → `MetaCommand(string? Op, JsonElement Raw)` ✓ (op set above)
- `request-run {setup?}` → `RequestRun(JsonElement? Setup)` ✓ (warren.js:135-147 buildSetup: difficulty/durationSec/waveCount(4 on the wire)/key1/key2/motion/effectIntensity/colorFlashes/boonDraftEnabled/allowCurses/dartersEnabled/enabledVariants)
- `run-started {difficulty, mode}` → `RunStarted` ✓; `run-ended {score, durationSec, elapsedSec, difficulty, difficultyMult, sparkGainMult, bestCombo, defused, detonated, trickleDrops, dripFeedMaxed, sessionStats}` (chaosRun.js:3580-3595) → `RunEnded(Score, DurationSec, Difficulty, Raw)` ✓ (remaining fields read from Raw)
- `asset-stats {stats:[...]}` (chaosRun.js:487-492) → `AssetStats(Raw)` ✓
- `loom-save {name, overwrite, params, gifBase64}` → `LoomSave(Name, Overwrite, Raw)` ✓ (gifBase64/params read from Raw); `loom-delete {slug}` → `LoomDelete(Slug)` ✓
- Host→page builders already pinned b2: meta {state, rev} (+testMode per WPF — added), favorites {names[]}, run-config {runConfig}, payout-result {flat 7 fields}, loom-list {spirals[{slug,url,params}]} ([JsonPropertyName("params")]), loom-result {op, ok, slug, error} ✓

### Design (pre-consult)

1. **Progression members on `DtrhSlotDocument` (no schema bump, b2 design intent):** every ChaosMetaState member EXCEPT SchemaVersion (the SP-005 store machinery owns schema) and the retired ToyPockets/AccessoryPockets (WPF do-not-read, migration-zeroed). ~45 new members, all WPF-neutral defaults (additive-only; ConsumableSlots=1 via initializer = the ChaosMetaStore.cs:71 clamp outcome). b2-era files lacking the members load clean with defaults; the meta engine logs once per slot at bind when the on-disk file lacks progression members ("absent progression members — neutral defaults applied (additive-only)") — FLAGGED, never silent (read-only check of the slot file JSON at bind). The `meta` snapshot serializes the typed members camelCase (STJ already camelCase in SP-005) with ExtensionData EXCLUDED from the page snapshot (unknown members stay file-only, round-tripped) + `{rev, testMode}`.
2. **Run setup on the INDEX document** (`DtrhSlotIndex` gains the 12 BuildRunSetup members — app-global like WPF AppSettings, which the setup is in WPF): difficulty/durationSec/waveCount/motion/enabledVariants/effectIntensity/colorFlashes/boonDraftEnabled/allowCurses/dartersEnabled/key1/key2 with WPF defaults. request-run's PersistRunSetup mutates it through the index store (absent fields untouched, WPF clamps). init's runSetup reads it (replaces the b2 hardcoded literal).
3. **`Features/Dtrh/DtrhMeta.cs`** (new): UI-free meta/payout/run-config engine (unit tests on the real slot stores + recording broadcast). Bound at descend to the active slot; test mode = deep-clone in-memory (WPF clone parity; WPF writes chaos_meta.test.json but NOTHING reads it — greenfield keeps the clone in-memory only, recorded). Full 23-op switch with verbatim guards/clamps/whitelists (reflection → explicit camelCase→accessor maps, same semantics); rev/Bump/Rebroadcast; 500ms debounce → store.Save() (SP-005 enqueue), FlushSave at run-end/teardown; AwardRun (real: full banking incl. FIRST_FALL_BONUS + ShotSparkMult; test: mirror minus first-fall); OnRunEnded payout (skillMult 1.0 — no greenfield skill tree, WPF null-SkillTree parity outcome, named limit; previousBest before banking; rankUp after banking; dryRun=testMode; app-level hooks — AddXP/achievements/bark/reveal-sync/sentinel/session-telemetry — NAMED LIMITS: no greenfield subsystems, quips row owns barks); OnRequestRun (persist setup → index, scripted-first-run deal, ForceScriptedRun one-shot consume at deal time + Rebroadcast, run-config build incl. the 6-upgrade catalog port + DefaultSinChance ramp + DifficultyMult table; thoughtTexts = empty → page default ['GIVE IN'] (no bouncing-text settings in greenfield); difficulty reveal-clamp SKIPPED — named limit, no reveal subsystem, page-side hub gates visually (DtrhHostService.cs:447-450 comment: "the hub applies its own reveal gating visually")); OnRunStarted (runActive + hygiene); AssetStats merge into a 5th SP-005 store `dtrh_asset_stats.json` (app-global participant store inside DtrhSaveSlots, own owner "DtrhAssetStats"; case-insensitive rebuild on load; per-merge save; TopAssets(12) ranking → favorites at ready).
4. **`Features/Dtrh/DtrhLoom.cs`** (contract-named): folder `<dataDir>/Spirals` (DtrhLoomStore.cs:28 parity). Store shape DECISION: **WPF folder discipline** — `loom_<slug>.gif` + `loom_<slug>.json` sidecar + temp-then-move atomic write; slug regex/slugify/12-cap/8MB ceilings/GIF magic+trailer validation/error-code vocabulary verbatim. **Rejected alternative: SP-005 adaptation** — SP-005 is a JSON-document store (schema/migration journal); Loom artifacts are binary GIFs with a convenience sidecar, no schema/migration need; wrapping them in a JSON index invents a structure WPF doesn't have and buys nothing. Settings.SpiralPath reset-on-delete = N/A (no greenfield spiral-overlay setting — named limit). Serving: media-origin route `GET /spirals/loom_<slug>.gif` (§4 discipline: GET-only, traversal refusal, .gif already in the pinned MIME allowlist, nosniff, CORS like other media routes); loom-list URL `{MediaOrigin}/spirals/loom_{slug}.gif`. **Rejected: third loopback origin** — §4's two-origin split is payload-vs-user-content taint; spirals are user-content class, a third listener adds no security. PostLoomList at ready (after manifest, WPF :209 order) + after each successful save/delete.
5. **User media:** folder contract DECISION: **`<dataDir>/assets/images` + `<dataDir>/assets/videos`** (WPF `App.EffectiveAssetsPath` default parity — UserAssetsPath; a future assets-picker row owns custom paths). **Rejected: the b3 overlay-staging shape as the user-media root** — the overlay is product-owned run-time staging; user media is user-managed content that must survive app updates (WPF semantics). Manifest = DtrhAssetManifest port (exts/caps/depth/dot-dirs/skipped/downsample; DisabledAssetPaths = empty, named limit). Serving: media-origin route `GET /umedia/*` → user-media root with an extended MIME table for this route (.jpg/.jpeg/.mp4/.m4v added to the pinned 9; deny-by-default 415 stays; CORS + Range like /media/*). **Rejected: serving user media under /media/*** — the payload root stays READ-ONLY payload-only (§4.1). Native video pool gains `<dataDir>/assets/videos` as a VideoRoot (WPF parity: native payloads play the user's pool). **Media-logging rule (packet honesty framing c):** user-media-class picks/enumerations log PRESENCE+SHAPE ONLY (kind/count/bytes/ext-class) — never filenames/URLs; b3's three log sites (sfx/whisper/video) gain a source-class gate (payload/overlay roots → name OK per b3's recorded scope; user-media roots → presence+shape). Loom logs presence+shape (bytes/count/status) — the slug is a user-authored filename.
6. **Protocol upgrade:** Classify → Handled for meta-command/request-run/run-started/run-ended/asset-stats/loom-save/loom-delete. Dispatch in the host window: run-started/run-ended invoke `_router.TryRunBoundaryHygiene` (the SAME b3 entry — WPF :252/:259/:513 parity, no behavior drift, b3 tests stay green) + the real meta effects. Ready seed order (WPF :175-216): init → meta snapshot → manifest (now real: user-media enumeration + payload fallback) → loom-list → favorites.
7. **Manifest upgrade:** b2's hardcoded 1-image/1-video literal → real enumeration: user-media roots (above) + the payload `assets/bubbles` fallback entries (kept so an empty user pool still shows the shipped bubble/spiral — WPF's manifest is user-pool-only; greenfield divergence recorded: the b1/b2 boot evidence depends on the payload entries, and the payload's own bubbles/spiral are the shipped default content).
8. **Harness (headed/WX evidence; File Scope amendment per the SP-023 norm — Program.cs + App.axaml.cs flag threading, `fileScopeMustNotChange` untouched):** `--dtrh-m2test` (init m2Test:true + meta test-mode clone — the payload's own m2test.js drives the FULL meta vocabulary + payout round-trip page-originated, M2TEST pass lines as evidence); fx-drive gains `loom-file:<name>` (reads a staged GIF from overlay staging, sends the REAL loom-save message with its base64 through the REAL dispatch path — video-file:/whisper-file: precedent) and a fields-carrying `run-ended-full` step (real run-ended with score/duration/bestCombo/etc. for the non-test payout → slot-doc banking proof).
9. **b4-gated page visuals (never faked):** (a) Cheshire tinted-portrait beat: boot with the real `meta` message → cheshireGuide.ensureInit succeeds → her welcome/greeting beat renders the tinted figure (cheshireVn, CHESHIRE_DISABLED=false) — pixel-verified; (b) freeze bubble: real request-run through the real dispatch → run-config → run starts (non-scripted after the payout proof banks run 1) → bambifreeze bubbles spawn in the mix (variants.js:48-50, weight 0.5) → canvas click-sweep catches one → activateFreeze renders ❄ FREEZE in-page + sends freeze-state (chaosRun.js:1149,1287-1298) — pixel-verified.

### Consults

#### Pre-approach consult (Step 1)

**Mode:** solo (council route broken — T-7). **Requested:** Fable 5. **Actual answering model:** NOT surfaced by the consult tool response (no model identity header — recorded honestly, same provenance discipline as SP-022…025).

**Verdict (decisive points folded into the design):**
1. **Snapshot/ExtensionData exclusion is a REAL trap, not a nicety** — STJ `[JsonExtensionData]` members flatten back into any serialization of the doc; the page snapshot must exclude them via a resolver modifier or an explicit snapshot shape. Implemented as an explicit snapshot map over typed members only.
2. **Rev semantics must port EXACTLY** — `Bump()` fires ONLY on applied ops and on `Rebroadcast()` (which also Bumps); m2test's pass/fail counts `metaRev` deltas off the clone (`applied === expectApplied`), so any over/under-Bump breaks the round-trip. The test-mode spark mirror omits FIRST_FALL_BONUS (m2test asserts only `sparks > 0 && dryRun`, but the mirror ports verbatim anyway).
3. **Run-setup on the INDEX document confirmed** — app-global in WPF (AppSettings, shared across slots); same File Scope constraint class as b2's activeSlot placement; additive members, no schema bump.
4. **skillMult 1.0 is not a limitation but the parity outcome** — WPF's own `?? 1.0` fallback; AddXP(baseXp-not-finalXp) divergence is WPF's own and N/A greenfield (no Progression).
5. **Manifest payload-fallback entries: REJECTED by the consult** — WPF's manifest is user-pool-only; keeping the b2 hardcoded bubble.png/spiral.webm entries would make the page treat shipped payload art as USER media (spawner/hostMedia behavior diverges from a fresh WPF install). **Design changed:** b4 replaces the b2 literal with the REAL user-media enumeration only; an empty user pool → empty manifest → `hasUserMedia()` false → the page renders its shipped in-page art = fresh-WPF-install parity. b1/b2 boot evidence never visually depended on those entries (engine-live pixel facts stand).
6. **Wire the b4 handlers through the SAME hygiene entry** (`_router.TryRunBoundaryHygiene`) with WPF ORDER preserved: run-ended applies `ApplyWorldFreeze(false)` FIRST (DtrhHostService.cs:513 is the first statement of OnRunEnded), then payout; run-started clears duck+freeze then sets run-active. b2/b3 tests pinning `Deferred(b4)` must be updated to Handled (classification change is the slice's purpose; the mid-freeze synthesis test now expects Handled + hygiene).
7. **No-store discipline:** the greenfield media URLs carry NO credentials (loopback, no query tokens) — the SP-018 V5 no-store case does not apply; record WHY rather than adding headers blindly.
8. Route-scoped MIME extension (.jpg/.jpeg/.mp4/.m4v for /umedia/* only) approved — payload table untouched, deny-by-default posture kept. Difficulty reveal-clamp skip acceptable as a named limit (must land on the board row): the ONLY path to a locked difficulty is a stale/foreign page since the hub gates visually.

**(d) test-mode clone:** in-memory-only is honest — WPF's chaos_meta.test.json is write-only (nothing reads it back); the M2TEST log lines + payout-result assertions are the evidence. No false-claim risk as long as record/evidence say "clone, never persisted".

### Step-1 design deltas post-consult

- Manifest: payload fallback entries DROPPED (consult item 5) — real user-media enumeration only.
- Snapshot: explicit typed-member map (ExtensionData never reaches the page).
- Handler order: hygiene FIRST inside run-started/run-ended (WPF :513/:259 order), via the b3 router entry.
- no-store: recorded N/A (no credentialed URLs), no blanket header.

---

## Step 2 — meta progression + payout on the b2 slot machinery

- **`DtrhSlotDocument` gains the full ChaosMetaState progression surface (~45 members, no schema bump — b2's recorded design intent):** every member except SchemaVersion (store-owned) and the retired ToyPockets/AccessoryPockets (WPF do-not-read). Neutral defaults = the WPF additive-only rule; a b2-era file loads clean and the meta engine FLAGS the absent members once at bind (read-only check of the slot file — "predates progression members", never silent). Unknown-member preserve round-trips both directions (test: `futureB5Field` survives a meta mutation + save).
- **`DtrhSlotIndex` gains the 12 run-setup members** (app-global, WPF AppSettings parity — consult item 3): PersistRunSetup writes them through the index store with WPF clamps; init's runSetup reads them back (the b2 literal replaced).
- **`Features/Dtrh/DtrhMeta.cs`** (the DtrhMetaBridge + payout/request-run port): all 23 meta ops with verbatim guards/clamps/whitelists (reflection → explicit camelCase maps, same semantics); rev/Bump/Rebroadcast (applied ops only — m2test counts these); 500ms debounce → SP-005 Save enqueue + FlushSave at run-end/close; AwardRun (full banking + FIRST_FALL_BONUS + ShotSparkMult real mode; the test mirror minus first-fall, clone-only, memory-resident — WPF's test file is write-only); OnRunEnded payout (clamps, capBase/baseXp, skillMult 1.0 = WPF's own `?? 1.0` fallback, previousBest BEFORE banking, rankUp AFTER banking, dryRun=testMode; app-level hooks — AddXP/achievements/bark/reveal-sync/sentinel/session-telemetry — named limits: no greenfield subsystems); OnRequestRun (setup persist → index, scripted classroom when RunsCompleted==0 || ForceScriptedRun, force consumed AT DEAL TIME + Rebroadcast); `DtrhRunConfig.cs` (FromSetup + FirstRun + the 6-upgrade catalog + DefaultSinChance ramp + DifficultyMult table; thoughtTexts empty → page default; difficulty reveal-clamp SKIPPED — named limit, no reveal subsystem; the hub gates visually per DtrhHostService.cs:447-450); `DtrhAssetStats.cs` (5th SP-005 store `dtrh_asset_stats.json`, app-global; merge clamps, case-insensitive rebuild, TopAssets(12) ranking → favorites).
- **Protocol upgrade:** all 7 b4 messages Deferred→Handled; run-started/run-ended invoke the SAME b3 hygiene entry FIRST (WPF :513/:259 order — b3 router tests updated, never a regression).
- **Harness (File Scope amendment per the SP-023 norm — THREE wiring files, each with per-file necessity; `fileScopeMustNotChange` untouched):** `Program.cs` + `App.axaml.cs` (the `--dtrh-m2test` flag threading — init m2Test:true + in-memory clone; the payload's own m2test.js drives the vocabulary + payout round-trip page-originated) and **`Lifecycle/CompositionRoot.cs`** (the data-directory thread to `DtrhParticipant` — the sole registration site; the Loom store + user-media roots live in the same data dir as the slot documents and the participant is constructed only there). Documented here + STATUS + the board row.
- **Tests (31 new, `DtrhMetaTests.cs` + classification updates):** op matrix (every op's mutation + clamp + rejection class), payout math vs WPF cases (m2test case base 750 / sparks 204+25, below-cap, clamps, dripfeed×shot stack, previousBest-before-banking, rankUp-after-banking + set-num ack flow, test-mode dryRun + clone isolation), AwardRun banking fields, request-run persist→init round-trip, scripted classroom deal, force-consume + rebroadcast, upgrade multipliers + habit filter, meta-derived config fields, asset-stats merge/rank/presence-only logs, malformed tolerance, absent-member flag, unknown-member preserve. **347/347 + 29/29 green.**

---

## Step 4 — headed/WX evidence + page-visual unlocks + board reconciliation

### WH (Windows headed, DISPLAY3 owner convention — SetWindowPos + GetWindowRect before EVERY capture, **rect lines REDIRECTED INTO the committed run transcripts** (the SP-025 land-consult BINDING: 10 rect lines across runA/B/C/D-drive.log); modal-drive rule honored (no modals driven; canvas clicks use the topmost-raise norm); transcripts `evidence/wh/`)

- **Run A (`runA.log` + `runA-drive.log`, EXIT=0):** `--dtrh-m2test` — the payload's OWN m2test.js drives the vocabulary page-originated: **M2TEST ALL PASS (8/8)** — payloads-sent, **meta-commands rev +18 (expected 18)** (the rev-discipline proof: Bump fires only on applied ops), meta-state (gold/dial/flag/boon deltas off the clone), crafting-p2-state (pin/denial/cubes), paperwall-sketch, **payout-xp-cap baseXp=750** + **payout-sparks 204 dryRun=true**. Engine logged `run complete — base 750 x 1.0 = 750 XP, 204 sparks (TEST, dry run)`. **Clone-isolation proof (`runA-slot1-proof.json`):** the real slot doc stays all-zeros — the m2test economy never touched it; no test file exists (memory-only clone).
- **Run B (`runB.log` + `runB-drive.log`, EXIT=0):** **the b4-gated Cheshire tinted portrait UNLOCKED BY THE REAL meta MESSAGE** — `cheshire: hub scene hub_welcome` fires on the first Warren open (ensureInit needs the meta snapshot; b3 named this gate), `runB-cheshire-a.png` pixel-verified (pink-tinted figure + welcome line; never faked — the page's own beat). **Loom through the REAL dispatch path:** fx-drive loom-file (2,236,341-byte real GIF staged from the payload's own spiral art into the RUN-TIME overlay — never a Z:\ reference in product code) → `loom-save` → `dtrh-loom: saved spiral` → loom-result ok → `loom-list posted (1 spiral(s))`; file proofs `runB-loom-gif-proof.gif` + sidecar `runB-loom-sidecar-proof.json`. **Deterministic payout banking:** run-started + run-ended-full → `run complete — base 750, 171 sparks` (146 formula + 25 FIRST_FALL_BONUS); `runB-slot1-proof.json`: sparks 171, runsCompleted 1, bestScore 5200, bestCombo 11, totalDefused 7, totalRunSeconds 172 — every banked field.
- **Run C (`runC.log` + `runC-drive.log`, EXIT=0):** the request-run → run-config → **REAL 150s descent** with staged user media (manifest `2 image(s), 1 video(s), 0 skipped` — presence+shape only). Background grid click-sweep = REAL gameplay (score 5458+ across attempts, rank card acknowledged via set-num — `lastRankSeen 1` in the slot proof). **The b4-gated FREEZE BUBBLE caught in-run ×2** (`world freeze ON/OFF` pairs at loop 3/4 + 4/4; freeze bubbles exist only in-run — the request-run gating): `runC-freeze.png` pixel-verified (❄ FREEZE announce, ×0.2 time dilation, frozen field mid-ACT-III — never faked; first attempt at 60s+120s runs spawned `bubble:bambifreeze` (slot codex proof) but the sweep missed it — spawn/catch mechanics documented in the surprise ledger). **User media rendered in-run:** `runC-after-run.png` — the gif cascade + bubbles tile the STAGED user images + a user-video bubble frame across the field, served from `/umedia/*`. **asset-stats merged** ×8 (row counts only, never names). **Natural page-originated run-ended → payout:** `run complete — base 625, 153 sparks` (real score). Slot proof: runsCompleted 4 across B+C attempts, sparks 577, bestScore 6314 — real progression banking across restarts.
- **Run D (`runD.log` + `runD-drive.log`, EXIT=0):** **serve→display pixel-verified:** the probe page renders the SERVED loom GIF (`/spirals/loom_<slug>.gif` — animated pink spiral) + both staged user images + the user VIDEO (playing) through the §4 media origin in the same engine (`runD-probe-media.png`); `probe-img rendered` lines carry url LENGTHS only (the media-logging rule holds page-side).

### WX (WSL2 Ubuntu, WSLg X11-via-XWayland; `~/ccp-sp026` native ext4, never /mnt/e; NO input automation, NO timing claims; Wayland never claimed §5)

- **Contract testCommand on the synced tree:** sln 0W/0E; **366/366 + 29/29 green** (≥ the 313/29 b3 floor); Rebuild 0 warnings. (First sync missed the payload's linked legacy tree — 4 payload-dependent test failures, tree-sync artifact fixed by syncing `ConditioningControlPanel/Resources/web/dtrh`; recorded honestly.)
- **wx run 1 (`evidence/wx-run1.log`, EXIT=0):** loom save through the real dispatch path on Linux (`dtrh-loom: saved spiral (2236341 bytes)`, file + sidecar in `~/.config/CcpClient/Spirals`), manifest `2 image(s), 1 video(s)`, **payout banking platform-identical: `run complete — base 750, 171 sparks`** (same math as Windows run B), `wx-slot1-proof.json`.
- **wx run 2/3 (`evidence/wx-run2-3.log`, EXIT=0):** the probe page renders the SERVED loom GIF + both user images on the WebKitGTK dialog (**`wx-probe-media.png`, XGetImage/xwd**); transport facts reconfirmed (invokeCSharpAction page→host, inbox host→page, preBuffer replay). **Session fact (named limit, SP-025 class):** the user VIDEO errors page-side on this WSLg image (WebKitGTK's GStreamer pipeline — `probe-img LOAD ERROR (video)`); the 3 image-class served URLs render. No timing claims; no input automation; Wayland untouched.

### Surprise ledger

0. **Named limit (pre-completion consult item 1):** the packet's letter is "Loom save → serve → display pixel-verified" — the display proof here is the probe page rendering the SERVED `/spirals/` URL in the same engine, NOT the payload's Loom rack (pane-gated behind crafting `the_loom` + 3D station navigation — deliberately not driven blind). The rack's `<img>` consumes the identical URL (loomStudio.js:305-307); serving + store + loom-list round-trip are product-proven.

1. **The freeze catch is a probability game with documented mechanics** — attempt 1 (60s) and 2 (120s) spawned the freeze bubble (slot codex `bubble:bambifreeze` proves the spawn) but the sweep missed the drift window; attempt 3 (150s + boonDraftEnabled:false — draft holds park the field up to 60s/run and eat the catch window; a legit setup toggle) caught it twice. Effect bubbles are ~20% of spawns early by design (plain-share ramps 80%→30%); bambifreeze is weight 0.5 at minIntensity 0.15, Easy pace surge −0.15.
2. **The sweep's recap clicks triggered "fall again"** in run C attempt 1 — a second real descent dealt + ran (the page's own flow responding to real clicks; harmless, recorded).
3. **WSL tree sync needs the legacy payload tree too** (the csproj links `ConditioningControlPanel/Resources/web/dtrh`); the first sync's 4 failures were the missing payload, never product behavior.
4. **The xwd window-id class again** (SP-013 lesson): `_NET_ACTIVE_WINDOW` returned a 10x10 helper; `xwininfo -root -tree` name-matched the real dialog.
5. Culture: the fx-drive/log lines render `1,0`/`0,6` on this box's session culture in INTERPOLATED log strings (display-only; protocol JSON is InvariantCulture — the SP-025 class, cosmetic).

#### Pre-completion consult (Step 4)

**Mode:** solo (council route broken — T-7). **Requested:** Fable 5. **Actual answering model:** NOT surfaced by the consult tool response (recorded honestly, same discipline as SP-022…025).

**Verdict: APPROVE — the evidence chain is sound; fix-first items all process/honesty completeness (ALL CLOSED before .DONE).**
1. **Loom display naming gap (CLOSED):** the packet's letter is "save → serve → display pixel-verified" — the probe rendering the served `/spirals/` URL is NOT the payload's Loom rack (pane-gated). The named-limit sentence (Surprise 0 + board row) scopes the claim: serving/store/loom-list product-proven; the rack consumes the identical URL (loomStudio.js:305-307).
2. **Freeze-catch evidence CONFIRMED sound** — spawn independently proven (codex), catch proven three ways (page freeze-state → host ON/OFF pairs, ❄ pixel, dilation HUD); drafts-off is a legit persisted setup toggle, not a cheat; the attempts history strengthens rather than undermines.
3. **File Scope amendment count corrected (CLOSED):** THREE wiring files (Program.cs + App.axaml.cs for `--dtrh-m2test`, CompositionRoot.cs for the data-dir thread — per-file necessity lines in record Step 2 + STATUS + board row).
4. Board row prepended in the b2/b3 style (done, below), row stays WIP, named limits verbatim incl. the b5 remainder + published-artifact-location UNDECIDED carry-through.

### Board reconciliation

Row "Implement web-only DTRH host" — b4 block prepended (slice-b4 evidence + named limits), row stays **WIP** (b5 remains; never DONE).

### Budgets

Windows headed runs A/B/C×3/D ≈ 12 min app time; WSL sync ~6 min (383MB payload); contract runs ~40s (Windows) / ~30s (WSL); freeze-catch forensics (spawn mechanics read + 3 attempts) ~45 min.


---

## Step 3 — the Loom + user/mod media serving

- **`Features/Dtrh/DtrhLoom.cs`** (contract-named): the WPF folder discipline — `loom_<slug>.gif` + `loom_<slug>.json` params sidecar + temp-then-move atomic write in `<dataDir>/Spirals` (DtrhLoomStore.cs:28). Slug regex `^[a-z0-9_-]{1,24}$` + Slugify port (:31-38); 12-cap (:21); base64 ≤8MB chars / decoded ≤8MB (:22-23); GIF87a/89a magic + 0x3B trailer + ≥16 bytes (:146-155); error codes bad-name/too-big/exists/cap-reached/bad-gif/io-failed verbatim. **Media-logging rule: slug NEVER logged** (user-authored filename) — save/delete logs carry byte counts + statuses only (a tightening over WPF, which logs the slug — recorded). Settings.SpiralPath reset-on-delete = N/A (no greenfield spiral setting — named limit).
- **Serving inside §4:** media-origin routes `GET /spirals/*` (loom store; .gif already in the pinned table) and `GET /umedia/*` (user-media root; route-scoped MIME table adding .jpg/.jpeg/.mp4/.m4v — the payload table stays pinned; deny-by-default 415 unchanged). Both with the full §4 discipline: GET-only 405, traversal 403 (encoded dots/backslash/drive-colon/escape-under-root), CORS scoped to the page origin + CORS-on-errors, Range 206/416, nosniff. **No-store N/A (consult item 7):** these URLs carry NO credentials (loopback, no query tokens — SP-018 V5's credentialed-URL case does not apply; recorded at the route). Rejected alternatives (Step-1 design): a third loopback origin (no security gain — §4's split is payload-vs-user-content class); serving user media under /media/* (payload root stays READ-ONLY payload-only).
- **User-media folder contract (Step-1 decision, consult-approved):** `<dataDir>/assets/images` + `<dataDir>/assets/videos` (WPF `App.EffectiveAssetsPath` default parity; a future assets-picker row owns custom paths). **`DtrhUserMedia.cs`**: the DtrhAssetManifest port (image jpg/jpeg/png/webp/gif :19, video mp4/webm/m4v :20, caps 50MB/500MB :21-22, MaxEntries 5000, walk depth 8 + dot-dir skip, media-like-undecodable → skipped :124-125, ratio-preserving downsample :127-137). DisabledAssetPaths = empty (no assets tree — named limit). **Manifest upgrade (consult item 5):** the b2 hardcoded payload entries are DROPPED — WPF's manifest is user-pool-only; an empty pool → empty manifest → the page renders shipped in-page art (fresh-WPF-install parity).
- **Native pool + media gate:** the user videos dir joins `VideoRoots` (WPF parity: native payloads play the user's pool); `DtrhNativeEffectsOptions.PresenceOnlyRoots` — files under it log `user pool (N bytes, .ext class)`, NEVER a filename (the b3-recorded gate, now implemented at all four log sites: sfx/whisper/video/refused).
- **Boot order (WPF :175-216):** init (runSetup now from the index document) → meta snapshot → manifest (real enumeration) → loom-list (PostLoomList port :327-343, `{slug, url:{MediaOrigin}/spirals/loom_{slug}.gif, params}`) → favorites (only when ranked).
- **Tests (19 new, `DtrhLoomTests.cs` + 1 in `DtrhNativeEffectsTests.cs`):** slugify matrix, save→list→delete lifecycle + sidecar, exists/overwrite, cap-at-12 (+overwrite-at-cap allowed), bad-name/too-big/bad-gif/io-failed codes, traversal-shaped delete refused; /spirals/ 200+MIME+CORS+nosniff, traversal 403, 405, 404, CORS-on-errors; /umedia/ jpg+mp4 MIME, Range 206/416, 415 outside table, traversal classes, no filenames in logs; manifest enumeration (exts, skipped classes, dot-dirs, 50MB cap via sparse file, URL shape, counts-only log); empty-pool manifest empty; the PresenceOnlyRoots gate (user pool vs payload names). **366/366 unit + 29/29 headless green; sln Rebuild 0W/0E** (one xUnit1031 batch + one CS8631 found on Rebuild per the xUnit1051 lesson — fixed at source).

