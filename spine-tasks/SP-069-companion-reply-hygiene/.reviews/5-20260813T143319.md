# Code Review — SP-069 Step 5: Testing & Verification

Baseline: `69c426a9` (packet authoring commit) → HEAD `b8baf349`.
Reviewer ran every gate independently rather than reading the worker's logs.

### Verdict: APPROVE

### Summary

The three hygiene layers land in one new file with exactly one definition per rule, applied at the
reply seam upstream of SP-068's unmodified link strip, and the union moderation rule is implemented
as specified. I re-derived H1/H2/H3 against the WPF sources myself: `Clean`
(`ConditioningControlPanel/Services/AIService/AiTextHygiene.cs:287-299`), the five `StripMetadataTags`
patterns in their fixed order (`:36,:40,:44,:53,:61`, collapse+trim `:69-81`) and
`LooksLikeEnvelopeLeak` (`AiResponseParser.cs:53-58`) are transcribed faithfully, including the
`(</\1>|$)` unterminated alternation and the `\b`/colon discipline that keeps `[Apple pie` and
`[giggles` alive. Contract testCommand passes end to end on my run at the exact pinned counts, the
four bite reverts are genuinely independent, and no assertion anywhere in the landed suite was
touched.

### Build / typecheck

Contract testCommand (`.spine/spine-config.json` `testing.build` plus the packet's wrapper chain),
run in full by me in this worktree:

- `node .spine/patches/verify.mjs` → exit 0, "OK — all patches applied on all roots" (9 project + 5 engine).
- `dotnet build client/CcpClient.sln -c Debug --nologo` → **Build succeeded, 0 Warning(s), 0 Error(s)**.
- `node client/tests/floor/check-floor.mjs` → exit 0:
  `FLOOR OK: CcpClient.Tests: 993/993 total, 2 skipped [SecretStoreTests.LinuxProbe_TypedOutcome_NeverFaked, ChaosTunnelCapabilityTests.Linux_UnavailableNamesTheTunnelsOwnTwoGaps]; CcpClient.HeadlessTests: 35/35 total, 0 skipped`

The skip set is exactly the 2 Windows-observed pinned names — not driven to 0, and nothing added to
`allowedSkips`. `dotnet test` was not invoked outside the wrapper.

### Coverage / test-depth gate

Not a line-coverage-tool project; the binding mechanism is the exact-count floor plus per-layer bite
reverts, and it is satisfied at the depth this packet requires:

- 47 new facts across `AiTextHygieneTests.cs` (15) and `AiReplyHygienePipelineTests.cs` (12, several
  parameterized) — every one of them executes, and the floor total moved `946 → 993` in the **same**
  commit as the tests (`abc53009`), with the reason in both the message and `floor.json.lastMovedBy`.
  `allowedSkips`, `admissionRule`, `skipSemantics` and the 5 pinned names are untouched.
- **Bite matrix is real, not shared.** Four reverts produced four *distinct* RED sets, which I
  verified from the committed wrapper logs rather than the record's table: R1 (H1) reds the artifact
  and seam pins; R2 (H2) reds the five-shape rows, the order pin and the trigger-echo seam facts; R3
  (H3) reds only the detection rows plus the three leak seam facts; R4 (union rule, pipeline-only)
  reds exactly the 3 pins that only the hygienic half can trip (`988 passed`). No revert's RED set is
  a superset of another's, which is what makes them independent evidence.
- **H2's order is pinned by construction, not by luck.** `AiTextHygieneTests.cs:119-132`
  (`H2_OrderPin_NestedTruncation_ProvesFixedOrder`) uses `"[Category: [Foo]: bar"` → `": bar"`; I
  traced the permuted path and it does collapse to `""`, so the fixture fails under reordering. This
  satisfies Step 3's hardest requirement.
- **Negative controls per layer are byte-identical**, including the required legitimate `{` case and
  a bracketed non-metadata phrase (`AiTextHygieneTests.cs:141-149`, `:164`).
- The port's own trigger is pinned from the real shape and cited to `AiAwarenessService.cs:229`
  (`AiTextHygieneTests.cs:100-110`, plus the seam fact `TriggerEcho_StrippedBeforeBubbleAndMemory`).

### Monotonicity and boundary checks (verified, not accepted)

- Union rule is implemented as `EvaluateOutput(raw)` then `EvaluateOutput(hygienic)` when the text
  changed, blocking if **either** hits (`AiOperationPipeline.cs:337-370`). The gate can only refuse
  more. Pinned in both directions (`UnionRule_TokenJoinedAcrossTagBoundary`, `..._ByArtifactCharacter`,
  `..._TokenInsideStrippedThinkBlock`, `..._TokenInsideStrippedMetadataEcho`).
- H3 is detection only — a `bool` predicate yielding the **existing** `AiReplyCodes.MalformedOutput`.
  No lift, unescape, repair, brace balancing or execution. `AiEnvelopeValidator` has zero product
  callers after this change: grep across `client/src/` returns only comment text and the unrelated
  pre-existing `AiModerationBoundary.cs:125` surface-registry string.
- No new observation: `git diff` over `client/src` shows **zero** added log, diagnostic, persist or
  network calls. No reply fragment, stripped tag or leaked JSON is written anywhere, tests included.
- SP-068's `StripUnsanctionedLinks` is unmoved and unmodified; it now reads the hygienic text, and the
  final assignment still compares against the **raw** text so hygiene-only changes reach the bubble
  and memory. The emptied-reply path keeps the atomic turn-pair rule.
- No new `AiReply` case; no request-shape or token-cap change; no `AiPrivacyFilters`,
  `AiOperationVocabulary`, `AiAwarenessService`, `AiCommandEnvelope`, `client/docs/**` or
  `ConditioningControlPanel/**` edit.

### Step 5 checklist, verified item by item

- Contract testCommand through the wrapper — **pass**, my own run, counts above.
- 3 consecutive greens with ≥1 cold: G1 (lane-1 warm), G2 (fresh worktree `C:/Code/CCP-SP069-coldcheck`
  @ `abc53009`, first-ever build, `evidence/g2-cold-floor.log`), G3 (`evidence/g3-warm-floor.log`),
  run table at `record.md` §10. G2's checkout point predates only the record/STATUS commits, so the
  cold build covers the full product+test tree — acceptable and stated plainly.
- `git diff --check` → clean.
- `git status --short` → only `spine-tasks/SP-069-companion-reply-hygiene/.DONE` (untracked, expected).
- `git status --porcelain --ignored=matching -uall` → no new ignored artifact; only the pre-existing
  `bin/`/`obj/`/`.pi/npm` entries. No results file or build output committed.
- Named flake did not fire in any run — correctly recorded as "nothing to record, nothing retried".

### Suggestions (non-blocking)

1. **`client/src/CcpClient.Desktop/Ai/AiOperationPipeline.cs:325-334`** — H3 evaluates `afterH1` and
   `hygienic` but never the raw text. The choice is strictly better than raw-only (a leak preceded by
   a think block is invisible in raw and visible after H1), and the miss case is a non-envelope like
   `{<think>"response"</think>}`, so nothing real escapes. Worth one line in a future row if the
   detection set is ever revisited.
2. **`record.md` honesty cell 9** — the worker itself flagged that SP-068's link-strip `\s{2,}`
   collapse runs *after* moderation and can join tokens the gate never saw joined. Correctly scoped
   out (`AiPrivacyFilters.cs` is not in File Scope) and correctly named as a candidate board finding;
   the orchestrator should file it.
3. **Honesty cell 7** — H2's unconditional whitespace collapse flattening multi-paragraph replies is
   WPF-identical parity but a real user-visible formatting loss on a live surface. Disclosed by name
   with the reason it cannot be pinned by a byte-identical control. No action here; a candidate row.

The `ai-operation-contract.md` §7 rule 1 wording the union rule needs is named in `record.md` §6 and
the file is unedited, as required.

```json
{"verdict":"APPROVE","feedback":"Step 5 verified independently. Contract testCommand run in full by the reviewer: verify.mjs exit 0; dotnet build 0 Warnings / 0 Errors; check-floor.mjs FLOOR OK at 993/993 unit and 35/35 headless with exactly the 2 pinned Windows-observed skips. H1/H2/H3 re-derived against WPF (AiTextHygiene.cs:287-299, :36/:40/:44/:53/:61 + :69-81, AiResponseParser.cs:53-58) and transcribed faithfully including the (</\\1>|$) unterminated alternation and the \\b/colon discipline. H2's order pin at AiTextHygieneTests.cs:119-132 genuinely fails under permutation. Bite matrix is four independent reverts with four distinct, non-nested RED sets (15/10/6/3), confirmed from the committed wrapper logs rather than the record's table. Union moderation blocks if either raw or hygienic hits and is pinned in both directions; H3 is detection-only yielding the existing MalformedOutput, with AiEnvelopeValidator still having zero product callers. Zero new log/persist/network calls, zero assertions weakened, floor 946->993 bumped in the same commit as the facts, file scope clean, git diff --check clean, no new ignored artifacts. Three non-blocking suggestions: H3 does not evaluate the raw text (strictly better than raw-only, miss case is not a real envelope); SP-068's post-moderation link-strip whitespace collapse remains a pre-existing out-of-scope finding the worker correctly named; H2's paragraph-flattening collapse is WPF parity but user-visible and unpinnable by a control, disclosed by name."}
```
