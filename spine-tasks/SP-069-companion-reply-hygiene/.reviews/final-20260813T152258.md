### Verdict: REVISE

### Summary
The previous REVISE is genuinely fixed: the replacement H2 order pin bites. I re-executed the five
transcribed regexes plus collapse+trim over all 120 permutations and confirmed the three pinned cases
break 104/120 permutations (40/120, 60/120, 60/120 individually), matching the record's table exactly.
One blocking defect remains, and it is the same class as the defect that caused the last REVISE: the
record and the test comment assert as **"provably UNOBSERVABLE"** a pattern-order pairing that is
demonstrably observable. I have a constructible counterexample.

### Verification performed
- `record.md` §2 table reproduced by execution: cases 1/2/3 yield `b]`, `""`, `""` under WPF order, and
  break 40/60/60 of the 120 permutations. Combined, the three pins fail 104/120 permutations.
- Machine contract verifier: PASS (testCommand, fileScopeMustChange, all fileScopeMustNotChange globs,
  `record.md` artifact).
- `git diff --check` clean; `git status --short` shows only `spine-tasks/SP-069-.../.DONE`;
  `--ignored=matching -uall` shows only pre-existing `bin/`/`obj/`/`.pi/npm` ignores, no new artifact.
- `AiTextHygiene.cs`: H3 is detection only (predicate, no lift/unescape/repair, no `AiEnvelopeValidator`
  call); pattern order is expressed as an explicit ordered sequence; WPF cite per rule. Scope respected —
  only `AiOperationPipeline.cs`, the new `AiTextHygiene.cs`, tests, `floor.json`, and the packet dir.

### Blocking issues

1. **`spine-tasks/SP-069-companion-reply-hygiene/record.md:58-61`** and
   **`client/tests/CcpClient.Tests/AiTextHygieneTests.cs:138-140`** — both state that
   `ClosedCategoryTag` vs `ClosedMetadataTag` "commute because the former's matches are a strict subset
   of the latter's" and that the unclosed passes "commute … with the closed passes on every input
   constructed". **This is false.** Per-match subsetting does not imply commuting outputs, because the
   two patterns' `[^\]]*` runs consume different spans when brackets nest. Counterexample (executed
   against the five transcribed regexes + collapse + trim):

   ```
   input:      [Context:[Foo: z[Category: x][Apple pie
   WPF order:  ClosedCategoryTag removes "[Category: x]" -> "[Context:[Foo: z[Apple pie"
               -> UnclosedKnownTag eats it all            -> ""
   P3-first:   ClosedMetadataTag removes "[Context:[Foo: z[Category: x]" -> "[Apple pie"
               -> no further pass matches                 -> "[Apple pie"
   ```

   So the closed-pass relative order **is** observable, and the 16 permutations that currently survive
   the three pins include `32145` (ClosedMetadataTag before ClosedCategoryTag), which this input
   distinguishes. The claim is exactly the class of unverified order assertion the prior final review
   rejected, restated in the honesty section as proven.

   **Fix (small):** add the counterexample as a fourth `[InlineData("[Context:[Foo: z[Category: x][Apple pie", "")]`
   row to `H2_OrderPin_ThreePermutationDistinguishers_ProveFixedOrder`, and correct `record.md` §2 and the
   test comment. Only the `UnclosedKnownTag` vs `UnclosedKeyedTag` pairing survived a 200k-input fuzz
   without a distinguisher — state that pairing alone as un-distinguished (and say "no distinguisher found",
   not "provably unobservable"), and drop the "commute with the closed passes" clause.

   **Missing test:** `client/tests/CcpClient.Tests/AiTextHygieneTests.cs` —
   `H2_OrderPin_ThreePermutationDistinguishers_ProveFixedOrder` has no case that pins the relative order of
   the two closed passes; the nested-bracket input above is the case.

   Floor bump follows in the same commit as the new case (995 -> 996), reason in the message, per framing j.

### Suggestions (non-blocking)
- The bite matrix R2 note already concedes a whole-function revert red-tells membership, not order. After
  the fourth case lands, the 120-permutation execution is the order proof; consider recording the exact
  surviving-permutation count (currently 16, expected 4 after the fix) so a future reader can re-derive it.

```json
{"verdict":"REVISE","feedback":"H2 order pin now genuinely bites (three cases break 104/120 permutations, reproduced by execution) and the prior REVISE is resolved. Blocking: record.md:58-61 and AiTextHygieneTests.cs:138-140 assert as 'provably UNOBSERVABLE' that ClosedCategoryTag and ClosedMetadataTag commute; they do not. Counterexample '[Context:[Foo: z[Category: x][Apple pie' yields \"\" in WPF order and '[Apple pie' when ClosedMetadataTag runs first, and 32145 is among the 16 permutations still surviving the pins. Add that input as a fourth InlineData case, correct record.md section 2 and the test comment (only the two unclosed passes survived a 200k fuzz with no distinguisher - say 'no distinguisher found', not 'provably unobservable'), bump floor.json 995->996 in the same commit. Contract verifier PASS, diff --check clean, scope clean, H3 detection-only confirmed."}
```
