### Verdict: REVISE

### Summary

The implementation is otherwise strong: H1/H2/H3 are transcribed verbatim from WPF (`AiTextHygiene.Clean`, `StripMetadataTags`, `AiResponseParser.LooksLikeEnvelopeLeak`), the seam wiring is subtractive and upstream of SP-068's link strip, the union moderation rule is implemented in the fail-closed direction, H3 is detection-only, the floor bump and four-way bite matrix are present, and the machine verifier passed. One completion criterion is empirically unmet: **H2's pattern order is not actually pinned** — the test claimed to pin it passes under all 120 permutations of the five patterns, and the record's justification for it is factually incorrect. Fix the pin (a distinguishing input does exist) or state honestly that the order is not demonstrable, then re-run final review.

### Blocking issues

1. **`client/tests/CcpClient.Tests/AiTextHygieneTests.cs:118-128`** — `H2_OrderPin_NestedTruncation_ProvesFixedOrder` does not pin the order. Input `"[Category: [Foo]: bar"` yields `": bar"` under **every one of the 120 permutations** of the five patterns (verified by executing the five transcribed regexes plus the collapse/trim over all permutations). `UnclosedKeyedTag` (`\[[A-Za-z][A-Za-z0-9 _-]*:[^\]\r\n]*$`) cannot match `[Foo]: bar` — the pre-colon class excludes `]`, so the claimed permuted path never fires. The test therefore passes with the patterns in any order; the completion criterion "H2's pattern **order** is pinned by a test, not merely preserved by luck" is unmet.

2. **`spine-tasks/SP-069-companion-reply-hygiene/record.md:43-49`** — the order argument is wrong on its own terms: "Permuted (unclosed passes first): `UnclosedKeyedTag` consumes `[Foo]: bar` (end-anchored) leaving `[Category: `" cannot happen for the reason above. Step 1 explicitly permitted the honest alternative ("If the order turns out not to matter for any input you can construct, **say so** rather than asserting significance you cannot demonstrate"); the record instead asserts significance it does not have. Correct or retract this section, and correct `record.md:192` (R2's RED list credits `H2_OrderPin_NestedTruncation` as order evidence, but that revert neutralizes the whole function, so it proves membership, not order).

3. **Missing test:** `client/tests/CcpClient.Tests/AiTextHygieneTests.cs` — no test fails when the five patterns are permuted. Distinguishing inputs do exist; e.g. `"[Category: x[Media/Streaming]"` → `""` in WPF order (closed-`Category` pass consumes through the reaction tag's `]`) but `"[Category: x"` survives the reaction-tag-first path before the end-anchored passes see a different string. Build the pin from an input whose output actually differs under permutation, cite the derivation in `record.md`, and bump `floor.json` `total` in the same commit if the fact count changes.

### Suggestions (non-blocking)

- `record.md:255` honesty item 7 (H2's unconditional `\s{2,}` collapse flattening paragraph breaks on the live chat surface) is a real user-visible parity consequence and is correctly disclosed; consider naming it as an explicit board finding alongside the SP-068 collapse note at `record.md:261`.
- H3's detection union over `afterH1` and `hygienic` (not the raw text) is a sensible consult-driven deviation and is documented at `AiOperationPipeline.cs:320-333`; worth one line in the contract-wording finding (§6) so the orchestrator writes it down.

```json
{"verdict":"REVISE","feedback":"H2's order pin does not bite: AiTextHygieneTests.cs:118-128 (H2_OrderPin_NestedTruncation_ProvesFixedOrder) yields ': bar' under all 120 permutations of the five patterns — verified by executing the transcribed regexes over every permutation. The record's justification (record.md:43-49) is factually wrong: UnclosedKeyedTag cannot match '[Foo]: bar' because its character classes exclude ']', so the claimed permuted path never fires; record.md:192 likewise credits a whole-function revert as order evidence. Completion criterion 'H2's pattern order is pinned by a test, not merely preserved by luck' is unmet. Fix: build the pin from an input whose output genuinely differs under permutation (e.g. '[Category: x[Media/Streaming]'), correct or retract record.md's order case, and bump floor.json total in the same commit if the fact count moves. Everything else verified: WPF transcription verbatim, subtractive seam upstream of SP-068's link strip, union moderation fail-closed, H3 detection-only with AiEnvelopeValidator unwired, four-way bite matrix, 3 greens incl. cold, git diff --check clean, scope respected."}
```
