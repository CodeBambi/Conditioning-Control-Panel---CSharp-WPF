# Code Review — SP-020 Step 5: Testing & Verification

### Verdict: APPROVE

### Summary

SP-020 delivers the `.spine/patches/` mechanism (anchor-based `manifest.json`, all-or-nothing tri-state `apply.mjs`, per-patch `verify.mjs`, README) plus an empirical inventory that corrected the T-1 row's premise (2 patches present, 2 lost to a past reinstall, 1 undocumented 4th, journal.mjs upstream-fixed, windowsHide absent). Step 5's contract claims were independently re-verified: build 0W/0E, 213/213 unit + 22/22 headless tests green, `git diff --check` clean, and all worker changes (baseline `8b34c8a7..HEAD`) confined to File Scope plus the two PROMPT-authorized doc files. The scripts are sound: occurrence-counted tri-state prevents double-apply and mis-patch, validation-before-write gives all-or-nothing, and anchors are content-based per the binding honesty framings.

### Build / typecheck

Commands run by reviewer (fail-closed gate, `.spine/spine-config.json` testing):

- `dotnet build client/CcpClient.sln -c Debug --nologo` → **Build succeeded, 0 Warning(s), 0 Error(s)**
- `dotnet test client/tests/CcpClient.Tests/CcpClient.Tests.csproj -c Debug --nologo` → **Passed 213/213**
- `dotnet test client/tests/CcpClient.HeadlessTests/CcpClient.HeadlessTests.csproj -c Debug --nologo` → **Passed 22/22**

`testWithCoverage` equals the test command in this project's config and was run green. The deliverable is node tooling under `.spine/patches/` (no client/src code changed), so the ≥77% client-module coverage gate does not attach to new product code; the scripts' behavioral evidence is the scratch-cycle transcript in record.md (negative control, apply, verify, idempotence, loud-fail drift demo with zero writes, 2.10.0 cross-version), plus reviewer syntax checks (`node --check` both scripts OK) and a live `verify.mjs` run behaving correctly against the lane's unpatched worktree install (5 missing, exit 1 — matches the documented T-5-trap reference-copy state).

### Diff, scope, and tests

- Worker diff `8b34c8a7..HEAD` touches only: `.spine/patches/{manifest.json,apply.mjs,verify.mjs,README.md}`, `client/docs/task-board.md` (T-1 row only, WIP not DONE, named limits present), `client/docs/port-lessons.md` (one dated durable-surprise line — authorized under Documentation Requirements "Check If Affected"), and `spine-tasks/SP-020-*/**`. No `.pi/**`, no `client` code, no `spine-tasks/CONTEXT.md`, no `.spine/spine-config.json` changes.
- `git diff --check` clean; `git status --short` shows only untracked `.pi/loops/*.json` (pi runtime artifact, uncommitted) and the untracked `.DONE`.
- Contract `fileScopeMustChange` (`manifest.json`, `README.md`) and all `artifactsMustExist` paths present.
- Honesty framings honored: empirical diff-driven inventory (not T-row transcription), anchor-based patches, automation limit named (no npm-hook — postinstall would live in the wiped package), post-land real-reinstall orchestrator gate named as the FIRST application of the two lost patches, scratch work in `%TEMP%` only, packet-assumption correction (stub mode never builds pi args) recorded prominently.

### Suggestions (non-blocking)

1. Contract `fileScopeMustNotChange: client/**` textually conflicts with File Scope's explicit `client/docs/task-board.md` allowance and the Documentation Requirements naming both doc files; the PROMPT's specific grants govern here, but future packets should carve doc exceptions out of the mustNotChange glob to keep the contract machine-checkable.
2. `apply.mjs` uses `String.prototype.replace` with a string pattern; `$`-sequence hazards are empirically excluded for the current manifest (post-apply byte-parity vs live tree verified), but a future patch whose replacement contains `$&`/`$'` would corrupt silently — consider `split(anchor).join(replacement)` when the manifest next changes.

```json
{"verdict":"APPROVE","feedback":"Step 5 verified independently: build 0W/0E, 213/213 + 22/22 tests green, git diff --check clean, scope confined to File Scope plus PROMPT-authorized docs (task-board T-1 row WIP with named limits, one port-lessons line). Patch mechanism sound: anchor-based tri-state occurrence-counted apply/verify, all-or-nothing validation, scratch-cycle evidence complete incl. negative control, idempotence, loud-fail drift demo, and 2.10.0 cross-version. Non-blocking: carve doc exceptions out of the client/** mustNotChange glob in future contracts; prefer split/join over String.replace to future-proof against $-sequence replacements."}
```
