# Final Review — SP-020 durable pi-spine local-patch mechanism (T-1)

### Verdict: PASS

### Summary

The machine verifier passes on all contract checks (testCommand, mustChange, mustNotChange, artifacts). The deliverable is complete and honest: an empirically generated 5-patch anchor-based manifest (including the two LOST patches re-authored and the undocumented SKILL.md 4th patch found only by the live-vs-pristine diff), a tri-state occurrence-counted all-or-nothing `apply.mjs`, a per-patch `verify.mjs`, and a README naming the automation limit. The full scratch cycle is proven outside the repo (negative control → apply → verify exit 0 → scratch preflight 15/15 → dotnet allowlist positive+negative → >16KB tail @file at the exported arg-builder with pristine inline negative control → stub batch E2E → idempotence → loud-fail drift demo with zero writes → 2.10.0 cross-version), and the real `.pi/npm` was never touched.

### Completion criteria verification

- **Empirical inventory:** record.md Step 1 table classifies every live-vs-pristine delta (2 fsync PRESENT, dotnet-allowlist LOST, @file-tail LOST, SKILL.md T-11 PRESENT-undocumented, journal.mjs upstream-fixed NOISE, windowsHide ABSENT/not-load-bearing, 2.8.0→2.10.0 version drift recorded). Not a transcription of the T-row — the row's premise was corrected (2 of 3 named patches lost).
- **`.spine/patches/` checked-in:** manifest.json (anchor-based, rationale-linked, testedVersions 2.8.0+2.10.0), apply.mjs (idempotent, fails loudly with named patch id, validate-all-before-write), verify.mjs, README.md with re-apply trigger and the named automation limit (no npm-hook — postinstall would live in the wiped package).
- **Scratch cycle:** all ten items in record.md Step 3, including the packet-assumption correction (stub mode never builds pi args; @file proven at `buildWorkerPiArgs`, the exact decision point feeding `spawnSync("pi", piArgs)`), which is the honest evidence shape.
- **T-12 decision:** STAYS UPSTREAM with three code locations cited (`git-helpers.mjs:22`, `merge.mjs` ~205-222, `diagnosis-merge-failure.mjs`) and an upstream fix sketch.
- **Board row:** T-1 → `WIP` (not DONE) with evidence and the three named limits, including the post-land real-reinstall orchestrator gate sharpened as the FIRST application of the two lost patches.
- **Consults:** both solo Fable consults recorded with actual answering model (claude-fable-5) and applied corrections.
- **Hygiene:** `git diff --check` clean; working tree shows only the untracked `.DONE` marker (in-scope); Do-NOT list respected (`.pi/**`, `client/src/**`, config untouched).

### Suggestions (non-blocking)

- The post-land orchestrator gate checklist in record.md should be executed promptly after land — until then the real install remains unpatched for dotnet gate evidence and >16KB tails, as the record itself flags.

```json
{"verdict":"PASS","feedback":"All contract checks pass; deliverables complete with honest scratch-cycle evidence (negative control, idempotence, loud-fail drift, 2.10.0 cross-version), empirical inventory correcting the T-row premise, T-12 stays-upstream decision with code locations, board row WIP with named limits, both consults recorded. Real .pi untouched. Post-land real-reinstall gate remains the orchestrator's responsibility as documented."}
```
