# SP-137 — record

**Two sentences this port shows to users had gone FALSE, and neither had been edited. The code
moved underneath them.**

Base `feat/crossplatform` @ `766be7ac0`. Branch `worktree-agent-a4047a34f4dfbe4ad`.

---

## 1. The universe, as a RULE

> **Every file whose path is under `client/src/CcpClient.Desktop/`, at any depth, excluding any
> path segment named `bin` or `obj`.** No other exclusion.

**Size: 330 files.** Two independent enumerations agree exactly —
`find client/src/CcpClient.Desktop -type f -not -path "*/bin/*" -not -path "*/obj/*"` = 330 and
`git ls-files client/src/CcpClient.Desktop` = 330 — which also proves there is no untracked file
and no generated byte inside the set. (A grep that reaches `bin/` returns `libvlc` DLLs and payload
copies; that is the tell that a universe was never defined, and it cannot happen here.)

By extension: **303 `.cs`, 16 `.axaml`, 3 `.json`, 3 `.bmp`, 1 `.png`, 1 `.manifest`, 1 `.js`,
1 `.html`, 1 `.csproj`.** Four carry image bytes, so the text-bearing universe is **326**.

### The extraction is a rule too

The 303 `.cs` files were **lexed**, not grepped, into string-literal spans and comment spans —
regular, verbatim `@"…"`, raw `"""…"""`, `//` and `/* */` each handled separately — producing
**28,086 spans**. Grep cannot tell a sentence a user reads from a sentence a maintainer reads, and
that distinction is the whole of §2. The 16 `.axaml` files were read separately for rendered
`Text=` attributes versus XML comments.

| Set | Rule | Size |
|---|---|---|
| Claim-vocabulary string literals | `cannot / can still / is not / does not / no longer / never / there is no / this build / not in this build / unsupported / unavailable / opens no / grants no / has no`, length ≥ 40 | **403** |
| **Build-scope** claim literals | contains `this build` or `this port` | **84** |
| **Build-scope** claims in comment spans | same needles, comment spans | **321** |
| `.axaml` rendered `Text=` carrying a claim | attribute value, not comment | **1** (`GoonHostWindow.axaml:56`, adjudicated TRUE) |

### The residue, stated rather than implied

**The 84 build-scope literals were adjudicated one by one against the code. The other ~319
claim-shaped literals were not** — they were counted. That residue is not theoretical: the
falsified Goon sentence says **"this host"**, not "this build". **Build-scope is not a superset of
the finding rule**, and any future audit that anchors on `this build` alone will miss the same
class again.

---

## 2. The classification, and where the line is drawn

**USER-FACING** = the value can reach a person running the built app **without reading source**,
because a sink renders it. Decided by tracing the sink, never by tone:

- `Views/Pages/*Notices.cs` constants and `Describe*()` returns → panel `TextBlock`s.
- `GoonDoors.Refused[*]` fields → the Goon refusal rail, row by row (`GoonHostWindow.axaml.cs:152-170`).
- Gate and fault text: `DtrhGate`, `HapticGate`, `IntakePassGate`, `Views/LaunchFaultText`.
- `CapabilityReason.Detail` from the platform factories — panels interpolate them verbatim
  (`SystemPage.axaml.cs:41`, `VisualsPanelNotices.DescribeSurface`). **This is why the eight
  "no Linux X backend" strings count as user-facing.**
- `.axaml` `Text=` literals, and payloads the page renders (`GoonProtocol.BuildNetPostResult`).

**MAINTAINER** = `///` and `//` spans, and `LogDiagnostic(…)` strings, whose only sink is
`DebugLogSink` → `Console.Error` (`CompositionRoot.cs:13`, `ApplicationHost.cs:88`). No surface in
this client renders the log. **Stated plainly: a user who launches from a console can see stderr,
so this is a line I drew, not one the code draws.**

Counts: **found FALSE and user-facing — 2 claims, 3 rendered string literals, 2 files. Fixed — 2
(all of them), plus 3 maintainer doc comments under the coherence exception. Reported, not fixed —
318 build-scope maintainer comment spans, plus ~319 non-build-scope claim literals, both as
counts.**

---

## 3. FALSE #1 — `Features/Goon/GoonDoors.cs`, the VoiceNotes refusal

### Before (verbatim)

> "…and its recorder asks the browser for the microphone directly **(ui/voice/recorder.js:591), so
> the browser can still ask you. Closing that needs a PermissionRequested-deny hook (D250)."**

False on Windows from the moment SP-135 landed (`b3e4a48f9`, 2026-08-21). Still true on Linux.

### After (verbatim, shipped)

> "…and its recorder asks the browser for the microphone directly **(ui/voice/recorder.js:591). ON
> WINDOWS this host now answers that request itself and answers it DENY, so the browser does not
> ask you (D289) — and on a run where that hook cannot be installed it says so instead of going
> quiet (D307). ON LINUX there is no permission hook to install at all, so the browser still owns
> the question and can still ask you. On neither has a real browser prompt ever been refused here:
> the deny is proved against a test double, and the run that would show it is a headed one nobody
> has made (D250)."**

### Proved true by CITING THE CODE, not by reading better

| Clause | Code |
|---|---|
| Windows answers DENY | `GoonHostWindow.axaml.cs:393` attaches; `WebViewPermissionDeny.cs:262-278` writes `DenyState` through `put_State` (args slot 7) for every kind but `AutoplayKind` (9). Microphone is kind 1. |
| "the browser does not ask you" | `WebViewPermissionDeny.cs:28-33` — `COREWEBVIEW2_PERMISSION_STATE_DEFAULT` is what prompts, so the DENY write IS the suppression. |
| "says so instead of going quiet" | `GoonHostWindow.axaml.cs:401-411` logs the typed `Unavailable(code, detail)` and names that the browser still owns the prompt on that run. |
| "ON LINUX there is no permission hook to install at all" | `WebViewPermissionDeny.cs:115-121` returns `Unavailable("unsupported-platform", …)` for `!OperatingSystem.IsWindows()`. |
| "no real browser prompt ever been refused here" | SP-135's own *"What SP-135 does NOT establish"*: every fact runs against a fake `ICoreWebView2` built in this process's memory. |

**The caveat is not deleted, D250 is not called closed, and Linux is not claimed.**

### The two sentences either side survive BYTE-IDENTICAL

Verified by diff and pinned by `UserFacingClaimTests.TheVoiceNoteRefusal_KeepsTheCrossingAndTheTightening`:
*"The CROSSING is what is missing: there is no peer to send one to."* and *"The host opens no
capture device …— a TIGHTENING over upstream, not a guarantee."*

---

## 4. FALSE #2 — `Views/Pages/HapticsPanelNotices.cs`, two rendered strings, **two false clauses**

### Before (verbatim)

`DescribeAbsences()`: > "And **even with a device attached, nothing would move: no effect in this
build sends anything to haptics yet.**"

`DescribeLiveState(Armed, …)`: > "It will not go further than this in this build — **nothing sends
anything to it yet.**"

**Both halves of the first are false.** Six live sites fire the limb —
`Effects/FlashSurfacePresenter.cs:314`, `Effects/MandatoryVideoEffect.cs:307`, `:340`, `:419`,
`Effects/SubliminalsEffect.cs:223`, `Effects/BouncingTextField.cs:237` — each incrementing
`HapticLimb.Moments` and posting a real envelope (`HapticLimb.cs:187-227`), with the **real** limb
wired in the shipping composition (`CompositionRoot.cs:239-241`, `haptics: haptics.Limb`). So the
modules DO send; and with a route admitted those sends would go out, so the counterfactual is wrong
too.

### After (verbatim, shipped)

`DescribeAbsences()`: > "**The effect modules are no longer the reason nothing moves: the flash
decay ladder, the video background layer and its stop, the subliminal pulse and the bounce tap all
command the haptic limb now. Every one of those commands ends at "there is no device to address",
because no provider route is admitted at all — so the thing to fix is the missing route, never the
modules and never your toy.**"

`DescribeLiveState(Armed, …)`: > "…— **the effect modules do drive the haptic limb now, but the
sink admits no provider route, so nothing has ever been sent.**"

### Proved true by the code

`HapticSinkFactory.AdmittedRoutes` is `[]` (`:27`), `Create()` returns `UnadmittedHapticSink`
(`:114`), and `HapticLimb.Send` returns at `EvaluationsWithNoDevice++` (`HapticLimb.cs:566-570`)
without ever reaching `IHapticSink.SetOutputsAsync`.

### The finding's real shape

**The sentence named the wrong cause** — the exact defect that file's own header exists to prevent,
citing upstream's *"No devices found. Connect your device in Intiface first."*
(`ButtplugProvider.cs:135`) misdirecting a user whose real problem is elsewhere.

**And it was foreseen.** D202 flagged the superseded version as *"stated on the panel where a user
reads it"*; D210 records that *"the reason moved one rung out, from 'the modules are silent' to
'there is nothing to address'"* and that *"`Views/**` was not touched"*. The XML docs beside the
code WERE maintained (`HapticLimb.cs:139-140`, `IHapticSink.cs:223`). **The code's comments were
kept current and the user's sentence was not.**

**The `Armed` arm is unreachable today** — it needs a really-reachable device, which an empty
`AdmittedRoutes` makes impossible — **so no user has read it, and it goes live at exactly the
moment a route is admitted, which is when the stale wording would have been at its most wrong.**

### Three maintainer doc comments corrected in the same file

`:10-17` (the "three no's" header, now two plus the corrected history), the `DescribeLiveState`
summary and the `DescribeAbsences` summary. Normally reported not fixed — but these document the
very strings being corrected, and leaving them would make a shipped refusal incoherent, which is
the exception the packet names.

---

## 5. Adjudicated TRUE, and one that was nearly broken by fixing it

Every other build-scope claim was checked against code. Named, with the check:

- **The 8 platform-arm strings** — `CreateFor(Linux)` still returns an `Unsupported*` in Audio,
  Glyph, Input, Overlay, Pointer, Tray and Video (×2).
- **The haptic SINK strings** (`HapticSinkFactory`, `HapticParticipant`) — `AdmittedRoutes` = `[]`.
- **"this port has no tiers page of its own yet"** — none exists.
- **"no desktop read-back capability at all"** — `PrintWindow`/`GetDC` appear only against the
  port's OWN windows in `Glyph/**`; nothing captures the desktop.
- **The scheduler's two balloons** — the first-minimize balloon that does ship
  (`ShellTray.cs:258`) is a different balloon; the scheduled start/end pair is still unsent.
- **Goon Host/Join "no outbound network connection of any kind"** — both product `HttpClient` sites
  are loopback-only (`LoopbackOllamaProvider.cs:93` refuses a non-loopback endpoint pre-socket;
  `IntakeHostWindow.axaml.cs:719` GETs the host's own origin under a harness flag).
- Mandatory Video's silent clips; Input's speech and multi-monitor absence; Bubble Count's
  strict/mercy absence; `GoonHostWindow.axaml:56`.

### `PointerPanelNotices.cs:43` — adjudicated TRUE against my own first reading (D309)

*"(this build has no progression system at all)"*. My first pass called this FALSE because
`Features/Progression/GradedRunAwards.cs` ships. **That reasoning rested on a directory name, not
on the codebase's vocabulary**, and the vocabulary is settled: "progression system" here means the
XP/level ladder (`BubblePopPresetDocument.cs:44`, `BubbleCountPresetDocument.cs:26`,
`EffectReasonCodes.cs:295`, `BubblePopSurfacePresenter.cs:84`). None of it exists —
`IntakeHostWindow.axaml.cs:538-541` computes completion XP and logs it *"computed, not granted (no
XP store — typed seam)"*, and `GradedRunAwards.AwardableIds` (`:165`) is CLOSED to two intake-only
ids, so a bubble pop can earn none of them. The sentence sits in a Bubble Pop notice justifying
that a pop pays nothing, and **an edit implying this build HAS progression would mislead that
reader far worse than the absolutism does** — this packet's own failure arriving from the other
direction. **Left alone; recorded as a precision nit.** The only defect is "at all".

---

## 6. The guard — `client/tests/CcpClient.Tests/UserFacingClaimTests.cs`

A **claim-to-code binding**, not a prose matcher. **THREE** entries, each pairing a sentence read
**from the running product** with **one machine-evaluated fact about this repository**:

| Id | Sentence (runtime) | Fact |
|---|---|---|
| `goon-voice-prompt` | `GoonDoors.For(Refused, VoiceNotes).Missing` | practice-host source carries `WebViewPermissionDeny.TryAttach(wv2.CoreWebView2` **and** the hook carries its `!OperatingSystem.IsWindows()` → `unsupported-platform` arm |
| `haptic-absence-line` | `HapticsPanelNotices.DescribeAbsences()` | ≥1 file under `Effects/` (same universe rule) contains `IHapticLimb` |
| `haptic-armed-arm` | `HapticsPanelNotices.DescribeLiveState(Armed, …)` | the same fact, bound SEPARATELY — a needle over the two haptic strings joined is satisfied by the absence line while this arm says anything at all, which is exactly how the self-refuting wording of §12.2 got through |

Five facts: the three binding checks (the two haptic ones share a fact body), the
surviving-true-sentences pin, and `TheLimbSiteExtraction_CountsRealFilesAndExcludesGeneratedBytes`
(a temp tree of three known files — one with the needle, one without, one under `obj/` — requiring
exactly **1**), with `EveryClaimBinding_IsRegisteredAndAgreesWithTheCode` pinning the registry's
size and its exact id list.

**The registry started at two and this record said so in four places after it became three** — see
§12.5. There was never a binding called `haptic-silence` in the landed code.

**The matcher that was deliberately NOT built.** A scanner over the 403 claim-shaped literals would
assert a reach it cannot enforce — wave 62's *"the fix is never a cleverer matcher"*. A
phrase-anchored enrolment list over the 84 build-scope literals was rejected for a harder reason:
**it would have caught neither defect here**, because nobody edited either sentence.

### What the guard CANNOT see

1. **It cannot discover a fourth claim.** Its reach is exactly the three bindings above, and the registry-size pin is what stops one being dropped silently.
2. **It cannot check any claim whose falsifier is not a fact about this repository's source** — a
   real browser, a Linux session, a headed prompt, a device, a server. That includes the whole
   residual gate the corrected Goon sentence names.
3. **Two of its three facts are syntactic token presence.** A call site that exists but is dead, or
   one that is renamed, moves the fact without moving the truth. Only the file-walk fact is pinned
   against a fixture.
4. **It checks the sentence, not the pixels.** A rail that stopped rendering `Missing` would leave
   all five green; `GoonPracticeHeadlessTests:194` is what carries the rendering.

---

## 7. Two edits outside the packet's enumerated May-change list

`client/tests/CcpClient.HeadlessTests/GoonPracticeHeadlessTests.cs` and
`client/tests/CcpClient.HeadlessTests/HapticsRowHeadlessTests.cs` each pinned the **exact false
sentence** being corrected, so both went red the moment the product text changed. Neither is named
in the Contract's `fileScopeMustNotChange`. Needles were re-pointed and **no pin was deleted**;
test counts are unchanged. The haptics fact gained a `DoesNotContain` on the falsified clause and
its method was renamed to
`THEABSENCELineSaysWhereTheSendReallyStops_WhichIsTheSinkNotTheModules`.

**A first revision claimed "re-pointed, never weakened" and that was WRONG for one needle.** The
Goon fact's old needle was `"the browser can still ask you"` (29 chars) and the replacement was
`"can still ask you"` (17) — a proper substring, therefore strictly looser: it no longer required
the sentence to name WHO can ask. Corrected. The needles are now scoped to the **VoiceNotes row**
(`Refusal{VoiceNotes}`) rather than to the whole rail, split at the platform boundary, and each
half is pinned to its own platform:

- `windowsClause` must contain `answers it DENY` and must **not** contain `can still ask you`;
- `linuxClause` must contain the full `the browser still owns the question and can still ask you`
  (44 chars — **longer** than the pre-SP-137 needle, keeping the actor and adding the custody);
- ordering is pinned (`linuxAt > windowsAt`), and `nobody has made` must survive.

**This closes a failure the old single needle did not have.** Three unordered `Contains` over the
joined rail would have passed on *"ON LINUX the host denies the request; on Windows the browser can
still ask you"* — the exact inverse of the truth. A split introduces polarity, and polarity has to
be pinned or the split is worse than the unconditional sentence it replaced.

---

## 8. Divergences

`client/docs/wpf-surface-reachability.md`, new `## SP-137` section, **D306-D310** (max id at the
head was D295; D296-D305 left to sibling lanes). Divergence rows only. **D289 and D294 were NOT
touched** — the coordinator is correcting those landed rows separately.

## 9. Floor

Declared `{ unit: 5, headless: 0 }`. Pin **2599 / 152**; observed **2604 / 152** = pin + delta,
exactly. `client/tests/floor/floor.json` was never opened.

---

## 10. Every new guard watched RED at the committed head

**Head: `0987a1a01`.** Every mutation below was re-run against that tree; each was reverted, and
the working tree was clean at `0987a1a01` afterwards. The landed commit is that tree with **this
file** amended — `record.md` is the only path that differs, and no product, test or doc file is
among them. A SHA cannot name itself from inside its own commit, so the measured SHA is quoted and
the one-file difference is stated rather than papered over.

**THIS PARAGRAPH WAS ITSELF FALSE FOR ONE REVISION, AND THE FIX IS WHAT BROKE IT.** It previously
read *"Head: `daa055f82` … the only path that differs, and no product or test file among them"*,
which was true when written and was verified true at `65c04b411`. The final review then required
changes to the two test files carrying these very pins, and
`git diff --name-only daa055f82 0987a1a01` returns **six** paths —
`GoonPracticeHeadlessTests.cs` (bounded `linuxClause`, uniqueness pins, symmetric negative),
`UserFacingClaimTests.cs` (non-vacuity removal, id-list pin), `wpf-surface-reachability.md`,
`floor-delta.json`, `plan.md` and this file — and `daa055f82` is **not an ancestor** of the head
(`git merge-base --is-ancestor` → NO; the amend rewrote it). Rather than correct the sentence, the
runs were re-measured so a single SHA covers all of them.

**A true provenance claim went false because the artifact it described moved underneath it — in
the section documenting the standing rule about exactly that.** Fourth instance of the class in
this packet, and the first one caused by a fix rather than by a lane elsewhere. The general lesson
is the one §12.5 already names: **a claim about a mechanism, written in prose beside it, rots the
moment the mechanism moves** — and re-measuring is cheaper than re-reasoning about whether the old
measurement still applies.

### Run 1 — both product files restored to their PRE-PACKET state (`766be7ac0`)

The exact false sentences this packet corrects.

```
Failed  TheHapticAbsenceLine_SaysWhereTheSendReallyStops
Failed  TheVoiceNoteRefusal_KeepsTheCrossingAndTheTightening
Failed  TheVoiceNoteRefusal_SaysWhatTheDenyHookActuallyDoes
Failed  EveryClaimBinding_IsRegisteredAndAgreesWithTheCode
Failed! - Failed: 4, Passed: 1, Total: 5
```

**The load-bearing evidence: the guard reds on the state the repository was actually in at wave 67.**

### Run 2 — SP-137's OWN self-refuting Armed wording restored

The wording review caught: *"the sink admits no provider route, so nothing has ever been sent."*

```
Failed  TheHapticAbsenceLine_SaysWhereTheSendReallyStops
        "the Armed arm renders only where a provider route HAS been admitted, so it may not claim
         that none is, and may not claim nothing has been sent. It reads: ..."
Failed  EveryClaimBinding_IsRegisteredAndAgreesWithTheCode
        "haptic-armed-arm: the shipped sentence disagrees with the code as it stands"
Failed! - Failed: 2, Passed: 3, Total: 5
```

**This is the fix proving itself.** The pre-review guard — one needle over the two haptic strings
JOINED — passed this wording, because the absence line satisfied the needle on the Armed arm's
behalf. The split binding catches it.

### Run 3 — the platform split INVERTED, and the old needle set measured against it

`GoonDoors.cs` mutated to *"ON WINDOWS … the browser still owns the question and can still ask you
… ON LINUX this host now answers that request itself and answers it DENY"* — the exact opposite of
the truth, with every individual needle still present somewhere in the rail.

| Needle set | Result on the inverted sentence |
|---|---|
| **Pre-review**: three unordered `Contains` over the joined rail | **PASSED** — 1 passed, 0 failed |
| **Landed**: per-row, bounded at BOTH boundaries, ordered, symmetric negatives | **FAILED** — `Assert.Contains() Failure: Sub-string not found` (re-measured at `0987a1a01`) |

**Measured, not asserted** — and scoped: this is evidence about `GoonPracticeHeadlessTests`, the
only place polarity is pinned. `UserFacingClaimTests.VoiceVerdict` is unordered, unscoped
`Contains` and passes every inverted variant; it is a DRIFT detector, not a polarity detector
(§12.6). A split sentence introduces a failure the old unconditional needle could not have, and an
unordered needle set over a joined rail cannot see it.

### Run 4 — the extraction stops excluding generated bytes

`.Where(path => !IsGeneratedPath(path))` deleted →
`TheLimbSiteExtraction_CountsRealFilesAndExcludesGeneratedBytes` reds
(`Assert.Equal() Failure: Values differ`), 1 failed / 4 passed.

### Run 5 — `VoiceVerdict` made constant-true: **NOT CAUGHT, and that is a real loss, measured**

```
Passed! - Failed: 0, Passed: 5, Total: 5
```

**A degenerate verdict implementation now passes every fact in the file.** This is the honest cost
of removing the flipped-fact assertion, and it is recorded rather than glossed:

- The removal was correct for the threat this guard exists for. For the shipped verdicts the flip
  is **logically entailed** — the false arm is `s.Contains(FalsifiedX)` and the true arm conjoins
  `!s.Contains(FalsifiedX)`, so asserting the true arm already forces the false arm, and it could
  never red on its own for a real implementation.
- But it was NOT entailed for a **mutated** implementation, and that is the one case where it did
  fire. Dropping it traded a false claim of non-vacuity for a genuine, narrow loss of coverage
  against guard-code degradation.

Both halves are true and neither cancels the other. The residual is stated here so a later lane
does not read this file as proving its own verdicts are non-degenerate: **it does not.** The
extraction — the other half of the machinery — IS pinned, by Run 4 and by the temp-tree fixture.

### Run 6 — the two structural needle pins, watched red

| Mutation | Old guard | New guard |
|---|---|---|
| **INV-B** — attribution smuggled a few words LATER: *"…and can still ask you — though in practice this host answers it DENY there as well."* Lands past the old guarded window, in the unbounded suffix | **invisible** (`linuxClause` ran to end-of-string with a positive needle only) | **FAILED** — `Assert.DoesNotContain() Failure: Sub-string found` |
| **INV-C** — a DECOY marker planted ahead of the real one: *"ON LINUX and ON WINDOWS this host now answers…"*, pre-empting `IndexOf` so every window is measured over the wrong span | **invisible** (no uniqueness pin) | **FAILED** — `Assert.Equal() Failure: Values differ` |

Both reverted; the tree is clean.

**And what these runs do NOT show.** They show two specific holes closed. They do not show the
guard is complete, and it is not: an adversarial pass found further false-but-passing variants, and
an append after the truthful residual is still unconstrained. **That bound is stated in the test, in
D310 and in §12.6 rather than chased**, because chasing it is the matcher this packet argued against
— an argument that has to hold against my own guard or it was never an argument.

### Final state at the committed head

| Gate | Result |
|---|---|
| `check-warnings.mjs` (forced `--no-incremental`) | **0 warnings, 0 errors**, 4 projects |
| `CcpClient.Tests` | **2604 total** = pin 2599 + declared delta 5 |
| `CcpClient.HeadlessTests` | **152 total** = pin 152 + declared delta 0 |

`check-floor.mjs` reports the 2604-vs-2599 drift **by design**; the orchestrator applies one summed
bump at land. The two skips are the pre-existing machine-property Linux skips, untouched.

## 11. What this work does NOT prove

- **Nothing was rendered, composited, focused, animated or heard.** Two headless rendering pins were
  re-pointed, but a headless frame is never `presentation-verified`. No headed capture was taken.
- **No browser was started and no permission prompt was ever raised.** The corrected Goon sentence
  says so itself; the headed Goon-voice-screen gate remains unrun, exactly as SP-135 left it.
- **No Linux session, no WSLg, no X11.** The Linux half of the corrected sentence rests on reading
  Avalonia's API surface, not on running it.
- **No haptic device and no admitted route.** The corrected haptics sentences describe a limb whose
  sends have never reached a device, because none can be attached in this build.
- **The audit is bounded.** 84 build-scope literals adjudicated one by one; ~319 further
  claim-shaped literals and 318 build-scope maintainer comment spans counted, not checked.
- **The guard's reach is THREE BINDINGS over two corrected sentences** — `goon-voice-prompt`,
  `haptic-absence-line`, `haptic-armed-arm`; the two haptic strings are bound separately because
  a needle over them joined is satisfied by one of them alone. **It cannot discover a fourth
  binding**, and the registry-size-and-id pin is what stops one being dropped silently. Stated in
  the unit the code uses, so this residual and `UserFacingClaimTests.cs:42-45` cannot disagree.
- **It detects DRIFT, not deliberate REWRITING.** Token presence and ordering cannot encode
  attribution; further false-but-passing variants exist and are named rather than chased (§12.6).


---

## 12. What CODE REVIEW caught, and what it says about this packet

Three blocking defects. Two of them are **this packet's own defect class, committed by this
packet** — which is the most useful thing in the record.

### 12.1 I wrote a NEW false present-tense mechanism claim into a permanent record

`HapticsRowHeadlessTests.cs` and the D-row both said renaming the stale method was rejected because
*"the floor pin is name-anchored and `client/tests/floor/**` is closed to this packet."* **False.**
`check-floor.mjs:222` compares `resultCount !== pinEntry.total` — a COUNT. Names are matched only
for `NotExecuted` results (`:231`), via `skipNameAllowed` against `allowedSkips`. That fact is not
skipped, and its name occurred in exactly two places in the whole checkout: the test file and the
row I had just written. **Renaming changed no pinned name and no total; no floor edit was ever
required.** I never opened `floor.json` — but `check-floor.mjs` was open to me and I did not read
it before asserting what it does.

The leftover name was never the defect. **Writing a new false mechanism claim into the permanent
record, in the packet whose entire subject is that class, was** — and the next lane would have cited
it to justify the same avoidance. Renamed to
`THEABSENCELineSaysWhereTheSendReallyStops_WhichIsTheSinkNotTheModules`; both the test comment and
the D-row now carry the correction rather than the excuse.

### 12.2 My Armed-arm replacement was FALSE in the only world where it renders

I correctly identified that the `Armed` arm goes live the day a route is admitted — then wrote text
that is false on exactly that day: *"the sink admits no provider route, so nothing has ever been
sent."*

Traced: `Dot => Enabled && LastObservation is { Confirmed: true }`
(`HapticParticipant.cs:214-215`); `Confirmed => Asked && ClientAdmitted && ServerAnswered &&
DeviceCount >= 1` (`IHapticSink.cs:136`); `LastObservation` is assigned only at
`HapticParticipant.cs:270`, past the `Sink.Route == HapticProviderRoute.None` early return at
`:259`. **The dot cannot be lit unless a route has been admitted.** In that world both clauses are
negated by the arm's own precondition, and a confirmed observation gives the limb non-empty
`DeviceKeys` (`:103`) so `Send` skips `EvaluationsWithNoDevice++` and reaches `Sends++` /
`SetOutputsAsync` (`HapticLimb.cs:575-585`). **Self-refuting, not merely stale** — and it failed the
criterion my own new doc comment states.

The arm now says what the dot MEANS in that world — it has only two states and reports **reach, not
traffic**, so it looks the same whether or not a pulse is going out — and names the **entitlement
gate** (`HapticLimb.cs:588` → `HapticParticipant.cs:102`, `:146`) as what decides delivery. True on
the day it renders, and true today.

**And the guard had cemented it.** A single needle over the two haptic strings JOINED was satisfied
by the absence line alone, leaving the Armed arm only negatively constrained. The registry now
holds **three** bindings — `goon-voice-prompt`, `haptic-absence-line`, `haptic-armed-arm` — each
verdict scoped to its own string, and **both** self-refuting wordings are banned by name
(`FalsifiedArmedClause`, `SelfRefutingArmedClause`). Fact count is unchanged at five.

### 12.3 One needle really was weaker

Covered in §7. `"the browser can still ask you"` → `"can still ask you"` is a proper substring and
strictly looser. Corrected, scoped per row, and polarity pinned.

### 12.4 Non-blocking, all taken

1. `GoonDoors.cs` now says the failed-attach case "says so **in the diagnostic log** instead of
   going quiet" — it resolves to `_host.LogDiagnostic`, not an on-screen notice, and a user reading
   the rail should not expect a visible message.
2. D307's citation is now `GoonHostWindow.axaml.cs:401-405` for the `Unavailable` arm plus
   `:409-414` for the non-WebView2 `else`, instead of a range straddling both.
3. The absence line no longer puts `"there is no device to address"` in quotation marks — that
   string appears nowhere in `client/src`, so the quotes implied a message the app never emits. It
   reads "ends with no device to address".
4. Squashed to one commit per the slice rule.

### 12.5 The registry count went false in four artifacts — the packet's own class, third instance

Splitting the guard from two bindings to three (§12.2) moved the mechanism and left the COUNT
behind in four places at once:

| Artifact | Said | Truth |
|---|---|---|
| `UserFacingClaimTests.cs:32` | "Its reach is exactly the two bindings below" | `Bindings()` returns three; `:187` asserted `Equal(3, …)` **in the same file** |
| `wpf-surface-reachability.md` D310 | "two entries" | three — in a permanent divergence row |
| `record.md` §6 | "Two entries", and a binding id **`haptic-silence`** | that id exists nowhere in `client/src` or `client/tests`; it would have told the next lane the Armed arm is unpinned when it is separately pinned |
| `floor-delta.json` | "two claim-to-code bindings" | three |

**A present-tense mechanism claim that went false in the same commit that moved the mechanism,
inside the guard built to catch exactly that.** All four corrected to three with the real ids.
This is the third instance of the class inside one packet (§12.1 the floor-pin premise, §12.2 the
self-refuting Armed arm, §12.5 the registry count) and the honest reading is that **counts written
in prose beside a mechanism rot faster than anything else here** — which is precisely why the
landed guard now pins the registry's size AND its exact id list, so the number cannot be wrong in
the code without a red.

### 12.6 The guard's stated reach now equals its measured reach

Two structural fixes to `GoonPracticeHeadlessTests`, and then a stated bound rather than a chase:

- **Both arms are BOUNDED.** `linuxClause` used to be `voiceText[linuxAt..]` — running to
  end-of-string with a positive needle only, leaving every byte after the first `ON LINUX`,
  including the whole OwnerGate block, unconstrained. It is now `voiceText[linuxAt..tailAt]`, with
  the residual as its own third window.
- **The negatives are SYMMETRIC.** `windowsClause` must not contain `can still ask you` and
  `linuxClause` must not contain `answers it DENY`.
- **Each marker must occur EXACTLY ONCE** (`ON WINDOWS`, `ON LINUX`, `On neither`), so a decoy or
  an append cannot pre-empt `IndexOf` and silently move every window measured from it.

**And the residual is stated, not chased.** This guard detects **DRIFT** — the code moves and the
sentence does not — which is the threat model it was built for and the one the red-watch
demonstrates. It does **not** detect a **deliberate rewrite** of the sentence into a false one, and
it cannot: token presence and ordering cannot encode ATTRIBUTION, and *"answers it DENY"* is a
substring of *"no code path answers it DENY"*. An adversarial pass found further false-but-passing
variants. Chasing them with more needles is the matcher this packet already argued against, and
that argument holds against my own guard as much as against a hypothetical one.

**One more correction of attribution:** the polarity pin lives in `GoonPracticeHeadlessTests`, NOT
in `UserFacingClaimTests`. The unit `VoiceVerdict` is unordered, unscoped `Contains` and passes on
an inverted sentence. §10's Run 3 is evidence about the headless half only, and now says so.