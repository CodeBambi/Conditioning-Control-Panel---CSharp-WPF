{ "packet": "SP-129-goon-game-census", "unit": 32, "headless": 0, "reason": "GoonGameCensusTests pins the Goon Game census against the shipping bytes: directory counts, the shipped-user-path closure, line-exact citations, the fraction table, a numeric-token sweep whose two sides strip the same classes, a label tally re-derived from the map's own rows, a port-anchor citation check, and a table-structure guard (markdown fails silently, so a malformed table renders as invisible prose)" }
