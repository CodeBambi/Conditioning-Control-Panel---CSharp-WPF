{ "packet": "SP-129-goon-game-census", "unit": 28, "headless": 0, "reason": "GoonGameCensusTests pins the Goon Game census against the shipping bytes: directory counts, the shipped-user-path closure, line-exact citations, the fraction table, a numeric-token sweep whose two sides strip the same classes, and a label tally re-derived from the map's own rows" }
