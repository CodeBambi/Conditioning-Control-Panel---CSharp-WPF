<Project Sdk="Microsoft.NET.Sdk">

  <!-- SP-032 Step-3 evidence harness — scratch console, NOT in client/CcpClient.sln
       (SP-029 precedent). Drives the REAL BarkPipeline on the REAL SoundArbitration over
       the REAL SoundFlow backend; transcript is the backend-event evidence. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>12.0</LangVersion>
    <AssemblyName>sp032-harness</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\..\client\src\CcpClient.Desktop\CcpClient.Desktop.csproj" />
  </ItemGroup>

</Project>
