SP-072 pre-fix observation — captured 2026-08-14, lane worktree spine-20260814T090009/lane-1,
BEFORE any product change (HEAD = 02ae3017, the wave-29 authoring commit).

METHOD: spine-tasks/SP-072-createplayer-orphan-disposal/evidence/probe/Probe.cs, run from a
TEMP copy (no build artifacts in the repo) against CcpClient.Desktop. The probe drives the
one seam a headless run can reach — SoundArbitration.PlaySfx with a parking IAudioBackend
fake whose CreatePlayer blocks on a ManualResetEventSlim (the wedged AssetDataProvider
stand-in). The REAL backends (SoundFlowAudioBackend / SoundFlowDtrhAudio) cannot be
constructed headless (real SoundFlow engine + playback device), so the real
`_device.MasterMixer.AddComponent(player)` lines (SoundFlowAudioBackend.cs:118,
SoundFlowDtrhAudio.cs:112) are verified by READING only: both CreatePlayerCore bodies call
it UNCONDITIONALLY before returning, and nothing on any path disposes a player whose caller
went away. Difference vs the fake: the fake's "attachment" is the SFX pool add + Play()
inside PlaySfx; the real attach is the mixer AddComponent. The lifecycle shape — construct,
then attach unconditionally, caller blocked for the whole duration — is identical.

CAPTURED OUTPUT (verbatim):

    init: Ready
    observation @2s: caller thread alive=True (blocked inside CreatePlayer — no bound exists), players constructed=0
    probe: releasing the wedge NOW — the moment for this sound passed 2s ago
    caller: PlaySfx returned Started after 2015ms (only because the probe released the wedge)
    after release: outcome=Started, player.Playing=True, player.Stopped=False, player.Disposed=False
    VERDICT: a construction that completes after its moment passes is attached and played TODAY;
    a caller that had stopped waiting would leave it attached, playing, and never disposed (orphan).

READING:
(1) NO BOUND: the calling thread was still blocked inside CreatePlayer 2 s in; on a wedged
    endpoint (SP-071's dead-endpoint window) that block is forever, and SP-070 established
    the calling thread can be the UI thread.
(2) NO ORPHAN SAFETY: when the wedge cleared 2 s late, the construction proceeded straight
    to attachment and playback (outcome=Started, Playing=True) — a ghost play. Had the
    caller stopped waiting, nothing anywhere would have disposed the player (Disposed=False,
    and no code path ever would): a leak whose only possible disposal would race SP-071's
    backgrounded device teardown.
(3) READ-ONLY HALF: both real CreatePlayerCore bodies attach unconditionally
    (`_device!.MasterMixer.AddComponent(player)` — SoundFlowAudioBackend.cs:118,
    SoundFlowDtrhAudio.cs:112); neither backend's Dispose takes any lock, so a late
    disposal would run concurrently with `_device.Stop()/Dispose()` + `_engine.Dispose()`.
