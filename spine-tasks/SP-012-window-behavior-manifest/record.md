# SP-012 record — build per-window behavior manifest

Worker session log. Enumeration method + raw inventory, classification decisions, consult verdicts (provenance), engine-review presence per call, demonstrator evidence, surprises.

## Engine-review presence log (T-2 watch — packet requires explicit presence/absence per call)

| Step | spine_review_step call | Result | Engine review fired? |
|------|------------------------|--------|----------------------|
| 1 | type=plan after step-1 commit | `skipped: true`, `spawnFailed: false` — "Nested reviewer spawn blocked inside pi worker session (SP-195); batch engine runs reviews after worker success" | NO in-worker (by design, SP-195); post-.DONE engine review is the orchestrator-observable check |
| 2 | type=plan after step-2 commit | `skipped: true`, `spawnFailed: false` (same SP-195 skip; reviewLevel echoed 2 — heading parsed) | NO in-worker |
| 3 | type=plan after step-3 commit | `skipped: true`, `spawnFailed: false` (same SP-195 skip) | NO in-worker |

(Rows for steps 4–5 are appended ONLY after the actual calls happen — a presence log logs events that happened (pre-completion consult correction).)

**In-worker skip = documented design (SP-195), NOT the T-2 regression** (reviewLevel echoed 2 in every response → the structured heading parses). Post-.DONE engine code+final review presence is observable only at land time; SP-011 proved reviews fire with the heading. If they skip post-.DONE here, that IS the regression — say so at land.

## Step 1 — mechanical enumeration (completeness-checkable)

### Method (auditable, re-runnable)

Three sweeps over `ConditioningControlPanel/` (READ-ONLY), excluding `CCP.*/` (first-attempt), `tests/`, `bin/`, `obj/`:

1. **XAML window roots:** every `*.xaml` whose root element is `<Window` (regex `^\s*<Window[\s>]` multiline). Sanity sweep for custom-root windows (`NavigationWindow`/`RibbonWindow`/`MetroWindow`): **zero**. Sanity sweep for `x:Class` XAML files with non-Window roots: only `Windows/FeatureSettingsPopup.xaml` (`<UserControl>` — not a window; its nested code-only `PhraseInputDialog : Window` IS counted in sweep 2).
2. **Code-only `Window` subclasses:** grep `class\s+\w+\s*:\s*(System\.Windows\.)?Window\b` over `*.cs`; partial-class companion files (`AvatarTubeWindow.*.cs`) deduplicated to their XAML window; nested/internal classes kept (`FlashWindow`, `BouncingTextWindow`, `PhraseInputDialog`).
3. **Ad-hoc `new Window` sites:** grep `new Window\s*$|new Window\s*[({]` over `*.cs` (both single-line and multiline initializer forms).
4. **Transitive subclass sweep (pre-approach consult correction b):** grep `class X : Y` where Y is any of the 101 enumerated window class names → **ZERO indirect subclasses**. Inventory is closed under inheritance.

Raw counts: **76 XAML windows + 25 code-only subclasses + 39 ad-hoc sites = 140 surfaces.** 139 after deduplicating `MainWindow.Browser.cs:2092/2256` (two creation sites for the same browser-popout surface) → **139 distinct surfaces.**

### Raw inventory

**XAML windows (76):** AvatarTube/AvatarTubeWindow; Chaos/ChaosHubWindow, ChaosHudWindow, ChaosOverlayWindow, ChaosSlotPickerWindow; Controls/SeasonRecapWindow; Dialogs/(35): AssetSubmitDialog, AttentionCheckSettingsDialog, AttentionTargetEditorDialog, AwarenessPresetDetailDialog, CataloguePickerDialog, CatalogueSubmitDialog, ChatShortcutCaptureDialog, ColorEditorDialog, CompanionPhraseEditorDialog, CompanionPromptEditorDialog, ContentPolicyWarningDialog, DisplayNameDialog, ExplicitContentAcknowledgementDialog, InputDialog, KnowledgeLinkEditorDialog, LinkPhoneDialog, LocalAiSetupWizard, LockCardColorDialog, LoginDialog, MicConsentDialog, ModManagerDialog, OfflineUsernameDialog, OpenAiCompatibleSamplerSettingsDialog, RoadmapConfirmDialog, RoadmapDiaryDialog, RoadmapStartDialog, RoadmapStepPopup, TextEditorDialog, UpdateNotificationDialog, UpdateProgressDialog, UsernamePickerDialog, WarningDialog, WebcamConsentDialog, WelcomeDialog, WhatsNewDialog; Features/FeaturePopupWindow; Lab/GazeMinigame/GazeMinigameWindow; MainWindow/MainWindow; Views/Deeper/(5): DeeperEditorWindow, EnhancementPlayerWindow, GazePickerWindow, NewEnhancementDialog, UrlPromptDialog; Windows/(27): AchievementPopup, AnnouncementPopup, BubbleCountResultWindow, BubbleCountWindow, BugReportWindow, EasterEggWindow, HapticsSetupWindow, HelpVideoWindow, LockCardWindow, MantraWindow, MiniPlayerWindow, ModCreatorWindow, PinkRushPopup, PopQuizWindow, QuestCompletePopup, QuizCategoryEditorWindow, QuizReportWindow, QuizWindow, SessionCompleteWindow, SessionEditorWindow, SessionLogHistoryWindow, SplashScreen, TutorialOverlay, WebcamCalibrationWindow, WebcamGazeTrackerWindow, WebcamLoadingSplash, WebcamQuickRecalWindow.

**Code-only subclasses (25):** Chaos/(19): ChaosAnnouncerOverlay, ChaosBoonBarOverlay, ChaosBubbleHostOverlay, ChaosCursorGlowOverlay, ChaosDvdHostOverlay, ChaosDvdOverlay, ChaosEStimOverlay, ChaosEffectBannerOverlay, ChaosFieldFxOverlay, ChaosFlashOverlay, ChaosFxWindow, ChaosGifCascadeOverlay, ChaosIntroWindow, ChaosPopText, ChaosSkiaFxOverlay, ChaosToyButtonWindow, ChaosUnlockCardOverlay, ChaosVibeTrailOverlay, ChaosWaveTimerOverlay; Overlays/(2): SpeakCueOverlay, TakeoverAnnouncerOverlay; Services/Compositor/CompositorHostWindow; Services/Flash/FlashService.cs:2901 `FlashWindow`; Services/Subliminal/BouncingTextService.cs:554 `BouncingTextWindow`; Windows/FeatureSettingsPopup.xaml.cs:628 `PhraseInputDialog`.

**Ad-hoc `new Window` sites (39, one deduplicated pair):** AvatarTube/AvatarRandomBubble.cs:269; Chaos/ChaosBackdropService.cs:178, ChaosHubWindow.xaml.cs:1417 (Diary popout), ChaosTunnelService.cs:182, ChaosWebViewHost.cs:106; Dialogs/CompanionPhraseEditorDialog.xaml.cs:375 ("Add Custom Phrase"); MainWindow/MainWindow.Browser.cs:2092+2256 (browser popout, one surface), MainWindow.Enhancements.cs:2204 (pink flash) + :2312, MainWindow.Marquee.cs:83 (welcome-back), MainWindow.Presets.cs:567 (`ShowStyledDialog` helper — one surface, many call titles) + :675 (media-drop); Services/AttentionCheckService.cs:327, BlinkTrainerService.cs:662, BubbleCountService.cs:355, BubbleService.cs:2099, Chaos/DtrhSpike.cs:119, Haptics/MockHapticProvider.cs:101, KeywordHighlightService.cs:110, Notifications/OverlayService.cs:990 (tint) + :1507 (spiral) + :1585 + :1973, Session/SessionEngine.cs:1367 (corner GIF), Subliminal/SubliminalService.cs:928, Tracking/GazeDebugCursorService.cs:127, Video/DualMonitorVideoService.cs:410, Video/VideoService.cs:1219 + :1614 + :1929 + :1965 + :2064 + :2538 + :3905 + :4018; Views/Deeper/DeeperEditorWindow.xaml.cs:774 (browser preview popout), EnhancementPlayerWindow.xaml.cs:1715 (video browser popout); Windows/BubbleCountWindow.xaml.cs:1097.

### Classification (full table lives in the manifest §2)

- **RETAINED (55 named manifest rows):** MainWindow; FeaturePopupWindow; 36 XAML owned-modal dialogs; 5 ad-hoc code dialogs (welcome-back Marquee.cs:83, styled-confirm `ShowStyledDialog` Presets.cs:567, media-drop Presets.cs:675, add-custom-phrase CompanionPhraseEditorDialog.xaml.cs:375, Chaos diary popout ChaosHubWindow.xaml.cs:1417); standalone editors/tools (ChaosHubWindow, ChaosIntroWindow, DeeperEditorWindow, EnhancementPlayerWindow, ModCreatorWindow, GazeMinigameWindow, MiniPlayerWindow, QuizWindow, QuizReportWindow, browser popout Browser.cs:2092/2256, HelpVideoWindow, DeeperEditor browser-preview popout :774, EnhancementPlayer video-browser popout :1715); interactive topmost attention surfaces (LockCardWindow, BubbleCountWindow, BubbleCountResultWindow, PopQuizWindow, SessionCompleteWindow, WebcamCalibrationWindow, WebcamQuickRecalWindow, WebcamGazeTrackerWindow, GazePickerWindow); non-activating notifications (AchievementPopup, AnnouncementPopup, PinkRushPopup, QuestCompletePopup, RoadmapStepPopup); startup/progress (SplashScreen, WebcamLoadingSplash, WelcomeDialog, UpdateProgressDialog); code-only PhraseInputDialog.
- **CROSS-REFERENCED (named, no field rows — dedicated rows own behavior):** AvatarTubeWindow + AvatarRandomBubble.cs:269 → board row "Prove AvatarTube rendered animation"; ChaosWebViewHost.cs:106 + ChaosSlotPickerWindow + DtrhSpike.cs:119 → SP-011 spike + rows "Admit DTRH browser and origin design" / "Implement web-only DTRH host" (save picker is literally in the host row's acceptance); VideoService.cs:1219/1614/1929/1965/2064/2538/3905 + DualMonitorVideoService.cs:410 → board row "Implement unified fullscreen video presentation" (BLOCKED, exists); ~45 overlay surfaces (19 Chaos*Overlay + ChaosPopText + ChaosFxWindow + ChaosToyButtonWindow + ChaosHudWindow + ChaosOverlayWindow + SpeakCueOverlay + TakeoverAnnouncerOverlay + CompositorHostWindow + FlashWindow + BouncingTextWindow + TutorialOverlay + OverlayService ×4 + SubliminalService:928 + SessionEngine:1367 + KeywordHighlightService:110 + GazeDebugCursorService:127 + AttentionCheckService:327 + BlinkTrainerService:662 + BubbleCountService:355 + BubbleService:2099 + BubbleCountWindow.xaml.cs:1097 + ChaosBackdropService:178 + ChaosTunnelService:182 + Enhancements.cs:2204/2312) → **overlay-clickthrough skill scope; NO board row authored yet** (consult correction a1 — cross-ref targets must be real rows or named as absent).
- **EXCLUDED-with-reason (5):** AttentionCheckSettingsDialog (dead — zero construction sites; only comment at App.xaml.cs:1571); MantraWindow (dead — zero construction sites; comments only at App.xaml.cs:689,755; mantra feature is voice-only via MantraService); MockHapticProvider.cs:101 toast (mock/dev provider surface, not product); VideoService.cs:4018 dummy collapsed window (invisible WPF trick, never a user surface); FeatureSettingsPopup.xaml (UserControl, not a window — nested PhraseInputDialog retained separately).

Re-runnable dead-window check: `grep -rn --include="*.cs" --include="*.xaml" "AttentionCheckSettingsDialog\|MantraWindow" .` (excluding CCP.*/tests/bin/obj) → comment/self references only.

## Pre-approach consult (solo; council unavailable per failed probe — packet honesty framing)

**Requested route:** solo consult, default project route. **First call failed:** "Council synthesizer returned no usable text" (bare `consult` defaulted into the broken council path — T-7 route still broken; retried with explicit `mode: "solo"`). **Answering model (provenance per T-7):** Fable family (solo default; the consult tool does not echo a model ID — recorded as "solo Fable-family per project route", consistent with SP-011 provenance practice). First solo reply **truncated** mid-Q1 (known Fable behavior); corrections/gap/Q2–Q4 recovered complete via pointed follow-up.

**Verdict (2026-07-20, condensed): "classification and format are sound — proceed, with two corrections and one verification gap."**

1. **Q1 ad-hoc dialogs: own rows, named by site.** The acceptance is one named row per retained window; an ad-hoc `new Window` has its own modality/owner/placement contract distinct from MainWindow's. Name by site since no class name exists.
2. **Correction 1 — cross-ref targets must be real rows, not implied ones.** The overlay bucket has NO dedicated board row — write "overlay-clickthrough skill scope; no board row authored yet". Real rows cited exactly: video windows → "Implement unified fullscreen video presentation" (exists, BLOCKED); ChaosSlotPickerWindow → "Implement web-only DTRH host" (acceptance covers the save picker); ChaosWebViewHost → SP-011 spike + admit row.
3. **Correction 2 — conditional/parameterized values record the governing expression, never a flattened boolean.** `Topmost = topmost` (HelpVideoWindow call-site parameter), `Topmost = ChaosWindowZ.BornTopmost` (Chaos windows), `ShowActivated = withAudio` (VideoService), `Owner = IsLoaded ? this : null` (SessionCompleteWindow), AvatarTube Topmost toggling — record the expression + call-site cite + what drives it.
4. **Gap closed — transitive subclass sweep** (`class X : Y` where Y ∈ enumerated window classes): ZERO indirect subclasses (recorded above). **Presets.cs:567 Owner resolved:** `ShowStyledDialog` helper — `Owner = this` inside try/catch (`InvalidOperationException` → CenterScreen fallback, issue #500 comment); the row records the conditional.
5. **Q2 no flips:** TutorialOverlay + ChaosToyButtonWindow → overlay scope (non-activation/Z-order/region-input policy = that skill's domain); GazePickerWindow RETAINED (purposeful picker, not an effect); both dead-window exclusions stand; state the re-runnable command (done above).
6. **Q3 shared procedures acceptable, with per-category variants** where the check fundamentally differs (activation assert-focus-did-NOT-move for ShowActivated=False notifications; modal vs modeless modality checks; multi-monitor variant for LockCardWindow-style per-monitor surfaces); per-row applicability notes only where the generic procedure doesn't apply.
7. **Q4 matrix columns confirmed:** Windows (observed), WSLg/X11 (observed — session facts only), distro/backend/WM "pending owner question". **No Wayland column — its absence IS the §5.1 posture.**

## Step 2 — manifest authoring

- `client/docs/window-behavior-manifest.md` authored: §0 honesty framings (named gate for exercise-every-row; code-derived evidence class; owner matrix question unanswered; observation-only demonstrator); §1 method + reading rules (defaults `d(...)`, conditional-expression rule, Alt-Tab expectation rule, the not-set-ShowInTaskbar note); §2 classification of all 139 surfaces (56 cross-ref with REAL row targets per consult correction — overlay bucket honestly named "skill scope; no board row authored yet"; 4 exclusions + 1 non-window note); §3 observation procedure per field (Windows headed/UIA/Win32; Linux xprop/wmctrl/XGetImage; WSLg input-injection limits named; per-category variants); §4 79 retained rows in 6 category tables (W-01…W-79), every field with `File.cs:line` evidence; §5 platform matrix (Windows + WSLg/X11 observed; distro/backend/WM pending owner question; NO Wayland column — §5.1 untouched); §6 ten shared-chrome constraints (constraints only, no design).
- Author-time corrections: the BubbleCount per-bubble host was initially drafted as a retained row, then correctly attributed to the overlay cross-ref set and the ID tables renumbered cleanly (W-01…W-79, 3+13+45+9+5+4 = 79); §2.3 count paragraph rewritten to match.

## Step 3 — dashboard observability demonstrator (observation-only)

**Windows (headed, RV):** `scratch/window-demo.ps1` against the Debug dashboard exe. Results (`artifacts/windows-demo-transcript.txt`): GW_OWNER=0; opens UNACTIVATED (foreground = another window, UIA FocusedElement = 'Recycle Bin' — SP-007 surprise #2 re-observed); WS_EX_TOPMOST absent; WS_EX_APPWINDOW present + UIA taskbar button found ('CcpClient.Desktop - 1 running window'); full native chrome (WS_CAPTION/THICKFRAME/MIN/MAX/SYSMENU); GetWindowRect (208,208) 536×719; Alt-Tab switcher capture shows the CCP Client thumbnail (verified by reviewing `artifacts/windows-alttab-switcher.png`); **right-edge drag +80px → width 536→616 (behavior-level resize proof)**; `CloseMainWindow` → **exit 0**. Captures: `artifacts/windows-dashboard-window.png`, `windows-alttab-switcher.png`.

**WSLg/X11 (session facts, never backend claims):** `scratch/wslg-demo.sh` (rsync → native ~/ccp-sp012 build, never /mnt/e for build/run). Session: WAYLAND_DISPLAY=wayland-0, DISPLAY=:0, XDG_SESSION_TYPE unset, kernel 6.6.114.1-microsoft-standard-WSL2, Ubuntu 26.04, SDK 10.0.110. Window found via `xwininfo -root -tree` (0x60000f "CCP Client" 520×680+38+59) — **`_NET_CLIENT_LIST` does not exist on the WSLg XWayland root** ("no such atom on any window"); `_NET_SUPPORTED` advertises only MOVERESIZE/STATE/STATE_FULLSCREEN/MAXIMIZED_*/ACTIVE_WINDOW/FRAME_EXTENTS. Properties (xprop): `_NET_WM_WINDOW_TYPE=NORMAL`; `_NET_WM_STATE` empty (no ABOVE); `WM_TRANSIENT_FOR` not found; `WM_NORMAL_HINTS` min 1×1 increment 1; `_NET_ACTIVE_WINDOW` = our window (session activated it — honest difference vs Windows unactivated); `WM_PROTOCOLS` advertises WM_DELETE_WINDOW; `_NET_FRAME_EXTENTS` 38,38,59,38. Capture: `artifacts/wslg-dashboard-window.bmp` (XGetImage — renders for real, session-facts line visible). Graceful close: `wmclose.py` → **exit 0** (wait on the real PID, second run). Modality: "procedure defined, not demonstrable on this window" both platforms. Resize drag + taskbar `_NET_CLIENT_LIST` procedure: defined, not demonstrable on WSLg (no xdotool; atom absent) — recorded honestly.

**Surprise (durable):** WSLg's XWayland root has NO `_NET_CLIENT_LIST` — the manifest §3 Linux taskbar procedure must not assume it (already written that way; the demonstrator confirms the caveat). Candidate for port-lessons if the owner row wants it.

## Step 4 — board reconciliation + pre-completion consult

- Board row "Build per-window behavior manifest" → **WIP** with evidence citing the manifest + record; two named remaining gates recorded (exercise-every-row before shared chrome; owner matrix question); never DONE; original acceptance text preserved verbatim (annotate-don't-rewrite).

### Pre-completion consult (solo, Fable family per project route — actual answering model not echoed by the tool; recorded per T-7 practice)

Full as-built summary + diff shape submitted. Verdict received **complete** (no truncation): **"the shape is sound and honestly scoped — proceed to Step 5 and .DONE after two small corrections. No rework of the manifest body."**

- Q1 (d(true) taskbar reading / W-16 dual mode): **both correct** — `ShowInTaskbar` defaults true and WPF gives owned windows a taskbar button when true (exactly why the codebase sets false explicitly on FeaturePopupWindow and a handful of others); W-16's dual modality is directly evidenced (`ShowDialog` at Awareness.cs:627/937/976, `.Show()` at Settings.cs:853) and is the strongest justification for shared-chrome constraint #9.
- Q2 (9/10 vs 7/10 accounting): **honest and conservative** — undercounting rather than overcounting (resize counted unproven on WSLg despite WM_NORMAL_HINTS; taskbar unproven despite tree enumeration).
- **Correction 1 (APPLIED):** board wording "78 of 79 windows do not exist" implicitly claimed the dashboard IS W-01's greenfield instance — it isn't (W-01 is `WindowStyle.None` custom-chrome CanResizeWithGrip minimize-to-tray; the dashboard is a default-chrome 520×680 demonstrator surface; the §7 demonstrator proves procedures, discharges no row). Reworded in the board AND in manifest §0.1 (which had the same "the other 78" defect): none of the 79 manifest windows exists; the dashboard is a new surface used only to demonstrate procedures; no manifest row (including W-01) is discharged.
- **Correction 2 (APPLIED):** record.md engine-review table rows 4–5 were pre-recorded before the calls happened — a presence log must log events that happened. Rows removed; steps 4–5 rows are appended only after the actual calls.
- Q3 (blocking .DONE): nothing structural. Sequence followed: corrections → this verdict persisted before checkbox → Step 5 full contract testCommand (build + BOTH test projects — only the app project was built during the demonstrator) → `git diff --check` → scope audit (gitignored `client/tools/verify/artifacts/` stays out; `~/ccp-sp012` stays WSL-side) → STATUS accurate → .DONE → exit immediately.
- Non-blocking note: the WSLg `_NET_CLIENT_LIST`-absent finding is genuinely durable (constrains every future Linux window-behavior gate on WSLg); port-lessons harvesting left to the orchestrator (port-lessons.md is outside this packet's File Scope).

