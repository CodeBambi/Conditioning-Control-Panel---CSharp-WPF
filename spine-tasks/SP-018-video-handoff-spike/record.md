# SP-018 record — spike browser-to-native online-video handoff

Task packet: `spine-tasks/SP-018-video-handoff-spike/PROMPT.md` · Board row: "Spike browser-to-native online-video handoff" (P0, Phase-3 final row) · Review Level 2 (plan review per step in-worker; code review engine-side after .DONE).

Engine-review presence log (T-2 heading format load-bearing): appended per `spine_review_step` call below.

## Step 1 — Video archaeology + source-matrix + decoder candidates

### 1.1 WPF archaeology (READ-ONLY, File.cs:line)

How online/video sources reach the native player today (LibVLCSharp.WPF 3.8.5 + VideoLAN.LibVLC.Windows 3.0.21 — `ConditioningControlPanel/ConditioningControlPanel.csproj:49,77`):

- **Direct URL handoff, no middleware (VERIFIED):** `Services/Video/VideoService.cs:1341` — `new Media(_libVLC!, url, FromType.FromLocation)`; same shape for dual-monitor (`Services/Video/DualMonitorVideoService.cs:120`). Bare HTTP/HTTPS string straight into libvlc's network stack.
- **No headers/cookies anywhere (VERIFIED):** grep of `Services/Video/*.cs` and the whole WPF tree for `:http-referrer`, `:http-cookie`, `addOption` — the only media option is `:no-audio` on secondary players (`VideoService.cs:1343`). WPF never authenticates a media request.
- **No expiry/refresh/retry (VERIFIED):** `VideoService.cs:1301-1320` and `:1783-1800` — `EncounteredError` logs + cancels + cleans up; `DualMonitorVideoService.cs:108` raises `PlaybackError`. One failure = one cleanup. No token renewal, no retry.
- **Limitation reporting is log-only for URLs (VERIFIED):** user-visible dialogs exist only for empty lists (`VideoService.cs:984`) and missing codecs (`VideoService.cs:2019-2024`); network/auth/expiry/DRM failures appear only in logs (`:1303`, `:1785`).
- **Contract surface (VERIFIED):** `PlayUrl(string url)` exists on the first-attempt-era `CCP.Core/Services/Video/IVideoService.cs:27` (lessons-only reference, not a product template).

### 1.2 First-attempt lessons (READ-ONLY, cited)

- `client/docs/first-attempt-lessons.md` ~470-476 (REJECT lesson, honored): "Online-source extraction is a separate gated capability; unsupported `blob:`, MSE, authenticated, or DRM playback reports a limitation instead of falling back to browser rendering." — this spike IS that gating capability's evidence pass.
- `first-attempt-lessons.md` ~498 (REJECT, honored): fullscreen browser presentation, screen-capture mirroring, non-uniform stretch, independent per-monitor decoders, silent no-op degradation — all forbidden as fallbacks; this spike reports typed limitations instead.
- `CCP.Core/Services/AIService/KnownVideoLinks.cs:31-75` — ~42 hardcoded `hypnotube.com` + 8 `bambicloud.com` URLs. NOT a test-vector source for this spike (real-site shapes untested by design; target sites = owned/public test content only per packet).
- `CCP.Core/Services/Video/VideoMetadataCache.cs:59,81` — duration cache uses `FromType.FromPath` (local only); no online caching pattern to inherit.

### 1.3 Source matrix definition (acceptance rows → fixture design)

All fixtures served by a spike-owned loopback lab (SP-011 `LoopbackServer.cs` pattern: GET-only, 127.0.0.1, ephemeral ports). Media bytes = tiny synthetic clips generated locally by the spike (no copyrighted fixtures); HLS/DASH served as manifests over the same generated bytes (EXT-X-BYTERANGE / SegmentBase over a single resource — no ffmpeg dependency), with a segmented variant if the decoder rejects byte-range playlists.

| # | Matrix row | Fixture design | Success shape | Failure/limitation shape |
|---|-----------|----------------|---------------|--------------------------|
| M1 | Direct MP4 | loopback `/media/clip.mp4` | native decode events: track metadata (codec/res/duration) + time progression + end event | unreachable → typed `source-unreachable` |
| M2 | Direct WebM | loopback `/media/clip.webm` | same | same |
| M3 | HLS manifest | loopback `/hls/vod.m3u8` (byte-range or segmented over generated media) | same | malformed playlist → typed `manifest-invalid` |
| M4 | DASH manifest | loopback `/dash/vod.mpd` (SegmentBase single-resource) | same | same |
| M5 | Target-site shape | owned loopback page embedding a `<video src>` (the ONLY site shape exercised; no commercial scraping) | discovery → transfer → decode events | indiscoverable → typed `source-not-extractable` |
| M6 | Cookies | loopback `/gated-cookie/clip.mp4` requires cookie; gate 401/403 without | decode events ONLY when cookie transfers | negative control: no-cookie → typed `auth-required` (401 observed, no retry-storm) |
| M7 | Custom headers | loopback `/gated-header/clip.mp4` requires `X-Spike-Gate` header | same | same negative control |
| M8 | Expiring signed URL | loopback `/signed/clip.mp4?exp=..&sig=..` HMAC TTL; valid + pre-expired shapes | valid → decode events | expired → typed `source-expired` (403, no retry) |
| M9 | `blob:`/MSE | fixture page with MSE-appended `blob:` video | transfer IMPOSSIBLE by construction → typed `blob-untransferable` with detection evidence (protocol is `blob:`) | never browser-fullscreen/capture fallback |
| M10 | DRM | fixture page/endpoint with EME signaling (fake ClearKey-style `requestMediaKeySystemAccess` probe + EME attributes); DRM-labeled manifest | detect → typed `drm-detected` limitation | NEVER bypass/key-extraction/capture; asserted in logs |

Decoder-event verification rule (packet honesty framing f): success claims come ONLY from native-decoder events (ParsedChanged/track metadata, TimeChanged progression, EndReached), never from HTTP 200 or call returns.

### 1.4 Native decoder candidates (live-feed verified 2026-07-21)

| Candidate | Exact version | Published | License | Native deps | Notes |
|-----------|---------------|-----------|---------|-------------|-------|
| **LibVLCSharp** (PRIMARY — WPF incumbent) | 3.10.0 | 2026-06-17 | LGPL-2.1-or-later | none bundled | net10.0 TFM group; flatcontainer index 3.6.5→3.10.0; WPF pins 3.8.5 (2024-05-23) |
| **VideoLAN.LibVLC.Windows** (native pair) | 3.0.23.1 | 2026-04-16 | LGPL-2.1-or-later | bundled libvlc per-RID | WPF pins 3.0.21 (2024-09-16) |
| Linux native | distro `libvlc`/`vlc` via apt (CI already does this — `.github/workflows/build.yml` installs `libvlc-dev vlc`) | — | LGPL-2.1-or-later | apt packages | NO official `VideoLAN.LibVLC.Linux.*` nuget exists (flatcontainer 404 verified) |
| FFmpeg.AutoGen (maintained alternative, doc-level) | 8.1.0 | 2026-04-28 | license-file (bindings LGPL; FFmpeg natives LGPL/GPL build-dependent) | external FFmpeg builds needed (no official native nuget) | full demux/decode control incl. custom AVIO with arbitrary headers — but hand-rolled demux pipeline is spike-overkill vs LibVLC's one-call URL open |
| Rejected without admission: mpv.net/libmpv (GPL native; stale bindings), MF (Windows-only), GStreamerSharp (stale) | | | | | |

Header/cookie capability note: libvlc media options support `:http-user-agent` / `:http-referrer` / `:http-forward-cookies`; arbitrary custom headers/cookies are NOT directly settable → spike design: loopback **relay** endpoint (decoder opens `http://127.0.0.1/relay/...`, relay injects cookie/header upstream) as the transfer strategy for M6/M7 if direct options prove insufficient — the relay is itself spike-owned loopback, evidence of strategy, not product code.

### 1.5 Package admission gate — solo Fable 5 consult (BEFORE Step 2)

**Verdict: ADMITTED (2026-07-21).** Route: solo consult, anthropic/claude-fable-5 (per board gate history — council broken/T-7, solo-Fable sanctioned; model not self-identified in output, recorded honestly).

(a) **Admission: NO BLOCKER.** LibVLCSharp 3.10.0 + VideoLAN.LibVLC.Windows 3.0.23.1 admitted for the quarantined spike. SP-017's LibVLCSharp rejection was backend-shape-for-AUDIO (per-instance player, no sample mixer) and does not apply — per-instance decode is exactly the video-handoff shape. Cautions: (i) version skew — spike facts are 3.10.0/3.0.23.1-specific while WPF pins 3.8.5/3.0.21; decoder selection for the unified-video row stays pending-owner; (ii) record the exact apt libvlc version on WSL2 at runtime (skew vs 3.10.0 binding); (iii) LGPL-2.1-or-later sidecar/relink obligations are an SP-010 packaging note, pending-owner, not a spike blocker.

(b) **Relay: honest ONLY with split evidence classes.** The relay must never be reported as "direct transfer to native decoder": the decoder sees a loopback URL, so the claim is proxy-mediated auth, not decoder auth. Matrix splits M6/M7 into: **direct-decoder-auth** (libvlc media options — expected to FAIL for arbitrary cookies: libvlc 3.x exposes `:http-user-agent`/`:http-referrer`/`:http-forward-cookies` but no `:http-cookie`; the negative result IS the honest evidence) vs **proxy-mediated-auth** (relay injection; strategy evidence, pending-owner admission — a local proxy holding authenticated media is a real architecture/security decision). Negative control sits at the UPSTREAM gate: relay WITHOUT injection must propagate 401/403 to the decoder and fail; relay WITH injection must produce decode events. Relay's own logs are inside the --audit-logs scope (the relay is where the secret value lives).

(c) **Vacuous-evidence traps called out (all incorporated):**
1. **Synthetic-media generation is the real Step-2 blocker:** hand-rolled MP4 is hard; WebM can NEVER be an HLS segment (HLS = TS or fMP4 only); EXT-X-BYTERANGE needs EXT-X-VERSION≥4 and fMP4 needs EXT-X-MAP — a regular MP4 is NOT an fMP4 segment; decoder leniency risk (libvlc may play malformed playlists — report the actual playlist shape served). Resolution: use ffmpeg ONLY at fixture-generation time if present on the box (testsrc2/lavfi = license-safe; generated bytes cached in gitignored scratch, regenerated per run — no ffmpeg needed to RUN the spike); fallback = hand-rolled MJPEG-in-MP4; repo-asset reuse (DTRH spiral.webm, READ-ONLY) as a WebM body candidate. Settle generation before lab endpoints.
2. **M9 circularity:** the discovery layer must actually DETECT the `blob:` protocol on the discovered src — the typed `blob-untransferable` report must carry that detection evidence, not an asserted impossibility.
3. **M10 detection trigger:** `encrypted` media event and/or observed `requestMediaKeySystemAccess` usage — NOT mere EME API presence.
4. **Typed limitations come from a lab PREFLIGHT probe layer:** libvlc `EncounteredError` is generic (no 401-vs-403 distinction); classification (auth-required/expired/manifest-invalid) is preflight-derived; preflight logging is redaction-safe and inside the audit scope.
5. **Pre-declared success thresholds:** time-progression deltas and EndReached window declared BEFORE runs (mirrors SP-017 pre-declared tolerances), never interpreted post-hoc.
6. **M8 named limit added:** mid-stream expiry (decoder re-opens on seek after TTL) is untested by design — recorded as a named limit, not silently absent.
7. **Headless decode:** probe uses libvlc dummy video output (no presentation — presentation is the unified-video row's scope); video track must NOT be disabled (track metadata is the evidence).

Step-3 browser package named in admission: SP-011-admitted `Avalonia.Controls.WebView` 12.0.1 (MIT) — reuse, no new package.

Engine-review log (T-2): Step 1 `spine_review_step` type=plan → **SKIPPED in-worker by design (SP-195)** (`skipped=true`, `spawnFailed=false`, reviewLevel echoed 2); artifact `1-20260721T112312.md`; engine runs reviews after .DONE.

## Step 2 — Loopback source lab + native decode handoff core (Windows, 2026-07-21)

Spike host `client/spikes/CcpSpike.VideoHandoff/` (console, NOT in the solution; inherits only `client/Directory.Build.props`): `Lab.cs` (GET-only loopback lab, SP-011 pattern), `Probe.cs` (LibVLCSharp decode probe), `Matrix.cs` (pre-declared expectations), `Redact.cs` (secret registry + scrub + audit), `SpikeLog.cs` (JSONL), `fixtures/` (generated media, recipe below).

**Fixtures (consult trap #1 settled):** ffmpeg IS present on this Windows box (gyan.dev full build 2025-06-04) but NOT on WSL2 → fixtures GENERATED ONCE via lavfi (`testsrc2` 96x96@10fps 2s + 440Hz sine; license-safe), COMMITTED under `fixtures/` (deterministic, no runtime ffmpeg need on either platform). SHA-256: clip.mp4 `eb14abd63a02a22029c513a4b512e2cecad34b2b0c9e31994030753c5d769fbc` (+faststart, h264+aac), clip.webm `5b32afa0e0085239359f57701220e9ac6f857c2756dc0d1d6029e04f6fdccdfb` (vp8+vorbis); HLS fMP4 v7 (EXT-X-MAP, per consult — NOT byte-range-over-regular-MP4), HLS TS variant, DASH static MPD (SegmentTemplate fMP4).

**Decode-level matrix 14/14 PASS, exit 0** (`evidence/run-windows.jsonl`, 128 obs): M1 mp4 (h264 96x96, 37 frames, end), M2 webm (VP80, 30 frames), M3 HLS-fMP4 (36 frames), M3b HLS-TS (34), M4 DASH (36), M6 direct-auth FAILS AS PRE-DECLARED (gate log: `presented=absent` ×3 — libvlc 3.x sends NO cookie from `:http-cookie` media option; option silently ignored), M6 relay SUCCESS (gate: `presented=valid` ×5), M6 relay-neg 401 typed auth-required, M7 same shape for custom headers, M8 valid/expired/badsig all typed. **Success = parse track metadata + vmem frame delivery + progression + EndReached against pre-declared thresholds** (dur [1500,2500]ms, frames ≥5, timeChanges ≥3, span ≥1000ms or maxPos ≥0.5, end ≤12s) — never HTTP 200.

**Findings (failures recorded, never patched over):**
- **V1 — D3D11VA hw decode segfaults this box** (libvlc 3.0.23.1 default) even parsing a LOCAL file (d3d11va decoder-setup path, verbose log captured) → probe forces `--avcodec-hw=none` (sw decode). The WPF incumbent plays with hw decode + real vout; the crash is specific to the headless-probe shape. Recorded as a matrix risk for the unified-video row.
- **V2 — dummy-vout sw-decode pipeline crashes ~at/after EndReached on a BACKGROUND libvlc thread** (asynchronous — raced dispose orders, `--no-audio`, WPF-pinned 3.8.5+3.0.21 combo; all crash; `--no-video` stable). Root cause isolated to the vout pipeline: **vmem memory-callback vout is STABLE (selftest 3/3 + matrix 2/2 exit 0)** and yields frame-level decode proof (display-callback count) — strictly stronger evidence than TimeChanged alone.
- **V3 — libvlc native teardown segfaults in this probe shape** (media/player release in ANY order incl. GC-then-release, both version combos) → spike hard-exits (`_exit`) after flushing evidence; all JSONL lines are line-durable. **Clean teardown is owned by the unified-video row** (named limit; presentation-row scope).
- **V4 — HLS playlists expose no tracks at playlist level via LibVLCSharp 3.10 parse API** (sub-items absent for media playlists; debug-logged). Track evidence for HLS = decoder-format-callback observed chroma/dims (`decoded:I420 96x98` — decoder-proposed dims include H.264 coded-height padding) + demuxer duration; codec NAME for HLS not observable through this API (recorded, not faked). DASH parse DOES expose `h264 96x96` directly. Adaptive demuxers report Time=0 → position-based progression evidence (`maxPos`) used per pre-declared threshold.

**Redaction discipline:** central `Redact` registry; every log line scrubbed; lab shapes cookie/header/sig at source (`cookie=present(len=40)` etc.); `--audit-logs` re-registers the accumulated registry (append-per-run — audit covers EVERY emitted log) and FAILS on any secret value. **Audit GREEN on the full evidence run (exit 0, zero hits).** badsig literal `deadbeef` registered for shape-consistency. Relay logs in audit scope (relay is where the secret lives).

## Step 3 — Windows browser→native handoff evidence (2026-07-21)

Browser layer `Browser.cs` (SP-011-pattern Avalonia host, SP-011-admitted `Avalonia.Controls.WebView` 12.0.1, WebView2 150.0.4078.83 Blink; code-only UI on a dedicated STA thread; app.manifest per SP-011 integration consequence). Per row: live-DOM discovery via `InvokeScript` (double-encoded JSON) → transfer → decode-event verification or typed limitation. **7/7 PASS, exit 0** (`evidence/run-windows-browser.jsonl`, 105 obs):

- **B1 site-direct (M5+M1 shape):** discovered `src` off the live element → transferred → h264 96x96, 37 frames, EndReached.
- **B2 signed-valid:** full signed URL discovered from the page → decode-verified.
- **B3 signed-expired:** typed `source-expired` at transfer (one decoder open, no retry-storm).
- **B4 cookie / B5 custom-header:** negative control direct-open → typed `auth-required` (401/403); host-owned credential via relay → decode-verified. Evidence classes SPLIT per consult (direct-decoder-auth vs proxy-mediated-auth; relay = strategy evidence pending-owner).
- **B6 blob:/MSE:** protocol `blob:` read off the LIVE DOM (not asserted); page really fetched + objectURL'd the media (`blob-src-set`) and MSE append SUCCEEDED (`mse-append-ok` — WebM/vp8 SourceBuffer); typed `blob-untransferable` — no decoder attempt, no browser-fullscreen/capture fallback.
- **B7 DRM:** EME USAGE observed (`eme-keysystem-access-granted keySystem=org.w3.clearkey` — requestMediaKeySystemAccess resolved on the page, consult trap 3 honored) → typed `drm-detected`; no bypass/key-extraction/capture attempted (asserted in log).

- **V5 — signed-token URLs persisted in the WebView2 HTTP cache** (`wv2-profile/EBWebView/Default/Cache/Cache_Data/data_1`, 2 sig hits — caught BY the audit on the first browser run). Mitigation: `Cache-Control: no-store` at the SOURCE on `/signed/*`, `/gated-*`, and the signed-embed page; re-run → **audit GREEN over the ENTIRE scratch INCLUDING the wv2-profile**. Product implication for the host row: token-bearing media URLs need no-store at the source and/or profile hygiene (owner decision).
- Decode matrix re-verified after the Lab change: 14/14 PASS exit 0; audit GREEN.

## Step 4 — WSLg/Linux gate (2026-07-21)

In-packet copy `~/ccp-sp018` (tar over wsl, never /mnt/e for build/run; Browser.cs synced via /mnt/e copy only). WSL2 Ubuntu 26.04, SDK 10.0.110, distro **libvlc 3.0.23-1** (apt: libvlc5/libvlccore9 preinstalled — skew vs LibVLCSharp 3.10.0 binding recorded per admission consult).

- **Native decode side REAL:** decode matrix **14/14 PASS exit 0** on WSLg (`evidence/run-wslg.jsonl`) — identical row outcomes to Windows incl. cookie/header relay + negative controls + signed valid/expired/badsig; vmem frame-level evidence holds; NO V1/V2/V3 crashes observed on Linux (HardExit still used by construction).
- **Browser-discovery side EXCEEDS the SP-011 inheritance:** embedded WebKitGTK 2.52.3 (auto-fallback, NativeDialog-scenarios adapter — never presents visually per SP-011 L4, presentation irrelevant here) navigates AND **InvokeScript WORKS** — full discovery→transfer→decode pipeline REAL on WSLg: **7/7 PASS exit 0** (`evidence/run-wslg-browser.jsonl`). Two engine differences recorded: **V6** (WebKitGTK returns InvokeScript results RAW where WebView2 JSON-encodes — both shapes accepted) and **V7** (WebKitGTK denies ClearKey EME with TypeError — B7 passes on the ATTEMPT-as-signaling rule per the admission consult; granted-variant evidence is Windows-only). First fix needed on Linux: `Thread.SetApartmentState(STA)` is COM-only (PlatformNotSupportedException) — guarded to Windows.
- **Sensitive-logging audit GREEN on WSLg** (decode + browser runs).
- **Contract pollution guard on WSL2:** `CcpClient.sln` 0W/0E, `CcpClient.Tests` 213/213, `CcpClient.HeadlessTests` 22/22 (copy missed `client/tools` on first pass — CS0246 in VerifyHarnessTests; copied, re-ran green; recorded honestly).
- Wayland untouched (§5.1). WSLg = X11/XWayland session facts.

Deliverable `client/docs/video-handoff-spike.md` written: named observation per matrix row (M1-M10) with evidence class per platform, supported/unsupported matrix PENDING-OWNER, findings V1-V7, WPF contract, 10 named limits.

## Step 4b — Pre-completion consult (solo Fable 5, 2026-07-21) + corrections

**Verdict (actual answering model: solo consult route per board gate history — Fable-sanctioned, model not self-identified in output):** no row verdict dishonest WITH corrections; EME-denied-as-signaling DEFENSIBLE with named limits; relay pending-owner framing adequate with an added security note; .DONE blockers = remaining contract/board/git steps only. Corrections applied:
1. **HLS/DASH claims re-scoped** — single-segment VOD HLS / single-chunk static DASH fixture scope now explicit in rows M3/M4 + named limit 11 (multi-segment/variant/live untested).
2. **M8-expired wording fixed in CODE + doc** — "one decoder open" was wrong: preflight short-circuits, ZERO decoder opens. Matrix.cs/Browser.cs strings corrected and ALL FOUR evidence runs re-generated with final code (win decode ×2 stability 14/14, win browser 7/7, wsl decode 14/14, wsl browser 7/7; audits GREEN both platforms).
3. **EME detection named limits added** (limit 12): fixture-instrumented detection; uncooperative-page detection needs host-injected observation (untested); `encrypted`-event variant untested; granted-keySystem/encrypted-reliant product detector has no Linux evidence yet.
4. **Relay security note added** (limit 3): spike relay unauthenticated on 127.0.0.1; product relay needs origin/auth binding in the pending-owner decision.
5. **Stale-evidence risk closed** — all evidence JSONL recopied from final-code runs.
- **V8 (new finding, surfaced by the re-runs):** adaptive demuxer Time/Position reporting flaky ACROSS runs (maxPos 0.63→0.00 HLS-TS/DASH with identical frames+end) → pre-declared frame-paced wall-clock progression prong added (frames ≥5 AND wall ≥1500ms); doc thresholds updated.
