# SP-127 — plan (committed BEFORE any mapping)

Review level 3, step 1. This file exists so the method is fixed before the answer is known.
**Nothing below is a finding.** Every number that will appear in the census is written here as a
placeholder to be produced by a command, never by me. The only numbers in this file are line numbers
in citations I opened (§11).

Branch `lane/SP-127-trainer-card-census`, worktree base `5fd9a8671` (`feat/crossplatform` tip).

---

## 0. Why this plan is shaped this way

### 0.1 The central trap, and why "read harder" is not the fix

This port has been wrong about an upstream count **five times**: haptic sites 8 → 13 → 14 → 18, and
For You Feed 3 → 9. **Every single correction came from widening the universe, never from reading the
same files more carefully.** Each of the five searches ran over a file LIST that somebody assembled by
hand. Thirteen missed the DEFAULT video engine because one file was not on the list. Fourteen missed a
module this port had already shipped.

**SP-125's own plan forbade hand-assembled lists — and then contained one**, in the very section whose
purpose was to prevent it, and had to be revised. That is the decisive fact for this plan: a prose
promise not to hand-assemble a list is not a control, because the author who writes the promise is the
same author who then writes the list.

**So this plan does not promise. It makes the walk a committed artefact (§1.2).** Every count in the
census will be produced by running one script whose only inputs are a ROOT DIRECTORY and a pattern set
over generated bytes, and that script is committed in this packet folder before the counting starts.
A number that did not come out of that script (or out of a `find`/`git ls-files` invocation printed
beside it, whose argument is a directory) may not appear in the census.

### 0.2 The board row's own evidence is a claim under test, not an input

`client/docs/task-board.md:109` carries this row's evidence, and it came from a sync ledger — the same
class of source that produced the "3 files" claim SP-125 found to be 9. **Five distinct counts in that
row are claims to be tested, and I inherit none of them:**

| # | The row's claim (`task-board.md:109`) | Status entering this packet |
|---|---|---|
| R1 | `Views/Controls/` (113 new files) | UNVERIFIED |
| R2 | `Resources/cosmetics/` (94) | UNVERIFIED |
| R3 | `Resources/banners/` (12) | UNVERIFIED |
| R4 | `Resources/achievements/` (6) | UNVERIFIED |
| R5 | `Services/Profile/` (3) | UNVERIFIED |

**If any of the five is wrong, that is the headline of the census, not a footnote.** SP-113 found
`AppSettings.cs` citations wrong by ~530 lines and in the wrong path; SP-120 found four wrong
citations inside its own packet; SP-125 caught one of its own. Being wrong here is the normal case,
not the exceptional one.

Three further shapes of error are specifically in scope, because SP-125 found all three:

- **A directory that is not the feature.** `Services/Fyp/Online/` was 42% of the named directory and
  was an app-wide subsystem with eleven consumers outside itself. `Views/Controls/` at 113 files is
  the single most likely place for this surface to repeat that: "Controls" is not a feature name.
  The census must decide, per file, whether it belongs to this surface — by evidence, not by folder.
- **A count that is right about files and wrong about kind.** 94 + 12 + 6 = 112 files under
  `Resources/` are very likely BYTES (images), not code. Bytes and code are counted separately and
  never summed into one "size" (§4.3).
- **A number the row simply does not mention.** The row names five directories. The universe is the
  repository, so a sixth location is findable; the five are not a boundary.

### 0.3 This surface differs from FYP in a way that can change the verdict

The same board row records that **part of this subsystem's consumer side may already be in the port**:
the v6.7 delta's `GamificationBridge.cs +157` is named as the CONSUMER side of `RaiseQuizCompleted`,
and SP-058 landed related work (`task-board.md:101`). A surface whose consumer is half-ported is a
different object from one that is wholly absent, so **`buildable-in-part` is a live outcome here in a
way it was not for FYP** — and if it is the outcome, §5 requires me to name exactly which part.

**I inherit nothing from that cross-reference either.** "SP-058 landed related work" is a claim about
the port that I will answer by walking `client/src/` (§5), not by reading the row.

### 0.4 One correctness trap is named in advance because it is silent

`honor_roll` is awarded over **DISTINCT** categories. If category normalisation is not verbatim, two
spellings of one category count as two categories and **the award fires early** — a wrong-answer bug
with no exception, no log line and no visible symptom. §6 fixes the method for pinning it. I do not
yet know what the upstream normalisation is; the row asserts `Trim().ToLowerInvariant()` and that
assertion is a claim under test like every other.

---

## 1. The universe, stated as directories

### 1.1 The roles

**The universe is the repository root, walked recursively.** There is no enumerated list of roots that
could be incomplete. The rows below are **ROLES over that one walk** — they say what a hit MEANS, not
where the walk goes.

| # | Role | Extent | In/out | What a hit there means |
|---|---|---|---|---|
| **U0** | **THE UNIVERSE** | **the repository root, recursive** | **IN** | Everything not matched by X2 is searched: dotted directories (`.github/`), spaced directories (`img state/`), `spine-tasks/`, `stream-overlay/`, `pack-zips/`, `redist/`, and every root-level file of every extension (`.bat`, `.iss`, `.gitignore`, `.md`, `.txt`, `.json`). **No source file is excluded by name, anywhere, for any reason.** |
| U1 | **behaviour** | `ConditioningControlPanel/` minus `CCP.*` | IN | The shipping WPF product. The only authority on what the Trainer Card does. |
| U2 | **behaviour** | `Tests/` | IN | The WPF test project. A behaviour asserted there is behaviour, and it names members the product tree may only reach reflectively or through XAML. |
| U3 | **drivers** | every other root-level path reached by U0 | IN | Non-C# drivers decide real things (SP-125 proved this: `build-installer.bat`, `.gitignore` and `MODDING.md` all carried load-bearing FYP facts its first-pass universe could not see). |
| U4 | **capability** | `client/` | IN | The port. Supplies the port-anchor side of every mapping **and the already-shipped-it check** — the check that the haptic count of 14 missed and that §5 exists to run properly here. |
| U5 | **rules** | `docs/`, `client/docs/`, `CLAUDE.md` files | IN | Governance. Consulted for RULES, never for behaviour and never for status. |
| X1 | `ConditioningControlPanel/CCP.*` | recursive | **IN the sweep, OUT of the capability side** | Searched, because a mention there is a lesson and (per D209) the shipping product project-references `CCP.Core`. **A `CCP.*` artefact may NEVER satisfy COVERED or PARTIAL, not even as corroboration** — `docs/constitution.md:32` forbids importing its classes, interfaces, timers, DI topology **or status claims**, and citing it as proof the port can do something IS a status claim. Enforced structurally: the port-anchor cell accepts `client/src/**` paths only. |
| X2 | `**/bin/`, `**/obj/`, `.git/`, `__pycache__/`, `**/node_modules/`, `*.log`, `*.binlog`, `*.nettrace*`, `*.etlx`, `*.speedscope.json` | n/a | **OUT** | Build output, package caches and traces. **Every exclusion is a pattern over GENERATED bytes. No exclusion names a source file.** |

`ConditioningControlPanel/CCP.*` note on evidence class: if this surface's settings turn out to live
in `CCP.Core/Models/AppSettings.cs` as FYP's did, D209's narrow resolution applies unchanged (a
linked project is behavioural evidence for the product that links it,
`ConditioningControlPanel.csproj:52`), it is re-verified rather than assumed, and it is flagged, not
resolved unilaterally.

### 1.2 The structural device: the walk is a committed script, not a promise

`spine-tasks/SP-127-trainer-card-census/walk.mjs` is committed in this packet **before any counting**.

- Its inputs are a **root directory** and the X2 pattern set. It has **no file-list parameter and no
  name-based exclusion**, so a hand-assembled list is not expressible in it.
- It prints, for a given root: the recursive file set, the count, the line count per file, and the
  `git ls-files` cross-count for the same root. **Two independent counts** per tree, so untracked
  bytes are detected rather than assumed absent; a disagreement is reported and explained, never
  silently reconciled.
- Every count in `client/docs/trainer-card-census.md` cites the exact invocation that produced it.
- **A number with no invocation beside it is a defect in the census**, and I will treat it as one.

This is the control that §0.1 says a prose promise is not: the reviewer can re-run it.

---

## 2. Method

**M1 — Directory-first counting.** Every count comes from a recursive walk over a DIRECTORY, via §1.2.
Two independent counts (`find`-equivalent and `git ls-files`) for any tree that could hold untracked
bytes. Disagreements are reported.

**M2 — Token sweep to a fixed point over U0.** Case-insensitive, recursive, over the repository root.

*Seeds, taken from the board row's own nouns plus the cross-reference's identifiers:*
`trainer card`, `trainercard`, `profile`, `wardrobe`, `adornment`, `banner`, `cosmetic`, `avatar`,
`achievement`, `badge`, `guild`, `leaderboard`, `pin`/`pinned`, `unlock`, `gamification`,
`GamificationBridge`, `RaiseQuizCompleted`, `OnQuizCompleted`, `top_of_the_class`, `honor_roll`,
`held_back`, `graded run`, `Graded runs`.

*Anchoring.* `profile`, `banner`, `pin`, `unlock` and `badge` over-match badly (`ProfileOptimization`,
`PinnedTab`, `BannerHeight`, `Unlock` on a mutex, `badge` in unrelated prose). Each is matched with a
word boundary and, **where an anchored needle still over-matches, I report the noise count and open
the matches rather than quietly narrowing the needle.** SP-125's lesson is precise here: its seed
`fyp` unanchored matched `NotifyPercent`, its own plan failed to anticipate that, and the error was
caught only by OPENING matches instead of counting them. **Anchored patterns of record are printed in
the census.**

*The reveal rule, so the fixed point is defined and not elastic.* A hit **reveals** a new token iff
**(a)** it is inside a file already established as part of the surface, or **(b)** it names one of the
surface's own members (type, method, setting key, resource key, achievement id, CLI flag, file name).
A hit that merely co-occurs with a needle reveals nothing. Without this rule the sweep either
terminates by fiat or expands to the whole repository. **This rule is what found the two `Chaos/`
files in SP-125 that contained no occurrence of the token `fyp` at all** — the difference between its
count and the four wrong haptic counts.

**M3 — Consumer closure before any equivalence claim.** "It is only these N files" is inadmissible
until every consumer is enumerated. For each public type and member of the surface, grep U0 for its
bare name and record every hit site, split into **compile-time consumers** and **comment-only
references** (SP-125's split, kept because conflating them overstates). A surface whose consumers were
sampled rather than enumerated is reported as **unbounded**, not as small.

**M4 — Every cited line is opened.** No `File.cs:line` appears in the census, the record or the
divergences unless I ran `sed -n` on that exact path and the text quoted is the text I saw. Citations
are taken against this worktree's HEAD and the SHA is recorded in the census. This plan already obeys
it (§11).

**M5 — Bytes are counted, classified, and never forked.** Any `Resources/` tree in scope is counted by
walk, by extension and by byte total, and classified as CODE or BYTES. The rule on bytes is fixed and
not mine to relax: upstream web/asset payloads are linked read-only out of the legacy tree by csproj
glob and copied to `payload/`; **the bytes stay owned by the legacy tree and are never forked into
`client/`** (root `CLAUDE.md`, and the `dtrh`/`intake`/`tunnel`/`vendor` precedent SP-125 read).
**No proposal to copy bytes into `client/` will be written.** If a tree cannot be served that way, the
reason is stated as a finding.

**M6 — The port side is walked, not remembered.** §5. The question "what does the port already have"
is answered by a recursive walk of `client/src/` plus a member-by-member check against the upstream
consumer, never by reading a board row or a landing log.

**M7 — Verbatim comparison for anything the row calls load-bearing.** §6. Where the row says a call is
load-bearing, the census quotes **the exact upstream expression** from an opened line and the **exact
port expression** from an opened line, and states `MATCHES` / `DIFFERS` / `ABSENT` — never a paraphrase
and never "equivalent".

---

## 3. Capability decision rule — fixed now, so the verdict is not a judgement call

Every behaviour B gets a row of **five** cells. **A row is invalid unless all five are filled.**

1. **WPF evidence** — `File.cs:line`, opened per M4, with the quoted text.
2. **Required primitive** — mechanism-free, phrased as an observable ("a grid of images the user can
   drag one item out of and drop onto a card"). Never an API name.
3. **Port anchor** — a `client/src/**` path and line, opened per M4, or the literal word `none`.
   **`CCP.*` paths are not accepted in this cell** (§1.1 X1).
4. **Label** — exactly one of four. **The vocabulary is closed; I may not invent a fifth.**

| Label | Assigned iff |
|---|---|
| **COVERED by \<anchor\>** | A type already in `client/src/**` exposes the required primitive, cited at a line I opened, **and** reaching it needs no new platform interop, no new OS API and no new owner decision. |
| **PARTIAL on \<anchor\>** | Such a type exists and is cited, but the behaviour needs a mode, parameter or member it does not have. **The missing member is NAMED. A PARTIAL that cannot name the missing member is demoted to GAP.** |
| **GAP: \<named primitive\>** | No such type exists. Names (a) the primitive, (b) the OS API or library WPF uses to get it, (c) what the port would have to build, in one sentence. |
| **OWNER-GATED** | The required primitive is governed by an owner decision (§7). Gets its own flagged section and **no size** (§4.2). |

**3.1 The anchor set is WIDER than the seven capabilities, and this is the methodological difference
from SP-125.** The port's seven landed capabilities are structural — they are directories in
`client/src/CcpClient.Desktop/`: `Overlay`, `Input`, `Audio`, `Video`, `Pointer`, `Glyph`, `Haptics`.
Those are **OS-interop** capabilities. The Trainer Card is, on its face, an **in-window UI surface over
persisted state**, so most of its behaviours will not map onto any of the seven, and a rule that only
admitted those seven would label ordinary widget work `GAP` and produce a false refusal.

So the port-anchor cell accepts **exactly two kinds** of anchor, both cited at an opened
`client/src/**` line:

- **(a) one of the seven OS capabilities** above, when the behaviour needs OS interop; or
- **(b) a SHIPPED in-window precedent** — a real page, control, persistence store or entitlement type
  already in `client/src/**` that does the same KIND of thing (renders a page, stores state to disk,
  gates on a tier). "Avalonia can do it" is **not** an anchor; a shipped file that does it is.

If neither exists, the label is GAP and the primitive is named. This keeps the rule mechanical while
refusing to launder "it is only UI" into COVERED.

5. **Platform** — `Windows: proven|unproven` / `Linux: proven|unproven`, and **when either is
   `unproven`, the exact manual gate is named IN the cell.**

**Why cell 5 is mandatory.** `docs/constitution.md:36`: *"**Windows AND Linux** (X11/Wayland
distinguished where behavior differs) or a documented product blocker. Compilation, a stub, a no-op
fallback, or a **Windows-only test never proves cross-platform support**."* And
`client/memories/port-status.md:89-93` records the measurement: `wsl.exe --list --verbose` reports
*"Windows Subsystem for Linux has no installed distributions."* **On this machine every Linux claim is
a named gate, never a discharge.** Default for this packet is `Linux: unproven` with a named gate,
and no row may launder a Win32-only mechanism into a cross-platform COVERED.

"Probably", "should be easy", "similar to" are not labels. **An unlabelled behaviour is a defect in
this census, not an acceptable residue. A GAP is a finding, not a blocker** — it is named and the
census continues.

---

## 4. Verdict rule — fixed now

### 4.1 "Essential" is anchored, not re-derived per behaviour

The owner already wrote this surface's identity as noun phrases, and **that fixed list is the test**.
From the row title and body at `client/docs/task-board.md:109`:

> Trainer Card profile redesign; **wardrobe/banners/achievement gating**; 12 hand-made scene banners
> (3 per mod, card-sized); a **wardrobe of 60 adornments** with a full editor + click-to-pin;
> **earn-it-to-wear-it** (wardrobe pieces, guild avatars and badges unlock through achievements);
> 12 preset blank-subject avatars (3 per mod); a leaderboard **privacy dialog** deciding exactly what
> is shared.

and from the SP-058 cross-reference in the same row, which is part of this row's own scope:

> the "Quiz" section renamed **"Graded runs"**; a **source-agnostic `OnQuizCompleted`** handler;
> **`top_of_the_class`** at the 90% bar; **`honor_roll` over DISTINCT categories**; **`held_back`**
> deliberately fail-streak-only.

A behaviour is **essential** iff it realises one of those phrases. The mapping from phrase to
behaviour row is written down explicitly, so the word "essential" cannot drift mid-census. Phrases the
walk proves do not exist in the shipping source are reported as such (that too is a row-evidence
finding), and phrases the walk finds that the row does NOT name are added as rows and flagged as
row omissions.

### 4.2 The verdict, applied mechanically

- **REFUSED** iff at least one essential behaviour is labelled GAP with no landed substitute.
- **BUILDABLE-IN-PART** iff the surface decomposes into a subset whose rows are all COVERED or
  PARTIAL, **that subset is independently user-observable** (a user could open it and use it), and the
  GAP set is named as the residue. **This is a live outcome here** (§0.3), and if it is the verdict
  the separable unit is named with its file inventory, not described.
- **BUILDABLE** iff every row is COVERED or PARTIAL.

**Owner-authority carve-out.** A behaviour whose required primitive touches **consent, sensors,
networking, persistence or entitlement** — the five the packet names — or is governed by an owner
decision in `client/docs/capability-inventory.md`, goes in its **own owner-flagged section** and is
**NEVER folded into the size.** It gets no label implying the port may proceed.
`capability-inventory.md:70`: *"Expanding sensors, derived data, persistence, networking, diagnostics,
or telemetry requires a consent-contract revision and owner review."* **A leaderboard and a privacy
dialog are named in the board row itself, so I expect at least one such section and will not be
surprised into folding it.**

### 4.3 Size is an inventory of named units. Bytes and code are never summed

The size that follows the verdict is **an enumeration of units with their file counts and line
counts**, never a t-shirt letter and never a day count. **Code files and asset bytes are reported in
separate columns and never added together**, because 112 PNGs and 112 `.cs` files are not the same
work and a single "226 files" number would hide which one this is. **I am not permitted to prefer an
outcome.** The verdict is whatever the labelled rows say.

---

## 5. What the port already has of the consumer side — decided by a walk, not by memory

This is the question that can move the verdict from REFUSED to BUILDABLE-IN-PART, so its method is
fixed here.

1. **Enumerate the upstream consumer.** Locate `GamificationBridge.cs` by the U0 walk (not by the path
   the row implies), open it, and enumerate every member that participates in the quiz-to-achievement
   path: the handler, the award ids, the thresholds, the streak state, the category set, the
   persistence.
2. **Walk the port.** Recursively walk `client/src/` for each of those member names and for the seed
   set in M2. **`client/src/` is walked whole** — not `client/src/CcpClient.Desktop/Features/Intake/`
   or any other guess at where SP-058 put things.
3. **Answer member by member**, in a table with one row per upstream member:
   `PRESENT` (cited at a `client/src/**` line I opened) / `PRESENT-BUT-DIFFERENT` (cited both sides,
   difference named) / `ABSENT`.
4. **State the seam.** SP-058 is recorded as computing and logging score/passed/perfect/category/
   mantra-credit while **raising nothing** (typed seams). Whether that is true is a claim under test:
   the census states what the port's code actually does at the point where upstream raises, with a
   citation, and names the exact wiring point a next packet would attach to.
5. **The finding is stated as a fraction of the enumerated members, not as an adjective.** "Half
   ported" is not admissible; "N of M members present, these N, these M−N absent" is.

---

## 6. Pinning `honor_roll`'s category normalisation — verbatim or not at all

The failure mode is silent (§0.4), so the method is verbatim comparison (M7):

1. Find upstream's `honor_roll` award in the U0 walk and open the lines that build its category set.
2. **Quote the exact normalisation expression** as it appears in the shipping source — the whole
   expression, in a code span, with the file and line. If it is not `Trim().ToLowerInvariant()`, the
   board row is wrong and that is a finding.
3. Quote the **distinctness mechanism** exactly too (what collection type, what comparer, whether the
   comparer is ordinal or culture-sensitive). **`ToLowerInvariant()` into a default `HashSet<string>`
   and a `StringComparer.OrdinalIgnoreCase` set are not the same object**, and a census that said
   "case-insensitive" for both would have missed the difference the packet is worried about.
4. Find the port's corresponding expression by the §5 walk and quote it from an opened line.
5. State `MATCHES` / `DIFFERS` / `ABSENT` and, if it differs, **state the user-observable
   consequence**: which two category spellings would count twice, and at which award count the badge
   would fire early.
6. **Pin whichever side exists** in `TrainerCardCensusTests` by re-deriving the expression text from
   the shipping bytes, so a later upstream edit reds the suite (§8).

---

## 7. The privacy test — applied to the whole surface

Four questions, each answered yes/no **with a citation**, for the surface as a whole. Any yes gets its
own owner-flagged section per §4.2 and is not folded into a size.

1. Does it change what is **persisted** to disk (profile state, achievement ledger, streaks,
   cosmetics owned, telemetry)?
2. Does it change what is **shown to anyone other than the local user** — a leaderboard entry, a
   shared card, a guild roster, a screenshot-friendly surface?
3. Does it change what **leaves the machine** (network, analytics, upload, a third-party service)?
4. **What sensor or consent state does it turn on, under whose consent**, when does it stop, and
   **can the user see it running?**

**Question 2 and 3 are near-certain yes for this surface** — the row names a "leaderboard **privacy
dialog** deciding exactly what is shared" — and the correct response is a flagged section stating what
the dialog decides, quoted from source, not a sentence in a size estimate. **A privacy-relevant
behaviour discovered during a sizing exercise is exactly the kind of thing that disappears into a
number, and it will not disappear into mine.**

---

## 8. Pinning — RE-DERIVED FROM THE SHIPPING BYTES, never a document agreeing with itself

`client/tests/CcpClient.Tests/TrainerCardCensusTests.cs` (new, pure logic, no Avalonia runtime).

The vacuity to avoid is already named in this repository's own words, at
`client/tests/CcpClient.Tests/HapticSiteCensusTests.cs:11-15`, which I opened:

> *"A guard that only checked the document against itself would have passed on all four numbers. So
> the document is the DATA and this file is the LOGIC ... the needle set and the file universe live
> HERE, and the candidate line set is RE-DERIVED from the shipping bytes on every run. Editing the
> census document can never shrink the search."*

and the file-list ban is stated at `:43-45`:

> *"The family: three directories, searched whole and recursively. NOT a file list — a file list is
> exactly how `VideoService.Browser.cs` and `BouncingTextService.cs` were missed twice."*

Built to that precedent, with no second convention invented for the same job:

- **The roots live in the TEST, as DIRECTORY strings, not as a file list.** Editing
  `client/docs/trainer-card-census.md` can never shrink the search.
- **The counts RE-DERIVE from the `ConditioningControlPanel/` tree on every run** and are compared
  against the census tables. Upstream gaining a file reds the suite.
- **The normalisation expression re-derives from the shipping bytes** (§6.6), so an upstream change to
  it is a red test rather than a stale sentence.
- **Repo root anchors on `client/CcpClient.sln`** (present in every checkout and in a worktree).
  **Root-not-found and missing-census are hard FAILURES that never skip.**
- **A half-present reference tree FAILS.** "Unreachable reference tree" means exactly one thing: no
  `ConditioningControlPanel/` directory at all. Directory present, surface tree absent = corrupt
  checkout = fail.
- **The branch taken is written to test output**, so a permanently-unreachable guard is visible in the
  TRX instead of passing silently forever.
- **Fixture facts** pin the parser and comparer against temp-dir repositories, so nothing rests on
  today's tree happening to agree.
- Document-shape assertions stay document-only, which is legitimate because their subject IS the
  document: every behaviour row carries one of the four labels and none is blank; every row carries a
  platform cell; the four privacy answers are present.

---

## 9. Constraints I am operating under

- **`client/src/` is CLOSED. This packet writes no product code.** If the census proves something
  must be built, that is the finding and the next packet is authored from it.
- `client/docs/wpf-surface-reachability.md` — **divergences ONLY**, numbered from **D210** onward.
  Verified: the highest existing id in that file is **D209** (`:1436`).
- **Pipes are escaped in table cells.** A bare `|` inside a code span silently truncates the row —
  how D197 and D209 lost their disposition and rationale, found only at a final review. Every `|`
  inside a code span in anything I write is `\|`, and I will grep my own output for unescaped pipes
  before committing.
- `client/tests/floor/floor.json` is **never opened**. My count change is declared in
  `spine-tasks/SP-127-trainer-card-census/floor-delta.json`. **Pin 2332 unit / 144 headless**; my
  observed floor will be **pin + declared delta**, and I state both numbers in the report.
- **No wall-clock waits.** These are pure assertions over documents and directory walks; none is
  needed. `Thread.Sleep`, bare `Task.Delay` and clock polls are not used.
- Both gates alone, from the repo root: `node client/tests/floor/check-warnings.mjs`, then
  `node client/tests/floor/check-floor.mjs`. Build must be 0 warnings / 0 errors.
- **Files I may change:** `client/docs/trainer-card-census.md` (new),
  `client/docs/wpf-surface-reachability.md` (divergences only),
  `client/tests/CcpClient.Tests/TrainerCardCensusTests.cs` (new), and
  `spine-tasks/SP-127-trainer-card-census/**`. Nothing else, and specifically not
  `client/docs/task-board.md`, not `client/tests/floor/**`, not `ConditioningControlPanel/**`.
- `.DONE` is created last and is **not committed**.

---

## 10. Order of work after this checkpoint

1. Commit `walk.mjs` (§1.2), then run it. **Verify R1–R5** (§0.2) by M1 — inheriting no number.
2. Token sweep to a fixed point by M2 over U0; report the universe delta against the row's five
   directories, including anything the row does not name.
3. Decide, per file in the named trees, whether it belongs to this surface — SP-125's `Online/`
   finding is the shape to look for, and `Views/Controls/` is where it would hide.
4. Consumer closure by M3 over the shipping tree.
5. **Walk `client/src/` per §5** and answer the consumer-side question member by member.
6. **Pin the `honor_roll` normalisation per §6**, verbatim on both sides.
7. Open and quote every citation by M4.
8. Run the four privacy questions (§7); write any yes into its own owner-flagged section.
9. Count and classify the `Resources/` trees per M5; state the serving mechanism; propose no fork.
10. Label every behaviour by the §3 five-cell rule; apply §4.2 mechanically.
11. Write `client/docs/trainer-card-census.md`; pin it with `TrainerCardCensusTests.cs` per §8; record
    divergences from D210; write `record.md`; declare the floor delta; run both gates; grep my own
    output for unescaped pipes.

---

## 11. Citations I opened before writing this plan (M4 applied to the plan itself)

| Cited | Verified? | What it actually says |
|---|---|---|
| `client/docs/task-board.md:109` | **yes** | The Trainer Card row. Carries the SP-058 cross-reference AND the five directory counts R1-R5, and ends `Size L`. |
| `client/docs/task-board.md:101` | **yes** | SP-058's row; names `GamificationBridge.cs +157` as found by a widened sweep and never ledger-named. |
| `client/tests/CcpClient.Tests/HapticSiteCensusTests.cs:11-15` | **yes** | "A guard that only checked the document against itself would have passed on all four numbers"; document is DATA, test is LOGIC; set re-derived from shipping bytes every run. |
| `client/tests/CcpClient.Tests/HapticSiteCensusTests.cs:43-45` | **yes** | "The family: three directories, searched whole and recursively. NOT a file list — a file list is exactly how `VideoService.Browser.cs` and `BouncingTextService.cs` were missed twice." |
| `docs/constitution.md:32` | **yes** | Read-only zones; `CCP.*` never imported — including "or status claims", which is why §1.1 X1 bars it as corroboration. |
| `docs/constitution.md:36` | **yes** | "Windows AND Linux ... a Windows-only test never proves cross-platform support." |
| `client/memories/port-status.md:89-93` | **yes** | WSL2 present, **no installed distributions**; every Linux claim is a named limit on this machine. |
| `client/docs/capability-inventory.md:70` | **yes** | "Expanding sensors, derived data, persistence, networking, diagnostics, or telemetry requires a consent-contract revision and owner review." |
| `client/docs/capability-inventory.md:78` | **yes** | Windows **and** Linux must provide real capture ... "A stub that says running is a failure." |
| `client/docs/wpf-surface-reachability.md:1436` | **yes** | D209 is the highest existing divergence id, so mine start at D210. |
| `client/src/CcpClient.Desktop/` directory listing | **yes** | The seven OS capabilities are structural: `Overlay`, `Input`, `Audio`, `Video`, `Pointer`, `Glyph`, `Haptics` are directories there. |
| `client/docs/fyp-census.md` (whole) | **yes** | Read in full as the standard this packet is held to. |
| Anything under `Services/Profile/`, `Views/Controls/`, `Resources/{cosmetics,banners,achievements}/`, or `GamificationBridge.cs` | **NO — deliberately not opened** | Surface content. Held until after this checkpoint so the method is fixed before the answer is known. Every claim about them in this plan is written as a CLAIM UNDER TEST, never as fact. |

---

## 12. Revision 2 — the plan-gate corrections, applied BEFORE mapping began

Plan APPROVED at the gate; seven items returned. Items 6 and 7 were defects in my own artefacts and
are fixed in place (`walk.mjs` now records and prints every symlink it declines to follow, because
the previous comment promised a summary that did not exist; the `HapticSiteCensusTests.cs` citation
is corrected from `:11-14` to `:11-15`, the line the quoted tail actually sits on). Items 1-5 change
RULES, so they are written here rather than left in a message, and they bind the census.

**One correction accepted about my own framing.** I called §3.1's anchor set a departure from SP-125.
It is not: `spine-tasks/SP-125-fyp-census/plan.md:136` already read *"A type already in
`client/src/**` exposes the required primitive"*. I opened that line to check. So §3.1 clarifies an
existing rule and **adds** a constraint ("no new owner decision") rather than loosening one. The
reasoning is stronger than I claimed: read narrowly, the rule would have contradicted this packet's
own trap 1 and Step 4, because a finding about an already-ported consumer could never have reached
the label column at all.

### 12.1 (Item 1) The owner carve-out is a test on the CONTRACT, not on the row

§4.2 listed "persistence" flatly and applied the carve-out per behaviour. For a surface that is
largely persisted state, that reading owner-gates nearly every row and empties the inventory —
**a refusal manufactured by my own rule rather than found in the code**, which is the opposite of
this packet's purpose. `client/docs/capability-inventory.md:70` says **"*Expanding* sensors, derived
data, persistence, networking, diagnostics, or telemetry"**, and the operative word is the one my
§4.2 dropped.

**The rule, restated and binding:**

| Situation | Disposition |
|---|---|
| The behaviour **expands** the consent contract, the persistence contract, the network boundary or the entitlement boundary — a new sensor, a new class of stored data, a new destination bytes travel to, a new thing shared with another person | **OWNER-GATED.** Own flagged section, no size, no label implying the port may proceed. |
| The behaviour **writes to a store the port already owns**, under a contract already in force | **SIZABLE.** Labelled COVERED / PARTIAL / GAP like any other row and counted in the inventory. |

The distinction is stated in the census itself, not just here, so a reader can check which side each
row landed on and why. The unit of the test is the **contract**, at surface level (§7), never the
individual row.

### 12.2 (Item 2) The verdict rule had two holes; both are closed

Both were real. A row set of COVERED/PARTIAL with at least one OWNER-GATED and zero GAP satisfied
none of my three clauses, so the verdict would have had to be *chosen* — precisely what a mechanical
rule exists to prevent. And REFUSED and BUILDABLE-IN-PART could both fire.

**Replacement rule, evaluated in this order, first match wins:**

1. **BUILDABLE** iff every row is COVERED or PARTIAL and there are no OWNER-GATED rows.
2. **BUILDABLE-IN-PART** iff a subset of rows is entirely COVERED/PARTIAL, that subset is
   independently user-observable (a user could open it and use it), and every remaining row is
   named in the residue with its label. **This clause fires whether the residue is GAP,
   OWNER-GATED, or both.**
3. **REFUSED** iff no such subset exists — i.e. every user-observable decomposition contains a GAP
   or an OWNER-GATED row.

**What an OWNER-GATED row does to the verdict, said plainly:** it behaves exactly like a GAP for
verdict arithmetic — it cannot appear inside the buildable subset — but unlike a GAP it is **not the
port's to close**, so it is reported in its own flagged section (§12.1) and never priced.
**Clause 2 outranks clause 3 on overlap**, which is the ordering SP-125 reached in practice when it
refused *and* named a separable unit: naming the buildable part is strictly more useful to the owner
than a bare refusal, and losing it to a rule collision would be the census failing at its job.
Where clause 2 fires with a large residue, the census says **BUILDABLE-IN-PART, and the residue is
the headline.**

### 12.3 (Item 3) "the same KIND of thing" is struck

The soft phrase in §3.1(b) is replaced by the label table's operative test, which was always the
real one: **the anchor must EXPOSE THE REQUIRED PRIMITIVE**, cited at an opened `client/src/**` line.
"A shipped page exists" is not an anchor for a drag-and-drop wardrobe editor; a shipped control that
takes a drag and produces a drop is. The census's anchor column carries this wording verbatim, and
where the anchor exposes only part of the primitive the row is PARTIAL with the missing member
named, never COVERED.

### 12.4 (Item 4) The unreachable branch: kept, and the reason is stated in the file — **REVERSED AT IMPLEMENTATION TIME, see §13**

`PROMPT.md:98` words a missing reference tree as FAILS; I kept the precedent's shape, in which a
**fully absent** `ConditioningControlPanel/` takes an unreachable branch that still enforces the
document's whole shape. That tree is in-repo, so on this repository the branch is unreachable and
the two readings agree on every real checkout.

**Resolution: keep the precedent's shape, and say why IN the test file**, so the choice is visible
to the next reader instead of inferred. The reason is that `HapticSiteCensusTests` and
`FypCensusTests` both already carry that branch; a third census guard inventing a second convention
for the same job would make the three disagree, and the packet's own instruction is to follow the
precedent rather than invent. **The branch is not a skip**: it writes which branch was taken to test
output, and it still fails a gutted census. Everything the FAILS wording is protecting against —
a half-present tree, a missing census, an unfindable repo root — is already a hard failure.

### 12.5 (Item 5) The threshold is pinned from bytes too

§6 pinned the normalisation expression and the distinctness comparer. The **count of distinct
categories that fires `honor_roll`** is the other half of the same silent bug class: a wrong
threshold and a wrong comparer both make the badge fire at the wrong moment, and neither throws.
So §6 gains a step: **the threshold literal is re-derived from the shipping bytes** in
`TrainerCardCensusTests` alongside the normalisation expression, and the census states it as a
number taken from an opened line. The same applies to `top_of_the_class`'s 90% bar and
`held_back`'s fail-streak length wherever the walk finds them.

---

## 13. Revision 3 — §12.4 REVERSED at implementation time, by the machinery

**§12.4 said the precedent's unreachable branch would be KEPT. It was not, and I was wrong to plan
that.** The reversal was not a judgement call; it was forced, and by three things at once.

1. **The packet asked for the opposite.** `PROMPT.md:98`: *"a missing reference tree FAILS rather
   than skips."* I planned around that wording instead of following it.
2. **The machinery refuses the shape.** `VacuousShapeDetector` classifies an early return past a
   filesystem predicate as a silencing shape (`early-return`, `fs-predicate`), and
   `VacuousShapeGuardTests.EverySilencingShapeSite_IsDispositionedInTheLedger` reds the floor unless
   the site is dispositioned in `client/tests/floor/vacuous-shape-ledger.json`. **My first
   implementation was red exactly there**, on the two facts that carried the branch.
3. **That ledger is outside this packet's write scope.** `client/tests/floor/**` is in
   `fileScopeMustNotChange`, so the disposition route was closed to me. Widening scope to take it
   would have been the wrong move; the rule is to report, not to widen.

So the branch is gone. `RequireReference()` now hard-asserts the repo root, the census document
**and** `ConditioningControlPanel/`, and every filesystem predicate lives in a helper rather than in
a fact body — which is how `FypCensusTests` already keeps itself out of the ledger, checked by
reading it rather than assumed.

**This is strictly stronger than the precedent, not weaker.** The guard cannot pass without the
reference tree at all, so there is no branch to be permanently-unreachable and nothing to report to
the TRX about which branch ran. A half-present tree still fails, a missing census still fails, an
unfindable root still fails. The reason is stated in the test file's own summary, so the next reader
finds the departure and its cause without reading this plan.

**The lesson worth keeping:** the plan gate offered "make it fail outright, **or** state why the
precedent's shape was kept", I chose the second, and the build system then proved the first was the
only in-scope answer. A rule enforced by code beat a rule I argued for in prose — which is the same
lesson §0.1 draws about hand-assembled file lists, arriving from the other direction.
