<Window x:Class="ConditioningControlPanel.TextEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Manager" 
        Height="500" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A2E"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Color x:Key="PinkColor">#FF69B4</Color>
        <Color x:Key="DarkPink">#FF1493</Color>
        <Color x:Key="PanelBg">#252542</Color>
        <Color x:Key="DarkerBg">#1A1A2E</Color>
        <Color x:Key="TextLight">#E0E0E0</Color>
        <Color x:Key="ItemBg">#1E1E3A</Color>
        <Color x:Key="ItemHover">#2A2A4A</Color>
        <Color x:Key="ItemSelected">#3A2A5A</Color>
        
        <SolidColorBrush x:Key="PinkBrush" Color="{StaticResource PinkColor}"/>
        <SolidColorBrush x:Key="DarkPinkBrush" Color="{StaticResource DarkPink}"/>
        <SolidColorBrush x:Key="PanelBgBrush" Color="{StaticResource PanelBg}"/>
        <SolidColorBrush x:Key="TextLightBrush" Color="{StaticResource TextLight}"/>
        <SolidColorBrush x:Key="ItemBgBrush" Color="{StaticResource ItemBg}"/>

        <!-- Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DarkPinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#404060"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#FF4757"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E84050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style -->
        <Style x:Key="ItemCheckBox" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="border" Width="20" Height="20" CornerRadius="4" 
                                    Background="#404060" BorderThickness="0"/>
                            <Path x:Name="checkmark" Width="12" Height="12" 
                                  Stretch="Uniform" Fill="White" Visibility="Collapsed"
                                  Data="M9.86 3.14a.5.5 0 0 1 0 .72L4.5 9.22 1.64 6.36a.5.5 0 1 1 .72-.72L4.5 7.78l4.64-4.64a.5.5 0 0 1 .72 0z"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PinkBrush}"/>
                                <Setter TargetName="checkmark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#505080"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkPinkBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock x:Name="TxtTitle" Text="Text Manager" Foreground="{StaticResource PinkBrush}" 
                       FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Sort A-Z" Style="{StaticResource SecondaryButton}" 
                        Margin="0,0,8,0" Click="BtnSort_Click"/>
                <Button Content="Toggle All" Style="{StaticResource SecondaryButton}" 
                        Click="BtnToggleAll_Click"/>
            </StackPanel>
        </Grid>

        <!-- Add New Item -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtNewItem" Grid.Column="0" 
                     Background="#1E1E3A" Foreground="White" 
                     BorderBrush="{StaticResource PinkBrush}" BorderThickness="1"
                     Padding="10,8" FontSize="13" VerticalContentAlignment="Center"
                     KeyDown="TxtNewItem_KeyDown"/>
            <Button Grid.Column="1" Content="+ Add" Style="{StaticResource ActionButton}" 
                    Margin="8,0,0,0" Click="BtnAdd_Click"/>
        </Grid>

        <!-- Item List -->
        <Border Grid.Row="2" Background="{StaticResource PanelBgBrush}" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
                <ItemsControl x:Name="ItemList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ItemBorder" Background="#1E1E3A" CornerRadius="5" 
                                    Margin="3" Padding="10,8" Cursor="Hand"
                                    MouseLeftButtonDown="Item_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" Style="{StaticResource ItemCheckBox}"
                                              IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                              Checked="CheckBox_Changed" Unchecked="CheckBox_Changed"
                                              Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Text}" 
                                               Foreground="White" FontSize="13"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="✕" Foreground="#808080" 
                                               FontSize="14" VerticalAlignment="Center"
                                               Cursor="Hand" MouseLeftButtonDown="BtnRemove_Click"
                                               ToolTip="Remove item">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="#FF4757"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#3A2A5A"/>
                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource PinkBrush}"/>
                                    <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Footer Buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="🗑️ Remove Selected" Style="{StaticResource DangerButton}" 
                    Click="BtnRemoveSelected_Click"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" 
                        Margin="0,0,8,0" Click="BtnCancel_Click"/>
                <Button Content="Save" Style="{StaticResource ActionButton}" 
                        Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
