# WPF Bark Rule Engine — Port Contract (BARK-1, prerequisite of AI-10)

Extracted read-only from the frozen WPF head (wpf-archaeologist, 2026-07-12). Source: `Services/Companion/BarkService.cs` (1678 lines), `Services/Bark/BarkState.cs`, `CCP.Core/Models/AppSettings.cs`. Citations = WPF head. WPF is never modified.

**Port state:** data models (`CCP.Core/Services/Bark/` BarkRule/BarkRuleSet/BarkRuleLoader/BarkVariant/BarkContext) + `bark_rules.json` manifests ARE ported but ZERO-consumed. `IBarkService` (App.cs:483) exposes only `NotifyChaos*` bare-string events → `AvaloniaBarkService` (AvaloniaHeadStubs.cs:714) emits `RaiseBark(kind)` bare string. The ENGINE below is unported. Un-stubbing it revives the entire companion reactive-dialogue system.

## (A) Raise pipeline
`private bool Raise(string trigger, Action<BarkContext>? fill=null, bool guaranteed=false)` — BarkService.cs:773. Returns true only when a bark spoke.
1. `rules = _rules.ForTrigger(trigger)` (priority-descending; empty→false) :777.
2. `ctx = new BarkContext(trigger)`; `fill?.Invoke(ctx)` stamps per-fire vars :780.
3. Under `lock(_gate)`: first rule where `ConditionsPass(rule,ctx)` → winner, break :791. No match→false.
4. `(toSpeak,variantIndex,pool)=DecideLocked(trigger,winner,guaranteed)` under lock :802.
5. OUTSIDE lock: if toSpeak→`Speak(...)` (Giggle marshals to UI) :805.
`DecideLocked` :825: ResolvePool→EvaluateGate→LogDecision; !WouldFire→(null,-1,null); else CommitFire; `DryRun?(null,-1,null):(winner,idx,pool)`. **CommitFire runs even in dry-run** (state advances, Speak suppressed).
Idle path `DispatchIdle` :847 (AvatarTube idle timer): skip if MasterVolume==0 or avatar IsSpeaking; `ForTrigger("Idle")`→filter ConditionsPass→`PickIdleRuleLocked` (pool-wide no-repeat, GatedIdleBias=0.35 :879)→DecideLocked.

## (B) Condition-key table (ConditionsPass :1038; ConditionPass :1048; ResolveField :1086)
Operator suffix on key: `_gte/_lte/_gt/_lt/_eq` (default `eq`). Any field X supports X, X_eq, X_gt, X_gte, X_lt, X_lte :1053.
- eq: bool-equality if bool; else numeric within 0.0001; else case-insensitive string :1068.
- gte/lte/gt/lt: both coerce to double via TryDouble else false :1076. actual==null→false.
- TryDouble: double/int/long/float/bool(1/0)/parseable-string. TryBool: bool/int/double/parseable-string.
Well-known LIVE fields (resolved before ctx, field lowercased) :1088: video_playing, webcam_running, session_running, setup_idle_sec, session_elapsed_sec, session_phase_index, blink_count, face_lost_sec, mod_switches_60s, clicks_60s, days_away, instant_relaunch, master_volume, mute, player_level, total_sessions, daily_quest_streak, current_streak, achievements_all_unlocked, all_skills_unlocked, is_nye, local_hour, phase_name, phase_is_deepener.
Fallback: any other field = `ctx.Values[field]` or null :1138. So all fill-stamped vars (app_cluster, app, category, activity, still_minutes, combo, payload, difficulty, wave, tier, tier_up, …) match here. `app_cluster_eq`/`app_eq` are NOT special-cased — resolve as ctx vars; populated only when AppClusterMap matched (else "" → rule won't match) :566.

## (C) Gates / cooldown / once-only + persistence  [PRIVACY/SETTINGS SURFACE]
`EvaluateGate` :1316, in order:
1. empty-pool→no fire. 2. `bypass = rule.Class==Safety || guaranteed`. 3. One-shot dedup (AlreadyFiredOnce; bypassed only by Safety, NOT guaranteed): `!isSafety && !Repeatable && AlreadyFiredOnce`→no fire :1332.
4. If !bypass, sequential floors: safety-hold (`_safetyHoldUntilUtc`, SafetyHoldMs=6000); whisper-active (`App.Audio.IsWhisperAudioPlaying`); narrator-active (`ChaosNarrator.IsPlaying`); chat-suppressed (`CompanionBusy(BarkChatSuppressionMs??10000)` = avatar busy OR `_lastUserMessageUtc` in window); anti-stale-speaking (non-preempting rule while avatar.IsSpeaking; willPreempt = Class!=Normal || Priority>=100, threshold 100); **global min-gap 60000ms** since `_globalLastFireUtc` unless trigger∈{"AttentionCheckFail"}; per-rule `CooldownMs` since `_lastFiredUtc[id]`; chance roll (Chance<1.0 && rng>=Chance) :1386.
5. Variant selection :1399: repeatable+pool>1→PickVariant (random-among-unused, global recency soft-pref RecentlySpokenMemory=8, recycle reseeds last); one-shot+pool>1→rng.Next(pool.Count).
`CommitFire` :1445: stamps `_lastFiredUtc[id]`, `_globalLastFireUtc`, RememberSpoken(audio); Safety sets safety-hold; !Repeatable→`_firedOnceSession` + (Scope!=Session && !DryRun) `MarkBarkFired(LatchKey)`; records variant key + PersistVariantRotation; idle→`_usedIdleRules`+PersistIdleRotation.
**BarkState.cs is entirely IN-MEMORY (no disk, no sensitive data).** Persistence lives in AppSettings→settings.json (all non-sensitive): `BarkLifetimeFired` (List<string>, key=id or id@Tier via LatchKey :1433; MarkBarkFired/IsBarkFired AppSettings.cs:350), `BarkVariantRotation` (Dict<string,List<string>> AppSettings.cs:376), `BarkIdleRotation` (List<string> :388), `BarkChatSuppressionMs` (:330), `DisabledPhraseIds`/`RemovedPhraseIds` (Phrase-Manager, `Bark:` id prefix, AppSettings.cs:4413; ResolvePool filters :1172), `LastAnnouncedStreakMilestone` (:546). Saves: `App.Settings.Save(suppressCloudBackup:true)`, dry-run-suppressed (PersistVariantRotation/PersistIdleRotation :1556; load on Start via LoadRotationFromSettings :1538). **Global 60s rate limit + per-rule cooldown both exist.**

## (D) Speak delivery (:1578)
Requires `App.AvatarWindow` (null→drop). `line = ApplySubstitutions(variant.Text, ctx)` ({0}→focused app name, {key}→ctx var, :1631). Blank→return.
Mute egg: Class==EasterEgg && MasterVolume==0 → silent `avatar.Giggle(line,mood)` + mute echo, return :1595.
Audio `ResolveBarkAudio(variant.Audio)` :1286, 3-tier: (1) active mod `InstalledPath/resources/sounds/companion_audio/<file>`; (2) embedded per-mod `CompanionAudioFolder/mods/<ActiveModId>/<file>`; (3) embedded shared `CompanionAudioFolder/<file>`; else null. **Prebaked audio ONLY, no TTS**; variant→audio by its `Audio` filename field.
Routing :1620: `priority = Class!=Normal || Priority>=100`. Priority→`avatar.GigglePriority(line, playSound: audioPath==null, phraseAudioPath: audioPath, barkVoice: audioPath!=null, mood)` (clears queue). Else `avatar.Giggle(...)` (queues). Post: `KeywordTriggers.MuteKeywordEcho(line, 8000)` self-echo guard :1628; DtRH clip-second telemetry if run active :1607.

## (E) Trigger inventory + ctx → NotifyChaos* mapping (map ALL so chaos barks don't regress)
Awareness: `ActivityChanged`{activity=ServiceName, category, app_cluster, app} :562; `StillOnActivity`{activity, still_minutes} :574.
Notify* :181-255: Panic, AppOpened{away_bucket}, StreakMilestone{streak_days}[guaranteed], FeatureOpened{feature}, TabNavigated{tab}, SettingChanged{setting,value}(+setup debounce), AvatarChanged, AvatarClicked{clicks_60s}, EnhancementApplied{enhancement_id}, UiAction{action}, LeaderboardViewed{rank,total}.
Chaos Notify* :261-365 (map each existing port NotifyChaos* onto Raise("<Trigger>",fill)): ChaosRunStarted{difficulty}, ChaosWaveEscalated{wave}, ChaosBubbleDefused{combo,payload,difficulty}, ChaosBubbleDetonated{payload,strength,shield_absorbed,run_detonations,combo,difficulty}, ...Absorbed{shield_absorbed=true,shields_left}, ChaosBenignPopped{variant_id,payload,combo}, ChaosBoonPicked{boon}, ChaosCursePicked{boon,rarity,run_mult_bonus}, ChaosBoonSkipped{shields_now}, ChaosDarterCaught{points,combo,quick}, ChaosFreezeCaught{points,combo}, ChaosComboMilestone{combo,difficulty}, ChaosComboBig{combo,threshold}, ChaosActChanged{act,wave}, ChaosWaveCleared{wave}, ChaosEndingSoon, ChaosRunCompleted{xp,difficulty,runs_completed,rank}, ChaosDefuseFirst/NoFocus/Release, ChaosClickDetonate, ChaosFocusLow, ChaosTeaseDebut/Denied{denied_count}/Clicked/DeniedStreak{denied_count}, ChaosResultsShown{score,best_score,pb_delta,is_pb,defused,detonated,best_combo,difficulty,runs_completed,rank}, ChaosRankUp{rank}, ChaosRevealFlash{element}, ChaosLessonComplete{lesson_id}, ChaosGiftGiven, ChaosFirstTime{bonus_id}, ChaosDuoDemo, ChaosGoldFirst, ChaosDollhouseFirstOpen, ChaosDraftAutopick.
Wired events (WireSubscriptions :427): webcam Blink{blink_count}/MouthOpen/TongueOut/LongStare/FaceLost/FaceFound/TrackingStateChanged{state}; gaze GazePopped/GazeActiveChanged{active}; BlinkTrainerStateChanged{running}; video VideoAboutToStart/Started/Ended; AttentionCheckPass/Fail{video_playing,fail_count}(gated Video.IsPlaying); bubbles BubblePopped/Missed/BubbleCountCompleted/Failed/BouncingTextBounce/Corner; LockCardCompleted(coin-flip :984→guaranteed FireLockCardPoolBark{phrase,mistakes,repeats}); fx FlashDisplayed/SubliminalDisplayed/BrainDrainTriggered/MindWipeTriggered; KeywordTriggerFired{keyword,kw_effect}; progression LevelUp{level}/XPChanged{xp}/CompanionLevelUp{level}/UserMessageSent(stamps _lastUserMessageUtc)/SkillUnlocked{skill}/LuckyProc/PinkRushStarted/Ended; AchievementUnlocked{achievement}/RoadmapStepCompleted/TrackUnlocked/BadgeEarned; QuestCompleted/ProgressChanged/QuestsRefreshed; QuizCompleted{passed,perfect}(STATIC); MantraCompleted/StreakChanged{streak}/StreakBroken; control LockdownActivated/Deactivated/CountdownTick{remaining_sec}/RemoteCommandReceived{command}/ControllerConnectedChanged/ModChanged{mod,mod_switches_60s}(→ReloadRules)/IdleStateChanged{idle}/UpdateAvailable/PatreonTierChanged{tier,tier_up}/DiscordAuthChanged{authenticated}/TutorialCompleted/PersistentMemoryRecalled(STATIC)/WakeBambiRequested. Session (AttachSessionEngine :683): SessionStarted/Stopped/Completed/PhaseChanged{phase_index,phase_name,phase_is_deepener}/Progress{session_elapsed_sec}; anticipatory SessionSetupReady from debounce :923.

## (F) Reload lifecycle
Start() :123: CCP_BARK_DRYRUN env→DryRun; `_rules=BarkRuleLoader.Load()`; LoadRotationFromSettings(); capture launch recency; WireSubscriptions().
ReloadRules() :406: Load() under _gate, swap _rules, CLEAR _firedOnceSession + _lastFiredUtc (ids shared across mods), PRESERVE variant/idle rotation + persisted lifetime/tier latches. Invoked from ModChanged after the switch bark :668.

## Windows-specific → Core seams needed
DispatcherTimer (setup-ready debounce :918) + DispatcherHelper.RunOnUI :906 → Avalonia dispatcher seam. System.Windows.Point (OnLongStare :441) → portable point. NAudio AudioFileReader (DtRH clip duration :1610) → IAudioPlayer/duration seam. Static singletons (App.*, ChaosNarrator.IsPlaying, DtrhHostService, QuizService/LocalAiService static events) → DI services. App.Mods.ActiveMod.InstalledPath + CompanionPhraseService.CompanionAudioFolder → mod-path/content seam.

## Ambiguities (confirm before/while wiring — do not guess)
- BarkRule fields (Class, Scope, Mood, Priority, Chance, CooldownMs, Repeatable, VariantPool/PoolRef, Conditions, Trigger, Id) + enums BarkClass{Normal,Safety,EasterEgg,…}/BarkScope{Session,Tier,Lifetime}: referenced but the WPF Services/Bark/BarkRule.cs/BarkRuleSet.cs/BarkRuleLoader.cs not opened this pass — CONFIRM the Core port's BarkRule/BarkRuleSet.ForTrigger priority-descending ordering + enum parity against those files before wiring.
- avatar.Giggle/GigglePriority full signature (playSound/phraseAudioPath/barkVoice/mood/aiGenerated) inferred from call sites — confirm against the port AvatarTubeWindow speech API (AI-2 used GigglePriority(reaction, aiGenerated:true)+PlayDoubleBounce).
- AttentionCheckFail global-gap exemption relies on rule.Trigger matching the constant exactly — verify shipped manifests author that trigger.

## Slicing note (big/M-L — advisor before dispatch)
Likely 2-3 slices: (1) engine core (Raise/ConditionsPass/DecideLocked/EvaluateGate/CommitFire/variant + BarkState + settings persistence + BarkRuleLoader wiring) with Core unit tests; (2) Speak delivery + audio resolution + AvatarTube bubble/GigglePriority wiring; (3) trigger wiring (map NotifyChaos* + wired events + session + awareness) — the awareness subset IS AI-10. Preserve existing NotifyChaos* bare-string behavior until the engine replaces it (regression risk).
