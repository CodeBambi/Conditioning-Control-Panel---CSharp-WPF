# v6.2.10 "The Fall" → Avalonia port catalogue

Tracks the port status of every v6.2.10 WPF change into the Avalonia head
(`CCP.Avalonia` / `CCP.Core`). Produced after merging `main` (v6.2.10) into
`feat/crossplatform`. Discipline: `wpf-parity` sync-from-main workflow.

> **Core principle for this catalogue:** nothing from 6.2.10 is marked
> ported/DONE unless it is genuinely ported AND verified. New WPF behaviour
> is recorded here as **NEEDS PORT** so it is not silently lost to the merge.

## Merge + version

- **Merge:** `main` (v6.2.10 `aa425555`, fix commit `d4926d71`) merged into `feat/crossplatform`. **Clean, zero conflicts** (ort strategy auto-merged `ConditioningControlPanel.csproj`, `Localization/Languages/en.json`, `Services/BubbleService.cs`, `Windows/LockCardWindow.xaml.cs`). Merge base was `ace7b6fc` (v6.2.9) — **so everything up to and including 6.2.9 was already in the branch**; the only genuinely new commits are `d4926d71` (the two fixes) and `aa425555` (the version bump). 23 files changed; **zero** under `CCP.Avalonia`/`CCP.Core` source (the loc/csproj/UpdateService shared files auto-merged without touching Avalonia port code or any co-agent WIP).
- **Build health:** the merge itself is clean — with co-agent WIP aside, `CCP.Desktop.slnf` = **0 errors / 0 warnings**, WPF `ConditioningControlPanel.sln` = **0 errors / 0 warnings**. The only current slnf build error (`ChaosEStimArcLayer.cs` CS0117) is an **untracked co-agent WIP** (E-Stim arc, blocked Q10b, off-limits Compositor area) — pre-existing, **not** caused by this merge.
- **Version bump (this session):** all cross-platform heads **6.2.9 → 6.2.10** (7 `<Version>` csprojs + Android `<ApplicationDisplayVersion>` + `CCP.Core/Services/Update/UpdateService.cs` `CurrentVersion`/`AppVersion`/`CurrentPatchNotes`). This stops the "update available" pill/dialog: `GetCurrentVersion()` reads `AssemblyInformationalVersion` (from csproj `<Version>`) and the constants both now read 6.2.10 == GitHub-latest ⇒ not-newer. Localization `btn_v6_2_10_is_out` etc. flowed in from the merge (WPF loc, Content-linked by Core).

## What actually changed in 6.2.10 (two fixes, one commit `d4926d71`)

The release is two fixes. Both are bucketed below.

### Fix #493 — Gif Rain / "Cascade" visibility + motion (BEHAVIOURAL — needs Avalonia port)

### Fix #494 — render-thread freeze (WPF-RENDERING-SPECIFIC — structurally N/A in Avalonia, verify only)

## Catalogue

| # | v6.2.10 change | WPF source | Avalonia/Core verdict | Status |
|---|---|---|---|---|
| 1 | **Gif Rain visibility (#493):** cascade overlay now spans the **full virtual screen** (sized once, never resized — resizing a realized layered window risks a render-thread deadlock) plus a per-`Show` **spawn-band** (`_spawnLeft`/`_spawnWidth`) that confines WHERE clips fall: all monitors for dashboard trigger or chaos+DualMonitor, primary-only for single-screen chaos. Root cause: window was sized to the **primary screen only** → on multi-monitor the rain fell off the visible area ("Gif Rain does nothing"). | `Chaos/ChaosGifCascadeOverlay.cs` | **NEEDS PORT** → `CCP.Avalonia/Compositor/Layers/ChaosGifCascadeLayer.cs` (UCE Skia layer). **OFF-LIMITS** to a general porter: `CCP.Avalonia/Compositor/*` requires the `unified-compositor-engine` skill / a work package. The behavioural spec to port: (a) spawn across the correct monitor set (all monitors when triggered from the dashboard or when DualMonitor is on; primary-only for a single-screen chaos run), (b) never fall off the active screen on multi-monitor rigs. Architecture differs (Skia layer vs WPF Window) so the mechanism is re-implemented, not copied. | **NEEDS PORT (compositor lane)** |
| 2 | **Cascade/Rain trigger-bubble motion (#493):** dashboard effect bubbles now honour the dashboard Motion setting (`ChaosMotionMode`: Mixed/FloatUp/RainDown/RoamBounce) instead of always `FloatUp` — so "Rain" makes ambient bubbles fall. | `Services/BubbleService.cs` (behavioural half, ~L981-1010) + `Services/Chaos/ChaosBubbleVariants.cs` (~L751, `motionOverride` honoured) | **NEEDS PORT** → `CCP.Avalonia/Services/AvaloniaBubbleService.cs` has **no** `ChaosMotionMode`/`motionOverride`/`FloatUp`/`RainDown` references at all (grep: 0 matches). There is **no** Avalonia `ChaosBubbleVariants` file (WPF-only). **CORRECTED target + SCOPE (the earlier pin to `AvaloniaChaosStubs.cs` was WRONG — that `ChaosBubbleVariants` is a STUB: `All = new()`, empty catalog, and its L306-308 `ChaosMotionMode` parse feeds the chaos-RUN config, which already honours it).** The real Avalonia dashboard/ambient bubble engine is **`CCP.Core/Services/Chaos/BubbleEngine.cs`** — but the ambient bubble is a **SIMPLIFIED physics-rising model**: `SpawnBubble()` (L1638) builds a `BubbleState` with `Vx/Vy` (`vy = -(20+rnd*45)` < 0 ⇒ always rises) and uses **NO `ChaosMotion` enum** (that enum drives chaos-*mode* bubbles only, L1001-1024). So honouring the dashboard `ChaosMotionMode` is **NOT a surgical plug-in** — it requires **adding a motion-model** to the ambient spawn (FloatUp=rise [current], RainDown=fall-from-top, RoamBounce=bounce) to match WPF's `ChaosBubbleSpec`-based dashboard bubbles (`BubbleService.cs:981-1010`). A medium behavioral change, and the outcome (does it actually rain/bounce) is **visual ⇒ unverifiable headless**. (Blocker now resolved: co-agent landed the E-Stim layer `05520f52`; build green, tree clean.) **Defer to a focused session with visual verification** — do not half-implement (e.g. ignoring RoamBounce), which would be a false port. | **NEEDS PORT (service+motion)** |
| 3 | **Attention-check keep-alive popup pool (#494):** realize the `AllowsTransparency` popup HWND **once**, then reuse via reposition (`Left/Top`) + `Hide()`/`Show()`; `WM_DPICHANGED` swallow hook for mixed-DPI. Root cause: a fresh layered `Window.Show()` runs synchronous `HwndTarget.OnResize → MediaContext.CompleteRender()` and wedges the UI thread when the render thread is saturated. | `Services/AttentionCheckService.cs` | **N/A (architecturally absent)** — the Avalonia head renders the attention-check ring through the UCE `AttentionCheckLayer` on a single Skia surface per monitor; there is **no per-check top-level HWND created**, so the realization deadlock cannot occur. | **CLOSED (N/A) — verify**: confirm `AvaloniaAttentionCheckService`/`AttentionCheckLayer` does not create a fresh `Window` per check. |
| 4 | **Avatar floating-bubble keep-alive window pool (#494):** fixed-size (200px) pooled windows, rented/returned, `WM_DPICHANGED` swallow; same deadlock mechanism. | `AvatarTube/AvatarRandomBubble.cs` | **N/A (architecturally absent)** — Avalonia avatar bubbles render through the UCE BubbleLayer (no per-bubble layered HWND). | **CLOSED (N/A) — verify**: confirm no per-bubble top-level window in the Avalonia avatar-tube path. |
| 5 | **Avatar emote idle crossfade deferred to `Dispatcher.Background` (#494):** so the steady-state ~1Hz idle crossfade can't collide with a layered-window present on the shared software render thread. | `AvatarTube/AvatarTubeWindow.CirceEmotes.cs` | **N/A for the deadlock**; minor load-leveling heuristic. | **CLOSED (N/A) — verify**: the Avalonia emote crossfade path is already off the render-critical path (no shared software render thread in the UCE model). |
| 6 | **Lock-card keep-alive pool + show-defer + `IsAnyOpen` + Win32 focus reclaim (#494):** pooled `LockCardWindow` shells reused via `Configure()`/`Hide()`/`Show()`; a `_deferTimer` holds a show while a display change settles; `IsAnyOpen()` now also true while a show is deferred (so a poller doesn't conclude a card closed before it appeared). Same layered-window deadlock class. | `Windows/LockCardWindow.xaml.cs` | **N/A for the deadlock** (Avalonia lock cards render via UCE/`CCP.Avalonia/Windows/LockCardWindow.axaml.cs`, not per-card HWND). **BUT VERIFY ONE BEHAVIOURAL SEMANTIC:** the `IsAnyOpen()` "deferred-show counts as open" rule — if the Avalonia head has an analogous poller (e.g. a result window that checks "is a lock card open"), it must not race the card's appearance. | **CLOSED (N/A) — verify the IsAnyOpen semantic only.** |
| 7 | **Size-aware bubble window pool (#494):** pool keyed by quantized square side (128/256/384…) so a rental is always an exact size → no layered-window resize → no `CompleteRender` wedge + no DIB realloc. | `Services/BubbleService.cs` (freeze half, ~L2001-2840) | **N/A (architecturally absent)** — Avalonia UCE bubbles; no per-bubble layered window to resize. | **CLOSED (N/A).** |

## Summary of port debt introduced by 6.2.10

- **Two NEEDS-PORT items, both under the #493 (Gif Rain) umbrella:**
  - **#1** cascade spawn-band / multi-monitor visibility → **Compositor lane** (off-limits; `unified-compositor-engine` skill / WP).
  - **#2** dashboard bubble motion-override (`ChaosMotionMode`) → **deferred (medium behavioral change + visual verify)**: Avalonia ambient bubbles are a physics-rising-only model with NO `ChaosMotion` enum (`BubbleEngine.SpawnBubble` L1638); porting = ADD a motion-model (rise/fall/bounce), not a plug-in. Blocker cleared (E-Stim layer landed `05520f52`).
- **Five items are CLOSED (N/A):** the entire **#494 freeze fix** is WPF-rendering-specific (`AllowsTransparency` layered-window realization → `MediaContext.CompleteRender` deadlock). The Avalonia head's UCE renders these effects as layers on a single Skia surface per monitor, so the deadlock class is structurally absent. Three carry a light **verify** note (no per-instance HWND; one behavioural `IsAnyOpen` semantic to check).

## Verification gates (this session)

- `CCP.Desktop.slnf` build: **0 errors / 0 warnings** (with co-agent WIP `ChaosEStimArcLayer.cs` aside; restored after).
- WPF `ConditioningControlPanel.sln`: **0 errors / 0 warnings** (guardrail — merge brought 6.2.10 WPF code unchanged from main).
- Core tests: **542/542** (floor held).
