# v6.2.9 "The Fall" → Avalonia port catalogue

Tracks the port status of every v6.2.9 WPF change into the Avalonia head
(`CCP.Avalonia` / `CCP.Core`). Produced after merging `main` (v6.2.9) into
`feat/crossplatform`. Discipline: `wpf-parity` sync-from-main workflow.

## Merge + version

- **Merge:** `722f7dfa` — `main` (v6.2.9 `ace7b6fc`) merged into `feat/crossplatform`. Clean, zero conflicts. Merge base was `2b66b3c6` (v6.2.8). 28 WPF/shared files changed; **zero** under `CCP.Avalonia`/`CCP.Core` (no overlap with the Avalonia port or any co-agent WIP).
- **Version bump:** `cfedf33c` — all cross-platform heads 6.2.8 → **6.2.9** (8 csprojs + Core `UpdateService` `CurrentVersion`/`AppVersion`/`CurrentPatchNotes`). This stops the "update available" pill/dialog (the check compares GitHub-latest 6.2.9 vs the head's assembly version; equal ⇒ not-newer). Localization `btn_v6_2_9_is_out` etc. flow in automatically (CCP.Core Content-links the WPF `Localization/Languages/*.json`, which the merge updated).

## Bucketing (wpf-parity §Sync-from-main)

- **Localization JSON (9 files):** auto-sync via Content-link. No action. ✅
- **WPF-only infra:** `build-installer.bat`, `installer.iss`, `ConditioningControlPanel.csproj`, `hang-hunt.ps1` (WPF PowerShell dev harness). No action. ✅
- **Portable service logic / UI:** analysed per-change below.

## Catalogue (analysis 2026-07-05, six read-only agents vs the WPF diff)

| # | v6.2.9 change | WPF source | Avalonia/Core verdict | Scope | Status |
|---|---|---|---|---|---|
| 1 | Phrase-load resilience — a single bad member no longer wipes all phrases | `Services/Settings/SettingsService.cs` (JSON `Error` callback) | **PORT** → `CCP.Core/Services/Settings/SettingsService.cs` `Load()` (~L88) has no `Error` callback | ~15 LoC | **DONE ✅ `29fa1aa9`** |
| 2 | Corrupt settings preserved w/ timestamp + flags (not reset) | `SettingsService.PreserveCorruptSettingsFile` | **PORT** → CCP.Core `QuarantineCorruptSettingsFile` (~L130) → timestamped `.corrupt-<ts>.json` + `WasSettingsFileCorrupt`/`LastCorruptBackupPath` | ~20 LoC | **DONE ✅ `29fa1aa9`** |
| 3 | Bambi Takeover pacing: retry-soon when busy; "time aware" toggle actually applies | `Services/AutonomyService.cs` | **PORT** → `CCP.Avalonia/Services/Autonomy/AvaloniaAutonomyService.cs:335-379` — add `RetryIntervalSeconds`, short-retry reschedule, apply `GetTimeMultiplier()` (currently computed-then-ignored) | ~60 LoC | **DONE ✅ `fa897a0f`** |
| 4 | Takeover quest counts real active time (no more 15-min→1-hr drift) | `Services/Progression/AchievementService.cs` | **PORT** → `CCP.Core/Services/Progression/AchievementService.cs:319-327` — replace hard `elapsed < 0.1` drop with `Math.Min(elapsed, AutonomyTickCreditCapMinutes)` | ~15 LoC + Core test | **DONE ✅ `39ec2471`** |
| 5 | Takeover slot leak — release the interaction slot on abnormal teardown | `Services/UI/InteractionQueueService.cs` `CompleteIfCurrent` | **BACKLOG (coordinated unit)** — the safe `CompleteIfCurrent` guard is only valuable wired into `AvaloniaVideoService.ForceCleanup`, which is a **CO-AGENT lane** (uncommitted WIP). Land the queue guard + wiring together once that lane is free. | ~40 LoC | **BACKLOG** |
| 6 | Lock-card phrases change with the active mode | `Services/ModService.cs` (`ReconcileLockCardPhrasesWithActiveMod`) | **PORT (own session)** → `CCP.Avalonia/Services/Mod/AvaloniaModService.cs` `ActivateMod` does no phrase reconciliation; port `ReconcileLockCardPhrasesWithActiveMod` + a default-phrase accessor | ~250 LoC | **BACKLOG** |
| 7 | Flash-window CompleteRender UI hangs | `Services/Flash/FlashService.cs` | **N/A** — WPF `FlashWindow`/`CompleteRender`/DPI-hook specific; the Avalonia head renders flash through the compositor (`FlashLayer` + `AvaloniaFlashService`, async decode → Skia), so the deadlock class is architecturally absent | — | **CLOSED (N/A)** |
| 8 | Season shimmer revived | `MainWindow/MainWindow.Animations.cs` | **BACKLOG (pre-existing gap)** — Avalonia `AnimationsTabView`/`AnimationsTabViewModel` toggles `IsSeasonTitleShimmerActive` but never implemented the shimmer render; not a regression from this merge | — | **BACKLOG** |
| 9 | Settings: advanced-audio options collapse | `Views/Tabs/SettingsTabView.xaml(.cs)` | **DONE ✅** — `CCP.Avalonia/Views/Tabs/SettingsTabView.axaml`: ▾/▴ ToggleButton (`BtnAudioAdvanced`) in the Audio header; advanced options (video volume, duck volume, output device, don't-duck-browser + test) wrapped in a `StackPanel IsVisible="{Binding #BtnAudioAdvanced.IsChecked}"`. Master volume + duck-enable stay always visible. Pure AXAML (no VM change), mirrors WPF `SettingsTabView.xaml:1193-1247`. Gates: slnf 0 err, smoke 44 tabs / Findings 5 / exit 0. | small | **DONE ✅** |
| 10 | Settings: logout pink border | `SettingsTabView.xaml` | **DONE ✅** — `CCP.Avalonia/Views/Tabs/SettingsTabView.axaml` logout button restyled to prominent (solid `PinkBrush` bg, white bold text, `Padding=16,7`, `FontSize=12`), matching WPF `SettingsTabView.xaml:1406-1419` ("Prominent, high-contrast Logout button"). Gates: slnf 0 err. | tiny | **DONE ✅** |
| 11 | Phrase export/import UI (Phrase Manager) | `MainWindow.PresetIO.cs` + new `PhraseBackupService` | **BACKLOG (new feature)** — no phrase-backup UI in Avalonia; needs a `CCP.Core/Services/Settings/PhraseBackupService` + a dialog/tab | large | **BACKLOG** |
| 12 | Mandatory video no longer jumps above bubbles when clicked (z-order) | `Services/Video/VideoService.cs` | **CO-AGENT LANE — note only** — `AvaloniaVideoService.cs`/`IVideoService.cs` are the co-agent's uncommitted WIP; do NOT touch | — | **CO-AGENT** |

## Actioned in this session (Core/service bug fixes, gates: slnf 0 · WPF 0 · Core ≥floor)

All three tier-1 ports landed, each its own gated commit (service/logic, no compositor
layer touched, so `--verify-layers` is not a gate):

- **#4** Takeover quest active-time → `39ec2471` (Core 540 → 541; extracted
  `ComputeAutonomyTickCredit` + regression test).
- **#1+#2** phrase/settings load resilience → `29fa1aa9` (Core 541 → 542; resilient JSON
  `Error` callback + timestamped corrupt-file preservation; updated 2 FileIo tests + 1 new).
- **#3** Takeover pacing → `fa897a0f` (short-retry-when-blocked + ported `GetTimeMultiplier`;
  slnf 0 · WPF 0 · Core 542 · `--smoke-test` Findings 5 baseline + chaos run clean).

Core test floor is now **542** (`run-gates.sh`).

## Deferred (backlog rows added to `avalonia-migration-task-board.md`)

- **#5** interaction-queue guard — coordinated with the co-agent's video lane.
- **#6** per-mode lock-card phrases — own focused session (~250 LoC).
- **#9** advanced-audio collapse (UI) — near-term.
- **#8** season shimmer, **#10** logout border, **#11** phrase export/import UI — lower priority.
- **#12** mandatory-video z-order — the co-agent owns `AvaloniaVideoService`.
