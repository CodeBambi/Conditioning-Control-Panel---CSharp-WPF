# Avalonia Migration Task Board

Master task list derived from `docs/crossplatform-rebuild-plan.md` and the detailed breakdowns produced by planning sub-agents.

> **Swarm execution:** this board is the live work queue for a multi-agent swarm. **Read
> `docs/crossplatform-rebuild-plan.md` §20 (Multi-Agent Swarm Execution & Context Discipline) before
> claiming or assigning anything.** Claim work via the **Active Claims Ledger** below — do **not** hand-edit
> the same file as another agent, and never touch a §20.1 chokepoint from a porter lane (route those changes
> through the Hand-off Queue to the orchestrator).

## Legend

- **Priority:** `P0` blocker · `P1` critical path · `P2` parallelizable · `P3` polish/final parity.
- **Status markers** (used in the ledger and, optionally, inline on a phase item):
  - `⬜ todo` — unclaimed, available to pick up.
  - `🚧 wip @agentN` — claimed / in progress by `agentN`.
  - `🔵 review` — complete in a worktree, awaiting orchestrator integration.
  - `✅ done` — merged and the integration build is green.
  - `🚫 blocked` — see the ledger Notes column.

---

## Swarm Coordination

> Single source of truth for *who is doing what right now*. Mirrors plan §20.4. Prefer **appending rows**
> over editing scattered list items — appends rarely conflict; in-place edits across the board do.

### Active Claims Ledger

Append a row to **claim**; the orchestrator marks `✅ done` and removes the row after a clean integration build.

| Lane (owned subtree) | Item | Owner | Status | Branch / worktree | Updated | Notes |
|---|---|---|---|---|---|---|
| `CCP.Core/App.cs` + Core `App.X` call sites | Collapse WPF `Models/` duplicates into `CCP.Core` single source of truth | orchestrator | `✅ done` | `main` | 2026-06-21 | Added `ProjectReference` from WPF to `CCP.Core`; pinned `Microsoft.WindowsAppSDK` with `NoWarn="NU1605"`; deleted WPF `Models/` folder; removed duplicate `CatalogueEntry`, `HapticProviderType`, `XPSource`, and `PackFileEntry` definitions from WPF services and redirected to Core models; made `AppSettings.MigrateFromContentModeToMod` public; qualified `LibVLCSharp.Shared.Core.Initialize` calls. Full solution (`ConditioningControlPanel.sln`) builds 0 errors/0 warnings; `CCP.Core.Tests` 95/95 pass; Avalonia Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Views/Deeper/DeeperEditorWindow` | Curve editor for custom haptic patterns | orchestrator | `✅ done` | `main` | 2026-06-21 | |
| `CCP.Avalonia/Views/Deeper/DeeperEditorWindow` | Browser preview + audio waveform cache | orchestrator | `✅ done` | `main` | 2026-06-21 | |
| `CCP.Avalonia/Dialogs/LocalAiSetupWizard` + `CCP.Core/Services/AIService` | Port remaining WPF-only dialog and its `OllamaSetupService` dependency to Avalonia/Core | orchestrator | `✅ done` | `main` | 2026-06-21 | |
| `CCP.Avalonia/Dialogs/MessageBoxStub.cs` + `Features/VideoFeatureControl` | Remove unused `MessageBoxStub` and wire `VideoFeatureControl.BtnTestVideo_Click` via `IDialogService` / `IVideoService` | swarm-1 | `✅ done` | `main` | 2026-06-21 | `MessageBoxStub.cs` deleted (0 real usages); `BtnTestVideo_Click` wired via `IDialogService` confirmations + `IVideoService`/`IInteractionQueueService` behind existing seams; `BtnManageAttention_Click`/`BtnAttentionStyle_Click` unblocked with ported dialogs. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass. |
| `CCP.Avalonia/Features/*` | Feature controls code-behind: file pickers, audio seams, dispatch seams, and `FeatureSettingsPopup` parity (wired `IUiDispatcher`/`IDialogService`/`IFilePickerService`; implemented video test/phrase/style handlers, mind-wipe audio reload, panic/offline dialogs; removed `StorageProvider`/`Dispatcher.UIThread` usage; added `x:DataType`/DynamicResource to touched `.axaml`) | swarm-2 | `✅ done` | `main` | 2026-06-21 | |
| `CCP.Avalonia/Views/Deeper/*` | Deeper player/editor integration live smoke test and fixes | swarm-3 | `✅ done` | `main` | 2026-06-21 | Wired `DeeperLibraryRowViewModel.OpenAsync`/`PlayAsync` and `DeeperHubTabViewModel.OpenEntryAsync`/`PlayEntryAsync` to real `DeeperEditorWindow`/`EnhancementPlayerWindow` instances. Implemented `DeeperTabViewModel.ImportEnhancementsAsync` file loading and `ScanLibraryAsync` auto-load from `DeeperLastDirectory`. Fixed compile regressions outside lane: `VideoFeatureControl.axaml.cs` missing `AppSettings` using, `SystemFeatureControl.axaml.cs` missing `AppSettings` using + `_logger`, `FeatureSettingsPopup.axaml.cs` missing `_dialogService`, `QuizWindow.axaml` WPF Storyboard resources conflicting with swarm-5's Avalonia Animation code-behind. Build gate 0 errors; Core tests pass; Windows head smoke test runs 20s without crash. |
| `CCP.Avalonia/Views/Tabs/*` | Partial tab view parity: Haptics, BlinkTrainer, Patreon, AvailableSubjects, DeeperSubmissions | swarm-4 | `✅ done` | `main` | 2026-06-21 | Replaced hard-coded accent/muted hexes with DynamicResource across all five tab AXAMLs; added missing `{loc:Str}` bindings and localized hard-coded VM strings. Haptics: tooltip guide, `PinkButton` theme, two-column per-event layout. BlinkTrainer: bound `IncludeVideos` ToggleSwitch, wired `PremiumGateOverlay.UnlockCommand`, added `UnlockCommand`. Patreon: local-brush resources for provider brand/success colors, localized status/expiry/link strings. AvailableSubjects: `BecomeSubjectCommand` binding, localized Connect/Taken buttons, DangerBrush error border, design-time sample cards. DeeperSubmissions: `ReflectionBinding` row command, design-time sample rows. New loc keys appended to `tools/new-localization-keys.json`. Build gate 0 errors; Core tests pass. |
| `CCP.Avalonia/Windows/Quiz*.axaml*` + `PopQuizWindow` | `QuizWindow` / `PopQuizWindow` layout and animation parity | swarm-5 | `✅ done` | `main` | 2026-06-21 | Replaced WPF Storyboard resources with Avalonia `Animation` equivalents (glow pulse, score pulse, question fade-in). Restored animated background gradient, drone audio loop, surrender easter-egg mode, random effect triggers, and avatar muting. Localized all hard-coded strings; new keys appended to `tools/new-localization-keys.json`. Desktop build and Core tests pass. |
| `CCP.Avalonia.Desktop.Windows` smoke test | Launch Avalonia Windows head, catalog runtime errors, and fill parity-matrix gaps | swarm-6 | `✅ done` | `main` | 2026-06-21 | Implemented `--smoke-test` mode in `Program.cs` + `SmokeTestRunner.cs`/`SmokeTestLogSink.cs`. 44 tabs, 2 dialogs, 3 mod switches exercised; 0 runtime exceptions, 0 raw loc keys, 0 binding/resource warnings. Build blockers from sibling lanes resolved; re-run confirmed green. |
| `CCP.Avalonia/App.axaml` + theme service | Per-mod dynamic palette: implement `AvaloniaThemeService`, wire mod-change theme application, add runtime theme resources | orchestrator | `✅ done` | `main` | 2026-06-21 | Added `IModService` theme accessors, `AvaloniaThemeService`, DI registration, and `App.axaml.cs` startup wiring. Smoke-test mod switches now exercise theme-change path with 0 exceptions. |
| Hard-coded theme hex audit — Views/Tabs + shell | Replace literal accent hexes in `Views/Tabs/*.axaml`, `Views/MainWindow.axaml`, `Views/MainView.axaml` with `DynamicResource` theme keys | swarm-7 | `✅ done` | `main` | 2026-06-21 | Replaced mapped theme hexes with DynamicResource keys (PinkBrush, DarkPinkBrush, PanelBgBrush, SurfaceBgBrush, ElevatedSurfaceBrush, PanelAccentBrush, TextDimBrush, TextLightBrush, DangerBrush, TransparentPink*Brushes, etc.). Added `x:DataType` checks for all touched `.axaml` roots. Left 3 ambiguous non-brush hexes with `theme-audit` comments (MainWindow BoxShadow #FF69B4; Awareness CommandParameter #FF69B4; Lockdown BoxShadow #FF1493). Brand/discord/deeper/one-off colors untouched. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass. |
| Hard-coded theme hex audit — Features | Replace literal accent hexes in `Features/*.axaml` with `DynamicResource` theme keys | swarm-8 | `✅ done` | `main` | 2026-06-21 | Replaced mapped theme hexes/named colors with DynamicResource keys across all `Features/*.axaml` and updated color references in `FeatureCard`, `ContentPackCard`, `PresetCard`, `SkillNodeCard`, and `SpiralFeatureControl` code-behind. Added missing `x:DataType` to all `.axaml` roots in the lane. Left ambiguous non-map colors (success greens, orange external badge, black overlays, dark greys) untouched. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass. |
| Hard-coded theme hex audit — Windows/Dialogs | Replace literal accent hexes in `Windows/*.axaml` and `Dialogs/*.axaml` with `DynamicResource` theme keys | swarm-9 | `✅ done` | `main` | 2026-06-21 | Replaced mapped theme hexes/named colors with DynamicResource keys across `Windows/*.axaml`, `Dialogs/*.axaml`, `DialogResources.axaml`, and code-behind. Added missing `x:DataType` to all `.axaml` roots in the lane. Updated `DialogResources.axaml` brushes to alias App theme colors dynamically. Fixed one build blocker outside the lane (`AvatarTubeWindow.axaml` DynamicResource→StaticResource for CLR converter properties). Left ambiguous non-map colors (e.g., dark purple greys, one-off illustration colors, BoxShadow hexes, mod/theme editor default strings) untouched. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass. |
| Hard-coded theme hex audit — Chaos/AvatarTube/Converters/Services | Replace literal accent hexes in `Chaos/*.axaml`, `AvatarTube/*.axaml`, `Converters/*.cs`, and related services with `DynamicResource` theme keys | swarm-10 | `✅ done` | `main` | 2026-06-21 | Replaced mapped theme hexes/named colors with `DynamicResource` keys across `Chaos/*.axaml`, `AvatarTube/*.axaml`, `Converters/*.cs`, `ChaosHubWindow`/`ChaosHudWindow`/`ChaosOverlayWindow`/`AvatarTubeWindow` code-behind, `AvaloniaBubble`, `AvaloniaOverlayService`, and `AvaloniaVideoService`. Added missing `x:DataType` to all `.axaml` roots in the lane. Updated `BoolToBrushConverter` and `MediaTypeToBrushConverter` to resolve theme resources at runtime; pink filter overlay and bubble fallback tint now derive color from active mod `PinkColor`. Left ambiguous non-map/special-effect colors untouched (cursor-glow pink/gold, vibe trail warm/cool gradients, E-Stim violet/cyan, focus bar cyan, currency gold, status greens/reds, Patreon brand purples, Discord blue, video message fallback magenta). `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass. |
| `CCP.Avalonia/Windows/SeasonRecapWindow` | Clipboard image copy + save-to-PNG parity | orchestrator | `✅ done` | `main` | 2026-06-21 | Added missing `using Avalonia.Input.Platform;` so `IClipboard.SetBitmapAsync(Bitmap)` resolves in Avalonia v12. Save-to-file path uses `RenderTargetBitmap.Save(Stream)` directly. Localized the clipboard-unavailable and save-error messages via `tools/new-localization-keys.json` + `python tools/merge-localization-keys.py`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Views/Deeper/NewEnhancementDialog` + `IDialogService` | New-enhancement dialog browse parity | orchestrator | `✅ done` | `main` | 2026-06-21 | Added optional `initialDirectory` to `IDialogService.ShowOpenFileDialogAsync` and `AvaloniaDialogService` (uses `StorageProvider.TryGetFolderFromPathAsync`). `NewEnhancementDialog` browse now starts in `DeeperLastDirectory`. HypnoTube tutorial button now mirrors WPF: selects the active mod's first TikTok-named `DefaultVideoLinks` entry (fallback to `https://hypnotube.com`) and sets `HasSeenDeeperHTInteractiveTutorial`. The interactive overlay tutorial itself remains unported. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/LabTabViewModel` + `App.axaml` | Lab tab dialog localization / theme status colors | orchestrator | `✅ done` | `main` | 2026-06-21 | Localized all user-facing dialog titles/messages and debug-log strings in `LabTabViewModel`. Added `Success`/`SuccessBrush` theme resources to `App.axaml`. Replaced hard-coded `#FF00C853`/`#FFFF69B4`/`#FF888888` status brush parses with runtime theme-resource lookups (`SuccessBrush`, `PinkBrush`, `TextMutedBrush`). `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/App.axaml.cs` + `ViewModels/Tabs/*` | Remaining hard-coded dialog string audit | orchestrator | `✅ done` | `main` | 2026-06-21 | Scanned all Avalonia `ShowMessageAsync`/`ShowConfirmationAsync` call sites; localized the last hard-coded titles/messages in `App.axaml.cs` (global exception + startup failure), `SettingsTabViewModel` (audio diagnostics), `AssetsTabViewModel` (selection saved), and `AppInfoTabViewModel` (audio/video smoke tests). Added 12 new loc keys via `tools/new-localization-keys.json` + merge. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Views/Tabs/*`, `AudioSpikeWindow` | Raw grey text-color audit | orchestrator | `✅ done` | `main` | 2026-06-21 | Replaced remaining raw `#CCCCCC` text foregrounds with `{DynamicResource TextDimBrush}` and raw `#888888` foregrounds with `{DynamicResource TextMutedBrush}` across `AwarenessTabView`, `BambiTakeoverTabView`, `LabTabView`, and `AudioSpikeWindow`. Verified no common grey hexes remain in `Foreground` attributes. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Views/Deeper/GazePickerWindow` | Embedded live video preview behind pick overlay | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `LibVLCSharp.Avalonia.VideoView` to `GazePickerWindow.axaml`; code-behind loads the enhancement's `MediaSource` (local file or remote URL) via DI-injected `LibVLC` and a local `MediaPlayer` when the window loads, mutes playback, and disposes the player/media on close. `DeeperEditorWindow` now passes `_enhancement.MediaSource` to the picker. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Views/TabContentDataTemplates.axaml` + `PlaceholderTabView` | Fix generic tab placeholder fallback (map `AvailableSubjectsTabViewModel`, add diagnostics) | orchestrator | `✅ done` | `main` | 2026-06-21 | Added missing `AvailableSubjectsTabViewModel` → `AvailableSubjectsTabView` mapping in `TabContentDataTemplates.axaml` (used by mobile shell). Added tab `Key` debug line to `PlaceholderTabView` so any future unmapped tab is identifiable. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Windows/ModCreatorWindow` | Wire cross-platform color picker + audio preview | orchestrator | `✅ done` | `main` | 2026-06-21 | `ShowColorPickerAsync` now opens `ColorPickerDialog` and writes the chosen hex back to the theme color text box. `ToggleAudioPreview` resolves resource keys via `_audioSlots` and plays the file through DI-injected `IAudioPlayer`, with stop/toggle UI feedback. Tutorial overlay remains unported. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Core/Services/Companion` + `CCP.Avalonia/ViewModels/Tabs/CompanionTabViewModel` | Port companion + community-prompt services to Core; wire Companion tab commands | orchestrator | `✅ done` | `main` | 2026-06-21 | Added `ICompanionService`/`ICommunityPromptService` to Core. Implemented `AvaloniaCompanionService` (switch/active/progress/XP) and `AvaloniaCommunityPromptService` (install/import/remove/activate/deactivate/fetch). Wired DI, updated `CompanionTabViewModel` to use real services for switch, assign personality, activate/remove/deactivate prompts, and open `CompanionPromptEditorDialog`. `CCP.Desktop.slnf` and full solution build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Services/Haptics/AvaloniaHapticsService` | Replace stub haptics service with real Buttplug.io implementation | orchestrator | `✅ done` | `main` | 2026-06-21 | Rewrote `AvaloniaHapticsService` to connect to Intiface via `ButtplugWebsocketConnector`, scan/discover vibrate-capable devices, send vibration test/pattern/stop commands, and raise `ConnectionStateChanged`/`DeviceAdded`/`DeviceRemoved` events. `CCP.Desktop.slnf` and full solution build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (0 findings). |
| `CCP.Avalonia/Windows/ModCreatorWindow` + tutorial service | Port Mod Creator interactive tutorial overlay | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `TutorialType.Modding` and `CreateModdingSteps` to `AvaloniaTutorialService`; `ModCreatorWindow.LaunchTutorial` starts the tutorial, wires `mod:xxx` section navigation, and shows `TutorialOverlay`. On window close the overlay is torn down. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Platform/AvaloniaAudioDeviceService` | Cross-platform default audio endpoint detection | orchestrator | `✅ done` | `main` | 2026-06-22 | Implemented `GetDefaultOutputDeviceId`: prefer explicit selection, then query PulseAudio (`pactl info`) on Linux and `SwitchAudioSource -c` on macOS, then fall back to the first enumerated LibVLC device. Windows continues to use `WindowsAudioDeviceService` in the Windows head. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia.Desktop.Windows/Services/Webcam` | Port WPF webcam tracker to Avalonia Windows head | agent-15 | `✅ done` | `main` | 2026-06-22 | Added `AvaloniaWebcamTrackingService` implementing `IWebcamService`, registered in `Program.cs`. Replaced WPF `System.Windows.Point`/`Screen`/`Dispatcher` with `CorePoint`/`IScreenProvider`/`IUiDispatcher`; kept privacy contract, ONNX model resolution, all detection logic and extra events. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/App.cs` + `CCP.Avalonia/AvatarTube` + Companion VM | Wire Companion tab avatar attach/detach toggle | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `IAvatarWindowService.SetDetached` (with default no-op so WPF still builds), implemented it in `AvaloniaAvatarWindowService`, exposed `AvatarTubeWindow.SetDetached`, and wired `CompanionTabViewModel.ToggleDetachCommand` to call the service. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Chaos/*` overlay windows | Apply Win32 toolwindow/noactivate/transparent ex-styles to Chaos overlays | agent-16 | `✅ done` | `main` | 2026-06-22 | Updated 13 Chaos overlay windows to set `IsHitTestVisible`, `ShowInTaskbar=false`, `ShowActivated=false`, etc., and call a shared Win32 helper to apply `WS_EX_TOOLWINDOW\|WS_EX_NOACTIVATE` (plus `WS_EX_TRANSPARENT` for click-through overlays). `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Dialogs/AttentionTargetEditorDialog` | Wire color picker + floating preview | orchestrator | `✅ done` | `main` | 2026-06-22 | Replaced `PickColor` stub with `ColorPickerDialog`; color buttons now open the picker and update the preview. `BtnTest` now spawns a centered, topmost, transparent, 3-second floating preview window using the current colors/font/border settings. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.Avatar.cs` | Switch companion when avatar set changes | orchestrator | `✅ done` | `main` | 2026-06-22 | Wired `AvatarTubeWindow.SwitchAvatarSet` to call `ICompanionService.SwitchCompanion` when the selected avatar set maps to a companion. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` | Wire content-policy warning dialog | orchestrator | `✅ done` | `main` | 2026-06-22 | `OnWarningTriggered` now opens `ContentPolicyWarningDialog` with the current hit count on the UI thread. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Moderation` + `CCP.Avalonia` | Port real sliding-window moderation counter to Core/Avalonia | orchestrator | `✅ done` | `main` | 2026-06-22 | Moved `IModerationCounter`/`ModerationCounterState` to Core; implemented `ModerationCounter` using `IAppEnvironment.ApplicationDataPath` with serialized disk writes to prevent lost updates. Replaced `AvaloniaModerationCounter` stub with Core implementation; registered in DI; load from disk at startup. Cooldown UI already wired in `AvatarTubeWindow`. Added 6 unit tests; `CCP.Core.Tests` now 101/101 pass. `CCP.Desktop.slnf` build 0 errors; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` | Implement `TriggerBambiCumAndCollapse` | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `VoiceLineFolder` to `ICompanionPhraseService`, implemented it in `AvaloniaCompanionPhraseService`, and wired the rapid-click trigger to play a random `come and coll*.mp3` via `IAudioPlayer` + show the priority giggle, matching WPF behavior. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Core/App.cs` + `CCP.Avalonia/Dialogs/WelcomeDialog` | Add `IModService.GetAffirmation` and wire WelcomeDialog | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `GetAffirmation()` to `IModService`, implemented it in `AvaloniaModService` (falls back to "Subject"), and updated `WelcomeDialog` to resolve the active mod's affirmation instead of hard-coding "Subject". `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.ChatInput.cs` | Cross-platform chat-input foreground focus | orchestrator | `✅ done` | `main` | 2026-06-22 | Implemented `ForceForegroundWindow` with Win32 `AttachThreadInput`/`SetForegroundWindow` on Windows and a Topmost-pulse fallback on Linux/macOS; wired it into `ShowInputPanel` before focusing the text box. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` | Markdown + URL hyperlink parsing in speech bubble | orchestrator | `✅ done` | `main` | 2026-06-22 | Replaced `BuildLinkedInlines` stub with markdown `[text](url)` and raw URL detection, rendering clickable `HyperlinkButton` links via `InlineUIContainer` (Avalonia v12 has no inline `Hyperlink`). Clicks open the URL with the system browser. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Platform/AvaloniaWallpaperProvider` | Cross-platform wallpaper override | orchestrator | `✅ done` | `main` | 2026-06-22 | Replaced the no-op stub with real implementations: Windows uses `SystemParametersInfo(SPI_SETDESKWALLPAPER)`, Linux tries `gsettings` then `feh`, macOS uses `osascript`. All failures are swallowed so the feature degrades gracefully on unsupported desktops. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Platform/AvaloniaWindowChrome` + `Views/MainWindow` | Windows dark title bar | orchestrator | `✅ done` | `main` | 2026-06-22 | Implemented `SetDarkTitleBar` using `DwmSetWindowAttribute` (DWMWA_USE_IMMERSIVE_DARK_MODE with pre-20H1 fallback) and wired `MainWindow` to apply it on construction, `Opened`, and theme changes. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Core/Platform/IMouseHook` + `CCP.Avalonia/Platform/AvaloniaMouseHook` + `CCP.Core/Services/Chaos/BubbleEngine` + `CCP.Avalonia/Services/AvaloniaBubbleService` | Add `IMouseHook.LeftButtonUp` and route shared-host bubble release to `EndActiveChaosChannel` | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `LeftButtonUp` event to `IMouseHook`; `AvaloniaMouseHook` raises it on `WM_LBUTTONUP`; `BubbleEngine.EndActiveChaosChannel()` ends the live bubble channel; `AvaloniaBubbleService` subscribes in shared-host mode. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Core/App.cs` + `CCP.Avalonia/Services/AvaloniaHeadStubs.cs` + `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` + `CCP.Avalonia/ViewModels/Tabs/CompanionHubTabViewModel` | Wire CompanionHub tab avatar commands through `IAvatarWindowService` | orchestrator | `✅ done` | `main` | 2026-06-22 | Extended `IAvatarWindowService` with `ShowTube`, `HideTube`, `SetPose`, `Giggle`, and `IsVisible`; implemented them in `AvaloniaAvatarWindowService` with lazy window creation; added public `AvatarTubeWindow.ShowGiggle(string)` wrapper. `CompanionHubTabViewModel` now routes Initialize/Show/Hide/Wake/ToggleDetach/SetPose/Mute to the service instead of showing "not implemented" dialogs. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/MindWipe/IMindWipeService.cs` + `CCP.Avalonia/ViewModels/Tabs/LevelFeaturesTabViewModel` | Wire LevelFeatures tab effect commands to live services | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `UpdateSettings` to `IMindWipeService`; injected `ISessionService`, `IBubbleCountService`, `IBouncingTextService`, `IMindWipeService`, and `IOverlayService` into `LevelFeaturesTabViewModel`. Toggles now start/stop services when a session is running, frequency/volume/intensity changes propagate live, test commands trigger Bubble Count/Bouncing Text/Mind Wipe, and Mind Wipe loop toggles `StartLoop`/`StopLoop`. Added `msg_level_feature_settings_on_dashboard` localization key. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/App.cs` + `CCP.Avalonia/Services/AttentionCheck/AvaloniaAttentionCheckService.cs` + `CCP.Avalonia/Features/AttentionCheckFeatureControl.axaml.cs` + `CCP.Avalonia/App.axaml.cs` | Port AttentionCheck service to Avalonia | orchestrator | `✅ done` | `main` | 2026-06-22 | Extended `IAttentionCheckService` with `Start`, `Stop`, `IsRunning`, `OnPass`, and `OnFail`; implemented a real Avalonia scheduler that creates a click-through topmost window with `AttentionCheckControl`, subscribes to `IWebcamService.OnGazeMove`, tracks dwell/grace, awards XP on pass, and applies the configured fail penalty (XP loss or lock card). `AttentionCheckFeatureControl` now starts/stops the service from its enable toggle; `App.axaml.cs` auto-starts it at startup when enabled. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/SessionLog` + `CCP.Avalonia/Services/Sessions/AvaloniaSessionEffectOrchestrator` + `CCP.Avalonia/ViewModels/MainWindowViewModel` + `CCP.Avalonia/Services/Flash/AvaloniaFlashService` + `CCP.Avalonia/Services/Video/AvaloniaVideoService` | Port SessionLogService to Core and wire into Avalonia | orchestrator | `✅ done` | `main` | 2026-06-22 | Created `ISessionLogService`/`SessionLogService` in `CCP.Core`; moved interface members (`BeginSession`, `EndSession`, `LogReady`, `LoadRecentLogs`) from `CCP.Core/App.cs` to the new namespace. Extended `IFlashService` with `LastDisplayedImagePaths` + events and `IVideoService` with `LastVideoPath` + events. Implemented path capture in `AvaloniaFlashService`; `AvaloniaVideoService` already had `LastVideoPath`/`VideoStarted`. Wired `AvaloniaSessionEffectOrchestrator` to begin logging, `MainWindowViewModel` to end logging + show `SessionCompleteWindow` on completion/stop, and `App.axaml.cs` to set `CoreApp.SessionLog`. Removed stub `AvaloniaSessionLogService`. `SessionLogHistoryWindow` now uses the real service. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF head still builds. |
| `CCP.Core/Services/BugReport` + `CCP.Avalonia/Windows/BugReportWindow` + `CCP.Avalonia/ServiceCollectionExtensions` | Port BugReportService + LogScrubber to Core and wire Avalonia window | orchestrator | `✅ done` | `main` | 2026-06-22 | Created `IBugReportService`/`BugReportService` and `LogScrubber`/`ScrubberCounts` in `CCP.Core` (namespace `ConditioningControlPanel.Core.Services.BugReport`). Ported the WPF service's metadata collection, log scrubbing, HMAC signature, and `/bug/upload` POST. Replaced the in-process stub in `BugReportWindow` with DI-injected `IBugReportService`. Registered `BugReportService` as a singleton in Avalonia DI. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF head still builds. |
| `CCP.Core/Platform/IRemoteControlService` + `CCP.Core/Services/RemoteControl` + `CCP.Avalonia/ServiceCollectionExtensions` + `CCP.Avalonia/ViewModels/Tabs/RemoteControlTabViewModel` | Port RemoteControl network polling to Core (slice 1: real session/poll/status/emote) | orchestrator | `✅ done` | `main` | 2026-06-22 | Extended `IRemoteControlService` with `ControllerIdle`, `CommandReceived`, `Tier`, `PushStatusNowAsync`, and `SendEmoteAsync`. Added Core `RemoteControlService` with network polling to `codebambi-proxy.vercel.app`, status pushes, directory opt-in, emote sending, and rate-limit backoff. Added `IRemoteCommandExecutor`/`IRemoteStatusProvider` seams for slice 2. Replaced Avalonia stub registration with Core `RemoteControlService`. Wired `RemoteControlTabViewModel` `CommandReceived` log and `SendEmoteAsync`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Services/RemoteControl` + `CCP.Avalonia/ServiceCollectionExtensions` + `CCP.Avalonia/ViewModels/Tabs/RemoteControlTabViewModel` | Wire RemoteControl command executor in Avalonia (slice 2) | orchestrator | `✅ done` | `main` | 2026-06-22 | Extended `IRemoteCommandExecutor` with `StopAllRemoteEffectsAsync`/`HandleControllerDisconnectAsync`; Core `RemoteControlService` calls them on session end/disconnect. Added `AvaloniaRemoteCommandExecutor` mapping server actions to Flash, Subliminal, Overlay, Bubble, Video, Haptics, AudioDucker, BubbleCount, LockCard, MindWipe, BouncingText, Session, Wallpaper, and BrowserHost services. Added `AvaloniaRemoteStatusProvider` reporting active services, session progress, and available sessions. Registered both in Avalonia DI. Respects `StopEffectsOnRemoteDisconnect`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Platform/AvaloniaSkillTreeService` + `CCP.Avalonia/App.axaml.cs` | Implement SkillTree streak shield / oopsie insurance / daily streak bonus / free reroll stubs and wire `CoreApp.SkillTree` | orchestrator | `✅ done` | `main` | 2026-06-22 | Added `Start`/`Stop` to `ISkillTreeService`; implemented `UseStreakShield`, `UseOopsieInsurance`, `GetDailyStreakBonus`, `GetDailyFreeRerolls`, and `AddConditioningTime` in `AvaloniaSkillTreeService` using local settings (mirrors WPF formulas). Wired `CoreApp.SkillTree` and call `Start()` in Avalonia `App.axaml.cs` so `AchievementProgress` and `QuestService` skill-tree calls actually work. Weekly streak shields reset on start if 7+ days elapsed; time-of-day usage tracked for secret skill unlocks. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Services/BubbleCountService.cs` | Fix BubbleCount XP duration scaling stub | orchestrator | `✅ done` | `main` | 2026-06-22 | Replaced the constant-1x `ScaleXpByDuration` stub with the WPF formula using `BubbleCountWindow.LastVideoDurationSeconds` and a 60-second full-XP threshold. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Views/Deeper/EnhancementPlayerWindow.axaml.cs` | Wire AudioWaveformCache into EnhancementPlayerWindow | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `AudioWaveformCache` and `IAppLogger`, load peaks asynchronously when an audio file is opened, added `WaveformCanvas.SizeChanged` handler to redraw on resize. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Services/AvaloniaHeadStubs.cs` + `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` | Wire Chaos bark notifications to AvatarTubeWindow speech | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `BarkRequested` event to `AvaloniaBarkService`, raised it for all Chaos notify methods, and subscribed the active `AvatarTubeWindow` to speak a random phrase (priority for rank-up/gold/results). `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Platform/AvaloniaSettingsBackupProvider.cs` | Implement local file settings backup provider for Avalonia | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the no-op provider with a local-file backup: copies `settings.json` to timestamped files under `UserDataPath/settings-backups`, retains the latest 20 copies, skips identical content by hash, and gates auto-backup on a non-empty `UnifiedId` to avoid circular DI. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Chaos/ChaosHudWindow.axaml.cs` | Apply Win32 overlay ex-styles to ChaosHudWindow | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Wired `ApplyExStyles` to `ChaosWin32Helper.ApplyOverlayExStyles(this, transparent: false)` so the interactive HUD gets `WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE` on Windows without losing hit-testing. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Platform/AvaloniaAppEnvironment.cs` | Respect CustomAssetsPath in EffectiveAssetsPath | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Wired lazy `IServiceProvider` into `AvaloniaAppEnvironment` so `EffectiveAssetsPath` returns `Settings.CustomAssetsPath` when it exists, falling back to `UserDataPath/assets` (mirrors WPF `App.EffectiveAssetsPath`). Smoke test confirmed flash service now resolves `Z:\CCP Vids\images`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Chaos/AvaloniaChaosCompat.cs` + `App.axaml.cs` | Wire IAppEnvironment/IVideoService into AvaloniaChaosEnv | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `AvaloniaChaosEnv.EffectiveAssetsPath` now falls back to `IAppEnvironment.EffectiveAssetsPath` (and is seeded at startup); `VideoIsPlaying` proxies `IVideoService.IsRunning`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Chaos/*.cs` + `docs/avalonia-ui-parity-matrix.md` | Clean up resolved Chaos TODO comments and update parity matrix | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Removed outdated `AvaloniaChaosArt` TODO; refreshed parity matrix READ-FIRST banner and rows for waveform rendering, Chaos bark, local backup, HUD ex-styles, CustomAssetsPath, NewEnhancementDialog tutorial, and AvatarTube companion wiring. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Chaos/ChaosOverlayWindow.axaml.cs` | Share a single ChaosHubWindow instance from ChaosOverlayWindow | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added a static shared `ChaosHubWindow` instance reused across `OpenHubAt` calls; recreated only after close, with activate+navigate on reuse. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Chaos/AvaloniaChaosStubs.cs` | Implement ChaosTips.Attach to show real tooltips | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the no-op stub with an Avalonia `ToolTip` content builder (title, description, optional extra/flavor, accent-colored bold title). `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); WPF solution still builds. |
| `CCP.Avalonia/Windows/BubbleCountWindow.axaml.cs` | Implement BubbleCountWindow bubble-spawn game loop | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the placeholder spawn tick with a full game loop: LibVLC video-duration parsing, mod-aware `bubble.png` loading, per-window topmost bubble windows with shared animation timer, scale/rotate/fade pop animation, and per-pop `MediaPlayer` sound from `Resources/sounds/bubbles/Pop*.mp3` mixed with master/bubbles volume. Cleanup stops timers and disposes bubbles on video end / close / panic. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Core/Platform/IAudioDeviceService` + `CCP.Avalonia/Platform/AvaloniaAudioDeviceService` + `AvaloniaAudioPlayer` + Windows/WPF services | Honor preferred audio device and propagate changes to active player | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `PreferredDeviceChanged` event to `IAudioDeviceService`; all implementations raise it on `SetPreferredDevice`. `AvaloniaAudioPlayer` subscribes and re-applies the device to its active `MediaPlayer`. Fixed `WindowsAudioDeviceService`/`WpfAudioDeviceService` to return the explicit preferred device before falling back to the system default. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Avalonia/ViewModels/Tabs/LabTabViewModel` | Wire Lab tab lockdown and wallpaper commands to real services | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `IWallpaperProvider`, `ILockdownService`, and `IAppEnvironment`. Wallpaper enable/shuffle now scans `EffectiveAssetsPath/wallpapers` and calls `SetWallpaper`; disable restores original. Lockdown activate/deactivate/exit/5-click reveal now route through `ILockdownService` with event-driven UI updates (`IsLockdownActive`, `LockdownTimerText`). Added 6 localization keys via `tools/new-localization-keys.json` + merge. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Core/Platform/IDialogService` + `CCP.Avalonia/Platform/AvaloniaDialogService` + `CCP.Avalonia/ViewModels/Tabs/AssetsTabViewModel` | Add `ShowInputDialogAsync` to `IDialogService` and use it for asset preset naming | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `ShowInputDialogAsync` to `IDialogService` and implemented it in `AvaloniaDialogService` using the existing `InputDialog`. Replaced the `AssetsTabViewModel.PromptNameAsync` stub with a real `ShowInputDialogAsync` call. Added 1 localization key via `tools/new-localization-keys.json` + merge. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Avalonia/Views/Deeper/EnhancementPlayerWindow.axaml.cs` | Wire Create New enhancement button to `DeeperEditorWindow` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `BtnCreateNewEnhancement_Click` now opens a new `DeeperEditorWindow(new Enhancement(), null)` instead of being a no-op. PiP and eye-tracking buttons remain parity stubs but now log localized diagnostics (`deeper_player_diag_pip_stub`, `deeper_player_diag_eye_tracking_stub`); synced both keys to `CCP.Core/Localization/Languages/en.json`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Avalonia/Chaos/ChaosHudWindow.axaml.cs` | Wire HUD element tooltips via `ChaosTips.Attach` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the `AttachHudTips` TODO with the WPF tooltip set: clock, score, multiplier, streak, focus, ripple, act/wave, difficulty, lust, mantras, resistance, toys, accessories, conditioning, modifiers, and feed. Uses the recently ported `ChaosTips.Attach` helper. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Avalonia/ViewModels/Tabs/AssetsTabViewModel` | Wire asset preview command to `MiniPlayerWindow` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `OpenAssetPreviewAsync` now opens `MiniPlayerWindow` for local and pack files. Pack files are extracted to a temp path via `IContentPackService.GetPackFileTempPath` before preview. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Avalonia/ViewModels/Tabs/DeeperTabViewModel` | Wire Open Player and webcam refresh commands | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `OpenPlayerAsync` now opens a standalone `EnhancementPlayerWindow`. `RefreshWebcamDevicesAsync` now calls `IWebcamService.RefreshDevices()` and refreshes the webcam UI. Added `IWebcamService` to constructor injection. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings); full WPF solution builds. |
| `CCP.Core/App.cs` + `CCP.Avalonia/Services/Mod/AvaloniaModService.cs` + `CCP.Avalonia/AvatarTube` | Implement AvatarTube mod tube layout offsets and tube style swap | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `IModService` tube-layout accessors (`GetAvatarScale`, `GetAvatarOffsetX`, `GetAvatarOffsetY`, `GetAvatarDetachedOffsetX`, `GetAvatarDetachedOffsetY`) with WPF-matching clamps. Wired `AvaloniaModResourceResolver` into `AvatarTubeWindow`, implemented `SetTubeStyle`, `ApplyTubeLayoutOffsets`, and `ModOverridesAttachedTubeOnly`, and called them from startup, mod change, attach, and detach. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Progression` + `CCP.Avalonia/Services/Progression/Auth` + `CCP.Avalonia/ViewModels/Tabs/LeaderboardTabViewModel` | Port LeaderboardService to Core and wire Avalonia Leaderboard tab | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added Core `ILeaderboardService`, moved `LeaderboardEntry`/`UserLookupResult` to Core, and implemented a cross-platform `LeaderboardService` using `ISettingsService`, `IUserIdentityProvider`, `IAppLogger`, and optional `ISeasonRecapService`. Added `AvaloniaUserIdentityProvider` (backed by `IAuthProvider` + settings) and a no-op `AvaloniaSeasonRecapService`. Registered everything in Avalonia DI; `LeaderboardTabViewModel.RefreshAsync` now calls the real service and maps results to row VMs. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Catalogue` + `CCP.Avalonia/ViewModels/Tabs/CatalogueSubmissionsTabViewModel` + `DeeperSubmissionsTabViewModel` + `DeeperHubTabViewModel` | Port CatalogueService to Core and wire catalogue/Deeper submissions tabs | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Core `ICatalogueService`/`CatalogueService` and `SubmissionResult` now handle Supabase token exchange, enhancement submission, and status polling. Avalonia DI registration added. `CatalogueSubmissionsTabViewModel` polls `FetchMyCatalogueAssetsAsync` for presets/sessions and surfaces accepted transitions; `DeeperSubmissionsTabViewModel` polls `FetchMySubmissionsAsync` and records submissions via `SubmitEnhancementAsync`; `DeeperHubTabViewModel.SubmitEntryAsync` shows the `CatalogueSubmitDialog`, submits, records, and maps results to localized dialogs. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/DeeperHubTabViewModel` | Wire DeeperHub tab library scan/load and row building | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `ReloadLibraryAsync` now scans `AppSettings.DeeperLastDirectory` for `*.ccpenh.json` files using `EnhancementSerializer`/`EnhancementValidator`, builds `DeeperLibraryRowViewModel` rows with media-source labels, relative timestamps, tags, haptics/webcam filter flags, and HypnoTube catalogue eligibility (mirroring WPF `IsCatalogueEligible`). Auto-loads on tab creation and via the Reload button. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/DeeperTabViewModel` + `Views/Tabs/DeeperTabView.axaml` | Wire DeeperTab row submit and catalogue eligibility/badge | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added a shared Avalonia `HtUrlHelper` and used it in `DeeperTabViewModel`/`DeeperHubTabViewModel` to compute HypnoTube catalogue eligibility. `DeeperTabViewModel` now reads submission status from `AppSettings.DeeperSubmissions` when building rows, and the row **Submit** button routes to a new `SubmitEntryAsync` command that shows `CatalogueSubmitDialog`, calls `ICatalogueService.SubmitEnhancementAsync`, records the result, updates the row badge, and maps `SubmissionResult` to localized dialogs. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/PresetsTabViewModel` + `PresetIOTabViewModel` | Wire preset catalogue share via `ICatalogueService` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `ICatalogueService` into both VMs. `SharePresetAsync` now opens `AssetSubmitDialog`, serializes the preset to a `JToken`, calls `ICatalogueService.SubmitCatalogueAssetAsync` with the `presets` kind and `ccp-preset/v1` schema, records the result in `AppSettings.CataloguePresetSubmissions`, and maps `SubmissionResult` to localized dialogs. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/DeeperHubTabViewModel` | Wire DeeperHub delete to remove the `.ccpenh.json` file | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `DeleteEntryAsync` now confirms, deletes the underlying `.ccpenh.json` file from disk, removes the row from `AllEntries`, and refreshes the filtered/sorted list. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Views/Tabs/SettingsTabView` | Wire dashboard logo rapid-click easter egg | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `ImgLogo_PointerPressed` now tracks avatar clicks via `IAchievementService.TrackAvatarClick`, notifies `IBarkService.NotifyAvatarClicked`, counts clicks in a 60-second window, and opens `EasterEggWindow` after 100 rapid clicks. A brief scale pulse animation provides visual feedback. `ProfileSync` reader count and the New Year audio note are not yet ported, so they are skipped with logging. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/EnhancementsTabViewModel` | Wire Enhancements tab stats from `AchievementProgress` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `IAchievementService` into `EnhancementsTabViewModel`. `RefreshStats` and `RefreshDitzyStats` now read live values from `AchievementProgress` (sessions started/completed/abandoned, total XP/skill points, longest session, attention checks, bubble count, flashes/bubbles/lock cards, video/pink/spiral time, consecutive days). Time values format to hours or minutes matching WPF. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/SeasonRecapViewModel` | Wire Season Recap mod-aware asset resolution | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `ResolveAssetUri` now uses `AvaloniaModResourceResolver.ResolveUri` so feature badges and other recap assets prefer the active mod's `resources/` override and fall back to embedded Avalonia assets. Legacy `pack://` fallback is preserved if the resolver is unavailable. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/LeaderboardTabViewModel` + `Views/Tabs/LeaderboardTabView` | Wire Leaderboard "View Profile" to switch to Profile tab | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `RequestSelectTab` event to `LeaderboardTabViewModel`; `ViewSelectedProfileAsync` now raises it with key `profile` instead of showing a not-implemented dialog. `LeaderboardTabView` subscribes to the event and routes it to `MainWindowViewModel.SelectTabCommand`, mirroring the `AvailableSubjectsTabView` tab-switching pattern. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/MarqueeTabViewModel` | Wire server announcement to `AnnouncementPopup` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the placeholder message-box announcement with a real `AnnouncementPopup` (id, title, message, image URL, link URL, theme). The popup is shown centered on screen; dismiss persists the dismissed announcement ID through the existing settings flow. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/MainWindowViewModel` | Wire title-bar Stop Webcam pill to `IWebcamService` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `IWebcamService` into `MainWindowViewModel`; `StopWebcamCommand` now calls `_webcamService.StopTracking()` instead of being a no-op TODO. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Windows/WebcamCalibrationWindow` | Wire calibration help button to `HelpVideoWindow` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `BtnCalibrationHelp_Click` now opens `HelpVideoWindow` for the `WebcamCalibration` help topic instead of showing a not-ported message dialog. Falls back to the dialog if the topic is missing. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Companion` + `CCP.Avalonia/ViewModels` | Wire companion XP/level/drain events and Brain Parasite drain timer | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `XPAwarded`, `LevelUp`, and `XPDrained` events to `ICompanionService`. `AvaloniaCompanionService` now raises them, implements the Brain Parasite drain timer via `IScheduler`, and is `IDisposable`. `MainWindowViewModel` flashes the XP bar on drain and shows an in-app notification + level-up sound on companion milestones. `CompanionTabViewModel` refreshes its cards while selected. Removed unused `OnXpDrainAsync`/`OnLevelUpAsync` stubs from `CompanionHubTabViewModel`. Added 4 localization keys. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Chaos/ChaosEStimGlowOverlay` | Cross-platform cursor following for E-Stim glow halo | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `ChaosEStimGlowOverlay` now resolves `IPointerState` from DI and uses `GetCursorPosition` to position the halo under the cursor on Windows; falls back to primary-screen center on platforms without global cursor support. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Chaos/ChaosOverlayWindow` | Remove stale low-level skip-hooks TODO | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | The low-level keyboard skip hook was already wired via `IInputHook.KeyPressed` in the constructor (`OnGlobalKey` finishes the countdown or advances story). Removed the stale TODO. Mouse-click skip is not supported by the current `IInputHook` seam. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow` | Animated avatar GIF playback + pause/resume | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added a `Stream` overload to `AvaloniaAnimatedGif`. `LoadAnimatedAvatar` now creates an `AvaloniaAnimatedGif` from the mod-resolved `file://` or `avares://` URI and assigns its `Source` to `ImgAvatarAnimated`, so the avatar actually animates. `PauseAvatarGif`/`ResumeAvatarGif` call `Stop`/`Start` on the renderer. The renderer is disposed on window close and when switching avatar sets. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Windows/MiniPlayerWindow` | Animated GIF preview playback | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | `MiniPlayerWindow.LoadGif` now creates an `AvaloniaAnimatedGif` from the file path and assigns its `Source` to `ImagePreview`. Play/pause toggles the GIF animation, and the renderer is disposed on close. Falls back to a static image if decoding fails. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Quiz` + `CCP.Avalonia/Services/Quiz` + `CCP.Avalonia/ViewModels/Tabs/LabTabViewModel` | Port PopQuizService to Core/Avalonia and wire Lab test button | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Moved `PopQuizQuestion` to `CCP.Core/Models`. Added `IPopQuizService` and `AvaloniaPopQuizService` (scheduler-driven random intervals, interaction-queue aware, closes open windows on stop). Registered in DI and wired `AvaloniaSessionEffectOrchestrator` to start/stop pop quizzes during sessions when `PopQuizEnabled`. `LabTabViewModel.TestPopQuizCommand` now shows a test pop quiz. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Autonomy` + `CCP.Avalonia/Services/Autonomy` + `CCP.Avalonia/ViewModels/Tabs/BambiTakeoverTabViewModel` | Port AutonomyService to Core/Avalonia and wire Bambi Takeover UI | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Moved `AutonomyActionType`/`AutonomyTriggerSource`/`AutonomyMood`/`AutonomyActionEventArgs` to `CCP.Core`. Added `IAutonomyService` and `AvaloniaAutonomyService` with scheduler-driven idle/random/heartbeat/cooldown timers, mood detection, action selection, and dispatch to Flash/Subliminal/MindWipe/LockCard/BubbleCount/Bubbles/BouncingText/Spiral/PinkFilter/BrainDrain/Comment services. Wired `BambiTakeoverTabViewModel` Start/Stop/Test/Force commands to the service. Mobile heads are no-ops. Follow-up: added true idle detection by subscribing to `IInputHook.KeyPressed`/`MouseMoved`, wired dispatch for `Video` (`IVideoService.PlayRandomVideo`), `WebVideo` (random URL from `IModService.GetVideoLinks` with active flag/watchdog), and `WallpaperShuffle` (random image from assets/wallpapers). Added `IModService.GetVideoLinks()` and `IVideoService.PlayRandomVideo()` seams. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Models/Quiz` + `CCP.Core/Services/Quiz` + `CCP.Avalonia/Windows/Quiz*.cs` + WPF quiz files | Move Quiz models to Core, define IQuizService, and align Avalonia stubs | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Moved `QuizCategory`/`TrendDirection`/`QuizArchetypeDefinition`/`QuizCategoryDefinition`/`QuizQuestion`/`QuizAnswerRecord`/`QuizResult`/`QuizHistoryEntry`/`QuizScoreTrend`/`QuizCompletedEventArgs`/`SessionTextContent` to `CCP.Core/Models/Quiz`. Added `IQuizService` interface. Updated Avalonia `QuizService` stub to implement `IQuizService` and updated `QuizWindow`/`QuizCategoryEditorWindow`/`QuizReportWindow` to use instance methods. Updated WPF `QuizService`/`QuizWindow`/`QuizReportWindow`/`QuizCategoryEditorWindow`/`GamificationBridge`/`BarkService`/`MainWindow.Lab` to reference Core quiz models. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Quiz` + `CCP.Avalonia/Windows/QuizStubs.cs` + WPF `Services/Quiz` | Port real QuizService AI/history logic to Core and replace Avalonia stub | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Rewrote `CCP.Core/Services/Quiz/QuizService.cs` to implement `IQuizService` and injected `ISettingsService`, `IAppEnvironment`, `IAppLogger?`, `IAiService?`, `IModerationGuard?`, `IModerationLog?`. Replaced all WPF static accessors (`App.Logger`, `App.Settings`, `App.UserDataPath`, `App.ModerationGuard`, `App.ModerationLog`, `App.UnifiedUserId`, `LocalAiService`, `UpdateService.AppVersion`) with injected seams. Added `IAiService.GetRawChatCompletionAsync` default method and `AvaloniaQuizAiService` for the quiz local Ollama path. Removed the `<Compile Remove>` from `CCP.Core.csproj`, deleted `CCP.Avalonia/Windows/QuizStubs.cs`, registered `IQuizService` as transient in Avalonia DI, and updated Avalonia `QuizWindow`/`QuizCategoryEditorWindow`/`QuizReportWindow` to use the injected service. Added WPF `using QuizService = ConditioningControlPanel.Services.QuizService;` alias to keep the WPF head unambiguous. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; full WPF solution build 0 errors; Windows head `--smoke-test` clean (44 tabs, 0 first-chance exceptions, 0 findings). |
| `CCP.Avalonia/AvatarTube/*` + `CCP.Avalonia/Services/Mod/AvaloniaModService.cs` | Fix Avalonia AvatarTube theme/avatar parity (set filtering, Circe emotes, bundled mods, sizing, titles) | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Added `IModService.IsAvatarSetSupported`/`GetCustomAvatarSets` and wired set filtering/level mapping in `AvatarTubeWindow`. Fixed set-1 pose path and single-emote detection for Bambi/Sissy. Made Circe emote engine engage whenever a registry folder exists, thread-safely dispatch UI work in `EnterAsync`, include `circe_emotes_p2/p3/p4` assets, and skip missing optional `clip_timing.json` without throwing. Ported bundled `drone-mode.ccpmod`/`locked-resources.ccpmod` extraction so themed dashboard/avatar/tube art resolves. Applied WPF-style adaptive tube sizing, per-set avatar scaling, and mod-aware avatar titles. Smoke test now captures per-theme tube screenshots and reports 0 first-chance exceptions / 0 findings. |
| `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` + `AvatarTubeWindow.Reactions.cs` | Wire AvatarTube `SessionStopped` to `OnEngineStopped` | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Injected `ISessionService`, subscribed `SessionStopped` to a UI-thread dispatcher handler that calls existing `OnEngineStopped`, and unsubscribed in `OnClosed`. Brings Avalonia in line with WPF `mainWindow.EngineStopped` wiring. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Services/Subliminal/AvaloniaSubliminalService.cs` | Implement `FlashSubliminalCustom` Deeper overload | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-22 | Replaced the no-op `(string text, int? overrideDurationMs, bool suppressHaptic)` stub with a forward to the existing real overload `(string text, int? opacity, int? overrideDurationMs)`, so Deeper custom subliminal cues now render on screen instead of being logged and ignored. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 first-chance exceptions, 0 findings). |
| `CCP.Avalonia/Features/MindWipeFeatureControl.axaml.cs` | Live-apply MindWipe frequency/volume without restarting | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Frequency and volume sliders now call `IMindWipeService.UpdateSettings` when the scheduled service is running, eliminating the audio interruption caused by `Stop`/`Start`. Loop mode volume changes continue to use `StartLoop`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Video/IVideoService.cs` + `CCP.Avalonia/Services/Video/AvaloniaVideoService.cs` + `CCP.Avalonia/Features/VideoFeatureControl.axaml.cs` | Implement one-shot video test trigger with stuck-state cleanup | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Added `TriggerVideo()` and `ForceCleanup()` to `IVideoService`; `AvaloniaVideoService` implements random playback + queue reset. `VideoFeatureControl.BtnTestVideo_Click` now calls `TriggerVideo()` instead of queuing a scheduler `Start()`. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/ViewModels/Tabs/DeeperHubTabViewModel.cs` + `DeeperLibraryRowViewModel` | Wire Deeper library row Delete/Submit commands | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Row `DeleteCommand` now confirms and deletes the `.ccpenh.json` file, then removes the row from the filtered list. `SubmitCommand` delegates to the parent's existing `SubmitEntryAsync` catalogue flow. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Services/Deeper/AvaloniaLibVlcTimeSource.cs` | Return real video screen rect for Deeper gaze rules | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | `GetVideoRect` now computes screen-space bounds from the hosted `VideoView`, scaling by the containing window's screen DPI. Returning `Empty` previously caused the enhancement engine to skip gaze-target evaluation. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Features/SystemFeatureControl.axaml.cs` | Run-on-startup warning + failure revert | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | `ChkWinStart_Changed` now warns when both startup and start-minimized are enabled, reads `IStartupRegistration.IsRegistered` after toggling, reverts the checkbox on failure, and shows an error dialog. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Mantra/IMantraService.cs` + `MantraService.cs` + `IQuestService.TrackMantraCompleted` + `CCP.Avalonia/Windows/MantraWindow.axaml.cs` | Port MantraService to Core and wire Avalonia MantraWindow | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Created Core `IMantraService`/`MantraService` with injected `ISettingsService`, `IProgressionService`, and `IQuestService`; preserves anti-cheat timing, XP awards, and quest progress. Added `TrackMantraCompleted()` to `IQuestService`. Replaced `MantraWindow` stub service with DI-resolved `IMantraService` and call `StartSession(10)` on load. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Subliminal/ISubliminalService.cs` + `CCP.Core/Services/MindWipe/IMindWipeService.cs` + `CCP.Avalonia/Services/Flash/AvaloniaFlashService.cs` + `CCP.Avalonia/Windows/QuizWindow.axaml.cs` | Wire QuizWindow random effect triggers | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Added `FlashSubliminal()` to `ISubliminalService` and `AudioFileCount` to `IMindWipeService`. Implemented real one-shot `TriggerFlashOnce` in `AvaloniaFlashService` (loads a random image and spawns a `FlashOverlayWindow` even when the scheduler isn't running). Replaced `QuizWindow.TriggerRandomEffect` stub with randomized Flash/Bubbles/Subliminal triggers and an independent MindWipe roll, matching WPF behavior. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Core/Services/Auth` + `CCP.Avalonia/Services/Auth` + `CCP.Avalonia/Dialogs/LoginDialog.axaml.cs` | Port Auth/Login V2 services to Core/Avalonia and wire LoginDialog | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Moved `V2AuthService`/`V2DeviceCodeService` response models and interfaces to `CCP.Core/Services/Auth`; implemented `AvaloniaV2AuthService`/`AvaloniaV2DeviceCodeService` using `ISettingsService`, `IProgressionService`, `IUpdateService`, and `ILogger<T>`. Added `GetTotalXP`/`GetCurrentLevelXP` to `IProgressionService` and `CurrentVersion` to `IUpdateService`. Replaced `LoginDialog` `CreateLegacyService` dynamic calls with DI-injected typed services. `CCP.Desktop.slnf` and full WPF solution build 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Chaos` + `CCP.Core/Services/Chaos` | Chaos backdrop glint FX + tuning: port `ChaosBackdropService` (+405 lines), `ChaosArt` (+38), `ChaosTuning` (+14) to Core/Avalonia; wire into Chaos overlays; add menu/backdrop assets and sounds/chaos/menu_theme.mp3 | agent-17 | `✅ done` | `feat/crossplatform` | 2026-06-23 | Code integrated; `CCP.Desktop.slnf` and smoke test green. Runtime verification tracked in matrix `Chaos run economy` row. |
| Subliminal double-flash + avatar focus fixes | Port WPF 6.1.7 subliminal double-flash fix + AvatarTube focus-steal fix to Avalonia; re-verify no double flash and typing isn't cancelled | subagent | `✅ done` | `feat/crossplatform` | 2026-06-23 | Code integrated; `CCP.Desktop.slnf` and smoke test green. Runtime verification tracked in matrix `Subliminal double-flash fix` / `Avatar focus-steal fix` rows. |
| `CCP.Core/Models/AppSettings.cs` + small service deltas | AppSettings +7 and small service deltas: apply Story re-lock/FIELD_PACE and related fields to CCP.Core/Models/AppSettings.cs; port BubbleService FIELD_PACE, KeywordTriggerService, BlinkTrainerService, AchievementService, UpdateService rework deltas | agent | `✅ done` | `feat/crossplatform` | 2026-06-23 | Ported FIELD_PACE to `ChaosTuning`/`BubbleEngine`; Story re-lock via `AvaloniaChaosMode.StoryModeEnabled` + `ChaosRunConfig.FromSettings`; UpdateService version bumped to 6.1.7; AchievementService autonomy quest tracking added (lazy `IAutonomyService` lookup to avoid DI cycle). Added missing `IQuestDefinitionService.QuestDefinitionsUpdated` event/impl and removed duplicate quest definition helpers to unblock Core/desktop build. KeywordTriggerService/BlinkTrainerService quest hooks remain blocked — no Core/Avalonia services exist yet. `CCP.Core` builds 0 errors; `CCP.Core.Tests` 101/101 pass; `CCP.Desktop.slnf` integration build 0 errors; Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |
| `CCP.Avalonia/Chaos/ChaosHubWindow.*` | Chaos 'Down the Rabbit Hole' main menu: port ChaosHubWindow.xaml/.xaml.cs (+1357 lines) into existing CCP.Avalonia/Chaos/ChaosHubWindow.* with neon logo, How-to-Play tutorial overlay, menu soundtrack, pink fog, intro reveal, FX crossfade; add ~70 assets as AvaloniaResource/Content | agent-18 / @you | `✅ done` | `feat/crossplatform` | 2026-06-23 | Menu view, `MenuArtControl` (Skia flipbook + glint FX + fog + intro reveal), options/how-to overlays, and menu soundtrack wiring ported. `ChaosBackdropService` verified wired: `Show(_state.ActIndex)` is called in `AvaloniaHeadStubs.BeginRun` and `CloseActive()` in `EndRun`. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test --smoke-screenshots` clean (44 tabs, 0 first-chance exceptions, 0 findings, 106 screenshots incl. `smoke-chaos-hub-menu.png`). Parity matrix updated. |
| Auth graceful browser-launch fallback | Port Helpers/BrowserLauncher.cs to Core using IBrowserHost/system-open; update Discord/Patreon/SubscribeStar providers in Avalonia to use it; verify login flow falls back gracefully | you | `✅ done` | `feat/crossplatform` | 2026-06-23 | Ported `BrowserLauncher` to `CCP.Core/Services/Auth`; wired Discord/Patreon/SubscribeStar providers with `IBrowserHost`+`IDialogService` and Avalonia clipboard fallback; hardened `AvaloniaBrowserHost` and Linux/macOS head hosts with xdg-open/open fallbacks. Concurrent build blockers resolved; `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Windows head `--smoke-test` clean. Runtime verification tracked in matrix `Account login + premium gating` row. |
| Matrix verification sweep | Exercise every tab, feature, dialog, and window in the running Avalonia app against WPF; mark `docs/avalonia-ui-parity-matrix.md` items `[x]` only when functional + visual + performance parity holds; log new gaps in task board | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | All 44 tabs, 12 dashboard feature-card popups, 33 parameterless dialogs, Chaos hub/run overlays, and 5 mod themes exercised via `--smoke-test --smoke-screenshots` with 0 runtime exceptions and 0 smoke-test findings. Webcam tab "unavailable" badge bug fixed by initializing `Capabilities`/`WebcamViewModel` before `InitializeComponent` in `WebcamFeatureControl` (and the same pattern in `Visuals`/`System`/`LockCard` feature controls). `docs/avalonia-ui-parity-matrix.md` updated. `CCP.Desktop.slnf` build 0 errors; `CCP.Core.Tests` 101/101 pass. Linux + macOS heads build green; Android workload missing locally (CI-only). |
| `CCP.Core/Services/Quests` + `CCP.Avalonia/Assets/quests` | Quest pool refresh: port WPF QuestService/QuestDefinitionService deltas and bundle 20 quest art PNGs into CCP.Avalonia; verify Quests tab shows free+patron quests | @you | `✅ done` | `feat/crossplatform` | 2026-06-23 | Ported `IQuestDefinitionService`/`QuestDefinitionService` to Core; enhanced `QuestService` with premium gating, 3-daily generation, time accumulators, all WPF tracking methods, and quest completion XP/streak logic. Copied 40 quest art PNGs to `CCP.Avalonia/Assets/quests` (covered by existing `Assets\**` AvaloniaResource include). Wired optional `IQuestDefinitionService` into `QuestsTabViewModel` for remote pools + season title. Registered `IQuestDefinitionService` in `CCP.Avalonia/ServiceCollectionExtensions.cs`. Also fixed unrelated `ChaosBackdropService` build blockers (AppSettings namespace, ILogger cast, Point/PixelRect ambiguity) so the desktop solution builds. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test` 44 tabs, 0 exceptions, 0 findings (Quests tab visited). Parity matrix updated for Quest pool refresh and Quests tab.

| `CCP.Avalonia/Chaos/ChaosOverlayWindow.axaml.cs` | Fix launch-key accidentally skipping the first "Down the Rabbit Hole" story card / countdown | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Added `_interactiveShownAt` debounce (`InputIgnoreMs = 500`) and restricted global story advance to Space/Enter/Right Arrow only, preventing the keypress/click that launches the run from also advancing the story or skipping the countdown. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |

| `CCP.Core/Services/Awareness` + `CCP.Avalonia/Services/KeywordTriggers` + `CCP.Avalonia.Desktop.Windows/Services/Ocr` | Port Awareness keyword-trigger and screen-OCR runtime to Avalonia | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Added `IKeywordTriggerService`, `IScreenOcrService`, `IKeywordHighlightService`, and `OcrWordHit` to Core. Implemented `AvaloniaKeywordTriggerService` (keyboard buffer, text/OCR matching, cooldowns, mute, action dispatch), `AvaloniaKeywordHighlightService` (per-screen transparent highlight windows), and `AvaloniaScreenOcrService` (Windows-only, `Windows.Media.Ocr`). Registered in DI and auto-started from `App.axaml.cs`. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). Runtime trigger/highlight verification still pending. |

| `CCP.Core/Services/BlinkTrainer` + `CCP.Avalonia/Services/BlinkTrainer` + `CCP.Avalonia/ViewModels/Tabs/BlinkTrainerTabViewModel` | Port WPF BlinkTrainer asset-overlay and GazeFocus/GazeDebugCursor services to Avalonia; wire BlinkTrainer tab commands | orchestrator (agent-1 timed out) | `✅ done` | `feat/crossplatform` | 2026-06-23 | Completed after agent-1 timeout: Core interfaces (`IBlinkTrainerService`, `IGazeFocusService`, `IGazeDebugCursorService`), Avalonia implementations (`AvaloniaBlinkTrainerService`, `AvaloniaGazeFocusService`, `AvaloniaGazeDebugCursorService`), asset pool, DI wiring, tab VM commands. Fixed compile errors (ambiguous `Point`/`PixelRect`, missing `UniformGrid`/`ILogger` usings, `DispatcherTimer` namespace), moved consent check before expensive asset enumeration to avoid UI freeze, and made `ToggleSessionAsync` run `Start/Stop` off the UI thread. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test` clean (44 tabs, 0 exceptions). |

| `CCP.Avalonia/ViewModels/Tabs/AwarenessTabViewModel.cs` | Wire Awareness tab toggles to live keyword/OCR services and implement custom-trigger import | orchestrator | `✅ done` | `feat/crossplatform` | 2026-06-23 | Injected `IKeywordTriggerService`/`IScreenOcrService`; `KeywordTriggersEnabled`/`ScreenOcrEnabled`/`AwarenessModeEnabled` now start/stop the live services; `ImportFromCustomTriggersAsync` calls `IKeywordTriggerService.ImportFromCustomTriggers()` and adds new triggers to the UI/settings; fixed `ToggleKeywordTriggersAsync` premium-lock gate. Added `msg_imported_n_triggers` + `msg_import_failed` loc keys. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test` clean (44 tabs, 0 exceptions, 0 findings). |

| `CCP.Avalonia/App.axaml.cs` + `CCP.Avalonia/ViewModels/Tabs/PresetsTabViewModel.cs` + `CCP.Avalonia.Desktop.Windows/Smoke/SmokeTestRunner.cs` + loc | Wire QuestCompletePopup/PinkRushPopup/QuizReportWindow/SessionEditorWindow/Webcam windows and extend smoke test | @you | `✅ done` | `feat/crossplatform` | 2026-06-23 | Verified `QuestCompletePopup` (via `IQuestService.QuestCompleted`) and `PinkRushPopup` (via `ISkillTreeService.PinkRushStarted`) are subscribed in `App.axaml.cs`. Replaced `SessionEditDialog` with `SessionEditorWindow` in `PresetsTabViewModel.CreateSessionAsync`/`EditSessionAsync`, matching WPF save-as-new-custom flow for built-ins. `QuizReportWindow` and the three webcam windows were already wired in `LabTabViewModel`. Extended `SmokeTestRunner.ExerciseWindowsAsync` to open all seven windows, scan them, and capture screenshots. Added missing `lab_view_quiz_report` loc key to English files. `CCP.Desktop.slnf` 0 errors; `CCP.Core.Tests` 101/101 pass; Avalonia Windows head `--smoke-test --smoke-screenshots` clean (44 tabs, 0 first-chance exceptions, 0 smoke-test findings). |
| *(orchestrator final verification)* | Final build/test/smoke gate + parity matrix sign-off | orchestrator | `✅ done` | `main` | 2026-06-23 | Re-ran `dotnet build CCP.Desktop.slnf` (0 errors, 4 warnings), `dotnet build ../ConditioningControlPanel.sln` (0 errors, 0 warnings), `dotnet test tests/CCP.Core.Tests` (101/101 pass), and Avalonia Windows head `--smoke-test` (44 tabs, 34 dialogs, 12 feature-card popups, Chaos run economy, secondary windows, 5-theme reskin; 0 first-chance exceptions, 0 smoke-test findings). `docs/avalonia-ui-parity-matrix.md` is all `[x]` (legend excluded). Known Functional Gaps P0 list is cleared; only P2 item M (effect FPS tooling) remains documented. Goal complete. NOTE 2026-07-02: this sign-off is VOID by owner ruling — smoke-test-only evidence; matrix reset, see `skia-rebuild-goal.md` WS0. |
| `CCP.Core` settings/persistence + `CCP.Avalonia` heads' data paths | **WS0 lot 1: data/settings persistence + paths review** (goal `skia-rebuild-goal.md`): settings load/save/auto-save, data-dir resolution both heads (same `%APPDATA%` store? concurrent-write safety?), serializer parity, secret store (DPAPI + non-Windows), migration/corruption/backup handling, all other persisted files (progression, quests, presets, session logs, profile sync). Three checks per goal WS0; findings fixed in-lot or filed as rows | @fable | `✅ done` | `feat/crossplatform` | 2026-07-02 | LOT PASSED 2026-07-02. Fixes: `e9501ce8` (secret-store seams + migrations + exit flush), `a2d1b9a8` (cross-head format drift), `b694b543` (corrupt-file quarantine + 11 file-I/O tests). Evidence: 5-agent adversarial verify w/ file:line proofs; slnf + WPF head builds 0 errors; Core tests 119/119 (baseline 108, no losses); `--smoke-test` 45 tabs / 0 findings >sev0, live settings saves to `%LOCALAPPDATA%\ConditioningControlPanel\settings.json` observed, 17/19 first-chance exceptions = deliberate OAuth-cancel harness noise, 2 = spiral.gif asset gap (filed, lot 3). NOT exercised: manual side-by-side UI settings editing vs WPF (unit+smoke cover load/save/migrations; residual risk low). Deferred findings: see "Discovered by WS0 lot 1" table. Original claim notes follow. CONTRACT (mapped, pending adversarial verify): both heads store under `%LOCALAPPDATA%\ConditioningControlPanel` (Avalonia `IAppEnvironment`/`AvaloniaAppEnvironment` collapses Roaming→Local w/ one-time migration + `.roaming-migrated` sentinel; Linux XDG_DATA_HOME); settings.json via Newtonsoft `ObjectCreationHandling.Replace`, atomic .tmp+Move write, 500ms debounce, `SaveImmediate` on exit; ~28 other artifacts path-parity per inventory sweep. CANDIDATE FINDINGS under verify: (1) DPAPI entropy mismatch WpfSecretStore `CCP_WindowsOnly_SecretStore_v1` vs DesktopSecretStore `CCP_Desktop_SecretStore_v1` (two explorer agents CONTRADICT on this); (2) Core SettingsService may skip `MigrateLoudnessThreshold` (+check `RunFlashClickableDecouplingMigration`); (3) SettingsService duplicated WPF-vs-Core, drift risk; (4) Avalonia may only flush settings via `ExitApplicationAsync`, other exit paths lose last 500ms (and achievements/quests 30s timers?); (5) last-write-wins with both heads open (no file locks); (6) no file-I/O tests for Load/Save/atomic recovery. Baseline: build 0 err, Core tests 108/108. |

| `CCP.Core` session engine/orchestration + both heads' START/stop paths | **WS0 lot 2: session engine + start/stop review** (goal `skia-rebuild-goal.md`): START button contract (quick-start from settings vs preset session), session lifecycle (Idle→Running→stop→Idle), which effect services start/stop and in what order, intensity ramp + scheduler semantics, panic/emergency stop, session timer/duration/completion (SessionComplete window, XP payout, session logs), pause/resume if any, WPF `Services/Session/SessionEngine.cs` + `MainWindow.StartStop.cs` vs Core `ISessionService`/orchestrator + `MainWindowViewModel.StartSessionAsync`. Three checks per goal WS0; findings fixed in-lot or filed as rows | @fable | `✅ done` | `feat/crossplatform` | 2026-07-03 | LOT PASSED 2026-07-03. Fixes: `9cd64bce` (cluster A: Core SessionService owns lifecycle — log single-owner, SessionSettingsScope apply/restore, session Lerp ramps, pause/resume events, achievement snapshots, TotalSessions), `70857d09` (cluster B: TickEffects delayed starts + bubble bursts, autonomy gates, mindwipe loop, unduck, stop reorder), `44fc1421` (cluster C: engine-only plain START, dead panic path deleted, double-panic sync teardown, lockdown gates, launch behaviors, jump-right-in, conditioning-tracker P0 guards, VM singleton), `61f339fc` (cluster D: SchedulerService + IntensityRampService + 31 tests). Evidence: build 0 err both heads; Core tests 119→159 (new floor); smoke 112.7s = baseline noise + 1 known stale harness assertion (`StartSession State=Idle, IsEngineRunning=True` proves the engine-only contract; SmokeTestRunner.cs is parallel WIP, follow-up chip task_627c90f2). NOT exercised: live side-by-side pause/panic/scheduler-window/ramp visuals vs WPF (unit+smoke+adversarial file:line review cover the logic; residual risk moderate — exercise during lot 9 sweep or first manual run). Deferred rows filed: brain-drain audio half (P2), subliminal whisper linked-audio (P1), Discord RPC (P2), phantom ISessionPlatformBridge (P3); corner GIF start/end TODO in orchestrator (no Avalonia surface yet); 5 new en.json keys need the other 8 languages eventually. REVIEW RECORD (19-agent adversarial workflow: 7 confirmed, 7 partial, 1 refuted of 15 candidates + 17 rubric + 6 critic findings; full verdicts w/ file:line + fixes in session scratchpad `lot2/verdicts.json`, also tasks/wecfon7t8.output). IN-LOT FIX CLUSTERS (dependency order): **A (Core SessionService owns WPF SessionEngine contract)**: session-log ownership into Core w/ SessionStoppedEventArgs{FinalElapsed,Completed} + single EndSession w/ real elapsed/XP (F15,R1-02,R2-log-ownership,R3-log-zero); delete duplicate BeginSession in orchestrator+remote executor (R1-06); IAchievementService hooks + start-snapshots (F9); SessionPaused/Resumed events (F6,R1-01); SessionSettingsScope apply/restore ~40 fields + phrase pools (F7); ISessionEffectOrchestrator.TickEffects seam from OnTick (F14); per-session Lerp ramps + RandomizeStartTimes port (F3a); TotalSessions++ (C3); divergence-warn-once (R3); delete dead _random/_pauseStartTime/CTS (R3); UI-thread timer hardening + BenchmarkContext ConfigureAwait (R1-05). **B (orchestrator)**: autonomy arm/stop gates (F2); MindWipeLoop (C2); TickEffects impl = delayed pink/spiral/bubbles/corner-GIF + intermittent bursts (F14); BubbleCountResultWindow.ForceCloseAll (F10); pause/resume Stop/StartEffects. **C (VM+MainWindow+App)**: delete dead VM panic path (F5,R1-03); double-panic sync teardown + exit flush (R1-04); lockdown stop/pause gates + XP-loss dialog (F11); plain START = engine-only run, no session lifecycle/XP/completion (F12 — reverts gap-#1 fix approach); AutoStartEngine/StartMinimized/ForceVideoOnLaunch consumers (C1); JumpRightIn split start (C4); SkillTree.Start per start (C5); conditioning-tracker guards (R3-P0); delete duplicate 1Hz status timer + redundant Posts (R3); VM→AddSingleton + unsubscribe-not-Dispose input hook (R2). **D (new)**: Core SchedulerService (window fn + 30s timer + auto-started/manual-stop flags + startup 60s grace + settings-change recheck) (F4); manual 2s intensity ramp w/ RampLink* + EndSessionOnRampComplete, suppressed during preset sessions (F3b). DEFERRED (rows filed): F1 brain-drain audio half, F13 subliminal whisper linked-audio (attention/webcam/haptics = confirmed false alarms, do NOT add), C6 Discord RPC, R2 phantom ISessionPlatformBridge. REFUTED: F8 (phases drive no effect params in WPF either), R3 handler-accumulation. Baseline: build 0 err, Core tests 119/119 (floor). |

| `CCP.Avalonia` compositor/overlays + click-through input | **WS0 lot 3: overlays/compositor + click-through review** (goal `skia-rebuild-goal.md`): CompositorEngine (per-monitor topmost click-through window, z-ordered IAvaloniaLayer, 60Hz tick) vs per-effect windows; overlay layers pink/spiral/brain-drain/subliminal/flash/bouncing-text; WS_EX_TRANSPARENT/LAYERED/NOACTIVATE styles + hit-test paths (overlay-clickthrough skill + callsite catalog); AvaloniaMouseHook click-swallow gap (WPF swallows bubble/flash pop clicks incl. hold-to-defuse exception — decide fix or documented acceptance); multi-monitor + hotplug + mixed-DPI; spiral.gif P1 asset gap (needs csproj chokepoint); capture-exclusion guardrails (subliminals stay IN capture WDA_NONE, keyword-highlight/brain-drain excluded). Three checks per goal WS0 | @fable | `✅ done` | `feat/crossplatform` | 2026-07-03 | LOT PASSED 2026-07-03. Fixes: `0c43277f` (spiral bundling + embedded fallback), `d45eaf9f` (wave 1: P0 capture-affinity dual-surface split w/ WDA_EXCLUDEFROMCAPTURE second window set; real brain-drain blur incl. level gate + ad-hoc + dead timer; P0 SkiaFrameSet ref-count lifetime; spiral real GIF frames + 0.1 opacity; flash LRU decode cache), `22798e8e` (wave 2: engine idle watchdog no destructive auto-stop; hotplug reconcile both surfaces; single-display honored; physical-px canonical coordinates fixing >100% DPI hit-tests/windows; ref-counted hook install; FlashClickable live + FlashGazePopEnabled gating [closes lot-1 row]; hold-to-defuse pin/stray-detonate; topmost watchdog + DesktopMode; keyword-highlight live capture toggle; subliminal opacity applied; live opacity pickup for lot-2 ramps; ad-hoc overlay ownership protocol [no blink on timed expiry]; --verify-spiral rewritten, PASSES). Evidence: build 0 err both heads; Core tests 159→164 (new floor); smoke 102.7s 0 findings / 0 first-chance exceptions (NOTE: compiled with the parallel session's uncommitted SmokeTestRunner.cs WIP which updated the stale START assertion + OAuth noise); --verify-spiral exit 0 (1 window = single-display honored). Benchmark: idle FPS parity (123.7 vs 123.9 ref); active FPS now display-synced (~124 vs ref 185 uncapped overdraw — dirty-gated present, fewer wasted frames, not a regression); WS +~90-130MB explained (spiral frame cache ~85MB, WPF-parity persistent + brain-drain capture buffers); startup 2781-3067ms vs ref 1977ms — ROW FILED for WS3 perf sweep. NOT exercised manually: OBS/PrtScr capture-exclusion check (steps: brain drain on → capture clean, subliminal visible in capture), multi-monitor hotplug, mixed-DPI live clicks. Click-swallow gap = documented WS2 acceptance (in-code comments at AvaloniaMouseHook + flash handler). Deferred rows below. REVIEW RECORD (18-agent workflow: 3 confirmed/11 partial/0 refuted + 36 rubric + 5 critic; full verdicts in session scratchpad `lot3/verdicts.json`, also tasks/whucd46w0.output). Spiral bundling already fixed in-lot (`0c43277f`). IN-LOT FIX WAVES: **Wave 1 — A (P0 capture affinity + brain-drain parity)**: dual-surface compositor (IAvaloniaLayer.ExcludeFromCapture; second per-monitor CompositorWindow w/ WDA_EXCLUDEFROMCAPTURE for BrainDrainLayer; main surface stays WDA_NONE for subliminals) (L3-1,R1/R2-braindrain-capture); brain-drain blur-vs-tint parity + level-70 gate + ad-hoc braindrain unregistered no-op + dead pulse timer (R1-blur-parity,L3-C3,R1/R2-adhoc,R3-dead-timer). **B (P0/P1 Skia lifetime + decode)**: SKImage dispose-vs-render race + use-after-free spiral/flash + leaks + triple decode + GIF eager frames (R2-dispose-race,R1-uaf x2,R1/R3-leaks); spiral GIF real frame animation not invented rotation (R1/R3-spiral-gif); spiral opacity 10x (L3-C1); GIF decode caching (L3-12). **Wave 2 — C (engine lifecycle/coords/hooks)**: engine autostop strands layers + no restart (R1/R2/R3-autostop, L3-10); hotplug dead Screens.Changed subscription + window lifecycle + stagger coverage/timers (L3-7,L3-14,R2-hotplug,R1-stagger); single-display setting ignored (L3-8 CONFIRMED); hook hit-test physical-vs-DIP mismatch + per-window coordinate space (R1/R2-coords); shared hook uninstall collision; flash hook-thread heavy work/decode off hook thread (R2/R3); permanent mousemove hook while idle (R3). **D (parity odds)**: keyword-highlight styles (L3-2); FlashClickable + gaze-pop gating (L3-3); hold-to-defuse + ad-hoc hold stomp (L3-4,L3-C4); topmost re-assert vs video (L3-5); Free Desktop mode (L3-6); live opacity pickup for lot-2 ramps + subliminal opacity ignored (L3-13,R1-subl-opacity); ad-hoc pink/spiral dead while service stopped (L3-C2); --verify-spiral harness rewrite to compositor assertions, NOT SmokeTestRunner.cs (L3-9); proportionate optimality (static-frame overdraw, per-tick LINQ/SKPaint, redundant hittests, mod FilterColor P3s — fix if cheap, else rows). DEFERRED: L3-11 per-frame alloc audit (WS1 Phase D), R2-clickswallow blast radius (WS2 + document acceptance in lot close), R3-benchmark-active-fps (re-measure at close; row if still regressed). Baseline: build 0 err, tests 159/159, smoke noise incl. 2x spiral.gif avares (should vanish: `0c43277f`). SmokeTestRunner.cs = parallel WIP, do not touch (chip task_627c90f2). |

| `CCP.Avalonia` video/audio services (legacy path) | **WS0 lot 4: video/audio review** (goal `skia-rebuild-goal.md`): the LEGACY video path is the contract holder until WS1 (UCE VideoLayer/MandatoryVideoLayer are WS1 scope — verify only that lot-4 code does not break them); WPF `Services/Video/VideoService.cs` (155KB) + Dual/Mirror/Wallpaper variants vs `AvaloniaVideoService`/`AvaloniaMultiMonitorVideoService`/`VideoOverlayWindow`; mandatory videos + in-video attention checks; audio ducking (`AudioService`, WASAPI) vs Avalonia ducker; volume/device routing, mute, MiniPlayer; video window ex-styles/topmost (32ms re-assert) + capture affinity; BubbleCount video minigame playback. Three checks per goal WS0 | @fable | `✅ done` | `feat/crossplatform` | 2026-07-03 | LOT PASSED 2026-07-03. Fixes: `1bef3b5b` (wave 1: legacy path default until UCE Phase E via CCP_UCE_VIDEO opt-in, compositor-end lifecycle, time×1000, live-volume device re-bind), `32ad38f8` (wave 2: WASAPI ducker rewritten to WPF semantics — relative refcounted duck, rescan, ducking_recovery.json crash persistence [P0], retry/watchdog, WebView2 exempt, pactl injection fix), `d6222069` (wave 3: video parity — shuffled anti-repeat queue + pack videos + DisabledAssetPaths, duck/flash/bubbles around videos, companion fail penalty, ESC/panic + strict keys on BOTH paths, DualMonitor/FillAll gates, ShowActivated=withAudio, Topmost, VideoHardwareDecoding, position-based watch credit, VideoAboutToStart+1.3s, FloatingText physical-px + TOOLWINDOW|NOACTIVATE + HWND_TOPMOST re-assert, pump/dispose/timer/seek-race/UNC rubric fixes), `a8d3df33` (wave 4: gaze attention-check dormant per WPF pre-ship contract [tab+auto-start removed, code kept], Debug-gated spike windows + LibVLC leak fix, MiniPlayer deferred dispose + GIF-Space no-op, device default markers + enumerator dispose, AvaloniaAudioPlayer device validation, wallpaper original capture/restore base+Linux). Evidence: 5-agent adversarial review (V1 22 / V2 9 / A1 18 / T1 7 / R1 22 findings), critical P0/P1s re-verified first-hand w/ file:line before fixing; slnf build 0 err; WPF sln 0 err (seam compat verified); Core tests 164/164 (floor held); smoke 2/3 runs clean (44 tabs = 45 minus removed Attention tab, 17 first-chance = documented OAuth harness noise, 1 blocker = known stale harness assertion owned by parallel WIP); 1/3 run hit an intermittent cross-thread SolidColorBrush crash NOT attributable to lot-4 code by inspection — row filed. PARTIALLY REFUTED during verify: A1-14 (synthetic System-default entry already existed in SettingsTabViewModel; only IsDefault marker was missing), V2-2 (Windows head's WpfWallpaperProvider already captured/restored; gap was base+Linux providers only). Wave-2 note: WPF remote duck hardcodes 80, port passes DuckingLevel (behavior-identical at default). NOT exercised: live side-by-side video/audio playback vs WPF (ducking audibility, device routing, multi-monitor fills, ESC/panic during real playback), OBS capture check — listed for lot-9/manual sweep; wave-3 agent report lost to host crash but all 12 spec items verified present in diff by hand. Deferred findings: see "Discovered by WS0 lot 4" table. Claim + review record follow. Claimed after lot 3 pass. Baseline: build 0 err both heads, Core tests 164/164 (floor), smoke 0 findings/0 exceptions, --verify-spiral PASS. Guardrail: never delete the legacy video path before WS1 Phase E. SmokeTestRunner.cs = parallel WIP, do not touch. REVIEW RECORD (5-agent adversarial: V1 core video 22 findings, V2 variants 9, A1 audio 18, T1 attention 7, R1 rubric 22; key P0/P1s re-verified first-hand w/ file:line). CONFIRMED CRITICAL: (ROUTE) compositor video path is the live default — AvaloniaVideoService ctor creates Mandatory/VideoLayer whenever DI CompositorEngine exists (ctor :129-141), PlayFile/PlayUrlCore gate legacy multi-monitor + per-window behind layer==null (:545,557,586,598) so the lot's designated contract holder is dead code AND compositor VideoEnded never clears _isVideoActive/max timer → scheduler permanently blocked after first natural end (R1-3); UCE plan says default flips only at Phase E. (TIME) PrimaryPlaybackTimeMsChanged emits Time*1000 = µs (:1296). (DUCK) WindowsSystemAudioDucker = absolute 0.20 ignoring DuckingLevel, bool not refcount, no mid-duck rescan, NO crash-recovery persistence (P0 — system-wide stuck-duck on app death), no deferred-restore retry, no watchdog, no WebView2 exemption, no MasterVolume==0 early-out, seam lacks strength/ForceUnduck (A1-1..8,12,18). (GAZE) Avalonia revives + auto-starts (App.axaml.cs:330-338) + surfaces UI for the gaze attention-check WPF scrapped pre-ship (AppSettings.cs:1588 comment) (T1-1/2). (DIP) FloatingText builds DIP bounds from physical WorkingArea/scaling then assigns PixelPoint physical → wrong region on scaled monitors (:1396-1400,1506) (R1-2). (PUMP) WaitWithMessagePump reentrant DoEvents loop on UI thread on EVERY video op incl. idle Stop() (MultiMonitor :150-170,240-258) (R1-4). Video duck/flash-stop/bubble-pause/haptics/companion-fail/freeze pre-roll/VideoAboutToStart all missing on video path (V1-7..12,15, A1-9/10, T1-3/4); no anti-repeat queue, no pack videos, no DisabledAssetPaths filter (V1-4/5); DualMonitorEnabled+FillAllMonitorsWithVideo ignored, focus-steal, no ESC/panic on live path (V2-3/4/6, V1-2/3/18); wallpaper restore is a no-op = data-integrity (V2-2); ScreenMirror unported (V2-1); AudioSync runtime unported (A1-16); spike harness windows reachable in Release (R1-10/11); sync UI-thread LibVLC dispose deadlock class (R1-6/14/15); VideoHardwareDecoding ignored (V1-16); watch-credit divergence (V1-13); session-lock/power cleanup missing (V1-14); chaos segment seek race = dead feature (R1-13). PLANNED FIX WAVES: **1 (routing+units, direct):** legacy default via UseCompositorVideo env flag (CCP_UCE_VIDEO=1 opt-in for WS1 dev), compositor-end lifecycle cleanup, Time*1000 fix, UpdateVolume volume-only. **2 (ducker, WPF semantics):** seam Duck(strength)/ForceUnduck, relative duck, refcount, 2.5s rescan, ducking_recovery.json persistence+restore-on-start, 3min retry, 5min watchdog, WebView2 exempt, MasterVolume gate; wire video/quiz duck triggers. **3 (video parity):** shuffled anti-repeat queue, DisabledAssetPaths, duck/flash/bubbles around videos, companion fail penalty, ESC/panic + strict keys on live path, DualMonitor/FillAll gates, ShowActivated parity, Topmost parity, VideoHardwareDecoding, FloatingText physical-px + TOOLWINDOW|NOACTIVATE + topmost re-assert, watch credit, VideoAboutToStart+1.3s. **4 (dormancy+rubric):** gaze system dormant per WPF contract, attention-ring ex-styles, kill message pump, deferred dispose (overlay window/MiniPlayer/Dispose-Wait), Task.Run→one-shot timers, segment-seek via LengthChanged, DEBUG-gate spike windows + leak fix, torn-read lock, UNC rejection, pactl quoting, per-tick ToArray cache, device IsDefault markers + player device validation. DEFER (rows): ScreenMirror port, AudioSync pipeline, wallpaper shuffle seam, whisper gate (blocked on whisper port), Bambi-freeze pre-roll (blocked on subliminal seam), session-lock/power events, pack-video decryption path, no-videos/codec dialogs, web-video scheduler defer, 16ms blit vs render-tick (WS3 perf). |

| `CCP.Core`/`CCP.Avalonia*` speech/mic + webcam/gaze/calibration | **WS0 lot 5: speech/mic + gaze/calibration review** (goal `skia-rebuild-goal.md`): WPF `Services/Speech/*` (SpeechService, SherpaWakeService, SileroVadGate) + MicConsentDialog vs `WindowsSpeechService`/Avalonia Sherpa/Vad ports + `NullSpeechService`; WPF `Services/Webcam/WebcamTrackingService.cs` + calibration/QuickRecal/GazeTracker/LoadingSplash windows + `Services/Tracking/*` (GazeFocus, GazeDebugCursor, GazeDriftCorrection, GazeContentScreenPolicy) + BlinkTrainer vs Avalonia counterparts; consent dialogs + PRIVACY GUARDRAIL (frames never disk/network, only calibration coefficients persisted, ConsentVersion bump on any broadening); TRIAGE the BLOCKED calibration-overhaul port (`7601da52` BLOCKED STOP, `837aaa1d` core landed after — goal doc's 'uncommitted WIP' note is stale, resolve status vs `docs/avalonia-calibration-overhaul-port.md`): decide resume/re-scope/revert and record. Three checks per goal WS0 | @fable | `✅ done` | `feat/crossplatform` | 2026-07-03 | LOT PASSED 2026-07-03. Fixes `2da98c0e` (wave A: P0 mic-consent gate + MicConsentDialog port + #441b name-match + honest voice-ack + pause/resume/help dispatches + S-7/8/9/10), `53c4b0ab` (wave B: GrantConsent→multi-gate dialog, Core ConsentVersion single source, version-aware gates w/ re-prompt, honest 16-point panel + REAL quick-recal + REAL tracker-test gaze, DeeperTab honesty, device combo, real startup-progress + failure-state surfacing, IWebcamService seam completed), `09d13f55` (wave C: cold-start readiness wait, calibration prerequisite, debug-cursor DIP→physical-px, GazeDriftCorrection Dispose leaks), +MVVMTK follow-up. Gates: slnf + WPF sln 0 err, Core tests 164/164 (floor held), smoke baseline (44 tabs; 5 findings = known stale harness StartSession blocker owned by parallel WIP + 4 ChaosRun infos; 18 first-chance vs lot-4 17, all benign). Privacy guardrail CLEAN: full grep sweep w/ per-hit verification — no frame/audio/mic data reaches disk or network on either head; calibration JSON = coefficients only; all 34 tracker log sites aggregate-only. Calibration triage resolved the goal's stale BLOCKED note (BLOCKED disproven, core landed `837aaa1d`; real gap = 16-point window pipeline, filed as its own row below). Wave-C agent report lost to host cleanup but all 4 implemented items + 2 documented skips verified in-diff by hand. Wave-B loc keys were initially written to a dead corrupt mirror json (`CCP.Core/Localization/Languages/en.json`, non-built, invalid JSON at line 873) — corrected: migrated 22 keys to the built source (`ConditioningControlPanel/Localization/Languages/en.json`), restored the mirror. PARTIALLY DEFERRED: flash gaze-linger + video gaze-target dwell (wave C skips — need flash-path BoostLifetime surface + target-surface work; filed). NOT exercised: live webcam/gaze/calibration vs WPF with real hardware (requires camera + calibrated run to resolve W-14 OnGazeMove coordinate-space ambiguity), real mic recognition/wake accuracy. Deferred findings: see "Discovered by WS0 lot 5" table. Claimed after lot 4 pass. Baseline: build 0 err both heads, Core tests 164/164 (floor), smoke 2/3 clean w/ documented noise (44 tabs; intermittent brush-crash row filed). Guardrails: webcam frames never hit disk/network; Calibrate()/TestTracker() no-op status per matrix row F. SmokeTestRunner.cs = parallel WIP, do not touch. REVIEW RECORD (4-agent adversarial: S speech/mic 10 findings, W webcam/gaze 14, C calibration-status 6, P privacy/rubric 11). HEADLINES: privacy guardrail CLEAN — no frame/audio data reaches disk/network on either head (full grep sweep w/ per-hit verification; calibration JSON = coefficients only; all 34 tracker log sites aggregate-only); consent PLUMBING defective — S-1/P-1 wake calibration opens mic w/o MicConsentGiven (P0), P-2 BlinkTrainer one-click GrantConsent bypasses the multi-gate dialog, S-2/P-3 MicConsentDialog unported (informed consent → bare checkbox), W-8/P-4 ConsentVersion triplicated ('1.0' ×3, dialog TODO), W-9/P-5 version ignored in UI gates (stale-version users get dead toggle, no re-prompt). CALIBRATION TRIAGE (goal doc BLOCKED note = STALE): 7601da52 BLOCKED STOP was disproven and 837aaa1d landed the core (13 algorithm hunks + data model, doc avalonia-calibration-overhaul-port.md verified accurate claim-by-claim); REAL remaining gap = the 3 calibration windows are FAKE-SUCCESS shells (C-1 16-point 'Done' persists nothing, C-2 quick-recal 'nudged' samples nothing, C-3 tracker-test dot is Random noise, C-4 DeeperTab sets 'calibrated' status doing nothing); seam is complete (OnRawIris/ApplyCalibration/SetRuntimeOffset/ApplyGazeTrim all live on the tracker); full 16-point pipeline port sized ~1300-1500 LoC window code — RE-SCOPED as its own row. Other verified: W-3/P-7 cold-start race (fire-and-forget StartTracking + immediate IsRunning check → first click fails), W-4 gaze-focus ignores calibration prereq, W-5 video gaze-targets dead code, W-6 flash gaze-linger no-op (settings+UI exist), W-7 blink-trainer per-screen decoders reintroduce fixed WPF desync, W-10 device combo discards enumeration, W-11/P-11 no failure-state surfacing + optimistic IsTracking + fake 500ms splash, W-13/P-9 debug cursor DIP→PixelPoint mismatch, W-14 OnGazeMove coordinate space assumed differently by 2 consumers (needs live run), S-4 device name-match (#441b) dropped both engines, S-5 7 grammar intents false-ack'd, S-7/8/9/10 minor engine drift, P-8 Dispose leaks, P-10 ~18k/s Gen0 landmark allocs. PARITY OK: camera-open consent gate INTACT (fail-closed IsConsentCurrent in Start), RevokeConsent parity, calibration persistence format/path identical (WPF calibrations migrate), recognition/wake/VAD pipelines 1:1, grammar verbatim, DI overrides correct. FIX WAVES DISPATCHED (3 parallel agents, disjoint files): A consent+speech (MicConsentDialog port, calibrate gate, name-match, honest ack, S-7/8/9/10), B consent integrity + honest calibration UX (GrantConsent→dialog, Core ConsentVersion single source, version-aware gates, honest 16-point panel, REAL quick-recal + REAL tracker-test gaze, DeeperTab honesty, device combo, state/progress wiring), C gaze consumers (start race, calibration prereq, flash linger, video targets, debug-cursor coords, Dispose leaks). DEFER (rows at close): 16-point pipeline port (P1), W-1 webcam barks (companion lot), W-7 single-decoder mirror, P-10 allocs (WS3), W-14 live coordinate verification, S-3 mic pill if unwired. |

| WPF `Services/Chaos/*` + `Chaos/*` (~50 files) vs `CCP.Avalonia/Chaos/*` (~52) + `CCP.Core/Services/Chaos/*` (~13) | **WS0 lot 6: chaos/game-mode review** (goal `skia-rebuild-goal.md`): functional PARITY + rubric review of the whole Chaos run — run engine/lifecycle (`ChaosModeService`: start/stop, waves, duration, draft, crash sentinel), economy (sparks/boons/upgrades/lifetime-boons/unlock-cards/meta-store/ranks/XP), narrative (narrator/story-cards/reveal/lessons/director/tips), visuals (field FX, DVD, GIF cascade, Skia FX, flash, pop-text, vibe-trail, wave-timer, banner, backdrop, cursor-glow, E-Stim, bubbles), HUD/boon-bar/hub/intro, SFX/art/image-pool, haptics (E-Stim), input (overlay ex-styles/click-through, countdown+story-card skip via IInputHook). Three checks per goal WS0 (contract + adversarial rubric + optimality). SCOPE NOTE: the ~23 Chaos overlays being separate windows (not compositor layers) is the WS2 workstream — verify NON-BREAKAGE only, do NOT migrate to the compositor in this lot. | @fable | `✅ done` | `feat/crossplatform` | 2026-07-03 | LOT PASSED 2026-07-03. Fixed in-lot across 3 commits (`b9fb74be` wave A engine data/wiring, `3f4cceb8` wave B overlay HiDPI+animations, `b828fea5` wave C perf+timer-leaks) the self-contained correctness/safety/lifecycle/visual bugs; the deep run-engine economy/spawn/scoring layer is RE-SCOPED as a dedicated workstream (see 'Discovered by WS0 lot 6'). Gates: slnf + WPF sln 0 err, Core tests 164/164 (floor held), smoke ChaosRun exercised clean (score=3083, sparks 15->104, xp advanced, run start+end, results overlay visible; 44 tabs, 17 first-chance = baseline, 5 findings = known stale-harness blocker + 4 ChaosRun infos). Wave-C agent report lost to host cleanup; all its changes verified in-diff by hand (GIF budget, off-thread flash decode, art cache, timer teardowns, tunnel ex-styles all present). Loose ends filed as rows: X2-22 ForceTopmost API added but call-sites unwired, X3-10 boon-bar tooltips (hit-test vs click-through), X3-3 chain_reaction uses a time-window proxy (no per-pop screen coords in Avalonia). Claimed after lot 5 pass. Baseline: build 0 err both heads, Core tests 164/164 (floor), smoke baseline (44 tabs, ChaosRun exercised clean per lots 4-5). REVIEW RECORD (4-agent adversarial, ~71 findings: X1 run-engine/economy 18, X2 visuals/overlays 28, X3 narrative/HUD/SFX 16, X4 input/ex-styles/rubric 9). STRUCTURAL VERDICT: the Avalonia Chaos RUN ENGINE is `AvaloniaChaosService` in `CCP.Avalonia/Services/AvaloniaHeadStubs.cs` — a SIMPLIFIED STAND-IN, not a faithful port of WPF's ~3000-line `Services/Chaos/ChaosModeService.cs`. It spawns one bubble/900ms from a 3-id pool at flat 45% (X1-8: no intensity escalation, density cap, behavioral/golden/prism/brittle/heavy variants), diverges on spark reward (X1-1 P0, uncapped + missing SparkGainMult/DripFeed/first-fall), feeds the WRONG quantity to progression XP (X1-2 P0, awards sparks not capped-score), hardcodes skillMult=1.0 (X1-3), never applies owned upgrades to a run (X1-6, no ApplyTo — all 6 upgrades inert), and the lifetime-boon upgrade economy is dead (X1-5, UpgradeCosts never seeded). Rank enum order+thresholds diverge (X1-4: Devoted/Entranced swapped, extra Lost, Claimed@100 unreachable) cascading to many gates. Per-pop scoring, EnabledVariants default, boon-draft duo/trio/sin/unique gating all simplified (X1-12/9/10). DifficultyMult constants wrong (X1-7). In-run gameplay is SILENT (X3-2: ~40 ChaosSfx cues never fired) and the drafted boon-bar ribbon never shows (X3-1). 3 lessons never tick → permanent item lock (X3-3). VISUALS: ~10 overlays mix physical-px StageBounds/Position with DIP sizes → oversized/mispositioned on HiDPI (X2-6/7/8/16/24/25); dropped entrance pops/animations (X2-3 ripple no-expand, X2-4 announcer, X2-5 unlock-card, X2-11 vibe pulse ~20× slow, X2-14/17/18 keyframes); PERF regressions reintroduce documented freezes (X2-1 GIF-budget removed — AppHangB1, X2-2 flash full-res sync decode on UI thread, X2-9/10 per-logo DVD window-move + judder, X3-9 art no-cache OOM risk, X2-28/X4-9 per-spawn decode). LIFECYCLE: run teardown closes only ~5 of ~20 keep-alive overlays (X4-5 P1 — stranded topmost windows after stop/panic), + timer leaks (X4-6 conversation bounce, X4-7 toy-button pulse, X2-13 pop-text handler accumulation), crash sentinel never armed by the engine (X1-13). PARITY OK (verified faithful): narrative director (15 cues/2 convos/gating), ChaosBoonColors, RevealService (18 predicates), ChaosLessons defs, meta persistence (chaos_meta.json atomic), upgrade+lifetime-boon+draftable catalogues (IDs/costs), HUD binding surface, hub debug strip, story-card display, announcer, SFX RESOLUTION (mod-overridable), art ResolveBanner/Recap, intro window, SkiaFx/backdrop draw math, GIF wired-to-animate, ex-styles CORRECT on passive+interactive overlays, capture-affinity CLEAN (0 WDA_EXCLUDEFROMCAPTURE), threading clean (DispatcherTimer, marshaled), compositor NON-BREAKAGE confirmed. PROPOSED SPLIT (validating w/ advisor): FIX IN-LOT = self-contained correctness/safety/lifecycle/visual bugs independent of the simplified spawn model (teardown-close-all + timer leaks + pop-text leak + crash-sentinel arm + dead no-op ChaosSfx trap; rank enum+names + remove passive focus-regen + remove 7 non-WPF boons; DPI/physical-px unit fixes across overlays + RaiseTopmost managed-flip; boon-bar ribbon create/populate + in-run SFX cue wiring + 3 lessons tick; GIF-budget + flash off-thread decode + art cache + decode caching; dropped entrance animations). RE-SCOPE as a dedicated 'Chaos run-engine faithful port' workstream (interdependent, only coherent ported together) = X1-1/2/3/6/8/9/10/12/16/18 spark+XP formulas, upgrade ApplyTo, full variant/behavioral/golden/prism/brittle/heavy spawn system w/ escalation+density, per-pop scoring depth, draft gating, payload maps; + X3-8 bubble hints; + dormant narrative gaps X3-5/6/7. SmokeTestRunner.cs = parallel WIP, do not touch. |
| WPF `Services/Progression/*` + `Services/Settings/ProfileSyncService.cs` (144KB) + Chaos economy (`ChaosUpgrades`, sparks) vs `CCP.Core/Services/Progression/*` + `CCP.Avalonia` progression VMs/services | **WS0 lot 7: progression/quests/economy review** (goal `skia-rebuild-goal.md`): XP/leveling (ProgressionService), quests (QuestService/QuestDefinitionService), achievements (AchievementService), leaderboard, skill tree + PRESTIGE + season recap (incl. the merged `5ce70de6` Ditzy Data PRO analytics), chaos economy (sparks/upgrades/lifetime-boons — coordinate w/ the lot-6 run-engine workstream), and the **P0#2 ProfileSyncService gap** (server sync + prestige/season-reset policy entirely UNPORTED). Three checks per goal WS0 (contract + adversarial rubric + optimality); findings fixed in-lot or filed as rows. Folds in the Sync-from-main lot-7 backlog rows. | @fable | `✅ done` | `feat/crossplatform` | 2026-07-04 | LOT PASSED 2026-07-04. 5-agent review (~42 findings); FIX-IN-LOT across 5 commits (`997aed61` interface seams, `750d2615` wave A achievements atomic-writer P0 + prestige, `bc1df7bb` wave B XP/economy parity, `b1236e8a` wave C Season Recap Core port P0 + OnSeasonReset + prestige accrual, `eaa33975` wave D quest streak/perf/persistence + VM leak). Gates: slnf + WPF sln 0 err (seam guardrail), Core 166→170, smoke baseline-clean (44 tabs, 17 first-chance, 5 findings = known stale-harness blocker + ChaosRun infos; quests+QuestCompletePopup exercised). DEFER: 2 workstreams (server-progression-sync `ProfileSyncService` ~2800 LOC absent; Ditzy Data PRO analytics UI ~832 LOC) + rows in 'Discovered by WS0 lot 7 review'. NOT exercised: live signed-in progression/season-rollover/leaderboard-submit vs WPF. Original claim follows. CLAIM. Baseline: HEAD `22f45b57`, build 0 err both heads, Core tests 166/166, smoke baseline-clean (44 tabs). Surface: WPF `Services/Progression/{Achievement,Leaderboard,Progression,QuestDefinition,Quest,SeasonRecap,SkillTree}Service.cs` + `Services/Settings/ProfileSyncService.cs` (144KB) + `Services/Chaos/ChaosUpgrades.cs`; Core `Services/Progression/{Achievement,Leaderboard,QuestDefinition,Quest}Service` + `ISeasonRecapService` + `Models/{SkillTree,AchievementProgress}.cs`; Avalonia `Platform/AvaloniaSkillTreeService.cs` + `Services/Progression/AvaloniaSeasonRecapService.cs` + `SeasonRecap{Card,ViewModel,Window}` + `ViewModels/Tabs/QuestsTabViewModel.cs` + EnhancementsTab (Ditzy Data PRO). HEADLINE GAPS (merge audit): P0#2 ProfileSyncService absent (prestige `lifetime_points_spent`, season-reset skill pruning w/ `PermanentIds`, balance-never-reset); Ditzy Data PRO analytics UI absent; SkillTree `OnSeasonReset` + `SeasonPeakLevel` capture + schema-2 recap snapshot unported. Guardrail: SmokeTestRunner.cs = parallel WIP, do not touch. |
| WPF `Services/Account/*` (Account/Patreon/Discord/DiscordRichPresence/V2Auth/V2DeviceCode) + `Browser/*`/`MainWindow.Browser.cs` + AvatarTube/Companion vs `CCP.Core/Platform/IBrowserHost` + `Services/Auth/*` + `CompanionServiceInterfaces` + `CCP.Avalonia/Services/Auth/*` + AvatarTube | **WS0 lot 8: browser/integrations review** (goal `skia-rebuild-goal.md`): in-app browser (WebView2 host + system-browser flow + OAuth browser via `IBrowserHost`/`BrowserLauncher`), account/auth (login/logout #455 AuthToken clear, V2 device-code, token STORAGE SECURITY + privacy — tokens never logged/plaintext), Patreon PREMIUM GATING (#465 linked-Patreon +14d grace + full `HasPremiumAccess`/`HasAiAccess` gate audit), companion/avatar integration (#463 busy-retry `ShowAvatarLine` 16×750ms while speaking, speech/reaction dispatch), Discord (OAuth link + Rich Presence session/idle/per-effect — lot-2 C6 defer). Three checks per goal WS0 (contract + adversarial rubric + optimality). Folds the Sync-from-main lot-8 backlog (#463/#465/#455 + Discord RPC). SCOPE NOTE: the big `ProfileSyncService` server sync is the lot-7 deferred workstream, NOT this lot; browser is a Windows WebView2 platform seam (Linux/macOS degrade gracefully). | @fable | 🚧 wip @fable | `feat/crossplatform` | 2026-07-04 | CLAIM. Baseline: HEAD `317236a0`, build 0 err both heads, Core tests 170/170, smoke baseline-clean (44 tabs, 17 first-chance). Surface: WPF `Services/Account/{AccountService,PatreonService,DiscordService,DiscordRichPresenceService,V2AuthService,V2DeviceCodeService}.cs` + `CCP.WindowsOnly/WpfBrowserHost.cs` + `Helpers/BrowserLauncher.cs` + `MainWindow/MainWindow.Browser.cs` (114KB); Core `Platform/IBrowserHost.cs` + `Services/Auth/{IV2AuthService,V2AuthModels,BrowserLauncher}` + `SecureAuthTokenStore.cs` + `CompanionServiceInterfaces.cs` + `Models/{DiscordModels,PatreonModels}`; Avalonia `Services/Auth/{AvaloniaV2AuthService,AvaloniaV2DeviceCodeService,AvaloniaPatreonProvider,AvaloniaDiscordProvider,AvaloniaSubscribeStarProvider,AvaloniaTokenStorage,AvaloniaUserIdentityProvider,AuthSecurityHelper}` + AvatarTube. KNOWN merge gaps to verify: #455 logout AuthToken clear, #465 Patreon +14d + HasPremiumAccess audit, #463 companion busy-retry, Discord Rich Presence absent (lot-2 C6). PRIVACY: auth tokens must be encrypted-at-rest + never logged. Guardrail: SmokeTestRunner.cs = parallel WIP, do not touch. |

### Hand-off Queue (porter → orchestrator)

When a lane reaches `🔵 review`, append an entry here. The orchestrator applies the chokepoint changes
(DI, csproj assets, loc merge), merges, builds, then deletes the entry.

| Lane | Blocker / needed change | Notes |
|---|---|---|

### Claim protocol (mirror of plan §20.4)

1. **Claim:** add your ledger row with `🚧 wip @agentN` and **commit that row first** (cheap "claim commit")
   so concurrent agents see it. Don't start an item that already has a `🚧`/`🔵` row.
2. **Work:** stay inside your lane's subtree (plan §20.1). If you need a chokepoint change, **do not edit it** —
   write it into the Hand-off Queue.
3. **Review → integrate:** set the row to `🔵 review`, fill the Hand-off Queue, hand the worktree to the
   orchestrator. The orchestrator integrates, sets `✅ done`, and clears the row.

---

## ⚠️ Known Functional Gaps (P0 — reported by maintainer)

The Avalonia app builds and mostly *looks* right, but several features **don't work yet** — "builds + looks right"
is not done (plan §13.6). Each row below is a P0 lane: fix it, then **prove it by exercising the feature in the
running app** (and side-by-side with the WPF app, plan §13.5). The maintainer said "to name a few" — assume more
exist; only a real click-through of every feature finds them, so do a full functional sweep, not just this list.

| # | Gap | Likely cause / where to look | Verify by |
|---|---|---|---|
| 1 | ✅ **START button launches the mode** — fixed: `MainWindowViewModel.StartSessionAsync` now falls back to `Session.QuickStartFromSettings(_settingsService.Current)` when no preset session is selected, mirroring WPF's direct `StartEngine` behavior. The quick-start session copies enabled toggles from `AppSettings` and the orchestrator starts Flash/Video/Subliminal/MindWipe/BouncingText/Bubbles/BubbleCount/LockCard/Overlay services. | Button was bound and the command called `ISessionService`, but it required a pre-selected `CurrentSession` and showed "select session first" when none was chosen; WPF directly starts engines from settings. | Click START → flashes/video/overlays/avatar actually begin, same as WPF. |
| 2 | ✅ **Left AI avatar reacts to clicks** — fixed: `AvatarBorder` now has `Background="Transparent"` so Avalonia hit-testing registers clicks over the avatar area; the click handler calls `IBarkService.NotifyAvatarClicked()` and the Avalonia head raises an `AvatarClicked` event that the active `AvatarTubeWindow` subscribes to, producing a random phrase giggle. Double-click now opens the chat input when AI is available (WPF parity). `AvatarEnabled` changes in Companion settings are now wired to show/hide the tube. | Avatar tube not wired to `IAvatarWindowService` / bark / companion services, or click/interaction handlers missing. | Click/interact with the avatar → it reacts (speech/emote) like WPF. |
| 3 | ✅ **"Down the Rabbit Hole" Chaos run now awards XP/progression** — fixed: `AvaloniaChaosService` now takes `IProgressionService` and calls `AddXP(..., XPSource.Chaos)` at the end of a run, matching WPF `ChaosModeService` payout. Dashboard banner rotator and `MarqueeTabViewModel` were already wired. The broader Chaos run-economy placeholders (SFX, catalogues, narrative audio, etc.) remain under gap C. | Progression/marquee not driven; check `IProgressionService` wiring + the marquee/banner. | Progression advances / marquee animates as in WPF. |
| 4 | ✅ **Pink-fill overlay blocks clicks** — fixed: `OverlayWindow` now sets `IsHitTestVisible=false`, and `ApplyWindowStyles` applies `WS_EX_TRANSPARENT\|WS_EX_LAYERED\|WS_EX_NOACTIVATE\|WS_EX_TOOLWINDOW` after creation. | Overlay window not fully input-transparent — missing `IsHitTestVisible=false` **and/or** `WS_EX_TRANSPARENT\|WS_EX_LAYERED\|WS_EX_NOACTIVATE` (applied after handle exists). Bypasses `WindowsOverlaySurface`/`AvaloniaOverlayService`. Plan §7.4. | With the overlay up: click the app's own buttons *through* it, AND fully use a second app placed behind it (click/type/Alt-Tab) — the overlay must not interfere. |
| 5 | ✅ **Spiral overlay blocks clicks** — fixed: `SpiralOverlayWindow` already set `IsHitTestVisible=false`; `ApplyWindowStyles` now ensures the same Win32 transparent/noactivate/toolwindow ex-styles. | Same root cause as #4 for the spiral overlay path. | Same as #4. |
| 6 | ✅ **Multi-monitor (N screens) correctness** — fixed: added `ScreenWindowHelper.ConstrainToScreen` so every screen-spanning window uses raw screen coordinates for `Position` and DIPs (`Bounds / Scaling`) for `Width`/`Height`. Updated `AvaloniaOverlayService`, `AvaloniaSubliminalService`, `AvaloniaBouncingTextService`, `AvaloniaFlashService`, `AvaloniaDualMonitorVideoService`, and `AvaloniaVideoService`. `AvaloniaScreenProvider` now wires `Screens.Changed` to `IScreenProvider.ScreensChanged`; `AvaloniaOverlayService` refreshes overlays on monitor hotplug/resolution changes. `AvaloniaDualMonitorVideoService` already iterated `Screens.All`; the scaling fix makes it correct on mixed-DPI layouts. | `AvaloniaScreenProvider`, `Helpers/ScreenWindowHelper.cs`, `Services/{Overlays,Subliminal,BouncingText,Flash,Video}/*.cs` | On a 3-monitor mixed-orientation layout (1 landscape + 2 portrait, and vice-versa): video/overlays fill each screen correctly with no stretch/clipping; single-display setting confines to one screen. |
| … | **More exist** ("to name a few") | Do a full functional sweep — every feature card, tab action, dialog, and overlay. | Exercise each; log new gaps as rows here. |

### Discovered by WS0 lot 1 review (2026-07-02) — persistence area

Fixed in-lot (commits `e9501ce8`, `a2d1b9a8`, `b694b543`): secret-store no-op stub regression (AuthToken/OpenRouterApiKey persisted nowhere in either head), missing `MigrateLoudnessThreshold`/`RunFlashClickableDecouplingMigration` in Avalonia, exit flush gaps (tray Exit/panic-key skipped SaveImmediate; achievements/quests never disposed → 30s counter loss per exit), quests.json serializer mismatch, roadmap photo-path/timestamp drift, Roaming→Local migration orphaning WPF achievements.json, corrupt settings.json clobbered without quarantine. Deferred rows:

| Priority | Item | Notes |
|---|---|---|
| P1 | Cloud settings backup unported | `AvaloniaSettingsBackupProvider` is a LOCAL folder stopgap (settings-backups/, 20 retained); WPF uploads via ProfileSync. Avalonia users have no off-machine backup. Belongs to profile-sync port (WS0 lot 8) |
| P1 | Patreon tab cloud restore is a stub | `PatreonTabViewModel.RestoreFromCloudAsync` never restores (shows "restore failed"). When porting, use the new `ISettingsService.CurrentReplaced` event (added in lot 1) so consumers rebind |
| P1 | Gaze-pop ignores FlashGazePopEnabled | `AvaloniaGazeFocusService` blink/dwell handlers (~:133-231) pop flashes without checking `FlashGazePopEnabled` — WPF divergence. Verify+fix in WS0 lot 5 |
| P2 | OAuth token import from WPF | Avalonia stores provider tokens under ISecretStore `{provider}:tokens`; WPF uses `{prefix}_auth.dat` (DPAPI, entropy `ConditioningControlPanel_{Label}_v1`). Switching heads forces re-login. Optional one-time Windows import would seed ISecretStore from the legacy files |
| P2 | CCP.WindowsOnly is an orphaned project | Referenced by zero projects; `WpfSecretStore` (entropy `CCP_WindowsOnly_SecretStore_v1`) is dead code. Audit and delete or wire deliberately |
| P2 | WPF achievements.json lives in Roaming | WPF-side quirk (everything else is Local). Heads fork achievement progress when alternated (accepted during port; migration now copies instead of moves so WPF keeps its data). Recommend main-side fix to Local; product decision |
| P3 | Both heads concurrently = last-write-wins | No file locks on settings.json etc. Accepted: verification harness runs heads for side-by-side comparison, not simultaneous settings editing |
| P3 | Roadmap legacy UTC timestamps | Old Avalonia builds stored UtcNow; now aligned to WPF DateTime.Now. Old entries show UTC wall-clock; `TimeToCompleteMinutes` spanning the fix bakes in the offset. No migration (cannot distinguish intent) |
| P2 | Brain-drain audio half unported (WS0 lot 2 finding F1) | WPF `Services/LockCard/BrainDrainService.cs` is audio-only (probability timer plays whispers from `Resources/sounds/braindrain`, fires `BrainDrainTriggered` for avatar giggles, Discord presence); no Avalonia equivalent; orphaned handler `AvatarTubeWindow.Reactions.cs:168` has zero subscribers. Visual blur half IS at parity via AvaloniaOverlayService. Ships-empty folder = low impact. Port in effects/audio lot per AvaloniaMindWipeService pattern; fix plan in lot 2 verdicts (F1) |
| P1 | Subliminal whisper linked-audio unported (WS0 lot 2 finding F13) | WPF `SubliminalService.cs:163-208/385/481-511` plays audio linked to subliminal phrases; Avalonia `AvaloniaSubliminalService` lacks the path. NOTE: attention checks/webcam/haptics at session start are confirmed FALSE alarms (WPF does not start them either) — do not add to orchestrator. Fix in subliminal/audio lot |
| P2 | Discord Rich Presence unported (WS0 lot 2 finding C6) | WPF sets session/idle/per-effect presence via `Services/Account/DiscordRichPresenceService.cs`; zero Avalonia equivalent. Defer to integrations lot 8; research IPC transport cross-platform first (avalonia-research) |
| P3 | Phantom seam `ISessionPlatformBridge` (WS0 lot 2 rubric R2) | One-method interface, one impl, body is portable BCL code — fold `OpenCustomSessionsFolder` into Core `SessionManager`, delete interface+impl+DI row. Ponytail-bar violation, no functional bug |
| P2 | Startup regression vs optimized baseline (WS0 lot 3) | MainWindowShownMs 2781-3067ms vs benchmark-optimized.json 1977ms (two runs, warm cache). Suspects: lot-2 service additions (scheduler/ramp wiring), first-tick compositor work, ambient variance. Profile + re-baseline during WS3 perf sweep (that is where the goal's hard benchmark gate applies). FPS/memory deltas are explained and accepted (display-synced dirty gating; spiral frame cache ~85MB is WPF-parity persistent) |
| P2 | ChaosDvdOverlay mixes physical origin with DIP size (WS0 lot 3 leave-with-row) | `Position.X + Width` rect sweep is exact only at 100% scaling; pre-existing chaos-window nit — fold into WS2 chaos migration (physical-px contract now documented on IAvaloniaLayer) |
| P2 | WarmSpiralCache has no chaos-start caller (WS0 lot 3) | WPF calls it at ChaosModeService.cs:406; Avalonia call site belongs in AvaloniaHeadStubs.StartRun — wire during WS2 chaos lot |
| P3 | Per-frame allocation audit for video layers (WS0 lot 3 defer) | L3-11: active layers cleaned in-lot; VideoLayer/MandatoryVideoLayer audit belongs to WS1 Phase D (UCE plan already tracks the SKBitmap alloc kill) |
| P1 | spiral.gif not bundled in CCP.Avalonia assets — **FIXED in WS0 lot 3** (`0c43277f`: AvaloniaResource link + embedded extract fallback; smoke avares exceptions gone) | Smoke run 2026-07-02: 2x first-chance `FileNotFoundException: avares://CCP.Avalonia/Assets/spiral.gif` — mod pack-URI translation (`AvaloniaBitmapHelper`/`PackUriToBitmapConverter`) targets an asset the Avalonia head never bundles (WPF bundles `Resources/spiral.gif`, csproj:216; no .gif exists under CCP.Avalonia/Assets). Default spiral overlay/thumbnail likely blank without user/mod spiral. Fix in WS0 lot 3 (overlays) — needs asset + csproj (chokepoint) |

### Discovered by WS0 lot 4 review (2026-07-03) — video/audio area

Fixed in-lot (commits `1bef3b5b`, `32ad38f8`, `d6222069`, `a8d3df33`): see the lot row above. Deferred rows:

| Priority | Item | Notes |
|---|---|---|
| P2 | ScreenMirror (display-topology clone) unported (V2-1) | WPF `Services/Video/ScreenMirrorService.cs:60` flips Windows into `SetDisplayConfig` CLONE topology for browser/enhancement fullscreen video, restores EXTEND on teardown. No Avalonia seam/impl. Port when the Avalonia browser fullscreen + Deeper enhancement-player fullscreen surfaces exist (lot 8 / Deeper lot) |
| P2 | AudioSync runtime pipeline unported (A1-16) | Settings/UI/pure `AudioAnalyzer` are in Core, but `AudioSyncService`/`ChunkManager`/`VideoDownloader` (web-video audio → haptics) have no Core/Avalonia counterpart; the toggle is inert. Port in haptics/integrations lot |
| P2 | Whisper-audio busy gate missing (A1-15) | WPF `AudioService.IsWhisperAudioPlaying/MarkWhisperAudio` keeps barks from talking over whispers. Blocked on the subliminal whisper linked-audio port (lot-2 deferred row F13) — implement together |
| P2 | Bambi-Freeze pre-roll around triggered videos (V1-7) | WPF `VideoService.cs:735-772` fires `TriggerBambiFreeze(deferReset:true)` + 800ms wait before videos (skipped when attention checks/minigame active), deferred reset on cleanup. Needs `ISubliminalService` freeze seam — subliminal/effects lot |
| P2 | Video haptics missing (V1-15/T1-4) | WPF starts a background vibe for the video, pulses `VideoTargetHitAsync` per attention hit, stops on cleanup. Needs haptics seam methods; haptics lot |
| P2 | Session-lock/sleep force-cleanup missing (V1-14) | WPF `VideoService.cs:516-628` tears down video on Win+L/suspend to avoid frozen-overlay-after-unlock. Needs Windows head SystemEvents (or WTS notification) wiring — WS3 sweep |
| P2 | Intermittent smoke crash: cross-thread `SolidColorBrush.SerializeChanges` (2026-07-03) | 1 of 3 lot-4 close smoke runs died: unhandled `InvalidOperationException` (calling thread cannot access) in `Brush.SerializeChanges` → `Compositor.CommitCore` during paint. Not attributable to lot-4 changes by inspection (all new service calls are UI-marshaled); 2 subsequent runs clean. Suspect theme-reskin/ChaosRun brush churn; investigate in WS3 or with the smoke-harness WIP owner (chip task_627c90f2) |
| P3 | "No videos found" guidance + one-time codec-failure dialog (V1-19/V1-20) | WPF shows a content-pack-aware MessageBox when triggering with an empty library and a one-time codec warning on MediaFailed; Avalonia logs only. Needs IDialogService plumbing into the video service or the calling feature control. (WPF's MediaElement fallback is WPF-only; not portable) |
| P3 | Scheduler does not defer to active web video (V1-21) | WPF skips scheduled videos while `App.Autonomy?.IsWebVideoActive`; no Avalonia equivalent flag exists yet. Wire when autonomy web-video state is ported (lot 8) |
| P3 | Remote `trigger_wallpaper` is toggle-only (V2-5) | WPF activates/shuffles; `IWallpaperProvider` has no Activate/Shuffle. Now that original-capture/restore exists, extend the seam when the wallpaper feature lot lands |
| P3 | 16ms DispatcherTimer blit vs render-tick (V2-7) | Multi-monitor + inline-loop video blit on a fixed 16ms timer instead of vsync-aligned rendering (WPF used CompositionTarget.Rendering). Smoothness measurable only at runtime — fold into WS3 perf sweep / WS1 UCE video work |
| P3 | Gaze attention-check revival kit (T1-1 follow-up) | Mechanic is now dormant per WPF contract. If revived by product call: apply overlay ex-styles to the ring window (R1-1: WS_EX_TRANSPARENT|LAYERED|TOOLWINDOW|NOACTIVATE missing), fix `_lastGazePoint` torn read (R1-20), and re-add the tab/DataTemplates removed in `a8d3df33` |
| P3 | MiniPlayer resize grip (V2-8 residue) | WPF uses `CanResizeWithGrip`; Avalonia plain CanResize (no grip visual). Cosmetic; dashboard-design pass |

### Sync-from-main: merge `5ce70de6` (2026-07-03) — WPF shipped, the port must catch up

Main merged into `feat/crossplatform` (range `f9348d7e..5ce70de6`; mostly binary asset
re-compression + 51 code files). Analyzed by 4 parallel parity agents + first-hand
verification of the two P0s. Themes: **feat/animated-webp**, **Ditzy Data PRO / Prestige**
(skill-tree analytics), a **#462 interaction-race** fix cluster, session-ramp + profile-sync
fixes, and startup-robustness (WPF-only). Core-model deltas (SkillTree 5 Tier-6 nodes +
`IsPermanent`/`PermanentIds`; SeasonRecap schema-2; `AchievementProgress.LifetimeSkillPointsSpent`;
AppSettings `SubliminalSolidMode`/`SeasonPeakLevel`/`SeasonPointsSpent`) **auto-flow** to both
heads — the fields already exist in the Avalonia build, unused until the services below consume
them. The 7 new loc keys + webp keys landed in the WPF-folder `Localization/Languages/*.json`
(the BUILT Content source per `CCP.Core.csproj:50`) so they **auto-flow too** — do NOT mirror into
the dead/corrupt `CCP.Core/Localization` copy.

| Priority | Item | Notes |
|---|---|---|
| ~~P0~~ **DONE** | Session ramp clobbers persisted pink/spiral opacity (RE-VISIT closed lot 2 — data-loss) | ✅ **FIXED `b1336991`**. `SessionService.UpdateRampingValues:400,408` now direct-drives the overlay via `IOverlayService.SetSustainedOverlayOpacity` (injected `IOverlayService?`) and no longer writes `_settings.Current`; new `ReleaseOpacityRampHolds()` on `IOverlayService`/`AvaloniaOverlayService` (nulls the `_adHoc*` holds + resets both dedupe sentinels) is called after `SessionSettingsScope.Restore` on stop. Ports WPF #471/#476. +2 regression tests (direct-drive-not-persisted + release-on-stop); `RampInterpolation_*` updated to the fixed behavior. Gates: slnf 0/0, WPF sln 0/0 (no WPF class implements Core `IOverlayService`), Core 164→166, smoke baseline-clean. **CORRECTION:** `IntensityRampService.cs:122,128` was a FALSE-POSITIVE — WPF `RampTimer_Tick` (StartStop.cs:466,474) also writes settings, so the manual-ramp writes are faithful parity and were left untouched (do NOT "fix" them into divergence). |
| **P0** | ProfileSyncService entirely unported — prestige + season-reset policy (RE-VISIT closed lot 1; spills lot 7/8) | VERIFIED: no `ProfileSyncService` class in CCP.Core or CCP.Avalonia (only Models/VMs reference it). Missing: monotonic `lifetime_points_spent` server sync, skill-point balance never reset at season, tree reset = server ∪ `PermanentIds`, `OnSeasonReset` teardown, `Track/ReconcileLifetimePointsSpent`. Pre-existing gap WIDENED by merge; blocks Ditzy Data PRO. Port into a Core `IProfileSyncService` seam. |
| P1 | Prestige counter in Core AchievementService | Add `TrackSkillPointsSpent(int)` + `ReconcileLifetimePointsSpent(long)` (monotonic, `_isDirty`). Model field already auto-flowed. Benefits both heads. Lot 7. |
| P1 | `AvaloniaSkillTreeService.OnSeasonReset` + prestige wiring | Method absent. Prune non-`PermanentIds` nodes + teardown pink_rush/streak-shield; on purchase call track + season-spend; capture `SeasonPeakLevel` on level-up (replace hardcoded `SkillPoints+=5` with a `PointsPerLevel` constant). Lot 7. |
| P1 | `AvaloniaSeasonRecapService` schema-2 snapshot + rollover | Currently builds ZERO snapshots. Populate `PeakLevel/PointsSpentSeason/LifetimePointsSpent/SkillsOwned/ActiveDays`, `Schema=2`; reset `SeasonPeakLevel`/`SeasonPointsSpent` at rollover; add `TrackPointsSpent`. (Core static `SeasonRecapService.LoadLatest/HasAnySnapshot` are TODO stubs — implement.) Lot 7. |
| P1 | #462 interaction-race cluster (RE-VISIT lot 2/3) | Port the re-entrant-fullscreen-during-close fix set: `AvaloniaInteractionQueueService.Complete` dispatches the queued trigger ASYNC (`Post/BeginInvoke`, never inline under `_lock`); `ForceReset()` BEFORE `ForceCloseAll` on stop (RemoteControl + StartStop equivalents); `LockCardWindow` guards `Complete` on `CurrentInteraction==LockCard`; teardown-aware session-summary defer (`ShowSessionSummaryWhenClear`); `BubbleCountResultWindow` panic bail `if(_allWindows.Count==0) return;`; verify PopQuiz `_answered`-before-`Complete` guard. |
| P1 | #463 companion awareness busy-retry | `AvaloniaKeywordTriggerService.ShowAvatarLine`: retry 16×750ms while `IsSpeaking‖IsSpeakingAudio` before `GigglePriority` (stop cutting her off). Confirm avatar service exposes both flags. Lot 8. |
| P2 | Animated .webp — gate-broadening only (RE-VISIT lot 3/6; ~70% already ported) | Avalonia `SkiaImageDecoder`/`SkiaFrameSet` (SKCodec) already decodes webp format-agnostically — **NO `AnimatedWebp.cs` port, NO new dependency** (user's under-the-hood freedom already satisfied by the fastest tech). Broaden the `.webp` extension gates: `ChaosFlashOverlay.axaml.cs:118`, `ChaosGifCascadeOverlay.axaml.cs:195/201`, `AvaloniaAnimatedGif.TryCreate`; add a webp-frame path for tease bubbles in UCE `BubbleLayer`; regression test. |
| P2 | #465 linked-Patreon premium window + gate audit | `AvaloniaDiscordProvider`: server-confirmed linked tier ≥1 → extend `PatreonPremiumValidUntil` +14d (only +25h today; never shorten). Sweep remaining Avalonia gate sites for stale `PatreonTier>=1‖IsWhitelisted` → `HasPremiumAccess`; Lab AI-effects clear must use the full entitlement union. Lot 8. |
| P2 | #455 logout clears AuthToken | Verify/port: Avalonia logout sets `AuthToken=null` (server rotates the token; a stale one breaks re-login). Lot 8. |
| P2 | SubliminalSolidMode #461 (RE-VISIT lot 3) | New `AppSettings.SubliminalSolidMode` routes subliminal text cards as children of the ONE shared click-through host (`ChaosBubbleHostOverlay`) instead of a per-screen keep-alive layered window (freeze cluster). Port the shared-host render path; ignored while `SubliminalStealsFocus` (host is NOACTIVATE). |
| P2 | Ditzy Data PRO — permanent badges + prestige row | Surface `IsPermanent` FOREVER badge vs OWNED, capstone chip (🎓 Certified Data Bimbo), prestige rank (`1 + LifetimeSkillPointsSpent/100`) + season-reset warning (`msg_skill_seasonal_note`) in `EnhancementsTabViewModel`/View. Data auto-flowed. Lot 7. |
| P2 | Ditzy Data PRO — analytics panels | 4 lazy expanders: PRO Lifetime dashboard, Season Rewind table (▲/▼ deltas, capstone-gated Spent column), Bestie Records, Brain Drain Report. VM-computed rows + AXAML DataTemplates; reads `AvaloniaSeasonRecapService` snapshots + live bucket. Lot 7/9. Depends on the P0 ProfileSync + P1 snapshot rows. |
| P2 | Prestige recap strip + season-reset dialog wording | `SeasonRecapViewModel.PrestigeVisibility/PrestigeLineText` + `SeasonRecapCard.axaml` gold strip; `MainWindow.Marquee` prestige wording (mechanical vs permanent, sparkle balance preserved). Lot 7. |
| P3 | Ditzy Data PRO — charts | Season minutes bar chart, peak-level sparkline, GitHub-style monthly activity heatmap. WPF draws imperatively on `Canvas`; do VM geometry + `ItemsControl`/Skia per chart conventions, or simplify. Cost driver. Lot 7. |
| P3 | Chaos `EffectPayload.Ambient` — fold into the lot-6 run-engine faithful-port workstream | Gate `VideoPayload.ArmRandomSegment` on `!Ambient` so a dashboard 'Trigger Bubble' video doesn't start a full mandatory video midway (#456/#458). Deferred until chaos un-stubs (belongs to the lot-6 P1 run-engine workstream below). |
| P3 | AvatarTube takeover countdown-bar margin re-glue on detach (#464) | Countdown bar floats on detach; re-glue `Margin` in both attached/detached branches. Lot 9. |
| — | WPF-only infra (NO port action) | `UiHangWatchdog` (+145 startup-phase 120s threshold + `--write-hang-dump` minidump), `SplashScreen` STA-thread overhaul, `App.xaml.cs` single-instance mutex-takeover + minidump, `UpdateService` `AppVersion` 6.2.5→6.2.7 + patch notes — Windows-head seams. File a lot-11 note only if the Windows Avalonia head later wants a startup watchdog. Coordinated port-csproj version bump is out of WS0 scope (do per AGENTS.md when the port ships). |

### Discovered by WS0 lot 7 review (2026-07-04) — progression/quests/economy area

5-agent adversarial review (A progression+achievements, Q quests+leaderboard, S skilltree+prestige+recap, PS profilesync+economy, R rubric/optimality); ~42 findings, all with WPF+PORT file:line. STRUCTURAL VERDICT: the XP/leveling logic lives in `CCP.Avalonia/Platform/AvaloniaProgressionService.cs` (NOT Core); Core has `AchievementService`/`QuestService`/`QuestDefinitionService`/`LeaderboardService` + `SeasonRecapService` (mostly TODO stubs); skill tree = `CCP.Avalonia/Platform/AvaloniaSkillTreeService.cs`; `ProfileSyncService` is **entirely absent** (only `AvaloniaV2AuthService` login-apply + a local `AvaloniaSettingsBackupProvider` stopgap). SCOPE SPLIT (lot-6 precedent): **FIX-IN-LOT** the self-contained correctness/safety/economy/persistence + the small pure-Core prestige primitives; **DEFER** the two large interdependent pieces as dedicated workstreams. Overlaps: A1=S9 (skill-pts 5 vs 1), A2=S6=PS-3 (prestige tracking), A8=S5 (SeasonPeakLevel), S10=R7 (SampleRank), S3=R8 (active-days/streak), Q3=R4 (per-tick save).

**FIX-IN-LOT (being worked this lot):**
- **Persistence safety (R1/R2/R12 P0):** `AchievementService.Save()` writes achievements.json non-atomically (`AchievementService.cs:110` naive WriteAllText) AND a background `OnAutoSaveTick` `Task.Run` writer races synchronous `Save()` callers → corrupt/torn file + "collection modified" serialization. Fix: single lock-guarded atomic (temp+rename) writer + tmp-recovery in LoadProgress.
- **XP/economy parity (`AvaloniaProgressionService.cs`):** A1/S9 skill points +5→+1 per level (WPF `PointsPerLevel=1`, 5× over-award); A3 companion XP fed multiplied not base; A7 quest XP fed multiplied not base; A8/S5 `SeasonPeakLevel` never captured on level-up; A9 `TrackSkillPointsEarned`/`TotalSkillPointsEarned` never called; A10 `AddXP(int)` truncates + `<=0` early-return drops the attention-fail penalty (widen to double); A6 `XPChanged` event dropped from `IProgressionService`; R14 silent catch-all warnings.
- **Prestige primitives (pure Core, unblocks the future sync port):** A2/S6/PS-3 add `TrackSkillPointsSpent`+`ReconcileLifetimePointsSpent` (monotonic) to `IAchievementService`/`AchievementService`; PS-4/S6 call them + `SeasonRecapService.TrackPointsSpent` from `AvaloniaSkillTreeService.PurchaseSkillAsync`; S4/PS-2 add `OnSeasonReset` (prune to `PermanentIds`) to `ISkillTreeService`/`AvaloniaSkillTreeService`.
- **Season Recap port (S1 P0/S2/S3/S10/R7/R8):** Core `SeasonRecapService` is TODO stubs (`LoadLatest`→null, `HasAnySnapshot`→false, `CaptureAndRollover` never writes disk, `MarkActiveToday`/`TrackStreakPeak` no-op, `DaysActive=0`) → recap re-view permanently dead + one-shot rollover lost. Port the WPF static `SeasonRecapService` body into Core (disk persistence, schema-2 `BuildSnapshot`, `RollBucket` reset, `SampleRank` in `AvaloniaSeasonRecapService`). S8 rollover path: use server season key + fallback dialog + forward-only guard.
- **QuestService (Core) + VM:** Q1 daily-streak inflation (up to 3×/day — gate `AdvanceQuestStreak` on `firstCompletionToday`); Q3/R4 synchronous quests.json write every increment (dirty-only + timer flush); Q7 `EnsureGenerated` rollover preserve/reconcile; R6 Oopsie streak-fix not persisted (XP spent for nothing — add `IQuestService.FixStreakDay`, debit XP only after write); Q2/Q4 missing skill-tree XP multipliers (add `GetRerollBonusMultiplier`+`CheckPerfectWeekBonus` to `ISkillTreeService`); R3 settings.json saved every second from the conditioning-minute tick (accumulate + 30s flush); R5 cross-thread `ObservableProperty` mutation in `QuestsTabViewModel` (dispatcher-marshal); R10 per-second reflection/`dynamic`/service-resolution in `TrackTimeBasedProgress` (typed calls + cache refs); R11 `QuestsTabViewModel` 5-event leak (IDisposable); R13 delete 2 dead methods.
- **Cheap UI slice (S7 partial):** FOREVER-vs-OWNED badge + prestige-rank row (`1 + LifetimeSkillPointsSpent/100`) in the Enhancements tab once prestige data accrues.

**DEFER (dedicated workstreams + rows):**

| Priority | Item | Notes |
|---|---|---|
| P0-workstream | **Server-progression-sync port** (`ProfileSyncService`, ~2800 LOC) | Entirely absent. PS-1 periodic bidirectional `/v2/user/sync`, PS-5 defaults-guard+boot-heal, PS-6 cloud settings backup (swap `ISettingsBackupProvider` impl — seam ready), PS-7 logout final push (data-loss risk), PS-8 heartbeat 120s + sync cadence (needs portable timer seam), PS-9 HMAC signing + 401 recovery, PS-10 server-validated oopsie, PS-11 server-authoritative flags (force_skills_reset/streak/quest, admin display-name/OG/whitelist), Q5 leaderboard SUBMIT (Avalonia players never appear on the board), Q6/Q11 logout reset + ForceRegenerate. Fully portable (no new platform seam; HMAC key + HttpClient + Newtonsoft + GZip all portable). Prestige-critical subset (~600-800 LOC) partly covered by the in-lot primitives above; full service = multi-session workstream. |
| P1-workstream | **Ditzy Data PRO analytics dashboard** (S7, ~832 LOC) | 4 lazy PRO panels (Lifetime dashboard, Season Rewind, Bestie Records, Brain Drain) + 3 Skia charts (activity heatmap, minutes bar, level sparkline) + PB timeline, absent from the Avalonia Enhancements tab. Depends on the in-lot prestige + recap-persistence primitives landing first. WPF `MainWindow.Enhancements.cs` (+832). |
| P1 | A4 idle passive-XP suppression | WPF drops Flash/Subliminal/BouncingText XP when idle (anti-farm); port has no idle gate. Needs an `IActivityTracker` seam. Passive AFK farming currently possible. |
| P2 | A5 login/offline XP gate | WPF withholds XP unless logged-in or offline+username; port awards unconditionally. Verify the port never awards XP signed-out; belongs with the sync/account workstream. |
| P2 | A11 attention-check XP revival | Port awards +20 XP on attention-pass that WPF scrapped pre-ship. Verify `AttentionCheckEnabled` defaults false + never starts; gate behind revival if it can run. |
| P2 | A12 level-up network side-effects | Discord RPC level update + milestone webhook + cloud ProfileSync on level-up (haptics fixable in-lot if a haptics seam is wired; network bits defer to the sync workstream). |
| P3 | A13/A14, Q8/Q9/Q10, R9/R15 | Bubble-milestone routes a synthetic id through `AchievementUnlocked` (A13); `TryUnlock` async vs sync ordering (A14); DailyQuestGeneratedAt Today-vs-Now (Q8); AddProgress clamp display (Q9); quest completion sound/haptics (Q10 — fixable if haptics seam); TrackBubblePopped economy-grant + auto-save from tracking path (R9 monitor; move to timer when persistence lands); quest-definitions cache non-atomic write (R15). |

### Discovered by WS0 lot 6 review (2026-07-03) — chaos/game-mode area

Fixed in-lot (commits `b9fb74be`, `3f4cceb8`, `b828fea5`): see the lot row. Deferred rows:

| Priority | Item | Notes |
|---|---|---|
| P1 | **Chaos run-engine faithful port** (dedicated workstream) | The Avalonia run engine `AvaloniaChaosService` (`CCP.Avalonia/Services/AvaloniaHeadStubs.cs`) is a simplified stand-in for WPF `Services/Chaos/ChaosModeService.cs` (~3000L). Its interdependent economy/spawn/scoring layer only makes sense ported together: X1-1 spark reward depth (SparkGainMult/DripFeed TrickleDrops/drip capstone/first-fall/duration cap), X1-2 XP quantity (award min(Score,250·durMin·diff) not the spark formula), X1-6 ChaosMeta.ApplyTo (all 6 purchasable upgrades currently inert — slow_fuses/silk_touch/popup_notification/pendulum_swing/draft4/extreme_tier), X1-8 the full weighted variant pool w/ intensity escalation + density cap + behavioral bubbles (Echo/Chaperone/Tease/Bound/Brittle) + Heavy Drop + golden/prism rolls (Avalonia spawns 1 bubble/900ms flat 45% from a 3-id pool), X1-9 EnabledVariants null=all default, X1-10 boon-draft duo/trio RequiresAny/All + sin-slot + Unique-taken exclusion + DraftChoices, X1-12 per-pop scoring depth (size/benign-baseline/PayMult/Pendulum/coin-flip/Blindfold/LastBreath), X1-18 dedicated Audio/BambiFreeze payloads. Pulls in X3-13 rabbit-spank lesson hook, X3-2 glass_shatter cue (needs Brittle path), X3-2 ui_unlock (reveal-card surface). Sized as a focused multi-thousand-LoC effort; parity of the visual/economy-data/lifecycle layer already landed in lot 6. |
| P2 | ChaosBubbleHints onboarding unported (X3-8) | WPF first-contact verb hints ('hold to snap'/'click to pop'/'let it leave', persisted learned-set) have no Avalonia port; only the `BubbleHintsLearned` meta field survives. New players get no per-bubble hints. Couples to the bubble layer |
| P2 | Boon-bar ribbon tooltips unreachable (X3-10) | The ribbon now shows (lot 6 X3-1) but is `IsHitTestVisible=false` + WS_EX_TRANSPARENT, so its BuildTip tooltips never fire; WPF keeps it hit-testable (NOACTIVATE+TOOLWINDOW, no TRANSPARENT). Making it hit-testable risks a top-of-screen click-through regression — needs care, deferred |
| P2 | DVD shared-host + vsync stepping (X2-9/X2-10) | Avalonia gives each DVD logo its own top-level window + 33ms DispatcherTimer; WPF routes non-clickable logos into one shared Canvas host stepped from a single CompositionTarget.Rendering vsync loop, because per-frame SetWindowPos on ~16 logos freezes the companion avatar + judders. Perf/architecture; WS3 or a DVD-specific pass |
| P3 | vsync-aligned stepping for gif-cascade/flash/skia (X2-21) | 16ms DispatcherTimer vs WPF CompositionTarget.Rendering; smoothness only, measurable at runtime; WS3 perf sweep |
| P3 | Dashboard-trigger ForceTopmost wiring (X2-22) | `AvaloniaChaosWindowZ.ForceTopmost` API was added in wave B but the call-sites (gif-cascade/flash show path when `!IsRunning`) are unwired — a stale Topmost=false can bury a dashboard-triggered wash behind the app. Trivial follow-up |
| P3 | Announcer/WaveTimer semantic palette theming (X2-20) | Avalonia routes Mantra→theme pink / Temptation→Danger / Depth→TextLight instead of the fixed gold/red/white announce color language. Arguably a reskin improvement (dashboard-design) but a WPF divergence — product decision |
| P3 | ChaosArt assets-root divergence (X2-26) | WPF searches App.UserAssetsPath (default %APPDATA%/assets); Avalonia searches EffectiveAssetsPath (user-chosen folder). A custom-assets user's art override appears in one head, not the other |
| P3 | Countdown click-to-skip (X4-2) | WPF skips the countdown on key OR click (WH_MOUSE_LL hook); Avalonia's IInputHook exposes only KeyPressed/MouseMoved (no mouse-button), so only keyboard-skip works. Needs an `IInputHook.MouseButtonPressed` Core seam addition |
| P3 | Tunnel z-guard EnforceBelowTunnel/WndProc (X4-4) | Avalonia tunnel only sinks-to-bottom every 1.5s (below MainWindow); WPF keeps it above MainWindow but below topmost overlays via a z-walk + WM_WINDOWPOSCHANGING hook. Default-off feature |
| P3 | Dormant narrative wiring gaps (X3-4/5/6/7) | StoryModeEnabled=false in both heads, so latent: companion lesson/first-time barks (no NotifyChaosLessonComplete/FirstTime on Avalonia IBarkService), narrator audio ducking, in-run story-card field-pause + zone backdrop, narrator MasterVolume. Revisit when story mode ships |
| P3 | Chaos cosmetic drift (X3-11/12/14/16, X2-23) | Boon-bar staggered tile pop-in, lesson tooltip detail truncation, tips order/timing, intro title glow, snap-ripple shard count/color. Low-value polish |

### Discovered by WS0 lot 5 review (2026-07-03) — speech/mic + gaze/calibration area

Fixed in-lot (commits `2da98c0e`, `53c4b0ab`, `09d13f55`, +MVVMTK): see the lot row above. Deferred rows:

| Priority | Item | Notes |
|---|---|---|
| P1 | 16-point calibration window pipeline port (~1300-1500 LoC) | The seam is complete (OnRawIris/ApplyCalibration/SetRuntimeOffset/ApplyGazeTrim all live on AvaloniaWebcamTrackingService) and the data model + 13 algorithm hunks landed (`837aaa1d`); only the producing window is missing. WPF WebcamCalibrationWindow.xaml.cs (1894L) stages: dot-grid→per-dot iris sampling (MinSamplesPerPoint 12 + retry)→OnRawIris/OnHeadPose bucketing→FinalizeCalibrationAsync (RobustPerDotMean 1044-1110, Cv2.FindHomography, FitCerrolazaPolynomial 1209-1355 + FitRidge 1151-1188 + 2-pass outlier rejection, IrisRange min/max, BuildAxisCorrection 1387-1470)→SetCalibrationLive→ApplyCalibration+persist→verify cursor→bubble test→FitAxisTrim→ApplyGazeTrim→dim-room warning. Lot 5 made the window honest ("not available yet"); a fresh Avalonia user cannot calibrate (WPF-era calibrations migrate transparently). Port when webcam accuracy is a release blocker. CalibrationSoundService (7 NAudio cues) travels with it |
| P2 | Webcam/gaze companion barks unported (W-1) | WPF BarkService wires OnBlink/OnMouthOpen/OnTongueOut/OnLongStare/OnFaceLost/OnFaceFound/OnTrackingStateChanged + GazeFocus.GazePopped into avatar speech; AvaloniaBarkService is a pure stub subscribing to none. Companion/avatar lot |
| P2 | Blink-trainer multi-monitor per-screen decoders (W-7) | Avalonia constructs a separate AvaloniaInlineLoopVideo (independent LibVLC decoder) per overlay, reintroducing the multi-monitor desync/stall WPF fixed with a single MediaElement + VisualBrush mirror (bd628e8). Needs a single-decoder-mirror approach (Avalonia has no VisualBrush) or UCE video layer |
| P2 | Flash gaze-linger (W-6, wave-C skip) | FlashGazeLingerEnabled/FlashGazeLingerExtensionMs settings + FlashFeatureControl UI exist but the Avalonia flash path has no BoostLifetime surface to extend a flash's lifetime while gazed. Needs the flash overlay/layer lifetime-extension API |
| P2 | Video gaze-target dwell (W-5, wave-C skip) | IVideoService.GetGazeTargets/GazeClick were ported (lot 4) and gated on VideoGazeClickEnabled, but AvaloniaGazeFocusService's dwell loop doesn't include video attention targets (only bubbles+flash). Needs target-scoring wired into the dwell loop |
| P2 | Mic-live privacy indicator (S-3, wave-A skip) | WPF lights a title-bar pill on Speech/Wake ListeningChanged; Avalonia fires both events but has zero subscribers. Needs a status surface in the Avalonia shell (MainWindow.axaml WebcamActiveText area) — out of the speech agents' file sets; small UI task |
| P3 | OnGazeMove coordinate-space ambiguity (W-14) | GazeDriftCorrectionService treats gaze as calibrated-monitor-local DIP; AvaloniaGazeFocusService treats it as absolute physical virtual-desktop px. Both cannot be right on a scaled/secondary monitor. Requires a real webcam + calibrated run to resolve (no Avalonia-produced calibration exists yet to project through) |
| P3 | ~18k/s Gen0 landmark allocations (P-10) | AvaloniaWebcamTrackingService per-frame allocates 46 float[2] (FaceMesh) + 71×2/eye (Iris) + ToArray copies (~18k small allocs/s @30fps). Mats/sessions correctly reused; landmark output arrays are not. Gen0 pressure only. WS3 perf sweep |
| P3 | Dead corrupt CCP.Core/Localization/Languages/ mirror json | Non-built (no csproj references it; the Core csproj includes the WPF-folder `..\Localization\Languages\*.json` instead), invalid JSON at line 873 (raw newline in tooltip_when_enabled_…), and it actively misled an agent this lot into writing loc keys to the wrong file. Recommend deletion (verify the folder isn't referenced elsewhere first); harmless at runtime |
| P3 | Push-to-talk self-declared incomplete (S-6) | AvaloniaAutonomyService.Voice wires StartPushToTalk→OnPttKeyPressed→RequestVoiceCommand, arming requires IInputHook; class doc says "no-op for now". Verify IInputHook is registered on the Windows head and establish parity with WPF PTT |

### Discovered by code audit (2026-06-22)

A grep sweep of `CCP.Avalonia` for stubs / `TODO`-wire / "not ported" markers surfaced these **already-known-broken**
areas (the code literally says so). Each is P0/P1 — port the missing Core service or dialog, wire the command, then
**exercise it in the running app**. File refs are starting points, not exhaustive.

| # | Area — what doesn't work | Where (markers) |
|---|---|---|
| A | ✅ **Account login / premium gating ported.** Replaced the Avalonia stubs with real `AvaloniaDiscordProvider`, `AvaloniaPatreonProvider`, and `AvaloniaSubscribeStarProvider` in `Services/Auth/`. Each provider implements `IAuthProvider`, performs the same browser + local `HttpListener` OAuth flow as WPF, exchanges/validates/refreshes tokens via `codebambi-proxy.vercel.app`, persists tokens and cached state through `AvaloniaTokenStorage` + `ISecretStore`, updates `AppSettings.Current` (`HasLinkedPatreon`, `HasLinkedDiscord`, `PatreonTier`, `PatreonPremiumValidUntil`, `UnifiedId`, `AuthToken`), and fails closed to no premium on validation errors. DI registrations updated; `AvaloniaUnifiedUserService` retained. | `Services/AvaloniaHeadStubs.cs` (`Avalonia{Discord,Patreon,SubscribeStar}Provider`, `AvaloniaUnifiedUserService`) |
| B | ✅ **Companion tab + avatar wired** — added `ICompanionService`/`ICommunityPromptService` in Core with Avalonia implementations. Companion switch, active companion details/progress, assign-personality import, activate/remove/deactivate community prompts, and custom-prompt editor are now wired. Avatar show/hide already responded to `AvatarEnabled`; the attach/detach toggle now calls `IAvatarWindowService.SetDetached` and is applied by `AvaloniaAvatarWindowService` to the active `AvatarTubeWindow`. | `ViewModels/Tabs/CompanionTabViewModel.cs`, `CCP.Core/Services/Companion/CompanionServiceInterfaces.cs`, `CCP.Avalonia/Services/Companion/AvaloniaCompanionService.cs`, `CCP.Avalonia/Services/Companion/AvaloniaCommunityPromptService.cs`, `CCP.Avalonia/Services/AvaloniaHeadStubs.cs`, `CCP.Avalonia/AvatarTube/AvatarTubeWindow.Windowing.cs` |
| C | ✅ **Chaos run economy wired** — `AvaloniaChaosSfx` now plays through DI-injected `IAudioPlayer` with mod-overridable cue resolution; unlock-card catalogues (`AvaloniaChaosUnlockCards`) populate from seeded boon/upgrade catalogues; `ChaosNarrator` resolves and plays narrator clips; `ChaosArt.ResolveBanner`/`ResolveRecap` load themed banner/recap images with mod override; `ChaosHubWindow` computes affordable Toybox/Bench badges; `ChaosOverlayWindow` applies Win32 toolwindow/noactivate/transparent styles and skips countdown/story cards via the global `IInputHook`. Remaining Chaos overlay windows (DVD, banner, E-Stim, field FX, flash, GIF cascade, pop text, Skia FX, toy button, unlock card, vibe trail, wave timer, etc.) now also apply the correct Win32 ex-styles and `IsHitTestVisible` settings. | `Chaos/AvaloniaChaosCompat.cs`, `AvaloniaChaosStubs.cs`, `AvaloniaChaosUnlockCards.cs`, `ChaosNarrativeDirector.cs`, `ChaosOverlayWindow.axaml.cs`, `ChaosHubWindow.axaml.cs`, `Chaos/*.axaml.cs` |
| D | ✅ **Assets / content packs wired** — extracted `IContentPackService` to Core, implemented `AvaloniaContentPackService` (download/install/encrypt/activate/uninstall/preview/thumbnails), and wired `AssetsTabViewModel` install/activate/uninstall/refresh/delete commands. | `ViewModels/Tabs/AssetsTabViewModel.cs`, `CCP.Core/Services/Content/IContentPackService.cs`, `CCP.Avalonia/Services/Content/AvaloniaContentPackService.cs` |
| E | ✅ **Feature-card editors wired** — `TextEditorDialog` opens from Subliminal, Bouncing Text, and Lock Card phrase editors; `ColorEditorDialog` and `LockCardColorDialog` open their Avalonia `ColorPickerDialog`; Bubble Count and Video strict-lock toggles show a confirmation dialog. `AttentionTargetEditorDialog` color buttons now also use `ColorPickerDialog`, and the Test button spawns a cross-platform floating preview window using the current colors/font. | `Features/{Subliminal,BouncingText,LockCard,BubbleCount,Video}FeatureControl.axaml.cs`, `Dialogs/AttentionTargetEditorDialog.axaml.cs` |
| F | ✅ **Webcam / gaze tracking frame capture** — `AvaloniaWebcamTrackingService` ported to Windows head and registered as `IWebcamService`. Uses OpenCV + ONNX (BlazeFace/FaceMesh/Iris) with `IScreenProvider`/`IUiDispatcher` replacing WPF `Screen`/`Dispatcher`; all detection/events preserved. `Calibrate()`/`TestTracker()` remain no-ops until the Avalonia calibration/test windows are wired to the live gaze stream. | `CCP.Avalonia.Desktop.Windows/Services/Webcam/AvaloniaWebcamTrackingService.cs`, `Program.cs` |
| G | ✅ **Roadmap diary persists** — `IRoadmapService.UpdateStepNote` added; `AvaloniaRoadmapService` now loads/saves `roadmap.json` and auto-saves dirty state; save button wires through the service. | `Dialogs/RoadmapDiaryDialog.axaml.cs`, `Platform/AvaloniaRoadmapService.cs`, `CCP.Core/Services/Roadmap/IRoadmapService.cs` |
| H | ✅ **Mod manager wired** — Export/Create work. Mod creator internal stubs: ✅ color picker now opens `ColorPickerDialog` and writes hex; ✅ audio preview plays/stops via `IAudioPlayer`; ✅ interactive tutorial overlay now launches the Modding tutorial through `AvaloniaTutorialService` and navigates sections via `RequiresTab="mod:..."`. | `Dialogs/ModManagerDialog.axaml.cs`, `Windows/ModCreatorWindow.axaml.cs`, `Services/Tutorial/AvaloniaTutorialService.cs` |
| I | ✅ **Companion phrase editor ported** — `ICompanionPhraseService` moved to `CCP.Core`, `AvaloniaCompanionPhraseService` now returns real built-in/voice-line/custom phrases from the active mod and settings, and audio file copy uses the service. Hard-coded add-phrase dialog strings localized. | `Dialogs/CompanionPhraseEditorDialog.axaml.cs`, `CCP.Avalonia/Services/Companion/AvaloniaCompanionPhraseService.cs`, `CCP.Core/App.cs` |
| J | ✅ **Generic tabs fall back to a blank `PlaceholderTabView`** — fixed: added the missing `AvailableSubjectsTabViewModel` → `AvailableSubjectsTabView` mapping in `TabContentDataTemplates.axaml` (the mobile shell's shared dictionary); the desktop inline mapping already existed. `PlaceholderTabView` now also displays the unmapped tab's `Key` for debugging. | `Views/TabContentDataTemplates.axaml`, `Views/MainWindow.axaml`, `Views/Tabs/PlaceholderTabView.axaml` |
| K | ✅ **Misc:** right-button-**up** mouse-hook event added to `IMouseHook` and `AvaloniaMouseHook`; Chaos ripple now casts on right-button release (WPF parity). `AvaloniaHapticsService` now uses a real Buttplug.io implementation. Audio default-device detection implemented in `AvaloniaAudioDeviceService.GetDefaultOutputDeviceId`: Windows head already uses `WindowsAudioDeviceService` (NAudio); shared fallback queries PulseAudio (`pactl info`) on Linux and `SwitchAudioSource` on macOS, then falls back to the first enumerated device. | `CCP.Core/Platform/IMouseHook.cs`, `CCP.Avalonia/Platform/AvaloniaMouseHook.cs`, `CCP.Avalonia/Services/Haptics/AvaloniaHapticsService.cs`, `CCP.Avalonia/Platform/AvaloniaAudioDeviceService.cs` |
| L | ✅ **Themed images/animations didn't switch with the active mod.** Avalonia image loading ignored mod `resources/` overrides and did not refresh when the mod changed. | `Services/Mod/AvaloniaModResourceResolver.cs`, `Helpers/AvaloniaBitmapHelper.cs`, `Converters/PackUriToBitmapConverter.cs`, `Features/*Card*.axaml.cs`, `Views/Tabs/SettingsTabView.axaml.cs`, `Services/Overlays/AvaloniaOverlayService.cs`, `AvatarTube/AvatarTubeWindow.Avatar.cs`, `Services/AvaloniaBubbleService.cs` |
| M | ✅ **Moderation counter was an Avalonia no-op stub.** Moved `IModerationCounter`/`ModerationCounterState` to `CCP.Core`, implemented real `ModerationCounter` with sliding-window escalation, persistence under `IAppEnvironment.ApplicationDataPath`, and serialized disk writes. Replaced the Avalonia stub in DI; hydration wired at startup; cooldown UI in `AvatarTubeWindow` now responds to real events. | `CCP.Core/Services/Moderation/IModerationCounter.cs`, `CCP.Core/Services/Moderation/ModerationCounter.cs`, `CCP.Avalonia/Services/Moderation/AvaloniaModerationCounter.cs`, `CCP.Avalonia/ServiceCollectionExtensions.cs`, `CCP.Avalonia/App.axaml.cs`, `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` |
| N | ✅ **Rapid avatar-click "Bambi Cum and Collapse" trigger was a no-op stub.** Added `VoiceLineFolder` to `ICompanionPhraseService`, exposed it from `AvaloniaCompanionPhraseService`, and implemented `AvatarTubeWindow.TriggerBambiCumAndCollapse` to play a random collapse voice-line through `IAudioPlayer` and show the priority giggle. | `CCP.Core/App.cs`, `CCP.Avalonia/Services/Companion/AvaloniaCompanionPhraseService.cs`, `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` |
| O | ✅ **WelcomeDialog used a hard-coded "Subject" affirmation.** Added `IModService.GetAffirmation()`, implemented it in `AvaloniaModService`, and wired `WelcomeDialog` to show the active mod's affirmation term. | `CCP.Core/App.cs`, `CCP.Avalonia/Services/Mod/AvaloniaModService.cs`, `CCP.Avalonia/Dialogs/WelcomeDialog.axaml.cs` |
| P | ✅ **Chat input focus was a no-op `Activate()` with a TODO for `AttachThreadInput`.** Implemented `ForceForegroundWindow` with Win32 thread-input attachment on Windows and a Topmost-pulse fallback on Linux/macOS; called from `ShowInputPanel` so the chat input actually receives focus when opened. | `CCP.Avalonia/AvatarTube/AvatarTubeWindow.ChatInput.cs`, `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` |
| Q | ✅ **Speech-bubble link parsing was a TODO stub.** Implemented `BuildLinkedInlines` to detect markdown `[text](url)` links and raw `http(s)://` URLs, render them as clickable `HyperlinkButton`s inside `InlineUIContainer`, and open them via the system browser. | `CCP.Avalonia/AvatarTube/AvatarTubeWindow.axaml.cs` |
| R | ✅ **Wallpaper override was a no-op stub.** Implemented `AvaloniaWallpaperProvider.SetWallpaper` with native Windows SPI, GNOME/gsettings + feh on Linux, and Finder/osascript on macOS. | `CCP.Avalonia/Platform/AvaloniaWallpaperProvider.cs` |
| S | ✅ **Window chrome / dark title bar was a no-op stub.** Implemented `AvaloniaWindowChrome.SetDarkTitleBar` with `DwmSetWindowAttribute` on Windows and wired `MainWindow` to apply it. | `CCP.Avalonia/Platform/AvaloniaWindowChrome.cs`, `CCP.Avalonia/Views/MainWindow.axaml.cs` |

> Method: `grep -rinE "TODO|stub|not ported|not wired|placeholder|NotImplemented|No-?op" CCP.Avalonia --include=*.cs`.
> Re-run it after each fix to watch the count drop; this list is a floor, not a ceiling — exercise every feature to
> find the gaps the markers don't name.

### Freshly found (2026-06-23) — add now

| # | Severity | Gap | Fix |
|---|---|---|---|
| L | ✅ **P0 data-loss — fixed.** `AvaloniaAppEnvironment.ApplicationDataPath` now returns `UserDataPath` (Local) on all platforms; Core services (`SessionLogService`, `SessionFileService`, `ModerationCounter`) now write to the legacy WPF folder. Added one-time `MigrateFromLegacyRoamingPath()` called at startup: moves Roaming files/folders to Local if a sentinel is absent, renaming on conflict. Verified settings load from `C:\Users\Micha\AppData\Local\ConditioningControlPanel\settings.json`; build + Core tests + smoke test pass. | Done |
| M | P2 | **WPF perf baseline partially captured** — startup + working set measured; effect frame rates still need tooling. WPF: startup ~4.21s, working set ~1,218MB (10s). Avalonia: startup ~2.55s, working set ~422MB (10s). Avalonia beats WPF on both metrics in this environment. Frame-rate capture (spiral/flash) blocked by lack of a built-in FPS counter; revisit with a lightweight overlay or ETW/PresentMon if needed. |
| N | ✅ **P0 runtime blockers from manual Chaos test — fixed.** (1) Circular `IProgressionService` DI cycle resolved by lazy service resolution in `AvaloniaProgressionService`. (2) Chaos run end crash when avatar tube was already closed fixed with `PlatformImpl != null` guard. (3) Surface/results popup buttons unresponsive / overlay staying open fixed: `ChaosOverlayWindow` now applies `WS_EX_TOOLWINDOW\|NOACTIVATE\|LAYERED` and toggles `WS_EX_TRANSPARENT` + Avalonia `IsHitTestVisible` so interactive cards receive input while ambient overlays stay click-through; `AvaloniaChaosService.EndRun` now reports `IsRunning=false` after showing results so dismissal can proceed; "fall deeper" now closes the old overlay before restarting. (4) Bubble/image/gif clickability improved by adding `WS_EX_LAYERED` to `AvaloniaBubbleWindow` and keeping the overlay click-through. Windows head smoke test passes (44 tabs, 0 findings). | Done |
| O | ✅ **P1 Webcam tab "unavailable" badge on Windows — fixed.** `WebcamFeatureControl` set `Capabilities` after `InitializeComponent()`, so `{Binding Capabilities.SupportsScreenCapture, ElementName=Root}` evaluated against a null source and never updated, showing the degraded badge even though desktop supports capture. Moved `Capabilities` (and `WebcamViewModel`) assignment before `InitializeComponent()` in `WebcamFeatureControl`, and moved `Capabilities` before `InitializeComponent()` in `VisualsFeatureControl`, `SystemFeatureControl`, and `LockCardFeatureControl` for the same reason. Smoke-test screenshot now shows the live webcam engine UI. | Done |

---

## Phase 0 — Cleanup (P0)

1. ✅ Remove dead packages (`SharpDX.*`, `OpenAI-DotNet`, `OllamaSharp`) — done; removed from all projects (0 references).
2. Verify and remove unused `MahApps.Metro` / `IconPacks` references.
3. ⚠️ **Corrected — do NOT remove `Microsoft.WindowsAppSDK`.** It is a required transitive of LibVLCSharp; pin it with `ExcludeAssets="all" PrivateAssets="all"` to avoid a WebView2 `NU1605` downgrade (see plan §5.1).
4. Delete `CopyLibVLCAfterPublish` and `IncludeWebView2LoaderInPublish` from shared project; move to Windows head later.
5. Add platform analyzers and remove `CA1416` `NoWarn` once cross-platform seams are in place.
6. Document feature matrix (portable vs. Windows-only) in `docs/platform-feature-matrix.md`.

---

## Phase 1 — Carve Out `CCP.Core` (P0)

1. Create solution file wiring `ConditioningControlPanel`, `CCP.Core`, and `CCP.WindowsOnly`.
2. Audit current extraction status (file-by-file map of models/services).
3. Move remaining root POCOs into `CCP.Core/Models/` and unify namespaces.
4. Finalize platform seam interfaces in `CCP.Core/Platform/` (add `IThumbnailProvider`, `IImageDecoder`, `IEffectSink`, `IAppEnvironment`).
5. Move AI orchestration into `CCP.Core/Services/AIService/`.
6. Move auth/networking transport into `CCP.Core/Services/Account/` and `CCP.Core/Services/Auth/`.
7. Move mod system logic into `CCP.Core/Services/Mod/`.
8. Move session/gamification engine into `CCP.Core/Services/Session/` and `CCP.Core/Services/Progression/`.
9. Replace `DispatcherTimer` with `IScheduler` in Core.
10. Replace `MessageBox.Show` with `IDialogService` in Core.
11. Replace DPAPI with `ISecretStore` in Core.
12. Replace `pack://` URI loading with `IAssetLoader` in Core.
13. Wire legacy WPF app to `CCP.Core` + `CCP.WindowsOnly` via DI.
14. Migrate legacy `App` static service accessors to Core abstractions.
15. Remove duplicate portable code from WPF head.
16. Create `tests/CCP.Core.Tests` project with unit tests.
17. Validate Core builds on Linux/macOS CI.
18. WPF regression smoke test.

---

## Phase 2 — Prove Core Off-Windows (P0)

1. Run `dotnet build CCP.Core/CCP.Core.csproj` on Linux/macOS CI.
2. Run Core unit/integration tests on Linux/macOS.
3. Ensure DPAPI, screen, audio, and UI dispatch seams are the only Windows leaks.

---

## Phase 3 — Avalonia Solution Skeleton (✅ mostly done)

1. ✅ Create `CCP.Avalonia` (shared UI).
2. ✅ Create `CCP.Avalonia.Desktop`.
3. ✅ Create `CCP.Avalonia.Desktop.Windows`.
4. ✅ Create `CCP.Avalonia.Desktop.Linux`.
5. ✅ Create `CCP.Avalonia.Desktop.macOS`.
6. ✅ Create `CCP.Avalonia.Android`.
7. ✅ Wire solution and packages.

---

## Phase 4 — Migrate WPF XAML/UI to Avalonia (P1, largest phase)

### Theme & Assets

1. Migrate app-level theme ResourceDictionaries to Avalonia (`Resources/Theme/*.xaml` → `CCP.Avalonia/Resources/Theme/*.axaml`).
2. Remove all remaining `pack://` URI usage; use `avares://CCP.Avalonia/Assets/...`.
3. ✅ Port WPF localization markup extension to Avalonia (`{loc:Str Key}`) — `CCP.Avalonia/Localization/LocExtension.cs` created and wired in `App.axaml`.

### Main Shell & Dashboard

4. ✅ Port Settings/Dashboard tab view (velvet mosaic grid + right panel) — **DONE**.
5. Finish `MainWindow.axaml` code-behind parity (drag-drop, resize grips, window state, XP bar, banner). Theme switching verified for all five mods; `FluentTheme` palette `Accent` is updated at runtime so default-styled dashboard controls (sliders, toggles, checkboxes, radio buttons) follow the active mod accent. Update pill shows localized celebratory version text on a filled accent background.
6. Implement Avalonia custom window chrome and resizing across desktops.
7. ✅ Implement `IBrowserHost` strategy — `WebView2BrowserHost` embedded via `NativeControlHost` on Windows; system-browser fallback on Linux/macOS/mobile.

### Views, Dialogs, Feature Controls

7. Port remaining WPF-only dialogs to Avalonia (`Dialogs/*.xaml` without `.axaml` counterparts).
8. Replace `MessageBoxStub` with `IDialogService` across Avalonia windows.
9. Complete `FeatureSettingsPopup`.
10. Port feature controls code-behind to Avalonia (file pickers, audio, dispatch seams).
11. ✅ Complete AvatarTube window — window now initializes at startup when `AvatarEnabled`, scales before first show to avoid `SizeToContent` blow-up, and repositions the main window so the attached tube stays on-screen.
12. Complete Chaos overlay windows and animations.
13. Port WPF-only utility windows (Splash, Tutorial, Webcam, Mantra, SessionEditor).
14. Port `Lab/GazeMinigame` window.
15. Convert WPF helpers/converters to Avalonia.
16. Implement cross-platform drag-drop for session import.
17. Add design-time data contexts and accessibility labels to all views.
18. Build and smoke-test Avalonia desktop app.
19. Run UI parity matrix.
20. Clean legacy WPF references from `CCP.Avalonia.csproj`.

---

## Phase 5 — Replace Media & Audio Stack (P1)

1. Audit and extend audio abstraction seams (`IAudioPlayer`, `IAudioDeviceService`, `ISystemAudioDucker`).
2. Wire short-SFX call sites to `IAudioPlayer`.
3. Port system-audio ducking to seam implementations (Windows full, Linux/macOS graceful).
4. Port Deeper long-form audio player (`EnhancementAudioPlayer`).
5. Replace NAudio `MediaFoundationReader` in audio analysis pipeline.
6. Port mandatory `VideoService` — core playback.
7. Port mandatory `VideoService` — attention checks and strict mode.
8. Implement mobile `IVideoSurface` for Android.
9. Verify memory-render video surfaces (`AvaloniaInlineLoopVideo`, dual-monitor mirror).
10. Finalize GIF/SVG migration (`AvaloniaAnimatedGif`, `Svg.Skia`).
11. Lock down native LibVLC packaging and discovery per RID.
12. Run Phase 5 build/test matrix.

---

## Phase 6 — Replace OS-Shell Features (P1)

1. Finalize `ITrayIcon` and migrate desktop tray behavior.
2. Port global hotkeys and panic key to `IHotkeyProvider`/`IInputHook` seams.
3. Implement `IWallpaperProvider` wiring and port wallpaper override.
4. Implement `IBrowserHost` per-platform and wire browser consumers.
5. Port WebView2 parity features into `WebView2BrowserHost`.
6. Implement `IWindowChrome` per-platform title-bar handling.
7. Provide `IOverlaySurface` implementations for desktop overlay windows.
8. Port overlay consumers to `IOverlaySurface`.
9. Define `IThumbnailProvider` and replace `ShellThumbnailHelper`.
10. Implement `IFrameSource` for screen capture and wire webcam/gaze windows.
11. Update `IPlatformCapabilities` and gate OS-shell UI options.
12. Register all Phase 6 implementations in the correct head DI containers.
13. Add Phase 6 smoke tests and document platform matrix.

---

## Phase 7 — Build & Publish Pipeline (✅ done)

1. ✅ Define RuntimeIdentifiers per head.
2. ✅ Desktop single-file + self-contained publish.
3. ✅ Android standard build with trimming roots.
4. ✅ CI matrix builds.
5. Code signing and notarization setup (macOS).
6. Android keystore and app bundle publishing.

---

## Phase 8 — Mobile Feature Gating & Adaptation (✅ done structurally)

1. ✅ Hide overlay effects on Android.
2. ✅ Hide global hooks/system-key suppression on Android.
3. ✅ Hide system tray on Android.
4. ✅ Hide wallpaper override on Android.
5. ✅ Hide GDI/desktop capture on Android.
6. ✅ Hide screen OCR on Android.
7. ✅ Hide multi-monitor video mirroring on Android.
8. Design mobile-first shell (tabs/navigation).
9. Touch-optimized controls.
10. Native camera + LibVLC for Lab/webcam features.
11. Background audio/execution restrictions on Android.
12. Address Google Play content policies.

---

## Localization Parity (P1)

1. Audit hard-coded strings and define key naming convention.
2. Move language assets into `CCP.Core` and wire loading.
3. Create Avalonia `{loc:Str}` markup extension.
4. Localize main shell and dashboard chrome.
5. Localize system/config tab views.
6. Localize content & lab tab views.
7. Localize account & catalogue tab views.
8. Localize primary feature controls.
9. Localize secondary feature controls and shared feature UI.
10. Localize account & onboarding dialogs.
11. Localize editor & catalogue dialogs.
12. Localize roadmap dialogs.
13. Localize session, quiz, mod-creator & haptics windows.
14. Localize webcam & tool windows.
15. Localize popups, splash & recap surfaces.
16. Localize Chaos hub, HUD & intro.
17. Localize Chaos overlay effects.
18. Localize Deeper editor & player.
19. Localize AvatarTube.
20. Externalize fallback strings in Core models.
21. Externalize user-facing messages in Core services.
22. Externalize dynamic strings in shell/system ViewModels.
23. Externalize dynamic strings in content/social ViewModels.
24. Localize tray/platform strings.
25. Add localized accessibility labels.
26. Add localization QA gate and tests.
27. Sync skeleton translations and update docs.
28. (Optional) Legacy WPF parity shim.

---

## Stability / Performance / Maintainability (P2)

1. Replace static service locator with scoped DI container.
2. Implement `IAsyncDisposable` for long-running services.
3. Move service initialization off UI thread with async startup + splash progress.
4. Pool overlay windows instead of creating/disposing per effect.
5. Use Avalonia/Skia `CompositionCustomVisual` for particle/spiral effects.
6. Pool audio players; use LibVLC for short SFX.
7. Replace `System.Drawing` with SkiaSharp / ImageSharp.
8. Move hard-coded paths/constants to `appsettings.json` + options pattern.
9. Add structured logging with correlation IDs.
10. Add feature flags for platform/experimental features.

---

## Settings & Data Backward Compatibility (P2)

1. Read existing WPF settings format on first launch.
2. Version settings schema and add `ISettingsMigration` classes.
3. Back up old settings before migration.
4. Preserve user data path across updates.

---

## Accessibility, Telemetry, Crash Reporting (P2)

1. Set `AutomationProperties.Name`/`HelpText` on every interactive control.
2. Ensure keyboard navigation (Tab order, access keys).
3. Respect system high-contrast and reduce-motion settings.
4. Replace `dbghelp.dll` minidumps with cross-platform crash handler.
5. Add opt-in telemetry for startup time, feature usage, crash-free sessions.
6. Ensure no PII in logs.

---

## Code Signing & Distribution (P3)

1. Windows code signing certificate for EXE + installer.
2. Linux GPG sign AppImage/package.
3. macOS Apple Developer ID notarization + staple.
4. Android upload/signing keys + Play Console.
5. Provide SHA256 checksums for artifacts.
6. Host native dependencies (Linux libvlc, macOS ARM64 libvlc) on CDN/release assets.

---

## Current Sprint Focus

§19.3 main-merge backlog is complete: `AppSettings`, `Fredoka`, `UpdateService`, service-deltas, `ChaosCrashSentinel`, `ChaosBoonColors`, shared-host overlays, `ChaosSkiaFxOverlay`, and the full `BubbleService` overhaul (ambient + chaos variants + field hazards + shared-host + global mouse hook) are ported. §19.4 project-reference collapse is **done**: WPF now references `CCP.Core`, the WPF `Models/` duplicate folder is deleted, and shared model types (`CatalogueEntry`, `HapticProviderType`, `XPSource`, `PackFileEntry`) are sourced from Core.

Chaos overlay integration is complete and build/test green: cursor-glow/banner/field-FX animations, `AvaloniaChaosWindowZ` topmost re-assert, full countdown → descent → spawn → scoring → draft → results lifecycle, meta persistence, `RevealService`, boon runtime, focus economy, active toys, lessons, narrative director, and happy-path scripting are in `main`.

AvatarTube behavior restoration is complete (speech/AI chat/Circe emotes/reactions/windowing/fullscreen detection/context-menu toggles/emotive portrait system).

Deeper runtime engine, dispatcher, and host are now in `CCP.Core`; the Avalonia player binds the engine via `AvaloniaLibVlcTimeSource`. A functional `DeeperEditorWindow` (metadata, regions/rules/haptics lists, save, preview, visual timeline with drag-create/resize/rubber-band, custom haptic curve editor, browser preview, and `AudioWaveformCache`) is at parity. `GazePickerWindow` is ported and wired for gaze-target/avoid rect authoring. The last WPF-only dialog, `LocalAiSetupWizard`, is now ported to Avalonia with its `OllamaSetupService` dependency moved to Core.

Recently completed:
- Ported `HelpContentService` to `CCP.Core/Services/Help` as the single source of truth for help/tutorial content. The WPF `Services/HelpContentService` is now a thin facade forwarding to Core. Wired Avalonia `SessionEditorWindow.BtnHelp_Click` to open `HelpVideoWindow` for the `SessionEditor` topic (with `TutorialOverlay` fallback when no clip is available), matching WPF behavior.
- Wired Avalonia `MantraWindow` audio through `IAudioPlayer`: background drone (90Hz + 180Hz sine loop) and typing/completion tones are generated as 16-bit mono WAV files and played via LibVLC, replacing the NAudio `SignalGenerator`/`WaveOut` stubs.
- Implemented content-pack virtual folders in `AssetsTabViewModel`: active packs now appear under a "📦 Content Packs" tree node with `images`/`videos` subfolders, files load into the thumbnail grid, check-state changes update `DisabledAssetPaths` using `pack:{id}/{name}` keys, and asset counts include active pack files.

Next priorities (pick one lane at a time):

1. **Phase 4 UI parity smoke-test / cleanup** — run the Avalonia desktop app, fix first-chance issues, replace `MessageBoxStub` with `IDialogService`, and fill `docs/avalonia-ui-parity-matrix.md` gaps.
2. **Feature controls code-behind** — wire file pickers, audio, dispatch seams, and complete `FeatureSettingsPopup` parity.
3. **Deeper player/editor integration live smoke test** — verify opening an enhancement from the hub plays correctly and editor edits persist.
