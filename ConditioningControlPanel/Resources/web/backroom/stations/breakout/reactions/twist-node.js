/* ============================================================================
 * stations/breakout/reactions/twist-node.js - the twist's event visuals (reactions.js registry: name -> (fx, d, snapshot) => void).
 * Twist: Node (The Hive). wire bricks powered by the core are armoured; cut a branch and it goes pale and soft.
 * SCAFFOLD STUB. The node lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* Cosmetic only. Honour fx.reduced (no shake, no bursts) and fx.colour. */
export const REACTIONS = {};

export default REACTIONS;
