/* ============================================================================
 * stations/breakout/reactions/twist-justone.js - the twist's event visuals (reactions.js registry: name -> (fx, d, snapshot) => void).
 * Twist: Just One (The Ward). a treat brick drops a treat: catching it buys a fireball and a longer comedown.
 * SCAFFOLD STUB. The justone lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* Cosmetic only. Honour fx.reduced (no shake, no bursts) and fx.colour. */
export const REACTIONS = {};

export default REACTIONS;
