/* ============================================================================
 * stations/breakout/reactions/twist-mirror.js - the twist's event visuals (reactions.js registry: name -> (fx, d, snapshot) => void).
 * Twist: Mirror (The Wardrobe). breaking a brick breaks its twin at column 15 - col, steel twin included.
 * SCAFFOLD STUB. The mirror lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* Cosmetic only. Honour fx.reduced (no shake, no bursts) and fx.colour. */
export const REACTIONS = {};

export default REACTIONS;
