/* ============================================================================
 * stations/breakout/twists/mirror.js - the pure sim half of the twist.
 * Twist: Mirror (The Wardrobe). breaking a brick breaks its twin at column 15 - col, steel twin included.
 * SCAFFOLD STUB. The mirror lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

export const id = 'mirror';

/** Called once after the authored wall is built. */
export function build(g, ctx) { /* the mirror lane fills this in */ }

export default { id, build };
