/* ============================================================================
 * stations/breakout/twists/node.js - the pure sim half of the twist.
 * Twist: Node (The Hive). wire bricks powered by the core are armoured; cut a branch and it goes pale and soft.
 * SCAFFOLD STUB. The node lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

export const id = 'node';

/** Called once after the authored wall is built. */
export function build(g, ctx) { /* the node lane fills this in */ }

export default { id, build };
