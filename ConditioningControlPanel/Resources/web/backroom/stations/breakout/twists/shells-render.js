/* ============================================================================
 * stations/breakout/twists/shells-render.js - the twist's own drawing.
 * Twist: Shells (act 4). Pale ghost shells over the field, under the HUD.
 * SCAFFOLD STUB: the act 4 lane fills it. Reduced motion: the shells hold their
 * pose and stop shimmering. How many touches a shell has taken still reads.
 * ==========================================================================*/

/** Field coordinates, over everything but the HUD. */
export function over(/* x, snap, t */) { /* act 4 lane */ }

export default { over };
