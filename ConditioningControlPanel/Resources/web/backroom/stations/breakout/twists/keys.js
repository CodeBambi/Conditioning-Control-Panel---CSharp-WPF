/* ============================================================================
 * stations/breakout/twists/keys.js - the pure sim half of the twist.
 * Twist: Keys and Gates (Lock and Key). a key brick opens every gate brick of its colour; the cracked key primes the clay.
 * SCAFFOLD STUB. The keys lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

export const id = 'keys';

/** Called once after the authored wall is built. */
export function build(g, ctx) { /* the keys lane fills this in */ }

export default { id, build };
