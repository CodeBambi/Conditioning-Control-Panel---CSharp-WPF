/* ============================================================================
 * stations/breakout/twists/justone-render.js - the twist's own drawing.
 * Twist: Just One (The Ward). a treat brick drops a treat: catching it buys a fireball and a longer comedown.
 * SCAFFOLD STUB. The justone lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/** Drawn for a brick the twist flags, inside the brick's own transform (origin = its centre). */
export function brick(ctx2d, br, snap, t) { /* the justone lane fills this in */ }

/** Drawn under the bricks, in field coordinates. */
export function under(ctx2d, snap, t) { /* the justone lane fills this in */ }

/** Drawn over everything but the HUD, in field coordinates. */
export function over(ctx2d, snap, t) { /* the justone lane fills this in */ }

export default { brick, under, over };
