/* ============================================================================
 * stations/breakout/twists/justone.js - the pure sim half of the twist.
 * Twist: Just One (The Ward). a treat brick drops a treat: catching it buys a fireball and a longer comedown.
 * SCAFFOLD STUB. The justone lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

export const id = 'justone';

/** Called once after the authored wall is built. */
export function build(g, ctx) { /* the justone lane fills this in */ }

export default { id, build };
