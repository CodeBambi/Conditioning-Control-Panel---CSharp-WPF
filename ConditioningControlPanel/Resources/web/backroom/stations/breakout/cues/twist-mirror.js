/* ============================================================================
 * stations/breakout/cues/twist-mirror.js - the twist's sounds (cues.js registry: name -> (synth, data) => void).
 * Twist: Mirror (The Wardrobe). breaking a brick breaks its twin at column 15 - col, steel twin included.
 * SCAFFOLD STUB. The mirror lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* House rules (AGENTS.md): pitched cues come from pentatonic() + ROOT_HZ and land
 * on quantise(); a physical impact plays at synth.now and is never quantised. */
export const CUES = {};

export default CUES;
