/* ============================================================================
 * stations/breakout/cues/twist-keys.js - the twist's sounds (cues.js registry: name -> (synth, data) => void).
 * Twist: Keys and Gates (Lock and Key). a key brick opens every gate brick of its colour; the cracked key primes the clay.
 * SCAFFOLD STUB. The keys lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* House rules (AGENTS.md): pitched cues come from pentatonic() + ROOT_HZ and land
 * on quantise(); a physical impact plays at synth.now and is never quantised. */
export const CUES = {};

export default CUES;
