/* ============================================================================
 * stations/breakout/cues/twist-shells.js - the sound of an old self.
 * Twist: Shells (act 4). SCAFFOLD STUB: the act 4 lane fills it.
 * A touch is a body and plays now; the pop is the release and is the only
 * pitched one, so it lands on quantise(). Nothing falls (AGENTS.md rule 6).
 * ==========================================================================*/
export const CUES = {
  // shellTouch, shellPop
};
export default CUES;
