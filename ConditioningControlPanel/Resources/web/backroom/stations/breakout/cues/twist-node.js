/* ============================================================================
 * stations/breakout/cues/twist-node.js - the twist's sounds (cues.js registry: name -> (synth, data) => void).
 * Twist: Node (The Hive). wire bricks powered by the core are armoured; cut a branch and it goes pale and soft.
 * SCAFFOLD STUB. The node lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* House rules (AGENTS.md): pitched cues come from pentatonic() + ROOT_HZ and land
 * on quantise(); a physical impact plays at synth.now and is never quantised. */
export const CUES = {};

export default CUES;
