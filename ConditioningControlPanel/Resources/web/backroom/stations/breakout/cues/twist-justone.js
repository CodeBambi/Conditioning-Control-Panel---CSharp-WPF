/* ============================================================================
 * stations/breakout/cues/twist-justone.js - the twist's sounds (cues.js registry: name -> (synth, data) => void).
 * Twist: Just One (The Ward). a treat brick drops a treat: catching it buys a fireball and a longer comedown.
 * SCAFFOLD STUB. The justone lane owns this file. See twists/CONTRACT.md.
 * ==========================================================================*/

/* House rules (AGENTS.md): pitched cues come from pentatonic() + ROOT_HZ and land
 * on quantise(); a physical impact plays at synth.now and is never quantised. */
export const CUES = {};

export default CUES;
