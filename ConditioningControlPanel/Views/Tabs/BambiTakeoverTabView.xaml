<UserControl x:Class="ConditioningControlPanel.Views.Tabs.BambiTakeoverTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:ConditioningControlPanel.Helpers"
             xmlns:fx="clr-namespace:ConditioningControlPanel.Controls"
             xmlns:feat="clr-namespace:ConditioningControlPanel.Features"
             xmlns:hover="clr-namespace:ConditioningControlPanel.Behaviors"
             xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Velvet Kit 2 · round 4. Takeover's hue is the house hot pink: this is the most
         "possessed" feature on the panel, so it gets the brand colour rather than a section
         tint. LITERAL colours, and LOCAL to this file - a fresh cross-dictionary
         StaticResource inside Resources/Theme/* trips WPF's deferred-BAML
         EndOfStreamException and takes every styled control down with it. Slider values and
         the mod-tinted accents below deliberately stay on {DynamicResource PinkBrush}, which
         re-tints per mod; these three are the page's own chrome. -->
    <UserControl.Resources>
        <SolidColorBrush x:Key="TabHueBrush" Color="#FF69B4"/>
        <SolidColorBrush x:Key="TabHueBorderBrush" Color="#40FF69B4"/>
        <LinearGradientBrush x:Key="TabHueWashBrush" StartPoint="0,0" EndPoint="0.9,0.7">
            <GradientStop Color="#14FF69B4" Offset="0"/>
            <GradientStop Color="#00000000" Offset="0.55"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <!-- Root grid exists so the premium gate can be the LAST sibling and therefore cover the
             whole page (hero + both columns), instead of the old overlay that only covered the
             settings sub-column and left the Start button of a locked feature looking live. -->
        <Grid Margin="36,28,36,28" MaxWidth="1180" HorizontalAlignment="Center">
            <StackPanel>

                <!-- ============ HERO ============
                     Replaces the 44px emoji + 26px title header row. The old header carried no
                     control at all and the master control (Start/Stop) sat two cards down the
                     page; it lives in the hero pill now, where every other treated feature page
                     keeps its master switch. x:Name/handler/ToolTip/loc content unchanged -
                     MainWindow.Autonomy.cs rewrites Content + Foreground on every state flip and
                     MainWindow.xaml.cs re-formats the ToolTip per mod. -->
                <Border Height="72" CornerRadius="16" Background="{DynamicResource SurfaceBgBrush}"
                        BorderBrush="{StaticResource TabHueBorderBrush}" BorderThickness="2" Margin="0,0,0,10">
                    <Grid>
                        <Border CornerRadius="0,16,16,0" Width="240" HorizontalAlignment="Right">
                            <Border.Background>
                                <ImageBrush x:Name="TakeoverHeroArtBrush"
                                            Stretch="UniformToFill" AlignmentX="Right" Opacity="0.9">
                                    <ImageBrush.ImageSource>
                                        <BitmapImage UriSource="pack://application:,,,/Resources/features/takeover.png" DecodePixelWidth="480"/>
                                    </ImageBrush.ImageSource>
                                </ImageBrush>
                            </Border.Background>
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#00000000" Offset="0"/>
                                    <GradientStop Color="#E6000000" Offset="0.55"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                        </Border>
                        <StackPanel VerticalAlignment="Center" Margin="18,0,0,0">
                            <TextBlock x:Name="TxtBambiTakeoverHeader" x:FieldModifier="internal" Text="{loc:Str tab_takeover}"
                                       Foreground="{StaticResource TextLightBrush}" FontSize="17" FontWeight="Bold"/>
                            <TextBlock Text="{loc:Str exclusives_tag_bambitakeover}" FontSize="11.5"
                                       Foreground="{StaticResource TextMutedBrush}" Margin="0,1,0,0"/>
                        </StackPanel>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0" CornerRadius="99"
                                Background="#8C131320" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Padding="6,4">
                            <Button x:Name="BtnAutonomyStartStop" x:FieldModifier="internal" Content="{loc:Str btn_start_2}"
                                    Background="#2A1A3A" Foreground="#90EE90" FontWeight="Bold" FontSize="14" Padding="20,7"
                                    BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"
                                    Click="BtnAutonomyStartStop_Click" ToolTip="{loc:Str tooltip_start_stop_takeover}">
                                <Button.Resources>
                                    <Style TargetType="Border"><Setter Property="CornerRadius" Value="99"/></Style>
                                </Button.Resources>
                            </Button>
                        </Border>
                    </Grid>
                </Border>

                <!-- ============ ROUND-3 CAPPED COLUMN + SIDE RAIL ============
                     Column 0 is capped at 780 so a slider stops being a ~1100px runway; the width
                     that buys back becomes the art plate and the "How it works" guide, which used
                     to be a bare `*` column that never got out of the way. Below 900px (roughly
                     the panel at MinWidth 1131) the rail collapses and the settings retake the
                     full width: Features/AdaptiveSideArt.cs. -->
                <Grid feat:AdaptiveSideArt.CollapseBelow="900">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" MaxWidth="780"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">

                        <!-- Description card (always visible — even to free users) -->
                        <Border Style="{StaticResource SettingCardStyle}">
                            <Grid>
                                <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                        Background="{StaticResource TabHueBrush}"/>
                                <Grid Margin="16,10,16,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="14"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- ImgBambiTakeoverDesc is the mod art hook: MainWindow.xaml.cs
                                         swaps its Source between "bambi takeover.png" and
                                         "takeover.png" per active mod, so this stays a real, named
                                         Image rather than becoming an ImageBrush. -->
                                    <Border Grid.Column="0" Width="104" Height="104" CornerRadius="10" ClipToBounds="True"
                                            BorderBrush="{StaticResource TabHueBorderBrush}" BorderThickness="2" VerticalAlignment="Top">
                                        <Image x:Name="ImgBambiTakeoverDesc" x:FieldModifier="internal"
                                               Source="pack://application:,,,/Resources/features/bambi takeover.png"
                                               Stretch="UniformToFill"
                                               hover:HoverPop.IsEnabled="True"/>
                                    </Border>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_what_is_this}" Foreground="{StaticResource TabHueBrush}"
                                                   FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <TextBlock Text="{loc:Str desc_takeover}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"
                                                   TextWrapping="Wrap" Margin="0,0,0,8" LineHeight="19"/>
                                        <TextBlock Text="{loc:Str desc_takeover_bullets}"
                                                   Foreground="{StaticResource TextMutedBrush}" FontSize="12"
                                                   TextWrapping="Wrap" LineHeight="18"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>

                        <StackPanel x:Name="BambiTakeoverControls">
                            <!-- Lifted from old Bambi Expander — every x:Name preserved -->
                            <!-- Locked State (legacy — kept hidden, replaced by gating overlay below) -->
                            <StackPanel x:Name="AutonomyLocked" x:FieldModifier="internal" Visibility="Collapsed">
                                <Grid>
                                    <helpers:EmojiTextBlock x:Name="TxtTakeoverLocked" x:FieldModifier="internal" Text="{loc:Str label_takeover}" Foreground="{DynamicResource TextDimBrush}" FontWeight="Bold" FontSize="13"/>
                                    <helpers:EmojiTextBlock x:Name="TxtAutonomyLockStatus" x:FieldModifier="internal" Text="{loc:Str label_patreon_only}" Foreground="{DynamicResource SecondaryBrush}" HorizontalAlignment="Right" FontSize="11"/>
                                </Grid>
                                <TextBlock x:Name="TxtAutonomyLockMessage" x:FieldModifier="internal" Text="{loc:Str label_support_on_patreon_to_unlock}" Foreground="{DynamicResource PinkBrush}" FontSize="11" Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State — always visible now; gating handled by overlay -->
                            <StackPanel x:Name="AutonomyUnlocked" x:FieldModifier="internal" Visibility="Visible">
                                <StackPanel x:Name="AutonomySettings">

                                    <!-- ============ STATE CARD — unmistakable ON/OFF ============ -->
                                    <Border Style="{StaticResource SettingCardStyle}">
                                        <Grid>
                                            <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                            <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                    Background="{StaticResource TabHueBrush}"/>
                                            <Grid Margin="16,10,16,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="18"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- The orb. Was two stacked Ellipses plus a spiral emoji standing in for
                                                     one; it is now a real Skia surface (Controls/TakeoverOrb.cs) that
                                                     draws its own layered glow, rotating inner swirl and orbiting wisps,
                                                     re-tints per mod and parks whenever the tab is not on screen.
                                                     112 rather than the old 92: the wisp ring orbits out to ~0.46 of the
                                                     shorter edge and WPF clips overflow silently, so the slot is sized
                                                     for the FX at peak rather than for the old flat disc. -->
                                                <fx:TakeoverOrb x:Name="TakeoverOrbFx" x:FieldModifier="internal"
                                                                Grid.Column="0" Width="112" Height="112" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                    <!-- #8888A0 is not an ad-hoc grey: it is the exact resting colour
                                                         MainWindow.TakeoverUi.cs re-applies (MutedColor) the moment the
                                                         page wakes, so the authored value has to match it. -->
                                                    <TextBlock x:Name="TxtTakeoverStatus" x:FieldModifier="internal" Text="○ DORMANT"
                                                               Foreground="#8888A0" FontSize="18" FontWeight="Bold"/>
                                                    <TextBlock x:Name="TxtTakeoverStatusSub" x:FieldModifier="internal" Text="She's not watching right now."
                                                               Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <!-- ============ LIVE VOICE PANEL — visible only during a prompt ============
                                         Deliberately NOT SettingCardStyle: it is a live readout, not a settings
                                         block, and the darker plate is what makes it read as "she is listening
                                         right now" when it flips visible mid-page. -->
                                    <Border x:Name="VoiceLivePanel" x:FieldModifier="internal" Visibility="Collapsed"
                                            Background="#1F1430" BorderBrush="{StaticResource TabHueBorderBrush}" BorderThickness="2"
                                            CornerRadius="12" Padding="16,12" Margin="0,0,0,10">
                                        <StackPanel>
                                            <TextBlock Text="SAY IT FOR ME" Foreground="{StaticResource TabHueBrush}" FontSize="10" FontWeight="Bold"/>
                                            <TextBlock x:Name="TxtVoicePromptPhrase" x:FieldModifier="internal" Text="“ … ”"
                                                       Foreground="{StaticResource TextLightBrush}" FontSize="22" FontWeight="Bold" Margin="0,6,0,10" TextAlignment="Center"/>
                                            <Border Height="14" Background="#2A2440" CornerRadius="7">
                                                <Border x:Name="VoiceLevelFill" x:FieldModifier="internal" HorizontalAlignment="Stretch"
                                                        CornerRadius="7" RenderTransformOrigin="0,0.5">
                                                    <Border.RenderTransform>
                                                        <ScaleTransform ScaleX="0"/>
                                                    </Border.RenderTransform>
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#FF69B4" Offset="0"/>
                                                            <GradientStop Color="#9B59FF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Border>
                                            <Grid Margin="0,10,0,0">
                                                <TextBlock x:Name="TxtVoiceHeard" x:FieldModifier="internal" Text="I heard: …"
                                                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" VerticalAlignment="Center"/>
                                                <Border x:Name="VoiceVerdictChip" x:FieldModifier="internal" Visibility="Collapsed"
                                                        HorizontalAlignment="Right" CornerRadius="10" Padding="10,3" Background="#2E7D32">
                                                    <TextBlock x:Name="TxtVoiceVerdict" x:FieldModifier="internal" Text="✓ MATCHED"
                                                               Foreground="White" FontSize="12" FontWeight="Bold"/>
                                                </Border>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- ============ SUBSECTION 1: CADENCE ============
                                         Four 2x2 slider blocks (label over slider, each in its own
                                         column) became four 36px rows in one card: same four
                                         sliders, same value TextBlocks, ~120px shorter, and the
                                         duplicated descriptions now live only in the row ToolTips. -->
                                    <Border Style="{StaticResource SettingCardStyle}">
                                        <Grid>
                                            <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                            <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                    Background="{StaticResource TabHueBrush}"/>
                                            <StackPanel Margin="16,8,16,8">
                                                <TextBlock Text="{loc:Str setting_interval}" Foreground="{StaticResource TabHueBrush}"
                                                           FontSize="10" FontWeight="Bold" Margin="0,2,0,4"/>

                                                <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_interval}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str setting_interval}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider Grid.Column="1" x:Name="SliderAutonomyInterval" x:FieldModifier="internal"
                                                            Minimum="30" Maximum="300" Value="60" VerticalAlignment="Center" Margin="14,0"
                                                            ValueChanged="SliderAutonomyInterval_Changed"/>
                                                    <TextBlock Grid.Column="2" x:Name="TxtAutonomyInterval" x:FieldModifier="internal" Text="{loc:Str label_60s}"
                                                               HorizontalAlignment="Right" VerticalAlignment="Center"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_cooldown}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_cooldown}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider Grid.Column="1" x:Name="SliderAutonomyCooldown" x:FieldModifier="internal"
                                                            Minimum="10" Maximum="300" Value="30" VerticalAlignment="Center" Margin="14,0"
                                                            ValueChanged="SliderAutonomyCooldown_Changed"/>
                                                    <TextBlock Grid.Column="2" x:Name="TxtAutonomyCooldown" x:FieldModifier="internal" Text="{loc:Str label_30s}"
                                                               HorizontalAlignment="Right" VerticalAlignment="Center"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_intensity}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str setting_intensity}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider Grid.Column="1" x:Name="SliderAutonomyIntensity" x:FieldModifier="internal"
                                                            Minimum="1" Maximum="10" Value="5" VerticalAlignment="Center" Margin="14,0"
                                                            ValueChanged="SliderAutonomyIntensity_Changed"/>
                                                    <TextBlock Grid.Column="2" x:Name="TxtAutonomyIntensity" x:FieldModifier="internal" Text="5"
                                                               HorizontalAlignment="Right" VerticalAlignment="Center"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_announce}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_announce}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider Grid.Column="1" x:Name="SliderAutonomyAnnounce" x:FieldModifier="internal"
                                                            Minimum="0" Maximum="100" Value="50" VerticalAlignment="Center" Margin="14,0"
                                                            ValueChanged="SliderAutonomyAnnounce_Changed"/>
                                                    <TextBlock Grid.Column="2" x:Name="TxtAutonomyAnnounce" x:FieldModifier="internal" Text="50%"
                                                               HorizontalAlignment="Right" VerticalAlignment="Center"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- ============ SUBSECTION 2: TRIGGERS ============ -->
                                    <Border Style="{StaticResource SettingCardStyle}">
                                        <Grid>
                                            <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                            <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                    Background="{StaticResource TabHueBrush}"/>
                                            <StackPanel Margin="16,8,16,8">
                                                <TextBlock Text="{loc:Str label_triggers}" Foreground="{StaticResource TabHueBrush}"
                                                           FontSize="10" FontWeight="Bold" Margin="0,2,0,4"/>

                                                <Grid Height="34" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_idle}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_idle}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1" x:Name="ChkAutonomyIdle" x:FieldModifier="internal"
                                                              Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                                              Checked="ChkAutonomyIdle_Changed" Unchecked="ChkAutonomyIdle_Changed"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="34" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_random}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_random_2}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1" x:Name="ChkAutonomyRandom" x:FieldModifier="internal"
                                                              Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                                              Checked="ChkAutonomyRandom_Changed" Unchecked="ChkAutonomyRandom_Changed"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="34" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_time_aware}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_time_aware}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1" x:Name="ChkAutonomyTimeAware" x:FieldModifier="internal"
                                                              Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                                              Checked="ChkAutonomyTimeAware_Changed" Unchecked="ChkAutonomyTimeAware_Changed"/>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- ============ SUBSECTION 3: BEHAVIORS (folded into an expander) ============ -->
                                    <Expander IsExpanded="False" Margin="0,0,0,10" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold">
                                        <Expander.Header>
                                            <TextBlock Text="Advanced — what she's allowed to do" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                        </Expander.Header>
                                        <Border Style="{StaticResource SettingCardStyle}" Margin="0,8,0,0">
                                            <Grid>
                                                <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                                <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                        Background="{StaticResource TabHueBrush}"/>
                                                <StackPanel Margin="16,8,16,10">
                                                    <Grid Margin="0,2,0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="16"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{loc:Str label_basic}" Foreground="{StaticResource TabHueBrush}" FontWeight="Bold" FontSize="10"/>
                                                        <TextBlock Grid.Column="2" Text="{loc:Str label_advanced_2}" Foreground="{StaticResource TabHueBrush}" FontWeight="Bold" FontSize="10"/>
                                                    </Grid>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="16"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <!-- Basic column -->
                                                        <StackPanel Grid.Column="0">
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_flash}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoFlash" x:FieldModifier="internal" Text="{loc:Str tab_flashes}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyFlash" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_video}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoVideo" x:FieldModifier="internal" Text="{loc:Str tab_videos}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyVideo" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_auto_plays_hypnotube_videos_fullscreen}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{loc:Str label_web_videos}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyWebVideo" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_protect_browser_video}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{loc:Str label_protect_browser_video}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkProtectBrowserVideo" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_subliminal}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoSubliminal" x:FieldModifier="internal" Text="{loc:Str tab_subliminals}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomySubliminal" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_comment}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{loc:Str label_comments}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyComment" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                        </StackPanel>
                                                        <!-- Advanced column -->
                                                        <StackPanel Grid.Column="2">
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_bubbles}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoBubbles" x:FieldModifier="internal" Text="{loc:Str label_bubbles}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyBubbles" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_pink_filter}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoPinkFilter" x:FieldModifier="internal" Text="{loc:Str label_pink_filter_2}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyPinkFilter" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_lock_cards}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoLockCards" x:FieldModifier="internal" Text="{loc:Str label_lock_cards}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyLockCard" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_bouncing_text}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoBouncing" x:FieldModifier="internal" Text="{loc:Str label_bouncing}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyBouncingText" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_mind_wipe}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" x:Name="TxtAutoMindwipe" x:FieldModifier="internal" Text="{loc:Str label_mindwipe}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyMindWipe" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                            <Grid Height="30" Background="Transparent" ToolTip="{loc:Str tooltip_autonomy_wallpaper}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{loc:Str label_autonomy_wallpaper}" FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                                <CheckBox Grid.Column="1" x:Name="ChkAutonomyWallpaper" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}" VerticalAlignment="Center" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                                            </Grid>
                                                        </StackPanel>
                                                    </Grid>

                                                    <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,8"/>

                                                    <!-- Wallpaper source folder — where she pulls desktop wallpapers from (blank = assets/wallpapers) -->
                                                    <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_wallpaper_folder}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Button Grid.Column="0" x:Name="BtnWallpaperFolder" x:FieldModifier="internal" Content="{loc:Str btn_choose_wallpaper_folder}"
                                                                Cursor="Hand" Background="{DynamicResource SecondaryBrush}" Foreground="White" FontSize="11"
                                                                BorderThickness="0" Padding="10,5" VerticalAlignment="Center" Click="BtnWallpaperFolder_Click">
                                                            <Button.Resources>
                                                                <Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style>
                                                            </Button.Resources>
                                                        </Button>
                                                        <TextBlock Grid.Column="1" x:Name="TxtWallpaperFolder" x:FieldModifier="internal" Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                                                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="10,0,0,0"/>
                                                    </Grid>

                                                    <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                    <!-- Wallpaper persistence — every change used to snap back after a hardcoded
                                                         30s, so you barely ever saw it behind your windows (#694). Keep it, or
                                                         pick how long the pulse lasts. The second line moved into the ToolTip. -->
                                                    <Grid Height="34" Background="Transparent" ToolTip="{loc:Str tooltip_wallpaper_keep}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{loc:Str label_wallpaper_keep}" FontSize="13" FontWeight="SemiBold"
                                                                   Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                        <CheckBox Grid.Column="1" x:Name="ChkWallpaperKeep" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}"
                                                                  VerticalAlignment="Center" Checked="ChkWallpaperKeep_Changed" Unchecked="ChkWallpaperKeep_Changed"/>
                                                    </Grid>

                                                    <!-- Panel, not a bare row: MainWindow.Autonomy.cs shows/hides the whole
                                                         duration control with the keep toggle, by this name and type. -->
                                                    <StackPanel x:Name="PanelWallpaperDuration" x:FieldModifier="internal">
                                                        <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_wallpaper_duration}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{loc:Str label_wallpaper_duration}" FontSize="13" FontWeight="SemiBold"
                                                                       Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                            <Slider Grid.Column="1" x:Name="SliderWallpaperDuration" x:FieldModifier="internal"
                                                                    Minimum="10" Maximum="600" Value="30" VerticalAlignment="Center" Margin="14,0"
                                                                    ValueChanged="SliderWallpaperDuration_Changed"/>
                                                            <TextBlock Grid.Column="2" x:Name="TxtWallpaperDuration" x:FieldModifier="internal" Text="30s"
                                                                       HorizontalAlignment="Right" VerticalAlignment="Center"
                                                                       Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </Expander>

                                    <!-- ============ SUBSECTION 3b: MANTRAS & SESSION ============ -->
                                    <Border Style="{StaticResource SettingCardStyle}">
                                        <Grid>
                                            <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                            <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                    Background="{StaticResource TabHueBrush}"/>
                                            <StackPanel Margin="16,8,16,10">
                                                <TextBlock Text="Mantras &amp; session" Foreground="{StaticResource TabHueBrush}"
                                                           FontSize="10" FontWeight="Bold" Margin="0,2,0,4"/>

                                                <!-- Surprise mantras = the Takeover auto-trigger (AutonomyCanTriggerVoiceCommand).
                                                     The on-demand mantra capability + all mic controls live in "She's Listening".
                                                     TxtAutonomyVoiceHint stays a VISIBLE second line rather than moving into the
                                                     ToolTip: MainWindow.Autonomy.cs rewrites it to explain why the mic is or is
                                                     not armed, and a warning nobody hovers is a warning nobody reads. -->
                                                <Grid Background="Transparent" Margin="0,2" ToolTip="During Takeover she'll occasionally ask you to repeat a phrase, listen for it offline, then give a custom response. First time on asks for mic consent. (On-demand mantras + the mic live in She's Listening.)">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                                        <TextBlock Text="🎤 Surprise me with mantras" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextLightBrush}"/>
                                                        <TextBlock x:Name="TxtAutonomyVoiceHint" x:FieldModifier="internal" Text="During Takeover she'll now and then ask you to repeat a phrase. Suppressed while you're driving the mic yourself." FontSize="11" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}"/>
                                                    </StackPanel>
                                                    <CheckBox Grid.Column="1" x:Name="ChkAutonomyVoice" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}"
                                                              VerticalAlignment="Center" Checked="ChkAutonomyVoice_Changed" Unchecked="ChkAutonomyVoice_Changed"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="34" Background="Transparent" ToolTip="When on, Takeover re-arms itself the next time you launch the app. Off by default — Takeover always starts off.">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="↻ Resume Takeover on startup" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1" x:Name="ChkAutonomyResumeOnStartup" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}"
                                                              VerticalAlignment="Center" Checked="ChkAutonomyResume_Changed" Unchecked="ChkAutonomyResume_Changed"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <Grid Height="34" Background="Transparent" ToolTip="Shows a thin pink bar under the avatar that drains as the next surprise Takeover approaches, so you can see it coming.">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="⏳ Show next-takeover countdown bar" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1" x:Name="ChkShowTakeoverCountdown" x:FieldModifier="internal" Style="{StaticResource ToggleStyle}"
                                                              VerticalAlignment="Center" Checked="ChkShowTakeoverCountdown_Changed" Unchecked="ChkShowTakeoverCountdown_Changed"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <!-- Mantra Chant (suggestion #653): loops this mod's VOICED mantras as ambient audio.
                                                     Self-contained here (no Takeover twin), so the handlers live in this tab's code-behind.
                                                     TxtMantraChantHint also stays visible - it is the "this mod has no voiced mantras"
                                                     line, rewritten from the code-behind on every tab show. -->
                                                <Grid Background="Transparent" Margin="0,2" ToolTip="{loc:Str tooltip_mantra_chant}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                                        <TextBlock Text="{loc:Str label_mantra_chant}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextLightBrush}"/>
                                                        <TextBlock x:Name="TxtMantraChantHint" Text="{loc:Str desc_mantra_chant}" FontSize="11" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}"/>
                                                    </StackPanel>
                                                    <CheckBox Grid.Column="1" x:Name="ChkMantraChant" Style="{StaticResource ToggleStyle}"
                                                              VerticalAlignment="Center" Checked="ChkMantraChant_Changed" Unchecked="ChkMantraChant_Changed"/>
                                                </Grid>

                                                <Grid Height="36" Background="Transparent">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_mantra_chant_volume}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider x:Name="SldMantraChantVolume" Grid.Column="1" VerticalAlignment="Center" Margin="14,0"
                                                            Minimum="0" Maximum="100" SmallChange="5" LargeChange="20" TickFrequency="10"
                                                            ValueChanged="SldMantraChantVolume_Changed"/>
                                                    <TextBlock x:Name="TxtMantraChantVolume" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                               FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PinkBrush}" Text="—"/>
                                                </Grid>

                                                <Grid Height="36" Background="Transparent">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto" MinWidth="56"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{loc:Str label_mantra_chant_gap}" FontSize="13" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                                    <Slider x:Name="SldMantraChantGap" Grid.Column="1" VerticalAlignment="Center" Margin="14,0"
                                                            Minimum="0" Maximum="60" SmallChange="1" LargeChange="5" TickFrequency="5"
                                                            ValueChanged="SldMantraChantGap_Changed"/>
                                                    <TextBlock x:Name="TxtMantraChantGap" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                               FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PinkBrush}" Text="—"/>
                                                </Grid>

                                                <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                                                <!-- Pointer to the one place that owns the mic. Until Phase 2 of the UX
                                                     restructure the five real voice controls were parked here, Collapsed,
                                                     as the hidden source of truth that She's Listening mirrored onto -
                                                     which meant the setting's only visible editor and its handlers lived
                                                     on different tabs. They moved to Settings → Devices intact (same
                                                     x:Names, same MainWindow.Autonomy.cs handlers), and this is now a
                                                     plain signpost with nothing hiding behind it. -->
                                                <Border Background="#22FF69B4" CornerRadius="8" Padding="12,10" Margin="0,2,0,0">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Image Grid.Column="0" Width="18" Height="18" VerticalAlignment="Top" Margin="0,1,10,0"
                                                               Source="{Binding Source='🎙️', Converter={StaticResource EmojiToImageSource}}"/>
                                                        <TextBlock Grid.Column="1" Foreground="{DynamicResource PinkBrush}" FontSize="12"
                                                                   TextWrapping="Wrap" LineHeight="17" VerticalAlignment="Center"
                                                                   Text="{loc:Str set2_takeover_mic_moved}"/>
                                                        <Button Grid.Column="2" Content="{loc:Str set2_configure_in_settings}"
                                                                Margin="12,0,0,0" Padding="10,4" FontSize="11" VerticalAlignment="Center"
                                                                Style="{StaticResource OutlineButton}"
                                                                Click="BtnOpenDeviceSettings_Click"/>
                                                    </Grid>
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- ============ SUBSECTION 4: DIAGNOSTICS (test triggers) ============ -->
                                    <Border Style="{StaticResource SettingCardStyle}">
                                        <Grid>
                                            <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                            <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                                    Background="{StaticResource TabHueBrush}"/>
                                            <Grid Margin="16,10,16,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Test triggers" Foreground="{StaticResource TextLightBrush}"
                                                           FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                <Button Grid.Column="1" Content="{loc:Str btn_test}" Background="{DynamicResource PinkBrush}" Foreground="White" FontWeight="Bold" FontSize="12"
                                                        BorderThickness="0" Cursor="Hand" Padding="14,7" Click="BtnTestAutonomy_Click" ToolTip="{loc:Str tooltip_test_action}">
                                                    <Button.Resources>
                                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
                                                    </Button.Resources>
                                                </Button>
                                                <!-- Dev/test affordance for the new offline "repeat after me" voice action (pass-2 UI will relocate this). -->
                                                <Button x:Name="BtnTestVoice" Grid.Column="3" Content="🎤 Test mantra" Background="#3A2A4A" Foreground="#90EE90" FontWeight="Bold" FontSize="12"
                                                        BorderThickness="0" Cursor="Hand" Padding="14,7" Click="BtnTestVoice_Click" ToolTip="Fire a spoken mantra now (requires a Vosk model in Resources/Models/vosk, a mic, and mantra content for the active mod).">
                                                    <Button.Resources>
                                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
                                                    </Button.Resources>
                                                </Button>
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <!-- Hidden elements. ChkAutonomyEnabled is the canonical master switch the
                                         premium rail, /remember and MainWindow.Settings all read and flip; the
                                         hero's Start/Stop button is the visible face of it, so it stays here,
                                         collapsed and named, exactly as before. -->
                                    <TextBlock x:Name="TxtTakeoverUnlocked" x:FieldModifier="internal" Visibility="Collapsed"/>
                                    <CheckBox x:Name="ChkAutonomySpiral" x:FieldModifier="internal" Visibility="Collapsed" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                    <CheckBox x:Name="ChkAutonomyBubbleCount" x:FieldModifier="internal" Visibility="Collapsed" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                                    <Button x:Name="BtnForceStartAutonomy" Visibility="Collapsed" Click="BtnForceStartAutonomy_Click"/>
                                    <CheckBox x:Name="ChkAutonomyEnabled" x:FieldModifier="internal" Visibility="Collapsed" Checked="ChkAutonomyEnabled_Changed" Unchecked="ChkAutonomyEnabled_Changed"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- ============ SIDE RAIL: art plate + the explanatory guide ============
                         The art is the Border's BACKGROUND, not a child Image: a Border clips its
                         Background to CornerRadius but does NOT clip child content, so a child
                         Image would square off the rounded corners (round-2 lesson). -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <Border CornerRadius="14" BorderThickness="2" BorderBrush="{StaticResource VelvetCardBorderBrush}"
                                MinHeight="200" MaxHeight="300" Margin="0,0,0,10">
                            <Border.Background>
                                <ImageBrush x:Name="TakeoverSideArtBrush" Stretch="UniformToFill">
                                    <ImageBrush.ImageSource>
                                        <BitmapImage UriSource="pack://application:,,,/Resources/features/takeover.png"
                                                     DecodePixelWidth="800"/>
                                    </ImageBrush.ImageSource>
                                </ImageBrush>
                            </Border.Background>
                            <!-- Scrim so the plate sits under the page instead of fighting it. -->
                            <Border CornerRadius="12">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                        <GradientStop Color="#66000000" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="0.45"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Border>

                        <!-- Explanatory guide -->
                        <Border Style="{StaticResource SettingCardStyle}">
                            <Grid>
                                <Border Background="{StaticResource TabHueWashBrush}" CornerRadius="11"/>
                                <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                        Background="{StaticResource TabHueBrush}"/>
                                <StackPanel Margin="16,12,16,14">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <Image Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"
                                               Source="{Binding Source='💡', Converter={StaticResource EmojiToImageSource}}"/>
                                        <TextBlock Text="{loc:Str guide_takeover_title}" Foreground="{StaticResource TabHueBrush}"
                                                   FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Step 1 -->
                                    <TextBlock Text="{loc:Str guide_takeover_step1_title}" Foreground="{StaticResource TextLightBrush}"
                                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                    <TextBlock Text="{loc:Str guide_takeover_step1_body}" Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="12" TextWrapping="Wrap" LineHeight="17" Margin="0,0,0,10"/>
                                    <!-- Step 2 -->
                                    <TextBlock Text="{loc:Str guide_takeover_step2_title}" Foreground="{StaticResource TextLightBrush}"
                                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                    <TextBlock Text="{loc:Str guide_takeover_step2_body}" Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="12" TextWrapping="Wrap" LineHeight="17" Margin="0,0,0,10"/>
                                    <!-- Step 3 -->
                                    <TextBlock Text="{loc:Str guide_takeover_step3_title}" Foreground="{StaticResource TextLightBrush}"
                                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                    <TextBlock Text="{loc:Str guide_takeover_step3_body}" Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="12" TextWrapping="Wrap" LineHeight="17" Margin="0,0,0,4"/>

                                    <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,8,0,10"/>

                                    <TextBlock Text="{loc:Str guide_takeover_settings_title}" Foreground="{StaticResource TabHueBrush}"
                                               FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <TextBlock Text="{loc:Str guide_takeover_settings_body}" Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="12" TextWrapping="Wrap" LineHeight="18" Margin="0,0,0,10"/>

                                    <Border Background="#22FF69B4" CornerRadius="8" Padding="10,8" Margin="0,0,0,10">
                                        <TextBlock Text="{loc:Str guide_takeover_tip}" Foreground="{DynamicResource PinkBrush}"
                                                   FontSize="12" TextWrapping="Wrap" LineHeight="17"/>
                                    </Border>

                                    <TextBlock Text="{loc:Str guide_takeover_patreon}" Foreground="{StaticResource TextDimBrush}"
                                               FontSize="11" TextWrapping="Wrap" LineHeight="16" FontStyle="Italic"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Translucent gating overlay (only for free users). Last sibling of the root grid so
                 it covers hero + both columns: a locked feature must not show a live-looking
                 Start button. RefreshPremiumGate(BambiTakeoverTab.BambiTakeoverGate) owns the
                 Visibility - name, field modifier and click handler are API. -->
            <Border x:Name="BambiTakeoverGate" x:FieldModifier="internal" Visibility="Collapsed"
                    Background="#99000010" BorderBrush="{DynamicResource FxGlowBrush}" BorderThickness="2"
                    CornerRadius="16" IsHitTestVisible="True">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Width="52" Height="52" HorizontalAlignment="Center" Source="{Binding Source='🔒', Converter={StaticResource EmojiToImageSource}}"/>
                    <TextBlock Text="{loc:Str gate_premium_locked}" FontSize="22"
                               FontWeight="Bold" Foreground="{StaticResource TextLightBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{loc:Str gate_premium_subtitle}" FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                    <Button Click="BtnGateUnlock_Click" Padding="20,8" Background="{DynamicResource FxGlowBrush}"
                            Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                        <helpers:EmojiTextBlock Text="{loc:Str gate_unlock_with_patreon}"/>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
