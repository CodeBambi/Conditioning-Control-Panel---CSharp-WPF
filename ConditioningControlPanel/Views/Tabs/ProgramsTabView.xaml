<UserControl x:Class="ConditioningControlPanel.Views.Tabs.ProgramsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:ConditioningControlPanel.Helpers"
             xmlns:hover="clr-namespace:ConditioningControlPanel.Behaviors"
             xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Session-progress bar, hero sized. Foreground is overwritten in code with the program's
             accent brush; the fallbacks stay DynamicResource so mods re-theme it at runtime. The
             track background is transparent because the bar sits inside SessionBarHost, which owns
             the track colour and the rounded clip (see SessionBarHost_SizeChanged). -->
        <Style x:Key="ProgramHeroBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="16"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="8"/>
                            <Border x:Name="PART_Track"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="8"
                                    Background="{TemplateBinding Foreground}">
                                <!-- Static top gloss so the fill reads as a lit bar, not a flat strip.
                                     Gradient only - no per-frame work. -->
                                <Border CornerRadius="8">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Color="#50FFFFFF" Offset="0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.55"/>
                                            <GradientStop Color="#22000000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Style="{StaticResource SectionPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15">

                    <!-- ================= Header ================= -->
                    <StackPanel Margin="0,0,0,14">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"
                                   Source="{Binding Source='📅', Converter={StaticResource EmojiToImageSource}}"/>
                            <TextBlock Text="{loc:Str programs_browse_header}"
                                       Foreground="{DynamicResource PinkBrush}"
                                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <!-- Same gold BETA pill as the nav button, scaled up for the header. -->
                            <Border Margin="8,0,0,0" VerticalAlignment="Center" CornerRadius="9999"
                                    Padding="7,2" Background="#33FFD700"
                                    BorderBrush="#AAFFD700" BorderThickness="1">
                                <TextBlock Text="BETA" FontSize="10" FontWeight="Bold" Foreground="#FFD700"/>
                            </Border>
                        </StackPanel>
                        <TextBlock Text="{loc:Str programs_browse_sub}"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0" MaxWidth="720"
                                   HorizontalAlignment="Left"/>
                    </StackPanel>

                    <!-- ================================================================= -->
                    <!-- BROWSE: nothing enrolled. One card per program in App.Programs.Library -->
                    <!-- ================================================================= -->
                    <StackPanel x:Name="ProgramsBrowsePanel" x:FieldModifier="internal" Visibility="Collapsed">

                        <TextBlock x:Name="TxtProgramsBrowseEmpty" x:FieldModifier="internal"
                                   Text="{loc:Str programs_browse_empty}"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   FontSize="13" Margin="2,10,0,0" Visibility="Collapsed"/>

                        <ItemsControl x:Name="ProgramLibraryList" x:FieldModifier="internal" Margin="0,10,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Tall hero card. Fixed width so cards read as objects, not strips;
                                         VerticalAlignment=Stretch lets the WrapPanel line height equalise
                                         every card in a row so the buttons line up. -->
                                    <Border Width="350" MinHeight="280" Margin="0,0,14,14" CornerRadius="12"
                                            VerticalAlignment="Stretch"
                                            Opacity="{Binding CardOpacity}"
                                            Background="{DynamicResource ElevatedSurfaceBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Accent strip across the top -->
                                            <Border Grid.Row="0" Height="5" VerticalAlignment="Top"
                                                    CornerRadius="11,11,0,0"
                                                    Background="{Binding AccentBrush}"/>

                                            <!-- Banner art band. The source is the Today card's 24.6:1
                                                 strip: its left half is pre-faded to alpha 0 and the
                                                 artwork densest at the right end, so UniformToFill with
                                                 AlignmentX=Right crops to the richest slice and the
                                                 fade bleeds into the card surface on its own. Margin 1
                                                 keeps the fill inside the card's 1px border. -->
                                            <Border Grid.Row="1" Height="64" Margin="1,0,1,0"
                                                    ClipToBounds="True"
                                                    Visibility="{Binding BannerVisibility}">
                                                <Rectangle IsHitTestVisible="False">
                                                    <Rectangle.Fill>
                                                        <ImageBrush ImageSource="{Binding BannerArt}"
                                                                    Stretch="UniformToFill"
                                                                    AlignmentX="Right" AlignmentY="Center"/>
                                                    </Rectangle.Fill>
                                                </Rectangle>
                                            </Border>

                                            <!-- Header + copy -->
                                            <StackPanel Grid.Row="2" Margin="18,16,18,0">
                                                <!--
                                                    Identity crest. Exactly one of this and the bare
                                                    glyph below it is ever visible - the same contract
                                                    as the run header's sigil-or-accent-bar slot. No
                                                    art resolves, no crest, and the card is exactly
                                                    the one that shipped before this existed.

                                                    Negative margins are load-bearing, not sloppy: the
                                                    halo is 8px wider than the chip on every side, so
                                                    pulling the host back by 8 lands the CHIP on the
                                                    StackPanel's 18,16 origin and keeps it optically
                                                    flush with the title underneath.
                                                -->
                                                <Grid Width="88" Height="88" HorizontalAlignment="Left"
                                                      Margin="-8,-8,0,2"
                                                      Visibility="{Binding ArtVisibility}">
                                                    <Ellipse Width="88" Height="88" Opacity="0.55"
                                                             IsHitTestVisible="False"
                                                             Fill="{Binding ArtGlowBrush}"/>

                                                    <Border Width="72" Height="72" CornerRadius="36"
                                                            Background="{DynamicResource SurfaceBgBrush}"
                                                            BorderBrush="{DynamicResource GlassBorderBrush}"
                                                            BorderThickness="1">
                                                        <!--
                                                            Program art is white RGB with its luminance in
                                                            the ALPHA channel (see Services/Program/ProgramArt.cs),
                                                            so it is a Rectangle FILLED with the program
                                                            accent and MASKED by the image - never an
                                                            <Image>, which renders a near-invisible
                                                            white-on-transparent smear, and never a
                                                            translucent accent over opaque greyscale, which
                                                            lifts the blacks and gives the washed-out
                                                            "failed tint" look. Fill + OpacityMask is also
                                                            why the crest can only ever be the program's own
                                                            colour, so it cannot clash under any mod.

                                                            Rounded to match the chip: the generated art does
                                                            not fall off to pure black at its edges, so a
                                                            square Rectangle paints a faint accent SQUARE
                                                            inside the round host.

                                                            Held just off full so the glyph on top stays the
                                                            thing the eye lands on.
                                                        -->
                                                        <Rectangle Width="66" Height="66"
                                                                   RadiusX="33" RadiusY="33" Opacity="0.8"
                                                                   Fill="{Binding AccentBrush}"
                                                                   hover:HoverPop.IsEnabled="True"
                                                                   OpacityMask="{Binding ArtMask}"/>
                                                    </Border>

                                                    <!-- The program's own glyph, on its own art. Four of the
                                                         five shipped programs have no sigil and land on the
                                                         same shared archetype plate, so the glyph is what
                                                         keeps their crests apart at a glance. -->
                                                    <!-- IsHitTestVisible=False so the glyph does not punch a
                                                         hole in the crest's hover: without it, sliding the
                                                         pointer onto the emoji reads as leaving the art and
                                                         the pop flickers. The halo Ellipse above is opted
                                                         out for the same reason. -->
                                                    <helpers:EmojiTextBlock Text="{Binding Icon}" FontSize="30"
                                                                            IsHitTestVisible="False"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                                </Grid>

                                                <helpers:EmojiTextBlock Text="{Binding Icon}" FontSize="44"
                                                                        HorizontalAlignment="Left"
                                                                        Margin="0,0,0,10"
                                                                        Visibility="{Binding IconOnlyVisibility}"/>
                                                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextLightBrush}"
                                                           FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
                                                <WrapPanel Margin="0,8,0,0">
                                                    <Border CornerRadius="9999" Padding="8,2" Margin="0,0,6,4"
                                                            Background="{Binding TierBackground}">
                                                        <TextBlock Text="{Binding TierLabel}" Foreground="{Binding TierBrush}"
                                                                   FontSize="10" FontWeight="Bold"/>
                                                    </Border>
                                                    <Border CornerRadius="9999" Padding="8,2" Margin="0,0,6,4"
                                                            Background="{DynamicResource SurfaceBgBrush}"
                                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                                        <TextBlock Text="{Binding LengthLabel}"
                                                                   Foreground="{DynamicResource TextLightBrush}"
                                                                   FontSize="10" FontWeight="SemiBold"/>
                                                    </Border>
                                                </WrapPanel>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource TextMutedBrush}"
                                                           FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Pitch}" Foreground="{DynamicResource TextLightBrush}"
                                                           FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap" LineHeight="18"/>
                                                <TextBlock Text="{Binding ReasonText}" Visibility="{Binding ReasonVisibility}"
                                                           Foreground="{DynamicResource TextMutedBrush}"
                                                           FontSize="11" FontStyle="Italic" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <!-- Enroll / locked - pinned to the bottom of the card -->
                                            <Button Grid.Row="3" Margin="18,16,18,16"
                                                    HorizontalAlignment="Stretch"
                                                    Content="{Binding ActionText}"
                                                    Tag="{Binding ProgramId}"
                                                    IsEnabled="{Binding IsActionEnabled}"
                                                    Style="{StaticResource PinkButton}"
                                                    Click="BtnProgramEnroll_Click"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- ================================================================= -->
                    <!-- RUN: an active or paused enrollment.                               -->
                    <!--                                                                    -->
                    <!-- Battle-pass framing: identity + a reward TRACK on one surface,     -->
                    <!-- then a hero band for today with the art bleeding in from the       -->
                    <!-- right, then the session bar and the task cards. Every accent      -->
                    <!-- comes from the program (the mod theme), never from a hard pink.   -->
                    <!-- ================================================================= -->
                    <StackPanel x:Name="ProgramsRunPanel" x:FieldModifier="internal" Visibility="Collapsed">

                        <!-- =================================================================
                             WHERE YOU ARE. Identity (sigil, title, badges) + run stats on the
                             first row, the reward track underneath, one surface - split, they
                             read as two half-empty boxes that answer the same question.
                             ================================================================= -->
                        <Border x:Name="RunHeaderPanel" x:FieldModifier="internal"
                                Padding="22,20" CornerRadius="14" Margin="0,0,0,14"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                            <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Identity slot. Exactly one of these is ever visible: the sigil
                                     when Resources/programs/sigil_<id>.png resolves, otherwise the
                                     original 4px accent bar. The screen is never broken by missing
                                     art, only plainer. See MainWindow.ProgramsTab.cs. -->
                                <Grid Grid.Column="0" Margin="0,0,18,0" VerticalAlignment="Top">
                                    <Border x:Name="RunAccentBar" x:FieldModifier="internal"
                                            Width="4" CornerRadius="2"
                                            HorizontalAlignment="Left" VerticalAlignment="Stretch"
                                            Background="{DynamicResource PinkBrush}"/>

                                    <Grid Width="84" Height="84">
                                        <!-- Soft accent halo behind the sigil chip. Fill is a frozen
                                             radial built from the program accent in code; static, no
                                             animation, just presence. -->
                                        <Ellipse x:Name="RunSigilGlow" x:FieldModifier="internal"
                                                 Width="84" Height="84" Opacity="0.55"
                                                 IsHitTestVisible="False" Visibility="Collapsed"/>

                                        <!-- White-RGB / luminance-in-alpha art, rendered as the program
                                             accent masked by the image, so it glows out of the panel
                                             with no rectangle edge and no hue clash under any mod. -->
                                        <Border x:Name="RunSigilHost" x:FieldModifier="internal"
                                                Width="64" Height="64" CornerRadius="32"
                                                Visibility="Collapsed"
                                                Background="{DynamicResource SurfaceBgBrush}"
                                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                            <!-- Clipped to a circle to match the chip behind it. The
                                                 generated art does not fall off to pure black at its
                                                 edges, so the residual halo would otherwise paint a
                                                 faint SQUARE of accent inside a round host. -->
                                            <Rectangle x:Name="RunSigil" x:FieldModifier="internal"
                                                       Width="60" Height="60" RadiusX="30" RadiusY="30">
                                                <Rectangle.OpacityMask>
                                                    <ImageBrush x:Name="RunSigilMask" x:FieldModifier="internal"
                                                                Stretch="Uniform"/>
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                        </Border>
                                    </Grid>
                                </Grid>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock x:Name="TxtRunProgramTitle" x:FieldModifier="internal"
                                               Foreground="{DynamicResource TextLightBrush}"
                                               FontSize="28" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock x:Name="TxtRunChapterName" x:FieldModifier="internal"
                                                   Foreground="{DynamicResource PinkBrush}"
                                                   FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <TextBlock Text="  ·  " Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="13" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="TxtRunDayCounter" x:FieldModifier="internal"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <WrapPanel Margin="0,8,0,0">
                                        <Border x:Name="RunStrictBadge" x:FieldModifier="internal"
                                                CornerRadius="9999" Padding="8,2" Margin="0,0,6,4"
                                                Background="{DynamicResource SurfaceBgBrush}"
                                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1"
                                                Visibility="Collapsed">
                                            <TextBlock Text="{loc:Str programs_strict_badge}"
                                                       Foreground="{DynamicResource PinkBrush}"
                                                       FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <Border x:Name="RunAttemptBadge" x:FieldModifier="internal"
                                                CornerRadius="9999" Padding="8,2" Margin="0,0,6,4"
                                                Background="{DynamicResource SurfaceBgBrush}"
                                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1"
                                                Visibility="Collapsed">
                                            <TextBlock x:Name="TxtRunAttempt" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="10" FontWeight="SemiBold"/>
                                        </Border>
                                    </WrapPanel>
                                </StackPanel>

                                <!-- Run stats. Battle-pass season chips: what you've banked, at a
                                     glance, filling what used to be dead air between the title and
                                     the buttons. Values are plain numbers set in code. -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal"
                                            VerticalAlignment="Center" Margin="0,0,18,0">
                                    <Border CornerRadius="10" Padding="14,8" Margin="0,0,8,0" MinWidth="86"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtRunStatDone" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str programs_stat_days_done}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="9" FontWeight="SemiBold" HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border CornerRadius="10" Padding="14,8" Margin="0,0,8,0" MinWidth="86"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtRunStatPerfect" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str programs_stat_perfect}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="9" FontWeight="SemiBold" HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border CornerRadius="10" Padding="14,8" MinWidth="86"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtRunStatDaysOff" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str programs_stat_days_off}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="9" FontWeight="SemiBold" HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- Pause / Resume + Withdraw. Withdraw is always here, always plain. -->
                                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Top">
                                    <Button x:Name="BtnProgramPauseResume" x:FieldModifier="internal"
                                            Style="{StaticResource SecondaryButton}" MinWidth="90"
                                            Margin="0,0,8,0" Click="BtnProgramPauseResume_Click"/>
                                    <Button x:Name="BtnProgramWithdraw" x:FieldModifier="internal"
                                            Content="{loc:Str btn_program_withdraw}"
                                            Style="{StaticResource SecondaryButton}" MinWidth="90"
                                            Click="BtnProgramWithdraw_Click"/>
                                </StackPanel>
                            </Grid>

                            <!-- ============ The reward track ============
                                 Day nodes distributed along a single track by a UniformGrid, not a
                                 WrapPanel: seven squares huddled at the left of a 1700px box read
                                 as a rendering accident. Done days are filled accent checks, today
                                 breathes (template storyboard), milestone days carry a reward
                                 glyph under the node. Width is clamped in code
                                 (MainWindow.BuildProgramDayStrip). -->
                            <StackPanel Margin="0,20,0,0">
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{loc:Str programs_track_header}"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <!-- Chapter reward chip: the carrot at the end of the track. -->
                                    <Border x:Name="RunChapterRewardChip" x:FieldModifier="internal"
                                            Grid.Column="2" CornerRadius="9999" Padding="10,3"
                                            Visibility="Collapsed"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel Orientation="Horizontal">
                                            <helpers:EmojiTextBlock Text="🎁" FontSize="12" VerticalAlignment="Center"
                                                                    Margin="0,0,6,0"/>
                                            <TextBlock Text="{loc:Str programs_chapter_reward}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="10" FontWeight="Bold" VerticalAlignment="Center"
                                                       Margin="0,0,6,0"/>
                                            <TextBlock x:Name="TxtRunChapterReward" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center"
                                                       MaxWidth="420" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <!-- The rail is width-limited by a capped star COLUMN, not by
                                     MaxWidth on the rail itself. A left-aligned element sizes to
                                     its content, so MaxWidth never binds and the nodes huddle at
                                     their natural width each - which is the bug this rail exists to
                                     fix. A star column honours both the cap and the remaining
                                     space, and re-measures on resize with no code behind it. -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="RailWidthColumn" x:FieldModifier="internal"
                                                          Width="*" MaxWidth="840"/>
                                        <ColumnDefinition x:Name="RailSpacerColumn" x:FieldModifier="internal"
                                                          Width="*"/>
                                    </Grid.ColumnDefinitions>
                                <Grid x:Name="ProgramDayRail" x:FieldModifier="internal" Grid.Column="0">
                                    <!-- Track. Node centres sit at y=31 of every 82px item (62px
                                         node area on top, glyph strip below), so the 5px line is
                                         top-aligned at 28.5 instead of centred in the rail. The
                                         done segment is sized in star units so it ends exactly on
                                         today's node centre at any program length.
                                         The node area is 62 and not the node's own 42 because the
                                         today halo is a 52px ellipse scaled to 1.12 by the breathing
                                         storyboard - 58.2px, so it needs 29.2 either side of centre.
                                         A shorter slot does not shrink it, it CLIPS it flat top and
                                         bottom (WPF's automatic layout clip), which is what a 48px
                                         area did. Keep these three numbers in step: node area,
                                         item height, and this offset. -->
                                    <Grid Height="5" VerticalAlignment="Top" Margin="0,28.5,0,0">
                                        <Border CornerRadius="2.5"
                                                Background="{DynamicResource PanelAccentBrush}"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition x:Name="RailDoneColumn" x:FieldModifier="internal"
                                                                  Width="0*"/>
                                                <ColumnDefinition x:Name="RailRestColumn" x:FieldModifier="internal"
                                                                  Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <!-- Grow-in entrance is a ScaleX animation on this
                                                 transform (StartProgramRunEntrance); it rests at 1. -->
                                            <Border x:Name="RailProgressFill" x:FieldModifier="internal"
                                                    Grid.Column="0" CornerRadius="2.5"
                                                    RenderTransformOrigin="0,0.5">
                                                <Border.RenderTransform>
                                                    <ScaleTransform x:Name="RailFillScale" x:FieldModifier="internal"
                                                                    ScaleX="1" ScaleY="1"/>
                                                </Border.RenderTransform>
                                            </Border>
                                        </Grid>
                                    </Grid>

                                    <ItemsControl x:Name="ProgramDayStrip" x:FieldModifier="internal">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Rows="1"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Width="54" Height="82" HorizontalAlignment="Center">
                                                    <Grid Height="62" VerticalAlignment="Top">
                                                        <!-- Panel-coloured backing so the track stops at
                                                             every node instead of drawing a line through
                                                             the transparent-filled today/missed states. -->
                                                        <Ellipse Width="{Binding NodeSize}" Height="{Binding NodeSize}"
                                                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                 Fill="{DynamicResource ElevatedSurfaceBrush}"/>
                                                        <!-- Breathing halo, today only. The storyboard runs
                                                             from the IsCurrent DataTrigger below; opacity +
                                                             scale only, and containers are rebuilt on every
                                                             refresh, so the loop dies with the item. -->
                                                        <Ellipse x:Name="PipGlow" Width="52" Height="52" Opacity="0"
                                                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                 IsHitTestVisible="False"
                                                                 Fill="{Binding GlowBrush}"
                                                                 Visibility="{Binding GlowVisibility}"
                                                                 RenderTransformOrigin="0.5,0.5">
                                                            <Ellipse.RenderTransform>
                                                                <ScaleTransform x:Name="PipGlowScale" ScaleX="1" ScaleY="1"/>
                                                            </Ellipse.RenderTransform>
                                                        </Ellipse>
                                                        <Border Width="{Binding NodeSize}" Height="{Binding NodeSize}"
                                                                CornerRadius="9999"
                                                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                Background="{Binding Fill}"
                                                                BorderBrush="{Binding Stroke}"
                                                                BorderThickness="{Binding PipBorderThickness}"
                                                                Opacity="{Binding PipOpacity}"
                                                                ToolTip="{Binding Tip}">
                                                            <TextBlock Text="{Binding Label}" Foreground="{Binding LabelBrush}"
                                                                       FontWeight="{Binding LabelWeight}"
                                                                       FontSize="{Binding LabelSize}"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </Grid>
                                                    <!-- Milestone reward glyph under the node (boss crown /
                                                         reward gift). Collapsed on plain days. -->
                                                    <helpers:EmojiTextBlock Text="{Binding RewardGlyph}" FontSize="13"
                                                                            Visibility="{Binding RewardVisibility}"
                                                                            ToolTip="{Binding RewardTip}"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Bottom"/>
                                                </Grid>
                                                <DataTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                                    <DoubleAnimation Storyboard.TargetName="PipGlow"
                                                                                     Storyboard.TargetProperty="Opacity"
                                                                                     From="0.35" To="0.95" Duration="0:0:1.2">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <SineEase EasingMode="EaseInOut"/>
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                    <DoubleAnimation Storyboard.TargetName="PipGlowScale"
                                                                                     Storyboard.TargetProperty="ScaleX"
                                                                                     From="0.9" To="1.12" Duration="0:0:1.2">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <SineEase EasingMode="EaseInOut"/>
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                    <DoubleAnimation Storyboard.TargetName="PipGlowScale"
                                                                                     Storyboard.TargetProperty="ScaleY"
                                                                                     From="0.9" To="1.12" Duration="0:0:1.2">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <SineEase EasingMode="EaseInOut"/>
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                    </DataTrigger>
                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                                </Grid>
                            </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Paused note -->
                        <Border x:Name="RunPausedNote" x:FieldModifier="internal" Visibility="Collapsed"
                                Padding="14,10" CornerRadius="10" Margin="0,0,0,12"
                                Background="{DynamicResource SurfaceBgBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                            <TextBlock Text="{loc:Str programs_paused_note}"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       FontSize="12" TextWrapping="Wrap"/>
                        </Border>

                        <!-- =================================================================
                             WHAT YOU DO NOW. A hero band: the day's art (keyed off the SESSION
                             TEMPLATE archetype, so four heroes dress a 7-day and a 28-day
                             program alike) bleeds in from the right under a gradient fade,
                             with an accent wash behind the whole band. Below it, the session
                             bar and the task cards.
                             ================================================================= -->
                        <Border x:Name="TodayPanel" x:FieldModifier="internal"
                                Padding="22,20" CornerRadius="14"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                            <StackPanel>

                            <Grid x:Name="TodayHeroGrid" x:FieldModifier="internal">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <!-- Width driven from code: 0 with no art (the copy takes the
                                         whole band - a star column would reserve empty space),
                                         2* capped when the hero art resolves. -->
                                    <ColumnDefinition x:Name="HeroArtColumn" x:FieldModifier="internal"
                                                      Width="0" MaxWidth="560"/>
                                </Grid.ColumnDefinitions>

                                <!-- Ambient accent wash across the band. Frozen radial built from
                                     the program accent in code; negative margins bleed it to the
                                     panel edge past the padding, radius matches the panel corner. -->
                                <Rectangle x:Name="TodayHeroGlow" x:FieldModifier="internal"
                                           Grid.ColumnSpan="2" IsHitTestVisible="False"
                                           Opacity="0.4" RadiusX="14" RadiusY="14"
                                           Margin="-22,-20,-22,-6"/>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="{loc:Str programs_today_header}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Border x:Name="TodayBossBadge" x:FieldModifier="internal" Visibility="Collapsed"
                                                CornerRadius="9999" Padding="8,2" Margin="10,0,0,0"
                                                Background="{DynamicResource TransparentPinkBrush}"
                                                BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                                            <TextBlock Text="{loc:Str programs_boss_badge}"
                                                       Foreground="{DynamicResource PinkBrush}"
                                                       FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <Border x:Name="TodayReturnBadge" x:FieldModifier="internal" Visibility="Collapsed"
                                                CornerRadius="9999" Padding="8,2" Margin="6,0,0,0"
                                                Background="{DynamicResource SurfaceBgBrush}"
                                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                            <TextBlock Text="{loc:Str programs_return_badge}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>

                                    <TextBlock x:Name="TxtTodayTitle" x:FieldModifier="internal"
                                               Foreground="{DynamicResource TextLightBrush}"
                                               FontSize="34" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <!-- Clamped: unclamped this ran as a 1400px one-line ribbon. -->
                                    <TextBlock x:Name="TxtTodayBlurb" x:FieldModifier="internal"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               FontSize="14" TextWrapping="Wrap" LineHeight="22" Margin="0,10,0,0"
                                               MaxWidth="820" HorizontalAlignment="Left"/>

                                    <!-- ============ What today actually runs ============
                                         The blurb says what today FEELS like ("something pretty
                                         turns up"); this says what the session will DO - the
                                         template's own identity plus one chip per layer it turns
                                         on, with the layers that were not in yesterday's session
                                         marked NEW. That is the escalation made legible, and on a
                                         feature that ramps flash rate, opacity and spiral on a
                                         curve it is also the duty-of-care answer to "what am I
                                         about to sit through" (plan §10.4).
                                         Every value is resolved in code onto a carrier, including
                                         the app's own feature icon - same set, same full colour as
                                         the task cards and the Dashboard.
                                         MaxWidth matches the blurb so the copy column keeps one
                                         edge and the chips never run under the hero art's fade. -->
                                    <StackPanel x:Name="TodayLayersPanel" x:FieldModifier="internal"
                                                Visibility="Collapsed" Margin="0,16,0,0"
                                                MaxWidth="820" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str programs_layers_header}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            <TextBlock Text="  ·  " Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" VerticalAlignment="Center"/>
                                            <TextBlock x:Name="TxtTodayTemplateName" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource PinkBrush}"
                                                       FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="TxtTodayTemplateBlurb" x:FieldModifier="internal"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0"/>

                                        <ItemsControl x:Name="TodayLayerList" x:FieldModifier="internal"
                                                      Margin="0,9,0,0">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="9999" Padding="9,4" Margin="0,0,7,7"
                                                            Background="{DynamicResource SurfaceBgBrush}"
                                                            BorderBrush="{Binding BorderBrush}" BorderThickness="1"
                                                            ToolTip="{Binding Tip}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Image Width="15" Height="15" Margin="0,0,7,0"
                                                                   VerticalAlignment="Center"
                                                                   Source="{Binding Icon}"
                                                                   Visibility="{Binding IconVisibility}"
                                                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                                            <TextBlock Text="{Binding Label}"
                                                                       Foreground="{Binding LabelBrush}"
                                                                       FontSize="11" FontWeight="SemiBold"
                                                                       VerticalAlignment="Center"/>
                                                            <!-- NEW pill: this layer was not in yesterday's
                                                                 session. Accent-filled, so it reads at a
                                                                 glance without a second colour in the theme. -->
                                                            <Border CornerRadius="9999" Padding="5,0" Margin="7,0,0,0"
                                                                    VerticalAlignment="Center"
                                                                    Visibility="{Binding NewVisibility}"
                                                                    Background="{Binding AccentBrush}">
                                                                <TextBlock Text="{loc:Str programs_layer_new}"
                                                                           Foreground="{DynamicResource TextLightBrush}"
                                                                           FontSize="9" FontWeight="Bold"/>
                                                            </Border>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- Today's reward chip (rare - boss days mostly) -->
                                    <Border x:Name="TodayRewardChip" x:FieldModifier="internal" Visibility="Collapsed"
                                            CornerRadius="9999" Padding="10,4" Margin="0,12,0,0"
                                            HorizontalAlignment="Left"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel Orientation="Horizontal">
                                            <helpers:EmojiTextBlock Text="🎁" FontSize="12" VerticalAlignment="Center"
                                                                    Margin="0,0,6,0"/>
                                            <TextBlock Text="{loc:Str programs_day_reward}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="10" FontWeight="Bold" VerticalAlignment="Center"
                                                       Margin="0,0,6,0"/>
                                            <TextBlock x:Name="TxtTodayReward" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center"
                                                       MaxWidth="480" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Day-complete banner. Scale transform is popped from code the
                                         moment the day flips to done (PopProgramScale). -->
                                    <Border x:Name="TodayCompleteBanner" x:FieldModifier="internal" Visibility="Collapsed"
                                            Padding="14,10" CornerRadius="10" Margin="0,14,0,0"
                                            HorizontalAlignment="Left"
                                            Background="{DynamicResource TransparentPinkBrush}"
                                            RenderTransformOrigin="0,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform x:Name="TodayCompleteBannerScale" x:FieldModifier="internal"
                                                            ScaleX="1" ScaleY="1"/>
                                        </Border.RenderTransform>
                                        <StackPanel Orientation="Horizontal">
                                            <helpers:EmojiTextBlock Text="✨" FontSize="14" VerticalAlignment="Center"
                                                                    Margin="0,0,8,0"/>
                                            <TextBlock Text="{loc:Str programs_day_complete}"
                                                       Foreground="{DynamicResource PinkBrush}"
                                                       FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- Hero art: the accent-tinted luminance plate, cropped to fill the
                                     band and faded into the panel on its left edge so it reads as
                                     part of the surface, not a pasted image. Collapses (and the
                                     column zeroes) when no art resolves. -->
                                <Border x:Name="RunHeroHost" x:FieldModifier="internal"
                                        Grid.Column="1" MinHeight="240" Visibility="Collapsed">
                                    <Border.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#00000000" Offset="0"/>
                                            <GradientStop Color="#FF000000" Offset="0.4"/>
                                            <GradientStop Color="#FF000000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.OpacityMask>
                                    <!-- Corners cut for the same reason as the sigil: the art's
                                         residual halo is a square, and a square of accent inside a
                                         rounded panel reads as a bounding box nobody drew. -->
                                    <Rectangle x:Name="RunHeroPlate" x:FieldModifier="internal"
                                               RadiusX="18" RadiusY="18">
                                        <Rectangle.OpacityMask>
                                            <ImageBrush x:Name="RunHeroMask" x:FieldModifier="internal"
                                                        Stretch="UniformToFill"/>
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                </Border>
                            </Grid>

                                <!-- Session slot.
                                     Row 0 is the always-present slot; row 1 is the live bar, shown
                                     only while THIS program's session is running - and then the bar
                                     is the hero: 16px tall, accent fill, a sheen sweeping it (code:
                                     EnsureProgramSessionSheen), the clock large beside the button.
                                     (MainWindow.ProgramsTab.cs / UpdateProgramSessionRow). -->
                                <Border Padding="16,14" CornerRadius="12" Margin="0,16,0,0"
                                        Background="{DynamicResource SurfaceBgBrush}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="TxtTodaySessionGlyph" x:FieldModifier="internal"
                                                   Grid.Row="0" Grid.Column="0" Text="○" FontSize="17" Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextMutedBrush}"/>
                                        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str programs_session_label}"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="14" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="TxtTodaySessionMinutes" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <!-- Live clock. Foreground is set to the program accent in
                                             code; collapsed unless our session is in flight. -->
                                        <TextBlock x:Name="TxtTodaySessionProgress" x:FieldModifier="internal"
                                                   Grid.Row="0" Grid.Column="2" Margin="0,0,18,0"
                                                   VerticalAlignment="Center" Visibility="Collapsed"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   FontSize="20" FontWeight="Bold"/>
                                        <Button x:Name="BtnStartTodaySession" x:FieldModifier="internal"
                                                Grid.Row="0" Grid.Column="3" MinWidth="190"
                                                Content="{loc:Str btn_program_start_session}"
                                                Style="{StaticResource PinkButton}"
                                                Click="BtnStartTodaySession_Click"/>

                                        <!-- Live progress bar (collapsed unless the program's own session is in flight) -->
                                        <Grid x:Name="TodaySessionProgressRow" x:FieldModifier="internal"
                                              Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
                                              Margin="0,14,0,2" Visibility="Collapsed">
                                            <!-- Host owns the track colour and a rounded Clip (set in
                                                 SessionBarHost_SizeChanged) so the sweeping sheen can
                                                 never spill past the bar's rounded ends. -->
                                            <Border x:Name="SessionBarHost" x:FieldModifier="internal"
                                                    Height="16" CornerRadius="8"
                                                    Background="{DynamicResource PanelAccentBrush}"
                                                    SizeChanged="SessionBarHost_SizeChanged">
                                                <Grid>
                                                    <ProgressBar x:Name="TodaySessionProgressBar" x:FieldModifier="internal"
                                                                 Minimum="0" Maximum="100" Value="0"
                                                                 Style="{StaticResource ProgramHeroBar}"/>
                                                    <!-- The sheen: a narrow white gradient swept across
                                                         the bar by a looping TranslateTransform anim
                                                         started/stopped in code with the session and
                                                         the tab's visibility. Opacity 0 at rest. -->
                                                    <Rectangle x:Name="SessionSheen" x:FieldModifier="internal"
                                                               Width="150" HorizontalAlignment="Left"
                                                               Opacity="0" IsHitTestVisible="False">
                                                        <Rectangle.Fill>
                                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                                <GradientStop Color="#00FFFFFF" Offset="0"/>
                                                                <GradientStop Color="#46FFFFFF" Offset="0.5"/>
                                                                <GradientStop Color="#00FFFFFF" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Rectangle.Fill>
                                                        <Rectangle.RenderTransform>
                                                            <TranslateTransform x:Name="SessionSheenSlide" x:FieldModifier="internal"
                                                                                X="-160"/>
                                                        </Rectangle.RenderTransform>
                                                    </Rectangle>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </Border>

                                <!-- Ambient row (only when the day carries one) -->
                                <Border x:Name="TodayAmbientRow" x:FieldModifier="internal" Visibility="Collapsed"
                                        Padding="14,12" CornerRadius="10" Margin="0,8,0,0"
                                        Background="{DynamicResource SurfaceBgBrush}">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str programs_ambient_header}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="11" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="TxtTodayAmbient" x:FieldModifier="internal"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        <TextBlock x:Name="TxtTodayAmbientProgress" x:FieldModifier="internal"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="11" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- ============ Checklist + the arc ============
                                     Tasks are quest cards, not full-width strips. But a day with
                                     ONE task still left a 360px card adrift in a 1700px band, so
                                     the card column is CAPPED in code to the width the cards
                                     actually occupy (3 per row, MainWindow.BuildProgramTodayPanel)
                                     and everything that cap frees goes to the arc: the next three
                                     days, the hour today closes at, and the streak. Both columns
                                     therefore hold real content at one task and at five.

                                     The cap is a ColumnDefinition MaxWidth on a STAR column, not
                                     an Auto column. An Auto column measures the WrapPanel at
                                     infinity, so five cards would lay out in one ~1850px row and
                                     WPF would clip the overflow silently - no scrollbar, no
                                     binding error. A capped star also shrinks below the cap on a
                                     narrow window instead of overflowing the grid. -->
                                <Grid Margin="0,18,0,0">
                                    <Grid.ColumnDefinitions>
                                        <!-- 2:1 so the cards reach their cap on a normal window (a
                                             1:1 split leaves two cards stacked at 1400px wide);
                                             the surplus a capped star does not use flows to the
                                             column beside it, and MinWidth keeps the arc readable
                                             when the window is narrow enough that the cap never
                                             binds at all. -->
                                        <ColumnDefinition x:Name="TaskListColumn" x:FieldModifier="internal"
                                                          Width="2*" MaxWidth="370"/>
                                        <ColumnDefinition Width="1*" MinWidth="240"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{loc:Str programs_tasks_header}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            <!-- Done counter: a lone card is a checklist of one, not
                                                 an orphan, once the set it belongs to is stated. -->
                                            <Border x:Name="TodayTasksDonePill" x:FieldModifier="internal"
                                                    Grid.Column="2" Visibility="Collapsed"
                                                    CornerRadius="9999" Padding="8,2" Margin="10,0,10,0"
                                                    Background="{DynamicResource SurfaceBgBrush}"
                                                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                                <TextBlock x:Name="TxtTodayTasksDone" x:FieldModifier="internal"
                                                           Foreground="{DynamicResource TextMutedBrush}"
                                                           FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>

                                <TextBlock x:Name="TxtTodayNoTasks" x:FieldModifier="internal" Visibility="Collapsed"
                                           Text="{loc:Str programs_no_tasks}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           FontSize="12" Margin="2,0,0,0"/>

                                <ItemsControl x:Name="TodayTaskList" x:FieldModifier="internal">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="360" MinHeight="92" Margin="0,0,10,10" Padding="14,12"
                                                    CornerRadius="12"
                                                    Opacity="{Binding RowOpacity}"
                                                    Background="{DynamicResource SurfaceBgBrush}"
                                                    BorderBrush="{Binding CardBorderBrush}" BorderThickness="1"
                                                    RenderTransformOrigin="0.5,0.5">
                                                <Border.RenderTransform>
                                                    <ScaleTransform x:Name="TaskCardScale" ScaleX="1" ScaleY="1"/>
                                                </Border.RenderTransform>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- The app's own product icon for whatever verifies
                                                         this task. Full-colour on purpose: the Dashboard
                                                         already shows this exact set full-colour under
                                                         every mod. When the task has no feature behind
                                                         it, the status glyph carries the slot instead. -->
                                                    <Image Grid.Column="0" Grid.RowSpan="2" Width="38" Height="38"
                                                           Margin="0,0,12,0" VerticalAlignment="Center"
                                                           Source="{Binding Icon}"
                                                           Visibility="{Binding IconVisibility}"
                                                           hover:HoverPop.IsEnabled="True"
                                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Grid.Column="0" Grid.RowSpan="2" Text="{Binding StatusGlyph}"
                                                               Visibility="{Binding GlyphVisibility}"
                                                               Foreground="{Binding StatusBrush}"
                                                               FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>

                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Description}"
                                                                   Foreground="{Binding TextBrush}"
                                                                   FontSize="13" FontWeight="SemiBold"
                                                                   TextWrapping="Wrap"/>
                                                        <!-- Mechanics line: which feature verifies this task and
                                                             what has to happen. The description above stays in the
                                                             program's voice; this one is deliberately literal. -->
                                                        <TextBlock Text="{Binding HowTo}"
                                                                   Visibility="{Binding HowToVisibility}"
                                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                                   FontSize="11" TextWrapping="Wrap"
                                                                   Margin="0,3,0,0"/>
                                                        <Border CornerRadius="9999" Padding="7,1" Margin="0,4,0,0"
                                                                HorizontalAlignment="Left"
                                                                Visibility="{Binding BadgeVisibility}"
                                                                Background="{DynamicResource ElevatedSurfaceBrush}">
                                                            <TextBlock Text="{Binding BadgeText}"
                                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                    </StackPanel>

                                                    <!-- Done chip, top-right - the battle-pass claim tick. -->
                                                    <Border Grid.Column="2" Width="22" Height="22" CornerRadius="11"
                                                            VerticalAlignment="Top" HorizontalAlignment="Right"
                                                            Visibility="{Binding DoneChipVisibility}"
                                                            Background="{Binding StatusBrush}">
                                                        <TextBlock Text="✓" Foreground="{DynamicResource TextLightBrush}"
                                                                   FontSize="12" FontWeight="Bold"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>

                                                    <!-- Mini progress bar for counted tasks -->
                                                    <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                                          Margin="0,10,0,0" Visibility="{Binding BarVisibility}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Grid Grid.Column="0" Height="6" VerticalAlignment="Center">
                                                            <Border CornerRadius="3"
                                                                    Background="{DynamicResource PanelAccentBrush}"/>
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="{Binding ProgressStar}"/>
                                                                    <ColumnDefinition Width="{Binding RemainderStar}"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Border Grid.Column="0" CornerRadius="3"
                                                                        Background="{Binding AccentBrush}"/>
                                                            </Grid>
                                                        </Grid>
                                                        <TextBlock Grid.Column="1" Text="{Binding ProgressText}"
                                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                                   FontSize="11" FontWeight="SemiBold"
                                                                   Margin="10,0,0,0" VerticalAlignment="Center"/>
                                                    </Grid>

                                                    <Button Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                                            MinWidth="120" Margin="0,10,0,0"
                                                            HorizontalAlignment="Left"
                                                            Visibility="{Binding SubmitVisibility}"
                                                            Content="{loc:Str btn_program_submit_photo}"
                                                            Tag="{Binding TaskId}"
                                                            Style="{StaticResource SecondaryButton}"
                                                            Click="BtnProgramSubmitRitual_Click"/>
                                                </Grid>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <!-- One-shot pop when a task flips to complete. The
                                                     carrier's JustCompleted is true only on the rebuild
                                                     immediately after the flip (code diffs against the
                                                     seen-incomplete set), so this cannot replay. -->
                                                <DataTrigger Binding="{Binding JustCompleted}" Value="True">
                                                    <DataTrigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="TaskCardScale"
                                                                                 Storyboard.TargetProperty="ScaleX"
                                                                                 From="0.93" To="1" Duration="0:0:0.35">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <BackEase EasingMode="EaseOut" Amplitude="0.6"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation Storyboard.TargetName="TaskCardScale"
                                                                                 Storyboard.TargetProperty="ScaleY"
                                                                                 From="0.93" To="1" Duration="0:0:0.35">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <BackEase EasingMode="EaseOut" Amplitude="0.6"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.EnterActions>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock x:Name="TxtRitualPrivacyNote" x:FieldModifier="internal" Visibility="Collapsed"
                                           Text="{loc:Str programs_photo_local_note}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           FontSize="11" FontStyle="Italic" Margin="2,2,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <!-- The arc, beside the checklist. Three things the run view
                                         could not say before: what the next days are (the
                                         pre-commitment beat - the enrollment ceremony already
                                         shows the whole arc, so nothing here is a new spoiler),
                                         the hour today closes at (the single mechanic that
                                         actually costs people a day), and the streak. Top-aligned
                                         so a two-row card list does not stretch it. -->
                                    <Border Grid.Column="1" Margin="14,0,0,0" Padding="14,12"
                                            VerticalAlignment="Top" CornerRadius="12"
                                            Background="{DynamicResource SurfaceBgBrush}"
                                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str programs_next_header}"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" FontWeight="SemiBold" Margin="0,0,0,10"/>

                                            <ItemsControl x:Name="TodayUpNextList" x:FieldModifier="internal">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <!-- Title and meta live in a STAR column, not a
                                                             horizontal StackPanel: a StackPanel measures
                                                             its children at infinite width, so
                                                             TextTrimming would never engage and a long
                                                             authored title would be clipped instead. -->
                                                        <Grid Margin="0,0,0,10" Opacity="{Binding RowOpacity}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Border Grid.Column="0" MinWidth="48" CornerRadius="6"
                                                                    Padding="7,3" Margin="0,0,10,0"
                                                                    VerticalAlignment="Top"
                                                                    Background="{DynamicResource ElevatedSurfaceBrush}">
                                                                <TextBlock Text="{Binding DayLabel}"
                                                                           Foreground="{Binding DayBrush}"
                                                                           FontSize="10" FontWeight="Bold"
                                                                           HorizontalAlignment="Center"/>
                                                            </Border>
                                                            <StackPanel Grid.Column="1">
                                                                <TextBlock Text="{Binding Title}"
                                                                           Foreground="{DynamicResource TextLightBrush}"
                                                                           FontSize="12" FontWeight="SemiBold"
                                                                           TextTrimming="CharacterEllipsis"/>
                                                                <TextBlock Text="{Binding Meta}"
                                                                           Foreground="{DynamicResource TextMutedBrush}"
                                                                           FontSize="11" Margin="0,2,0,0"
                                                                           TextTrimming="CharacterEllipsis"/>
                                                            </StackPanel>
                                                            <helpers:EmojiTextBlock Grid.Column="2" Text="{Binding Glyph}"
                                                                                    FontSize="12" Margin="8,0,0,0"
                                                                                    VerticalAlignment="Top"
                                                                                    ToolTip="{Binding GlyphTip}"
                                                                                    Visibility="{Binding GlyphVisibility}"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>

                                            <!-- Shown on the last day, where there is no "next". -->
                                            <TextBlock x:Name="TxtTodayUpNextFinal" x:FieldModifier="internal"
                                                       Visibility="Collapsed"
                                                       Text="{loc:Str programs_next_final}"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="12" TextWrapping="Wrap"/>

                                            <Border Height="1" Margin="0,4,0,10"
                                                    Background="{DynamicResource GlassBorderBrush}"/>

                                            <TextBlock x:Name="TxtTodayCloses" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextLightBrush}"
                                                       FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock x:Name="TxtTodayClosesNote" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"/>
                                            <TextBlock x:Name="TxtTodayStreak" x:FieldModifier="internal"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- ================================================================= -->
                    <!-- LAPSED: one screen, two buttons, no editorialising                 -->
                    <!-- ================================================================= -->
                    <StackPanel x:Name="ProgramsLapsedPanel" x:FieldModifier="internal" Visibility="Collapsed">
                        <Border Padding="24,22" CornerRadius="12"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{loc:Str programs_lapsed_title}"
                                           Foreground="{DynamicResource TextLightBrush}"
                                           FontSize="18" FontWeight="Bold"/>
                                <TextBlock x:Name="TxtLapsedBody" x:FieldModifier="internal"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           FontSize="13" TextWrapping="Wrap" LineHeight="20"
                                           Margin="0,10,0,0" MaxWidth="620" HorizontalAlignment="Left"/>
                                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                    <Button x:Name="BtnProgramRestart" x:FieldModifier="internal"
                                            Content="{loc:Str btn_program_restart}"
                                            Style="{StaticResource PinkButton}" MinWidth="150"
                                            Click="BtnProgramRestart_Click"/>
                                    <Button Content="{loc:Str btn_program_withdraw}"
                                            Style="{StaticResource SecondaryButton}" MinWidth="120"
                                            Margin="10,0,0,0" Click="BtnProgramWithdraw_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- ================================================================= -->
                    <!-- GRADUATED                                                          -->
                    <!-- ================================================================= -->
                    <StackPanel x:Name="ProgramsGraduatedPanel" x:FieldModifier="internal" Visibility="Collapsed">
                        <Border Padding="24,22" CornerRadius="12"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="22" Height="22" Margin="0,0,8,0" VerticalAlignment="Center"
                                           Source="{Binding Source='🏆', Converter={StaticResource EmojiToImageSource}}"/>
                                    <TextBlock Text="{loc:Str programs_graduated_title}"
                                               Foreground="{DynamicResource PinkBrush}"
                                               FontSize="19" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock x:Name="TxtGraduatedSub" x:FieldModifier="internal"
                                           Foreground="{DynamicResource TextLightBrush}"
                                           FontSize="14" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                <TextBlock x:Name="TxtGraduatedStats" x:FieldModifier="internal"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                <Button x:Name="BtnProgramDismissGraduated" x:FieldModifier="internal"
                                        Content="{loc:Str btn_program_dismiss}"
                                        Style="{StaticResource PinkButton}" MinWidth="130"
                                        HorizontalAlignment="Left" Margin="0,20,0,0"
                                        Click="BtnProgramDismissGraduated_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
