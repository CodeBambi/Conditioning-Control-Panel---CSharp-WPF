<UserControl x:Class="ConditioningControlPanel.Views.Controls.Companion.CompanionHeroCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ConditioningControlPanel.Views.Controls.Companion"
             mc:Ignorable="d"
             d:DesignWidth="1160" d:DesignHeight="360"
             d:DataContext="{d:DesignInstance Type=local:MockCompanionHeroCardVm, IsDesignTimeCreatable=True}">

    <!-- ================================================================
         Z0 header band + Z1 THE COMPANION CARD (hero).
         The Trainer Card recipe applied to her: banner + left-dark scrim,
         portrait ring, identity stack, status pills, XP, quick actions,
         gold level plate, and the constellation band along the bottom.

         Traps honoured here:
          · The banner is a Border + brush, NEVER an <Image>. An
            unconstrained Image in a StackPanel/ScrollViewer measures to
            its natural size and blew the profile hero to 1383px.
          · The XP fill is a star-width column — no ActualWidth maths.
          · The mini toggles bind IsChecked OneWay and act through their
            command. IsMuted / IsCompanionShown are read-only on the
            interface, so a TwoWay binding would either fail silently or
            fight the command and undo the user's click.
          · This control owns the tab's ONE ambient loop (the portrait
            breathe). Nothing else here may use RepeatBehavior=Forever,
            and the loop is parked entirely while she is disabled.

         Free / logged-out users see this card in full — barks are free,
         and the page must never look broken. Only the header plate dims
         (behind a Vault teaser ribbon) and only Z2 locks.
         ================================================================ -->

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ConditioningControlPanel;component/Views/Controls/Companion/Themes/CompanionTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Local-only styles. Anything the whole page would want belongs in the shared
                 theme; these three are the header band's private jewelry. -->
            <Style x:Key="CmpHeroTierPlateTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource CmpTextBrush}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- .tierplate, but it dims itself when the user has no AI entitlement. -->
            <Style x:Key="CmpHeroAiPlateStyle" TargetType="Border" BasedOn="{StaticResource CmpTierPlateStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding HasAiAccess}" Value="False">
                        <Setter Property="Opacity" Value="0.28"/>
                        <Setter Property="Effect" Value="{x:Null}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- the Velvet-Vault sell, shrunk to a ribbon: gradient chip + lock glow, and it is a
                 button, not a veil — the header never blocks anything. -->
            <Style x:Key="CmpHeroTeaserRibbonStyle" TargetType="ButtonBase"
                   BasedOn="{StaticResource CmpCtaPillStyle}">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Padding" Value="12,5,12,5"/>
                <Setter Property="Margin" Value="8,0,0,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================================================================
             Z0 — HEADER BAND. Collapses whole when the host supplies no
             header viewmodel (ICompanionHeroCardVm.Header defaults to null),
             so a page that draws its own title never gets two.
             ================================================================ -->
        <!-- The visibility binding lives on this outer Grid because the inner one re-points its
             DataContext at Header: an element's own DataContext wins for every binding on it, so
             the two cannot share a level. -->
        <Grid Grid.Row="0" x:Name="HeaderBand" Margin="0,0,0,18"
              Visibility="{Binding Header, Converter={StaticResource CmpHasContentToVis}}">
        <Grid DataContext="{Binding Header}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" Style="{StaticResource CmpPageTitleStyle}"/>
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource CmpPageSubtitleStyle}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                        Margin="16,0,0,0">
                <Button Style="{StaticResource CmpChipButtonStyle}" Command="{Binding TutorialCommand}"
                        Margin="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="13" Height="13" Margin="0,0,6,0" VerticalAlignment="Center"
                               Source="{Binding Source='🎓', Converter={StaticResource CmpEmojiToImage}}"/>
                        <TextBlock Text="{Binding TutorialLabel}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- the AI entitlement plate: the tab-level "is she awake in the cloud?" glance -->
                <Border Style="{StaticResource CmpHeroAiPlateStyle}">
                    <TextBlock Text="{Binding AiPlateLabel}" Style="{StaticResource CmpHeroTierPlateTextStyle}"/>
                </Border>

                <!-- the tier above is always dim: aspiration, not a lock -->
                <Border Style="{StaticResource CmpTierPlateDimStyle}">
                    <TextBlock Text="{Binding NextTierPlateLabel}" Style="{StaticResource CmpHeroTierPlateTextStyle}"/>
                </Border>

                <Button Style="{StaticResource CmpHeroTeaserRibbonStyle}"
                        Command="{Binding OpenPatreonCommand}"
                        Visibility="{Binding HasAiAccess, Converter={StaticResource CmpBoolToVisInverse}}">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="11" Height="11" Margin="0,0,6,0" VerticalAlignment="Center"
                               Source="{Binding Source='🔒', Converter={StaticResource CmpEmojiToImage}}"/>
                        <TextBlock Text="{Binding TeaserRibbonLabel}" VerticalAlignment="Center"
                                   FontWeight="Bold" Foreground="{StaticResource CmpTextBrush}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
        </Grid>

        <!-- ================================================================
             Z1 — THE COMPANION CARD
             ================================================================ -->
        <Border Grid.Row="1" CornerRadius="{StaticResource CmpRadiusCard}" ClipToBounds="True"
                Background="{StaticResource CmpCardBrush}"
                BorderBrush="{StaticResource CmpCardBorderBrush}" BorderThickness="1"
                Effect="{StaticResource CmpHeroCardShadow}">
            <Grid>
                <!-- ========== banner + scrim ========== -->
                <Border Background="{StaticResource CmpHeroBannerBrush}" Opacity="0.9"/>
                <Border Background="{StaticResource CmpRoomGlowPinkBrush}"/>
                <Border Background="{StaticResource CmpHeroScrimBrush}"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- ========== upper band: portrait | identity | rail ==========
                         MinHeight fixes the strip so a short flavor line in one language and a
                         wrapped one in another do not give the hero two different shapes. -->
                    <Grid Grid.Row="0" Margin="22,20,22,14" MinHeight="176">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- ===== portrait: ring, hole, bust, shine, presence dot ===== -->
                        <Grid Grid.Column="0" Width="138" Height="138" Margin="0,0,20,0"
                              VerticalAlignment="Top"
                              Opacity="{Binding IsCompanionEnabled, Converter={StaticResource CmpBoolToOpacity},
                                                ConverterParameter=1.0|0.45}">
                            <!-- The ring. Its ScaleTransform is the one Forever animation on this
                                 tab; when she is disabled it goes flat gray and the code-behind
                                 parks the clock, matching the mockup's animation:none. -->
                            <Ellipse x:Name="PortraitRing" RenderTransformOrigin="0.5,0.5">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Effect" Value="{StaticResource CmpRingGlow}"/>
                                        <Setter Property="Fill">
                                            <Setter.Value>
                                                <!-- WPF has no conic gradient; the mockup's sweep
                                                     reads the same as a four-stop diagonal. -->
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#FFFF69B4" Offset="0"/>
                                                    <GradientStop Color="#FFB478FF" Offset="0.35"/>
                                                    <GradientStop Color="#FFFF8FCB" Offset="0.65"/>
                                                    <GradientStop Color="#FF9370DB" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompanionEnabled}" Value="False">
                                                <Setter Property="Fill" Value="#FF3A3552"/>
                                                <Setter Property="Effect" Value="{x:Null}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                                <Ellipse.RenderTransform>
                                    <ScaleTransform x:Name="PortraitRingScale" ScaleX="1" ScaleY="1"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>

                            <!-- the hole -->
                            <Ellipse Margin="3">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF0E0A1C" Offset="0"/>
                                        <GradientStop Color="#FF1B0F2A" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>

                            <!-- The placeholder bust, drawn the way the mockup draws it: a head
                                 disc, a shoulder cap over it, one specular dot. Vector only, so it
                                 is DPI-safe and needs no art file; it yields to the real companion
                                 portrait the moment there is one. -->
                            <Grid Width="132" Height="132" Margin="3"
                                  Visibility="{Binding Portrait, Converter={StaticResource CmpEmptyToVis}}">
                                <Grid.Clip>
                                    <EllipseGeometry Center="66,66" RadiusX="66" RadiusY="66"/>
                                </Grid.Clip>

                                <Ellipse Width="69" Height="69" HorizontalAlignment="Center"
                                         VerticalAlignment="Top" Margin="0,21,0,0">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush Center="0.38,0.30" GradientOrigin="0.38,0.30">
                                            <GradientStop Color="#FFFFC4E4" Offset="0"/>
                                            <GradientStop Color="#FFFF8FCB" Offset="0.45"/>
                                            <GradientStop Color="#FFC86BD9" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#FFFF69B4" BlurRadius="18" ShadowDepth="0" Opacity="0.40"/>
                                    </Ellipse.Effect>
                                </Ellipse>

                                <Border Width="127" Height="69" HorizontalAlignment="Center"
                                        VerticalAlignment="Bottom" Margin="0,0,0,-16"
                                        CornerRadius="63,63,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FFB478FF" Offset="0"/>
                                            <GradientStop Color="#FF7A4FC9" Offset="0.6"/>
                                            <GradientStop Color="#FF4A2B85" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <Ellipse Width="12" Height="12" HorizontalAlignment="Left"
                                         VerticalAlignment="Top" Margin="32,26,0,0" Opacity="0.85">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.7"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Grid>

                            <!-- the real portrait, when there is one -->
                            <Ellipse Margin="3" Visibility="{Binding Portrait, Converter={StaticResource CmpHasContentToVis}}">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding Portrait}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>

                            <!-- presence dot = AI state. Pink when the provider is live, gray when
                                 off, gone entirely when she is asleep (there is no presence to
                                 report). -->
                            <Ellipse x:Name="PresenceDot" Width="20" Height="20"
                                     HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                     Margin="0,0,6,8" Stroke="{StaticResource CmpCardBrush}" StrokeThickness="3">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#FF4A4560"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAiLive}" Value="True">
                                                <Setter Property="Fill">
                                                    <Setter.Value>
                                                        <RadialGradientBrush Center="0.35,0.35" GradientOrigin="0.35,0.35">
                                                            <GradientStop Color="#FFFFB3DB" Offset="0"/>
                                                            <GradientStop Color="#FFFF69B4" Offset="1"/>
                                                        </RadialGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#FFFF69B4" BlurRadius="12"
                                                                          ShadowDepth="0" Opacity="0.9"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCompanionEnabled}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>

                        <!-- ===== identity stack ===== -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="0,2,16,0">

                            <!-- name + mod chip + mood token -->
                            <WrapPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" FontFamily="/Fonts/#Fredoka" FontWeight="SemiBold"
                                           FontSize="31" Foreground="White" VerticalAlignment="Center"
                                           Margin="0,0,11,0" Effect="{StaticResource CmpNameGlow}"/>

                                <Border Style="{StaticResource CmpModChipStyle}" Margin="0,0,11,0">
                                    <TextBlock Text="{Binding ModName}" Style="{StaticResource CmpModChipTextStyle}"/>
                                </Border>

                                <!-- Daily mood token. Pre-Train 4 the view substitutes a sleeping
                                     moon on a cool orb at 40% and the tooltip makes the promise —
                                     present and dormant, never a hidden control. -->
                                <Border x:Name="MoodToken" CornerRadius="{StaticResource CmpRadiusPill}"
                                        Background="#FF2A1A3A" BorderBrush="#55FF69B4" BorderThickness="1"
                                        Padding="6,4,12,4" VerticalAlignment="Center"
                                        ToolTip="{Binding MoodCaption}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Opacity" Value="0.4"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMoodLive}" Value="True">
                                                    <Setter Property="Opacity" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="22" Height="22" CornerRadius="11" Margin="0,0,7,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <RadialGradientBrush Center="0.35,0.30" GradientOrigin="0.35,0.30">
                                                                <GradientStop Color="#FF9C93C4" Offset="0"/>
                                                                <GradientStop Color="#FF5C5480" Offset="1"/>
                                                            </RadialGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMoodLive}" Value="True">
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <RadialGradientBrush Center="0.35,0.30" GradientOrigin="0.35,0.30">
                                                                        <GradientStop Color="#FFFFD27A" Offset="0"/>
                                                                        <GradientStop Color="#FFFF8C42" Offset="1"/>
                                                                    </RadialGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Setter Property="Effect">
                                                                <Setter.Value>
                                                                    <DropShadowEffect Color="#FFFF8C42" BlurRadius="10"
                                                                                      ShadowDepth="0" Opacity="0.55"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <!-- Twemoji covers 🌙 but not the symbol glyphs a mood may
                                                 use (✧ has no emoji file), so the image carries a
                                                 text fallback that shows only when the lookup missed. -->
                                            <Grid>
                                                <Image x:Name="MoodGlyphImage" Width="12" Height="12" Stretch="Uniform">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <!-- dormant: the sleeping moon, per design §3 Z1 -->
                                                            <Setter Property="Source"
                                                                    Value="{Binding Source='🌙', Converter={StaticResource CmpEmojiToImage}}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMoodLive}" Value="True">
                                                                    <Setter Property="Source"
                                                                            Value="{Binding MoodGlyph, Converter={StaticResource CmpEmojiToImage}}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           FontSize="12" Foreground="White"
                                                           Visibility="{Binding Source, ElementName=MoodGlyphImage,
                                                                                Converter={StaticResource CmpEmptyToVis}}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="☾"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMoodLive}" Value="True">
                                                                    <Setter Property="Text" Value="{Binding MoodGlyph}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding MoodWord}" FontSize="11.5" FontWeight="Bold"
                                                       Foreground="#FFFFD9A8"/>
                                            <!-- The dormant token is a moon and a word; its promise
                                                 belongs in the tooltip, not spread across the name
                                                 row where it competes with her name. -->
                                            <TextBlock Text="{Binding MoodCaption}" FontSize="9.5"
                                                       Foreground="{StaticResource CmpFaintBrush}"
                                                       MaxWidth="150" TextTrimming="CharacterEllipsis"
                                                       Visibility="{Binding IsMoodLive, Converter={StaticResource CmpBoolToVis}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>

                            <!-- one-line flavor, or the asleep copy when she is disabled -->
                            <TextBlock Text="{Binding Flavor}" Margin="0,7,0,0" FontSize="13"
                                       Foreground="{StaticResource CmpDimBrush}" TextWrapping="Wrap"
                                       MaxHeight="38" TextTrimming="CharacterEllipsis"
                                       Visibility="{Binding IsCompanionEnabled, Converter={StaticResource CmpBoolToVis}}"/>

                            <WrapPanel Orientation="Horizontal" Margin="0,7,0,0"
                                       Visibility="{Binding IsCompanionEnabled, Converter={StaticResource CmpBoolToVisInverse}}">
                                <TextBlock Text="{Binding AsleepCopy}" Style="{StaticResource CmpFlavorTextStyle}"
                                           FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Button Content="{local:CmpStr companion_hero_wake}" Style="{StaticResource CmpCtaPillStyle}"
                                        Command="{Binding WakeCommand}" VerticalAlignment="Center"/>
                            </WrapPanel>

                            <!-- status pills: both are deep-links now -->
                            <WrapPanel Orientation="Horizontal" Margin="0,11,0,0">
                                <Button Content="{Binding AiPillText}" Command="{Binding OpenEngineRoomCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource CmpAiPillStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAiLive}" Value="False">
                                                    <Setter Property="Background" Value="#FF1B1B2C"/>
                                                    <Setter Property="BorderBrush" Value="#40FFFFFF"/>
                                                    <Setter Property="Foreground" Value="{StaticResource CmpMutedBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="{Binding AwarenessPillText}" Command="{Binding FocusAwarenessCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource CmpAwarenessPillStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAwarenessOpen}" Value="False">
                                                    <Setter Property="Background" Value="#FF1B1B2C"/>
                                                    <Setter Property="BorderBrush" Value="#40FFFFFF"/>
                                                    <Setter Property="Foreground" Value="{StaticResource CmpMutedBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </WrapPanel>

                            <!-- Companion XP. The bar is capped by a star column with a MaxWidth
                                 rather than by the element's own MaxWidth: WPF degrades a Stretch
                                 element that cannot fill its slot to Center, and HorizontalAlignment
                                 Left would instead collapse the track to its (zero) desired width.
                                 A greedy star column that stops growing at 430 does what the mockup
                                 draws, at every tab width. -->
                            <Grid Margin="0,13,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" MaxWidth="430"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Style="{StaticResource CmpGaugeTrackStyle}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="{Binding XpFraction, Converter={StaticResource CmpFractionToStar}}"/>
                                                <ColumnDefinition Width="{Binding XpFraction, Converter={StaticResource CmpFractionToRemainderStar}}"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Style="{StaticResource CmpGaugeFillStyle}"/>
                                        </Grid>
                                    </Border>
                                    <Grid Grid.Row="1">
                                        <TextBlock Text="{Binding XpLabel}" Style="{StaticResource CmpGaugeLabelStyle}"
                                                   HorizontalAlignment="Left"/>
                                        <TextBlock Text="{Binding NextLevelLabel}" Style="{StaticResource CmpGaugeLabelStyle}"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </StackPanel>

                        <!-- ===== right rail: chat CTA, mini chips, level plate ===== -->
                        <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <Button Command="{Binding ChatCommand}" Style="{StaticResource CmpCtaButtonStyle}"
                                    HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="15" Height="15" Margin="0,0,8,0" VerticalAlignment="Center"
                                           Source="{Binding Source='💬', Converter={StaticResource CmpEmojiToImage}}"/>
                                    <TextBlock Text="{local:CmpStr companion_hero_chat}" VerticalAlignment="Center" FontWeight="Bold"
                                               Foreground="White" FontSize="13"/>
                                    <!-- live shortcut only; its editor lives in the Workshop -->
                                    <Border Background="#30000000" CornerRadius="5" Padding="6,2,6,2" Margin="9,0,0,0"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding ChatShortcutHint, Converter={StaticResource CmpHasContentToVis}}">
                                        <TextBlock Text="{Binding ChatShortcutHint}" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="White" Opacity="0.8"/>
                                    </Border>
                                </StackPanel>
                            </Button>

                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                <Button Content="{local:CmpStr companion_hero_switch}" Style="{StaticResource CmpChipButtonStyle}"
                                        Command="{Binding SwitchCommand}"/>
                                <Button Content="{local:CmpStr companion_hero_detach}" Style="{StaticResource CmpChipButtonStyle}"
                                        Command="{Binding DetachCommand}"/>

                                <!-- OneWay + command: the interface exposes these as read-only, and a
                                     TwoWay binding here would race the command and undo the click. -->
                                <ToggleButton Style="{StaticResource CmpChipButtonStyle}"
                                              IsChecked="{Binding IsCompanionShown, Mode=OneWay}"
                                              Command="{Binding ToggleShownCommand}"
                                              ToolTip="{local:CmpStr companion_hero_show_tip}">
                                    <Image Width="13" Height="13">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Source"
                                                        Value="{Binding Source='👁', Converter={StaticResource CmpEmojiToImage}}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompanionShown}" Value="False">
                                                        <Setter Property="Source"
                                                                Value="{Binding Source='🙈', Converter={StaticResource CmpEmojiToImage}}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                </ToggleButton>

                                <ToggleButton Style="{StaticResource CmpChipButtonStyle}" Margin="0"
                                              IsChecked="{Binding IsMuted, Mode=OneWay}"
                                              Command="{Binding ToggleMuteCommand}"
                                              ToolTip="{local:CmpStr companion_hero_mute_tip}">
                                    <Image Width="13" Height="13">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Source"
                                                        Value="{Binding Source='🔊', Converter={StaticResource CmpEmojiToImage}}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                                        <Setter Property="Source"
                                                                Value="{Binding Source='🔇', Converter={StaticResource CmpEmojiToImage}}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                </ToggleButton>
                            </WrapPanel>

                            <Border Style="{StaticResource CmpGoldPlateStyle}" HorizontalAlignment="Right"
                                    Margin="0,12,0,0">
                                <StackPanel>
                                    <TextBlock Text="{local:CmpStr companion_hero_level_plate}" Style="{StaticResource CmpGoldPlateKeyStyle}"/>
                                    <TextBlock Text="{Binding Level}" Style="{StaticResource CmpGoldPlateValueStyle}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- ========== constellation band ========== -->
                    <local:RelationshipConstellation Grid.Row="1" x:Name="ConstellationBand"
                                                     DataContext="{Binding Constellation}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
