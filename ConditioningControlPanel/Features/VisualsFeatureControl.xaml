<UserControl x:Class="ConditioningControlPanel.Features.VisualsFeatureControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
             xmlns:helpers="clr-namespace:ConditioningControlPanel.Helpers"
             xmlns:feat="clr-namespace:ConditioningControlPanel.Features">

    <!-- Velvet Kit 2 density pass. The root stays a StackPanel: MainWindow.SessionFeatureLock.cs
         inserts the session-lock banner at index 0 of this panel. -->
    <StackPanel>

        <!-- HERO. No art (Resources/features has no visuals plate) and no enable pill: Visuals is
             the one module with no master toggle at all - StudioTabView deliberately gives its
             rack row no state dot for the same reason. Title only. -->
        <Border Height="72" CornerRadius="16" Background="{DynamicResource SurfaceBgBrush}"
                BorderBrush="{StaticResource SectionHueStudioBorderBrush}" BorderThickness="2" Margin="0,0,0,10">
            <Grid>
                <StackPanel VerticalAlignment="Center" Margin="18,0,0,0">
                    <helpers:EmojiTextBlock Text="{loc:Str section_visuals}" FontSize="17" FontWeight="Bold"
                                            Foreground="{StaticResource TextLightBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- VELVET KIT 2 · ROUND 3 · DENSITY. The settings column is capped (3*, MaxWidth 780) so a
             slider stops being a ~1200px runway in the wide Studio detail pane, and the width that
             buys back becomes ONE big art card instead of dead margin. Everything below column 0 is
             the previous page contents moved verbatim - same names, handlers, lock markers and loc
             keys. Below 700px (the 520px feature popup) the art column collapses to zero and the
             settings retake the full width: Features/AdaptiveSideArt.cs. -->
        <Grid feat:AdaptiveSideArt.CollapseBelow="700">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" MaxWidth="780"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <!-- ONE dense card -->
                <Border Style="{StaticResource SettingCardStyle}">
                    <Grid>
                        <Border Background="{StaticResource SectionHueStudioWashBrush}" CornerRadius="11"/>
                        <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                                Background="{StaticResource SectionHueStudioBrush}"/>
                        <StackPanel Margin="16,8,16,8">

                            <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_scale_of_flash_images_100_original_size_200_d}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="56"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{loc:Str setting_size}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                <Slider Grid.Column="1" x:Name="SliderSize" Minimum="50" Maximum="250" Value="100"
                                        feat:SessionLock.Owned="True" VerticalAlignment="Center" Margin="14,0"
                                        ValueChanged="SliderSize_Changed"/>
                                <TextBlock Grid.Column="2" x:Name="TxtSize" Text="100%" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold"/>
                            </Grid>

                            <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_transparency_of_flash_images_lower_more_see_t}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="56"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{loc:Str setting_opacity}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                <Slider Grid.Column="1" x:Name="SliderOpacity" Minimum="10" Maximum="100" Value="100"
                                        feat:SessionLock.Owned="True" VerticalAlignment="Center" Margin="14,0"
                                        ValueChanged="SliderOpacity_Changed"/>
                                <TextBlock Grid.Column="2" x:Name="TxtOpacity" Text="100%" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold"/>
                            </Grid>

                            <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_fade_in_out_animation_speed_0_instant_appear}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="56"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{loc:Str setting_fade}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                <Slider Grid.Column="1" x:Name="SliderFade" Minimum="0" Maximum="100" Value="50"
                                        VerticalAlignment="Center" Margin="14,0"
                                        ValueChanged="SliderFade_Changed"/>
                                <TextBlock Grid.Column="2" x:Name="TxtFade" Text="50%" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold"/>
                            </Grid>

                            <Grid Height="36" Background="Transparent" ToolTip="{loc:Str tooltip_how_long_images_stay_on_screen_only_used_when}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="56"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{loc:Str setting_duration}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                <Slider Grid.Column="1" x:Name="SliderDuration" Minimum="1" Maximum="30" Value="5"
                                        feat:SessionLock.Owned="True" VerticalAlignment="Center" Margin="14,0"
                                        ValueChanged="SliderDuration_Changed"/>
                                <TextBlock Grid.Column="2" x:Name="TxtDuration" Text="5s" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource GlassBorderBrush}" Margin="0,6"/>

                            <!-- Link to Audio.
                                 Deliberately NOT SessionLock.Owned, even though SessionSettings.FlashAudioEnabled
                                 is a session-prescribed enable. Killing sound is the one change a user most
                                 often needs to make RIGHT NOW and for reasons outside the session (someone
                                 walked in). That puts it on the safety side of the line, like volume. -->
                            <Grid Height="34" Background="Transparent" ToolTip="{loc:Str tooltip_when_enabled_flash_duration_matches_audio_len}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <helpers:EmojiTextBlock Grid.Column="0" Text="{loc:Str label_link_to_audio_trigger}" FontSize="13" FontWeight="SemiBold"
                                                        Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                                <CheckBox Grid.Column="1" x:Name="ChkAudio"
                                          Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                          Checked="ChkAudio_Changed" Unchecked="ChkAudio_Changed"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- SIDE CARD, no art: this feature has no plate in Resources/features and round 3 invents
                 no assets. The page's own glyph, huge and nearly transparent, on the section wash. -->
            <Border Grid.Column="1" Margin="10,0,0,0" CornerRadius="14"
                    BorderThickness="2" BorderBrush="{StaticResource VelvetCardBorderBrush}"
                    Background="{StaticResource SectionHueStudioWashBrush}"
                    VerticalAlignment="Stretch" MinHeight="200">
                <Image Width="120" Height="120" Opacity="0.18"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Source="{Binding Source='👁', Converter={StaticResource EmojiToImageSource}}"/>
            </Border>
        </Grid>

    </StackPanel>
</UserControl>
