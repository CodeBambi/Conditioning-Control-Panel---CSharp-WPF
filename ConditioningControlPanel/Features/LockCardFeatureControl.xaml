<UserControl x:Class="ConditioningControlPanel.Features.LockCardFeatureControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
             xmlns:helpers="clr-namespace:ConditioningControlPanel.Helpers"
             xmlns:feat="clr-namespace:ConditioningControlPanel.Features">

    <Grid>
        <StackPanel>
            <!-- HERO. The old standalone Enable card is gone: its checkbox moved into the
                 pill on the right (same name, same handlers, same SessionLock marker) and its
                 description is the subtitle. Hero art is a static pack-URI image, so it is
                 exempt from the mod repaint map. -->
            <Border Height="72" CornerRadius="16" Margin="0,0,0,10"
                    Background="{DynamicResource SurfaceBgBrush}"
                    BorderBrush="{StaticResource SectionHueStudioBorderBrush}" BorderThickness="1">
                <Grid ClipToBounds="True">
                    <Border Width="240" HorizontalAlignment="Right" CornerRadius="0,16,16,0">
                        <Border.Background>
                            <ImageBrush Stretch="UniformToFill" AlignmentX="Right" Opacity="0.9">
                                <ImageBrush.ImageSource>
                                    <BitmapImage UriSource="pack://application:,,,/Resources/features/Phrase_Lock.png"
                                                 DecodePixelWidth="480"/>
                                </ImageBrush.ImageSource>
                            </ImageBrush>
                        </Border.Background>
                        <Border.OpacityMask>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#E6000000" Offset="0.55"/>
                            </LinearGradientBrush>
                        </Border.OpacityMask>
                    </Border>

                    <StackPanel VerticalAlignment="Center" Margin="16,0,0,0">
                        <helpers:EmojiTextBlock Text="{loc:Str label_lock_card}" FontSize="17" FontWeight="Bold"
                                                Foreground="{StaticResource TextLightBrush}"/>
                        <TextBlock Text="{loc:Str tooltip_show_lock_cards_that_require_typing_to_dismis}"
                                   FontSize="11.5" Foreground="{StaticResource TextMutedBrush}"
                                   TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                    </StackPanel>

                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0"
                            CornerRadius="9999" Padding="12,6"
                            Background="#8C131320" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{loc:Str btn_enable}" FontSize="11.5" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <CheckBox x:Name="ChkEnable" feat:SessionLock.Owned="True"
                                      Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                      Checked="ChkEnable_Changed" Unchecked="ChkEnable_Changed"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- ONE dense controls card: two dials, then the two switches. Every description
                 that used to sit under a control is now that row's ToolTip. -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Border Background="{StaticResource SectionHueStudioWashBrush}" CornerRadius="11"/>
                    <Border Width="3" HorizontalAlignment="Left" Margin="0,10" CornerRadius="0,2,2,0"
                            Background="{StaticResource SectionHueStudioBrush}"/>
                    <StackPanel Margin="16,8,16,8">

                        <!-- Per hour -->
                        <Grid Height="36" ToolTip="{loc:Str tooltip_lock_card_frequency_1_10_per_hour}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" MinWidth="56"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{loc:Str setting_per_hour}" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" x:Name="SliderFreq" Minimum="1" Maximum="10" Value="2"
                                    feat:SessionLock.Owned="True" VerticalAlignment="Center" Margin="10,0"
                                    ValueChanged="SliderFreq_Changed"/>
                            <TextBlock Grid.Column="2" x:Name="TxtFreq" Text="2" HorizontalAlignment="Right"
                                       VerticalAlignment="Center" FontSize="13" FontWeight="Bold"
                                       Foreground="{DynamicResource PinkBrush}"/>
                        </Grid>

                        <!-- Repeats -->
                        <Grid Height="36" ToolTip="{loc:Str tooltip_number_of_times_to_type_the_phrase_1_10}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" MinWidth="56"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{loc:Str setting_repeats}" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" x:Name="SliderRepeats" Minimum="1" Maximum="10" Value="3"
                                    feat:SessionLock.Owned="True" VerticalAlignment="Center" Margin="10,0"
                                    ValueChanged="SliderRepeats_Changed"/>
                            <TextBlock Grid.Column="2" x:Name="TxtRepeats" Text="3x" HorizontalAlignment="Right"
                                       VerticalAlignment="Center" FontSize="13" FontWeight="Bold"
                                       Foreground="{DynamicResource PinkBrush}"/>
                        </Grid>

                        <Border Height="1" Margin="0,6" Background="{DynamicResource GlassBorderBrush}"/>

                        <!-- Strict lock -->
                        <Grid Height="34" ToolTip="{loc:Str tooltip_cannot_escape_must_complete_typing}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{loc:Str setting_strict_lock}" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                            <CheckBox Grid.Column="1" x:Name="ChkStrict"
                                      Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                      Checked="ChkStrict_Changed" Unchecked="ChkStrict_Changed"/>
                        </Grid>

                        <!-- Solve by voice. TxtVoiceHint stays VISIBLE, not a ToolTip: code-behind
                             repaints it with "no microphone detected" / "speech model not installed",
                             which is the difference between the switch working and silently not. -->
                        <Grid Height="34">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🎤 Solve by voice" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextLightBrush}" VerticalAlignment="Center"/>
                            <CheckBox Grid.Column="1" x:Name="ChkVoiceMode"
                                      Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                      Checked="ChkVoiceMode_Changed" Unchecked="ChkVoiceMode_Changed"/>
                        </Grid>
                        <TextBlock x:Name="TxtVoiceHint"
                                   Text="Say the phrase out loud instead of typing it (offline mic). Falls back to typing if no mic."
                                   Foreground="{StaticResource TextMutedBrush}" FontSize="10.5" FontStyle="Italic"
                                   TextWrapping="Wrap" Margin="0,0,0,4"/>

                    </StackPanel>
                </Grid>
            </Border>

            <!-- Actions -->
            <Grid Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="{loc:Str btn_manage_phrases}"
                        Style="{DynamicResource OutlineButton}" Click="BtnManagePhrases_Click"/>
                <Button Grid.Column="2" Content="{loc:Str btn_test_2}"
                        Style="{DynamicResource OutlineButton}" Click="BtnTest_Click"/>
                <Button Grid.Column="4" Content="⚙" Width="44"
                        Style="{DynamicResource OutlineButton}" Click="BtnColorSettings_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
