<Window x:Class="ConditioningControlPanel.AvatarTubeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        xmlns:local="clr-namespace:ConditioningControlPanel"
        xmlns:gif="https://github.com/XamlAnimatedGif/XamlAnimatedGif"
        Title="{loc:Str dialog_avatar_tube}"

        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        FontFamily="Segoe UI">

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:AvatarTubeWindow.OpenChatCommand}" Executed="OpenChatCommand_Executed"/>
    </Window.CommandBindings>

    <Window.Resources>
        <!-- Purplish gradient background for speech bubble -->
        <LinearGradientBrush x:Key="BubbleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{DynamicResource PatreonDarkPurple}" Offset="0"/>
            <GradientStop Color="{DynamicResource DarkerBg}" Offset="1"/>
        </LinearGradientBrush>

        <!-- Tiny pink scrollbar style -->
        <Style x:Key="TinyPinkScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Background="{DynamicResource PinkBrush}" BorderThickness="0">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollViewer style using tiny pink scrollbar -->
        <Style x:Key="TinyPinkScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Margin="0,0,4,0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Style="{StaticResource TinyPinkScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Viewbox scales content uniformly to fit any screen size -->
        <Viewbox x:Name="ContentViewbox" Stretch="Uniform" ClipToBounds="False">
            <!-- Design canvas at fixed reference size - will be scaled -->
            <Grid Width="780" Height="1080" ClipToBounds="False" SnapsToDevicePixels="True">
            <!-- Tube frame FIRST (behind) - Z-Index 0 implicit -->
            <Image x:Name="ImgTubeFrame"
                   Source="pack://application:,,,/Resources/tube.png"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   IsHitTestVisible="False"/>

            <!-- Speech Bubble - Z-Index 10, positioned next to avatar -->
            <!-- Dynamic bubble that sizes to content, grows UPWARD, scrolls when exceeding max -->
            <Border x:Name="SpeechBubble"
                    Panel.ZIndex="10"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Opacity="0.92"
                    Margin="0,0,125,550"
                    MouseEnter="SpeechBubble_MouseEnter"
                    MouseLeave="SpeechBubble_MouseLeave"
                    MinWidth="120" MaxWidth="380"
                    Background="{StaticResource BubbleGradient}"
                    CornerRadius="20"
                    BorderBrush="{DynamicResource PinkBrush}"
                    BorderThickness="3"
                    Padding="15,12">

                <Grid>
                    <!-- Single-message mode: existing bubble content -->
                    <ScrollViewer x:Name="SpeechScroller"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  MaxHeight="280"
                                  Style="{StaticResource TinyPinkScrollViewer}">

                        <!-- Speech text with bubbly pink font -->
                        <TextBlock x:Name="TxtSpeech"
                                   Text=""
                                   Foreground="{DynamicResource PinkBrush}"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   TextWrapping="Wrap"
                                   TextAlignment="Left"
                                   Padding="0,0,8,0">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1.5" Direction="315" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </ScrollViewer>

                    <!-- AI-generated content badge (top-left). Visibility is controlled in code-behind
                         based on whether the current bubble text came from an AI inference vs. a canned
                         phrase. Required for CCBill AI Content Merchant Addendum visible-labeling. -->
                    <Border x:Name="AiBadge"
                            Visibility="Collapsed"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Margin="-6,-6,0,0"
                            Width="22" Height="14"
                            CornerRadius="4"
                            Background="{DynamicResource PinkBrush}"
                            Panel.ZIndex="20"
                            IsHitTestVisible="False">
                        <TextBlock Text="{loc:Str label_ai_badge}"
                                   Foreground="{DynamicResource AccentForegroundBrush}"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Moderation-refusal POLICY badge (same slot as AiBadge). Shown by code-behind
                         when ModerationGuard blocks an input or output. Mutually exclusive with the
                         AI badge - they share the corner slot but visibility is managed in one place
                         (ShowGiggle / ShowModerationRefusalBubble). Amber to distinguish from the pink
                         AI badge. Part of P1.1 (CCBill substantive moderation). -->
                    <Border x:Name="PolicyBadge"
                            Visibility="Collapsed"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Margin="-6,-6,0,0"
                            Width="44" Height="14"
                            CornerRadius="4"
                            Background="#FFC107"
                            Panel.ZIndex="20"
                            IsHitTestVisible="False">
                        <TextBlock Text="{loc:Str label_policy_badge}"
                                   Foreground="Black"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Chat history mode: scrollable WhatsApp-style log -->
                    <Grid x:Name="ChatHistoryView" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" Margin="0,0,0,6" LastChildFill="True">
                            <Button x:Name="BtnCloseChatHistory"
                                    DockPanel.Dock="Right"
                                    Content="✕"
                                    Width="24" Height="24"
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Background="Transparent"
                                    Foreground="{DynamicResource PinkBrush}"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="BtnCloseChatHistory_Click"
                                    ToolTip="{loc:Str tooltip_close_chat_history}"/>
                            <TextBlock Text="{loc:Str label_chat_history_title}"
                                       Foreground="{DynamicResource PinkBrush}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"/>
                        </DockPanel>

                        <ScrollViewer x:Name="ChatHistoryScroller"
                                      Grid.Row="1"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      MaxHeight="420"
                                      Style="{StaticResource TinyPinkScrollViewer}">
                            <Grid>
                                <!-- Empty-state hint when no chat history yet -->
                                <TextBlock x:Name="TxtChatHistoryEmpty"
                                           Text="{loc:Str label_chat_history_empty}"
                                           Foreground="#AAAAAA"
                                           FontSize="14"
                                           FontStyle="Italic"
                                           HorizontalAlignment="Center"
                                           Margin="0,20,0,20"
                                           Visibility="Collapsed"/>

                                <ItemsControl x:Name="ChatHistoryList"
                                              HorizontalContentAlignment="Stretch">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="10" Padding="9,6" Margin="2,3" MaxWidth="380">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                                        <Setter Property="Background" Value="#3D2A55"/>
                                                        <!-- Both rows now inherit their text colour from the row
                                                             Border; the companion row keeps the white it always had. -->
                                                        <Setter Property="TextElement.Foreground" Value="White"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                                <!-- The row's own text has to follow the accent it now sits on. -->
                                                                <Setter Property="TextElement.Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel>
                                                    <TextBlock Tag="{Binding Text}"
                                                               Loaded="ChatHistoryText_Loaded"
                                                               FontSize="14"
                                                               TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding TimeLabel}"
                                                               Opacity="0.75"
                                                               FontSize="10"
                                                               HorizontalAlignment="Right"
                                                               Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>

            <!-- Text Input Panel (hidden by default) - Z-Index 3, positioned near avatar -->
            <Border x:Name="InputPanel"
                    Panel.ZIndex="3"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,126,520"
                    Background="{DynamicResource PanelBgBrush}"
                    CornerRadius="10"
                    Padding="10"
                    BorderBrush="{DynamicResource PinkBrush}"
                    BorderThickness="2">
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="TxtUserInput"
                             Width="180"
                             Height="30"
                             FontSize="14"
                             Background="{DynamicResource DarkerBgBrush}"
                             Foreground="White"
                             BorderBrush="{DynamicResource PinkBrush}"
                             BorderThickness="1"
                             Padding="5,3"
                             VerticalContentAlignment="Center"
                             KeyDown="TxtUserInput_KeyDown"/>
                    <Button x:Name="BtnSendChat"
                            Content="{loc:Str btn_send}"
                            Width="50"
                            Height="30"
                            Margin="5,0,0,0"
                            Background="{DynamicResource PinkBrush}"
                            Foreground="White"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BtnSendChat_Click"/>
                </StackPanel>
            </Border>

            <!-- Avatar/Sprite - Z-Index 1 implicit, with floating animation -->
            <Border x:Name="AvatarBorder"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="5,100,126,210"
                    Cursor="Hand"
                    MouseLeftButtonDown="ImgAvatar_MouseLeftButtonDown"
                    MouseRightButtonDown="ImgAvatar_MouseRightButtonDown"
                    ClipToBounds="False">
                <Border.ContextMenu>
                    <ContextMenu x:Name="AvatarContextMenu" Opened="AvatarContextMenu_Opened">
                        <ContextMenu.Resources>
                            <!-- Dark themed MenuItem style with custom template -->
                            <Style TargetType="{x:Type MenuItem}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type MenuItem}">
                                            <Grid>
                                                <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                        BorderThickness="0" Padding="{TemplateBinding Padding}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto" SharedSizeGroup="Arrow"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ContentPresenter x:Name="Icon" Grid.Column="0" ContentSource="Icon"
                                                                          VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                        <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                                          VerticalAlignment="Center"/>
                                                        <Path x:Name="Arrow" Grid.Column="2" Data="M0,0 L4,4 L0,8"
                                                              Fill="{TemplateBinding Foreground}" Margin="8,0,0,0"
                                                              VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </Grid>
                                                </Border>
                                                <!-- Submenu popup for items with children -->
                                                <Popup x:Name="PART_Popup" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                                       Placement="Right" HorizontalOffset="-2" AllowsTransparency="True" Focusable="False"
                                                       PopupAnimation="Fade">
                                                    <Border Background="{DynamicResource DarkerBgBrush}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2"
                                                            CornerRadius="8" Padding="4">
                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#3D3D60"/>
                                                </Trigger>
                                                <Trigger Property="HasItems" Value="True">
                                                    <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Foreground" Value="#666666"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                            <!-- Dark themed Separator -->
                            <Style TargetType="{x:Type Separator}">
                                <Setter Property="Height" Value="1"/>
                                <Setter Property="Margin" Value="8,4"/>
                                <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Separator}">
                                            <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ContextMenu.Resources>
                        <ContextMenu.Style>
                            <Style TargetType="{x:Type ContextMenu}">
                                <Setter Property="Background" Value="{DynamicResource DarkerBgBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Padding" Value="4"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ContextMenu}">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ContextMenu.Style>
                        <MenuItem x:Name="MenuItemTalkToBambi" Header="{loc:Str section_talk_to_companion}" Click="MenuItemTalkToBambi_Click" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold"/>
                        <Separator/>
                        <MenuItem x:Name="MenuItemDetach" Header="{loc:Str section_detach}" Click="MenuItemDetach_Click"/>
                        <MenuItem x:Name="MenuItemAttach" Header="{loc:Str section_attach_to_main_window}" Click="MenuItemAttach_Click" Visibility="Collapsed"/>
                        <MenuItem x:Name="MenuItemShrink" Header="{loc:Str section_shrink}" Click="MenuItemShrink_Click" Visibility="Collapsed"/>
                        <MenuItem x:Name="MenuItemGrow" Header="{loc:Str section_grow}" Click="MenuItemGrow_Click" Visibility="Collapsed"/>
                        <MenuItem x:Name="MenuItemDismiss" Header="{loc:Str section_dismiss}" Click="MenuItemDismiss_Click" Visibility="Collapsed"/>
                        <Separator/>
                        <MenuItem x:Name="MenuItemEngine" Header="{loc:Str section_start_engine}" Click="MenuItemEngine_Click" Foreground="#90EE90"/>
                        <MenuItem x:Name="MenuItemTriggerMode" Header="{loc:Str section_trigger_mode}" Click="MenuItemTriggerMode_Click"/>
                        <MenuItem x:Name="MenuItemBambiTakeover" Header="{loc:Str section_takeover}" Click="MenuItemBambiTakeover_Click"/>
                        <MenuItem x:Name="MenuItemPersonality" Header="{loc:Str section_personality}" Foreground="{DynamicResource PinkBrush}">
                            <MenuItem.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Padding" Value="10,6"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type MenuItem}">
                                                <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                        BorderThickness="0" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsHighlighted" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#3D3D60"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </MenuItem.ItemContainerStyle>
                        </MenuItem>
                        <MenuItem x:Name="MenuItemMute" Header="{loc:Str section_mute_avatar}" Click="MenuItemMute_Click"/>
                        <!-- Emote MenuItems replace the 5 items above (Engine, TriggerMode, BambiTakeover,
                             Personality, Mute) whenever a remote controller is connected. Header strings
                             and Tag references are populated in AvatarContextMenu_Opened from
                             App.Settings.Current.RemoteEmotePresets. Edits via the small picker propagate
                             here automatically because Header is re-read on every menu open. -->
                        <MenuItem x:Name="MenuItemEmote1" Header="" Visibility="Collapsed" Click="MenuItemEmote_Click"/>
                        <MenuItem x:Name="MenuItemEmote2" Header="" Visibility="Collapsed" Click="MenuItemEmote_Click"/>
                        <MenuItem x:Name="MenuItemEmote3" Header="" Visibility="Collapsed" Click="MenuItemEmote_Click"/>
                        <MenuItem x:Name="MenuItemEmote4" Header="" Visibility="Collapsed" Click="MenuItemEmote_Click"/>
                        <MenuItem x:Name="MenuItemEmote5" Header="" Visibility="Collapsed" Click="MenuItemEmote_Click"/>
                        <Separator/>
                        <MenuItem x:Name="MenuItemShowChatHistory" Header="{loc:Str section_show_chat_history}" Click="MenuItemShowChatHistory_Click"/>
                        <MenuItem x:Name="MenuItemMuteWhispers" Header="{loc:Str section_mute_whispers}" Click="MenuItemMuteWhispers_Click"/>
                        <MenuItem x:Name="MenuItemPauseBrowser" Header="{loc:Str section_pause_browser}" Click="MenuItemPauseBrowser_Click"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <Grid x:Name="AvatarBounceHost" RenderTransformOrigin="0.5,1.0">
                    <!-- Click-bounce layer: scales ALL avatar images together. Composes on top of the
                         per-image float transforms (breathing/wobble/bob) and the AvatarBorder shift,
                         so a click squash never fights the 60fps writes. Origin at bottom-centre so she
                         bounces up from her feet. -->
                    <Grid.RenderTransform>
                        <ScaleTransform x:Name="AvatarBounceScale"/>
                    </Grid.RenderTransform>
                    <!-- Static PNG avatar (default) / emotive-portrait crossfade layer A.
                         RenderTransform is a group: Scale (breathing) + Rotate (wobble) + Translate (bob),
                         all written by the 60fps float timer in portrait mode. Translate stays LAST so the
                         legacy Y-bob still works unchanged when portrait mode is off. -->
                    <Image x:Name="ImgAvatar"
                           Stretch="Uniform"
                           MaxHeight="306"
                           MaxWidth="198"
                           RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="AvatarScale"/>
                                <RotateTransform x:Name="AvatarRotate"/>
                                <TranslateTransform x:Name="AvatarTranslate" Y="0"/>
                            </TransformGroup>
                        </Image.RenderTransform>
                        <Image.Effect>
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                        </Image.Effect>
                    </Image>
                    <!-- Emotive-portrait crossfade layer B (hidden unless portrait mode is active) -->
                    <Image x:Name="ImgAvatarB"
                           Stretch="Uniform"
                           MaxHeight="306"
                           MaxWidth="198"
                           Opacity="0"
                           Visibility="Collapsed"
                           RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="AvatarScaleB"/>
                                <RotateTransform x:Name="AvatarRotateB"/>
                                <TranslateTransform x:Name="AvatarTranslateB" Y="0"/>
                            </TransformGroup>
                        </Image.RenderTransform>
                        <Image.Effect>
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                        </Image.Effect>
                    </Image>
                    <!-- Mist overlay (emotive-portrait mode only): a soft drifting glow over the avatar.
                         The stops below are a DESIGN-TIME SEED. AvatarTubeWindow.ApplyTubeAccent
                         rebuilds this brush from the active mod's accent at startup and on every
                         mod switch, so a non-pink mod does not glow pink. -->
                    <Ellipse x:Name="MistOverlay"
                             IsHitTestVisible="False"
                             Visibility="Collapsed"
                             Opacity="0.12"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <ScaleTransform x:Name="MistScale"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Fill>
                            <RadialGradientBrush GradientOrigin="0.5,0.45" Center="0.5,0.45" RadiusX="0.6" RadiusY="0.72">
                                <GradientStop Color="#FFFF8FCF" Offset="0.0"/>
                                <GradientStop Color="#66FF69B4" Offset="0.45"/>
                                <GradientStop Color="#00FF69B4" Offset="1.0"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <!-- Animated GIF avatar (for level 20+) -->
                    <Image x:Name="ImgAvatarAnimated"
                           Stretch="Uniform"
                           MaxHeight="306"
                           MaxWidth="198"
                           Visibility="Collapsed"
                           RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="AvatarAnimatedTranslate" Y="0"/>
                        </Image.RenderTransform>
                        <Image.Effect>
                            <!-- BlurRadius 20->10 (2026-07-05): during a crossfade BOTH animated layers
                                 composite a full-size blur shader every frame; Gaussian cost scales ~radius^2,
                                 so halving the radius is ~4x cheaper per layer. Cuts the render-thread load
                                 that feeds the emote/CompleteRender freeze (#494) with only a subtly tighter glow. -->
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                        </Image.Effect>
                    </Image>
                    <!-- Second animated layer: crossfade target for Circe WebP emotes
                         (AvatarTubeWindow.CirceEmotes.cs). Mirrors ImgAvatarAnimated. -->
                    <Image x:Name="ImgAvatarAnimatedB"
                           Stretch="Uniform"
                           MaxHeight="306"
                           MaxWidth="198"
                           Opacity="0"
                           Visibility="Collapsed"
                           RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="AvatarAnimatedTranslateB" Y="0"/>
                        </Image.RenderTransform>
                        <Image.Effect>
                            <!-- BlurRadius 20->10: see ImgAvatarAnimated above (halves the crossfade blur cost). -->
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                        </Image.Effect>
                    </Image>
                    <!-- Possession (Lockdown R4, "glitchportrait"): the corrupted-portrait layer.
                         A SIBLING of the avatar images inside AvatarBounceHost on purpose - a copy
                         placed here inherits the exact same layout slot, so a sliced duplicate lines
                         up with the real portrait pixel for pixel without measuring anything, and it
                         rides the bounce/float transforms with it. Filled and emptied entirely by
                         AvatarTubeWindow.GlitchPortrait; the real emote pipeline never sees it.
                         Services/Possession/POSSESSION.md - Companion wave. -->
                    <Grid x:Name="PossessionGlitchLayer"
                          IsHitTestVisible="False"
                          Visibility="Collapsed"
                          ClipToBounds="False"/>
                </Grid>
            </Border>

            <!-- Possession (Lockdown R4): the warden's note, left in the tube while the companion is
                 away (Warden.LeaveAsync) and taken down by ReturnAsync. Crimson - the Lockdown THEME
                 colour, never ember: ember means something is happening RIGHT NOW, and a note is the
                 opposite of that. Same margin as AvatarBorder so it sits where she was standing. -->
            <Border x:Name="PossessionNoteCard"
                    Panel.ZIndex="6"
                    Visibility="Collapsed"
                    IsHitTestVisible="False"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Margin="5,100,126,210"
                    MaxWidth="210"
                    Padding="16,12"
                    CornerRadius="10"
                    Background="#F21A0A0E"
                    BorderBrush="#DC143C" BorderThickness="1.5"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <RotateTransform x:Name="PossessionNoteRotate" Angle="-3"/>
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect Color="#DC143C" BlurRadius="18" ShadowDepth="0" Opacity="0.55"/>
                </Border.Effect>
                <TextBlock x:Name="TxtPossessionNote"
                           Foreground="#FFE3E8" FontSize="15" FontWeight="SemiBold"
                           TextWrapping="Wrap" TextAlignment="Center" LineHeight="20"/>
            </Border>

            <!-- Takeover countdown bar - thin pink bar under the avatar, drains toward the next
                 random Takeover action. Shown only while Takeover is running (driven in code-behind).
                 Fill is scaled horizontally via TakeoverCountdownScale (1.0 = full, 0 = about to fire). -->
            <Border x:Name="TakeoverCountdownBar"
                    Panel.ZIndex="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,116,246"
                    Width="120" Height="9"
                    CornerRadius="4"
                    Background="#EE14141C"
                    BorderBrush="#FFFF1493"
                    BorderThickness="1"
                    Visibility="Collapsed"
                    IsHitTestVisible="False">
                <!-- Fill is left-anchored and horizontally scaled (1.0 full -> 0 empty), so the
                     blackish track shows through the drained portion like a real progress bar. -->
                <Border x:Name="TakeoverCountdownFill"
                        HorizontalAlignment="Left"
                        Width="116" Height="5"
                        CornerRadius="2"
                        Background="#FFF7A8D8"
                        RenderTransformOrigin="0,0.5">
                    <Border.RenderTransform>
                        <ScaleTransform x:Name="TakeoverCountdownScale" ScaleX="1" ScaleY="1"/>
                    </Border.RenderTransform>
                    <Border.Effect>
                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                    </Border.Effect>
                </Border>
            </Border>

            <!-- THE FUSE's candle (CONTRACT-FUSE-0816 2.2). From twelve hours out, a small flame
                 stands beside the tube. It says nothing and does nothing; it is company.

                 Decorative-element recipe, same as ImgTubeFrame / MistOverlay / TakeoverCountdownBar:
                 a sibling of the 780x1080 design grid with an explicit ZIndex and
                 IsHitTestVisible="False", so AvatarBorder's click, right-click and context menu are
                 untouched. Collapsed here and shown only by AvatarTubeWindow.DescentFuse.cs, which
                 does nothing without a cached ceremony timestamp.

                 Two layers: a soft radial halo and the flame body. The flicker animates the halo's
                 opacity and the body's scale; when ambient loops are off, both park at rest and the
                 candle is a STATIC flame - present, snapped, never missing. -->
            <Grid x:Name="FuseCandleHost"
                  Panel.ZIndex="2"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Bottom"
                  Margin="96,0,0,214"
                  Width="54" Height="74"
                  Visibility="Collapsed"
                  IsHitTestVisible="False">
                <Ellipse x:Name="FuseCandleHalo" Width="54" Height="54"
                         VerticalAlignment="Top" Opacity="0.5">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Offset="0" Color="#90FFC864"/>
                            <GradientStop Offset="0.55" Color="#30E0B052"/>
                            <GradientStop Offset="1" Color="#00E0B052"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <!-- The flame body. Teardrop, origin at its base so the flicker's scale reads as a
                     flame stretching upward rather than a shape growing in place. -->
                <Path x:Name="FuseCandleFlame" Width="18" Height="30" Stretch="Fill"
                      HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"
                      RenderTransformOrigin="0.5,1"
                      Data="M 9,0 C 15,10 18,16 18,21 C 18,26 14,30 9,30 C 4,30 0,26 0,21 C 0,16 3,10 9,0 Z">
                    <Path.Fill>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Offset="0" Color="#FFFFE9B0"/>
                            <GradientStop Offset="0.5" Color="#FFE0B052"/>
                            <GradientStop Offset="1" Color="#FFB4642A"/>
                        </LinearGradientBrush>
                    </Path.Fill>
                    <Path.RenderTransform>
                        <ScaleTransform x:Name="FuseCandleFlameScale"/>
                    </Path.RenderTransform>
                </Path>
                <!-- The stub it burns on. Static, always. -->
                <Border Width="14" Height="26" CornerRadius="3,3,1,1"
                        HorizontalAlignment="Center" VerticalAlignment="Bottom"
                        Background="#FF2A2440" BorderBrush="#40E0B052" BorderThickness="1"/>
            </Grid>

            <!-- Title/Level Box with Avatar Selection - Z-Index 4 to be above everything -->
            <Border x:Name="TitleBox"
                    Panel.ZIndex="4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,121,180"
                    Background="#CC1A1A2E"
                    CornerRadius="12"
                    BorderBrush="{DynamicResource PinkBrush}"
                    BorderThickness="2"
                    Padding="8,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Arrow (Previous Avatar) - 50% bigger -->
                    <Border x:Name="BtnPrevAvatar" Grid.Column="0"
                            Background="Transparent" Cursor="Hand"
                            MouseLeftButtonDown="BtnPrevAvatar_Click"
                            Visibility="Collapsed"
                            VerticalAlignment="Center" Margin="0,0,10,0">
                        <TextBlock Text="◀" FontSize="27" Foreground="{DynamicResource SecondaryBrush}" FontWeight="Bold">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="{DynamicResource PatreonPurple}" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>

                    <!-- Title and Level -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtAvatarTitle"
                                   Text="{loc:Str label_basic_subject_2}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource PinkBrush}"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock x:Name="TxtAvatarLevel"
                                   Text="{loc:Str label_lv_1_2}"
                                   FontSize="13"
                                   Foreground="#B0B0B0"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Margin="0,-2,0,0"/>
                    </StackPanel>

                    <!-- Right Arrow (Next Avatar) - 50% bigger -->
                    <Border x:Name="BtnNextAvatar" Grid.Column="2"
                            Background="Transparent" Cursor="Hand"
                            MouseLeftButtonDown="BtnNextAvatar_Click"
                            Visibility="Collapsed"
                            VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="▶" FontSize="27" Foreground="{DynamicResource SecondaryBrush}" FontWeight="Bold">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="{DynamicResource PatreonPurple}" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>
                </Grid>
            </Border>

        </Grid>
        </Viewbox>
    </Grid>
</Window>
