<Window x:Class="ConditioningControlPanel.ColorEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Subliminal Colors" 
        Height="480" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A2E"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PinkBrush" Color="#FF69B4"/>
        <SolidColorBrush x:Key="DarkPinkBrush" Color="#FF1493"/>
        <SolidColorBrush x:Key="PanelBgBrush" Color="#252542"/>
        <SolidColorBrush x:Key="TextLightBrush" Color="#E0E0E0"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DarkPinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#404060"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColorButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" 
                                BorderBrush="#606080" BorderThickness="2">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="track" Width="44" Height="22" CornerRadius="11" 
                                    Background="#404060" Cursor="Hand"/>
                            <Border x:Name="thumb" Width="18" Height="18" CornerRadius="9" 
                                    Background="White" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{StaticResource PinkBrush}"/>
                                <Setter TargetName="thumb" Property="Margin" Value="24,0,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🎨 Subliminal Colors" 
                   Foreground="{StaticResource PinkBrush}" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Color Settings -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            
            <!-- Background Color -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Background" Foreground="{StaticResource TextLightBrush}" 
                           FontSize="14" VerticalAlignment="Center"/>
                <Button x:Name="BtnBgColor" Grid.Column="1" Style="{StaticResource ColorButton}"
                        Click="BtnBgColor_Click" ToolTip="Click to change"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Transparent" Foreground="#808080" FontSize="11" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <CheckBox x:Name="ChkBgTransparent" Style="{StaticResource ToggleStyle}"/>
                </StackPanel>
            </Grid>

            <!-- Text Color -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Text" Foreground="{StaticResource TextLightBrush}" 
                           FontSize="14" VerticalAlignment="Center"/>
                <Button x:Name="BtnTextColor" Grid.Column="1" Style="{StaticResource ColorButton}"
                        Click="BtnTextColor_Click" ToolTip="Click to change"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="Transparent" Foreground="#808080" FontSize="11" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <CheckBox x:Name="ChkTextTransparent" Style="{StaticResource ToggleStyle}"/>
                </StackPanel>
            </Grid>

            <!-- Border Color -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Border/Outline" Foreground="{StaticResource TextLightBrush}" 
                           FontSize="14" VerticalAlignment="Center"/>
                <Button x:Name="BtnBorderColor" Grid.Column="1" Style="{StaticResource ColorButton}"
                        Click="BtnBorderColor_Click" ToolTip="Click to change"/>
            </Grid>
        </StackPanel>

        <!-- Preview -->
        <Border Grid.Row="2" x:Name="PreviewBorder" Background="Black" CornerRadius="8" 
                Height="80" Margin="0,0,0,20">
            <TextBlock x:Name="PreviewText" Text="BAMBI SLEEP" 
                       FontSize="28" FontWeight="Bold" FontFamily="Arial"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Cancel" Style="{StaticResource SecondaryButton}" 
                    Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Grid.Column="2" Content="Save" Style="{StaticResource ActionButton}" 
                    Click="BtnSave_Click"/>
        </Grid>
    </Grid>
</Window>
