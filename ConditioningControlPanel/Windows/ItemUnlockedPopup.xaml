<Window x:Class="ConditioningControlPanel.ItemUnlockedPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Item Unlocked!"
        Width="340" Height="104"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <!-- 10px transparent margin so the accent glow has room to bleed: 340x104 window = 320x84 card -->
    <Border x:Name="CardBorder" Background="#E0151530" CornerRadius="12" BorderThickness="2"
            Margin="10">
        <!-- BorderBrush + Effect are set in code-behind from the item's mod accent -->

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Item art (or a gift glyph when the PNG never landed) -->
            <Border Grid.Column="0" Width="64" Height="64" CornerRadius="10"
                    Background="{DynamicResource DarkerBgBrush}" Margin="0,0,10,0">
                <Grid>
                    <Image x:Name="ItemImage" Stretch="Uniform" Margin="4"/>
                    <Image x:Name="FallbackGlyphImage" Width="30" Height="30" Visibility="Collapsed"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock x:Name="FallbackGlyphText" Text="&#x1F381;" FontSize="26" Visibility="Collapsed"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Header / item name / call to action -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                    <Image x:Name="HeaderIcon" Width="13" Height="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock x:Name="TxtHeader" Text="NEW ITEM UNLOCKED!" Foreground="#FFD700"
                               FontWeight="Bold" FontSize="11" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Registry proper noun - deliberately NOT localized -->
                <TextBlock x:Name="TxtItemName" Text="Item Name"
                           FontWeight="Bold" FontSize="16"
                           TextTrimming="CharacterEllipsis"/>

                <TextBlock x:Name="TxtSub" Text="Wear it in Profile &gt; Customize"
                           Foreground="#AAAAAA" FontSize="10" Margin="0,2,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
