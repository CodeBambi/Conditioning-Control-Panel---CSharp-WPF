<Window x:Class="ConditioningControlPanel.CornerGifWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Corner GIFs"
        Width="460" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="#0F0F1E"
        Foreground="#E0E0E0"
        ResizeMode="CanResizeWithGrip"
        MinWidth="420" MinHeight="480">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="🖼 Corner GIFs"
                   Foreground="{DynamicResource PinkBrush}"
                   FontSize="20"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Text="Pin a looping GIF to a screen corner — on all the time, not just during a session. Turn on one or both slots and point each at a different corner (great for covering minimaps, HUD corners, or just decoration). Overlays are click-through."
                   Foreground="#A0A0B0"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,6,0,14"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="SlotsPanel"/>
        </ScrollViewer>

        <Button Grid.Row="3"
                x:Name="BtnClose"
                Content="Done"
                Style="{DynamicResource OutlineButton}"
                Padding="16,8"
                HorizontalAlignment="Right"
                Margin="0,14,0,0"
                Click="BtnClose_Click"/>
    </Grid>
</Window>
